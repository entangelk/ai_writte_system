# 개발 전용 의존성 — 프로덕션 이미지에 들어가지 않는다.
#
# services/application/requirements.txt 에 섞지 말 것. 섞으면 타입체커가
# 프로덕션 이미지에 실려 나간다(docs/plans/script-rot-guard-decisions.md 결정 1-b).
#
# 설치: python3 -m pip install -r requirements-dev.txt
#
# mypy 는 tests/test_typecheck.py 가 부른다. 설치 안 된 머신에서 그 셀은
# skip 이 아니라 실패한다 — skip 은 가드를 조용히 없앤다(같은 문서 결정 1-b).
mypy>=2.3,<3

# elasticsearch 는 프로덕션 의존성(services/application/requirements.txt)이지만
# 여기에도 있어야 한다 — **전수가 컨테이너가 아니라 호스트에서 돈다.** 앱은 이
# 패키지를 lazy import 하므로(indexing/*_lexical_index.py) 호스트에 없으면
# lexical retrieval 셀 3종이 실패가 아니라 **skip** 이고, 요약줄은 초록이다
# (2026-09-06 알파에서 실측 — skip 이 1이 아니라 4였다).
# ★ 핀은 services/application/requirements.txt 를 따라간다. 갈라지면 호스트가
#   프로덕션과 다른 major 를 패치하게 되고, 그 차이는 전수가 아니라 배포에서만
#   드러난다.
elasticsearch>=8,<9
