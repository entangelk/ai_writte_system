# HANDOFF

> **다음 작업자가 지금 일을 시작하는 데 필요한 것만.** 이력이 아니다.
> 완료 서술은 여기 쓰지 않는다 — `docs/daily_logs/`(상세) · `docs/system-contract-sot.md` 변경이력 · `CHANGELOG.md`(마일스톤) · `docs/verifications/`(독립 검증)에 이미 있다.
> 편집 규칙은 `CLAUDE.md`·`AGENTS.md`의 "HANDOFF.md" 절에 있다. **길이 상한은 없다** — 대신 **~200줄을 넘으면 자가 검수**하고(그 뒤로는 ~100줄마다) 결과를 아래 한 줄로 남긴다. 길어야 할 이유가 있으면 길어도 된다. 안 보는 것이 문제다.
>
> 마지막 자가 검수: **2026-08-10 · 310줄 (전체 검수는 아니고 `Owner Decisions Needed` 절을 제대로 봤다 — 직전 검수가 "안 본 절"로 남겨 둔 그 자리다). **그 절이 완료 이력 창고가 돼 있었다** — 12개 항목 중 **진짜 대기는 dogfood 하나**였고 나머지는 끝난 슬라이스 서사였다(Phase 9 Slice 9.0 이 아직 "구현 대기 — 지금 가장 앞선 작업"으로 적혀 있었다). **지우지 않고 압축**했다 — 끝난 것은 새 절 "완료 슬라이스"로 내리되 **다음 사람이 밟을 함정 문장은 그대로** 옮기고, 편집 후 키워드 10종이 저장소에서 사라지지 않았는지 grep 으로 확인했다. **안 본 절: 함정 · Active Decisions · 추적 부채** — 다음 작업자가 400줄 트리거에서 본다)

## 머신 구성 (알파 · 베타 · 감마)

이 프로젝트는 성격이 다른 세 머신을 옮겨 다닌다. **HANDOFF·문서가 "환경과 안 맞는다"고 느껴지면 먼저 지금 어느 머신인지부터 본다.** 아래는 각 머신이 *무엇을 띄울 수 있는지*(항구적 성질)이지, *지금 무엇이 떠 있는지*(머신-로컬 관측치)가 아니다 — 후자는 각 절에서 날짜를 달아 따로 적는다.

| 머신 | 역할 | LLM | 띄울 수 있는 것 |
|---|---|---|---|
| **알파(Alpha)** | 서비스 배포용 | **in-stack llama**(GPU **RTX 3060 12GB** — `nvidia-smi` 실측, `docker-compose.llama.yml`) | 전체 스택 + 자체 GPU LLM |
| **베타(Beta)** | 테스트·개발용 (**다음 작업 예정 — 상태 재확인 필수**) | **외부 LLM**(gemma-4-12B, LAN) — 주소·context는 `.env`와 `/props`로 다시 잰다. 이 머신 GPU는 **GTX 1060 3GB**라 12B를 못 올린다 | 전체 스택. gateway는 `.env`의 `LLAMA_BASE_URL`로 외부 서버를 가리킨다 |
| **감마(Gamma)** | 사이드 개발용 (노트북) | **없음** — LLM을 못 띄운다 | CPU 기반 컨테이너·DB 정도(mongo/test-mongo·ES·chroma). LLM 관통 작업은 불가 |

- **어느 머신에서든 `docker compose up`만으로 같은 포트로 뜬다**(포트는 repo에 고정, 아래 "기동·실행법"). 머신별로 달라지는 것은 **LLM을 어디서 얻느냐**뿐이다:
  - 알파: `docker compose -f docker-compose.yml -f docker-compose.llama.yml up`(in-stack llama 9080).
  - 베타: `.env`에 `LLAMA_BASE_URL=http://192.168.1.22:9080`(커밋 금지 — gateway 기본값 `host.docker.internal:9080`을 외부 서버로 덮는다).
  - 감마: LLM 관통이 필요 없는 작업(회귀·저장소·색인)만. 필요하면 알파/베타로 옮긴다.
- **★ 2026-08-03: 알파→베타 전환이 끝났고 베타에서 재측정했다**(아래 "지금 상태"의 베타 관측치). repo 동기화도 확인됐다(`c2ca946`과 마감 커밋 존재, HEAD `05286a6` clean). 다음에 또 머신을 옮기면 같은 절차를 반복한다 — `git status --short`·`git log -1 --oneline`·`docker compose ps`·이미지 날짜·외부 LLM `/props`.
- **함정**: HANDOFF가 "스택이 내려가 있다/떠 있다"고 적었으면 그건 **그 시점 그 머신의 관측치**다. 다른 머신에서 그대로 믿지 말고 `docker compose ps`로 직접 확인한다(memory 규칙 "verify-machine-state-before-claiming-blocked").
- **★ 알파로 옮길 때 반드시 먼저 할 것 — `LLAMA_CTX_SIZE=16384`(2026-07-29).** 알파의 in-stack llama는 [`docker-compose.llama.yml:29`](docker-compose.llama.yml#L29)에서 **기본 8192**이고 `.env.example`에 항목이 없다. 그런데 **2026-07-29 회계 수정 후에도 report 프롬프트는 약 11,000~11,900 tok**이라, 창 8192에서는 **프롬프트만으로 창을 넘어 HTTP 400(`exceed_context_size_error`) → `provider_error` → generation job 실패**가 된다 — **오늘 아침 베타에서 4/4 실패했던 그 증상 그대로**다. 베타는 오너가 외부 서버를 16384로 올려 뒀지만 **그것은 그 서버의 설정이고 repo가 옮겨 주지 않는다.** 그러므로 알파에서 `long`을 돌리기 전 `.env`에 `LLAMA_CTX_SIZE=16384`를 넣는다(C-1 실측: 기동 성공, VRAM 9,481/12,288, 속도 무변). **안 넣고 실패를 보면 "오늘 수정이 안 먹었나"로 오독하기 쉽다 — 수정은 먹었고 창이 작은 것이다.** 근본 해결은 R-e(포인터 제거)이며 그 뒤에는 8192에서도 여유가 생긴다.

## 지금 상태

- 정본은 `docs/system-contract-sot.md` **v1.7.96**(Approved). 미확정 항목은 추측 구현하지 않는다.
- **★ 배포되는 앱이 이제 둘이다(Slice 2, 2026-08-09). 앱을 만지는 사람이 가장 먼저 알아야 한다.** `/admin` 8 operation 은 **`admin` compose 서비스**(application 이미지 재사용·command 만 다름·**포트 미게시**)가 서빙하고, 도달 경로는 nginx `location /api/admin/` 하나다. 제품 앱에는 그 route 가 **없으므로** LAN 에서 치면 가드가 아니라 **라우터가 404** 다(`require_admin_user` 는 두 번째 겹으로 남는다). **알아야 하는 것 다섯**: ① **`create_app()` 은 사라지지 않았고 여전히 76 operation 합집합**이다 — 테스트·경계 행렬·`scripts/dump_openapi.py`(= 프론트 `schema.d.ts`)가 전부 그것을 쓴다. 브라우저는 nginx 뒤에서 한 origin 만 보므로 **계약은 하나여야 한다**. ② **세 factory(`create_app`·`create_product_app`·`create_admin_app`)는 한 함수 본문**이고 `include_product`·`include_admin` 으로만 갈린다 — 그것이 H-2(shim drift) 의 처방이다. **배포 앱만 따로 조립하는 코드를 새로 만들면 안 된다**(그 순간 아무도 구동하지 않는 두 번째 배선이 생긴다). 성질은 [`tests/test_admin_surface_separation.py`](tests/test_admin_surface_separation.py)가 잠근다 — 합집합 = 제품 ∪ 관리자 · 교집합 = `/health` · operation 별 가드·상태코드·에러 선언 동일. ③ **`register_*` 호출 순서를 재배열하지 말 것** — 합집합 앱의 route 순서가 OpenAPI `paths` 순서이고 그것이 프론트 생성물의 입력이다. surface 를 건너뛰는 것은 `if` 를 **제자리에** 두는 방식으로만 한다(초판이 admin 을 auth 앞으로 옮겼다가 지문이 깨질 뻔했다). ④ **`main:app` 은 제품 앱**이다(이미지 기본 CMD). 관리자 컨테이너는 [`admin_asgi.py`](services/application/app/admin_asgi.py) 를 띄운다 — 그 모듈을 import 하면 `main` 의 모듈 수준 제품 앱도 함께 만들어져 **관리자 컨테이너는 안 쓰는 앱 하나를 더 든다**(알려진 대가, 서빙되는 것은 관리자 앱뿐). ⑤ **admin 서비스 env 는 Mongo 셋뿐**이다 — 관리자 operation 전부가 Mongo 이고 purge 도 벡터·lexical 파기를 outbox 로 넘긴다. gateway·chroma 를 물리려면 이유를 함께 적을 것.
- **★ 활동 로그가 생겼다(Phase 9 Slice 9.0, 2026-08-09) — 사용자 행위를 남기는 첫 저장소다.** `activity_events` 에 **정본 변경 11 + 검토 결정 9 = 20 경로**가 한 행씩 남기고(A2=B, 2026-08-09 에 오너가 `writing/accept` 를 더해 19 → 20), 소유자는 `GET /projects/{id}/activity`(operation **77**)로 읽는다. **알아야 하는 것 여섯**: ① **★ 이 컬렉션은 8.2c `project_name_history` 와 정반대다** — `project_id` 필드를 쓰고 **파기와 함께 사라진다**(I1·I2). 그쪽의 `_id` 트릭(reconciler 회피)을 여기 복사하면 **개명 이력 전체가 삭제 예외로 승격돼 D8-6 이 무너진다**. 실 Mongo 셀 둘이 두 방향을 나란히 잰다. ② **분류표가 mutating 40 전수다**([`activity/actions.py`](services/application/app/activity/actions.py)) — 새 mutating route 는 `logged`/`excluded(사유)` 중 하나로 등재해야 하고 미등재는 가드가 실패시킨다. **A2 를 C(AI 요청까지)로 넓히는 일은 `ai_request` 14 행의 값을 바꾸는 것**이어야 한다(오너 조건) — **그때 A8(중복 없음)을 함께 다시 본다**. ③ **기록은 handler 본문에서, 결과를 안 뒤에**(A7=A). dependency 로 옮기면 404·409 로 끝난 요청이 "했다"로 남는다. **가드가 `inspect.getsource(route.endpoint)` 를 보므로 헬퍼 안에 넣으면 안 잡힌다.** ④ **기록 실패는 요청을 안 죽인다**(A4=A 격리, 경계는 서비스 안 **한 곳**) — 대가는 **조용한 구멍**이고, 그래서 배선 누락도 런타임에 안 보인다(전수 가드가 유일한 신호다). **반면 파기 실패는 삼키지 않는다.** ⑤ **`before`/`after` 는 라벨만**(이름·제목·상태, **200자 상한**). 본문을 넣으면 `draft_versions` 와 두 정본이 된다. ⑥ **`writing/accept` 도 기록한다** — 정본 draft version 저장이기 때문이다(오너 2026-08-09). **A8 개정이 아니다**: 남기는 것은 AI 요청이 아니라 정본 저장이라 관측·원장과 다른 사실이고, **A2=C 확장(AI 요청 자체)과는 축이 다르다**(대상은 `ai_request` **13 행**이며 그때 A8 을 함께 다시 본다). **기록 조건은 상태코드가 아니라 정본이 바뀌었는가** — Gate 거부(200·`saved=null`)는 안 남기고 **502 partial(version 은 저장됨)은 남긴다**. ⑦ **★ 전수 가드는 배선의 *존재*만 본다 — *분기*는 못 본다**(N9 실측): 한 handler 에 기록 분기가 둘이면 하나를 지워도 소스 스캔이 만족돼 통과한다. **분기마다 행위 셀이 필요하다.**

- **진행 중 페이즈 = 다중 사용자 인증(D8).** 오너 지시로 **슬라이스를 잘게 쪼개 진행 중**이다(한 번에 큰 덩어리 금지). 진행표:

  | 슬라이스 | 상태 | 내용 |
  |---|---|---|
  | 1a·1b·1c | **완료** | User 저장(Argon2id) · 서버 세션 · `/auth/login`·`/auth/logout`·`/auth/me` (operation 62→**65**), 실 스택 관통 검증 |
  | **2a** | **완료** | `Project.owner_id: str \| None` 필드 + Mongo 왕복. `create_project(name, owner_id=None)`. **공개 API 무변** |
  | **2b** | **완료** | `POST /projects`가 세션이 있으면 생성자를 owner로 **기록**. 세션 없으면 unowned로 **여전히 200** |
  | 2c(선택) | 미착수 | `owner_id`를 공개 payload에 노출할지 — **프론트가 읽을 이유가 생길 때** 하면 된다(schema.d.ts 변경이므로 공짜 아님) |
  | **D8-4 선행** | **완료·검증 합격** | 프론트 로그인·세션 만료·route guard |
  | **3-a** | **완료** | 인증 dependency + 61 operation 401 선언 + 두 겹 전수 가드 |
  | **3-b** | **완료** | project-scoped 59 operation 소유권(403) + `GET /projects` 저장소 필터. `owner_id=None` 항상 deny |
  | **3-c** | **완료** | 401·403 결합 boundary matrix 감사. **D8-3 시행 종료** |
  | **5-a** | **완료** | `require_admin_user` + 관리자 tier 전수 가드 + `/admin/users` 목록·생성·비활성화 |
  | **5-c** | **완료** | 전역 관측 KPI `GET /admin/observability/kpi` — 감사 저장소 전역 조회 + 집계 재사용 |
  | **5-e** | **완료** | **승격(access grant)** — C-1~C-5 확정(TTL **1시간**·읽기 전용·사유 필수·append-only). `POST /admin/projects/{id}/access-grants`(201, operation **72**) + `require_project_owner` 인지 |
  | **5-f** | **완료** | **C-3 operation 단위 감사 + C-4 사후 조회**. 승격 아래 요청마다 `access_grant_uses` 한 행(사유 denormalize) + `GET /projects/{id}/access-log`(operation **73**). **기록은 fail-closed** |
  | **5-b** | **완료** | `GET /admin/projects` — 전 프로젝트 **메타데이터**(id·name·archived·owner_id, operation **74**). **내용은 안 준다**(승격 우회 방지) · 목록 조회는 감사 미기록 |
  | **C-6** | **완료** | 관리자가 정한 비밀번호는 **1회용** — 첫 로그인에 교체(409 → `new_password`). 정책 **최소 12자** |
  | **5-d** | **완료·검증 합격·hardening 폐쇄** | `/admin` guard/lazy 화면 · 강제 교체 UI · 전 프로젝트 목록 · 사용자 관리 · 전역 KPI · 승격 발급 · 소유자/관리자 접근 이력 |
  | **6 (D8-6)** | **완료·독립 검증 합격(`c2ca946`)** | archive-only purge UI+감사. Blocking/Hardening 0, 7축 mutation 전부 작동, 실제 Mongo에서 reconciler가 `target_project_id` tombstone을 보존함을 실증했다. **D4=A는 현행 core-first+reconciler**, **D4-D operation journal/saga는 원격 저장소·다중 worker 또는 수동 reconciler 부담이 생길 때 확장**한다. operation 75(ADMIN 8). 기준선 backend 1911/4/1625, frontend 236/17, build 698 modules. 검증 `docs/verifications/2026-08-02/d8_6_purge_ui.md`. |
  | **7 (D8-7)** | **절반 완료 — G1=C 시행(A), 나머지는 배포 시점** | **인프라 노출면.** 오너가 **G1=C**를 확정했다(2026-08-02) — 위험을 자격증명이 아니라 **노출면 축소로 없앤다**. 저장소·내부 5종(`mongo`·`chroma`·`elasticsearch`·`gateway`·`embedding`)과 `test-mongo`가 **`127.0.0.1:` 바인드**다. **코드 0줄**(compose 3파일뿐). **"외부 노출 금지"의 해제 조건이 *인증*에서 *노출 없음*으로 개정됐다**(SoT v1.7.75). **G2~G6(자격증명 SCRAM·keyfile·basic auth)은 각하가 아니라 유예** — 원격/다중 호스트 배포가 계기이며 브리프가 그대로 실행 계획이다 |

- **D8-3 E1~E4 = 전부 A로 확정** — `plans/auth-d8-3-enforcement-decisions.md`(Resolved). `owner_id=None`은 탈퇴·삭제 누락 같은 미래 비정상 잔존도 **항상 deny**. project 경로는 소유권, 그 외는 인증이며 `GET /projects`는 저장소 조회에서 본인 소유만 반환한다.
- **HTTP 인증·프로젝트 인가는 섰고 결합 감사까지 닫혔다.** 세션 없는 요청은 `/health`와 공개 `/auth` 두 곳을 빼고 401, project-scoped **59 operation**은 타인 소유·`owner_id=None`에 403이다. `GET /projects`는 Mongo `owner_id` 쿼리 경계에서 본인 소유만 반환한다. **저장소 계층은 D8-7 G1=C로 닫혔다** — 인증한 것이 아니라 **노출을 없앴다**(아래 항목).
- **★ 저장소는 이제 LAN에서 보이지 않는다(D8-7 G1=C, 2026-08-02) — 그러나 인증된 것은 아니다.** `mongo`·`chroma`·`elasticsearch`·`gateway`·`embedding`과 `test-mongo`가 compose에서 **`127.0.0.1:` 바인드**로 게시된다. 종전에는 이 셋이 0.0.0.0이라 **LAN의 누구나 무인증으로 붙을 수 있었고**, 그것이 "외부 노출 금지"의 실체였다. 오너 결정으로 그 위험을 **자격증명이 아니라 노출면 축소로** 없앴다(SoT v1.7.75가 해제 조건 문구를 *인증*→*노출 없음*으로 개정). **알아야 하는 것 넷**: ① **저장소 자체는 여전히 무인증**이라 포트를 다시 0.0.0.0으로 열면 원래 위험이 그대로 돌아온다 — 바인드가 유일한 방어다. ② **컨테이너 간 통신은 무영향**(compose 네트워크 이름 `mongo:27017`·`gateway:8001`)이고 **호스트 툴링도 무영향**(localhost로 붙는다 — mongosh·`migrate_ordered_units.py`·호스트 pytest·runbook curl 전부 그대로). 앱 코드는 **0줄** 바뀌었다. ③ **일부러 공개인 것은 셋뿐**이다 — `application`·`frontend`(제품 표면, 세션 뒤) · **`llama`(9080)**(GPU 머신이 12B를 못 올리는 머신에 모델을 주는 유일한 크로스머신 의존. 여기를 loopback으로 묶으면 베타가 스택을 못 돌린다 — 회귀가 "LAN에서 보여야 한다"로 잠근다). ④ `.env`로는 **번호만** 바뀌고 바인드 주소는 못 바꾼다. 가드는 `tests/test_compose_exposure.py`이며 **새 서비스가 포트를 게시하면 분류를 강제**한다(미분류면 실패 — 멀티라인·인라인·따옴표 유무 전 표기). **★ 다만 이것은 파일 수준의 시행이다** — **이미 만들어진 컨테이너는 옛 매핑을 그대로 들고 있으므로 재생성 전까지 런타임은 안 닫힌다**(아래 함정 절 참조). 독립 검증(`verifications/2026-08-02/`)이 잡은 항목이며 **재생성은 오너 결정 대기**다.
- **★ 유료 9경로에는 이제 요청 quota 시행이 붙어 있다(Slice 8.3, 2026-08-04). 인증 경계를 건드릴 사람이 이것도 알아야 한다.** 시행은 `Depends(enforce_quota)`이고 **`_REQUIRE_PROJECT_OWNER_BILLABLE` = 소유권 + 시행** 순서다 — 그 순서가 곧 "404·403은 무과금"이며, 앞으로 옮기는 리팩터링은 **상태코드를 하나도 바꾸지 않으므로 요청 구동 테스트로는 안 보인다**(route 선언을 읽는 셀이 그 자리다). **알아야 하는 것 다섯**: ① **차감은 성공했을 때만**이고 "성공"은 `2xx` **그리고 provider 호출**이다 — `analysis_extract` replay처럼 아무 일도 안 한 200은 안 센다. ② **정산(원장 → 잠금 해제)은 dependency의 `yield` 뒤가 아니라 `QuotaSettledRoute` wrapper에 있다** — partial envelope과 202는 예외가 아니라 `JSONResponse`를 *반환*해서 dependency가 상태코드를 볼 수 없기 때문이다. **wrapper를 벗기면 잠금이 영영 안 풀린다.** ③ **입장 뮤텍스(`admission:{user}`) 임계 구역에 provider 호출을 넣지 말 것** — 넣는 순간 한 회원의 요청이 91초씩 직렬화된다(over-strict 셀이 문다). ④ **새 유료 경로는 분류표·시행 dependency·402/429 선언·확인 헤더가 함께 간다** — 넷 다 전수 가드가 있다. ⑤ **도메인 스위트는 `tests/auth_support`가 입장만 우회한다**(8.2b 잠금 최소 창 5초 때문에 같은 동작을 연달아 두 번 POST하는 셀이 429가 된다) — 시행을 진짜로 재는 것은 `tests/test_quota_enforcement_api.py`이고 그쪽은 우회하지 않는다.
- **★ 기본 한도가 켜져 있다 — 무제한이 아니다.** 정책 행이 없는 회원은 코드 기본값 **일 20 / 주 100**(P7 잠정, `QUOTA_DEFAULT_DAILY_LIMIT`·`QUOTA_DEFAULT_WEEKLY_LIMIT`로 override)을 받는다. 그리고 **진행 중 요청과 대기·실행 중 async job이 그 한도를 차지한다**(Q3=E). dogfood나 라이브 관통에서 "갑자기 402"가 나오면 그것은 장애가 아니라 시행이다. **관리자도 신분으로는 예외가 아니다**(8.4 W1) — 안 막히는 관리자는 `create_user.py --admin`이 만들어 준 **`limit=None` 정책 행**을 갖고 있는 것이고, 그 행이 없으면(예: `POST /admin/users`로 만든 관리자) 똑같이 일 20 / 주 100을 받는다. 같은 이유로 **유료 경로를 치는 smoke·진단 스크립트를 5초 안에 다시 돌리면 429가 난다** — 실패가 아니라 중복 가드이며, 의도한 재실행이면 `X-Confirm-Duplicate` 헤더를 싣거나 5초를 기다린다.
- **관리자 경계(D8-5·D8-6)**: `/admin/*` **8개 operation**이 `require_admin_user` 뒤에 있다. 비관리자는 403이고, 관리자는 project 내용에 자동 접근하지 않는다. 내용 조회는 사유 필수·1시간·읽기 전용 승격만 가능하며 `owner_id=None`은 승격으로도 열리지 않는다. 승격 사용 감사(`access_grant_uses`)는 fail-closed이고 TTL이 없다. purge는 내용 열람이 아닌 별도 ADMIN 예외이며, archive·사유·삭제 tombstone 계약을 따른다. 첫 관리자는 `create_user.py`로 부트스트랩한다.
- **마지막 활성 관리자는 비활성화되지 않는다**(F2=A, 409). 불변식은 호출자가 아니라 **활성 관리자 population**에 대한 것이다. 비활성화는 **단방향**이다 — 재활성화 API는 D6=A 범위 밖이라 만들지 않았고, 되살리려면 컨테이너에서 직접 고쳐야 한다.
- **인증 경계를 건드릴 사람이 알아야 하는 것**: 가드는 `tests/test_auth_api.py`의 `AuthenticationBoundaryTest`·`ProjectAuthorizationTest`·`CombinedBoundaryMatrixTest` 세 겹이며, 현재 **77개 operation 중 ADMIN 8개**다(Phase 9 가 `GET /projects/{id}/activity` 를 project tier 에 더해 62 가 됐다). 새 endpoint는 세 클래스가 모두 본다. `POST /projects`·`GET /projects`는 인증 전용이고, 403 생산자는 소유권·관리자 정확히 둘이다. 도메인 스위트는 dependency 해석만 override하므로 경계 테스트는 override 없는 앱으로 쓴다. 숫자의 권위는 코드의 tier 단정이다.
- **함정 — 인증은 두 겹이라 한 겹이 빠져도 아무 테스트도 실패하지 않는다**(2026-07-28 뮤테이션 실측): project route가 `_REQUIRE_PROJECT_OWNER`에서 인증 dependency를 먼저 선언하고 `require_project_owner`도 같은 dependency를 하위로 갖는다. 어느 한 겹을 지워도 **관측 상태코드가 전혀 변하지 않아** 요청 구동 테스트로는 원리적으로 볼 수 없다(둘 다 지워야 401이 403으로 샌다). 안쪽 겹은 **바깥 겹을 뺀 일회용 앱에 `require_project_owner`만 마운트해** 격리 구동하는 셀로 잠갔다(`CombinedBoundaryMatrixTest.test_the_ownership_dependency_cannot_run_without_authentication`). 인증을 유지한 합성·래퍼 dependency 리팩터링은 이 셀을 깨지 않지만(실측), **인증을 실제로 잃는 리팩터링은 깬다** — 여기서만 그것이 보인다.
- **첫 계정 만드는 법**(`POST /admin/users`가 생겼지만 그것을 쓰려면 이미 관리자여야 하므로 **부트스트랩은 여전히 스크립트**다. **★ C-6 이후 그 비밀번호는 1회용이다** — 첫 로그인에 `new_password`(12자 이상)를 함께 보내야 세션이 나온다): `docker exec -e PYTHONPATH=/app -e AUTH_BOOTSTRAP_PASSWORD='…' ai_writte_system-application-1 python scripts/create_user.py <username> --admin`. **`PYTHONPATH=/app`이 필수다** — 이미지에 PYTHONPATH가 없고 `python scripts/x.py`는 CWD를 sys.path에 넣지 않는다.
- **관측 KPI 페이즈의 결과물**(다음 작업이 이 위에서 돈다): LLM을 부르는 **호출부 8곳 전부**가 seam C(provider 데코레이터)로 계측되고 — **★ "8"의 기준은 `LlmCallSite` enum 리터럴 수(= LLM 어댑터 수)이며 `ObservedProvider(` 감싸기 지점 수와 일치한다(둘 다 8, 2026-08-02 실측). scope 진입 10·endpoint 9·TASK_TYPE 5는 *다른 것*의 개수라 일치할 이유가 없다 — 이 혼동이 실제로 독립 검증에서 "기준 불명" 지적을 낳았다. 그리고 "전부"는 호출부 단위이지 "모든 LLM 호출"이 아니다**(요청 경로 + 생성 워커는 기록, script·diagnostic은 계약상 미기록) — `analysis_extractor`·`writing_gate`·`compare_judge`·`query_planner`·`writing_retrieval_planner`·`writing_generation`·`writing_revision`·`writing_report` — `GET /projects/{id}/observability/kpi`가 집계를 내고, `/projects/:id/observability` 화면이 그것을 그린다. QUAL-1(제품 품질·수기·dogfood)과 별개 트랙이며 운영기획 포트폴리오는 `docs/observability-kpi-rationale.md`.
- **새 호출부를 계측하는 법**(Phase 7 등): ① `main.py` 조립 지점에서 provider를 `ObservedProvider(inner, call_site=…)`로 감싼다(도메인 코드는 건드리지 않는다). ② 그 호출이 일어나는 요청 경로에서 `llm_call_scope(...)`를 연다 — **감싸기와 scope 개방은 항상 함께 간다. 빠뜨리면 레코드가 조용히 0건인데 스위트는 green이다**(scope 유닛 테스트로는 안 물리므로 배선 회귀를 함께 넣는다). ③ **조립 가드도 함께 넣는다** — 하네스는 `ObservedProvider`를 직접 만들기 때문에 `_default_*`가 감싸기를 빠뜨려도 green이고 **배포에서만** 계측이 사라진다(실측: gate 조립에서 wrapper를 벗겨도 56 passed). 가드는 **리터럴까지 단정한다** — 잘못된 site로 감싸는 것은 안 감싸는 것과 똑같이 틀렸다. ④ 도메인만 아는 판정(gate decision·파생점수)은 `scope.annotate_last(...)`, 최종 도메인 거부는 **`scope.reclassify_last_as_parse_error(...)`**(마지막 레코드가 `success`일 때만 동작 — `provider_error` taxonomy를 덮지 않기 위한 가드)로 flush 전에 얹는다. `ContextSearchFailed` 계열은 같은 모듈의 `reclassify_planner_parse_error(scope, exc)`가 `llm_error` 계보만 걸러 준다 — **endpoint와 worker가 같은 정의를 쓴다**(복제하면 두 정책이 조용히 갈라진다).
- **계측에서 지켜야 하는 계약**(SoT §"LLM 파이프라인 관측(KPI)"): ① 레코드는 **provider가 실제로 호출된 경우에만** — seam C에서는 구조적으로 참이다. ② **실패한 호출도 센다**(성공만 세면 성공률이 영구히 100%). ③ **scope 밖 호출(worker·script)은 기록하지 않는다** — 추측 `project_id`는 오염이다. ④ 격리(`_flush`)를 **좁히지 않는다** — 좁히면 감사 저장소의 pymongo 예외가 전역 handler(v1.7.38)에 도달해 **정상 200이 503으로 뒤집히고**, flush가 `finally`에 있어 요청의 원래 예외까지 덮어쓴다.
- **`parse_error` 재분류는 호출부가 명시할 때만 일어난다**(데코레이터는 도메인 거부를 모른다). v1.7.47부터 **`analysis_extractor`만 재분류하지 않고 나머지 7 site는 재분류한다** — 재분류가 마지막 호출 1건만 건드리므로 repair로 회수된 첫 호출은 `success`로 남고 repair 빈도 신호가 손상되지 않기 때문이다. 따라서 **extractor의 `parse_error`=0은 구조적 사실이지 데이터 부족이 아니다**(`outcome`이 `success`·`provider_error` 둘뿐). **같은 repair 구조인데 정책이 갈리는 상태**이며, 정렬 여부는 아래 오너 결정 대기 항목이다.
- **집계 API를 읽을 때의 함정 3가지**(v1.7.48, 전부 응답이 분모를 함께 실어 방어한다): ① `total_tokens`는 `success`+`parse_error` 행만 — 분모는 `tokens_counted_from`. ② **표본이 0이면 비율은 `null`이지 `0.0`이 아니다**(`gate.avg_quality_score`·`loop.non_convergence_rate`) — loop 감사는 opt-in(기본 off)이라 기본 배포에서 `loop.runs_considered`=0이 정상이다. ③ **`multi_call_correlations`는 repair 수가 아니다** — site 고정 후 레코드 2건 이상인 correlation 수이며, repair 구조 site(extractor·compare·planner)에서만 repair를 뜻하고 writing loop에서는 **설계된 라운드**다(gate 최대 3회).
- **집계를 손볼 때 지켜야 하는 것**(v1.7.57): per-project(`GET /projects/{id}/observability/kpi`)와 전역(`GET /admin/observability/kpi`)이 **`kpi.py`의 `_fold` 한 곳을 공유한다** — 위 함정 3종이 전역에서도 성립하는 이유가 그 공유다. 규칙을 한쪽에만 고치는 형태로 갈라 놓으면 두 화면이 다른 사실을 말하게 된다. 그리고 **`multi_call_correlations`의 버킷 키는 `(project_id, correlation_id)`다** — `correlation_id`는 호출자가 준 `request_id`라 project가 다르면 같은 문자열이 나올 수 있고, project를 키에서 빼면 per-project는 멀쩡한데 **전역만 조용히** 일어나지 않은 repair를 센다.
- **loop 내부 gate 레코드에는 `decision`·`gate_quality_score`가 없다**(v1.7.47 알려진 공백): 파생점수는 endpoint가 `annotate_last`로 얹는데 revise loop이 round별 판정을 결과에 노출하지 않는다(`WritingLoopStage`는 stage/ordinal/status만). 그 필드는 **독립 `POST …/writing/gate` 호출에만** 채워지므로 집계가 전수 커버리지를 가정하면 안 된다.
- 공개 API 계약(H3)은 닫혀 있다: **전체 76개 operation**이 realistic 에러 상태를 OpenAPI에 선언하고 미매핑 500 부채는 0건이다. 새 endpoint는 `responses=`와 dependency를 함께 붙이고, `tests/test_auth_api.py`의 tier 전수 가드에 등재한다. 저장소 예외를 광의 `except Exception`보다 먼저 503으로 보존한다.
- 회귀 기준선: backend **test-mongo ON 2271 passed / 1 skipped / 2430 subtests**(2026-08-11 베타 실측, 1093초 — Slice 10.1. **셀 +3 · subtest +62** = 신규 [`test_design_token_provenance.py`](tests/test_design_token_provenance.py) · **subtest +1** = 검증 기록 한 건(`bd2c679`, 코드 무관)). **★ 이 backend 값은 2026-08-11 이후 재측정되지 않았다** — 10.3 은 백엔드 코드를 0줄 바꿨고 스타일시트를 읽는 두 가드(`test_design_token_provenance.py`·`test_activity_ui_labels.py`)만 표적 실행해 `11 passed / 119 subtests` 로 green 을 봤다. 전수는 다음 작업자가 돌린다. frontend **318 passed / 26 files**(2026-08-12 베타 실측, 412초 — Slice 10.3 타이포·차트 + 검증 보강 2회 + **Slice 10.4 배치**. **셀 +12 · 파일 +3** = 신규 [`typeScale.test.ts`](frontend/src/typeScale.test.ts) 3 · 신규 [`chartColors.test.ts`](frontend/src/observability/chartColors.test.ts) 3 · `designTokens.test.ts` 에 TS 색 리터럴 셀 1 · 검증 보강 2(차트 검산 출처 · overlay 전수) · **[`pageLayout.test.ts`](frontend/src/pageLayout.test.ts) 3**(10.4). **프로덕션 CSS 는 회귀를 한 셀도 물지 않았다**), build **704 modules · 진입 421.78 kB 무변** · CSS 30.94 → **31.76 kB** · 관측 lazy 386.70 → **387.43 kB**(`chartColors` 모듈이 **lazy 경계 안**에 들어갔다 — 진입이 안 움직인 것이 그 증거다). **그 아래는 이력이다** — 종전 frontend `309/24`(타이포만) · `306/23`(2026-08-11 — 10.2 날짜 그룹 +11 · 검증 보강 +1), build **703 modules · 진입 421.78 kB** · CSS **30.94 kB**. **그 아래는 이력이다** — 종전 frontend `294/22`(+3 cells · +1 file = [`designTokens.test.ts`](frontend/src/designTokens.test.ts)), build **702 modules · 진입 420.81 kB 무변**(색은 CSS 에만 있다) · CSS 26.62 → **30.47 kB**. **그 아래는 이력이다** — 종전 backend `2268/1/2367`(2026-08-11 베타 실측, 986초 — `/me/activity` 500 수정. **셀 +2 · subtest 0** = 주입 없는 조립 가드 `PersonalActivityAssemblyTest` 2셀이고 subtest 를 만들지 않는다. 같은 날 문서 3건 추가는 `plans/` 라 판정 열 셀을 안 건드렸다). frontend **291 passed / 21 files**(2026-08-11 실측 — 10.0 검증 조건 폐쇄. **셀 +2 · 파일 +1** = 제품명 가드 [`productName.test.ts`](frontend/src/productName.test.ts)), build **702 modules · 진입 420.81 kB 무변**(탭 타이틀은 JS 번들 밖이다). **그 아래는 이력이다** — 종전 frontend `289/20`(10.0 본체, +4 cells) · `285/20`(10.0 착수 전) · 진입 `420.08 kB`. **그 아래는 이력이다** — 종전 backend `2266/1/2367`(2026-08-10 베타 실측, 879초 — 9.2 검증 hardening 반영. **셀 +1 · subtest +3** = 상한 연결 가드를 **짝 전수**로 넓힌 것 + 검증 기록 1건). **그 아래는 이력이다** — 종전 backend `2265/1/2364`(2026-08-10 베타 실측, 862초 — Slice 9.2. **셀 +10**(통합 HTTP 4 · 저장 4 · **어댑터 2**) · **subtest +9**(tier 전수 가드가 operation 78 을 돈다)). frontend **285 passed / 20 files**(+12 cells · +1 file), build **702 modules · 진입 420.08 kB**(417.09 → +2.99, lazy 청크 무변). **★ 종전 이 줄이 적던 704 는 측정하지 않고 쓴 값이다**(빌드 출력에서 모듈 줄이 잘렸는데 채워 넣었다) — 독립 검증이 702 로 잡았고 재측정으로 확인했다. **부하 지표(진입 kB·lazy kB)는 정확히 일치**한다. **그 아래는 이력이다** — 종전 backend `2255/1/2355`(2026-08-10 베타 실측, 1024초 — 9.1 hardening 반영. **셀 +1 = 상한 연결 가드** · **subtest +1 = 검증 기록 한 건**(`b18cb83`, 코드 무관)). frontend **273 passed / 19 files**(2026-08-10 IA 1차 — 접근 이력 링크 제거 over-strict 셀 +1). build **701 modules · 진입 417.09 kB**(417.19 → **-0.10**, 링크 하나 분량. lazy 청크 무변). **그 아래는 이력이다** — 종전 frontend `272/19`(백엔드 전수와 **동시 실행**에서도 green — 원 flake 의 과부하 조건 재현). **그 아래는 이력이다** — 종전 backend `2254/1/2354`(2026-08-10 베타 실측, 906초 — Slice 9.1. **셀 +4 · subtest +28 은 전부 새 연결 가드 [`tests/test_activity_ui_labels.py`](tests/test_activity_ui_labels.py)** 이고 **프로덕션 백엔드 코드는 0줄 바뀌었다**). frontend **272 passed / 19 files**(+7 cells · +1 file), build **701 modules · 진입 417.19 kB**(414.36 → +2.83, lazy 청크 무변 — 이 화면은 eager 다). **그 아래는 이력이다** — 종전 backend `2250/1/2326`(2026-08-10 **베타** 실측 — `elasticsearch` 패키지가 있어 **원시값이 곧 보정값**이다, 857초. **셀 증감 0 · subtest +1 은 검증 기록 한 건**이며 코드와 무관하다 — 2026-08-10 검증 기록이 `test_docs_indexes.py` 의 판정 열 전수 셀을 `240 → 241` 로 올린 것이다). **그 아래는 이력이다** — 종전 `2250/1/2325`(2026-08-09 검증 보강 실측 — 알파 원시값 `2247/4`, 185초). 직전 `2249/1/2324` 에서 **셀 +1 · subtest +1**: 셀은 502 partial 분기를 잠그는 [`test_writing_accept.py`](tests/test_writing_accept.py)의 `test_a_partial_accept_still_records_the_saved_version`(검증 Blocking 폐쇄), **subtest +1 은 검증 기록 한 건**(`service_activity_log_accept_extension.md` — 코드와 무관한 그 자리다). **그 아래는 이력이다** — 종전 `2249/1/2324`(2026-08-09 accept 확장 실측 — 알파 원시값 `2246/4`, 187초. **독립 검증이 같은 값을 재현했다** — `2246/4/2324`, 191초, skip 4건은 live Chroma 1 + `elasticsearch` 미설치 3). 직전 `2247/1/2322` 에서 **셀 +2 · subtest +2**(accept 행위 셀 둘 + 전수 가드가 logged 20 을 도는 subtest). **그 아래는 이력이다** — 종전 `2247/1/2322`(2026-08-09 Phase 9 본체 실측 — **알파 원시값 `2244/4`** 에 `elasticsearch` 패키지 부재 3건을 보정한 값, 180초). 직전 `2211/1/2247` 에서 **셀 +36 · subtest +75** 이고 전부 신규(전수 가드 7 · 저장 계약 16 · HTTP 12 · reconciler 1). **operation 은 76 → 77**(`GET /projects/{id}/activity`). 프론트는 `265/18` 무변이고 `schema.d.ts` 만 재생성됐다(+118줄). **그 아래는 이력이다** — 종전 `2211/1/2247`(2026-08-09 Slice 2 실측 — **알파 원시값은 `2208/4`이고 `elasticsearch` 패키지 부재 3건을 보정한 값**, 171초). 직전 `2200/1/2169`에서 **셀 +11 · subtest +78** 이고 전부 신규 가드다 — 셀은 [`test_admin_surface_separation.py`](tests/test_admin_surface_separation.py) 10 + [`test_app_import_paths.py`](tests/test_app_import_paths.py) 진입점 1, subtest 78 은 앞 파일의 operation 76 전수 계약 대조 + 2. **operation 은 76 무변**이다. **★ 남의 테스트에서 리터럴을 재사용할 때 클래스를 import 하면 pytest 가 그 클래스를 여기서 한 번 더 수집·실행한다** — 이 파일이 처음에 22 cells / 475 subtests 로 세어졌다. **모듈만 import 하고 속성으로 읽는다**(`878f24d`). **그 아래는 이력이다** — 종전 값 `2200/1/2168`(2026-08-08 `3ff4274` 실측 — **알파 원시값은 `2197/4`이고 `elasticsearch` 패키지 부재 3건을 보정한 값이 2200/1이다**, 194초. 알파가 베타보다 4배 빠르다 — 843초는 베타 값이었다). 직전 `2200/1/2167`(`c289bce`)에서 **셀 증감 0** — **미사용 import 21개 제거가 셀을 하나도 안 건드렸다**. **subtest +1 은 4차 검증 기록(`5e19867`)**이 판정 열 전수 셀을 1 늘린 것으로 코드와 무관하며, 그날 검증자가 예고한 값과 일치한다. **★ 다음 전수 회귀는 `2200/1/2169` 다** — 2026-08-08 검증 기록(`b9fca77`)이 같은 셀을 또 1 늘린다(측정한 값이 아니라 예고값이다. `test_docs_indexes.py` 단독은 236 → **237 subtests** 로 실측했다). **그 아래는 이력이다** — `c289bce`(4차)는 그 직전 `2200/1/2165`(3차 `70584c2`)/`2166`(3차+검증기록 `4ebde99`)에서 **셀 증감 0**(operation 76 유지). **subtest +1 은 4차 글롭 가드**(routers 10→11, [`test_app_import_paths.py`](tests/test_app_import_paths.py)의 라우터 먼저-로드 셀). 직전 `2200/1/2160`(같은 날 검증 hardening)에서 **셀 증감 0** — **25 operation 이동이 셀을 한 개도 더하거나 빼지 않았다**. **subtest +3**은 `routers/*.py` 가 6 → 9개가 되며 [`test_app_import_paths.py`](tests/test_app_import_paths.py)의 라우터 먼저-로드 셀이 3 subtest 더 돈 것이다(그 셀은 목록을 글롭으로 읽으므로 **새 라우터마다 자동으로 오른다**). **★ subtest 수가 코드와 무관하게 오르는 자리가 둘 있다 — 회귀로 오독하지 말 것**: `routers/*.py` 개수(위)와 **검증 기록 건수**(`test_docs_indexes.py` 의 판정 열 전수 셀이 기록 한 건당 1 subtest — 그래서 검증 기록을 쓸 때마다 1씩 오른다). **★ 종전 이 줄이 적던 `2196/1/1933`은 그 시점에도 이미 낡은 값이었다** — 2026-08-06 `abcface`가 `test_docs_indexes.py`에 **셀 1 + subtest 222**를 더했는데(그날 work_log Task 6이 `12/10 → 13/232`로 적는다) 이 줄이 갱신되지 않았다. 즉 라우터 분해 착수 직전의 진짜 기준선은 **2197/1/2155**였다. 거기서 **subtests +4는 전부 [`test_app_import_paths.py`](tests/test_app_import_paths.py)의 `test_a_router_module_loads_before_main`이 하드코딩 2개 → `routers/*.py` 전수(6개)로 넓어진 것**이고, **5 operation 이동은 셀 수도 operation 수도 한 자리 안 바꿨다** — 무변의 실측은 숫자가 아니라 `repro_router_split.py` 지문 IDENTICAL 이다. 그 아래는 이력이다: `2193/1/1931`(H-3)에서 **+3은 전부 `test_app_import_paths.py` 신규 셀**(라우터 먼저 로드 · `python -m` · 모듈 동일성)이고 **subtests +2**는 그중 라우터 먼저 셀의 subtest 2개다. 직전 `2191/1/1931`(8.2c hardening)에서 **+2는 전부 신규 [`test_app_import_paths.py`](tests/test_app_import_paths.py) 2 cells**이고, **라우터 분해 12 operation 이동은 이 숫자를 한 자리도 안 바꿨다**(subtests 1931 무변 = operation 76 유지). 그 아래 8.4→8.2c 비교는 이력으로 남긴다: **8.4 값 2173/4/1931과 비교할 때 skip 4→1을 먼저 본다** — 줄어든 3건은 코드가 아니라 **이 셸에 `elasticsearch` 패키지가 있어** `test_context_search_memory_lexical_retrieval.py` 3 cells가 이번엔 돌았기 때문이다(HANDOFF가 여러 번 겪은 그 드리프트, 이번엔 반대 방향). 그것을 보정하면 **+15가 전부 8.2c 신규 셀**이다(이름 이력 도메인 4·Mongo 어댑터 4·purge HTTP 5·실 Mongo 2 — 마지막 넷 중 둘은 독립 검증 hardening). **subtests가 1931로 무변인 것이 operation 76 유지의 실측이다.** 남은 skip 1건은 호스트에서 구조적으로 항상 skip되는 live Chroma 셀. frontend **265 passed / 18 files**(8.2c는 기존 purge 셀에 문구 단정 2줄을 더해 셀 수 무변), build **진입 414.36 kB · AdminConsole lazy 8.39 → 8.50 kB · 관측 lazy 386.70 kB**. skip 수는 머신·인프라 기동 여부마다 달라 같은 환경에서 비교한다. backend는 `argon2-cffi`, frontend는 `npm install`이 선행돼야 한다. 종전 값 이력: 2200/1/2163(라우터 분해 잔여 2차) · 2200/1/2160(검증 hardening) · 2197/1/2159(라우터 분해 잔여 1차) · 2197/1/2155(그 착수 직전 · `abcface` 반영) · 2196/1/1933(공유 prelude 추출 시점 실측) · 2191/1/1931(8.2c hardening) · 2189/1/1931(8.2c 검증 시점) · 2173/4/1931(8.4) · 2150/1/1923(8.3 hardening) · 2062/1/1725(8.3 착수 전) · 1911/4/1625(알파 D8-6).
- **스택 health를 읽는 법**(머신 무관, 구조적 사실): 정상 상태는 **healthy 8**(`application`·**`admin`**·`gateway`·`mongo`·`elasticsearch`·`embedding`·`chroma`·`frontend`) + **healthcheck 없는 2**(`worker`·`generation_worker` — async 배경 워커라 by design, "Up"이지 "healthy" 아님). **"전부 healthy"라고 쓰지 않는다.** **`admin` 은 2026-08-09 Slice 2 가 더한 여덟 번째다** — 포트를 게시하지 않으므로 호스트에서는 안 보이고, 확인은 `docker compose ps` 나 `docker compose exec admin python -c "…/health…"` 로 한다.
- **[★ 최신 베타 관측, 2026-08-10 — 지금 작업 중인 머신]** `nvidia-smi` **GTX 1060 3GB**로 베타 확정(2026-08-09 작업은 알파였다 — 기록의 실측 라벨을 읽을 때 머신을 먼저 본다). **★ 스택이 떠 있고 `application`·`admin`·`frontend` 이미지를 이 날 재빌드했다** — 그 전 이미지는 11일 전 것이라 **활동 API(operation 77)가 아예 없었다**(화면 확인이 재빌드를 필수로 요구한 이유이며, 옛 이미지로 확인하면 404 를 보고 "구현이 안 됐나"로 오독한다). `docker compose ps` = **healthy 8**(application·admin·gateway·mongo·elasticsearch·embedding·chroma·frontend) + healthcheck 없는 2(worker·generation_worker). **게이트웨이 healthcheck 는 liveness 전용이라 외부 LLM 이 죽어 있어도 스택은 뜬다**(활동 화면 확인에 LLM 불필요). test-mongo 는 전수 회귀 때만 `docker compose -f docker-compose.test.yml up -d`로 올렸다 내린다(그 절차가 표준이다). **★ 이 호스트에는 `pyflakes`가 없고 `pip install --user`가 PEP 668 로 거부된다** — scratchpad venv 를 쓴다. **★ Mongo DB 이름은 `ai_writing_system` 이다**(`ai_writing` 이 아니다 — 이 세션이 그 이름으로 조회해 "DB가 비었다"는 **거짓 판독**을 한 번 했다. `CORE_SOT_MONGO_DB` 로 확인한다).
- **[★ 베타 육안 확인 준비 완료, 2026-08-10 — 이 머신 관측]** 활동 타임라인 화면을 **눈으로 볼 준비가 끝났다**. **주소** `http://localhost:5520/projects/6a795ab928e4a53aa000a824/activity` · **계정** `timeline_demo` / `timeline-demo-0810`(확인용으로 이 세션이 만든 계정이다 — 기존 `owner`·`probe`·`d8four` 를 건드리지 않으려고 따로 만들었다). 그 프로젝트에 **활동 5건**이 실제로 쌓여 있다(프로젝트 생성·개명 · 원고 생성·개명 · 원고 저장) — 라벨·`before → after`·`draft` 행 링크·`draft_version` 행 무링크(F7)를 한 화면에서 볼 수 있다. **API 관통은 이미 확인됐다**(응답 5건 · 최신순 · 라벨표로 렌더 대조) — 남은 것은 **브라우저 렌더 육안 확인 하나**다. **★ 부수 확인**: `idempotency_key` 를 빠뜨린 **422 는 활동을 남기지 않았다** — A7("결과를 안 뒤에 기록")의 라이브 증거다. **정리하려면** 그 project 를 파기하면 활동도 함께 사라진다(I1) 또는 `timeline_demo` 계정을 비활성화한다.
- **[베타 DB·계정·시드, 2026-07-31 — 베타로 돌아갈 때만 쓴다]** 상태 서술(스택 기동·이미지 날짜)은 위 2026-08-03 관측이 대체했으므로 지웠다. 남는 것은 **베타 DB 안의 물건**뿐이다: 프로브가 만든 project와 `llm_call_audits`가 육안 확인용으로 **일부러 남아 있다**. 계정 `probe`(admin). Mongo `prompt_templates`는 **`analysis_extract` v1~v4 4행뿐**(report·gate·생성 템플릿은 in-memory seed). **예산 포화 프로젝트(재측정용)**: `6a6be9c0dbb39de0a51ed8ba` / draft `6a6be9c0dbb39de0a51ed8bb` / version `6a6be9c0dbb39de0a51ed8bc` — 밀도 1.63으로 만든 쪽이며 **이것을 쓴다**(`6a6be92bda7b035f309a8005`는 1차 시드, 밀도 1.54로 대표성 낮음). 머신 무관 함정 둘은 여기 남긴다 — **첫 `docker` 명령이 30초 넘게 안 돌아올 수 있다**(데몬 워밍업, 죽은 것으로 오독 금지) · **라이브 검증은 작업 트리를 마운트해 돌린다**(`docker compose run --rm --no-deps -v "$PWD/services:/app/services" …` — 재빌드 없이 실 파이프라인을 관통하는 표준 방법).
- **[베타, 2026-07-27 — 여전히 유효한 함정]** 이 머신 DB는 그때 비웠다. 구 `analysis_extract_v3` 본문(sha `fb4e272…`)이 현재 canonical(sha `4376310…`)과 달라 `PromptTemplateConflict`로 app이 죽었고, **오너 판단으로 데이터 볼륨(mongo·es·chroma)을 비워**(embedding 모델 캐시는 보존) 해소했다. 코드 회귀가 아니라 **오래된 볼륨과 현행 프롬프트 핀의 충돌**이며, 오래된 볼륨을 가진 다른 머신에서도 같은 일이 난다(위 "출시된 프롬프트 본문은 immutable" 함정과 같은 뿌리).
- **[알파 관측, 2026-08-08 — 지금 작업 중인 머신이 아니다. 현재 머신은 위 ★ 베타 항목]** `nvidia-smi` **RTX 3060 12GB**로 알파 확정. `docker compose ps`에는 **`frontend`(healthy)·`worker`·`generation_worker` 3개만** 있고 application·mongo·gateway·embedding·chroma·ES는 떠 있지 않다. test-mongo는 전수 회귀 때만 `docker compose -f docker-compose.test.yml up -d`로 올렸다 내렸다(그 절차가 표준이다). **★ 이 호스트에는 `pyflakes`가 없고 `pip install --user`가 PEP 668 `externally-managed-environment`로 거부된다** — `--break-system-packages`를 쓰지 말고 **scratchpad에 venv를 만들어 설치한다**(호스트 무오염·repo에 남는 것 0). 라우터 분해 슬라이스가 출발값으로 쓰는 F401 실측이 이 도구에 묶여 있으므로 다음 작업자도 같은 벽을 만난다. 이미지 부채는 아래 ★★ 항목이 여전히 유효하다 — **스택을 올렸으면 `curl :8520/projects`가 401인지 먼저 본다**.
- 인증 백엔드 변경은 application, 로그인 UI 변경은 frontend 이미지 rebuild가 필요하다.
- **★ 앱 이미지는 이제 하나다 — `ai_writte_system-app`(2026-08-11 수정).** `application`·`admin`·`worker`·`generation_worker` 네 서비스가 compose 에서 **같은 `image:` 태그**를 공유하므로 `docker compose build` 한 번이면 넷이 함께 갱신된다(빌드도 1회만 돈다 — 실측). **그 전에는 넷이 각자 태그를 가졌고**, 주석은 *"Shares the application image"* 라고 적었지만 설정이 그것을 구현하지 않아 **`build application` 이 나머지 셋을 갱신하지 않았다.** 실제 피해가 있었다: **worker 이미지가 07-27 로 15일 뒤처져 `PROJECT_PURGED` drain 이 아예 없는 채로 돌고 있었다**(D8-6 파기의 워커 절반이 죽어 있었다. outbox 가 비어 있어 아직 터지지는 않은 잠복 상태였다). **새 서비스가 이 Dockerfile 을 쓰면 `image: ai_writte_system-app` 을 함께 적는다** — 빠뜨리면 같은 병이 재발한다.

## 기동 · 실행법

**포트는 전용 대역으로 repo에 고정돼 있다.** env 없이 `docker compose up`만으로 어느 머신에서든 같은 포트로 뜬다. 값과 근거는 [`.env.example`](.env.example)에 있고, 머신별로 바꿔야 하면 `.env`로 복사한다(커밋 금지). **2026-08-02부터 아래 표의 포트는 종류가 둘이다** — `application`·`frontend`(+ 별도 파일의 `llama` 9080)만 전 인터페이스이고, **나머지는 `127.0.0.1` 전용**이라 다른 기기에서는 보이지 않는다(D8-7 G1=C, 위 "지금 상태" ★ 항목).

| 서비스 | 포트 | 서비스 | 포트 |
|---|---|---|---|
| application | 8520 | chroma | 8523 |
| **admin** | **게시 없음**(nginx `/api/admin/` 경유 전용) | | |
| gateway | 8521 | elasticsearch | 9520 |
| embedding | 8522 | frontend | 5520 |
| mongo | 27520 | test-mongo | 27020 |

표준 포트(27017·8000~8003·9200·5173)를 쓰지 않는 이유는 `.env.example` 상단에 있다 — 여러 머신을 옮겨 다니는 프로젝트라 충돌하고, 임시 env 회피는 repo에 안 남아 문서에 머신-로컬 관측치가 사실처럼 적히게 된다.

**백엔드 테스트** — `docker compose -f docker-compose.test.yml up -d` 후 `python3 -m pytest -q`. env 불필요(기본 URI가 27020 replica set `rs-test`). 미기동이면 Mongo 테스트가 **skip**(실패 아님). 끝나면 `... down`.

> **함정 — `up -d` 직후 곧바로 돌리지 말 것(2026-07-29 실측).** healthcheck는 `rs.initiate` 후 **writable PRIMARY**가 될 때까지 healthy를 보고하지 않으므로 기동에 수십 초가 걸린다. 고정 `sleep`을 주고 시작하면 **초반 모듈만 skip되고 나머지는 붙어**, 전량 실패가 아니라 **부분적으로 잘못된 기준선**이 나온다(실측: `1698 passed / 9 skipped` — 정상은 `/ 1`). 증상이 조용해서 "내 변경이 8건을 깨뜨렸나"로 오독하기 쉽다. **healthy를 기다린 뒤 시작한다**: `until [ "$(docker inspect -f '{{.State.Health.Status}}' ai_writte_system-test-mongo-1)" = healthy ]; do sleep 2; done`. 숫자가 안 맞으면 `-rs`로 skip 사유부터 본다.

**live Chroma 테스트**(호스트 `pytest`에서는 항상 skip되는 1건) — 호스트를 오염시키지 않고 이미 `chromadb`가 있는 application 이미지에서 돌린다:

```bash
docker compose up -d --no-deps chroma
docker compose run --rm --no-deps -v "$PWD/tests:/app/tests" \
  -e CHROMA_TEST_URL=chroma:8000 \
  application python -m unittest tests.test_chroma_adapter.ChromaAdapterLiveTest -v
```

**창 가드 라이브 확인**(K-3) — 배포된 게이트웨이 컨테이너에 붙어 `창 ± 1` 경계를 **자기 교정**으로 본다(창을 응답에서 읽으므로 배포 창이 8192든 16384든 그 배포의 경계를 본다):

```bash
docker compose run --rm --no-deps -v "$PWD/scripts:/app/scripts" -e PYTHONPATH=/app \
  application python scripts/gateway_generate_live_smoke.py --gateway-base-url http://gateway:8001
```

`context_window_guard.exercised: false`는 실패가 아니라 **창을 아직 몰라 판정 대상이 아니었다**는 뜻이다(프로세스 첫 호출·`/props` 실패). 게이트웨이 코드를 고친 뒤라면 `docker compose build gateway && docker compose up -d --no-deps gateway`가 선행돼야 한다.

**프론트** — `cd frontend && npm run gen:api && npx tsc --noEmit && npm run build && npx vitest run`.

## 함정 (모르면 시간을 잃는 것들)

- **출시된 프롬프트 본문은 immutable이다.** `tests/test_prompt_templates.py`가 `analysis_extract` **v1~v4 전부**의 본문 sha256을 핀한다(**현행 v4가 빠져 있던 것을 2026-07-30에 발견해 채웠다** — 옛 버전은 아무도 고칠 이유가 없지만 **현행 본문은 고칠 이유가 늘 있어서** 핀이 가장 필요한 자리가 그쪽이다. 핀 목록이 출시 버전 전부를 덮는지도 같은 셀이 단정한다). 본문을 고치면 테스트가 깨지는데 **해시를 갱신하면 안 되고 새 버전을 만들어야 한다.** 어긴 결과: 기존 Mongo를 가진 배포가 `PromptTemplateConflict`로 전부 죽어 스택이 3일간 안 떴다. Mongo에 영속되는 프롬프트는 `analysis_extract` 하나뿐이라, 다른 프롬프트를 영속으로 옮기면 sha256 핀도 함께 확장해야 한다.
- **compose의 `ulimits.nofile`은 튜닝이 아니라 필수다.** Docker 기본 1024면 WiredTiger가 `Too many open files`로 mongod를 죽이는데, **test-mongo에서는 증상이 skip이 아니라 failure라 코드 회귀처럼 보인다.** 배포 `mongo`(64000)·`elasticsearch`(65535)에도 같은 이유로 들어가 있다 — 값이 다른 것은 각 데몬이 요구하는 최소치를 그대로 쓴 것이다.
- **뮤테이션 테스트 원복에 `git checkout -- <file>`을 쓰면 미커밋 작업이 사라진다.** 이 프로젝트는 "변형을 넣고 테스트가 무는지 본 뒤 원복"을 표준 절차로 쓰는데, **슬라이스 자체가 아직 커밋되지 않은 상태가 흔하다** — 그때 `git checkout --`은 변형만 지우는 게 아니라 **HEAD로 되돌려 그 슬라이스를 통째로 날린다**. 실제 사고(2026-07-30 독립 검증): `git checkout -- writing/report.py`가 미커밋 R-e(v2)를 v1으로 되돌렸고, 검증자가 처음 읽은 내용으로 복구해 무사했다. **원복은 역방향 Edit이나 사전 `cp` 백업으로 한다.** **★ 2026-08-03에 재발했다**(8.2b 검증 보강 중 `git checkout -- services/application/app/quota/`로 미커밋 수정 소실 — `cp` 백업과 재작성으로 복구). 규칙을 아는 것만으로는 부족하다는 증거이므로 **순서를 고정한다: 수정을 먼저 커밋 → 뮤테이션 → `git checkout`으로 원복**(CLAUDE.md §6이 checkpoint 커밋을 요구하는 이유가 정확히 이것이다). **★★ 2026-08-04 사고를 계기로 로그를 전수 조사했더니 이 사고는 총 9건이었고, 그중 8건이 구현자였다**(2026-07-26·07-30×2·08-02×4·08-03·08-04 — 검증자는 07-30 한 건뿐이고, 08-02에는 같은 사람이 하루에 세 번 밟았다). 그래서 규칙을 문서 여러 곳의 주의사항이 아니라 **실행하는 게이트**로 옮겼다: **뮤테이션 전 `git status --short`가 비어 있어야 한다**(비어 있지 않으면 먼저 커밋). 절차 정본은 [`docs/guides/verification.md`](docs/guides/verification.md) §"Mutation testing"이고 — 커밋할 수 없는 검증자용 분기(`cp` 백업 + 역방향 Edit + 바이트 대조)까지 거기 있다 — `CLAUDE.md`·`AGENTS.md` §6이 전제조건과 함께 그곳을 가리킨다. 그리고 남이 복구했다고 하면 **백업과 `diff`로 직접 대조한다** — `git diff --stat`이 같아도 내용이 같다는 보장은 아니다.
- **★ 뮤테이션 결과를 `grep FAILED` 로 읽으면 subtest 실패를 통째로 놓친다**(2026-08-09 실측). `pytest-subtests` 는 실패한 subtest 를 **`SUBFAILED(...)`** 로 찍으므로 `^FAILED` 필터에는 **한 줄도 안 걸리고**, "물지 않았다"로 읽힌다 — 요약줄에만 `9 failed` 가 남는다. 실제로 Slice 2 독립 검증이 이 필터로 A6 셀을 *"재실패 없음"* 이라 읽었다가 단독 실행으로 정정했다. **하필 전수 대조 셀(가장 강한 가드)이 대부분 `subTest` 로 돈다.** 규칙: **요약 count 줄을 읽고**, 페어링할 때는 `FAILED|SUBFAILED` 를 매칭한다. 절차 정본은 [`docs/guides/verification.md`](docs/guides/verification.md) §"Mutation testing" 에 있다 — **필터 때문에 "안 문다"고 기록하면 다음 사람이 멀쩡한 가드를 지우러 온다.**
- **★ compose의 포트 매핑 변경은 이미 만들어진 컨테이너에 적용되지 않는다 — `up -d`로도 안 된다**(2026-08-02 독립 검증이 잡았다). 포트 매핑은 **컨테이너 생성 시점에 고정**되므로, D8-7 G1=C의 loopback 바인드는 **파일 수준에서만 참**이고 기존 컨테이너는 `docker ps -a`에 여전히 `0.0.0.0:27520->27017` 같은 **옛 매핑을 들고 있다**. 지금은 그 컨테이너들이 Exited라 실제 노출은 없지만, **옛 컨테이너를 그대로 다시 살리면 LAN 노출이 그대로 복귀한다.** 반영하려면 **재생성**이 필요하다(`docker compose down` 후 `up -d`, 또는 `up -d --force-recreate`). 볼륨은 named라 `down`으로 지워지지 않는다(`down -v`는 지운다 — 쓰지 말 것). **확인법은 파일이 아니라 `docker port <container>`다** — compose 파일을 읽고 "닫혔다"고 결론내면 안 된다. 같은 성질이 env·command 변경 전반에 적용된다.
- **백엔드는 `pytest`가 아니라 `python -m pytest`로 실행한다.**
- **★ 계약에 "…는 잠기지 않는다 / …해도 안 깨진다" 류의 *방어적 단언*을 쓰면, 그 단언을 지나는 셀이 있는지 그 자리에서 확인한다.** 2026-08-02 하루에 두 번 걸렸다 — D8-5e의 `owner_id=None`(**Blocking**: 무소유 project가 승격으로 열렸다)과 C-6의 pre-C-6 Mongo 행(`.get(..., False)`를 하드 서브스크립트로 바꿔도 **1898 전부 통과**). **둘 다 결함을 *넣는* 뮤테이션으로는 원리적으로 안 보인다** — 없는 셀은 무엇을 깨뜨려도 실패하지 않기 때문이다. 잡은 것은 **방어를 없애 보거나 고칠 것을 미리 넣어 보는** 뮤테이션이다. 후자는 특히 fake가 늘 새 필드를 갖는 저장소 계층에서 위험하다(아래 naive-datetime 함정과 같은 형태 — 스위트는 green, 배포만 죽는다).
- **pymongo는 BSON 날짜를 naive로 돌려준다**(client가 `tz_aware=True`가 아닌 한). 그것을 aware `datetime.now(UTC)`와 비교하면 `TypeError`다. **fake-collection 테스트는 이걸 재현하지 못한다** — aware를 넣으면 aware가 나오므로 **스위트는 green인데 배포만 깨진다**. 실측(2026-07-27): 세션 `expires_at` 비교가 실 Mongo에서 `GET /auth/me`를 전량 500으로 만들었는데 유닛 46건은 전부 통과했다. 규칙: **Mongo 날짜를 파이썬으로 끌어와 비교하면 `_entry` 경계에서 UTC를 재부착**하고(`auth/sessions_mongo.py`의 `_aware`), fake collection이 **드라이버처럼 naive를 돌려주는** 회귀를 함께 넣는다. 기존 코드가 무사한 이유는 같은 판정을 **쿼리 서버측**(`{"$lte": …}`)에서 하기 때문이다(`generation_job_mongo.py:85`·`indexing/mongo_repository.py:130-136`) — 그 방식을 따르면 이 함정 자체가 없다.
- **쿠키 인증 테스트는 `TestClient(app, base_url="https://testserver")`로 만든다.** 세션 쿠키는 `Secure`가 기본 on이라 http 클라이언트는 쿠키를 **조용히 버린다** — 세션 테스트가 엉뚱한 이유로 실패한다(실제로 처음 4건이 그렇게 실패했다).
- **in-stack llama(`-hf`)가 캐시가 멀쩡한데도 모델을 다시 받는 이유가 잡혔다**(2026-07-28 알파 실측): `-hf …:Q4_0`은 리비전을 고정하지 않고 `main`을 따라가는데, HF 캐시의 `refs/main`이 `29d0977…`로 이동한 반면 디스크 snapshot은 `f6e7774…`·`2b318d6…`뿐이다. 즉 **"정체"가 아니라 새 리비전을 받는 중**이며, 기다리면 언젠가 뜨지만 6.5 GB를 다시 받는다. 회피는 캐시 snapshot을 `-m`으로 직접 지정하는 것: `-m /models/models--google--gemma-4-12B-it-qat-q4_0-gguf/snapshots/2b318d6ebebf093f50ca4376e858325f10703358/gemma-4-12b-it-qat-q4_0.gguf`. 볼륨에 stale `.downloadInProgress` 약 4.9 GB가 남아 있다(11.8 GB 중). **repo는 아직 안 고쳤다** — 리비전 고정 여부는 추적 부채.
- live 작업 시 외부 llama(`192.168.1.22:9080`)가 죽어 있으면 in-stack llama로 돌린다.
- **[프론트 회귀 플레이크, 2026-07-31 관측]** `DraftEditor.test.tsx` 전체 스위트가 희귀하게(~9회 중 1회)
  "1 failed | 40 passed"로 떨어진다 — 독립 검증 중 관측, 희귀해 테스트명 못 잡았다. 유력 원인 (a) 기존 타이밍
  의존 테스트의 사전 플레이크, 차선 (b) K-4(b) `useWritingBudget` mount-fetch가 DraftEditor 렌더 경로에 새로
  들어간 비동기-온-마운트와의 레이스. **핵심은 프론트 변경 후 이 플레이크가 떨어지면 내 변경 탓이 아닐 수 있다는
  것** — 재현 시 특성화하고 (b)가 의심되면 `useWritingBudget` mount-fetch부터 본다. 상세는
  `docs/verifications/2026-07-31/k4_front_counter_budget.md`.

## Active Decisions (앞으로의 작업을 구속하는 것)

완료 이력이 아니라 **표준 제약**만. 근거는 각 `docs/plans/*-decisions.md`.

- **★ 정본을 나누는 것을 두려워하지 않는다(2026-08-10 오너).** *"서로 다른 섹션의 정본이면(중복된 내용이 아니라면) 정본은 몇 개가 되든 상관없어. **인덱싱만 제대로 되어 있고 연결만 되어 있으면.**"* — 그래서 판단 기준은 "정본이 몇 개인가"가 아니라 **셋**이다: ① **중복인가**(같은 내용을 두 곳이 말하는가) ② **인덱싱돼 있는가**(찾을 수 있는가) ③ **연결돼 있는가**(한쪽이 바뀔 때 다른 쪽이 알게 되는가). **③이 보통 테스트 가드다.** 선례: 9.1 의 프론트 라벨표는 백엔드 `activity/actions.py` 와 **다른 섹션의 정본**이고(배선·분류 ≠ UI 문구) 전수 가드가 연결선이다. **이 원칙이 뒤집는 것**: "정본이 둘이 된다"를 그 자체로 단점으로 적던 습관 — 나쁜 것은 분할이 아니라 **끊긴 분할**이다.

- **개발 단계(2026-07-20 오너)**: "Gate 우선" 단계는 끝났다. 지금은 **Gate ↔ UI/UX 왕복**이 주축이다.
- 아이디에이션·계획이 충돌하면 임의 구현 없이 오너 결정을 받는다. 나중 요청이 기록된 결정과 충돌하면 어느 쪽이 canonical인지 먼저 묻는다.
- monorepo + 독립 LLM Gateway/Worker, Application = FastAPI. 경계는 `project_id`이며 **모든 저장·검색·Gate·tool handler가 강제한다**.
- **개발 단계(2026-07-26 오너)**: MVP 단일 사용자 유예가 만료돼 **다중 사용자로 확장 중**이다(정본 **v1.7.49** = D0=A 단계 전환 — 2026-08-02 독립 검증이 정정. 종전 이 줄이 적던 v1.7.57은 전역 관측 KPI(D8-5c)라 무관하다). 사용자·세션·프론트 로그인과 HTTP 소유권 격리·결합 감사는 섰다. **저장소 계층은 D8-7 G1=C(노출면 축소)로 닫혔다 — 조건은 "인증"이 아니라 "노출 없음"이다**(v1.7.75). 소유권은 `project_id` 강제를 대체하지 않고 그 위에 얹힌다.
- frontend = React + TS + Vite, 서빙은 별도 compose 서비스(nginx). OpenAPI→TS 타입 생성 + 얇은 `fetch` 래퍼.
- **Core SOT**: offset = raw Unicode code point, `content_hash` = raw UTF-8 SHA-256. `source_ref` span은 단일 `source_block` 안에 든다. persistence는 Mongo transaction 기본이고 non-transaction fallback은 **single-writer local/test 전용**. project/draft는 archive(soft delete)하고 snapshot/version/source_ref는 보존한다(archive = 읽기 허용, 본문 쓰기·rename 409).
- **memory는 append-only**. AI가 직접 덮어쓰지 않고 검색·대조·Gate·검토·versioned upsert를 거친다. canonical만 `memory_vectors`에 색인하며 트리거는 async outbox→worker다. semantic 매칭은 **off 기본**.
- **재색인 enqueue는 무조건 choke point다**(v1.7.37): canonical을 만드는 모든 경로가 `MemoryService._enqueue_reindex`를 지나고 **idempotent replay도 재enqueue한다**. outbox가 PENDING/RUNNING 항목에만 dedup하므로 pending 중 replay는 no-op이고, drain된 뒤의 replay만 재색인을 한 번 더 돌린다(upsert라 오염 아님). **`promoted[]` 보고 의미론은 불변** — replay는 여전히 제외되며 바뀐 것은 색인 side-effect뿐이다.
- taxonomy 3종(`character_observation`/`event_observation`/`open_question_observation`), provenance `source_observed`/`ai_inferred`.
- **agent loop 계약층은 더 진행하지 않는다**(tool-call parsing·wire format 미계약). sub-agent spawn 없이 bounded flat loop만.
- **에러 계약(H3)**: 본문은 균일 `{"detail": <string>}`. 상태코드=기계용, `detail`=사람용이라 **`detail` 문자열로 분기하면 안 된다**. 503은 **세 얼굴** — 협력자 **미구성**(배포 구성) · 데이터 **무결성**(`scripts/migrate_ordered_units.py`) · **정본 저장소 장애**(v1.7.35, 복구 후 재시도가 유효한 유일한 얼굴). **상류가 없는 게 아니라 있는데 실패한 것은 502**이고, **정본 저장소는 상류가 아니라 503**이다. 저장소 face는 `create_app`의 **전역 exception handler**가 매핑하므로 새 endpoint가 자동 상속한다 — 다만 **`responses=`에 503을 붙이는 것은 여전히 수동**이고 전수 가드가 빠뜨림을 잡는다(`/health`만 제외).
- **균일 본문의 유일한 예외 = partial envelope**, 허용 지점 **정확히 6곳**(revise-and-gate 4 · accept 1 · auto-promote 1). 되돌릴 수 없는 성공 부분이 이미 영속된 실패 경로만 해당하며, 새 Union은 정본 목록을 함께 넓히는 명시 결정으로만 들어온다(트랙별 over-strict 가드가 drift를 막는다).

## 추적 부채

- **★ [Phase 10 이 연 부채 셋 — 오너 지시로 *다음 세션이 이것부터* 한다](2026-08-12).** 오너: *"다음에는 열려 있는 부채들 먼저 작업하고 검증 한꺼번에 돌리면 될 것 같다."* 셋 다 **가드가 자기 밖을 못 보는** 같은 계열이다.
  - **① 기본 동작 버튼의 겉모습이 다섯 곳에 흩어져 있다** — [`styles.css`](frontend/src/styles.css) 의 `.auth-submit` · `.form-controls button` · `.editor-actions button` · `.writing-actions button, .candidate-actions button, .loop-retry` · `.row-actions button`. 같은 선언이 다섯 벌이라 **새 표면이 어디에도 안 들어가면 조용하다**. **★ 관측된 피해는 0건이다** — 유일한 증거였던 "정문 버튼" 은 하네스 오류였고 정정됐다(`2c6eb9e`). **시각을 바꾸지 않고 닫는 길이 있다**: *겉모습*(색·테두리·커서·transition)만 한 규칙으로 묶고 **패딩은 자리마다 남긴다**(0.72/1.1 · 0.72/1.5 · 0.5/1.1 · 0.78/1.35 로 서로 다르다 — 이걸 합치면 시각이 바뀐다). 그 뒤에야 *"겉모습을 정하는 자리가 하나인가"* 가드가 틀린 전제 없이 선다.
  - **② `typeScale.test.ts` 의 이관 목록은 자기가 부른 이름만 본다**(M5 실측 — 행을 지우면 **아무 셀도 실패하지 않는다**). **닫는 방법이 보인다**: `styles.css` 에서 `var(--type-*)` 를 쓰는 규칙 집합을 뽑아 **`MIGRATED` 의 키 집합과 같은지** 단정하면 행 삭제가 즉시 불일치가 된다. 10.4 의 `pageLayout.test.ts` 가 이미 그 방식(마크업에서 유도)으로 같은 한계를 피했다.
  - **③ T1 — 68rem 확장이 목록 행의 제목↔`→` 간격을 벌린다.** `.resource-link` 가 `justify-content: space-between` 이라 오른쪽 끝까지 밀린다. 헤더 정렬 이득이 더 크다고 판단했으나 **육안 확인 때 다시 볼 자리**이며, 불편하면 행 안쪽에 측정폭 상한을 주는 것이 처방이다(컨테이너 폭은 건드리지 않는다 — 10.4 규칙 ②).

- **★ [유예 · 트리거 있음] Phase 9 화면의 후확장 F1~F6.** 정본은 [`plans/09-1-activity-timeline-screen-decisions.md`](docs/plans/09-1-activity-timeline-screen-decisions.md) §"나중에 여는 문"(각 행에 **여는 트리거**가 붙어 있다). 요약: **F1 커서 페이징**(지금은 최신 100건 고정 — 여는 순간 operation 77 계약 변경 + `(project_id, at)` 인덱스 + `at` 동률 tie-breaker. **관측 화면 `?since=` 와 같은 축이라 따로 열지 않는다**) · **F2 replay 접기**(순수 프론트) · **F3 replay 기록 정책**(A7·A8 재검토 동반, 가장 비싸다) · **F4 행위자 열**(공동 작업이 생기는 날 — `actor_user_id` 필드는 응답에 남아 있어 backend 0줄) · **F5 링크 확장**(candidate·gate_finding·review_queue_entry 에 전용 화면이 생기면) · **F6 필터·검색**(F1 종속) · **F7 `draft_version` 행의 링크**(S6 이 의도했으나 payload 에 `draft_id` 가 없어 불가능했다 — 응답 필드 추가 = operation 77 계약 변경이라 구현자가 그 자리에서 넓히지 않았다). **★ 이것들은 각하가 아니라 유예다** — 이 저장소는 §43 `system_events` 가 문서에만 있고 코드 0줄로 한 페이즈 내내 남았던 형태를 이미 겪었고, 그것이 Phase 9 의 발원이었다. **트리거 없는 유예는 망각이다.**

- **★ [폐쇄 2026-08-09 · 독립 검증 완료 2026-08-10 — 이제 남은 것은 일반 규칙뿐] 활동 로그 `writing/accept` 의 502 partial 분기 셀.** 독립 검증이 **Blocking** 으로 잡았던 자리 — SoT v1.7.93 이 *"502 partial envelope 은 **남긴다**"* 로 명시한 계약 요구 분기인데, [`routers/writing.py:1273-1278`](services/application/app/routers/writing.py#L1273)의 `activity.record(...)` 6줄을 지운 채 **전수 회귀 `2246/4/2324` 가 전부 green** 이었다. 전수 가드는 **같은 handler 의 성공 분기**에 남은 `activity.record(` 로 소스 스캔이 만족돼 통과한다(위 활동 로그 항목 ⑦이 예고한 그 자리인데 정작 셀이 안 들어갔다). **오너 지시로 같은 날 닫았다** — `WritingAcceptApiTest::test_a_partial_accept_still_records_the_saved_version`, **양방향 실측**(분기 record 삭제 · 같은 분기 이중 기록 둘 다 그 셀 하나가 재실패). **2026-08-10 독립 세션이 폐쇄를 재현해 판정을 `합격`으로 승격했다**(셀이 삭제·이중 기록·리터럴 변조·`target_id` 변조 **네 방향**으로 물고, 502 분기를 지워도 전수 가드는 통과하므로 **유일 방어**임까지 재확인). **★ 남기는 것은 일반 규칙 하나**: 한 handler 가 기록 분기를 여럿 가지면 **분기마다 행위 셀**이 필요하다 — 전수 가드는 배선의 *존재*만 보고 *분기*는 못 본다. 기록 [`verifications/2026-08-10/accept_activity_cell_reinforcement.md`](docs/verifications/2026-08-10/accept_activity_cell_reinforcement.md).
- **[폐쇄 2026-08-10] "73 → 74 cells" 라벨이 파일 하나에 잘못 붙어 있던 것.** [`verifications/2026-08-09/service_activity_log_accept_extension.md`](docs/verifications/2026-08-09/service_activity_log_accept_extension.md) §6-b 표와 [2026-08-09 work_log Task 8](docs/daily_logs/2026-08-09/work_log.md) Completed work 표가 **`tests/test_writing_accept.py` 행에** 그 수를 적었는데 **그 파일은 50 → 51 cells** 다. **숫자는 맞고 귀속만 틀렸다** — 73/74 의 출처는 같은 기록 §4 의 **3-파일 세트**(`test_writing_accept.py` + `test_activity_actions.py` + `test_activity_log.py`)이고 test-mongo ON 으로 돌리면 정확히 `74 passed / 79 subtests` 다(2026-08-10 실측). **왜 적어 두는가**: 이 저장소의 표준 확인법이 "그 파일을 열어 세어 보기"라, 다음 사람이 51 을 세고 23 만큼 어긋난 채 *"셀이 사라졌나"* 를 조사하게 된다. **오너 지시로 같은 날 두 행을 정정했다** — 이제 두 기록이 **파일 하나 50 → 51 · 3-파일 세트 73 → 74** 를 함께 적는다(지적 원문은 발행 시점 그대로 보존). **남기는 일반 규칙**: 셀 수를 적을 때는 **무엇을 돌린 수인지**를 같은 줄에 적는다 — 이 저장소의 확인법이 "그 파일을 열어 세어 보기"라, 세트 수를 파일 행에 적으면 다음 사람이 재현할 수 없는 숫자가 된다.
- **[판단 사안 · 오너 결정 대기 · 2026-08-09 독립 검증] idempotent replay 가 활동 이벤트를 매번 추가한다 — 이 슬라이스가 만든 편차가 아니다.** 같은 `idempotency_key` 3회 POST → 이벤트 3건(대상 draft version 1개). **`writing/accept`(200·502 둘 다)와 `drafts/{id}/versions`(수동 저장, 9.0 본체)가 똑같다** — 즉 일관되며 A2·A8 어느 조항도 이 축을 말하지 않는다. **화면(활동 타임라인) 슬라이스가 "저장 3번"으로 그리게 되므로 그때 정한다**: ⓐ 그대로 ⓑ `idempotent_replay` 를 기록에서 제외 ⓒ 이벤트에 replay 표식.
- **[판단 사안 · 2026-08-09] 활동 로그가 auto-promote 의 503 partial envelope 경로는 기록하지 않는다.** [`routers/analysis.py`](services/application/app/routers/analysis.py) 의 `auto_promote_job` 은 두 503 분기에서 **mint 가 durable 한데도** `activity.record` 를 안 부른다(그 자리 주석에 명시). **반면 `writing/accept` 의 502 partial 은 기록한다** — 두 처리가 다른 이유는 accept 의 partial 이 **정본 version 하나**라 `exc.saved` 로 대상이 특정되고 한 행이 정확한 반면, auto-promote 의 partial 은 envelope 이 열거하는 **목록**이라 행 하나가 담을 수 있는 것이 개수뿐이고 재시도와 대조되지 않기 때문이다. **선택지**: ⓐ 그대로 둔다(현행 — 그 경로는 드물고 envelope 이 이미 무엇이 저장됐는지 말한다) ⓑ 승격된 memory 마다 한 행씩 남긴다(정확하지만 검토 결정 축의 다른 행들과 입자가 달라진다) ⓒ 개수 한 행을 남긴다(지금 성공 경로와 같은 모양). **ⓐ가 현 단계에 맞다고 본다** — 다만 활동 로그를 소비하는 화면이 "전부 있다"를 전제하면 그때 다시 본다.
- **★ [계약에 회귀가 없다, 2026-08-08 뮤테이션이 드러냄] `AUTH_SESSION_TTL_HOURS` 의 "기동 거부"를 지키는 셀이 0건이다.** [`main.py:386-393`](services/application/app/main.py#L386)이 이 env 를 읽어 **`<= 0` 이면 `ValueError` 로 기동을 거부**하고 아니면 `timedelta(hours=…)` 로 세션 TTL 을 만든다. 2026-07-27 work_log 가 *"조용한 fallback 은 무한 세션을 만들 수 있다"*는 **보안 근거까지 달아 계약으로 적었는데**, `grep -rn AUTH_SESSION_TTL_HOURS tests/` = **0건**이다. 실측: `timedelta` import 를 지워도 회귀가 통과했고(그래서 발견됐다), 같은 이유로 **`raise ValueError` 를 지워도 아무것도 안 문다** — 그 순간 잘못된 env 가 조용히 기본 TTL 로 떨어진다. **닫는 비용은 작다**(env 를 세우고 `_default_session_service()` 를 부르는 셀 2개 — 음수/0 거부 + 양수 반영). **import 정리 슬라이스의 범위가 아니라 손대지 않았다.** 인증 쪽을 다음에 만지는 사람이 같이 닫는 것이 자연스럽다.
- **[판단 사안 · 2026-08-08] 미사용 import 에는 회귀 가드가 없다 — 유일한 신호가 스위트 밖 linter 다.** 뮤테이션 M5 실측: 제거한 import 를 되돌려 넣어도 **139 셀 전부 green** 이고 pyflakes F401 만 0 → 1 이 된다. **가드를 안 만든 이유 셋**: ① repo 전체 F401 가드는 **즉시 실패한다** — `main.py` 밖에 기존 3자리가 있다([`indexing/chroma.py:27`](services/application/app/indexing/chroma.py#L27) `MEMORY_VECTOR_COLLECTION` · [`scripts/gateway_generate_live_smoke.py:30`](scripts/gateway_generate_live_smoke.py#L30) `sys` · [`scripts/report_budget_measure.py:45`](scripts/report_budget_measure.py#L45) 3심볼). ② `main.py` 만 0 으로 잠그는 셀은 기준이 자의적이다. ③ pyflakes 를 테스트 의존성으로 들이면 이 저장소가 피해 온 "요청 안 한 인프라"가 된다. **선택지**: ⓐ 그대로 둔다(정리 슬라이스마다 수동 측정 — 지금까지 그래 왔고 실제로 작동했다) ⓑ 기존 3자리를 먼저 닫고 repo 전수 F401=0 을 셀로 잠근다 ⓒ 의존성 없는 AST 셀로 `services/` 만 잠근다. **ⓐ가 현 단계에 맞다고 본다** — 정리가 슬라이스 단위로만 일어나고 그때마다 실측하기 때문이다.
- **[확장 방향 확정·착수 조건 대기] D8-6 D4-D purge operation journal/saga.** 현재는 오너 결정 A대로 core SOT 선삭제 + `purge_reconciler.py` 수습이며, 503 후 UI 재시도는 금지한다. **원격 저장소·다중 worker를 도입하거나 수동 reconciler가 실제 운영 부담이 되는 시점**에 durable operation id와 단계별 상태·재개 worker를 가진 D안으로 확장한다. 그 전에는 core-first 순서나 never-existed 404 의미를 임의로 바꾸지 않는다. 정본 v1.7.82 · 결정 브리프 `docs/plans/auth-d8-6-purge-ui-decisions.md` D4.
- **★ [부하 의존 회귀 플레이크, 2026-08-05 실측 — 제품 결함 아님] 8.3 동시성 셀이 *거절 사유*에서 흔들린다.** [`test_quota_enforcement_live_mongo.py`](tests/test_quota_enforcement_live_mongo.py)의 `test_only_one_of_many_concurrent_requests_takes_the_last_slot`이 **바쁜 머신에서 간헐 실패**한다(2026-08-05 전체 suite 1회 실패 → 단독 3/3 통과 → busy loop 24개 아래서 재현). **흔들리는 것은 셋째 단정뿐이고 `admitted == 1`은 지켜진다** — 즉 **초과 입장은 없다**. 원인은 `AdmissionMutex._acquire`의 예산이 **5회 × 20 ms**([`enforcement.py:74`](services/application/app/quota/enforcement.py#L74))이고 20 스레드가 한 뮤텍스에 몰리면 굶는 스레드가 나오는 것이다. 그때의 `AdmissionUnavailable`은 **SoT v1.7.88이 정한 정상 동작**이다(*"Q4=A 전면 fail-closed … 뮤텍스 획득 실패 포함"*). **그래서 이것은 셀이 계약보다 엄격한 자리다**(over-strict). **회귀를 돌리다 이 셀이 떨어지면 자기 변경 탓으로 오독하지 말 것** — 먼저 단독 재실행하고, 실패 메시지에 `AdmissionUnavailable`이 섞였는지 본다. 섞였으면 부하이고, `admitted`가 2 이상이면 그때가 진짜 결함이다. **권고 수정**(미실시 — 8.3의 검증된 표면이라 별도 판단): 셋째 단정을 `QuotaRefused` **또는** `AdmissionUnavailable` 허용으로 바꾼다. 그래도 뮤텍스를 지우는 뮤테이션은 첫째 단정이 문다.
- **[알고 두는 것, 배포 무영향 — 독립 검증 2026-08-04 H-4] 워커와 앱은 서로 다른 `QuotaEnforcementService` 인스턴스를 쓴다.** 둘 다 `_default_quota_enforcement_service(...)`로 각자 만들며, **배포(Mongo)에서는 같은 컬렉션을 공유하므로 무영향**이다. 갈라지는 것은 **인프라 없는 in-memory 조립뿐**이고 거기서는 원장이 프로세스마다 따로 산다. 수리하지 않은 이유: 인스턴스를 공유시키려면 워커가 앱 객체에 의존해야 해서 경계가 나빠지고, **async 우회 방지의 핵심인 진행 중 job 계수는 원장이 아니라 공유 job 저장소를 직접 조회**하므로 정확도가 걸린 자리가 아니다. 원격 배포로 워커를 다중화할 때 다시 볼 항목이며, 그 트리거는 D4-D·저장소 교체와 같다.
- **★ [결정 완료 2026-08-05 · Slice 1 진행 중 — auth·admin 2 도메인 완료·독립 검증 합격] `main.py` 라우터 정리 + 관리자 표면을 다른 주소로.** 오너 문언: *"이건 기본이잖아."* **결정 = R1(register 함수)·A1(ⓑ 별도 compose 서비스)** — 브리프 [`plans/router-split-and-admin-separation-decisions.md`](docs/plans/router-split-and-admin-separation-decisions.md). **의도적으로 미뤘던 것이지 놓친 것이 아니다** — 지금까지의 경계는 파일 배치가 아니라 **dependency tier + 전수 가드**로 세웠고, 그 가드가 선 채로 파일을 쪼갠다.
  - **지금 어디까지 왔나**(`e8b9908`): **12 operation이 나갔다** — [`app/routers/auth.py`](services/application/app/routers/auth.py)(`register_auth`, 4 op) · [`app/routers/admin.py`](services/application/app/routers/admin.py)(`register_admin`, 8 op). **나머지 64 op는 여전히 `main.py`**였다(2026-08-07 에 5 op 가 더 나가 지금은 59). 독립 검증 **합격**([기록](docs/verifications/2026-08-05/router_split_slice1_auth_admin.md)) — route 76 무변 · OpenAPI 바이트 동일 · dependant 트리 76/76 동일 · 전수 suite 기준선 무변.
  - **★ 남은 도메인 재검증은 스크립트 하나로 끝난다**: [`repro_router_split.py`](docs/verifications/2026-08-05/repro_router_split.py)를 분해 전/후 트리(`git worktree`)에서 돌려 지문을 `diff` 한다 — 비면 행위 무변이다. **★ 함정 — 이 스크립트를 `> out.json 2>&1` 로 받지 말 것**(2026-08-08 실측): JSON 지문은 **stdout**, 사람용 요약표 76행은 **stderr** 로 나가므로 합치면 파일이 27,652 → 35,318 바이트가 되고 **그 파일의 sha256 은 지문의 sha256 이 아니다**. `diff` 비교는 양쪽 다 같은 방식이면 여전히 유효하지만(오히려 route→모듈 표까지 비교해 더 엄격해진다) **해시를 인용하면 아무도 재현하지 못한다** — 실제로 독립 검증이 8종을 시도해 전부 불일치했다. **인용 관례는 `openapi_sha256=f8b42ef1… + "diff empty"`**이고 1~4차 기록이 전부 그 형태다. stdout-only 지문 전체의 sha256 은 `c3dfb391…`. **단 순서 성질은 자동 보존이 아니다**: 이동한 route는 `register_*()` 호출 지점에서 등록되므로 **등록 순서가 바뀐다**. 지금은 literal↔`{param}` 충돌 쌍이 0이라 무해하지만, `{project_id}`·`{draft_id}`를 쓰는 도메인이 옮겨가면 그 실행에서 order-sensitive pairs가 0인지 다시 본다(스크립트가 센다).
  - **실측한 현황**(2026-08-04, 줄 수는 2026-08-05 재측정): `APIRouter` 사용 **0건** — 76 operation이 `@app.*` 데코레이터로 선언된다(분해 전 [`main.py`](services/application/app/main.py) 한 파일 **6,145줄**·데코레이터 77개였다). 관리자 8 · 인증 3 · 관측 1 · 제품 62 · health 1.
  - **비용이 어디 있는가 — 클로저다.** `create_app` 지역 바인딩이 **33개**(`core_sot`·`writing`·`context_search`·`llm_call_audit`·`users`·`sessions`…)이고 **모든 endpoint가 그것을 클로저로 끌어 쓴다**. 그래서 단순히 파일만 옮기면 안 되고, 라우터 모듈마다 `def register(app, *, core_sot, writing, …)` 형태로 협력자를 **명시 인자로 받는 등록 함수**를 두는 모양이 가장 안전하다(DI 컨테이너를 새로 들이지 않는다 — 그건 이 저장소가 피해 온 것이다).
  - **[해소됨 2026-08-05] 가드 2개의 `main.py` 정규식 파싱은 없어졌다.** [`tests/test_billable_actions.py`](tests/test_billable_actions.py)는 **route-driven**으로 전환됐고(`app.routes`를 순회하며 `inspect.getsource(route.endpoint)` — 파일 배치 무관), 독립 검증이 **이동한 파일에 `llm_call_scope(`를 넣는 뮤테이션으로 실제로 무는 것**을 확인했다(2 셀 재실패). 어느 테스트도 이제 `app/main.py`를 파일로 읽지 않는다.
    - **★ 대신 남은 것 = 경로가 하드코딩된 가드 하나.** [`tests/test_auth_api.py:1610`](tests/test_auth_api.py#L1610)의 `_create_user_flags`가 `"services/application/app/routers/admin.py"`를 **문자열 경로로 열어 AST 파싱**한다(`create_user(must_change_password=True)` 호출을 본다). Task 4가 `main.py`→`routers/admin.py`로 올바르게 갱신했다. **남은 4 도메인을 옮길 때 `create_user`처럼 경로로 지목되는 호출부가 또 나오면 이 자리를 함께 고친다** — 안 고치면 `FileNotFoundError`나 `[] != [True]`로 **시끄럽게 실패**하므로 조용히 약해지지는 않는다.
  - **★ [완료 2026-08-09 `5bdaf15`·`878f24d` — 독립 검증 합격(`9ddff6e`)·비차단 3건 처리] Slice 2(ⓑ) = 관리자 표면이 다른 주소로 갔다.** `admin` compose 서비스(이미지 재사용·command 만 변경·**포트 미게시**) + nginx `location /api/admin/` + `create_product_app()`/`create_admin_app()`. **제품 앱에 `/admin` 0건**이라 LAN 직접 hit 은 가드가 아니라 **라우터 404** 다. 공개 표면 무변 — 지문 IDENTICAL(route 76 · order-pairs 0 · openapi sha `f8b42ef1…`). 상세·판단 근거·뮤테이션 7종은 [2026-08-09 work_log](docs/daily_logs/2026-08-09/work_log.md), 계약은 SoT v1.7.91. **선행이던 H-2 는 이 슬라이스가 함께 닫았다**(아래). **★ 아직 아무도 안 본 것 = nginx 홉 하나뿐이다** — 호스트 소켓·`compose config`·`nginx -t` 에 더해, 독립 검증이 **실 application 이미지에 트리를 마운트해 `admin_asgi:app` 기동까지 입증**했다(`/admin/users` 401). 남은 것은 `frontend` → `admin` 컨테이너 한 홉이고, 스택을 세우는 첫 사람이 `curl :5520/api/admin/users` 가 **401**(404 가 아니라)인지로 닫는다. 404 면 nginx location 이나 rewrite 를 본다. **재빌드가 선행된다** — 현재 이미지에 `admin_asgi.py` 가 없다(아래 ★★ 이미지 부채).
  - **[폐쇄 2026-08-09] H-2(shim drift 가드) — 감시가 아니라 구조로 닫았다.** 요구는 *"`create_app()` shim 이 진짜 배포 앱과 갈라지는 것을 막는 가드"* 였다. **세 factory 를 한 함수 본문**(`include_product`·`include_admin`)으로 두어 갈라질 코드 자체를 없애고, 그 위에 [`test_admin_surface_separation.py`](tests/test_admin_surface_separation.py) 가 성질을 단정한다 — 합집합 = 제품 ∪ 관리자 · 교집합 = `/health` · **operation 별 가드·상태코드·에러 선언·route 클래스 동일**. 뮤테이션 M6(**배포 관리자 앱에만** route 추가)이 정확히 H-2 형 drift 이고 3 셀이 문다. **앞으로 알아야 하는 것**: 배포 앱을 위해 조립 코드를 따로 만들면 그 순간 H-2 가 되돌아온다.
  - **관리자 주소 분리 — ⓑ 별도 compose 서비스로 확정(2026-08-05, 시행 완료 2026-08-09)**: ⓐ 같은 이미지·2차 ASGI(루프백 바인딩 트릭은 공짜지만 2차 앱·uvicorn 기동·프론트 재라우팅은 전부 신규) ⓑ **별도 compose 서비스(선택 — 이미지 재사용·포트 미게시·nginx 경유, 기존 worker/generation_worker 패턴)** ⓒ nginx only(현 토폴로지에선 분리 안 됨) ⓓ 완전 분리(범위 밖).
  - **★ Slice 2(ⓑ) 착수 전 선행이던 두 항목은 **둘 다 닫혔다**(H-3 `59fe1a1` · H-2 `5bdaf15`).**
    - **[해소됨 2026-08-05 `59fe1a1`] H-3 import 경로.** `main.py`의 routers import가 **상대 경로**(`from .routers.admin import …`)로 바뀌어 앱이 **두 이름 모두**로 로드된다. 종전에는 절대(main) ↔ 상대(routers) 혼합 때문에 짧은 이름(`PYTHONPATH=services/application` + `import app.main`)이 **하드 `ImportError`**였다(분해 전에는 살아 있던 경로). **앞으로 알아야 하는 것**: 이 자리를 절대 경로로 되돌리면 [`tests/test_app_import_paths.py`](tests/test_app_import_paths.py)가 문다(뮤테이션 실측 — 짧은 이름 셀 fail·FQ 셀 pass). 그 가드는 **서브프로세스**로 도는데, 한 프로세스에서 두 이름을 import 하면 모듈 사본이 둘 생겨 다른 테스트의 `patch("services.application.app.main…")`가 엉뚱한 사본을 건드리기 때문이다.
    - **[해소됨 2026-08-09 `5bdaf15`] H-2(shim drift 가드).** 원 지적: `create_app()`을 테스트·스크립트용 shim 으로 남기고 배포용을 나누면 **shim 이 진짜 배포 앱과 갈라지는 것을 막는 가드가 함께 가야 한다** — 이 저장소가 `ObservedProvider` 계측 누락으로 데인 **fake green·배포에서만 드러남**과 동형이기 때문이다. 처방과 실측은 위 폐쇄 항목에 있다.
- **★ [폐쇄 2026-08-06 `2f20fbb`·`2d68630`] H-3-A = 공유 prelude 추출로 순환을 없앴다.** 브리프 [`plans/router-split-shared-prelude-decisions.md`](docs/plans/router-split-shared-prelude-decisions.md)(§9 = 구현 결과). **독립 검증 합격**([`shared_prelude_extraction.md`](docs/verifications/2026-08-06/shared_prelude_extraction.md), 2026-08-06) — 행위 무변(지문 IDENTICAL · `f8b42ef1…`) · 순환 폐쇄(죽던 로드 경로 3종 부활) · 뮤테이션 3종 양방향 물림 · 회귀 `2196/1/1933/0` 재현(1131s).
  - **무엇이 생겼나**: [`app/api/models.py`](services/application/app/api/models.py)(78) · [`errors.py`](services/application/app/api/errors.py)(38) · [`dependencies.py`](services/application/app/api/dependencies.py)(17) · [`app/env.py`](services/application/app/env.py)(2). **의존 방향은 단방향 `errors → models → env`이고 `dependencies`는 독립**이다 — 이 방향이 뒤집히면 없앤 순환이 형태만 바꿔 돌아온다(AST로 양방향 쌍 0 확인).
  - **★ `app/env.py`가 `api/` 밖인 이유**: `_env_int`·`_env_bool`은 **API 계약이 아니고**, 이동분(19회)과 `main.py` 잔류 조립부(7회)가 **둘 다** 쓴다. 어느 한쪽에 넣으면 다른 쪽이 그것을 import 하며 방향이 뒤집힌다. 브리프 §5의 "3분할"에 대한 의도된 보완이다.
  - **결과**: `main.py` **5,843 → 4,808줄**(구현자 초판이 4,806이라 보고했는데 그것은 **주석 재작성 전 생성기 중간 출력**이다 — 독립 검증이 잡았다), `routers/*`의 `from ..main` **0건**. 행위 무변 — `repro_router_split.py` 지문 **IDENTICAL**(route 76 · order-pairs 0 · openapi sha `f8b42ef1…`). 죽어 있던 로드 경로 4종(라우터 먼저 · `python -m` · 짧은 이름 · uvicorn)이 전부 살아났다.
  - **★ 앞으로 이 코드를 만질 사람이 알아야 하는 것 셋**: ① **`routers/*`에 `from ..main import`를 되살리면 안 된다** — 그것이 순환이었고 [`test_app_import_paths.py`](tests/test_app_import_paths.py)의 `test_a_router_module_loads_before_main`이 문다. ② **`main.py`에 재수출 shim을 두지 않은 것은 의도다** — 두면 핸들러는 새 모듈을 보는데 테스트는 `main`을 patch해서 **조용히 빗나간다**(H-2 shim drift와 같은 형태). 심볼이 옮겨가면 import를 따라 옮긴다. ③ **`main.py`의 routers import는 상대 경로를 유지한다** — 이제 이유가 순환이 아니라 **모듈 동일성**이다(절대면 짧은 이름 로드에서 라우터가 FQ 트리에 실려 사본이 둘 생긴다).
  - **★ 가드가 잠그는 성질이 바뀌었다(뮤테이션 실측)**: 추출 뒤 기존 두 셀(`test_the_*_name_loads`)은 **상대/절대 import를 더 이상 가르지 못한다** — 절대로 되돌려도 4 cells 전부 통과했다. 순환이 있을 때만 이름 혼용이 치명적이었기 때문이다. 그 자리는 신설 `test_the_short_name_load_keeps_the_routers_in_one_tree`가 대신하며 절대 복귀 시 **그 셀만** 실패한다(1 failed / 4 passed). **"가드는 통과하는데 이유가 낡은" 형태를 뮤테이션이 드러낸 사례**다.
  - **함정 — 이동 심볼의 import를 옮길 때 `as` 별칭을 버리지 말 것.** 전수 회귀 9 subtest 실패가 이것이었다(`require_project_owner as owner` → 별칭 소실 → `NameError`). 추출 자체가 아니라 **import 재작성**의 실수였고, 실패한 셀은 HANDOFF가 예고한 그 자리다(`BillableRouteWiringTest::test_enforcement_is_declared_after_ownership` — 순서가 곧 계약이라 요청 구동으로는 안 보이는 셀).
  - **다음**: 이 위에서 **잔여 도메인 이동이 기계적**이 되고 **Slice 2(`create_admin_app()`)가 열린다** — 이제 `routers.admin`을 그냥 import 할 수 있고 제품 앱을 짓지 않는다.
- **★ [2026-08-07 · 1차 `b6eec79`·`925a321` 독립 검증 합격(`06e7440`)·비차단 2건 폐쇄 / 2차 `131bc2a` 독립 검증 대기] 라우터 분해 잔여 도메인 = 30 operation 이 더 나갔다(12 → 42, **잔여 34**).** 1차(5 op) = `health`·`memory`·`observability`·`context_search`, 2차(25 op) = `projects`(11)·`drafts`(10)·`source_refs`(4). **잔여는 `analysis`(21)·`writing`(13) 둘뿐**이다. `main.py` **4,808 → 3,924줄**(1차 −274 · 2차 −610). 행위 무변은 `repro_router_split.py` 지문 **IDENTICAL**(route 76 · order-pairs 0 · openapi sha `f8b42ef1…`)이고 **order-pairs 0 은 이번에 실제 검사였다** — `{project_id}` 를 쓰는 route 가 register 로 나간 것이 처음이다.
  - **★ 잔여 2 도메인(`analysis` 21 · `writing` 13)을 옮길 사람에게 — 결합도는 이미 재 뒀다.** AST 실측 결과 **둘의 유일한 공유 직렬화기는 `_analysis_job_payload` 하나**다(그것만 `api/payloads.py` 로 내리면 된다). `analysis` 는 직렬화기가 아닌 헬퍼(`_transition_gate_finding`·`_review_source_pointer`)도 끌고 있어 1·2차보다 손이 더 간다. **★ 함정 — route 줄 범위로 헬퍼 소속을 세면 안 된다**: route 사이에 헬퍼 정의가 끼어 있어서 `_writing_*_payload` 가 analysis 와 "공유"인 것처럼 나온다(실측 허위). handler 본문만 AST 로 봐야 맞는다.
  - **★ 이동할 사람이 따를 패턴 셋(1차가 정했다)**: ① **공유 직렬화기는 [`api/payloads.py`](services/application/app/api/payloads.py), 한 도메인 전용은 그 라우터 모듈.** `routers/*` 가 `from ..main import` 를 못 쓰므로 **이동분과 잔류분이 둘 다 쓰는 직렬화기는 `api/` 로 내려가는 것이 강제**다(`env.py` 를 `api/` 밖에 둔 것과 같은 형태의 문제). 반대로 전부 모으면 `payloads.py` 가 두 번째 `main.py` 가 된다 — `_context_*_payload` 를 안 내린 이유가 그것이다. ② **`_require_project_exists` 는 새로 쓰지 말고 [`project_existence_check(core_sot)`](services/application/app/api/dependencies.py)** 를 부른다(사본 8벌이 될 자리를 하나로 합쳤다). ③ **각 이동 후 지문 diff.** ④ **`writing` 이 가장 크다** — `_writing_*_payload` 9종·유료 5경로이고, 거기서 `_draft_payload`·`_analysis_job_payload` 가 다시 공유 대상으로 올라온다.
  - **★ 가드가 또 한 번 "통과하는데 범위가 낡은" 형태였다(뮤테이션 실측).** [`test_app_import_paths.py`](tests/test_app_import_paths.py)의 `test_a_router_module_loads_before_main` 이 `routers.admin`·`auth` **두 개만 하드코딩**하고 있어, 늘어난 4개 모듈은 자기 이름으로 검사되지 않았다(`routers/memory.py` 에 `from ..main import` 를 되살렸을 때 다른 4 셀은 물었는데 **이 셀만 통과**). 목록을 `routers/*.py` 글롭으로 바꿨다(subtest 2 → 6) — **잔여 도메인은 자동으로 범위에 든다.**
  - **[폐쇄 2026-08-07] `GET …/memory` 응답의 `scope` 는 이제 그 자리에서 단정된다.** 종전에는 `api/payloads.py::_scope_payload` 에서 `scope_id` 를 빼도 `test_memory_api.py` 가 **전부 통과**하고 `test_analysis_compare_api.py` 한 셀만 물었다 — 즉 memory 읽기 응답의 형태가 analysis 도메인 셀에 **전이적으로만** 걸려 있었다(이 슬라이스 이전의 부채이며, 독립 검증의 유일한 비차단 지적이었다). `MemoryReadScopeSerializationTest` **3 cells** 로 닫았다. **★ 계약 근거는 정본 v1.6.42** — *"`MemoryEntry` 에 `scope` 필드 추가 … Mongo round-trip·**`_memory_payload` 포함**"* 이라고 직렬화기를 이름으로 지목한다(검증자가 "별도 계약 확인 필요"로 남긴 자리이고, 확인한 결과 계약이 있었다). **셀을 고칠 사람이 알아야 하는 것**: `event_observation`·`open_question_observation` 의 `scope: null` 은 결측이 아니라 **계약된 값**이다(`derive_scope` — 엔티티 id 가 없다). "비었으니 채우자"는 과잉 교정을 셋째 셀이 문다.
- **★ [2026-08-07 · 3차 `70584c2` 독립 검증 대기] analysis 21 op 가 더 나갔다(42 → 63, 잔여 13 = `writing` 만).** `_analysis_job_payload` 하나를 [`api/payloads.py`](services/application/app/api/payloads.py) 로 내렸다(analysis↔writing 공유 — 위 177 의 실측대로). 직렬화기 13종·헬퍼 1종(`_transition_gate_finding`)은 analysis 전용이라 [`routers/analysis.py`](services/application/app/routers/analysis.py). `main.py` **3,924 → 3,106줄**. 행위 무변 — `repro_router_split.py` 지문 IDENTICAL · byte 36/36 · 전수 `2200/1/2165`(셀 무변, subtest +1 은 글롭 routers 9→10). 뮤테이션 N1~N5 전부 작동(**N4 가 BILLABLE 9개 전수로 analysis run/compare 까지 잡는다** — 의존성 순서 가드가 이동해도 자리를 안 잃는다). **잔여는 `writing`(13) 하나** — partial envelope 5곳·유료 6경로·헬퍼/상수 다수라 analysis 보다 손이 간다. **★ `_require_project_exists` 는 main.py 에 남겨 뒀다** — analysis 라우터는 자체 `project_existence_check(core_sot)` 를 쓰지만 writing 잔류(13 op)가 main.py 의 같은 클로저를 14곳에서 쓰므로(이동 후 실측 — 정의 1 + writing 호출 13; 종전 '33'은 이동 전 전체 grep), analysis 블록과 함께 지우면 writing 이 깨진다. writing 이 나가는 4차에서 같이 정리한다(출발값 = 14).
- **★ [2026-08-07 · 4차 `c289bce` 독립 검증 합격(`5e19867`)·비차단 2건 폐쇄(`d65a1c9`) — Slice 1 종료] writing 13 op 가 나갔다(63 → 76, **잔여 0**).** **★ 모자 증명**: HEAD ≡ `9bc06e3`(Slice 1 착수 전) 지문 IDENTICAL — 모놀리스 `main.py`(76 op 인라인) → `routers/` 11 모듈의 4-슬라이스 리팩터 전체가 원래 공개 표면과 같음이 한 diff 로 증명됐다. partial envelope 5곳(revise-and-gate 4 + accept 1)·유료 6경로를 byte-동일로 옮겼다. **★ import 문을 AST 로 자동 생성했다** — handler 13개가 참조하는 심볼이 80+개라 heredoc 대신 `ast.walk` 로 모아 기계 생성. **asname 함정 2종**(`InvalidJobStateTransition as InvalidGenerationJobStateTransition`·`TEMPLATE as REPORT_SYSTEM_TEMPLATE`)을 이 과정에서 놓쳤다가 `pyflakes` F821 로 보완했다. **`_require_project_exists` 정의(main.py)를 삭제** — writing 이 마지막 사용자였고 이제 각 라우터가 자체 `project_existence_check(core_sot)`. `main.py` 3,106 → 1,860줄(조립 코드만). 행위 무변 — 지문 IDENTICAL · byte 24/24 · 전수 `2200/1/2167`(subtest +1 은 글롭 routers 10→11). 뮤테이션 N1~N5(**N3 partial envelope 502→500 · N4 유료 순서** 가 검증자의 추가 의심축). **★ Slice 1(제품 라우터 정리)이 끝났다** — 76 op 전부가 `routers/` 11 모듈로.
- **★ [2026-08-08 `3ff4274` · 독립 검증 합격(`b9fca77`)·비차단 1건 폐쇄] 이동이 남긴 미사용 import 21개를 정리했다 — 이것으로 Slice 1 뒷정리도 끝이다.** pyflakes F401 **21 → 0**(F821 0), `main.py` **1,860 → 1,840줄**, 변경 파일 **`main.py` 하나**. 행위 무변 — 지문 **`diff` 출력 없음(pre `d65a1c9` ≡ post)** · route 76 · order-pairs 0 · openapi sha `f8b42ef1…`. **★ 다음 작업자가 알아야 하는 것 셋**: ① **이 정리에는 회귀 가드가 없다**(뮤테이션 M5 실측 — 미사용 import 를 되돌려 넣어도 139 셀 전부 green). 유일한 신호가 pyflakes 인데 **스위트 밖 도구**다. ② **`main.X` 를 patch 하는 자리는 저장소 전체에 셋뿐**(`connect_chroma_collection`·`_build_embedding_provider`·`GatewayGenerateProvider`)이고 이번 21개에 없었다 — 앞으로 심볼을 지울 때 이 스윕을 먼저 한다. **테스트가 patch 하는 심볼이 main 에서 미사용이 되면 그 patch 는 이미 조용히 빗나가고 있다**(제거하면 `AttributeError` 로 시끄러워지는데 그게 개선이다). ③ **`_default_model_capabilities`·`_report_output_cap`·`seed_*` 는 지울 것이 없었다** — 셋 다 살아 있는 조립 코드이고 첫째는 [`test_writing.py:810`·`:871`](tests/test_writing.py#L810)이 직접 patch 한다. "조립 전용 잔류 심볼" 항목은 **확인 결과 대상 없음**으로 닫힌다.
- **★ [폐쇄 2026-08-06 `abcface`] 검증 판정 분포 = 그 기록의 *최종* 판정이다.** 오너 결정: *"테스트의 목적은 조건이 닫히는 거니까."* 조건부로 나갔다가 조건이 닫혀 승격된 기록은 **그 기록 자신의 최종 문구**를 따른다. 발행 시점 판정이 섞여 있던 **17건을 본문과 대조해 정렬**했다.
  - **분포가 이렇게 움직였다**: 합격 150→**154** · 조건부 57→**66** · 불합격 1→**2** · 서술형 14→**0**. **조건부 비율 26% → 30%**(최상위 README 의 "절차가 형식이 아니라는 증거" 문장이 이 숫자를 인용한다).
  - **★ 불합격이 하나 묻혀 있었다** — [`2026-07-29/slice1b_context_window_output_cap_reaudit.md`](docs/verifications/2026-07-29/slice1b_context_window_output_cap_reaudit.md) 는 `## Verdict — **FAIL**`(이전 PASS 를 폐기한 재감사)인데 인덱스가 `—` 였다. 이를 뒤집는 후속 기록은 없다. **"불합격 1건"이라 적던 종전 주장은 틀렸다.**
  - **★ 분류를 정규식에 맡기면 안 된다(실측)**: 판정 문구가 영·한 혼용에 형식도 제각각이다(`PASS` · `Conditional pass` · `조건부 승인` · `합격(조건부)` · 한 기록 안에 커밋별로 두 판정). 규칙을 바꿔 두 번 시도했더니 각각 **5건·4건을 오분류**했고 두 번째는 `불합격`을 4건으로 읽었다(정본 2건). **분류는 사람이 하고 가드는 구조만 잠근다** — 그것이 아래 셀의 설계다.
  - **가드**: [`test_docs_indexes.py`](tests/test_docs_indexes.py) `VerificationsIndexTest::test_every_record_row_states_a_verdict` 가 **222 기록 전수**로 ① 행이 3열인가 ② 판정 열이 세 값 중 하나인가를 본다. 이번에 드러난 두 결함(`—` 등재 14건 · 파이프 누락으로 판정이 설명 칸에 먹힌 행 1건)을 **함께** 막는다. 뮤테이션 2종이 각각 해당 기록의 subtest 만 물었다.
  - **★ 새 기록을 쓸 때 — 판정 어휘가 가이드에서 고정됐다**(2026-08-06). [`guides/verification.md`](docs/guides/verification.md) §Required sections 가 **`합격` · `조건부 합격` · `불합격` 셋뿐**임을 명시하고 **첫 줄 형식**까지 규정한다(조건·위반은 같은 줄에 적는다). 종전에는 그 자리가 `pass / conditional pass / fail` 이라 한·영 두 어휘가 모두 승인돼 **222건이 111가지 표현**을 낳았다. 인덱스 판정 열도 같은 세 값이다.
  - **`서술형` 은 폐기됐다**(2026-08-06). 정의(*"초기 기록"*)에 해당하는 기록이 **222 전수 확인 결과 0건**이었고 가이드에 정의된 적도 없다 — 인덱스 표에만 있던 칸이 `—` 14건을 흡수하고 있었다. 분포는 이제 3종이다.
  - **소급 정리는 하지 않는다**(오너 2026-08-06). 기존 222건 본문의 111가지 표현은 그대로 두고 **신규 기록부터** 적용한다 — 인덱스 판정 열이 정본이고 이미 가드로 잠겨 있어 얻는 것이 미학뿐이다(`docs/plans/` 재편을 미룬 것과 같은 계산).
- **[알고 두는 것 · 2026-08-06] 숫자 주장 중 가드가 원리적으로 못 덮는 자리가 하나 있다 — 회귀 기준선.** 최상위 [`README.md`](README.md) 절차 표 ②의 `N passed / N subtests`는 **전수 suite를 돌려야 알 수 있어** 가드를 붙일 수 없다. 실제로 `2,170`에 얼어 있던 것을 2026-08-06 보강 패스가 `2,193`으로 정렬했다(건수·분포와 같은 병, 처방만 다르다). **기준선을 갱신하는 슬라이스는 이 한 줄도 함께 고친다** — 아무 테스트도 잡아 주지 않는다.
- **[미구현이나 구현 결정됨, 포트폴리오 정확성 주의] cross-encoder(뉴럴) 리랭커는 아직 없다.** 현재 RAG 리랭킹은 **RRF(Reciprocal Rank Fusion) 융합만**이다 — 벡터(Chroma/BGE-m3-ko) + lexical(ES/nori) 두 랭킹을 `1/(k+rank)`(k=60)로 합쳐 재정렬([`context_search/service.py:279`](services/application/app/context_search/service.py#L279)·[`:495`](services/application/app/context_search/service.py#L495), env `vector/lexical/hybrid`). query-document 쌍을 신경망으로 재채점하는 cross-encoder 리랭커는 2026-07-05에 유예됐다가([`plans/04-real-vector-backend-decisions.md:11`](docs/plans/04-real-vector-backend-decisions.md#L11)) **2026-07-27 구현하기로 결정**됐다(`plans/external-api-expansion-decisions.md` D5): 로컬은 self-host **`dragonkue/bge-reranker-v2-m3-ko`**(임베딩 서비스와 같은 패턴), 외부 리랭커 API도 붙일 수 있게 `RerankProvider` seam을 함께 뚫는다. **별도 슬라이스**(인증·외부 API LLM/임베딩 슬라이스 뒤). **삽입 자리**: RRF 융합 결과 뒤, `retrieve()` seam 다음. **포트폴리오/README 표기**: 현재 상태는 "RRF 하이브리드 융합 리랭킹 있음, 뉴럴 cross-encoder 리랭커는 미구현(구현 예정)"이 정확 — 코드가 붙기 전까지 "있음"으로 쓰면 거짓이다.
- **[미래 확장, 지금 범위 아님] 공유·협업 글쓰기.** 오너(2026-07-27)가 "생각 안 해봤다 — 나중에 한 번 생각해보자"며 유예. 다중 사용자 소유권은 **D3=A(`Project.owner_id` 한 필드 = 격리, 1 project 1 owner)**로 가되, 공유/협업(권한 등급·workspace·`members[]`)은 미래 확장이다. D3=A가 그 문을 **닫지 않게** 설계돼 있다(`owner_id`는 나중에 `members[]` 첫 원소나 workspace 소유로 승격 가능 — `plans/multi-user-auth-cms-decisions.md` D3). 착수 시점 아님.
- **[해소됨 2026-08-01] 앱 route를 치는 스크립트 9종에 세션 로그인이 붙었다.** 공용 코드는 [`scripts/script_auth.py`](scripts/script_auth.py) 한 곳이고 종류가 둘이다 — **운영자용 8종**은 `--username <계정>` + **`APPLICATION_PASSWORD` env**(비밀번호 argv 금지 — `create_user.py` 선례, shell history·`ps` 노출), **자기 스택을 소유하는 in-process smoke 1종**(`phase2a_provider_live_smoke`)은 **일회용 계정을 스스로 발급**해 로그인한다(빌릴 운영자 계정도, 실행 뒤 남는 것도 없다). **자격증명을 안 주면 종전대로 익명 진행.**
  - **★ 세션을 실을 때 지켜야 하는 것**: 로그인 응답의 쿠키를 **httpx jar의 자동 왕복에 맡기면 안 된다** — 세션 쿠키는 `Secure`라 plain http(`application:8000`·ASGI `application-smoke`)에서 조용히 안 실린다(뮤테이션 실측 `cookie: None`). 지금 구현은 **명시 `Cookie` 헤더**지만 **헤더가 유일한 정답은 아니다** — `client.cookies.set(...)`도 동작한다(손으로 넣은 쿠키엔 Secure가 없다, 실측). 회귀는 그래서 *행동*만 잠근다. **헤더 전용으로 조이지 말 것**(정상 구현을 깨는 과잉 교정).
  - **가드는 두 겹이다**(`tests/test_script_login.py::ScriptLoginWiringCoverageTest`): ① 레지스터의 9종이 실제로 로그인하는가 ② **`scripts/*.py`를 읽어 앱 route 마커(`"/projects`·`\bseed_context\b`)가 있는데 레지스터에 없는 스크립트가 있는가**. ②가 없던 첫 판에서 **`phase2a_provider_live_smoke`를 놓친 채 "8종 전부"라고 보고했다**(스윕이 `application:8000` 문자열만 봤고 그 스크립트는 ASGI 가상 호스트를 쓴다). **새 스크립트는 레지스터에 넣는다 — 안 넣으면 ②가 실패한다**(이제 규칙이 아니라 강제다).
  - 진단 2종(`diagnose_writing_report`·`diagnose_writing_gate`)은 **`--current-position`을 주면 시드를 건너뛰므로 로그인도 필요 없다**(로그인이 필요한 것은 시드 write뿐).
  - 라이브 확인: 실 앱 관통으로 무자격 401 → 로그인 후 통과(phase3a exit 0, phase2a 인증 write 6건 200), in-process smoke는 죽은 llama를 가리켜 인증 통과 후 `final_job=failed`. **워커는 여전히 무관**(HTTP를 안 쓰고 Mongo 직접 — D8-7).
  - **첫 관리자 계정만** 부트스트랩 스크립트로 만든다. 그 뒤 계정 생성·확인은 `/admin` 화면에서 한다.
- **[해소됨 2026-08-02] 기획 축 "제품 한 장 요약" = [`docs/product-overview.md`](docs/product-overview.md).** 제품 컨셉·MVP 범위·핵심 위험이 `abstract.md`(48KB) 안에만 있던 공백을 닫았다. **원본 요약이 아니라 코드 실측 기준으로 다시 썼고**, 초안(2026-06)에서 달라진 5건을 §5에 따로 모았다 — ① 단일 사용자 → 다중 사용자 ② 추출 5종 → **관찰 3종**(장소·관계 미착수, 떡밥은 `open_question_observation`) ③ Gate 4종 분리 → **Writing Gate 하나**(지적 4유형·판정 5종, `style`은 판정을 안 바꾼다) ④ 문체는 학습이 아니라 **선언**(프로젝트 브리프 4필드) ⑤ 관측이 제품 기능으로 추가. **★ 이 다섯이 "초안만 읽으면 틀리는 지점"이다** — 포트폴리오·설명 자료를 쓸 때 abstract를 그대로 인용하면 거짓이 된다. MVP 4는 **승인 UI만 섰다**(Review Inbox 병합/분할까지 배선, 인물카드·타임라인·관계 그래프 화면은 0건). README 기획 축의 시작점이 이 문서로 바뀌었다(abstract는 "원본"으로 유지).
- **★ [권위 소스의 낡은 단언, 3건 미정정] 위 문서를 쓰며 패턴 스윕으로 찾았고, 고칠 권한이 없거나 결정 사안이라 남겼다.** (a) **정본 `system-contract-sot.md` §"현재 구현 상태"(L696~710)가 자기 자신과 모순된다** — L696이 "Review Inbox v1.6.67 구현"·L709가 "Writing 작업공간 구현 완료"라고 적으면서 **L710이 `Phase 2~6 UI | 미구현`이라 단언**한다. **(2026-08-02 독립 검증이 정밀화한 지점: 종전 이 줄은 L710을 그냥 "거짓"이라 적었으나 과했다** — Phase 2/3/4 UI 중 **인물카드·타임라인·관계 그래프는 실제로 미구현**이라 그 행은 *부분적으로 성립*한다. 진짜 결함은 거짓이 아니라 **같은 표 안의 모순**이다.) v1.6.x 이후 갱신이 끊긴 표이며 **현재 상태의 실제 권위 소스는 변경이력과 코드**다. 정정은 버전 개정 사안(v1.7.77): ⓐ 갱신 ⓑ **"이력 표이며 현재 상태는 변경이력을 본다"로 성격 명시(추천 — 갱신하면 같은 부채가 세 번째로 쌓인다)** ⓒ 걷어내기. (b) `observability-kpi-rationale.md` §6 로드맵 4항목 중 **2항목이 이미 완료**(호출부 계측 확대·집계/대시보드) — 기획 톤 문서라 표현 손질이 논조 변경이고 포트폴리오 정문이라 오너 취향 사안. (c) `plans/00-foundations.md` "전역 착수 전 결정사항" 체크박스 미갱신(에러 envelope=H3·삭제 정책=D5=A·monorepo 경계는 확정됨) — 거짓 단언이 아니라 미갱신이라 급하지 않다. **정정된 5건은 그날 work_log 표에 있다**(그중 `plans/README.md`가 가리키던 `tests/test_plans_index.py`는 **존재하지 않는 파일**이었다 — 인덱스 가드는 `.md` 링크만 보므로 코드 파일명 참조는 안 잡힌다).
- **[문서 부채 — 도달 가능성은 해소됨 2026-08-02, 디렉터리 재편은 여전히 미결정] `docs/plans/`가 크다(89개 문서, 그중 73개가 `*-decisions.md` 브리프).** **가장 아프던 증상은 닫혔다**: 종전 [`plans/README.md`](docs/plans/README.md)는 평평한 번호 목록이라 **90개 중 51개가 미등재**였고 그래서 오너가 자기 결정 브리프를 못 찾았다. 이제 **89개 전부가 트랙별 표로 등재**돼 있고, [`tests/test_docs_indexes.py`](tests/test_docs_indexes.py)가 **미등재 문서**와 **깨진 링크**를 양방향으로 막는다 — 새 브리프를 인덱스에 안 넣으면 실패한다(규칙이 아니라 강제). 같은 가드가 **검증 기록**과 **최상위 `README.md` 링크**도 덮고, 2026-08-02부터 **"검증 기록 N건"이라는 숫자 주장 네 곳과 판정 분포 합계까지** 디스크 실측에 묶는다(`VerificationCountClaimsTest`) — 세 문서가 각자 세던 탓에 하루 세 번 갈라졌고 **매번 최상위 README 만 뒤처졌다**. 링크는 강제였는데 숫자는 아니었던 자리다. 접두 체계가 무너져 있어(`00`~`07` 계열 + 접두 없는 최근 것들) 인덱스는 **접두가 아니라 트랙**으로 묶었다.
  - **★ 남은 미결정 = 디렉터리 재편이고, 2026-08-02에 실측해 보니 "지금 하지 않는다"가 근거 있는 판단이다.** 브리프 경로 인용이 **1,083건 / 253개 파일**이며 그중 **183개가 `docs/verifications/`의 과거 검증 기록**이다 — 즉 파일을 옮기면 **지나간 검증 기록 본문을 대량으로 고쳐 쓰는 일**이 되고, 그 작업이 평가자·작업자 어느 쪽에도 주는 가치는 사실상 0이다(브리프가 `plans/`에 있든 `plans/decisions/`에 있든 달라지는 것이 없다). **아프던 증상(도달 불가)은 인덱스로 이미 닫혔으므로 재편의 남은 동기는 미학뿐이다.** 열려는 사람은 이 비용을 알고 열 것.
  - **주의: 브리프는 오너 결정의 근거 기록이라 삭제·병합하면 "왜 그렇게 정했는가"가 사라진다** — 이동·인덱싱은 되지만 통폐합은 결정 이력 손실이므로 별도 판단이다.
- **[수리됨 2026-07-30] `long` report의 창 초과는 R-e로 없어지고, 남는 초과는 가드가 400으로 거부한다.** 2026-07-29에는 `writing_report`가 4/4 `provider_error`(400 하드 거부)로 죽었고 원인은 항목마다 붙는 포인터 JSON이었다(report 컨텍스트의 79%). **R-e**(SoT v1.7.61)가 프롬프트를 11,841 → 3,216 tok으로 줄여 헤드룸을 −1,665 → +7,024로 바꿨고, **K-3 가드**(v1.7.62)가 그래도 넘는 요청을 **모델을 부르기 전에** 400으로 거부한다(왕복 0회). 창 8192에서 `3,216 + 6,144 = 9,360 > 8,192`인 경우가 그 자리다 — 종전에는 이것이 400이 아니라 **조용한 잘림**이었다. 알파에서 `LLAMA_CTX_SIZE=16384`를 넣는 이유는 이제 "안 죽게 하려고"가 아니라 **거부당하지 않으려고**다(위 ★ 함정 유지).
- **[프론트 UX 부채, 미수리] 결정적 `provider_error`에도 프론트가 "다시 시도"를 준다.** 문구는 "생성 모델 호출에 실패했습니다."([`GenerationPad.tsx:20`](frontend/src/writing/GenerationPad.tsx#L20))다. 게이트웨이는 4xx를 `retryable=False`로 분류하지만 **그 정보가 화면까지 오지 않아** 재시도가 반드시 같은 실패로 끝나는 경우에도 버튼이 그대로 있다. 종전에 이 항목을 눈에 띄게 만든 원인(`long` report 400)은 R-e로 사라졌지만 **UX 부채 자체는 남는다**(창을 넘기는 다른 입력·모델 4xx 전반).
- **[관측·분류의 잔존 한계, 2026-08-03 독립 검증 H1] "provider를 부르는 route는 반드시 분류된다"는 강제되지 않는다 — 강제되는 것은 *scope를 여는* route까지다.** [`ObservedProvider.generate`](services/application/app/observability/llm_call_scope.py#L209)는 scope가 없으면 호출을 **미기록 통과**시킨다(worker 진입점·script를 위해 계약이 그렇게 정했다 — 추측 `project_id`로 오염시키지 않으려는 것). 따라서 미래에 **provider를 부르되 `llm_call_scope`를 안 여는 route**가 열리면 ① 관측에 안 남고 ② `llm_call_scope(` 문자열이 없어 Slice 8.0 분류 가드에도 안 잡혀 **무료로 조용히 빠진다**. **지금은 안전하다** — 유료 9경로 전부가 per-endpoint 관측 셀(요청을 구동해 레코드가 남는 것을 단정)에 묶여 있고 그 대응표 자체를 `BillableActionObservabilityCoverageTest`가 잠근다(새 유료 동작을 관측 셀 없이 추가하면 실패). **새 LLM 호출부를 만들 사람이 알아야 하는 것**: 감싸기(`ObservedProvider`)와 scope 개방은 늘 함께 가고, Phase 8 이후로는 **분류표 등재까지 셋이 함께 간다**. 닫으려면 route 본문에서 scope 블록 밖의 provider 호출을 정적 탐지하거나 scope-None 경로를 진단 메트릭으로 노출해야 하는데, 둘 다 별도 판단이라 하지 않았다.
- **[관측 공백] `llm_call_audits`로는 "출력이 잘렸다"를 직접 볼 수 없다.** v1.7.59가 `prompt_tokens`·`completion_tokens`·`context_window`·`max_output_tokens`를 추가해 헤드룸은 계산할 수 있지만 **`finish_reason`·`truncated`는 여전히 없다**. 따라서 상한 기준 초과는 보이되 실제 잘림 여부는 `parse_error`로만 간접 관측된다.
- **[알파 작업을 막는 것, 미결정] `docker-compose.llama.yml`의 `-hf`가 리비전을 고정하지 않는다.** `-hf …:Q4_0`은 `main`을 따라가는데 업스트림 `refs/main`이 `29d0977…`로 이동해, 캐시에 온전한 6.5 GB 모델이 있어도 **새 리비전을 다시 받는다**(위 함정 절에 회피법). 고정하려면 `-hf`에 리비전을 붙이거나 `-m`으로 캐시 경로를 직접 쓰면 되지만, **모델 리비전을 최신으로 올릴지 현행을 고정할지가 먼저 정해져야 한다** — 새 리비전이 프롬프트 sha 핀·gate 동작에 어떤 영향을 주는지 미측정이다. 볼륨의 stale `.downloadInProgress` 약 4.9 GB 정리도 함께 판단할 사안.
- **[★★ 알파 이미지 부채 — 증상 정정 2026-08-02, 종전 서술이 위험을 과소평가했다] 오래된 `application` 이미지는 죽지 않는다. 조용히 *인증 없는 제품*으로 뜬다.** 종전 이 항목은 "임포트 체인이 `ModuleNotFoundError`로 **죽는다**"고 적었으나 **실측은 반대였다**(2026-08-02, 스택 재생성 후): 당시 이미지가 **2026-07-22 빌드**(= auth 슬라이스 D8-1~6 전체보다 앞선다)라 그 코드에는 `argon2` import 자체가 없고, 그래서 **앱은 정상 기동해 healthy를 보고한다**. 대신 **`/auth/login`이 404이고 `GET /projects`가 세션 없이 200**이다 — 즉 **HTTP 인증 시행이 통째로 없는 제품**이 뜬다. `docker exec … python -c "import argon2"`는 여전히 `ModuleNotFoundError`이므로 **"argon2가 없다"는 관측은 맞았고 그로부터 추론한 증상이 틀렸다**. **왜 위험한가**: 죽으면 즉시 알지만 이건 healthy라 안 보이고, 하필 `application`·`frontend`는 **일부러 LAN에 공개**하는 두 표면이다(D8-7 G1=C가 "세션 뒤에 있다"를 근거로 공개를 유지한다) — **오래된 이미지에서는 그 근거가 성립하지 않는다.** **규칙: 스택을 올린 뒤 `curl :8520/projects`가 401인지 확인한다. 200이면 이미지가 낡은 것이고 즉시 재빌드한다**(`docker compose build application frontend && docker compose up -d --no-deps application frontend`). **트리 마운트는 코드만 덮고 파이썬 패키지는 이미지 것**이라 마운트로는 안 고쳐진다. **★ 날짜 갱신(2026-08-09 실측, 독립 검증이 짚었다): 지금 알파의 `ai_writte_system-application:latest` 는 `2026-08-02 07:57:51` 빌드다** — 즉 인증은 들어 있어 위 "401 확인" 은 통과하겠지만, **Slice 2(08-09)보다 앞서므로 `admin_asgi.py` 가 이미지에 없다**(`docker run … ls` 로 확인). 그것이 지금 nginx 관통을 막는 사유이고, 트리 마운트로는 새 진입점만 띄울 수 있다(검증자가 그렇게 기동을 입증했다).
- **[프론트 스타일 부채, 오너 관측 2026-07-29] 이어쓰기 예비 원고가 줄바꿈되지 않아 탭이 좌우로 길어진다.** 원인은 정확히 하나다 — [`ScratchRecovery.tsx:83`](frontend/src/writing/ScratchRecovery.tsx#L83)이 `<pre className="scratch-recovery-text">`로 그리는데 **`styles.css` 1,693줄에 그 클래스 규칙이 0건**이라 `<pre>`가 브라우저 기본 `white-space: pre`로 떨어진다(= 줄바꿈 없음). **바로 옆 후보 산문 블록은 이미 `white-space: pre-wrap`을 쓰고 있어**([`styles.css:1117`](frontend/src/styles.css#L1117)) 이 클래스만 빠진 형태다. **증상 자체는 한 줄 수정**이지만, 오너 판단은 **이것을 계기로 프론트 스타일을 제대로 한 번 잡자**는 쪽이다 — 그래서 단발 수정으로 닫지 않고 부채로 둔다. 스타일 슬라이스를 열 때 **최소한 함께 볼 것**: 긴 본문을 그리는 다른 지점의 줄바꿈·넘침 정책, 좁은 화면에서 표가 넘치는 문제(관측 화면), 그리고 실패 UX(위 `provider_error` 항목의 "다시 시도" 문구).
- **관리자 제품 표면(v1.7.82)**: 첫 관리자만 부트스트랩 스크립트로 만든다. `/admin`에서 사용자·전 project 메타데이터·전역 KPI·승격/접근 이력과 **archive project 영구 삭제·최근 삭제 감사**를 다룬다. 최초 비밀번호는 같은 로그인 화면에서 12자 이상으로 교체한다. `owner_id=None`은 승격으로도 안 열린다. purge는 정확한 이름+사유가 필수이고 503 뒤 재시도하지 않는다. **★ 2026-08-05부터 파기는 프로젝트 이름 한 값을 남긴다**(8.2c, `project_name_history`) — 화면 경고가 그 예외를 명시하므로 문구를 "전체가 삭제"로 되돌리면 거짓이 되고 프론트 셀이 문다. 첫 관리자 없이 자기 가입하는 표면은 없다.
- **[해소됨 2026-07-30, 대신 알아야 하는 것] 컨텍스트 항목 렌더링은 이제 한 정의다.** [`context_search/item_render.py::render_context_item`](services/application/app/context_search/item_render.py)을 프롬프트(`writing/prompt.py`)와 예산 회계(`estimate_rendered_item_tokens`)가 **함께** 쓴다(사본을 두게 했던 순환 import가 R-e로 사라졌다). **항목 렌더링을 바꿀 때 예산이 자동으로 따라오지만 정확히 한 곳에 여유가 있다**: 회계는 항목 생성 시점에 그 항목의 인용 번호를 모르므로 `_BUDGET_CITATION_NUMBER=999`로 센다 — 항목당 최대 1토큰 과대평가이며 세 회계 셀이 `0 ≤ 여유 ≤ 항목수`로 단정한다. **여유를 넓히려면 그 단정을 함께 고친다**(밴드 뒤에 숨기면 항목을 두 번 세는 과잉 교정이 통과한다).
- **[오너 결정 대기, K-3 가드의 의도된 공백] 창을 모르는 호출은 가드 밖이다.** 게이트웨이 프로세스의 **첫 생성 1회**와 **`/props` 조회가 실패한 프로세스 전체**가 그렇다(1b가 실패를 재시도하지 않기로 정했으므로 후자는 계속 꺼져 있다). 닫으려면 가드가 창을 짧은 예산 안에서 **기다려야** 하는데, 실측해 보니 **v1.7.60이 계약으로 못박은 "생성과 동시에 시작하고 결과를 기다리지 않는다"를 어기고** B1 회귀 셀([`test_llama_provider_client.py`](tests/test_llama_provider_client.py)의 `test_a_slow_probe_does_not_delay_or_fail_the_generate`)이 그 예산만큼 매달린다. **성능 문제가 아니라 계약 개정 문제**이며 임의로 뒤집지 않았다. 선택지: ⓐ 그대로 둔다(첫 호출만 노출) ⓑ 가드 경로에 한해 짧은 대기를 허용하도록 v1.7.60을 개정한다 ⓒ `/props` 실패를 1회 재시도하게 바꾼다(1b 결정도 함께 개정).
- **[기존 불일치, 미정렬] `/writing/generate`만 provider TIMEOUT을 502로 낸다.** gate·revise·report·compare는 504다([`main.py`](services/application/app/main.py)의 `_provider_error_status`가 504로 정한다). generate는 자기 `except ProviderError`에서 502를 내고 그 502를 [`test_writing.py`](tests/test_writing.py)의 셀이 잠그고 있다. **2026-07-30 K-3 슬라이스가 만든 것이 아니다** — 창 가드 분기만 더하고 정렬은 손대지 않았다. 정렬하려면 선언 surface(504 declared 여부)와 잠긴 셀을 함께 바꿔야 하므로 별도 판단이다.
- **[재평가 트리거, 아직 아님] 외부 API(D1~D6)가 붙어 응답이 수초로 줄면 quota 상수 셋을 다시 잰다.** **설계는 안 바뀐다** — 8.3의 입장 뮤텍스는 Mongo 왕복 두 번이라 모델 지연과 무관하다. 다시 재는 것은 ① **8.2b 최소 창 5초** — G1=C가 그 값을 고른 전제가 "요청이 창보다 오래 걸린다"(23초)였는데 3초 요청에서는 **냉각이 작업보다 길어진다**(값은 env지만 숫자는 오너의 제품 정책이라 임의 변경 금지) ② **lease 180초** — 하한이 게이트웨이 timeout(`LONGEST_SYNCHRONOUS_SECONDS=120`, [`lock.py:63`](services/application/app/quota/lock.py#L63))에 묶여 있어 외부 provider timeout이 다르면 그 상수부터 다시 잰다 ③ **기본 한도 일 20/주 100**(P7이 잠정으로 둔 값). 덤으로 8.2 L3=A(행 count가 정본)의 근거가 "창당 수십 행"이었으므로, 요청량이 크게 늘면 그때가 파생 캐시 재검토의 실측 트리거다.
- **[계약 위반, 미수리 — 2026-08-04 독립 검증 N-2] 프론트가 `detail` 문자열로 분기하는 자리가 하나 있다.** H3는 **상태코드=기계용·`detail`=사람용**이고 문자열 분기를 금지하는데, [`describeWritingError`](frontend/src/api/client.ts#L725)는 `status`를 보기 **전에** `detail.includes("invalid_candidate_report")` 등 **세 부분문자열**로 먼저 갈라진다(`client.ts:729-748`). 그 세 사건이 전부 502라 코드로는 구분되지 않아 생긴 자리이며 **동작 자체는 사용자에게 유용하다**. **왜 지금 적어 두는가**: 8.3 Q5(초과·정지 상태코드)가 "H3가 detail 분기를 금지한다"를 근거로 삼는데 **현실에는 이미 예외가 있다** — Q5=A(429 하나 + 문구 구분)를 고르면 그 예외를 하나 더 만드는 것이고, B(코드 셋)를 고르면 계약을 지키는 것이다. 닫는 방법은 둘 뿐이다: ⓐ H3를 개정해 기계 판독 코드 필드를 도입(8.2b G6 선택지 C가 기각한 것) ⓑ 그 세 사건에 서로 다른 상태코드를 주고 프론트를 정리. **어느 쪽도 8.3 범위가 아니다.**
- **[누수 아님, 의존성 주의] `context_search/service.py:199`·`:406`의 `embed()`**: 자체적으로 `EmbeddingProviderError`를 안 잡지만 호출자(step runner `:752`·`:835`)의 광의 `except Exception` → `BACKEND_ERROR` → 502가 이미 보호한다. **그 catch를 좁히면 그 순간 500 누수가 된다.**

## Owner Decisions Needed

**지금 오너의 답이 있어야 움직이는 것만.** 끝난 슬라이스의 계약·함정은 아래 "완료 슬라이스" 절로 내렸다.

- **[타이포 축 — 10.3 이 열었고, 남은 것은 화면 이관뿐]** 결정은 2026-08-11 오너 *"그냥 축소로 하고 나중에 필요하면 확장하지 뭐"*, 축은 2026-08-12 에 트리거대로 세웠다(`--type-*` 8계단, 위 "CSS 를 만질 사람이 알아야 하는 것 다섯").
  - **남은 부채는 이관이다** — `font-size` 리터럴 **선언 45곳 · 22종**이 다른 **12개 표면**에 있다(0.78rem **11곳**·0.82rem 8곳이 가장 큰 뭉침. **단위는 선언(규칙)이지 선택자 이름이 아니다** — 그룹 선택자 하나가 이름 넷일 수 있고, 그것을 혼동해 초판이 17곳이라 적었다). **트리거는 그대로**: 그 화면을 실제로 다시 그릴 때 그 화면의 font-size 부터. 전역 일괄 치환을 하지 않는다 — 같은 값이라고 같은 자리가 아니다.
  - **★ 오너가 종전 항목을 못 읽었다는 사실은 남긴다**: *"타이포 스코프 볼 수 있는 곳이 없네… 뭐가 뭔지도 잘 모르겠고."* → **이런 판단을 물을 때는 화면이나 실물 예시를 함께 낸다.** 숫자(종 수)만으로는 오너가 무엇을 고르는지 알 수 없다.

- **★ H2 = API 문서의 제품명 잔재. ★ 착수 전에 오너에게 *자세히 설명하고* 시작할 것(오너 지시 2026-08-11).**
  **오너가 "무슨 내용인지 모른다"고 명시했으므로, 이 항목을 여는 사람은 작업을 시작하기 전에
  아래를 오너의 말로 풀어 설명하고 결정을 받는다.** 설명에 반드시 담을 것:
  - **무엇인가** — 제품 이름을 "에-라잇"으로 통일했는데(10.0, D5) **화면이 아닌 곳 세 군데**가
    아직 옛 이름이다. FastAPI 가 서비스마다 들고 있는 `title=` 인자다:
    [`application/main.py:1581`](services/application/app/main.py#L1581) `"AI Writing System Application"` ·
    [`embedding/main.py:76`](services/embedding/app/main.py#L76) · [`llm_gateway/main.py:127`](services/llm_gateway/app/main.py#L127).
  - **어디서 보이는가** — 사용자가 쓰는 화면에는 **안 나온다.** 나오는 곳은 **API 문서 페이지**
    (`/docs`·`/redoc`)와 그 원본인 OpenAPI 문서다. 즉 **개발자가 보는 자리**이고, 지금은
    1인 프로젝트라 그 관객이 오너 자신뿐이다. **급하지 않다는 뜻이며 그것이 10.0 범위 밖이었던 이유다.**
  - **왜 10.0 에서 안 했나** — 10.0 은 **backend 코드 0줄**이 슬라이스의 성질이었다. 이걸 건드리면
    Python 을 고치고 `npm run gen:api`(= `dump_openapi.py` → `schema.d.ts`)를 다시 돌려야 해서
    **성질이 다른 작업**이 된다.
  - **고치면 무엇이 따라오나** — 실측(2026-08-11): 프론트 타입 `schema.d.ts` 에는 이 문자열이
    **0건**이라 **프론트는 안 흔들린다**. 중간 산출물 `frontend/openapi.json` 은 gitignore 대상이다.
    즉 비용은 **Python 3줄 + 재생성 + 회귀** 정도로 작다. **다만 세 서비스 이름을 각각 무엇으로
    할지는 오너가 정할 일이다**(예: `"에-라잇 Application"` vs 영문 유지 vs 다른 표기) — 그 선택을
    구현자가 대신 하면 안 된다.
  - **묶어서 할 후보** — 같은 성질의 "렌더 밖 노출면" 정리(예: `<head>` 의 `og:title`·manifest 가
    생길 때). 10.0 이 만든 [`productName.test.ts`](frontend/src/productName.test.ts) 스윕이
    프론트 쪽은 이미 덮고 있으므로, 이 항목은 **백엔드 쪽 짝**이다.

- **★ dogfood 착수(GATE-1)** — **기술적 선행 조건이 없다.** 실 12B 풀스택 관통은 끝났고, 인증·소유권·quota·활동 로그가 전부 서 있다. 착수하면 `OPS-1` Ready 승격. 종전 걸림돌이던 *"인가 없이 dogfood 하면 데이터가 섞인다"* 는 D8-3 시행으로 사라졌다. **오늘 기준 진짜로 남은 유일한 오너 결정이다.**
- **★ Phase 10 = 프론트 디자인 시스템 착수 여부(2026-08-10 방향 확정, 브리프 미작성).** 오너가 *"이제 전체적으로 프론트 쪽 디자인을 진행하면 될 것 같다"* 고 했고 **IA(9.2)가 끝나 순서가 왔다**. 착수하려면 브리프가 먼저이며, **회귀 285셀이 문구·구조를 상당히 잠그고 있어 그 수정 비용을 예산에 넣어야 한다**(예: `"최근 100건"` 문구는 서빙 상한과 짝으로 묶여 있다). 색·타이포·간격·컴포넌트 → 화면별 적용 순서.
- **외부 API 확장 D1~D6 = 전부 결정됨, 착수 대기**(2026-07-27, `plans/external-api-expansion-decisions.md` §2). 세 축 전부 확장하되 **슬라이스 분리**(LLM → 임베딩 → 리랭커) · D2=A(generic OpenAI 호환) · D3=A(env 키) · D4=A(전역 기본) · D5=리랭커 포함(self-host `bge-reranker-v2-m3-ko` + 외부 seam). **코드 실측 공백 = 인증 헤더 주입 지점·provider 선택 config.** 착수 조건이던 "인증 다음"은 충족됐다.
- **`analysis_extractor`를 D4로 정렬할지**(v1.7.47). 지금 이 site 만 최종 도메인 거부를 `parse_error` 로 재분류하지 않아 **같은 repair 구조인 `compare_judge` 와 정책이 갈린다.** 정렬하면 둘이 같은 규칙을 따르고, 두면 v1.7.46 결정이 유지된다. 어느 쪽이든 이행 무손실 증명이 필요한 별도 증분.
- **loop 의 round 별 gate decision 노출 여부**(v1.7.47 공백). 노출하면 loop 내부 gate 레코드에도 파생점수를 얹을 수 있다. 도메인 계약(`WritingLoopStage`) 변경이라 D2-B(파생점수 정교화)와 함께 볼 사안.
- **Phase 8 회원별 요청 제한 = 상위 방향 확정, 남은 슬라이스는 착수 시 별도 브리프**(오너 2026-08-02, 부모 계획 `plans/08-member-request-quota.md`). 기준은 **토큰량이 아니라 서비스 요청 횟수**이며 토큰은 각 요청의 생성 상한에서 통제한다. **8.0~8.4 는 완료**(아래 절), 남은 것은 **8.5 관리자 quota 운영 API**와 8.6 결제 entitlement seam이고 실제 결제·환불·CMS 프론트는 범위 밖이다.

## 완료 슬라이스 — 계약 요약과 함정만 (이력이 아니라 **지금도 구속하는 것**)

> 서사는 SoT 변경이력·`docs/verifications/`·`daily_logs/` 에 있다. 여기 남기는 것은 **다음 사람이 코드를 만지다 밟을 것**뿐이다.

- **Phase 9 활동 로그(9.0·9.1·9.2, SoT v1.7.92~v1.7.96)** — 상세는 "지금 상태" ★ 항목과 Next Tasks. **함정 넷**: ① 분류표는 **mutating 40 전수**이고 미등재 route 는 가드가 실패시킨다 ② **기록은 handler 본문에서, 결과를 안 뒤에**(dependency 로 옮기면 404·409 가 "했다"로 남는다) ③ **전수 가드는 배선의 *존재*만 보고 *분기*는 못 본다** — 기록 분기가 여럿인 handler 는 **분기마다 행위 셀**이 필요하다 ④ **`activity_events` 는 `project_id` 필드를 쓰고 파기와 함께 사라진다**(I1) — 8.2c 와 **정반대**이며 뒤집으면 D8-6 이 무너진다.
- **Phase 8 quota(8.0~8.4, SoT v1.7.83~v1.7.89)** — 시행·원장·정책·프론트 배선 전부 완료·검증 합격. **함정 다섯**: ① **유료 분류의 정본은 코드다** — [`quota/billable_actions.py`](services/application/app/quota/billable_actions.py), 강제는 `tests/test_billable_actions.py`(양방향) ② **★ 원장 필드명은 반드시 `target_project_id`** — `project_id` 로 적으면 purge reconciler 가 그 필드를 가진 컬렉션을 발견해 **과금 기록을 지운다** ③ **[`quota/policy.py`](services/application/app/quota/policy.py) 가 이 저장소의 유일한 지역 시간대(KST) 지점**이다 — 창 키 계산을 다른 곳에서 또 하면 화면과 시행이 다른 "오늘"을 말한다 ④ **정산은 `QuotaSettledRoute` wrapper 에 있다**(dependency 의 `yield` 뒤가 아니다) — 벗기면 잠금이 영영 안 풀린다 ⑤ **입장 뮤텍스 임계 구역에 provider 호출을 넣지 말 것**(한 회원 요청이 91초씩 직렬화된다). 그리고 **`policy.limits` 는 유효 한도가 아니다** — 읽기는 항상 `effective_limits`/`limits_for` 를 지난다.
- **Phase 8 잠금·이름 이력(8.2b·8.2c, SoT v1.7.87·v1.7.90)** — **함정 셋**: ① **`_id` 충돌은 "잠겨 있음"의 증거가 아니라 신호다** — 충돌 뒤 살아 있음을 다시 확인하고 없거나 만료면 원자적으로 재차지한다(초판은 **DB 에 쓰지 않은 `LockGranted`** 를 돌려줘 다음 요청도 통과시켰고 **회귀가 그것을 정상으로 고정**하고 있었다) ② **`project_name_history` 에 `project_id` 필드를 절대 더하지 말 것** — reconciler 가 그 필드로 sweep 대상을 발견하므로 더하는 순간 이 컬렉션이 지워진다(활동 로그와 **정반대 방향**) ③ **이름 스냅샷은 파괴 앞이고 fail-closed** — 뒤로 옮기거나 `try/except` 로 감싸면 이름 없이 사라지는 프로젝트가 생긴다.
- **다중 사용자 인증 D0~D8 + E1~E4(제품 시행·검증 완료)** — 경계·tier·승격 계약은 "지금 상태" 절에 있다. **여기 남기는 함정 하나**: **인증은 두 겹이라 한 겹이 빠져도 아무 테스트도 실패하지 않는다**(2026-07-28 실측) — 안쪽 겹은 바깥 겹을 뺀 일회용 앱에 `require_project_owner` 만 마운트해 격리 구동하는 셀로만 보인다.
- **[폐쇄 2026-08-10 · 다시 제기하지 말 것] 관리자가 접근 이력을 보려면 승격이 필요하고, 그 열람 자체도 감사에 남는다.** 구현자가 *"이력만 보고 싶을 때도 grant 를 발급해야 하는 것은 어긋날 수 있다"* 고 관찰했고 **오너가 검토 후 현행 유지로 결정했다**(*"개인 자산을 보는 건데… 이력만 보는 것도 감사대상으로 보자"*). **★ 그리고 그것이 이미 코드의 동작이다** — `GET /projects/{id}/access-log` 는 `_REQUIRE_PROJECT_OWNER` 라 관리자는 살아 있는 grant 없이 403 이고, grant 로 통과한 요청은 [`dependencies.py:128`](services/application/app/api/dependencies.py#L128) 의 `record_use` 가 **단일 choke point** 에서 남긴다. **UI 의 `disabled` 는 백엔드 제약을 비추는 것이지 UI 판단이 아니다.**

## Next Tasks

> **★ 2026-08-12 마감 메모 — 내일 여기서 시작한다.**
>
> **오늘 한 것**: Phase 10 **Slice 10.3**(타이포 축 + `DraftEditor`, `d4bf832`) → **10.3 관측 차트**(`0aa787f`) → **Slice 10.4 배치 통일**(`4db744c`·`e749873`). 세 본체 **전부 독립 검증 합격**(`4f67779`·`e124879`·`33e8783`)이고, 검증이 낸 비차단 4건도 같은 날 닫았다. 상세는 [work_log](docs/daily_logs/2026-08-12/work_log.md).
>
> **★ 내일 = 열려 있는 부채 셋부터. 오너 지시다**(2026-08-12): *"다음에는 열려 있는 부채들 먼저 작업하고 **검증 한꺼번에 돌리면** 될 것 같다."* 셋은 위 **추적 부채** 절 첫 항목에 처방까지 적어 뒀다 — ① 버튼 겉모습 다섯 곳 분산(**시각을 안 바꾸는 길이 있다**) ② `typeScale` 이관 목록의 M5 한계(**닫는 방법이 보인다**) ③ T1 목록 행 간격(육안 선행). **셋을 다 한 뒤 검증을 한 번에 돌린다** — 슬라이스마다 검증하지 않는다.
>
> **★ 전수 회귀는 그 다음이다**(오너 지시). 오늘 backend 프로덕션 코드는 **0줄**이라 스타일시트·문서를 읽는 가드 셋만 표적 실행했다(`24 passed / 369 subtests`). **기준선 `2271/1/2430` 은 2026-08-11 값이며 오늘 재측정하지 않았다** — 부채 작업이 끝난 뒤 `docker compose -f docker-compose.test.yml up -d`(healthy 대기) 후 전수를 돌려 갱신한다.
>
> **미검증 구간 다섯**: `e5c0fac`·`b7c6453`·`444ab1b`·`f724fce`(검증 뒤 보강분) · **`2c6eb9e`(정정)**. 본체 셋은 합격이다. **★ `2c6eb9e` 를 특히 봐 달라** — 검증자와 구현자가 **같은 함정을 함께 밟은 자리를 되돌린** 커밋이다.
>
> **육안 확인은 Phase 10 끝에 한 번에**(오너 결정 D-10.3-b). `docker compose build frontend && docker compose up -d frontend` 선행 — CSS·TS 변경이 이미지에 안 들어가 있다. **볼 것 넷**: ① 첫 화면에 콘텐츠가 들어오는가 ② 오른쪽 끝이 헤더와 맞는가 ③ **목록 행이 68rem 에서 너무 벌어지지 않는가**(T1) ④ 차트 막대의 크림색 테두리가 사라졌는가.
>
> **★ 오늘 배운 것 하나로 요약하면 — 측정은 했는데 *무엇을* 측정했는지 확인하지 않았다. 세 번 났다.** ① 집계 스크립트 출력을 눈으로 다시 세다가 **단위가 바뀌었다**(선언 11곳을 선택자 이름 17개로) ② 뮤테이션이 넓어 **셀 귀속이 흐려졌다**(두 번) ③ **하네스 마크업에 실제 클래스를 빠뜨려 없는 결함을 만들었다** — 그리고 그것이 *"실측 스크린샷"* 이라는 **가장 믿음직한 형태**로 나왔다. **렌더한 픽셀은 하네스가 준 마크업의 픽셀이지 제품의 픽셀이 아니다.** 셋 다 잡은 것은 재측정이었지 리뷰가 아니었다.
>
> **오너 결정 대기 잔여**: dogfood 착수(GATE-1) · H2(API 문서 제품명 — **착수 전 오너에게 자세히 설명할 것**).
>
> **확인용으로 남아 있는 것(정리 대상)**: 계정 `timeline_demo`/`timeline-demo-0810` 와 프로젝트 `6a795ab928e4a53aa000a824`(활동 5건). 파기하면 활동도 함께 사라진다(I1 실증 기회) 또는 계정을 비활성화한다.

**★ CSS 를 만질 사람이 알아야 하는 것 여섯** (10.1 · 10.3 · 10.4):

- **★ 페이지 폭을 화면마다 정하지 말 것**(10.4, 2026-08-12). **`main` 의 상한과 같은 68rem 을 한 규칙에서만** 정한다(`.workspace-page, .admin-page`) — 종전에는 52/62/68rem 세 종류였고 규칙이 적힌 곳이 없어 **헤더 밑줄이 한쪽으로만 삐져나와** 있었다(왼쪽 32 · 오른쪽 224px). **좁은 측정폭이 필요하면 그 블록이 스스로 제한한다**(`.page-heading` 42rem) — 컨테이너를 좁혀서 얻지 않는다. **화면 제목은 `--type-title` 한 계단**이고 예외는 로그인뿐이다(`--type-display`, 정문이라서). [`pageLayout.test.ts`](frontend/src/pageLayout.test.ts)가 **"폭을 정하는 자리가 하나인가"** 를 잠근다(값이 아니라 규칙성을 잰다 — 값은 디자인 판단이라 바뀔 수 있다). **★ 인증 화면은 규칙 밖**이다: `main.auth-shell` 은 `max-width: none` 이라 **소속이 다르다.**

- **★ 크기도 토큰이다 — `font-size` 리터럴을 쓰지 말 것**(10.3, 2026-08-12). `--type-micro`~`--type-title` **8계단**이며 값은 **1.125 등비 램프 계산 결과**다(`round(1.125**n, 3)`, 지수는 `:root` 주석에). [`typeScale.test.ts`](frontend/src/typeScale.test.ts)가 **값 ↔ 지수 주석**이 갈리는 것을 양방향으로 막고, 죽은 계단과 이관된 규칙의 리터럴 복귀도 문다. **새 계단을 더할 때 지수 주석(`/* 1.125^n */`)을 반드시 함께 적는다** — 안 적으면 예전에는 램프 검사를 조용히 빠져나갔고, `e5c0fac` 이후로는 실패한다. **`--type-reading`(1.125rem)이 원고 본문**이며 종전 1.12rem 과 사실상 같게 잡은 것이 램프 선택의 이유다 — **여기를 움직이면 가장 많이 읽는 글자가 움직인다.** ★ **다른 12개 표면은 아직 리터럴이고**(선언 45곳·22종) 그것은 결함이 아니라 점진의 중간 상태다. **화면을 옮기면 그 화면의 선택자를 `typeScale.test.ts` 의 이관 목록에 더한다 — 가드는 목록이 부른 이름만 본다**(목록에서 행을 지우면 아무 셀도 실패하지 않는다. 뮤테이션 M5 실측).

- **색을 직접 쓰지 말 것.** 화면은 **semantic 토큰만** 본다(`--text-body`·`--action-primary`…). primitive(`--blue-600`)를 화면에서 쓰거나 hex 를 박으면 [`designTokens.test.ts`](frontend/src/designTokens.test.ts)가 실패한다 — 그것이 D6=ⓑ(다크 팔레트는 안 만들되 `:root` 한 곳으로 테마가 갈리게)의 실체다.
- **★ `:root` 의 hex 를 손으로 고치지 말 것 — 이제 강제된다.** 값은 [`docs/plans/10_palette_contrast.py`](docs/plans/10_palette_contrast.py)가 만들고 [`tests/test_design_token_provenance.py`](tests/test_design_token_provenance.py)가 둘이 갈리는 것을 막는다(양방향 — 어느 한쪽만 고쳐도 실패). 스크립트를 고치고 다시 돌린 뒤 출력을 옮긴다.
- **★ 새 표면을 만들면 대비를 다시 계산한다.** 그리고 **최악 배경은 "주로 쓰는 면"이 아니라 정의된 표면 전체의 최소**다 — 10.1 이 이것을 두 번 틀렸다(카드만 보고 → 페이지로 고쳤다가 → 침강면이 더 어두웠다). `--slate-400`·`--slate-450`은 최악 면에서 **여유 없이 딱 맞는 값**이라 조금만 밝게 해도 미달한다.
- **가드가 못 보는 것: 의미의 오배정.** 정의된 토큰끼리 잘못 바꾸면(예: 페이지 배경에 `--surface-sunken`) 참조는 멀쩡해 전부 green 이다. 10.1 이 실제로 그랬고 **배포 CSS 를 눈으로 보다 잡았다.** 표면을 바꿨으면 화면을 연다.

**★ Phase 10 = 진행 중. 착수 전 알아야 하는 것 넷** (브리프 §확정값이 정본):

- **오너가 구현자 권고 둘을 고쳤다.** **D2**: 구현자가 *"현행 크림+테라코타는 글쓰기 도구다운 방향이 이미 잡혀 있다"* 며 유지를 권고했으나 **오너가 기각했고 그것이 옳았다** — 현행 `#f4f0e7` + `#a4452f` 는 **생성형 AI 디자인이 수렴하는 대표 기본값 좌표**다. **D4**: 드롭다운을 과설계로 봤으나 오너가 관용성을 근거로 **ⓐ+ⓒ 결합**으로 확정했다.
- **★ 팔레트는 계산해서 세웠고 재현 가능하다** — [`plans/10_palette_contrast.py`](docs/plans/10_palette_contrast.py). OKLCH 지각 균등 램프(hue 250 고정 · L 등간격) + **WCAG 2.2 AA 18짝 전수 검산, 실패 0**(본문 3짝은 AAA). **브리프의 숫자를 손으로 고치지 말 것** — 스크립트를 고치고 다시 돌린다. 색역 이탈은 **L 이 아니라 채도**를 줄여 들여놓는다(L 을 건드리면 대비 수치가 어긋난다).
- **회귀가 잠그는 것은 시각이 아니라 의미다**(실측): `toHaveClass` **0** · `ByTestId` **0** 이라 색·간격·타이포·배치 변경은 **비용 0**. 비싼 것은 **문구 311곳**(`ByText` 251 + `toHaveTextContent` 60)과 **접근성 semantics 478곳**(`ByRole` 299 + `ByLabelText` 179)이다. 새 드롭다운도 `role="menu"` 규율을 따른다.
- **[닫힘 2026-08-12 · 10.3] 관측 차트는 이제 색을 `:root` 에서 읽는다.** recharts 가 색을 SVG **속성**으로 받아 `var(--)` 가 안 닿으므로 [`chartColors.ts`](frontend/src/observability/chartColors.ts)가 `getComputedStyle` 로 읽는다 — **fallback 은 없다**(토큰이 없어도 그럴듯하게 그려지면 아무도 모르는 8.4 `.writing-confirm` 구조가 되기 때문). **★ 새 계열색·새 토큰을 더할 사람이 알아야 하는 것 셋**: ① **계열색은 UI 색과 기준이 다르다** — 배경 대비가 아니라 **서로 구별되는가**(색각 이상 포함)이고, **앱 상태색을 그대로 쓰면 떨어진다**(`--warn-700`↔`--danger-600` 정상 시야 ΔE 13.9 · protan 4.5 로 뭉친다. 2026-08-12 실측). 검산은 dataviz 스킬의 `validate_palette.js` 이며 명령이 `styles.css` 주석에 통째로 적혀 있다. ② **검산 배경은 `--surface-raised`** 다 — 차트가 `.chart-frame` 안에 그려지기 때문이며 **페이지 배경이 아니다.** 표면을 바꾸면 다시 돌린다. ③ **팔레트나 표면을 바꾸면 가드가 재검산을 요구한다** — [`chartColors.test.ts`](frontend/src/observability/chartColors.test.ts)가 **`:root` 계열색 ↔ 주석의 검산 명령 ↔ `.chart-frame` 이 실제로 그리는 배경** 셋을 묶는다(독립 검증 H1 로 열렸던 구멍이며, 종전에는 *"표면을 바꾸면 다시 돌린다"* 를 아무도 집행하지 않았다). **ΔE 자체는 셀이 재지 않는다** — 검증기와 갈리는 두 번째 정본을 안 만들려는 것이고, 실패하면 할 일은 **주석의 명령을 그대로 다시 돌리는 것**이다. ④ **`<Tooltip>`·`<Legend>` 는 스타일 prop 을 반드시 준다** — recharts 기본값(흰 배경·`#ccc`)으로 돌아가는 것을 전수 셀이 문다. *"색을 안 쓴 것"* 과 *"라이브러리 색을 쓴 것"* 은 다르고 **리터럴을 세는 가드는 그 둘을 구별하지 못한다.** ⑤ **렌더 테스트는 이 자리에 장님이다** — jsdom 이 스타일시트를 로드하지 않아 읽기가 전부 빈 문자열이어도 recharts 는 `fill=""` 로 조용히 그리고 그 화면 15셀은 전부 green 이다. 토큰 실재는 [`chartColors.test.ts`](frontend/src/observability/chartColors.test.ts)만 본다.


**0~3번이 전부 구현됐다.** 2026-08-05에 착수한 라우터 정리·관리자 분리 페이즈가 끝났고(0~2), **Phase 9 서비스 활동 로그도 2026-08-09에 구현됐다**(3). Slice 1은 4차에 걸쳐 76 operation(전부)을 `routers/` 11 모듈로 옮겼고(4차 모두 독립 검증 합격 — 1차 `06e7440` · 2차 `5aaf202` · 3차 `4ebde99` · 4차 `5e19867`+`d65a1c9`), 뒷정리인 미사용 import 21개 제거도 2026-08-08에 끝났다(`3ff4274`, 검증 합격 `b9fca77`). **Slice 2(관리자 표면 주소 분리 ⓑ)는 2026-08-09에 구현됐다**(`5bdaf15`·`878f24d`, SoT **v1.7.91**) — 선행이던 H-2도 함께 닫혔다. **Slice 2도 독립 검증 합격**(`9ddff6e`, Blocking 0 · 비차단 3건 처리 완료). **Phase 9 도 독립 검증 합격**(`ad195e5`, Blocking 0 — N2 뮤테이션 표기 정밀화 + accept 판단 유지). **자기 검증 구간은 없다.** **`writing/accept` 분류는 오너가 넓히기로 확정했고**(2026-08-09, SoT v1.7.93) 그 확장까지 구현됐다. **★ accept 확장 두 커밋(`170ea3a`·`ca97f2b`)과 그 보강분(`66f2845`·`33fe4b2`)까지 전부 독립 검증을 받았다 — 보강분 검증이 2026-08-10 `합격`(Blocking 0)이고 그 결과로 확장분 판정도 `조건부 합격` → `합격` 으로 승격됐다**(기록 [`verifications/2026-08-10/accept_activity_cell_reinforcement.md`](docs/verifications/2026-08-10/accept_activity_cell_reinforcement.md)). **★ 그래서 지금 미검증 구간이 없다.** 그 검증이 남긴 비차단 1건은 아래 추적 부채에 있다(숫자 라벨 오귀속 — 값은 맞다). 오너 쪽에 남은 것은 **dogfood 착수(GATE-1)** 하나다 — **Slice 2 관통 한 홉은 2026-08-10 에 닫혔다**(재빌드 후 실측: `:5520/api/admin/users` **401** · 제품앱 `:8520/admin/users` **404** = 라우터가 없다는 그 토폴로지). **★ Slice 9.1 화면은 2026-08-10 에 구현·독립 검증 합격했다(SoT v1.7.94 · 구현 `86ca173` · 검증 `b18cb83` Blocking 0 · 비차단 2건도 같은 날 폐쇄 `4097437`). 미검증 구간이 없다.** 브리프 [`plans/09-1-…`](docs/plans/09-1-activity-timeline-screen-decisions.md) Resolved. **앱을 만질 사람이 알아야 하는 것 넷**: ① **행위자 열이 없는 것은 의도다**(S3=ⓑ — 관리자 행위가 이 컬렉션 밖이라 `actor_user_id` 가 항상 본인이다. access-log 선례를 따라 원시 id 를 렌더하면 over-strict 셀이 문다) ② **"최근 100건" 문구는 두 겹으로 잠겨 있다** — 프론트 셀이 *문장이 있는지*를, [`test_activity_ui_labels.py`](tests/test_activity_ui_labels.py)의 `ActivityCeilingClaimTest` 가 *그 수가 서빙 상한과 같은지*를 본다(**백엔드 `list_for_project(limit=…)` 기본값을 바꾸면 그 셀이 실패한다** — F1 커서 페이징 작업이 여기서 걸린다. 그때는 문구도 함께 고치라는 뜻이다) ③ **`draft_version` 행은 링크가 없다** — payload 에 `draft_id` 가 없어서이며 사유와 함께 비링크로 등재돼 있다(유예 F7) ④ **선택 영역 단정은 `waitFor` 여야 한다** — 선택은 값과 다른 effect 라 동기 읽기가 과부하에서 깨진다(`DraftEditor.test.tsx` 두 자리, 2026-08-10 폐쇄). **★ 브리프 §"나중에 여는 문" F1~F7 을 지우지 말 것** — 미룬 판단이 트리거와 함께 사는 자리다. **남은 것은 육안 확인 하나**(프론트 재빌드 후 `/projects/:id/activity` — 오너 판단).

0. **[완료 2026-08-06] 미검증 구간 5커밋(`da35489`~`9caa76c`) 독립 검증 — 합격·Blocking 0.** 기록 [`verifications/2026-08-06/h3_closure_and_record_bundle.md`](docs/verifications/2026-08-06/h3_closure_and_record_bundle.md)(+ 보강 패스 §B). **자기 검증 구간은 이것으로 없어졌다.** 남긴 것 둘은 아래 추적 부채로 옮겼다 — **H-3-A**는 2026-08-06에 **결정됐고**(순환이 원인, 처방은 공유 prelude 추출 = Next Tasks 1번), **판정 분포 대조 17건**은 여전히 오너 결정 대기다. 검증이 채운 것: 신규 가드의 **over-strict 방향**(작업 세션은 under-strict 1종만 쟀다) · 미실측 로드 경로 4종 · repro 지문 IDENTICAL · 기준선 `2193/1/1931` 재현. **아직 남은 미실측 하나** — H-3 수정의 **실 컨테이너 관통은 여전히 아무도 안 봤다**(이미지가 낡아 재빌드가 선행돼야 하고 그것은 오너 판단 사안이다. `Dockerfile`의 `uvicorn services.application.app.main:app`은 FQ 경로라 가드 셀이 덮지만, 컨테이너에서 뜨는 것을 본 사람은 없다).
1. **[완료 2026-08-06 · 독립 검증 합격 `9b400d2`] 공유 prelude 추출**(`2f20fbb`·`2d68630`) — `app/api/{models,errors,dependencies}.py` + `app/env.py`. 상세는 추적 부채 절 H-3-A 항목, 브리프 §9.
2. **[완료 2026-08-09 · 독립 검증 합격(`9ddff6e`)] `main.py` 라우터 정리 + 관리자 표면 주소 분리**(R1=register 함수 · A1=ⓑ, 브리프 [`plans/router-split-and-admin-separation-decisions.md`](docs/plans/router-split-and-admin-separation-decisions.md)). Slice 1 = 76/76 operation(4차 `c289bce`), **Slice 2 = `admin` compose 서비스 + nginx `/api/admin/` + `create_product_app()`/`create_admin_app()`**(`5bdaf15`·`878f24d`). 따라야 할 패턴·재검증법·뮤테이션·**앱을 만질 때의 함정 다섯**은 "지금 상태" ★ 항목과 추적 부채 절. 검증 기록 [`verifications/2026-08-09/admin_surface_separation.md`](docs/verifications/2026-08-09/admin_surface_separation.md).
   - **8.4는 닫혔다**(W1~W7 확정·구현·독립 검증 합격, SoT v1.7.89, Blocking 0 + 비차단 3건 폐쇄. 검증 기록 [`verifications/2026-08-04/slice_8_4_product_wiring.md`](docs/verifications/2026-08-04/slice_8_4_product_wiring.md)). **앞으로도 유효한 사실 넷**: ① **한 번의 "생성" 클릭이 유료 요청 2~3건**이다(generate → gate → 지적 있으면 revise-and-gate, accept까지 4회) — 화면 문구·잔여 해석·P7 숫자 재평가가 전부 이 사실 위에 선다 ② **프론트가 부르는 유료 경로는 9개가 아니라 5개**다 ③ **면제는 신분이 아니라 정책 행**이라 `enforce_quota`에 tier 분기가 0줄이다 ④ **정지(403)는 소유권 403과 코드가 겹치므로** 정지 판정의 정본은 상태코드가 아니라 `GET /me/quota`의 `status`이며, `quota`가 로드되기 전이면 **403에서만** 한 번 다시 읽어 확정한다(402·429는 재조회하지 않는다). **남은 것은 렌더 육안 확인뿐이다**(아래 4번).
3. **[완료 2026-08-09 `65507d9`(본체, 독립 검증 합격 `ad195e5`) + accept 확장(`170ea3a`·`ca97f2b`, 독립 검증 **조건부 합격** — 502 partial 셀 하나가 조건)] Phase 9 서비스 활동 로그.** A1~A8 은 **2026-08-09 에 전부 확정됐다**(위 "결정 완료" 절에 확정값·A2 조건·주의점). 브리프 [`plans/09-0-service-activity-log-decisions.md`](docs/plans/09-0-service-activity-log-decisions.md) · 부모 계획 [`plans/09-service-activity-log.md`](docs/plans/09-service-activity-log.md). 라우터 정리가 끝나 **A7 가드가 `main.py`를 파일로 읽어야 할 이유가 없어졌다** — 그것이 이 페이즈를 Phase 9 앞에 둔 이유였고, 이제 가드는 route-driven 으로 썼다. **구현 결과·판단·뮤테이션 8종은 [2026-08-09 work_log Task 4](docs/daily_logs/2026-08-09/work_log.md)**, 계약은 SoT **v1.7.92**·`mongo_collections.md` **§43G**(+§43 폐기). **검증할 사람에게**: 8.2c 와의 대칭(N5)·404/409 셀의 역할 분담·`writing/accept` 판단(추적 부채)이 볼 만한 축이다. **화면은 아직 0줄**이다(A5=B 는 API 까지).
4. **베타 상태 확인 뒤 화면 육안 확인 3건**(전부 로직은 회귀로 잠겨 있고 렌더만 미검증이다 — 8.4의 확인 대화·잔여 타일이 셋째다). 2026-07-31에는 계정 `probe`와 감사 데이터가 있었지만 현재 존재를 다시 확인한다:
   (a) 관측 화면 `/projects/:id/observability` — 차트 라벨 충돌·막대 배치·좁은 화면 표 넘침. **`heavy long report probe` project를 보면 `provider_error`가 섞인 분포**를, `long report probe`를 보면 성공 분포를 볼 수 있다.
   (b) 비동기 패드 — 렌더 · 이어쓰기 탭 완료 배지 · 5초 폴링 · "다시 시도" 버튼 · 탭 전환 후 폴링 생존. **failed job이 4개 남아 있어 실패 UX를 바로 볼 수 있다** — 위 추적 부채대로 "다시 시도"가 결정적으로 재실패하는 것도 여기서 확인된다.
5. **관측 화면을 더 키우는 것은 API에 시간 창(`?since=`)이 생긴 뒤**가 옳다 — 지금 차트가 그리는 것은 누적 스냅샷이라 추세가 없고 막대는 표와 같은 정보를 말한다. 무엇을 더하든 **`React.lazy` 경계 안**에 둘 것(밖으로 나가면 진입 번들이 다시 두 배가 된다).
6. **dogfood 관찰 항목**: `report field must be an array` 실패율(12B 간헐 비-배열, repair가 흡수 — 잦으면 repair 횟수/프롬프트 축 판단) · `analysis_extract_v4`의 `aspect` 오분류 빈도 · scratch per-draft 상한(기본 20) 밀어냄.
7. **Deferred(오너 결정 선행)**: 중첩 chapter→scene tree · ProjectBrief→Draft provenance · 관계 graph/완전 timeline · saved publication manifest · Phase 7 대화형 수정(`plans/07-conversational-authoring.md`).

## Project Structure

```text
docker-compose.yml            # 배포 스택: application·mongo(rs0)·gateway·embedding·chroma·elasticsearch(nori)·worker·generation_worker·frontend(nginx)
docker-compose.test.yml       # 테스트 전용 단일노드 RS mongo(27020) — 명시 -f 로만 뜬다
docker-compose.llama.yml      # opt-in: in-stack llama.cpp GPU 서버(9080)
.env.example                  # 호스트 게시 포트 전용 대역 + 근거
CLAUDE.md / AGENTS.md         # 작업 규칙(동일 내용). HANDOFF 편집 규칙·자가 검수 트리거 포함
docs/
├── system-contract-sot.md    # ★ 정본 계약 + 변경이력(버전별)
├── plans/                    # 계획 + 착수 결정 브리프(*-decisions.md)
├── daily_logs/YYYY-MM-DD/    # 작업 상세 이력
├── verifications/YYYY-MM-DD/ # 독립 검증 기록
├── runbooks/                 # 로컬 llama 등 운영 절차
└── abstract.md 등            # 보존된 아이디에이션 원본
schemas/                      # W0 등 계약 schema
scripts/                      # 마이그레이션·live smoke·worker 엔트리포인트
services/
├── application/app/          # FastAPI 본체
│   ├── main.py               # 전 endpoint + 에러 선언 상수(_ERRORS_*)
│   ├── auth/                 # 사용자·Argon2id·서버 세션·쿠키 정책 (인가는 D8-3에서)
│   ├── core_sot/             # 정본 저장(project/draft/version/snapshot/source_ref)
│   ├── analysis/ memory/     # 추출·후보·비교·승격
│   ├── context_search/       # ContextPackage 구성 + Gate
│   ├── writing/              # 생성·Gate·revise·accept·scratch·생성 job
│   ├── indexing/             # vector/lexical 색인, embedding provider
│   └── observability/        # per-LLM-call 감사 + ObservedProvider/llm_call_scope(seam C)
├── llm_gateway/              # LLM 경계(ProviderError taxonomy)
└── embedding/                # 임베딩 서비스(BGE-m3-ko, 1024-dim)
frontend/                     # React+TS+Vite SPA (recharts는 관측 화면 전용·lazy 로드)
├── src/auth/                 # 로그인·세션 확인/만료·route guard·서버 로그아웃
├── src/observability/        # KPI 대시보드(계약이 정한 오독 방어 3종을 화면이 물려받는다)
├── nginx.conf                # /api 리버스 프록시(변수 upstream + resolver)
└── src/api/schema.d.ts       # gen:api 생성물 — 손으로 고치지 않는다
tests/                        # 백엔드 회귀(python -m pytest)
```
