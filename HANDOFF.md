# HANDOFF

> **다음 작업자를 위한 현재 상태 스냅샷.** 이력/일지가 아니라 **지금 사실이고 실행 가능한 것**만 둔다.
> 상세 이력은 `docs/daily_logs/`, 독립 검증은 `docs/verifications/`, 주요 마일스톤은 `CHANGELOG.md`.

## Current Status

- 정본 SoT는 `docs/system-contract-sot.md`이며 현재 **v1.7.16**(Approved). SoT가 정본 우선순위이고, 미확정 항목은 추측 구현하지 않는다.
- **Writing Workspace V2 W3 완료 — explicit Writing intent + 전체 closure(v1.7.16, WI-01~22·SC-01/02)**: W0 §3의 `intent=append_current|start_next_unit`을 구현했다. `WritingRequest/WritingCandidate`가 `intent`+`NextUnit{title,unit_kind,goal}`를 싣고 accept가 provider/write 전에 binding(append+next_unit·start+missing·candidate mismatch·blank title/goal)을 400으로 막으며 replay lookup을 stale base·Gate보다 먼저 한다. `start_next_unit`은 **신규 Core SOT 6-surface 원자 write**(position shift[archived 포함]·새 active Draft[current+1]·version 1·snapshot·blocks·`WritingAcceptReceipt`)를 한 transaction으로 commit하고 실패 시 여섯 표면 rollback(transaction·single-writer fallback). append는 receipt 없는 save-key read-through replay(WI-17)를 유지한다. 두 intent 모두 commit 뒤 `analyze:{snapshot_id}` job, 실패는 `502+accepted=true+intent+saved` partial. accept 응답에 `intent`, `saved`에 `draft_id/unit_kind/position` 추가. SC-01/02는 W2 ProjectBrief에 더해 W3 OU·Writing intent fragment를 W0 catalog와 동형 대조한다(`nextUnitSpec.goal`은 catalog대로 required-nullable). 프론트는 채택 방식 선택+새 유닛 입력을 배선했다. Mongo 미기동 시 backend **1181 passed/73 skipped(Mongo-gated)**, frontend **146/10**, gen:api byte-identical·build 96 modules. **live replica-set 실증 완료**: `docker compose up -d mongo`로 RS 기동 후 `CORE_SOT_TEST_MONGO_URI=mongodb://localhost:27017/?replicaSet=rs0`로 실행 시 WI-11 six-surface transaction rollback 포함 backend **1254 passed/4 skipped(Chroma 1+ES 3, W3 무관)**.
- **Writing Workspace V2 W4 완료 — 프로젝트 전체 ordered-latest export(v1.7.17, D6=A, EX-01~13)**: W0 §범위의 마지막 slice다. 신규 `GET /projects/{id}/export?format=txt|markdown&manifest=<bool>&include_archived=<bool>`가 project의 Draft를 `position` 순으로, 각 unit의 **latest version**(`max(version_number)`) snapshot `raw_text`를 이어 붙인다. body는 단일 version export 선례대로 **AI metadata 미삽입 verbatim**이고 unit 경계에만 제목을 넣는다(**오너 결정**: md=`# {title}` heading, txt=제목 줄, unit_kind별 레벨 매핑 없음). archived는 **기본 제외 + `include_archived=true` 옵트인**(오너 결정), version 없는 Draft는 snapshot이 없어 body/manifest에서 skip(순서 변형 없음). export delivery manifest는 **요청 시에만**(오너 결정: 같은 endpoint `manifest=true`, 미요청 `manifest=null`) `{project_id,format,include_archived,units[]{draft_id,title,unit_kind,position,version_id,version_number,snapshot_id,content_hash}}`로 body와 동형 traceability를 기록한다. archived project export는 read라 200. saved publication manifest(version 선택 저장 정본)는 계속 Deferred. `_EXPORT_FORMATS`(txt/markdown)를 단일 version export와 공유하고, `main.py`에 `ProjectExportResponse/Manifest/UnitModel` response_model(exact-key 회귀 선행). W0 §4에 **EX-01~13**(fire 6/not-fire 7)+§6 export 계약 추가. backend **1198 passed/73 skipped(Mongo-gated)/297 subtests**, frontend **146/10**(export는 backend slice, 프론트 소비 미배선), `gen:api`가 export path/타입 추가·build OK. LLM 미사용. **오너 독립 검증 PASS(조건 없음)** — `docs/verifications/2026-07-19/w4_project_export.md`(mutation testing으로 under/over-strict 실증). 검증 뒤 §6.3 빈-body clause를 EX-13으로 보강, 잔여 hardening 후보 5건은 선례 일치라 skip. **W1~W4 전체 완료.**
- **프로젝트 export 프론트 UI 완료(v1.7.18→19, frontend 슬라이스, backend 무변)**: DraftList(`/projects/:projectId`) 원고 목록 하단에 export 컨트롤을 배선했다 — **"전체 원고를 한 파일로"**(TXT/Markdown = W4 export body Blob 다운로드)와 **"회차별 개별 파일 (ZIP)"**(TXT/Markdown). bulk는 **신규 endpoint 없이** `GET /projects/{id}/export?manifest=true`로 포함 unit을 열거→각 unit을 기존 단일 version export(`.../versions/{vid}/export`)로 verbatim 본문 취득→`jszip`으로 `{position2자리}-{제목}.{ext}` 항목을 묶어 `{project_id}.zip` 다운로드(**오너 결정: bulk=ZIP**). zip 항목명은 canonical 계약이 아니라 프론트 표현 결정(path-unsafe `\/:*?"<>|`만 `_` 치환, 빈 title→draft_id fallback). 두 체크박스: **"보관된 원고 포함"**(default off, archived draft가 있을 때만 노출; combined·bundle 양쪽 `include_archived`)과 **"추적 정보(manifest) 함께"**(default off → on이면 combined은 body + 별도 `{project_id}.manifest.json` 2파일, bundle은 zip에 `manifest.json` 추가). archived-only project는 **컨트롤 표시 + 내보낼 unit 0이면 버튼 disable + "보관된 원고 포함" 토글로 활성화**(`canExport = drafts.some(includeArchived || !archived)`; 빈 export 방지). `exportingRef` 동시 실행 가드, 진행 중 전 버튼 disable. `client.ts`에 `exportProject(projectId, format, {manifest?, includeArchived?})` 추가(기존 `exportDraftVersion` 재사용). **v1.7.18은 오너 독립 검증 PASS(조건 없음)** — `docs/verifications/2026-07-19/w4_export_frontend_zip.md`(mutation 4종); 검증 뒤 H2/H3/H4 보강, H1/H5 skip. frontend **155 passed/10 files**, build JS 389.54 kB(JSZip 포함), `gen:api` byte-identical(backend/OpenAPI 무변), `tsc` clean, `jszip@^3.10.1`. **회차별 개별=DraftEditor에서 하나씩도 여전히 가능**. export UI 트랙은 실용 범위를 채웠다. **알려진 한계(비차단)**: version 없는 non-archived draft만 있는 project는 `canExport=true`지만 backend가 version 없는 unit을 skip해 빈 export가 된다 — `listDrafts`가 version 유무를 안 줘서 프론트가 추가 fetch 없이 구분 불가(희귀·무해). version-aware gating은 별도 슬라이스.
- **미채택 Writing candidate 복구 안전망 구현 완료(2026-07-20, 오너 D0=B/D1=B/D2=A)**: generate가 만든 candidate는 accept 전까지 in-memory라 새로고침하면 소실됐다. 신규 Core-SOT-**외부** collection `writing_drafts_scratch`(`writing/scratch.py` + `scratch_mongo.py`)에 `(project_id, draft_id)`별 **미채택 이력**을 append한다. generate는 `current_position`이 있을 때만 저장하고(키 없으면 미저장), accept는 **`result.accepted`일 때만** 해당 draft scratch를 정리한다 — 비-PASS Gate는 저장된 정본이 없으므로 복구 대상이 남아 있어야 한다(양방향 회귀로 잠금). 두 훅 모두 best-effort라 안전망이 정본 경로를 막지 않는다. 신규 `GET/DELETE /projects/{id}/writing/scratch?draft_id=`(loop-audits 선례대로 plain dict, `response_model` 없음). 프론트는 `ScratchRecovery` 배너(복사 / 모두 버리기)를 편집기 이어쓰기 패널 위에 얹었다. **보존/만료(per-draft 상한 20 · accept 성공 시 즉시 삭제 · 시간 만료 없음)는 오너 위임에 따른 구현자 잠정값이며 아직 정본 계약이 아니다 — 오너 SoT 승격 대기**. **오너 독립 검증 조건부 합격 → blocking B-1 closure 완료** — `docs/verifications/2026-07-20/writing_scratch_recovery.md`(boundary matrix 14행). B-1은 **Mongo 어댑터(`scratch_mongo.py`) 회귀 부재**였다: `gate_findings`/`loop_audit`/`core_sot`/`analysis` 어댑터가 모두 갖는 fake-collection 테스트 관행에서 scratch만 이탈해, durable 모드에서만 터지는 field drift가 잠금 없이 남아 있었다. `tests/test_writing_scratch_mongo.py`(6건, mutation 6/6 bite)로 닫았다. 함께 **H-2**(502 partial도 정리하도록 `_clear_scratch_for_saved_accept()` 추출 — 브리프 rationale과 정합)·**H-1/H-4**(scratch가 raise해도 generate/accept 200, generate 실패 시 0건)·**H-3**(`next_unit` 제외 근거 브리프 기록)을 보강했다. backend **1228 passed/70 skipped/299 subtests**, frontend **159 passed/11 files**, `tsc` clean, build 101 modules(JS 391.41 kB), `gen:api`는 scratch path 2개 additive. LLM 미사용. **정리 대상 세 분기(200 정리 / 502 partial 정리 / 비-PASS 미정리)는 브리프에 명시 + 회귀로 잠금.** **per-draft 상한은 `WRITING_SCRATCH_MAX_PER_DRAFT`(compose `${...:-20}`, `WRITING_LOOP_MAX_*` 선례와 동형)로 조정한다 — 1 미만은 기동 실패(0/음수를 허용하면 안전망이 지켜야 할 초안을 조용히 지운다).** **테스트 하네스 주의**: `ScratchRecovery`가 편집기 마운트 시 fetch를 1건 더 내므로 `DraftEditor.test.tsx`/`App.test.tsx`의 순서 기반 `mockFetch`가 깨졌다 — 두 파일 모두 scratch URL을 **기록된 mock 밖에서** 빈 목록으로 응답하는 우회(`stubFetch`)를 넣어 기존 index/count 단언이 편집기 자신의 요청만 기술하도록 유지했다. 편집기에 새 fetch를 추가하는 후속 작업은 같은 지점을 다시 건드려야 한다. **신규 Mongo 어댑터를 추가할 때는 fake-collection 회귀를 반드시 함께 붙인다**(이번 B-1의 재발 방지선 — 선례를 인용하면 그 선례의 테스트까지 인용해야 한다).
- **테스트베드 슬라이스 독립 검증 조건부 합격 → blocking 3건 closure + D5=A 정렬(v1.7.7), 배포·라이브 확인 완료**: 오너 39-에이전트 adversarial 검증(`docs/verifications/2026-07-18/testbed_abc_slice.md`)이 v1.7.6을 조건부 합격시킨 뒤 blocking 3건을 닫았다. (C-1/C-2) progress 단계·candidate summary 렌더 under-strict 회귀 2건(mutation bite), (Q1) `ensureSourceRefCatalog` coverage 기반 멱등 자가복구 + anchorable 0 friendly 422 + AnalysisTrigger **busyRef**. **D5=A(오너=완전 정렬)**: analysis job key는 snapshot 유도 `analyze:{snapshot_id}`이며 accept와 프론트가 동일 literal을 파생한다. 독립 재검증: 백엔드 `python3 -m pytest --ignore=tests/test_memory_mongo.py -q -p no:cacheprovider` **1119 passed / 48 skipped / 273 subtests**, 프론트 **121 passed / 8 files**, build·schema diff 0. 2026-07-18 application/frontend 재빌드·재배포 후 브라우저 동등 `:5173/api` 라이브에서 같은 snapshot 재호출이 동일 job(`create/run idempotent_replay=true`)·candidate 2→2/ID 불변으로 수렴했고 Mongo도 **job 1·candidate 2(중복 0)**를 확인했다. **주의: 백엔드는 직접 `pytest`가 아니라 `python -m pytest`로 실행한다.**
- **D5=A accept-report 라이브 hardening도 폐쇄**: 독립 검증 기록 `docs/verifications/2026-07-18/d5a_live_deploy.md`가 핵심 D5를 합격시키면서 남긴 유일한 live 미실증 축을 오너 요청으로 추가 관통했다. 실 12B `:5173/api`에서 accept Gate `pass`→report 보유 pending job→같은 snapshot trigger가 동일 job replay→첫 run 실제 소비/둘째 run replay. Mongo는 snapshot job **1**, `writing_candidate_report` 존재, candidate **1**(report의 “은빛 열쇠 발견” claim과 payload 일치, 중복 0). 따라서 accept가 심은 report의 run 소비는 더 이상 코드 추론만이 아니라 라이브+정본 확증이다.
- **이어쓰기 후 재분석 `source_ref not found` 폐쇄(v1.7.8, 오너 C, 독립 검증 PASS)**: 실검수 브리프 `docs/live_review_briefs/2026-07-18/analysis_retry_after_accept.md`, 검증 `docs/verifications/2026-07-18/analysis_retry_v3_live.md`. 현재 기본 `analysis_extract_v3`는 정확한 candidate/full-anchor shape, advisory report 뒤 authoritative catalog 직렬화, report ID를 제외한 repair payload를 사용한다(v1·초기 v2 immutable 보존). 신규 `POST .../analysis/jobs/{job_id}/retry`만 같은 job `failed→pending`과 failure clear를 허용하며 다른 상태는 409; 일반 replay/succeeded 불변. 프론트는 create replay가 failed면 retry→run하고 HTTP 200 non-succeeded도 오류로 처리한다. 원 실패 job `6a5aeba1...950`을 재배포 스택에서 같은 ID로 성공시켜 candidate **5**, 재클릭 `idempotent_replay=true`·동일 ID 5개(중복 0)를 확인했다. 검증 후 repair system prompt의 구 key 지칭을 실제 `authoritative_source_ref_catalog`로 교정하고 구 pointer 미노출 회귀를 보강했다. 최신 회귀: backend **1126 passed/48 skipped/276 subtests**, frontend **124 passed/8 files**, build·OpenAPI 생성 PASS.
- **테스트베드 사용가능화 슬라이스 완료(2026-07-18, v1.7.6, 오너 dogfood 발견 대응)**: (A) 이어쓰기 502 "report field must be an array"는 **간헐적 12B 비-배열 report**가 원인 — `report.py` repair를 1→**2회**(`MAX_REPORT_REPAIRS=2`)로 늘리고 프론트 `describeWritingError`로 사람이 읽을 안내+"다시 생성" 재시도를 붙였다(strict 계약·OpenAPI 무변). (B) 집필 화면에 **"이 원고 분석"(`AnalysisTrigger`) + "검토함 →" 링크** 추가 — 분석 추출은 snapshot에 **source_ref catalog가 선행돼야 하고 없으면 `source_ref catalog is required` 400**이므로, `analyzeVersion`이 catalog가 비면 **블록별 full-span source_ref를 먼저 생성**(offset만 필요, 서버가 quote/hash 도출)한 뒤 job create→run. (C) WritingPanel이 파이프라인을 coarse 단계(근거 검색·초안 생성→Gate→자동 개선)로 표시 + "근거 주장 N개" 요약. 라이브(실 12B): generate 정상·analyzeVersion catalog 3→run 200→candidate 3→검토함 3(어제 400 해소). 백엔드 1118 passed, 프론트 116 passed. 차후(오너 보류): 멀티턴 대화·스트리밍·2중 탭.
- **Product shell A + Writing C0/C1/C2 + Review Inbox B(핵심 검토 루프) 구현 완료(v1.6.99~v1.7.5)** — editor/save/history/export와 generate→Gate→accept/save, 자동 bounded revise/retrieve loop, 그리고 **B Review Inbox 목록+근거 detail+전체 이진/편집/충돌 action**까지 연결됐다. **B 슬라이스(v1.7.4 첫·v1.7.5 둘째, 오너 D=B)**: `/projects/:id/review`(목록)·`/review/:candidateId`(근거 detail)가 Phase 6 어포던스(v1.6.67 `{action,eligible,reason}`)를 **순수 소비**한다 — 프론트는 자격을 재계산하지 않고 `eligible`로 disable·`action`으로 분기·`reason`은 display로만 쓴다. 모든 action은 성공 후 서버 재조회(낙관적 패치 없음). **실 스택 dogfood로 Review Inbox 7/7 write action 전부 라이브 검증 완료**. 이 기준선을 유지하면서 Workspace V2 **W0/W1/W2는 완료**, 다음 code slice는 W3다. OPS-1·QUAL-1·GATE-1은 W2 완료만으로 상태를 바꾸지 않는다.
- **제품화 준비는 별도 Phase가 아니라 트리거 기반 개선 백로그로 관리**한다: `plans/product-readiness-backlog.md`. `UX-1`은 A+C+B 관통 뒤 dogfood에서 Workspace V2 구조 결손이 확인돼 **In progress(W0~W4)**, `ARCH-1`은 Done, `PROC-1`은 상시 적용한다. `OPS-1 Lite/Full`은 실 12B 관통 조건은 충족했지만 별도 dogfood 착수 결정 전이라 Waiting이다. `QUAL-1`은 실제 원고 2주 사용 때, 저장소 rename·라이선스는 외부 공개/협업/상업 trigger 때만 연다. **UX-1+QUAL-1 전 Phase 7 착수 금지**가 기본 진입 게이트다.
- **현재 프론트 표면**: `frontend/`(Vite+React+TS SPA, `react-router@8.2.0`) + compose `frontend` 서비스(nginx, `${FRONTEND_PORT:-5173}:80`). 공개 UI route는 `/`, `/projects/:projectId`, `/projects/:projectId/overview`, `/projects/:projectId/review[/:candidateId]`, `/projects/:projectId/drafts/:draftId`; version은 editor component state이고 query는 additive 후속이다. 단일 origin은 nginx `location /api/`→`proxy_pass http://application:8000/`(trailing slash가 prefix strip)로 유지하고 **CORS는 열지 않는다** — 프론트는 절대 API base를 쓰면 안 된다. dev는 Vite proxy가 같은 모양(`VITE_API_TARGET`, 기본 `http://localhost:8000`). nginx `try_files`가 BrowserRouter deep link를 `index.html`로 fallback하고, `proxy_read_timeout 120s`는 Writing loop wall-clock 60s(v1.6.89)보다 커야 한다. 실행: `docker compose up -d frontend` 후 `http://localhost:5173`.
- **운영 관찰(비차단)**: frontend nginx는 `127.0.0.1:80` 및 호스트 `:5173`에서 200이지만 compose healthcheck의 `http://localhost/`가 컨테이너에서 `::1`로 해석돼 connection refused/`unhealthy`가 된다. 실제 서비스 장애가 아닌 false-negative이며 이번 D5 배포에서는 코드·compose를 변경하지 않았다. 다음 운영 정리 때 probe를 `127.0.0.1`로 고정하는 별도 소수정 후보.
- **타입 생성의 실제 범위(v1.6.95 척추 + v1.7.1 Writing 4)**: `cd frontend && npm run gen:api`(=`scripts/dump_openapi.py`→`openapi-typescript`). **척추 14 endpoint**(projects 5·drafts 5·versions 4)는 `response_model`이 붙어 **응답 타입이 실재**하고(rename mutation으로 실증), **Writing 4 endpoint**(generate/gate/revise-and-gate/accept)도 **v1.7.1(C0)로 타입화**됐다: 성공 경로 `response_model`(`WritingCandidatePayload`·`WritingGatePayload`·`WritingReviseGateResponse`·`WritingAcceptResponse`), partial-failure 응답은 `JSONResponse` 직접 반환이라 runtime 검증은 우회하나 `responses={}`로 **OpenAPI 문서화는 된다**(revise-and-gate 400/502/503/504 = `WritingReviseGatePartial | ErrorDetailResponse`, accept 502 = `WritingAcceptAnalysisPartial | ErrorDetailResponse` — accept의 **502+`accepted=true`+`saved`** 보존). partial의 유일한 runtime lock은 exact-key 회귀(`Writing*EnvelopeKeyTest`). **나머지 30개(Writing 4 제외)는 여전히 무타입**이라 소비 시 손선언이 필요하며 해당 UI 슬라이스에서 확장한다.
- **`response_model` 취급 규칙(v1.6.95, 새 endpoint에 모델 붙일 때 반드시 읽을 것)**: FastAPI는 **모델이 선언하지 않은 필드를 조용히 삭제**한다(오류·경고 0). 따라서 **모델을 붙이기 전에 그 envelope의 exact-key 회귀를 먼저 깐다**(선례: `tests/test_application_api.py::SpineEnvelopeKeyTest`). 실증: `snapshot.project_id`를 모델에서 빼면 공개 API에서 사라지는데 **1104개 중 exact-key 회귀 1개만** 잡았다. 또한 **save surface와 read surface가 같은 키 이름(`draft_version`/`snapshot`/`blocks`)에 다른 shape**을 담으므로 모델을 공유하면 안 된다.
- **입력 검증 계약(v1.6.95, D3=A)**: project/draft의 `name`/`title`은 HTTP 경계에서 `NonBlankName`(strip 후 `min_length=1`) — `"  x  "`→`"x"` 저장, 공백-only는 **422**. **Core SOT는 계속 검증하지 않는다**(정본 계약 무변); 모든 클라이언트가 HTTP를 지나므로 경계가 authority다. 프론트 trim은 UX 편의일 뿐 계약이 아니다. `raw_text`·`idempotency_key` 제약은 아직 없다(에디터 슬라이스 후속).
- **Phase 5.10 verification closure ready**: 독립 검증 기록 `docs/verifications/2026-07-13/writing_loop_aggregate_budget.md`와 B1 legacy-Mongo test closure + H2 의미 주석이 반영되었다. 검증은 972/48/215로 green이다.
- 개발 진입점은 `docs/plans/README.md`. `docs/` 루트의 설계 문서는 초기 아이디에이션 자료다.
- **Latest Writing increment (v1.6.90)**: Gate 과민 `revise`/`not_eligible` 튜닝의 전치로 5 decision 전부를 7개 라벨 fixture로 고정한 read-only 품질 벤치마크를 추가했다. `writing/gate_quality.py` + `scripts/benchmark_writing_gate.py` (repeats 기본 3)가 production Gate factory/prompt/model/thinking/max_tokens를 재사용해 expected/actual decision·finding type·token·error·accuracy를 bodyless JSON으로 출력한다. 정상 역내 이동·정본과 양립하는 새 행동이 `pass` over-strict guard다. runtime prompt/schema/literal 무변, 쓰기 0, raw prose 미출력. 실 12B baseline→prompt 변경 브리프→동일 fixture 재측정은 후속.
- **Latest Writing increment (v1.6.92)**: **stable context pointer 구현 완료** — self-report `D2=A first→B`의 B가 닫혔다. 신규 `ContextPointer{collection,document_id,version_id,content_hash}`(`writing/models.py`) + `writing/context_pointer.py`(projection·allowlist·wire). claim의 `related_context_pointers`(required array, empty 허용)가 report wire·HTTP·Gate prompt·accept advisory에 동일 literal로 실린다. **pointer 노출은 report extractor 한 turn뿐** — `format_context_package(package, include_pointers=True)` opt-in이라 generation/revise/Gate 평문 prompt는 무변. parser는 exact 4-key/string → **current-package exact membership** → claim 내 중복 금지로 hallucinated/other-package pointer를 fails-closed로 막고, cross-project·불변식 위반 item은 **provider 호출 전** 거부한다. **필드 불변식은 origin별 테이블(오너 sub-decision P-i)**: `source_blocks`=4필드 non-empty / `memory_entries`=`content_hash`만 `""` / `analysis_candidates`=`version_id`·`content_hash` `""` / 미지 collection은 pointable 아님 — 두 store에 그 필드가 실제로 없기 때문이며(hash할 raw 정본 없음), 식별력은 memory=버전별 고유 id·candidate=edit별 고유 id로 유지된다. 신규 persistence/registry/retention 없고 쓰기 수·repair 횟수·Gate decision schema 무변. 회귀 +31(양방향, 검증 hardening H1/H2 포함). **오너 독립 검증 PASS(조건 없음)** — 아래 Verification. **실 12B pointer 준수 관측은 오너 풀스택 후속**(sandbox 검증 불가).
- **Gate quality live baseline 완료**: `192.168.1.22:9080` / `google/gemma-4-12B-it-qat-q4_0-gguf:Q4_0`, 7 case×3에서 **21/21 match, accuracy 1.0, complete=true**, fault 0. 아티팩트 `docs/benchmarks/2026-07-15/writing_gate_quality_q4_baseline.json`. 명확한 경계에서 과민 판정이 재현되지 않아 **현 prompt 튜닝은 보류**. 실 오판을 재현하는 fixture가 생길 때만 재개한다.
- 구현된 계층(모두 회귀로 잠김, 아래는 현재 동작하는 표면):
  - **LLM Gateway**: FastAPI shell(`/health/live`·`/health/ready`·`/v1/generate`), llama.cpp 호환 provider + httpx async adapter, provider error 5종→HTTP status 매핑.
  - **Core SOT**: project/draft CRUD·version read/export(txt·markdown)·source_ref catalog HTTP API. pymongo(sync) adapter(transaction 기본 + single-writer fallback), idempotency는 unique index 강제.
  - **Agent loop 계약층**: decision 7종·budget 5차원·tool registry·self-report parser·completion 판정·minimal provider composition runner. (tool-call branch는 상류 의존으로 보류 — Active Decisions 참조.)
  - **Analysis(Phase 2A)**: taxonomy 3종 extraction, job 상태 전이, provider/Gateway wiring + JSON repair, job/candidate/run HTTP API.
  - **Memory(Phase 2B.1~2B.7)**: canonical `MemoryEntry` store + candidate 승격(수동/threshold), compare→ActionProposal→versioned upsert(append-only), compare judge(Gateway 1-turn), memory→vector 재색인(async outbox→worker), event/open_question semantic identity(off 기본), character alias 탐지(v1.6.62) + 동명이인 반증·사람 승인 merge/split(v1.6.63, threshold off 기본). conflict review queue(v1.6.59)와 resolve/dismiss(v1.6.61), Review Inbox 목록/상세(v1.6.64)까지 실경로화됐다.
  - **Indexing(Phase 3A/3B + b-2)**: source block index rebuild(HTTP/CLI/deployed smoke), index sync outbox + worker(archive drain + memory reindex drain + **candidate 색인 drain**; one-shot CLI + b-6 증분1 `--loop` compose 서비스). candidate 색인 파이프라인(v1.6.54): `record_candidate(s)` → `CANDIDATE_UPSERTED` outbox → worker composite(vector `candidate_vectors` + lexical ES `candidate_lexical`). candidate 상태 전이(v1.6.61)로 **de-index 실경로화**: `CANDIDATE_REMOVED` 이벤트 → worker `index_candidate` 재유도가 not-needs_review면 delete(색인 self-heal). **ES-lexical backfill(v1.6.58)**: `scripts/phase2b5_reindex_memory.py`(memory vector+lexical)·`scripts/phase2b5_reindex_candidate.py`(candidate vector+lexical) 일괄 backfill — b-6 증분2 accepted limitation #1 해소, outbox 우회 D7 자세, live 실행은 sandbox 밖.
  - **Context search(Phase 4)**: LLM planner + orchestration + Context Gate, HTTP API, 공유 in-process vector index, real Chroma+embedding 백엔드(env 구성 시), canonical memory 포함(⑤ §5 B) + `needs_review` candidate 포함(라벨·micro·권위필드 배제, Gate가 candidate origin만 예외 허용). **canonical·candidate memory retrieval 모두** env로 backend 선택(canonical v1.6.51/52, candidate v1.6.55): `CHROMA_HOST`+`EMBEDDING_SERVICE_URL` 시 **vector relevance**, `ELASTICSEARCH_URL` 시 **ES lexical(nori)**, 둘 다면 **hybrid(RRF)**, 없으면 Mongo-direct — 모두 권위는 Mongo/analysis store 재유도(seam·item·Gate 불변). worker memory/candidate drain은 ES 구성 시 vector+lexical composite. **canonical↔candidate 승격 dedup(v1.6.60)**: candidate_memory step이 승격된 candidate(memory store `find_memory_by_candidate` 링크 존재)를 억제 — `PromotedCandidateResolver` seam(`MemoryService.is_candidate_promoted`, 미주입 시 억제 없음=종전 D7), canonical이 같은 package에 있든 없든 store 권위로 판정(D1), `ExcludedHit reason="candidate_promoted"` trace. `source_candidate_id` identity dedup이라 라이브 불요. Phase 6 candidate de-index 도입 시 상위집합 흡수(forward-defense — v1.6.61로 실경로화).
  - **Review 백엔드(Phase 6, v1.6.61~67)**: candidate confirm/reject+de-index, conflict resolve/dismiss, 사람 승인 merge/split, Review Inbox candidate/detail/source pointer, Context Gate reject finding durable persistence+inbox additive+명시 lifecycle API, candidate edit(v1.6.66), **Review Inbox 액션 어포던스(v1.6.67: list/detail·conflict·gate finding payload에 `{action,eligible,reason}` — candidate confirm/reject/edit·conflict merge[character+matched]/split[character]·gate resolve/dismiss[open]; read-time 재계산·write가 authority·additive)** 까지 구현. 남은 UI 결정은 부분 승인/retry·merge/split 일반화·frontend다.
  - **Writing AI(Phase 5.1~5.10, v1.6.68~92)**: 평문 candidate 생성→typed report(claim별 stable context pointer 포함, v1.6.92)→Writing Gate→accept/save→pending Analysis job. inline report/revise와 `/writing/revise-and-gate` bounded 합성까지 구현. 합성은 단일 continuity finding auto-revise와 targeted retrieval을 설정 가능한 구조적 상한(기본 revision 총2·retrieval1·Gate3) 안에서 순서와 무관하게 이어간다. **aggregate budget(v1.6.80)**: revise/report(+repair)/Gate/retrieval planner(+repair)의 provider usage를 내부 채널로 정확히 한 번씩 합산하고, optional `max_total_tokens|max_wall_clock_ms`를 post-accounting/deadline 시맨틱으로 강제한다. env/Compose `WRITING_LOOP_MAX_TOTAL_TOKENS|WRITING_LOOP_MAX_WALL_CLOCK_MS`; 미설정은 off이며 production 숫자는 B2b live 계측 후 확정한다. 초과 결과는 채택하지 않고 마지막 완전 candidate/최근 Gate를 보존하며, 첫 Gate 전 소진은 `gate=null` 가능. **B2b harness(v1.6.81)**는 deployed HTTP+opt-in audit에서 terminal/retrieve_more/max structural trace의 raw token/time·failure를 기록하며, live p95/여유 ceiling 승인만 남았다. ephemeral `loop`/`stages`는 무변이고 aggregate는 opt-in persisted audit summary/detail에만 노출된다. **persisted loop audit(v1.6.78→79 opt-in)**: append-only immutable bodyless trail+final candidate text, 요청 `persist_audit`/env 기본 off, persist 실패 격리+`audit_error`; retention은 운영과제 후속. side effect는 감사 쓰기 1건뿐(Core SOT/Analysis/memory 0). **Gate live diagnostics(v1.6.82, D1=A)**: operator-only one-shot `scripts/diagnose_writing_gate.py`가 `/writing/revise-and-gate` 엔드포인트의 사전 Gate 파이프라인(context→revise→report→gate)을 동일 production collaborators/prompt/model/thinking/max_tokens로 재현해 Gate의 raw response와 strict-parse error만 stdout에 출력한다. Mongo/audit/API/file 쓰기 없음. Gate는 `_default_writing_gate_service(provider=None)` seam으로 production config를 재사용(`RawCaptureProvider`로 raw 캡처). 실행은 application 컨테이너: `docker compose run --rm --no-deps application python scripts/diagnose_writing_gate.py --project-id <id>`. **독립 검증(2026-07-14)이 live 실행으로 exact 위반 clause를 확정** — root cause = **markdown fence 래핑**(`gate_prompt.py` `json_object()`가 fence strip 없이 `json.loads`; JSON 자체는 유효). **D2=A remediation 완료(v1.6.83)**: `json_object`에 `_strip_code_fence` 추가(parser 정규화·구조 완화 아님) + `WRITING_GATE_TEMPLATE`에 fence 금지. 동일 root-cause가 report/compare/extractor/planner/retrieval에도 있으나 repair로 완충돼 있어 Gate만 우선 수정(추적 부채 참조). **Report live diagnostics(v1.6.84, D1=A)**: gate 진단과 동일 패턴의 `scripts/diagnose_writing_report.py`가 report의 **first + 1-call repair raw response**와 strict-parse error만 stdout 출력(재측정 1/12 success에서 `invalid_candidate_report` 502가 남아 raw report output이 필요). Mongo/audit/file 쓰기 0. report는 `_build_report_service(capture)`로 production config 재사용. 실행: `docker compose run --rm --no-deps application python scripts/diagnose_writing_report.py --project-id <id>`. **report D2=A remediation 완료(v1.6.85)**: 진단 live 실행으로 report first output이 gate와 동일하게 ```` ```json ```` fence로 감싸짐을 확인(repair가 살림). `parse_report`에 공유 `writing/json_extract.py::strip_code_fence` 추출 적용(first·repair 양쪽 커버) + `TEMPLATE` fence 금지. "버그가 아니라 추출 과정" 프레이밍. tracked debt `report.py:113` 해소. **잔존 4개 parser(compare_judge/extractor/planner/retrieval) fence-strip 스윕은 v1.6.86으로 완료** — 4곳 모두 공유 `writing/json_extract.py::strip_code_fence`를 채택해 추적 부채 0. Phase 2A extraction 계약 clause("markdown-fenced JSON=실패→repair")도 fence 추출 정책에 맞게 정정. **B2b ceiling Option A per-stage 측정 도구(v1.6.87, M-i)**: 오너가 측정 메커니즘 M-i(각하 M-ii=audit 계약 수정+Gate 독립성 재발) 확정 → `writing/per_stage_measure.py`(`run_per_stage_measurement`)+`scripts/measure_writing_stages.py`가 revise/report/gate/retrieve_plan/context_search를 production seam으로 조립·실 gateway 격리 호출해 `TokenUsage`+`perf_counter`를 stage별 기록. context_search token 제외(항상 0)·wall-clock만(loop metered() 경계 동형), retrieve_plan은 합성 retrieve_more Gate(Gate 독립성 회피), repeats간 보수적 MAX, stage fault→`incomplete_stages`. CLI가 측정 dict→`compose_worst_case_ceiling`(env 정책)→raw ceiling JSON. 쓰기 0(gate/report 진단 동형, 출력 숫자만이라 persist 안전). **B2b 종결(v1.6.89)**: 외부 llama(9080) 연결 풀스택에서 실 12B 라이브 per-stage 수집(3 passes, complete) → raw ceiling 4991tok/51755ms(context_search 27s는 하네스 콜드스타트 아티팩트 → 프로덕션 warm steady-state ~28.8s) → **오너 B4 ~2x default-on: `WRITING_LOOP_MAX_TOTAL_TOKENS=10000`·`WRITING_LOOP_MAX_WALL_CLOCK_MS=60000`**(compose 배포 기본 발화, code env-미설정은 계속 off=M6=A 무변). 아티팩트 `docs/benchmarks/2026-07-15/`. **multi-finding revise(v1.6.88, D1=A/D2=A/D3=A)**: bounded loop의 `_eligible_revision_finding`이 자동 revise 대상을 정확히 1개로 제한해 Gate가 revise 분기에서 다수 continuity finding을 낼 때 `len != 1`만으로 `not_eligible` 종료하던 gap을 닫음. D1=A continuity-only 유지(do_not_use/pov 자동 splice 제외)·D2=A sequential(자격 함수를 "정확히 1개"→"N개 중 최우선 1개"로 완화, loop 기존 re-gate가 다음 라운드 finding 선택, finding당 revision round 1·구조 상한 bound)·D3=A(error severity 먼저, 동급 Gate 순서). 변경 표면=자격 함수 1개(reviser/splice/report/Gate/audit/budget·public literal·schema·서비스 경계 무변). Deferred: batch reviser(D2=B)·do_not_use/pov 자동 revise(D1=B)·max_revision_rounds 상향(live 후).
- **Compose 런타임**: base `docker-compose.yml`(application + Mongo replica set + gateway[외부 llama 클라이언트] + embedding + chroma + **elasticsearch**[nori, v1.6.53] + **worker**[b-6 증분1]). application에 `ELASTICSEARCH_URL` 배선 → 배포 canonical·candidate retriever 기본 = **hybrid(vector+lexical RRF)**. ES는 자체 `services/elasticsearch/Dockerfile`(공식 ES + analysis-nori, single-node·보안 off). opt-in `docker-compose.llama.yml`로 in-stack llama.cpp GPU 서버(port 9080)를 띄운다. runbook: `docs/runbooks/local-llama-server.md`. **B2b aggregate budget 배포 기본 발화(v1.6.89)**: application `WRITING_LOOP_MAX_TOTAL_TOKENS` 기본 `10000`·`WRITING_LOOP_MAX_WALL_CLOCK_MS` 기본 `60000`(라이브 측정 ceiling ~2x, override 가능). code 기본(env 미설정)은 계속 off(M6=A). **worker(index 색인 적재 drain)는 상시 compose 서비스**(`--loop` drain + SIGTERM graceful shutdown, b-6 증분1 / SoT v1.6.56) — 이전 수동/out-of-band에서 전환. atomic outbox claim이 이미 이중 실행을 막아 single-replica로 안전. **outbox per-sink bookkeeping 완료**(b-6 증분2 / SoT v1.6.57, G3=B/G4=B): enqueue는 targets 빈 dict(sink-agnostic)로 두고 worker가 claim 시 configured sink(vector/lexical)를 materialize, 각 sink가 독립 `attempt_count`/`last_error`를 가져 한 sink가 죽어도 healthy sink는 SUCCESS로 동결·재색인되지 않고 all-terminal 시에만 entry 삭제. composite `drain(entry, skip=SUCCESS)`가 per-sink `SinkOutcome`을 반환(all-or-nothing raise 폐지). retriever는 인덱스 비어도 graceful.
- **테스트**: 백엔드 `python3 -m pytest --ignore=tests/test_memory_mongo.py -q -p no:cacheprovider` → ES **미설치** 환경 기준 **1117 passed / 48 skipped / 273 subtests**(2026-07-16, v1.7.1 C0 `Writing*EnvelopeKeyTest` +9 포함). ES Python dependency **설치** 환경에선 관련 3개가 실행돼 1120/45가 된다. skip은 대부분 live Mongo/Chroma/embedding 미가용 통합 테스트. **프론트**는 별도 스위트 — `cd frontend && npm test -- --run` → **108 passed / 7 files**(C1/C2 82 + Review Inbox B 26; WritingPanel 33). `npm run build` 93 modules(CSS 12.28/JS 261.92 kB), `npm run gen:api` 후 `schema.d.ts` IDENTICAL. node_modules 부재 시 `npm ci` 선행. v1.7.4·v1.7.5 B 슬라이스도 backend/schema 무변(순수 소비)이라 백엔드 수치는 불변. 두 스위트는 서로 독립이다.

## Active Decisions

표준 제약(향후 작업을 구속하는 것)만 둔다. 완료 이력은 `CHANGELOG.md`, 근거는 각 `docs/plans/*-decisions.md` 브리프에 있다.

### 문서 · 프로세스
- **개발 단계 전환(2026-07-20, 오너)**: "게이트(Writing Gate) 우선" 개발 단계는 끝났다. 지금부터는 **게이트 ↔ UI/UX를 동반 정합**하며 나아가는 단계이고, 따라서 **SoT/정본 계약 변경 작업이 잦아지는 구간**이다. 계약 변경은 예외가 아니라 상시로 예상하고, 변경 시 canonical plan/SoT 반영을 구현과 함께(before/alongside) 처리한다. Phase 7 P1(conversation/turn 정식 착수)은 지금 진입하는 것은 아니지만 **이 구간이 향해 가는 방향으로 염두에 둔다**(미채택 candidate 영속 D1=B가 그 첫 좁은 걸음).
- 아이디에이션과 계획이 충돌하면 임의 구현 없이 사용자 결정을 받는다. 나중 요청이 기록된 결정·설계 방향과 충돌하면 어느 쪽이 canonical인지 먼저 확인한다.
- Phase를 계획의 주 축으로, MVP는 별도 축으로 관리한다. 원본 `docs/abstract.md`는 보존한다.

### 아키텍처
- monorepo + 독립 LLM Gateway/Worker, Application API backend = FastAPI. Worker는 Application 코드/계약을 공유하되 느슨히 결합하고 나중에 별도 entrypoint/process로 분리 가능하게 둔다.
- MVP는 계정/인증 없는 단일 사용자 시스템이며 프로젝트 경계는 `project_id`로 유지한다.
- **frontend = React + TypeScript + Vite로 확정**(v1.6.93, 오너 D1=A — 종전 "보류" 해소). **서빙 = 별도 compose 서비스(nginx)**(D2=B): 프론트를 application 이미지에 섞지 않고 Gateway/Worker/embedding/ES와 같은 독립 서비스 경계로 둔다. nginx가 `/api`를 application으로 reverse-proxy해 **단일 origin 유지 · CORS 미개방**(인증 없는 단일 사용자 API의 노출을 늘리지 않는다). **editor shell(frontend)은 이제 범위 안**이며 첫 슬라이스는 Product shell 척추(D3=A). 근거·각하 대안은 `plans/frontend-kickoff-decisions.md`.
- 프론트 기본값(별도 결정 아님): OpenAPI→TS 타입 생성, 얇은 `fetch` 래퍼(캐시 라이브러리는 필요해질 때), 평문 `textarea` 에디터 + 명시적 저장(Core SOT "명시적 version save only" 계약 그대로). **autosave는 도입 시 "저장=version mint" 계약 변경 여부를 먼저 결정**한다(version 폭증 위험).
- 초기 local runtime은 외부 queue 제품을 전제하지 않고 in-process/background boundary로 시작한다.
- Dockerfile/Compose는 dependency manifest 복사·설치 레이어를 소스 복사보다 앞에 두어 빌드 캐시를 보존한다.
- 외부 `/mnt/d/devel/gemma4_12b`는 선택적 provenance이며 현재 repo runtime dependency가 아니다. model/quant = 공식 QAT GGUF Q4_0.
- compose 기본 gateway는 `LLAMA_BASE_URL`(기본 `http://host.docker.internal:9080`)의 외부 llama.cpp 호환 endpoint를 호출한다. 계약은 `GET /health` + `POST /v1/chat/completions`(OpenAI 호환 + `chat_template_kwargs`)뿐이다.
- embedding 서비스는 gateway와 분리한다(LLM-독립). real vector = Chroma + `dragonkue/BGE-m3-ko`(1024-dim); `CHROMA_HOST`/`EMBEDDING_SERVICE_URL` 미구성 시 fake fallback.

### Core SOT 계약
- text/reference는 raw snapshot 기준: offset = raw Unicode code point, `content_hash` = raw UTF-8 SHA-256, `normalized_text_hash`는 v1 필수 아님. `source_blocks` = Markdown heading / 단독 `---`·`***` scene marker / 빈 줄 paragraph deterministic split이며 AI 추론 split은 넣지 않는다.
- `source_ref` span은 하나의 `source_block` 안에 포함된다(다중 block 인용은 후속).
- persistence: Docker 정상 runtime = MongoDB transaction 기본, non-transaction fallback = **single-writer local/test 전용**(동시성 안전은 transaction 경로 담당). 명시적 version save only(autosave 후속). draft save는 `idempotency_key` 필수이며 같은 `project_id+draft_id+idempotency_key` 재시도는 같은 `draft_version`을 반환한다.
- project/draft는 archive(soft delete)하고 snapshot/version/source_ref는 보존한다. archive = 읽기 허용 + 본문 쓰기·rename 차단(409). SOT 본문은 archive 무관 항상 불변이라 source_ref 생성은 archived에서도 허용된다.
- adaptive/semantic/length chunking은 Phase 3+ 파생 index 후보다. MongoDB raw snapshot 정본을 대체하지 않고 pointer/version/hash로 재조회 가능해야 한다.

### Agent loop / flat loop (계약층 — 현재 더 진행하지 않음)
- sub-agent spawn은 제외하고 bounded flat loop만 사용한다.
- 종료 decision 7종: `completed`/`awaiting_review`/`blocked`/`budget_exhausted`/`invalid_tool_arguments`/`tool_error`/`provider_error`. self-report wire = provider 응답 JSON top-level `self_report` = 정확히 `finalize`|`defer`(nested는 종료채널 아님).
- registry = Application/Worker 소유, task별 서버 allowlist, strict JSON Schema. v1 validator 범위 = `{required, type, additionalProperties, array items}`; enum/bounds는 해당 keyword를 쓰는 tool 첫 등록 시점까지 deferred. read-only v1 tool 6종. `project_id`는 신뢰된 실행 context에서 주입(모델 인자 아님).
- allowlist: `analysis_compare` 5 tool, `context_search` 3 tool, `writing_generate` no tool.
- budget 5차원(iteration/wall-clock/total-token/tool-call/repeated-call), retry도 같은 budget을 소비하며 초과를 성공으로 위장하지 않는다. production 기본값은 Gemma Q4 benchmark(`docs/benchmarks/2026-06-30/gemma_q4_llama_cpp_repeats3_warmup1.json`) 근거: `analysis_compare` 2/45s/1024/5/repeat2/retry1+1, `context_search` 3/60s/1536/8/repeat2/retry1+1, `writing_generate` 1/120s/1024/no tool/retry1. 모델·quant·prompt·tool latency가 바뀌면 benchmark를 다시 만들어 재확정.
- completion 판정 = 하이브리드(구조 조건 AND self-report). 종료채널(finalize/defer) ⟂ candidate status(산출물 데이터 채널).
- **보류 사유**: tool-call branch는 (1) Gateway tool-call parsing 미구현, (2) model tool-call wire format 미계약, (3) `ProviderTurnResult`가 terminal content만 받는 구조 — 3중 상류 의존이 해소돼야 착수한다. `artifact_present`도 task별 payload schema 확정 시 profile별로 교체한다.

### Analysis / Memory (Phase 2·2B)
- taxonomy 3종(`character_observation`/`event_observation`/`open_question_observation`), provenance `source_observed`/`ai_inferred`, confidence range 강제.
- 기억 갱신은 AI가 직접 덮어쓰지 않고 검색·대조·Gate·검토·versioned upsert를 거친다. memory는 append-only(version마다 새 id, 이전 id는 `superseded`).
- canonical memory는 별도 `memory_vectors` collection(`IndexRecordKind.MEMORY`)에 **canonical-only**로 색인한다. 트리거 = async outbox→worker(2B.5 D3=B). commit-후-enqueue skew는 정기 memory backfill이 유일 수렴 수단이다. enqueue는 `MemoryService` choke point로 중앙화(promote/versioned upsert 3경로를 한 번에 커버).
- event/open_question semantic identity는 compare `_find_matches`에서 처리한다(2B.6 D1=A). semantic 매칭은 **off 기본**(D4=A): `ANALYSIS_SEMANTIC_MATCH_THRESHOLD` 미설정 시 always-create 보존, 실 embedding 캘리브레이션 후 발화. character는 결정적 name-key가 **1차**이며, **별칭 semantic 보강은 v1.6.62로 추가**(브리프 `plans/02b-7-character-alias-homonym-decisions.md`, D1=A 탐지만·D2=A 별칭만): name-key 매칭 0일 때만 alias matcher(2B.6 `EmbeddingSemanticMatcher` 재사용, 별도 env·off 기본)로 다른 이름 canonical과의 의미적 근접을 조회 → 히트 시 `conflict`(review, 자동 병합 없음 — 2B.3 D2=A "자동 병합 없음" 존중). name-key≥1은 종전 judge 경로 불변. 동명이인 false-positive 반증(name-key=1 분기)·threshold 실 캘리브레이션·merge/split write는 후속.
- Writing ContextPackage의 memory 포함(⑤ §5 B, v1.6.48 canonical + v1.6.50 candidate): retrieval 레이어와 권위 재유도(항상 store 재검증)를 분리 설계했다 — retrieval을 교체해도 item 변환·Gate는 불변. **canonical retrieval은 v1.6.51 vector + v1.6.52 ES lexical/hybrid(RRF)로 실현**(`VectorCanonicalMemoryRetriever` `memory_vectors` cosine, `LexicalCanonicalMemoryRetriever` ES nori, `HybridCanonicalMemoryRetriever` RRF 융합; 전부 `get_memory` 권위 재유도→canonical-only; env로 backend 선택, 없으면 Mongo-direct). worker drain은 ES 시 vector+lexical composite. **outbox per-target bookkeeping은 미룸**(enqueue는 배포 ES 구성 모름→영구 pending 회피, worker가 configured sink로만 fan-out). **candidate retrieval도 v1.6.54 색인 파이프라인 + v1.6.55 retriever로 실현**(`VectorCandidateMemoryRetriever`·`LexicalCandidateMemoryRetriever`·`HybridCandidateMemoryRetriever`, 전부 `get_candidate` 권위 재유도→needs_review-only, canonical과 동형 env 선택; 색인은 별도 `candidate_vectors`/`candidate_lexical` 물리 분리). candidate 색인 de-index·drain self-heal delete는 **v1.6.61 Phase 6 전이로 실경로화**(`CANDIDATE_REMOVED` + not-needs_review 재유도 delete); retriever needs_review 필터도 전이된 candidate를 실제로 배제. **candidate 포함(v1.6.50)**: candidate는 `status=candidate`·`review_status` 라벨 + `micro_evidence`만 + 권위필드(constraints/do_not_use) 배제(Phase 6 §62 위장 금지). Gate candidate 금지는 폐지가 아니라 candidate origin(`analysis_candidates`)만 예외로 좁힘. `needs_review→confirmed/rejected` 전이는 Phase 6이지만, **승격 candidate 양쪽 노출은 v1.6.60 dedup으로 닫힘**(retrieval-time 억제, 아래 항목).
- **canonical↔candidate 승격 dedup(v1.6.50 D7 후속, v1.6.60)**: 승격 candidate는 status가 `needs_review`로 남아 candidate retriever가 계속 반환하는데, 그 canonical 사본이 같은 지식을 이미 grounding하므로 candidate_memory step이 억제한다(브리프 `plans/04-canonical-candidate-dedup-decisions.md`). **D1=승격됐으면 항상 억제**(store 권위 `find_memory_by_candidate`, `source_candidate_id` identity 링크 — canonical이 같은 package에 있든 없든; D5=A "승격 candidate는 canonical 경로로만 서빙"에 부합)·**D2=retrieval-time interim 억제**(context_search 조립 단계 additive, 상태 모델·색인 무변). `PromotedCandidateResolver` seam(`MemoryService.is_candidate_promoted`)은 optional — 미주입 시 억제 없음(종전 D7 하위호환). embedding 아닌 identity dedup이라 라이브 불요. Phase 6 candidate de-index 도입 시 상위집합으로 흡수되는 forward-defense.
- **conflict review queue 영속화(2B.4 후속, v1.6.59)**: conflict를 결정적 id로 durable `review_queue`에 멱등 upsert한다. resolve/dismiss는 v1.6.61, 사람 승인 merge/split reconciliation은 v1.6.63, candidate+conflict Review Inbox read surface는 v1.6.64로 실경로화됐다.

### 추적 부채
- 없음. **strict JSON parser fence-strip 스윕은 v1.6.86으로 완료**(잔존 0): `gate_prompt`(v1.6.83)·`report`(v1.6.85)에 이어 `compare_judge`·`extractor`·`planner`·`retrieval` 4곳이 공유 `writing/json_extract.py::strip_code_fence`를 채택했다. Phase 2A extraction 계약 clause("markdown-fenced JSON=first-parse 실패→repair")도 fence 추출 정책에 맞게 정정했다.
- (이전 부채 #8 `ProviderError`→502는 조사 결과 stale — 두 경로 모두 이미 502였음 — 로 v1.6.49에서 명시 분기+회귀 lock으로 폐쇄했다.)

## Owner Decisions Needed

- **Phase 5.10 Gate + Report live diagnostics + remediation + 재측정 — RESOLVED (gate D1=A/D2=A (a)+(c) v1.6.82→83; report D1=A v1.6.84 + D2=A (a)+(c) v1.6.85)**: `docs/plans/05-writing-gate-live-diagnostics-decisions.md`, `docs/plans/05-writing-report-live-diagnostics-decisions.md`, `docs/verifications/2026-07-14/writing_gate_live_diag.md`, `docs/benchmarks/2026-07-14/writing_loop_b2b_q4_post_fence_fix.json`. gate fence fix로 `invalid_gate_result` 502 0건. report 진단(v1.6.84) live로 report first output이 ```` ```json ```` fence임을 확인 → **report D2=A(v1.6.85)**: `parse_report`에 공유 `strip_code_fence` 추출 적용(first·repair 양쪽) + `TEMPLATE` fence 금지. tracked debt `report.py:113` 해소. **진행 상황**: (1) v1.6.86으로 잔존 4개 tracked debt parser fence 추출 완료. (2) **`unexpected_loop_trace` 조사 완료** → 근본 원인은 Gate 독립성(prose로 retrieve_more 유발 불가, live 0/12) + fixture 자명성이며 harness 버그 아님(work_log 2026-07-14 "Task 2 조사"). **오너가 ceiling 도출 Option A(per-stage 합성) 선택** → 브리프 `docs/plans/05-writing-loop-ceiling-composition-decisions.md`, 합성 코어(`worst_case_stage_counts`/`compose_worst_case_ceiling`) 구현·회귀. **오너가 측정 메커니즘 M-i 확정 → per-stage 측정 도구 v1.6.87 구현**(`writing/per_stage_measure.py`+`scripts/measure_writing_stages.py`, 회귀 10, sandbox 내 검증 완료). **B2b 종결(v1.6.89)**: 라이브 per-stage 수집(실 12B, 외부 llama 9080) → raw ceiling 4991tok/51755ms → 오너 B4 ~2x default-on(`WRITING_LOOP_MAX_TOTAL_TOKENS=10000`·`WRITING_LOOP_MAX_WALL_CLOCK_MS=60000`, compose 배포 기본 발화). Option A 완료. Gate 과민 revise/not_eligible은 별도 Gate 프롬프트 튜닝 트랙(J1의 Gate 판, 잔여).

- **B2b harness independently re-verified PASS (v1.6.81), live run blocked by structured Gate output — diagnostic surface 확보(v1.6.82).** `scripts/benchmark_writing_loop.py`는 benchmark project에 deterministic draft/version을 seed해 required `current_position`을 각 deployed POST에 전달한다. persisted audit으로 502을 재도출했다: provider token은 실제 기록되고 revise/report는 완료되지만 dominant failure가 Gate stage `invalid_gate_result`다(일부 `invalid_writing_revision`·`invalid_candidate_report`). gateway upstream ready·served model id exact match·전용 replica-set Mongo transaction writes도 확인되어 auth/model-id/Mongo permission 원인은 배제됐다. raw Gate content는 저장되지 않으므로 **`scripts/diagnose_writing_gate.py`로 exact parse/semantic clause를 관측**한 뒤 Gate prompt/repair의 별도 결정 브리프를 만들고, 성공 report의 p95/failure rate로 aggregate default off→on 및 token/time 여유 ceiling을 owner가 확정한다.

- **Phase 5.9 L9 B persisted loop audit 완료(v1.6.78→80)**: P1=B/P3=A/P4=A/P5=A + **P2=B opt-in**(v1.6.79 재개정). loop 종료를 append-only immutable 레코드로 영속화하고 list/detail로 읽는다. v1.6.80에서 run-level `total_tokens`/`wall_clock_ms`를 summary/detail에 additive로 추가했다(구문서 default 0). **retention(TTL/archive)은 명시된 운영과제 후속** — 오래된 run 보존, 자동 삭제 없음. 전체 중간 artifact 본문(P1=C)은 후속.
- **Phase 5.9 G8 bounded loop 완료(v1.6.77)**: L1~L9 승인·구현. 상한은 `WritingLoopPolicy`와 `WRITING_LOOP_MAX_REVISION_ROUNDS|MAX_RETRIEVAL_ROUNDS|MAX_GATE_EVALUATIONS`(Compose 노출)로 조정 가능하다.

- **Phase 5.7 G3 B 완료(v1.6.75)**: 기존 합성 endpoint가 동일 ContextPackage로 revise→report→Gate를 실행한다. report 실패 partial envelope와 R5=A first→C(`stages` additive 후속)를 확정했다. 후속은 G8 B(policy/budget 내부 loop), retrieve_more 재검색/메모리 재접근.

- **Phase 5.6 부분 revise 완료(v1.6.73)**: D1~D8 확정·첫 slice 구현. 순차 후속은 retrieve_more 별도 브리프, multi-finding C 비용 측정, 자동 Gate B→bounded loop C 정책, offset/hash, persisted revision 감사 API.

- **Phase 5.5 inline `/writing/report` 완료(v1.6.72)**: A=inline+서버 ContextPackage 재구성 구현. B=persisted candidate/report 감사 이력과 id 기반 API는 확정 additive 후속. **full pointer는 v1.6.92로 구현 완료**(claim `related_context_pointers`); agent-loop 종료 `self_report`와는 여전히 별도 데이터 계약이다.

- **Phase 5.3 accept→save→analysis 완료(v1.6.70)**: D1~D7 확정·구현. D2 C(client offset/separator patch)와 D5 C(background run)는 확장 가능 후속.

- **Phase 5.2 Writing Gate 결정·구현 완료(v1.6.69)**: D1=A(별도 1-turn LLM Gate)·D2=B(decision+구조화 findings)·D3=A(side-effect-free 판정, finding evidence/recommended_decision으로 후속 부분 revise 연결)·D4=A first(별도 evaluate API, generate 합성 확장 가능).

- **Gate finding persistence + Review Inbox 통합 완료(v1.6.65)**: reject-only durable store, idempotency/lifecycle, fingerprint/pointer 기반 최소 재현, inbox additive + Gate API. pass 감사 이력과 immutable GateRun manifest는 서비스화 시 후속.

- **Phase 6 Review Inbox 백엔드 완료(v1.6.64/65)**: candidate+open conflict+Gate finding 통합, matched canonical diff, source_ref pointer. **candidate edit(v1.6.66)로 review action 3종(approve/reject/edit) 백엔드 완결. 액션 어포던스(v1.6.67)로 inbox가 항목별 가용 action+자격을 선언(프론트가 한 계약으로 구동).** 남은 Phase 6 결정은 부분 승인/retry·merge/split 일반화·frontend.

- **(c-2) 완료(v1.6.63)**: 동명이인 직접 semantic 반증(off 기본), 라벨 기반 calibration harness, review-entry 기반 append-only merge/split write. production threshold 실값 채택은 실제 라벨 데이터 실행 결과 검토 대기.

- (d) review queue(v1.6.59)·(e) 승격 dedup(v1.6.60)·Phase 6 candidate 전이(v1.6.61)·(c) character alias(v1.6.62)·**(c-2) 동명이인 반증/calibration harness/merge-split write(v1.6.63) 완료**. production threshold 실값 채택은 라벨 데이터 실행 결과 검토 대기다. 다음 slice는 오너 선택 대기((b-4) hybrid 튜닝 / Phase 6 UI 잔여).

## Next Tasks

- **★ 다음 작업 = 오너가 scratch 보존/만료 잠정 정책을 SoT로 승격·확정한다**. 미채택 candidate 복구 안전망은 구현·회귀 완료(위 Current Status)지만, **per-draft 상한(기본 20, `WRITING_SCRATCH_MAX_PER_DRAFT`로 조정) · accept 성공 시 즉시 삭제 · 시간 기반 만료 없음**은 오너 위임으로 구현자가 정한 **잠정값이며 정본 계약이 아니다**. **승격 대상은 "20"이라는 숫자가 아니라 "기본 20 + 운영자 조정 가능"이라는 계약**이다(오너가 "사람마다 다르다"는 이유로 환경변수화를 지시). 브리프 [`plans/unaccepted-candidate-persistence-decisions.md`](docs/plans/unaccepted-candidate-persistence-decisions.md) "잠정 보존/만료 정책" 절을 검토해 SoT에 반영하면 이 항목이 닫힌다. 그 전까지 이 정책에 의존하는 후속(예: 이력 UI 확장)은 착수하지 않는다. **dogfood 때 실제로 몇 건이 쓸모 있었는지 관찰하면 승격 숫자의 근거가 생긴다.**
- Writing Workspace V2 W1~W4 완료 + 프로젝트 export UI(통합/개별 ZIP + include_archived/manifest 토글) 완료(v1.7.19). export 계약 상세는 W0 contract §6·SoT v1.7.17, export UI는 v1.7.18·v1.7.19. dogfood는 오너가 "어느정도 완성되면" 착수한다.
- **다른 후보(브리프/게이트 대기)**: saved publication manifest 정본(brief 선행), 오너 dogfood/OPS-1 착수(오너 결정). 큰 Deferred(중첩 chapter→scene tree, 관계 graph/완전 timeline, ProjectBrief→Draft provenance)는 작은 슬라이스가 아니라 미착수.
- **W3 live 축 완료**: WI-11(`WritingIntentMongoTest::test_start_next_transaction_rolls_back_entire_write_set`)과 Mongo start_next 경로를 이 머신의 replica-set Mongo(`docker compose up -d mongo`)에서 실행해 six-surface transaction rollback을 실증했다(`test_core_sot_mongo.py` 54 passed/0 skipped). **주의**: 이 테스트들은 Mongo가 기동돼 있어야 실행되며, 미기동 시 skip일 뿐 인프라 부재가 아니다. 실행법: `docker compose up -d mongo` 후 `CORE_SOT_TEST_MONGO_URI=mongodb://localhost:27017/?replicaSet=rs0`(RS member가 `mongo:27017`로 광고되므로 호스트에서 붙으려면 `/etc/hosts`에 `mongo`→127.0.0.1 매핑, 또는 단일노드 RS를 `localhost`로 임시 `rs.reconfig`).
- **Deferred(W4 이후/오너 결정)**: 중첩 chapter→scene tree, ProjectBrief→Draft provenance, 관계 graph/완전 timeline, saved publication manifest 정본. (미채택 candidate 영속은 2026-07-20 D0=B/D1=B/D2=A로 구현 완료 → 남은 것은 보존/만료 정책의 SoT 승격뿐이며 ★ 다음 작업에 있다.)
- **✅ C0 HTTP contract(v1.7.1) + C1 기본 Writing UI(v1.7.2) + C2 bounded loop UI(v1.7.3) + B Review Inbox 첫·둘째 슬라이스(v1.7.4·v1.7.5) 완료**: C0는 Writing 4 endpoint 응답 타입화, C1은 generate→Gate→pass accept/save, C2는 eligible revise finding 자동 loop·6 status/stage·partial candidate 보존/5xx exact-body retry, B는 목록+근거 detail+**candidate confirm/reject/edit·conflict merge/split·gate resolve/dismiss** 전체 어포던스 소비를 닫았다. 전부 backend/schema 무변.
- **✅ Review Inbox 7/7 write action 라이브 검증 완료**: 5개(2026-07-17 `review_inbox_live_e2e.md` — candidate confirm/reject/edit·conflict merge/split) + **2개(2026-07-18 `gate_finding_live_trigger.md` — gate finding resolve/dismiss)**. 07-18 관통: Context Gate `reject`를 **결정적으로** 유발(`stale_item` via **archived draft + `current_scene`** — Mongo-direct라 retrieval-time 배제 없이 gate까지 도달; **`budget_exceeded`는 `_apply_budget` 선-트림으로 엔드포인트에서 도달 불가**라 07-17 max_tokens=1 접근이 실패했던 것) → `persist_rejection` 영속 → review-inbox 어포던스 `{resolve:true,dismiss:true}` → resolve `resolved`·dismiss `dismissed`·open 서버 재조회 0. application 직접(`:8000`)·브라우저 동등 프록시(`:5173/api`) **양 경로 exit 0**. 신규 `scripts/phase6_gate_finding_live_smoke.py`(프로덕션 무변). **운영 발견(07-17)**: (1) 이미지 stale 시 기동 전 `docker compose build application frontend`, (2) `-hf` llama 재다운로드 정체 → 캐시 blob `-m` 직접 지정 우회, (3) 추출 anchor echo는 단일 라인 quote 요구.
- **OPS-1/QUAL-1 트리거는 병행 보류**: 실 12B 관통 조건은 충족됐지만 OPS-1 Ready 승격·본격 2주 dogfood 착수는 별도 오너 결정이다. Workspace V2 W0/W1/W2 완료와 혼동하지 않는다.
- **트리거 체크**: A+C+B로 UX-1 기본 루프 완료 + Review Inbox 7/7 라이브 검증. **`OPS-1`은 "실 12B 관통 확인" 충족·"오너 dogfood 착수 결정"만 대기**라 Waiting 유지 — 오너 결정 시 Ready. dogfood 1주/2주차에 `QUAL-1`, B+dogfood 종료 시 `GATE-1`(Phase 7 진입)을 확인한다. `ARCH-1`은 Done이며 v1.7.4·v1.7.5·07-18 라이브 스모크는 backend 무변이라 재발화 없음.
- **C2 검증 후속**: 독립 검증 `docs/verifications/2026-07-16/c2_writing_loop_ui.md`는 PASS(조건 없음). H1~H4는 테스트로 반영해 evidence 2회 차단·non-revise 미진입·4 discriminator 표시·성공 후 retry 버튼 숨김을 잠갔다. H5(설정성 503도 “재시도 가능”)는 현 C2의 5xx 재시도 계약과 맞물려 있어 프로덕션 코드 무변으로 두고 B/OPS UX 점검 때 별도 판단한다.
- **실 LLM 관통(오너 풀스택, C 후속)**: compose 스택에서 브라우저로 `프로젝트→원고 저장→이어쓰기 생성→Gate→자동 revise/retrieve→채택→새 version`을 관통 확인한다. sandbox는 12B 불가라 unit/build/gen:api 증거로 대체했다.
- **Writing 도메인 로직 잔여**: C0/C1/C2 frontend 소비 트랙 외의 새 Writing 동작은 즉시 실행하지 않는다 — stable pointer(v1.6.92)로 확정 계약 잔여가 소진됐다. 남은 도메인 항목은 **owner brief 선행** 또는 **풀스택 실행**이다: (1) persisted audit retention(P5=A "자동 삭제 없음" 변경 브리프 필요), (2) Gate 프롬프트 튜닝(21/21 baseline으로 보류 — 실 오판 재현 fixture가 먼저), (3) 실 12B pointer 준수 관측(아래). Phase 6 UI 잔여 중 frontend는 C 뒤 B 트랙이며, 부분 승인/retry·merge/split 일반화는 여전히 오너 결정 대기다.
- **실 12B pointer 준수 관측(오너 풀스택, v1.6.92 후속)**: 새 report 지시문("package item의 pointer object를 정확히 복사, 없으면 `[]`")에 대한 12B 준수율은 sandbox에서 검증 불가다. 실 gateway로 `/writing/report`나 `/writing/revise-and-gate`를 돌려 `invalid_candidate_report` 502가 늘지 않는지 확인하고, 실패하면 `scripts/diagnose_writing_report.py`가 first+repair raw로 exact 절을 보여준다(1회 repair는 기존대로 완충). 실패가 재현될 때만 프롬프트를 손댄다 — Gate quality baseline 선례.

1. **Phase 5.2 Writing Gate 구현 완료(v1.6.69)** — 별도 LLM Gate service/prompt/models + `POST /projects/{id}/writing/gate`, 구조화 findings와 다섯 decision/우선순위, project isolation·strict JSON/provider error mapping을 회귀로 잠갔다. 다음 핵심 후보:
   - **accept→save→analysis 재진입 완료(v1.6.70)** — 다음 Writing 후보는 구조적 self-report 또는 finding evidence 기반 부분 revise/retrieve orchestration.
   - **bounded revise/retrieve loop + persisted audit + aggregate budget 완료(v1.6.71~85)** — B2b context seed 보완 후 remote 12B live는 Gate stage `invalid_gate_result`(root cause: markdown fence 래핑)로 502가 됐다. **gate D1=A diagnostic(v1.6.82) + D2=A fence-strip(v1.6.83) + 재측정 + report D1=A diagnostic(v1.6.84) + D2=A fence-strip(v1.6.85) 완료** — gate·report 양 parser의 fence 추출 해소. 재측정 1/12 success(나머진 `unexpected_loop_trace`). **잔존 4개 tracked debt parser fence 추출은 v1.6.86으로 완료(추적 부채 0).** **`unexpected_loop_trace` 조사 완료** → Gate 독립성(prose steer 불가, retrieve_more live 0/12)이 근본 원인, harness 버그 아님. **오너가 ceiling 도출 Option A(per-stage 합성) 선택**, 합성 코어 구현·회귀(`docs/plans/05-writing-loop-ceiling-composition-decisions.md`). 측정 메커니즘 M-i 확정·도구 v1.6.87 구현 → **B2b 종결(v1.6.89)**: 라이브 per-stage 수집(실 12B) → raw ceiling 4991tok/51755ms → 오너 B4 ~2x default-on(10000tok/60000ms, compose 배포 기본). **multi-finding revise는 v1.6.88로 완료(D1=A/D2=A/D3=A)**. **stable context pointer도 v1.6.92로 완료**(D1=A/D2=A/D3=A + 오너 sub-decision P-i origin 불변식 테이블 — self-report D2=A first→B의 B 종결). 잔여는 persisted 감사 retention·전체 중간 artifact(P1=C), Gate 프롬프트 튜닝(J1의 Gate 판), 실 12B pointer 준수 관측이며 전부 owner brief 또는 풀스택 선행이다.
   - **Phase 6 UI 잔여** — frontend는 ★ 트랙으로 이관(v1.6.93 React+TS+Vite 확정, Review Inbox는 프론트 순서상 A→C→**B**). 부분 승인/부분 retry·merge/split의 event/open_question 일반화는 오너 결정 대기.
   - **(b-4) hybrid 튜닝**(최후순위) — RRF k·가중치·fetch depth 실 데이터 캘리브레이션. *실 embedding/데이터 의존 → 서브 머신 막힘.*
   - **(c-2) production threshold** — 실 라벨 데이터로 threshold 값 산출(운영 튜닝).
2. **sandbox 밖 실행 — 비-튜닝 live 검증 배치 소진(2026-07-12, 풀스택 머신)**:
   - **✅ 인덱싱 live 4종**: 2B.5 memory reindex·⑤ §8 ES lexical/hybrid·b-2 candidate 색인·Phase 3B archive→실 Chroma delete(DRAFT+PROJECT) — 실 Mongo·Chroma·ES·embedding 위에서 PASS(`docs/verifications/2026-07-12/indexing_live_smokes.md`, 오너 독립 감사 PASS). 실행법: `docker compose up -d mongo chroma elasticsearch embedding` 후 `docker compose run --rm --no-deps worker python scripts/<smoke>.py`.
   - **✅ 실 LLM(12B) 경로 live 4종**: Phase 2A provider 추출·2B.3.2 compare judge·Phase 4 planner·Phase 4 deployed e2e — in-stack llama(`docker-compose.llama.yml`, GPU) 위에서 wiring PASS(`docs/verifications/2026-07-12/llm_path_live_smokes.md`). 실행법: `docker compose -f docker-compose.yml -f docker-compose.llama.yml up -d` 후 `docker compose run --rm --no-deps -e LLAMA_BASE_URL=http://llama:9080 worker python scripts/<smoke>.py`(deployed는 `-e APPLICATION_BASE_URL=http://application:8000`).
   - **남은 sandbox-밖 = 전부 튜닝(최후속)**: 2B.6 semantic threshold 실 캘리브레이션(cosine 분포 관찰→`ANALYSIS_SEMANTIC_MATCH_THRESHOLD`), (b-4) hybrid RRF 튜닝, compare judge **J1 프롬프트 판별 튜닝**. J1 live 신호: `update`→conflict가 **재실행에도 일관(체계적 편향, 우선 타깃)**, `no_change`는 샘플링 변동. **J1 착수 브리프는 별도 deterministic 벤치마크(fake 회귀 라벨 + real 정확도)를 반드시 포함**해야 함 — live smoke는 라벨을 assert하지 않아 프롬프트 판별 정확도를 검증하지 못한다(오너 독립 감사 비차단 #2).
3. **Phase 4 real vector 잔여**: real embedding quality spike, ES lexical 튜닝(§8 (b-4)). embedding 이미지 CPU-only torch pin은 오너 지시로 최후순위(GPU 기본 유지). 운영 주의: `docker compose restart chroma application`은 `depends_on: service_healthy`를 보장하지 않아 application이 Chroma ready 전에 실패할 수 있다 — Chroma까지 재시작한 뒤 `docker compose -f docker-compose.yml -f docker-compose.llama.yml up -d application`처럼 health dependency를 다시 적용한다.
4. **보류 계약층**: `/v1/generate-structured`(비용 확인으로 보류, adapter가 JSON 검증+1회 repair 소유), domain tool-call branch(상류 의존 해소 후), task별 `artifact_present`(payload schema 확정 시).
5. **Phase 7(대화형 수정·아이디에이션·저작 감독) 계획 수립됨** — 신규 페이즈 계획 `plans/07-conversational-authoring.md`(아이디에이션 원본 `docs/chat-revision-ideation.md`). **순차 구현**이라 Phase 5(글 생성)·Phase 6(검토) 이후이며, 후속 `product-readiness-backlog.md` `GATE-1`에 따라 **UX-1 기본 루프 + QUAL-1 2주 dogfood 검토 뒤** 첫 slice를 고른다. directive 감독면(P5)은 Phase 6와 공동 설계. 확정 결정 D1~D10·슬라이스 P1~P5는 계획 문서에 있고, 슬라이스별 착수 브리프는 구현 시점에. *지금 당장 착수 대상 아님(현재 실 타깃은 Next Tasks #1).*

## Verification

- **Writing Workspace V2 W2 운영 closure 재감사 conditional → B1/B2 closure → 최종 PASS**: 최초 기록 `docs/verifications/2026-07-19/w2_operational_closure.md`의 전체 schema byte-identical 주장은 재감사 `w2_operational_closure_audit.md`가 **fresh 106805 vs committed 106941 bytes**로 반박했다. W2 ProjectBrief 18행은 정합했지만 stale Writing `-Input/-Output` component 때문에 fresh gen 뒤 build가 깨졌다. current app의 단일 model이 C0 정본(성공/partial envelope 타입화)과 W2 직전 precedent에 맞으므로 owner fork 없이 옵션 (b)를 적용했다: schema fresh 재생성 + client alias 2곳 단일 component 복원. red는 두 missing property+파생 implicit-any로 재현했고, green은 두 번째 gen byte-identical·focused backend **27/5 subtests**·지원 Node frontend **143/10**·build **96 modules**. 기존 image/Mongo/nginx/Chrome 증거는 재감사도 재현해 유지된다. 비차단: FastAPI 범위 의존에 따른 OpenAPI 표현 차이는 향후 CI 생성환경 고정 후보다.

- **Writing Workspace V2 W2 독립 검증 PASS + hardening closure(v1.7.13)**: `docs/verifications/2026-07-19/w2_project_brief_overview.md`가 PB/SC 14행 empty cell 없음과 수치를 독립 재도출해 PASS(조건 없음)했다. 비차단 H1에 따라 OpenAPI nonblank `pattern:\S`와 SC 단정을, H2에 따라 existing project/no brief `build_context_package→None` named 회귀를 추가했다. H3는 deterministic collision→stale-not-replay와 실제 Mongo 병렬 test node를 추가했고 테스트 머신 운영 closure에서 fallback/transaction 양쪽 실행까지 완료했다. focused **20 passed**, backend full **1144 passed/52 skipped**, frontend full **143 passed/10 files**, TypeScript/Vite build **96 modules**, `gen:api` 재생성·`git diff --check` PASS. H4(empty brief prompt)는 owner 계약 없는 관찰이라 유지, H5(history client binding)는 확정 API 대칭이라 유지했다.

- **Writing Workspace V2 W1 독립 검증 조건부 합격 → blocking closure(v1.7.12)**: 원 기록 `docs/verifications/2026-07-18/w1_split_workspace.md`의 B1~B6을 모두 닫았다. 원본에서 cross-Draft confirm 0회→즉시 이동 red와 stale source notice red를 재현한 뒤 guard/clear로 green. focused `DraftEditor`+`WorkspaceReviewPanel` **40 passed**, W1 관련 3 files **54 passed**, 전체 **139 passed/9 files**, build **95 modules**. backend/OpenAPI/W0 schema diff 0, `git diff --check` clean. 독립 재판정은 미실행이므로 원 conditional verdict은 역사 보존한다.

- **Frontend editor/save A2(v1.6.99) 독립 검증 PASS + hardening closure**: `docs/verifications/2026-07-16/frontend_editor_save_a2.md`. boundary matrix 13개 계약 분기 빈 셀 없음·red-first 7/16·정량·backend export 공급 계약을 독립 재현했다. 비차단 H1~H5 중 **H1** 실제 backend export envelope(`d1-v3.*`, charset 포함 MIME) fixture 정렬, **H2** selection in-flight textarea read-only/aria-busy·save/history disabled→완료 후 해제 양방향 회귀, **H3** 무의미 지역 상수 단언 제거, **H4** SoT surface 목록 보완을 반영했다. **H5** export↔selection 교차 차단은 call 시점 version id 캡처로 안전하고 불필요한 상호 차단이라 유지했다. focused `DraftEditor` **24 passed**, 전체 프론트 **47 passed/4 files**, build **90 modules**, `gen:api` PASS·`schema.d.ts` IDENTICAL, backend 범위 diff 0, `git diff --check` clean. compose는 worker만 실행 중이라 live browser smoke는 미실행했다.
- **Frontend editor/save A1(v1.6.98) 독립 검증 CONDITIONAL PASS → B1 closure, 현재 commit-ready**: `docs/verifications/2026-07-16/frontend_editor_save.md`. 원 검증은 boundary matrix 16행 빈 셀 없음·백엔드 무변·정량 재현을 확인했지만 저장 중 타이핑 silent loss를 실증했다. 오너 지시로 원인 `setRawText(intent.rawText)`를 제거하고 전용 회귀가 원본에서 **Expected AB / Received A**로 red→수정 후 green임을 확인했다. H1 replay mock은 실가능 same-key 흐름으로 분리, H3 beforeunload dirty/clean 양방향 회귀 추가, H2 pagination은 trigger 전 보류. `DraftEditor` **16 passed**, 전체 프론트 **39 passed/4 files**, build **90 modules**(CSS 5.62 kB gzip 1.79, JS 238.17 kB gzip 76.18), `gen:api` PASS·`schema.d.ts` IDENTICAL, backend 범위 diff 0, `git diff --check` clean.
- **백엔드 공개 계약 조이기(v1.6.95, D1=A/D2=A/D3=A) — 오너 독립 검증 PASS(조건 없음)**: `docs/verifications/2026-07-16/backend_contract_tightening.md`. mutation 4종(A 안전망 단독 bite·B 프론트 `tsc` rename 검출·C/D H2 양방향)과 정량(1111/45/273·프론트 10·schema 재생성 IDENTICAL)·live 키 집합(read 5/6/8·save 3/2/4)을 전부 독립 재현. 비차단 4건 중 3건 반영(**문서·주석만, 코드 무변**): **H1-d2** "2개는 JSONResponse 직접 반환이라 response_model 우회"가 부정확했음을 정정 — **성공 경로는 dict라 적용 가능**하고 uncoverable한 것은 **partial-failure envelope**뿐(결론·fork 불변) · **H1-d1** 브리프 D1=A 행 "13개(projects 2+drafts 8+snapshots 3)"→**14개(projects 5+drafts 5+versions 4)** 정정 · **H1-d3** 모델 분리 주석의 mechanism 방향 정밀화. 422 detail 표시는 기존 follow-up 유지. 이하 원 검증 내용: **H1**: 척추 14 endpoint에 `response_model`. **안전망을 모델보다 먼저 깐 것이 이 슬라이스의 핵심 절차** — 기존 회귀엔 exact-key assertion이 0이라 필드 유실이 green으로 통과했을 것이다. `SpineEnvelopeKeyTest` 5를 현 dict payload에서 통과 확인 후 모델 적용. **mutation 4종 bite 실증**: ① `ProjectPayload.archived` 제거 → 새 회귀+기존 3개 / ② `SnapshotDetailPayload.project_id` 제거 → **1104개 중 새 회귀 1개만**(안전망 고유 가치) / ③ `NonBlankName`→`str` → 12 실패 / ④ strip 누락 → 10 실패. **H1 성과 실증**: 백엔드 `archived`→`is_archived` rename + `gen:api` → 프론트 `tsc`가 `ProjectList.tsx(71,24)` 짚어 실패(이전엔 조용히 빌드). `client.ts` 손선언 삭제→생성 타입 소비(v1.6.94 H5 자연 해소). **H2**: `NonBlankName`(strip→min_length) 4종 적용, Core SOT 무변. 회귀 +12(양방향), 백엔드 **1111/45/273**, 프론트 10 passed. **live 관통**(격리 네트워크): padding strip·공백 422·척추 envelope 키 전부 보존(version detail `draft_version` 5키·`snapshot` 6키·`blocks` 8키)·enum `"heading"` 직렬화·export body 원문 PASS. `docker compose config --quiet`·`py_compile`·`git diff --check` 통과. 독립 검증 기록은 자체 구현 routine self-check라 미생성.

- **Frontend 첫 슬라이스(v1.6.94, D1=A/D2=B/D3=A) — 오너 독립 검증 PASS(조건 없음)**: `docs/verifications/2026-07-16/frontend_first_slice.md`. 경계 매트릭스 빈 셀 없음, 손선언 응답 타입이 백엔드 `_project_payload`/`list_projects`/`CreateProjectRequest`와 literal 1:1 일치(1차 소스 재도출), D2=B 단일 origin 상쇄가 nginx 구성 + live 관통(음성 `/api/nonexistent` 404 포함)으로 입증, 정량 주장 전부 독립 재현(9 passed · 195.14/61.64 kB · 1099/45/260 · `schema.d.ts` 재생성 IDENTICAL), 백엔드 git 무변경(`services/**`·`tests/**` 0건). 응답 타입 갭을 조용히 넘기지 않고 정본 개정+오너 결정 상향으로 처리한 점이 합격 근거에 포함됐다. **비차단 hardening 6건 중 3건 반영**: **H6**(이중 제출 회귀 — `saving` 가드가 프로덕션에 있으나 잠금 부재 → `fireEvent.submit`으로 disabled 버튼을 우회해 submit() 내부 가드를 직접 pin; `|| saving` 제거 mutation에서 **정확히 이 1개만 bite** 실증, 나머지 9개 무영향) · **H3**(브리프 "관련 정본" v1.6.92→v1.6.94 이력 표기) · **H4**(compose 주석이 결정 v1.6.93과 구현 v1.6.94를 구분). hardening 턴은 **프로덕션 코드 무변(테스트/문서만)**. **H2**(백엔드 공백 이름 검증 부재 — 프론트 trim은 spec-silent UX)는 공개 계약 표면 결정이라 코드 무변으로 두고 **H1과 묶어 Owner Decisions로 상향**. **H5**(schema 경로 타입 call-site 미소비)는 검증자도 "결함 아님, 엔드포인트 배선이 늘면 자연 소비"로 판단해 보류(억지 소비는 §2 speculative). 프론트 회귀 **10개**(양방향). 전체: 프론트 10 passed, 백엔드 1099/45/260.
- **Frontend 프로젝트 내비게이션(v1.6.96) — 독립 검증 PASS(조건 없음)**: `docs/verifications/2026-07-16/frontend_project_navigation.md`. boundary matrix 16행 빈 셀 없음, 프론트 22/3·build 89 modules·OpenAPI IDENTICAL·backend diff 0·공급 endpoint 5개와 409 literal 정합을 독립 재현했다. 비차단 H1~H4 중 **H3 반영**(공백-only `fireEvent.submit` submit-level 회귀, `trimmed === ""` 제거 mutation에서 단독 bite)·**H2 명확화**(새 항목 강조는 실제 위치 상실 관측 시)했고, H1 전용 404는 editor slice·H4 same-component animation은 polish로 보류했다. 프로덕션 코드 추가 변경 없음.

- **Writing stable context pointer(v1.6.92, D1=A/D2=A/D3=A + P-i) — 오너 독립 검증 PASS(조건 없음)**: `docs/verifications/2026-07-15/writing_stable_context_pointer.md`. 경계 매트릭스 빈 셀 없음(계약 1~6 should-fire/should-NOT-fire 전부 named 회귀), 착수 전 계약 모순 포착·P-i 개정 절차가 §1에 정합, spec↔코드 literal 일치·불변 표면(Gate decision schema·loop audit bodyless·쓰기 0) 독립 확인, fails-closed 이중 방어(P-i projection + parse membership). 비차단 hardening 3건 중 2건 반영: **H2**(generation/revise **service-seam** no-pointer tripwire 2건 — formatter 단위 테스트는 call-site에 `include_pointers=True`가 끼어들어도 통과하므로, 실 `WritingService.generate`/`WritingRevisionService.revise`의 provider request 본문을 assert. 두 call site mutation 시 **정확히 이 2개만 bite** 실증)·**H1**(`related_context_pointers` 비-배열 str/dict/int 매개변수화 — `_list` 우회 mutation에서 3 subtest bite). **H3**(실 12B pointer 준수율)은 sandbox 본질적 한계로 코드 무변 — 아래 Next Tasks의 오너 풀스택 후속. hardening 턴은 프로덕션 코드 무변(테스트만). focused 31/20, full 1096/48/260.

- **Writing Gate quality benchmark(v1.6.90)**: 7개 fixture가 5 decision을 전부 커버하고, `pass_live_seed_transition`·`pass_compatible_new_action`이 과민 판정 over-strict guard를 담당한다. scorer는 decision + required finding type을 채점하며 wrong decision·invalid parse를 mismatch로 fails-closed, case별 fault 격리, repeats 분리 채점. script는 `_default_writing_gate_service` production config parity. focused 6 passed; full 수치는 오늘 work log의 최종 Verification 참조.
- **Writing Gate quality live**: application 컨테이너 production network에서 7×3 실행 → **21/21 match, accuracy 1.0, complete=true**, token 506~613, fault 0. 호스트 첫 실행의 sandbox `gateway unavailable` 21건은 모델 결과가 아니므로 artifact에서 제외.

- **Writing loop multi-finding revise(v1.6.88, D1=A/D2=A/D3=A)**: `docs/plans/05-writing-multi-finding-revise-decisions.md`. `_eligible_revision_finding`을 "정확히 1개"→"자격 continuity revise finding 중 severity desc→gate순서 최우선 1개 선택"으로 완화(per-finding 자격 `_is_eligible_continuity_revise` 추출, do_not_use/pov 제외 유지). loop 기존 re-gate가 남은 finding을 다음 라운드에 선택해 순차 소진; finding당 revision round 1이라 구조 상한이 총량 bound. 변경 표면=자격 함수 1개(reviser/splice/report/Gate/audit/budget·public literal·schema·서비스 경계 무변). 회귀 +7(`EligibleRevisionFindingTest` 5: 자격 0→None·단일·2개 첫 선택·error 우선 order-independent·ineligible 혼재 시 eligible 선택 / `MultiFindingSequentialLoopTest` 2: 실 reviser 관통 순차→pass·기본 상한 budget_exhausted). 기존 "2 findings→not_eligible" boundary case를 새 계약으로 정정. 패턴 스윕: `_eligible_revision_finding`이 유일 소비처(다른 단일-finding 가정 없음). **오너 독립 검증 PASS(조건 없음)**(`docs/verifications/2026-07-15/writing_multi_finding_revise.md`) — 경계 매트릭스 11 cell 전부 lock, 순차 소진 전제(re-gate) loop 본체 확인, 실 service+reviser end-to-end 관통. 비차단 hardening 3건 반영: H1(브리프 "revise 분기 모든 finding revise 추천" → "recommended_decision=revise만 선택하므로 pass 혼재 안전"으로 정밀화)·H2(DO_NOT_USE 명시 ineligible 케이스)·H3(first-round finding=client 제공, selector는 후속 라운드만 — 설계 비대칭 문서화). focused 47/54, full 1062/45/240. `py_compile`·`docker compose config --quiet`·`git diff --check` 통과.
- **B2b ceiling Option A per-stage 측정 도구(v1.6.87, M-i)**: `docs/plans/05-writing-loop-ceiling-composition-decisions.md`. 오너 M-i 확정 후 `writing/per_stage_measure.py`(`run_per_stage_measurement`)+`scripts/measure_writing_stages.py`가 각 loop stage를 production seam으로 조립·실 gateway 격리 호출해 `TokenUsage`+`perf_counter` 기록. context_search token 제외(합성 코어 `_TOKEN_STAGES` 동형)·retrieve_plan 합성 retrieve_more Gate(Gate 독립성 회피)·repeats 보수적 MAX·stage fault→`incomplete_stages`·쓰기 0. **오너 독립 검증 PASS(조건 없음)**(`docs/verifications/2026-07-15/writing_per_stage_measure_mi.md`) — 경계 매트릭스 빈 cell 없음, load-bearing 동형성 3건 1차 소스 CONFIRMED, tripwire bite 실증. 비차단 hardening 3건 반영: **H6**(CLI가 incomplete 측정에서 ceiling 무조건 합성→과소 산출 footgun; `compose_ceiling` 래퍼가 fails-closed — incomplete면 `ceiling.complete=false`+숫자 null)·**H2**(env→policy wiring 회귀)·**H1**(SoT v1.6.86 row에 합성 코어 역참조 보강). H3/H4/H5는 일반 메커니즘 커버·cosmetic·scope-외라 보류. 회귀 14개(초기 10 + hardening 4). focused 14, full 1055/45/235. `py_compile`·`docker compose config --quiet`·`git diff --check` 통과. **live per-stage 수집→합성→B4 승인은 sandbox 밖 풀스택 후속(오너)**.
- **B2b `unexpected_loop_trace` 조사 + ceiling Option A 합성 코어**: `docs/plans/05-writing-loop-ceiling-composition-decisions.md`. 조사(work_log Task 2): 실 run trace 분해로 retrieve_more가 12 run 전체에서 0회임을 확인 → Gate가 side-effect-free 독립 평가자라 `request.instruction`으로 경로 steer 불가가 근본 원인(harness 버그 아님). 오너 Option A 선택. 합성 코어 `worst_case_stage_counts`/`compose_worst_case_ceiling`(`benchmark_writing_loop.py`) — 정책 상한→최악경로 카운트 재도출, context_search는 token 제외·wall-clock 포함(loop metered 경계 반영). 회귀 5개(카운트=max_structural·gate cap 양방향·context_search 불변식·미측정 stage=0). **오너 독립 검증 PASS**(`docs/verifications/2026-07-14/writing_loop_ceiling_and_fence_hardening.md`); 비차단 hardening#1 반영 — context_search token 제외가 `revise_gate.py:474` 직접 호출 구조에 묶여 있어, (a) 교차참조 주석 + (b) loop 레벨 tripwire 테스트(`test_context_search_usage_excluded_from_aggregate_tokens`, metered() 경유 mutation 시 22→1021로 bite)로 잠금. focused 16+budget 20, full 1041/45/235. **측정 메커니즘(M-i) + live per-stage 수집은 오너 확정·sandbox 밖 후속.**
- **잔존 4개 strict JSON parser fence-strip 스윕(v1.6.86) — 오너 독립 검증 PASS**: `docs/verifications/2026-07-14/residual_parser_fence_strip_sweep.md`. gate(v1.6.83)·report(v1.6.85)와 동일 root-cause를 `compare_judge._json_object`·`extractor._json_object`·`planner._json_object`·`retrieval.parse_writing_retrieval_plan`에 공유 `strip_code_fence` 적용으로 해소. parser 정규화이며 strict schema/literal 검증 무변. 양방향 회귀 +8(각 parser: fenced valid first-call 통과 under-strict + fenced schema/object-invalid still rejected over-strict). compare/extractor/planner의 기존 fence→repair 테스트 4개(compare 1·extractor 2·planner 1)는 fence가 더 이상 first-parse 실패가 아니므로 새 계약(fenced valid=first-call 통과, 실패 시뮬레이션은 genuinely-malformed JSON으로 교체)에 맞게 정정. SoT Phase 2A extraction clause도 정정. 순환 import 없음(`json_extract`는 `re`만 import). full 1035/45/235. 검증 후 hardening 반영: over-strict 4종 message-pin 통일(reject 이유가 object/schema check임을 assert — 우연한 JSON error가 아님, 검증 item#1). **오너 인지 2건**: (F10) 이 4개에 대한 strip은 gate/report와 달리 **defensive/parity** — 실 12B live 관통(compare·planner)에서 no-op mutation에도 succeeded, 즉 현재 모델은 prompt fence 금지에 compliance해 raw JSON을 뱉으므로 실현된 502가 아닌 **이론적/예방적** 위험 제거다("live 502를 고쳤다"는 오독 금지; extractor·retrieval은 전용 smoke 부재로 미관통). (F6) 이 slice가 **SoT 정본 텍스트를 수정한 유일한 지점** — D2=A 프레이밍의 Phase 2A(분석 파이프라인) 확장에 오너 1회 가시적 확인 권장(인가 체인은 건전).
- **Writing candidate report live diagnostics(v1.6.84, D1=A) + report fence-strip(v1.6.85, D2=A (a)+(c))**: `docs/plans/05-writing-report-live-diagnostics-decisions.md`. 진단(`scripts/diagnose_writing_report.py` + `report_live_diag.py`)이 report first+repair raw + parse error를 stdout 출력. 진단 live 실행으로 report first output이 ```` ```json ```` fence임을 확인(repair가 살림) → **v1.6.85**: `report.parse_report`에 공유 `writing/json_extract.py::strip_code_fence` 추출 적용(first·repair 양쪽 커버, "버그가 아니라 추출 과정" 프레이밍) + `TEMPLATE` fence 금지. parser 정규화·완화 아님(strict 4-field schema/item 검증 동일). 양방향 회귀: 진단 12개 + `ReportFenceStrippingTest` 7개(first+repair fence 추출 under-strict·fenced invalid over-strict·mutation bite). tracked debt `report.py:113` 해소. focused report 19, full 1027/45/223.

- **Writing Gate fence-strip remediation(v1.6.83, D2=A (a)+(c))**: `gate_prompt.py` `json_object`가 `_strip_code_fence`로 whole-content markdown code fence(```` ```lang…``` ````)를 parse 전 정규화하고 `WRITING_GATE_TEMPLATE`가 fence를 금지. **parser 정규화·구조 완화 아님** — strict schema/enum/priority/evidence는 parsed dict에 동일 적용, public literal·schema·서비스 경계 무변(유일한 동작 변화 = fence-wrapped 유효 JSON이 502→정상 parse). 양방향 회귀(`tests/test_writing_gate.py::GateFenceStrippingTest` 9개): under-strict(fence-wrapped valid parse·strip 제거 시 B2b 502 재현) + over-strict(fence-wrapped invalid가 올바른 이유로 거부)를 mutation으로 bite 실증. prompt version `writing_gate_v1` 유지(InMemory seed만). **패턴 스윹 부채**: 동일 root-cause가 `report.py:113`·`compare_judge.py:202`·`extractor.py:281`·`planner.py:239`·`retrieval.py:212`에도 있으나 repair로 완충돼 Gate만 우선 수정(추적 부채). focused 30/29, full 1008/45/220.

- **Writing Gate live diagnostics(v1.6.82, D1=A) — 독립 검증 PASS + live root cause 확정**: `docs/verifications/2026-07-14/writing_gate_live_diag.md`. `scripts/diagnose_writing_gate.py` + `gate_live_diag.py`가 `/writing/revise-and-gate` 엔드포인트의 사전 Gate 파이프라인을 동일 production config로 재현해 Gate raw response + strict-parse error만 stdout 출력, Mongo/audit/file 쓰기 0(**live 실행으로 no-write+parity 실측**). 회귀(`tests/test_writing_gate_live_diag.py` 13개)는 Gate request parity·`build_search_request` 엔드포인트 동형·raw capture(parse 실패 under-strict·성공 over-strict)·upstream 실패 분류·provider fault→`gate_provider_error`·no-write call sequence spy·`format_diagnosis`(SENSITIVE/raw/usage)를 양방향으로 잠갔다. **root cause(독립 검증 live 실행)**: B2b 502 `invalid_gate_result` exact 위반 clause = **markdown fence 래핑**(`gate_prompt.py:71` `json_object()`가 fence strip 없이 `json.loads` → JSONDecodeError; JSON 자체는 유효). 비차단 hardening 반영: `format_diagnosis`에 `prompt_version=WRITING_GATE_PROMPT_VERSION` 명시 전달(hard-coded 표시 상수화 해소). `_default_writing_gate_service(provider=None)` seam은 `_build_revise/_build_report_service` 선례와 일치, 기본 동작 무변. focused 13, writing+app 회귀 210/114, full 999/45/217. `py_compile`, `docker compose config --quiet`, `git diff --check` 통과.

- **Writing aggregate token/time budget(v1.6.80)**: owner-approved M1~M6에 따라 loop collaborator 4종의 success/repair/final-parse-failure usage를 내부 채널로 누적한다. token `> limit` 결과 미채택·`== limit` 완료, overrun의 parse-error 우선, 초기/revise/report/Gate/planner 간 경계, provider/search 전 deadline, provider timeout 구분, default/env off/on, persisted audit aggregate+legacy Mongo default 0, ephemeral `loop` 4키/stages bodyless lock을 양방향 회귀로 잠갔다. focused 116/90, full non-Mongo 971/48/215. `py_compile`, `docker compose config --quiet`, `git diff --check` 통과. 독립 검증 기록은 routine self-check라 생성하지 않았다.
- **Writing aggregate budget 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_loop_aggregate_budget.md`. M1~M6 mutation 6종은 bite했으나 SoT v1.6.80의 legacy Mongo aggregate 부재→0 회귀가 빈 셀(B1)이었다. 전용 test가 필드 없는 doc의 `total_tokens=wall_clock_ms=0`을 직접 잠그며 M7(`doc.get`→`doc[key]`) 재변이에서 `KeyError`로 bite했다. H2 `>`/`>=` 비대칭 주석을 보강했다. H1(bare fallback fail-fast)은 신규 injection/error 계약, H3(overrun stage status)는 M5=C 관측 schema라 보류. closure 후 focused audit/B2 39/6, full 972/48/215. 원 독립 verdict는 역사적으로 보존하며 별도 독립 재판정은 미실행.
- **Writing persisted loop audit(v1.6.78) 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_persisted_loop_audit.md`. 모든 loop 종료(성공·4종 partial 실패)의 append-only 기록, pre-loop 거부 미기록(over-strict guard), bodyless per-stage hash/fingerprint/pointer + final candidate text, `final_candidate_hash==stages[-1].candidate_hash`, retrieval pointer_ids 캡처, project isolation·cross-project 404, 재시도=distinct id·목록 newest-first·요약 bodyless, no Core SOT save를 양방향 회귀 잠금. **B1(detail trail 표면 token/latency·stage body 부재 lock 누락) closure**: detail top-level 15키 + stage row 6키 exact-set assertion 추가(mutation 재-bite 확인). **H1**(Mongo fake round-trip, `tests/test_writing_loop_audit_mongo.py` 선례 패턴)·**H2**(나머지 4종 loop_status 감사 명시) 보강. **H3는 오너 재설계로 해소**: v1.6.79에서 P2=A→B(opt-in) 재개정 — `persist_audit` 플래그·env 기본 off, persist를 loop 밖 try/except-격리해 쓰기 실패해도 loop 결과 보존+`audit_id=null`+`audit_error`. 신규 회귀 default-off·env default·persist-failure isolation 추가. **v1.6.79 delta 독립 재검증 합격(B2 closure)** — `docs/verifications/2026-07-13/writing_loop_audit_optin_reverification.md`: opt-in 양방향 잠금·격리·`audit_error` taxonomy·B1 detail lock 유지 확인; 유일한 빈 셀 B2(성공-path `audit_error` nullness)를 1-line assertion으로 채움(mutation re-bite로 bite 확인). focused 19/6, full 947/45/215(ES 의존 머신 기준; verifier 머신 ES 미설치 → 944/48/215, subtests 215 동일).
- **Writing bounded loop(v1.6.77) 독립 검증 PASS**: `docs/verifications/2026-07-13/writing_bounded_loop.md`. boundary matrix 빈 셀·정본/코드 literal 불일치 없음. 비차단 H1은 `WritingLoopPolicy()` 기본 `(2,1,3)` exact assertion, H2는 기본 상한에서 두 번째 revise 허용/세 번째 호출 전 `budget_exhausted` 회귀로 보강했다. H3 loop endpoint의 방어 `InvalidWritingRevision` catch는 향후 wrapping 변화에도 502 taxonomy를 유지하므로 보존했다. Writing focused 116/108, full 928/45/209. save/accept/Analysis/persistence 없음.
- **Writing targeted retrieve_more(v1.6.76)**: follow-up planner strict query+needs/repair, canonical need allowlist(candidate 금지·position 없으면 current/recent 제외), delta 우선 pointer dedup+전체 budget merge, report 재호출 0, Gate 최대 2회, 두 번째 retrieve_more 종료, planner/context partial artifact taxonomy를 양방향 회귀로 잠갔다. 독립 검증 `docs/verifications/2026-07-13/writing_retrieve_more.md` 조건부 합격의 B2(spec-silent position 제외)를 SoT/브리프에 명시해 폐쇄하고, H1 누락 taxonomy 4종·H2 retrieval no-save·H3 request/project/candidate_id를 전용 회귀로 보강했다. focused 105/97, full 917/45/198. 독립 verdict 재판정은 후속 verifier 대상.
- **Writing revise→report→Gate(v1.6.75)**: 단계 순서·동일 ContextPackage·최신 report Gate 전달, report timeout/unavailable/invalid/unexpected partial envelope와 Gate 미호출, reporter 필수 503를 양방향 회귀로 잠갔다. focused 94/84, full 906/45/185.
- **Writing revise→report→Gate 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_revise_report_gate.md`. Gate 실패 partial candidate의 최신 report 보존 빈 셀을 `candidate_claims="fresh"` assertion으로 닫았다. H1 “non-pass 5종”은 “decision 5종(pass + non-pass 4종)”으로 정정했고, H2/H3은 코드 무변이다.
- **Writing partial revise(v1.6.73) 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_partial_revise.md`. context budget→504/backend→502 HTTP 빈 셀을 닫고, 빈 입력 pre-context guard·no-save spy·Markdown fence Application unwrap을 보강했다. focused 84/68, full 896/45/169.
- **Writing revise→Gate(v1.6.74) 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_revise_gate.md`. composition 전용 revise validation→400·provider timeout→504 빈 셀과 Gate 미호출을 잠갔다. Gate validation 400·예상 밖 평가 실패 502 partial envelope도 보강하고 SoT taxonomy를 명시했다. focused 93/80, full 905/45/181.

- **Writing report API(v1.6.72) 독립 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-13/writing_report_api.md`. report endpoint 전용 unsupported task→400 빈 셀을 named test로 닫았다. 비차단 H1 invalid-context→400을 SoT+test로 명시하고 H2 Core SOT no-save spy, H3 explicit query, H4 current_position wiring도 보강했다. focused 71/54, full 883/45/155.

- 현재 시스템 전체 스위트: `python3 -m pytest --ignore=tests/test_memory_mongo.py -q -p no:cacheprovider` → **1062 passed / 45 skipped / 240 subtests**(본 환경 ES 패키지 설치 — 미설치 시 45→48 skip; v1.6.88 multi-finding 7 + v1.6.87 M-i per-stage 측정 도구 14 포함). `git diff --check` clean.
- **Writing 생성(v1.6.68) 오너 독립 검증 합격(조건 없음)**: `docs/verifications/2026-07-12/writing_generation.md`(매트릭스 10행+mutation 6종 bite). 자체 회귀 +19(prompt 조립 4·generate 7·HTTP 8). 비차단 H2(504/502 orchestration 브랜치) HTTP +2 보강(504 mutation bite). H1(self_reported forward-defense)·H3(평문 live)·H4(budget 1차원) 코드 무변. 실 LLM live smoke는 후속.
- **Review Inbox 액션 어포던스(v1.6.67) 오너 독립 검증 합격(조건 없음)**: `docs/verifications/2026-07-12/review_inbox_affordances.md`. 매트릭스 11행 전수 잠금, 자격 변형 4종 bite. 자체 회귀 +7(pure 자격 5 + HTTP envelope 2).
- **candidate edit B1 closure 재검증 합격(조건부→승격)**: `docs/verifications/2026-07-12/candidate_edit_b1_closure.md`. 비차단 hardening 보강: confirm/reject 테스트에도 conflict-queue `status is RESOLVED/DISMISSED` over-strict assertion 추가(3개 review action 일관 잠금, mutation bite 재실증). 어포던스 `reason`은 display text·machine contract는 `action`+`eligible`임을 SoT/브리프 명시(H2).
- **candidate edit(v1.6.66) 오너 독립 적대적 검증 조건부 합격 → B1 closure**: `docs/verifications/2026-07-12/candidate_edit_backend.md`. 매트릭스 13행 lock list + 6 mutation. 차단 B1(행 7 "conflict resolve(dismiss 아님)" over-strict 미잠금 — `list_open()==()`가 RESOLVED/DISMISSED 구분 못 함)을 `queue.get().status is RESOLVED` assertion으로 보강(M3 재bite 실증, 구현 무변). 자체 회귀 +15(양방향 guard). 비차단 H1(2-연산 원자성 live 후속)·H2(409/400 우선순위)·H3(in-memory duplicate) 코드 무변. 독립 재판정은 후속 verifier.
- **Gate finding 독립 감사 조건부 합격 후 차단 조건 코드 폐쇄**: `docs/verifications/2026-07-12/gate_finding_persistence.md`의 §9.5 candidate action 분리·§9.6 inbox items 불변 회귀를 추가했다. 권장 404와 Mongo adapter index/round-trip도 보강했다. 독립 재판정은 후속 verifier 대상.
- **Review Inbox 독립 적대적 검증 PASS(조건 없음)**: `docs/verifications/2026-07-12/review_inbox_backend.md`. Obs1 실제 Core SOT source_ref의 resolved pointer 전체 envelope, Obs2 direct-promoted+needs_review candidate 억제를 HTTP 회귀로 보강했다.
- **(c-2) 오너 독립 적대적 검증 PASS(조건 없음)**: `docs/verifications/2026-07-12/character_homonym_reconciliation.md`. Obs1 전체 suite 결과를 정정했고, Obs2 reconcile HTTP 200/404/409 영속 회귀 4개와 Obs4 `MemoryError`/`InvalidCandidateStateTransition` 명시 409 매핑을 보강했다.
- **(c) character 별칭 semantic 라이브 관통(2026-07-12, 풀스택 머신) — PASS**: `scripts/phase2b7_character_alias_live_smoke.py` — Mongo promote→worker drain→실 Chroma `memory_vectors`→compare alias(실 BGE-m3-ko + Chroma query_similar). `철수`(별칭)→`conflict`+canonical id·`영희`(타인)→`create`, threshold 0.7, `memory_backend: chroma+elasticsearch`, self-cleaning. **관통·wiring 검증이며 라벨 정확도·threshold 실값 캘리브레이션은 후속**(D5/D7).
- **(c) 오너 독립 적대적 검증 PASS(조건 없음)**: `docs/verifications/2026-07-12/character_alias_semantic.md` — 경계 매트릭스 10행 빈 cell 없음(직접 5+위임 5), judge 우회·apply review_queue 전달·scope character 한정 재도출 확인, pytest 755/48 독립 재현. 비차단 관찰 4건 중 **Obs2(alias self-exclusion)·Obs4(scope-less 미영향)를 회귀로 실경로 보강**(mutation bite 재실증, 757 passed); Obs1(SoT 산문 순서, 기존 부채)·Obs3(rationale id, 계약 미고정)은 코드 무변.
- **live 관통 검증(2026-07-12, 풀스택 머신) — 2배치**:
  - **인덱싱 4종**: 2B.5 memory reindex·⑤ §8 ES lexical/hybrid·b-2 candidate 색인·Phase 3B archive→실 Chroma delete(DRAFT+PROJECT 2분기)를 실 Mongo·Chroma·ES(nori)·embedding 위에서 전부 PASS(`docs/verifications/2026-07-12/indexing_live_smokes.md`). **오너 독립 적대적 감사 PASS(조건 없음)**(`indexing_live_smokes_independent_audit.md`) — 비차단 관찰 3건 보강(PROJECT_ARCHIVED live 승격 포함). 신규 smoke 2개(candidate·archive) 프로덕션·계약·SoT 무변.
  - **실 LLM(12B) 경로 4종**: Phase 2A provider 추출·2B.3.2 compare judge·Phase 4 planner·Phase 4 deployed e2e를 in-stack llama(GPU) 위에서 wiring PASS(`docs/verifications/2026-07-12/llm_path_live_smokes.md`). **오너 독립 적대적 감사 PASS(조건 없음)**(`llm_path_live_smokes_independent_audit.md`) — 비차단 관찰 3건(gateway 컨테이너 관통 편중·라벨 품질 live 미검증·비결정성) 검증 기록에 보강(코드 무변). compare judge J1 프롬프트 판별은 튜닝 과제로 분리(신호만 기록). v1.6.61 독립 검토 CONDITIONAL PASS → 최종 합격(`docs/verifications/2026-07-11/candidate_state_transition.md`), 비차단 관찰 2건 closure(Obs1 rejected→needs_review 거부 테스트·Obs2 CANDIDATE_REMOVED routing 테스트+docstring). mutation 3종 양방향 bite(불법 전이·idempotency·routing, cp 복원). v1.6.60 독립 검토 조건부 합격 → 최종 합격(`docs/verifications/2026-07-11/canonical_candidate_dedup.md`), 비차단 관찰 I1~I3 closure.
  - 종전 3 환경-의존 failed(`ConnectElasticsearchTest`, b-5 도입)는 2026-07-10 skip guard(`@unittest.skipUnless(importlib.util.find_spec("elasticsearch"))`)로 해소 — 패키지 없는 sandbox에선 skip(45→48), 있는 환경에선 종전대로 3개 실행(회귀 잠금 유지). 테스트 전용 수정, 계약·프로덕션 코드 무변. **양방향 실증됨**(2026-07-10 검증 보강): 패키지 격리 설치(PYTHONPATH 주입, 시스템 무오염) 시 전체 스위트 **707 passed/45 skipped**로 3개 실행·PASS, 미주입 시 704/48로 원상복구.
  - `--ignore=tests/test_memory_mongo.py`는 프로젝트 검증 관례다(해당 4개는 사전-존재 localhost Mongo env artifact이며 코드 회귀가 아니다).
  - skip 45개는 live Mongo/Chroma/embedding 미가용 통합·smoke 테스트(sandbox 밖에서 실행).
- live/배포 검증 이력은 `docs/verifications/YYYY-MM-DD/`에 있다. 각 slice의 자체 회귀·mutation 재실증 상세는 `docs/daily_logs/YYYY-MM-DD/work_log.md`에 있다.

## Project Structure

```text
docker-compose.yml               # base: application + Mongo replica set + gateway(외부 llama 클라이언트) + embedding + chroma + elasticsearch(nori) + worker + frontend(nginx, v1.6.94)
docker-compose.llama.yml         # opt-in override: in-stack llama.cpp GPU 서버(server-cuda, -hf 12B QAT, port 9080)
.dockerignore / .gitignore
docs/
├── runbooks/local-llama-server.md   # 로컬 llama.cpp GPU 서버 opt-in 기동/설정/smoke runbook
├── README.md                    # 문서 분류와 진입점
├── system-contract-sot.md       # 정본 계약 SoT(Approved, v1.7.13)
├── abstract.md / *.md           # 보존된 아이디에이션 원본과 주제별 상세
├── plans/                       # 계획 + 착수 결정 브리프(README 인덱스)
│   ├── README.md · 00-foundations.md · implementation-plan.md
│   ├── 01-core-sot.md ~ 06-review-ui.md   # Phase별 계획
│   ├── flat-loop-gate.md · llm-gateway.md · gemma4-reuse.md · product-shell.md · analysis-memory-taxonomy.md
│   ├── 02-* / 02b-* / 03-* / 04-*-decisions.md   # slice별 착수 결정 브리프(대부분 Resolved)
│   └── frontend-*-decisions.md · product-readiness-backlog.md   # frontend 착수 fork + trigger 기반 횡단 backlog
├── benchmarks/2026-06-30 … 2026-07-15/   # Gemma Q4 budget·Writing loop·Gate quality live 근거
├── daily_logs/2026-06-24 … 2026-07-19/work_log.md   # 상세 이력
└── verifications/2026-06-24 … 2026-07-19/           # 독립 검증 기록
schemas/
└── writing-workspace-v2-w0.schema.json   # v1.7.10 W0 public contract schema catalog
frontend/                        # Product shell SPA(v1.6.99, React+TS+Vite+Declarative BrowserRouter). 소스는 저장소 루트에 두고
│                                #   Dockerfile만 services/frontend/에 둬 다른 서비스와 자리를 맞춘다.
├── package.json                 # scripts: dev · build(tsc --noEmit && vite build) · test(vitest) · gen:api
├── vite.config.ts               # dev /api proxy(VITE_API_TARGET, 기본 localhost:8000) + vitest(jsdom) 설정
├── nginx.conf                   # 배포 서빙: /api/ → application:8000(trailing slash prefix strip) + SPA try_files
└── src/
    ├── App.tsx                  # `/` 프로젝트 + drafts + overview + review + editor route spine
    ├── api/client.ts            # 생성 OpenAPI 타입을 소비하는 얇은 fetch 래퍼. API_BASE="/api". Writing 호출(accept partial 정규화 + revise-and-gate outcome, v1.7.3) + Review Inbox 호출(list/detail/confirm/reject/resolve/dismiss v1.7.4 + edit/reconcile v1.7.5, 손선언 타입)
    ├── drafts/DraftList.tsx     # 선택 프로젝트의 원고 목록/생성 + editor deep link + 검토함 진입 링크
├── drafts/DraftEditor.tsx   # v1.7.11 split workspace: editor + query-backed right rail + exact version/offset source jump
    ├── writing/WritingPanel.tsx # v1.7.3 C1+C2: generate→Gate→eligible bounded loop→pass accept, 6 status/stages, partial candidate 보존·5xx retry, exact-body accept intent
    ├── api/schema.d.ts          # gen:api 생성물(50 endpoint; 척추 14 + Writing 4[v1.7.1] 응답 typed). 커밋 대상
    ├── projects/ProjectList.tsx # 프로젝트 목록/생성 + 상세 route 진입
    ├── projects/ProjectOverview.tsx # v1.7.13 ProjectBrief onboarding/clear + canonical-only overview/pending 분리
└── review/                  # B Review Inbox + WorkspaceReviewPanel(v1.7.11 editor rail: query detail/source restore, confirm/reject, exact source callback)
                                 #   ReviewInboxDetail은 candidate edit + conflict merge/split full surface 유지. 어포던스 순수 소비(자격 미재계산)
services/
├── frontend/Dockerfile          # v1.6.94 D2=B: node build(npm ci→npm run build) → nginx:1.27-alpine 서빙 2-stage
├── elasticsearch/Dockerfile     # 배포 ES = 공식 ES 8.13.4 + analysis-nori(v1.6.53); compose build 컨텍스트
├── llm_gateway/app/             # main(shell) · payload · provider · errors · transport · client · httpx_transport
├── embedding/app/main.py        # FastAPI /embed·/health, dragonkue/BGE-m3-ko lazy 로드
└── application/app/
    ├── main.py                  # FastAPI shell + 전 도메인 HTTP wiring
    ├── core_sot/                # models · splitter · repository · mongo_repository · service
    ├── analysis/                # models · schema · extractor · runner · source · service · repository · mongo_repository
    │                            #   + compare(+ character 별칭 alias_matcher seam, v1.6.62) · compare_judge · semantic_matcher(2B.6, 별칭도 재사용) · apply(2B.4) · review_queue(+mongo, 2B.4 후속 v1.6.59) · candidate_review(Phase 6 상태 전이, v1.6.61)
    ├── memory/                  # 2B.1 canonical store: models · scope · service · repository · mongo_repository
    ├── indexing/               # models · embedding · chroma · memory_index(2B.5/6) · memory_lexical_index(§8 ES) · candidate_index(b-2 vector) · candidate_lexical_index(b-2 ES) · service · mongo_repository
    ├── context_search/          # models · planner · service(⑤ canonical+candidate memory retriever/gate + 승격 dedup resolver, (e) v1.6.60)
    ├── agent_loop/              # decision · budget · registry · parser · completion · resolution · runner
    └── writing/                # Phase 5: models/prompt/service + gate/report/revise/retrieval/accept
    │                            #   + revise_gate(bounded loop) · metering(v1.6.80 aggregate usage) · loop_audit(+mongo) · audit_hash
    │                            #   + gate_live_diag(v1.6.82 operator-only raw Gate diagnostics D1=A)
    │                            #   + report_live_diag(v1.6.84 operator-only raw report diagnostics D1=A, first+repair capture)
    │                            #   + json_extract(v1.6.85 공유 strip_code_fence — gate/report fence 추출)
    │                            #   + per_stage_measure(v1.6.87 M-i per-stage 비용 측정 코어 — synthetic retrieve_more Gate, context_search token 제외, no-write)
    │                            #   + gate_quality(v1.6.90 7-case labelled Gate quality benchmark core, no-write)
    │                            #   + context_pointer(v1.6.92 stable pointer projection·package allowlist·wire — P-i origin 불변식 테이블)
    │                            #   + http_models(v1.7.1 C0 Writing HTTP 응답 계약 — 성공 response_model + partial responses={} + exact-key)
tests/                           # 91개 test 모듈(도메인별 회귀 + live/smoke skip-aware) + fixtures/core_sot.py
scripts/
├── dump_openapi.py              # v1.6.94 read-only OpenAPI 덤프(create_app() 조립만, 요청 처리·연결 없음) → frontend gen:api
├── smoke_llm_provider.py · benchmark_llm_provider.py
├── phase2a_*_smoke.py · phase3a_rebuild_source_block_index.py(CLI rebuild) · phase3a_*_smoke.py · phase4_context_search_*_smoke.py · phase4_lexical_memory_live_smoke.py
├── index_sync_worker.py         # 3B archive + 2B.5 memory reindex + b-2 candidate 색인 drain (--loop compose 서비스, b-6 증분1)
├── phase2b3_compare_judge_live_smoke.py · gateway_generate_live_smoke.py(gateway 컨테이너 /v1/generate 직접, 2026-07-12 신규)
├── phase2b7_character_alias_live_smoke.py   # (c) character 별칭 alias→conflict 라이브 관통(실 embedding+Chroma, self-cleanup, 2026-07-12 신규)
├── phase2b_candidate_index_live_smoke.py · phase3b_archive_chroma_live_smoke.py   # b-2 candidate·3B archive live 관통 smoke(2026-07-12 신규, Mongo doc self-cleanup)
├── benchmark_writing_loop.py   # B2b deployed Writing loop benchmark(v1.6.81) + 합성 코어 worst_case_stage_counts/compose_worst_case_ceiling(v1.6.86 Option A) + diagnose_writing_gate.py(v1.6.82 D1=A operator-only raw Gate diagnostics) + diagnose_writing_report.py(v1.6.84 D1=A raw report diagnostics)
├── measure_writing_stages.py   # v1.6.87 M-i per-stage 비용 측정 CLI(production seam 조립·실 gateway 격리 측정→compose_worst_case_ceiling, no-write, JSON 출력)
├── benchmark_writing_gate.py   # v1.6.90 production Gate 7-case labelled quality benchmark(repeats, fail-closed JSON, no-write)
└── phase2b5_reindex_memory.py · phase2b5_reindex_candidate.py(둘 다 vector+lexical backfill, v1.6.58) · phase2b5_memory_reindex_live_smoke.py
```
