"""Authentication and user ownership (multi-user expansion).

Slice order per docs/plans/multi-user-auth-cms-decisions.md D8. This first slice
adds user + password storage only; authorization enforcement is a later slice, so
existing endpoints are unchanged until then.
"""
