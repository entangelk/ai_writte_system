# 시스템 정본 계약 SoT

상태: `Approved`
계약 버전: `v1.7.32`
승인일: `2026-06-26`
최근 갱신일: `2026-07-23`
목적: 흩어진 계획 문서의 확정된 계약과 서비스 경계를 한 곳에서 추적한다.  
적용 범위: 제품 경계, 서비스 책임, 데이터 정본, Gateway, AgentLoopRunner, Gate 합성, 검증 기록.

이 문서는 세부 Phase 계획을 대체하지 않는다. 대신 구현자가 먼저 확인해야 할 **정본 계약 인덱스**다. 아래 계약과 세부 계획이 충돌하면 이 문서의 [문서 우선순위](#문서-우선순위)에 따라 조정한다.

## 문서 우선순위

1. 사용자가 명시 승인했고 이 문서 또는 해당 Phase 계획에 반영된 결정
2. `Approved` 상태의 본 SoT와 Phase 계획
3. `Draft` 상태이지만 구현·검증·커밋으로 잠긴 계약 문서
4. `docs/plans/`의 미구현 Phase 계획
5. `docs/` 루트의 아이디에이션 문서

동일 우선순위 문서끼리 충돌하면 구현하지 않는다. 충돌을 작업 로그와 해당 문서에 기록하고 사용자에게 어느 쪽이 canonical인지 확인한다.

이 문서의 `Approved` 상태는 정본 계약 인덱스와 문서 우선순위의 승인을 뜻한다. 아래 "미확정" 항목은 이 승인으로 확정되지 않으며, 사용자 결정 또는 별도 승인된 Phase 계획 없이는 추측해 구현하지 않는다.

## 계약 버전 관리

- 이 문서의 계약 버전은 사용자 결정으로 정본 계약, 문서 우선순위, 서비스 경계, public literal, 미확정 항목의 상태가 바뀔 때 올린다.
- 호환 가능한 명확화, 링크 보정, 검증 근거 추가는 patch 수준으로 기록한다.
- 구현 계약이나 public literal 의미가 바뀌면 minor 이상으로 기록하고, 영향받는 계획 문서·테스트·검증 기록을 함께 갱신한다.
- 기존 승인 결정과 충돌하는 새 요청이 들어오면 구현 전에 충돌을 명시하고 어느 쪽이 canonical인지 사용자에게 확인한다.
- 상세 작업 이력은 `docs/daily_logs/`에 남기고, 이 문서에는 현재 정본에 영향을 주는 변경만 기록한다.

## 계약 변경 이력

| 버전 | 날짜 | 변경 | 근거 |
|---|---|---|---|
| v1.7.32 | 2026-07-23 | **H3 에러 응답 계약 S4 — memory/source 트랙 7 endpoint 에러 선언(D2=A/D1=A)**. S2(CRUD 20)·S3(analysis 21)에 이어 memory read 2 · snapshot source-ref 3(create/list/get) · index rebuild 1 · context-search 1 = **7**의 realistic 에러 상태를 `responses=`로 선언한다. **이 슬라이스가 새로 선언하는 것은 504**다 — `context-search`는 writing 트랙 밖에서 **자기 예산을 소진할 수 있는 유일한 endpoint**(`ContextSearchBudgetExceeded`)라, 504가 **writing 밖 endpoint로는 처음** 선언된다. (504 선언 자체가 처음인 것은 아니다 — `writing/accept`·`writing/revise-and-gate`는 H3 이전부터 에러 본문 모델과 함께 504를 선언해 왔다.) 신규 공유 상수는 `_ERRORS_400_404_502_504_CONFIG`(context-search) **1종**뿐이고 나머지 6 endpoint는 기존 상수(`_ERRORS_404` 5 · `_ERRORS_400_404` 1)를 재사용한다. context-search의 503은 **구성 얼굴**(`"context search service is not configured"`)이라 S3의 `_CONFIG_503`을 그대로 쓰며, 회귀가 `_MIGRATION_503` 문안 차용을 차단한다. **런타임 무변**(상태코드·`detail`·분기·프론트 에러 UX 전부 무변, D4=A). 회귀 신규 9(+19 subtest): `MemorySourceErrorContractDeclarationTest`(7행 exact lock 양방향 · **트랙 전수 선언 가드**[`/memory`·`/snapshots/`·`/source-refs`·`/context-search` 경로 중 lock 리스트 밖 operation이 0] · 본문 단일 모델 12 · context-search 503 구성 얼굴 문안) + `MemorySourceErrorBodyExactKeyTest`(404·400 wire 본문 `{detail}` 단일 키) + **`ContextSearchErrorBodyExactKeyTest`**(502·503·**504** 본문 — 이 셋은 planner/clock 픽스처가 필요해 `test_context_search_api.py`의 기존 하네스 옆에 두고 중복 구축을 피했다). mutation 4종 실증(선언 삭제·안 던지는 409 추가·503 문안 차용·미선언 신규 트랙 endpoint 각각 해당 회귀만 bite). backend **1437 passed/1 skipped/462 subtests**(전용 test-mongo 27020 RS), frontend **194 passed/13 files**(무변), `gen:api` **+108행/-0행 순수 additive**, `tsc` clean, build JS 399.03 kB. LLM 미사용. **다음 = S5**(writing 잔여 + `start_next_unit` 503 방어 — 동적 `ProviderError` 매핑 9곳이 전부 그 구역에 있어 "realistic 집합만 선언" 스코프가 처음 실제로 발화한다). | 오너 결정 D1~D4=A(`plans/api-error-response-contract-decisions.md` S4 행), `services/application/app/main.py`, `tests/test_application_api.py::MemorySourceErrorContractDeclarationTest`·`MemorySourceErrorBodyExactKeyTest`, `tests/test_context_search_api.py::ContextSearchErrorBodyExactKeyTest`, `frontend/src/api/schema.d.ts`, `daily_logs/2026-07-23/work_log.md` |
| v1.7.31 | 2026-07-23 | **H3 에러 응답 계약 S3 — analysis 트랙 21 endpoint 에러 선언(D2=A/D1=A)**. S2의 CRUD family에 이어 analysis 구역(jobs 7·candidates 4·compare/apply/context·review-queue 2·review-inbox 2·gate-findings 4 = **21**)의 realistic 에러 상태를 `responses=`로 선언한다. 이 트랙이 S2와 다른 점은 **실패 어휘**다: **502**(upstream provider/LLM)와 **503의 협력자-미구성 얼굴**이 writing endpoint 밖에서 처음으로 선언 표면에 나타난다. S1이 명문화한 "503의 두 얼굴"에 따라 **무결성 얼굴과 구성 얼굴의 description을 분리**한다 — 신규 `_CONFIG_503`은 "이 배포에 협력자가 구성되지 않았다, 배포 환경에서 구성하라, 재시도만으로는 성공할 수 없다"를 명시하며 `_MIGRATION_503`의 `scripts/migrate_ordered_units.py` 문안을 **쓰지 않는다**(회귀가 두 문안의 혼입을 양방향으로 잠근다). 구성 얼굴은 런타임 `detail`이 이미 어떤 협력자인지 이름을 대므로 endpoint별 문안을 나누지 않고 상수 1개를 공유한다. 신규 공유 상수는 `_CONFIG_503`·`_ERRORS_404_502_CONFIG`(compare)·`_ERRORS_400_404_409_502_CONFIG`(run) 3종이고 **나머지 19 endpoint**는 S2 상수(`_ERRORS_404` 11·`_404_409` 6·`_400_404_409` 1·`_400_404` 1)를 재사용한다. **브리프 스코프 유지**: endpoint별 realistic 집합만 선언하고 동적 `ProviderError` 매핑의 가능 코드를 전부 열거하지 않는다(analysis 구역의 `ProviderError`는 `run`·`compare` 두 곳 모두 명시 502 분기라 동적 열거 문제가 실제로는 발생하지 않음을 실코드에서 확인). **런타임 무변**(상태코드·`detail`·분기·프론트 에러 UX 전부 무변, D4=A) — 이 구역은 `response_model`이 없는 무타입 endpoint가 대부분인데 **성공 모델 부착은 하지 않았다**(H1 잔여 범위, 섞으면 silent field loss 위험이 이 페이즈로 들어온다). 회귀 신규 9(+59 subtest): `AnalysisErrorContractDeclarationTest`(21 endpoint 선언 집합 exact lock 양방향 · **트랙 전수 선언 확인**[lock 리스트에 없는 `/analysis/` endpoint가 0이어야 함 — 신규 endpoint가 선언 없이 실리는 것을 차단] · 본문 단일 모델 · config 503 description 문안) + `AnalysisErrorBodyExactKeyTest`(404/409/400/**502**/**503** 실제 wire 본문이 정확히 `{detail}` 단일 키). mutation 4종 실증(선언 삭제·안 던지는 504 추가·본문 모델 교체·미선언 신규 endpoint 추가 각각 해당 회귀만 bite). backend **1428 passed/1 skipped/443 subtests**(전용 test-mongo 27020 RS), frontend **194 passed/13 files**(무변), `gen:api` **순수 additive**(+324행/-0행), `tsc` clean, build JS 399.03 kB. LLM 미사용. **다음 = S4**(memory/source). | 오너 결정 D1~D4=A(`plans/api-error-response-contract-decisions.md` S3 행), `services/application/app/main.py`, `tests/test_application_api.py::AnalysisErrorContractDeclarationTest`·`AnalysisErrorBodyExactKeyTest`, `frontend/src/api/schema.d.ts`, `daily_logs/2026-07-23/work_log.md` |
| v1.7.30 | 2026-07-23 | **H3 에러 응답 계약 S2 — CRUD family 20 endpoint 에러 선언(D2=A/D1=A)**. S1이 정본화한 전역 의미론을 첫 endpoint 집합에 적용한다. **H1 spine 14**(projects 5·drafts 5·versions 4) **+ CRUD 형제 6**(brief 4·draft-order·project-export) = **20**의 realistic 에러 상태를 `responses=`로 선언해, 지금까지 런타임에는 정확히 반환되지만 공개 계약에는 없던 404·409·400·503이 OpenAPI와 프론트 생성 타입에 나타난다. 선언은 공유 상수 7종(`_ERRORS_404`/`_400_404`/`_404_409`/`_400_404_409`/`_404_MIGRATION`/`_400_404_MIGRATION`/`_404_409_MIGRATION`, `main.py`)으로 조립하며 모두 단일 `ErrorDetailResponse`를 참조한다(D1=A 균일 본문). **CRUD family의 503은 전부 데이터 무결성 얼굴**(`DraftOrderIntegrityError`: drafts list/create·project export 3곳)이라 선언에 **`scripts/migrate_ordered_units.py` 실행이 해결책임을 description으로 명시**한다 — "로그에서 유추"를 없애는 것이 이 페이즈의 목적이므로 코드만 적어 두지 않는다. `POST /projects`·`GET /projects`는 도메인 에러가 없어 선언 대상이 아니다(422는 FastAPI 자동, 전역 정책상 계약 밖). **런타임 무변**: 상태코드·`detail`·분기·서비스 경계 어느 것도 바뀌지 않고 선언만 추가된다(D4=A, 프론트 에러 UX도 무변). 회귀 신규 7(+53 subtest): `CrudErrorContractDeclarationTest`(20 endpoint 선언 집합 exact lock — under-strict[선언 누락]·over-strict[안 던지는 코드 선언] 양방향 · 선언된 모든 에러 본문이 `ErrorDetailResponse` 단일 참조 · migration 503 description이 운영 조치를 명시) + `CrudErrorBodyExactKeyTest`(404/409/400/503 실제 wire 본문이 정확히 `{detail}` 단일 키 — 후속 `reason`은 drift가 아니라 명시 결정으로만 들어온다). mutation 3종 실증(선언 삭제·허위 코드 추가·본문 모델 교체 각각 해당 회귀만 bite). backend **1379 passed/41 skipped/384 subtests**(`CORE_SOT_TEST_MONGO_URI=…:27018`), frontend **194 passed/13 files**(무변), `gen:api` **순수 additive**(+270행/-0행), `tsc` clean, build JS 399.03 kB. LLM 미사용. **다음 = S3**(analysis 트랙). **오너 독립 검증 PASS(조건 없음)** — `verifications/2026-07-23/h3_error_response_contract_s1_s2.md`; 비차단 H-1(문서 오기)·H-2(reorder 비대칭 근거 정밀화, 위 절에 반영)·H-3(migration description 3번째 endpoint 직접 pin, subTest 2→3) 반영, H-4(AST 자동 대조)는 현 규모에서 과설계라 skip. | 오너 결정 D1~D4=A(`plans/api-error-response-contract-decisions.md` S2 lock 리스트 20), `services/application/app/main.py`, `tests/test_application_api.py::CrudErrorContractDeclarationTest`·`CrudErrorBodyExactKeyTest`, `frontend/src/api/schema.d.ts`, `daily_logs/2026-07-23/work_log.md` |
| v1.7.29 | 2026-07-23 | **H3 에러 응답 계약 S1 — 전역 HTTP 에러 계약 섹션 신설(D1=A/D3=A, 문서 전용)**. H1 성공 `response_model`(v1.6.95)·H2 입력 검증이 남긴 **에러 응답**의 전역 의미론을 "확정된 전역 계약"에 정본화한다. **에러 본문은 균일한 단일 키 `{"detail": <string>}`**(`ErrorDetailResponse`)이고 계약은 **3층**(상태코드=기계용 의미론 · `detail`=사람용 메시지 · 후속 `reason`=기계용 코드)이며 **`detail` 문자열 자체는 계약이 아니다**(클라이언트 패턴 매칭 금지, 기계 분기가 필요하면 `reason`을 additive로). 400/404/409/422/502/503/504와 동적 `ProviderError` 매핑의 의미론을 표로 고정하되, **endpoint별 realistic 코드 집합은 OpenAPI `responses=`가 기계적 진실**이고 SoT는 endpoint×코드 표를 중복 유지하지 않는다(drift 방지, 검증은 `dump_openapi.py` 재덤프 self-discovery). **503의 두 얼굴**을 명문화: (1) 협력자 미구성(`"<X> is not configured"`, 배포 구성 문제) (2) 저장 데이터 마이그레이션 필요(`DraftOrderIntegrityError` — list/create/project-export 3곳 방어, 해결책은 `scripts/migrate_ordered_units.py`). 공통 규칙 = "요청을 고쳐 성공시킬 수 없고 서버 측 조치가 선행돼야 한다". **`InvalidDraftOrder`의 의도적 비대칭**(reorder는 요청이 전체 순열을 실어 오므로 409 유지)과 **알려진 결손 `start_next_unit` 500 누수**(accept 경로에 `DraftOrderIntegrityError` 절 없음 → H3 S5에서 503으로 폐쇄)도 함께 명문화해 "로그에서 유추" 갭을 닫는다. 422는 FastAPI 자동 생성이라 본문 형태가 다르고 이 계약 밖이다. **런타임·OpenAPI·코드 무변**(문서 전용 슬라이스, `responses=` 선언은 S2부터). 실측 재도출: endpoint 61, 404×62·400×30·502×20(19 raise + 1 partial JSONResponse)·503×18(구성 15 + 무결성 3)·409×16·504×7·동적×9. **오너 독립 검증 PASS(조건 없음)** — `verifications/2026-07-23/h3_error_response_contract_s1_s2.md`(S1+S2 합동); 비차단 H-2로 reorder 비대칭의 **근거 문안**을 정밀화했다(무결성 face는 순열 검사 이전 `_require_ordered_drafts`에서 발화하므로 409는 "입력으로 판별돼서"가 아니라 기존 절 흡수의 무변경 결과 — **행동·선언은 무변**). | 오너 결정 D1~D4=A(`plans/api-error-response-contract-decisions.md`), 독립 검증 `verifications/2026-07-22/h3_error_response_contract_plan.md`(F1/F2/F3 반영), `services/application/app/main.py`·`core_sot/service.py`(의미론 재도출 1차 소스), `daily_logs/2026-07-23/work_log.md` |
| v1.7.28 | 2026-07-22 | **비동기 생성 job 재시도 endpoint(async-pad D4=A 재시도 UI 슬라이스)**. 증분 2b에서 "재시도 UI 슬라이스로 지연"했던 `FAILED→PENDING` 전이를 그 전이를 구동하는 endpoint와 함께 구현한다(dead 분기 방지 원칙). 신규 `POST .../writing/generation-jobs/{job_id}/retry`(200 + `WritingGenerationJobPayload`)가 **FAILED job을 PENDING으로 되돌려** failure_reason/detail·claim lease를 지우고, 생성 worker의 claim 루프가 이를 다시 집어 재실행한다. **Analysis retry endpoint 미러이되 별도 `run` 호출이 없다** — Analysis는 retry 후 프론트가 `run`을 POST하지만, 생성 worker는 PENDING을 자동 claim하므로 retry만으로 재실행된다. **FAILED만 재시도 가능**하고 그 외 상태(pending/running/succeeded)는 `InvalidJobStateTransition`→**409**, 미발견·타 프로젝트는 **404**다. `generation_job.py` `_ALLOWED_TRANSITIONS`에 `(FAILED,PENDING)` 추가 + `mark_pending_for_retry` 서비스 메서드(failure/claimed_at clear). **main.py 주의**: `InvalidJobStateTransition`이 analysis·writing 두 모듈에 각각 존재하므로 retry endpoint는 writing 것을 별칭(`InvalidGenerationJobStateTransition`)으로 import해 catch한다. 프론트 결과 패드는 실패 job에 "다시 시도" 버튼(`onRetryFailed`→`client.retryGenerationJob`)을 노출하고, 성공하면 훅이 job을 PENDING으로 되돌려 폴링을 재개한다. backend **1322 passed/73 skipped/328 subtests**(신규 10: 서비스 RetryTest 5[failed→pending clear·재claim 가능=under-strict·pending/running/succeeded 거절=over-strict] + mongo retry round-trip 1 + endpoint RetryTest 4[200 failed·409×3 subtest·404 unknown·404 wrong-project]), frontend **192 passed/13 files**(신규 4: 훅 retry 3[reset→재개·retry 실패 시 failed 유지·비-failed no-op] + GenerationPad "다시 시도" 버튼 1), `gen:api`에 retry path 1개 additive(49줄, 0 삭제), tsc clean, build 103 modules(JS 398.69 kB). LLM 미사용(단위). | 오너 지시(재시도 UI 진행), `plans/async-generation-pad-decisions.md` (D4=A "orphan/retry Analysis 계약 재사용"), `services/application/app/writing/generation_job.py`·`main.py`, `frontend/src/writing/{useGenerationJobs,GenerationPad}.tsx`·`api/client.ts`, `tests/test_writing_generation_job.py::RetryTest`·`test_writing.py::WritingGenerationJobRetryTest`, `daily_logs/2026-07-22/work_log.md` |
| v1.7.27 | 2026-07-21 | **비동기 생성 + 결과 패드 슬라이스 증분 2c — generate endpoint 동기/비동기 분기(D5=A)**. 2b의 저장소·worker에 이어 **생산자(endpoint)를 배선**해 async 경로를 end-to-end로 개통한다. `POST .../writing/generate`가 `output_length`로 분기한다 — **`short`(1024)는 동기**(200 + `WritingCandidatePayload`, 무변), **`medium`(2048)/`long`(4096)은 job을 enqueue하고 `202 Accepted`로 `{job, idempotent_replay}`를 즉시 반환**(비블로킹; worker가 claim해 실행). **async 프리셋에 `current_position`이 없으면 400**(패드는 per-draft 키). 신규 `GET .../writing/generation-jobs/{job_id}`(200 + `WritingGenerationJobPayload`; 404 if not found/wrong project)이 상태 read(증분 3 폴링용)를 담당한다. **상태코드 202는 구현자 판단**: 200 + `response_model=WritingCandidatePayload` 정직성을 유지하면서 async envelope를 `responses={202: …}`로 문서화(revise-and-gate/accept의 분기 응답 메커니즘과 동형)하며 HTTP 의미론(202 Accepted = 백그라운드 처리 수락)에 부합. 대안 200+JSONResponse(response_model 거짓)·200+Union oneOf(선례 없음)은 기각. endpoint는 async 분기에서 writing/context_search/scratch를 부르지 않고(전부 worker 역할) 동기 전용 503 검사도 우회한다. enqueue는 `(project_id, request_id)` 멱등(재POST → 같은 job, `idempotent_replay=true`). endpoint 배선 forward-marker를 현재형으로 전환하고, "status는 항상 candidate"를 **동기 후보 응답 한정**으로 정밀화(비동기는 candidate가 아닌 job을 반환). 프론트 `WritingPanel.runGenerate`는 `"job" in produced` 가드로 async를 감지해 candidate/gate/루프를 건너뛰고 "백그라운드 생성 시작" notice만 표시(패드 + 5초 폴링은 증분 3). backend **1312 passed/73 skipped/325 subtests**(신규 16: async 분기 14 + envelope-key 2 + preset 테스트 2c 동기/비동기 재작성), frontend·gen:api는 async 응답 additive. LLM 미사용(단위). | 오너 결정(D5=A, `plans/async-generation-pad-decisions.md`; 상태코드 202는 구현자 판단), `services/application/app/writing/http_models.py`·`main.py`, `frontend/src/writing/WritingPanel.tsx`·`api/client.ts`, `tests/test_writing.py::WritingGenerateAsyncBranchTest`·`WritingGenerationJobEnvelopeKeyTest`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.26 | 2026-07-21 | **비동기 생성 + 결과 패드 슬라이스 증분 2b — 생성 worker 실행 루프(D3=B)**. 증분 2a(생성 job 저장소)에 이어, worker가 job을 claim해 실제로 실행하는 조각. **worker의 최초 LLM/gateway 호출**이다. 신규 `scripts/generation_job_worker.py`(색인 worker의 graceful-shutdown/`--loop`/JSON 이벤트 패턴 미러, **별도 compose 서비스** `generation_worker` — 색인 sync outbox와 무관) + `writing/generation_worker.py`(테스트 가능한 실행 코어 `execute_generation_job`). claim된 job을 동기 generate와 같은 파이프라인(ContextSearch build → WritingService.generate)으로 돌리고 결과를 scratch에 append(version_id=D7 포함) 후 `mark_succeeded(result_scratch_id=)`; 예외는 taxonomy대로 `mark_failed`. **검증 hardening 반영**: (H-2) 매핑 안 된 infra/버그 예외용 **catch-all `internal`** reason을 추가해 job이 RUNNING→lease 재claim→재실패 livelock에 빠지지 않게 종료 상태로 보냄; (H-3) reclaim 재실행 시 이전 scratch를 `request_id`로 지우고 다시 써 **결과 저장을 job 기준 멱등**으로. worker 조립은 `main.build_async_generation_collaborators()`(create_app과 같은 env 팩토리 재사용) + `_default_writing_generation_job_service()`. **scratch 용도 문구 확장(§261 (2) 비동기 결과 보관) — 증분 1에서 지연했던 유일한 개정을 여기서 반영**. **endpoint 배선(D5)은 증분 2c**라 현재 generate는 전부 동기이며 job을 enqueue하지 않는다(저장소·worker는 갖췄으나 생산자 없어 async 경로 dormant). backend **1295 passed/73 skipped/326 subtests**(신규 16: 실행 executor 성공/실패 매핑 7종+catch-all+H-3 멱등, worker 루프/CLI/gateway 게이팅). frontend·gen:api 무변(endpoint 미배선). LLM 미사용(단위) — 실 12B 라이브 스모크는 별도. | 오너 결정(2a/2b/2c 분할 + 다른 작업자 인계), `plans/async-generation-pad-decisions.md` (D3=B), `services/application/app/writing/generation_worker.py`·`generation_job.py`, `scripts/generation_job_worker.py`, `docker-compose.yml`, `tests/test_writing_generation_worker.py`·`test_generation_job_worker.py`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.25 | 2026-07-21 | **비동기 생성 + 결과 패드 슬라이스 증분 1 — scratch tier 패드 준비(D2=A/D7)**. 브리프 `plans/async-generation-pad-decisions.md`(D1~D7 확정, 2026-07-20)의 scratch 계약 개정 2건을 구현과 함께 정본화했다. **(D2=A) accept 정리를 draft 전체 삭제에서 채택 항목 단위로 축소**: 정본 version이 저장된 accept는 이제 그 accept의 `request_id`와 일치하는 scratch 항목만 정리하고, 같은 draft의 다른 생성 결과는 보존한다(대응 항목 없으면 no-op). v1.7.20 승격 당시 이 조항은 whole-draft 삭제였고 granularity를 문구로 명시하지 않았는데(rationale·구현에만 존재), v1.7.25가 항목 단위로 축소하며 명시한다(검증 H2). 근거: 원 rationale("정본 확정 → 미채택본 무의미")은 **채택된 항목에 대해서만** 참이고, 다른 생성 결과는 accept 뒤에도 복사·복구 가치가 있어 비동기 결과 패드가 재사용할 기반이 된다. **(D7) scratch schema에 nullable `version_id` 추가**: generate의 `current_position.version_id`에서 오며(없으면 None) 패드가 "이 version 기준으로 생성됨"을 표시할 근거다 — `intent`와 동형인 additive nullable seam이라 기존 레코드는 마이그레이션 없이 None으로 읽힌다. 정본(`source_snapshots`/`draft_versions`)·명시 save·stale guard·accept 원자성은 **무변**(패드는 accept를 타지 않는다). 비동기 실행(job 테이블·worker LLM 루프·1024 동기/2048·4096 비동기 분기)과 scratch 용도 문구 확장은 증분 2로 이어진다. backend **1257 passed/73 skipped/326 subtests**(scratch per-item 정리·no-op·version_id round-trip/legacy 회귀 추가), frontend **162 passed/11 files**(무변, `ScratchCandidate` hand-declared 타입에 `version_id` 미러 추가), `gen:api` byte-identical(scratch endpoint는 `response_model` 없음). LLM 미사용. | 오너 결정(D2=A/D7, `plans/async-generation-pad-decisions.md`), `services/application/app/writing/scratch.py`·`scratch_mongo.py`·`main.py`, `tests/test_writing_scratch.py`·`test_writing_scratch_mongo.py`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.24 | 2026-07-21 | **문체/분량 슬라이스 증분 3(2/2) — Gate `style` finding + 문체 우선순위(D5=A/D6=A)**. Writing Gate가 후보 산문을 저자의 ProjectBrief 문체 설정(tone·style_rules·preferred_patterns·forbidden_patterns·style_examples, v1.7.21)과 대조해 이탈 시 `style` finding을 낸다(`WritingGateFindingType`에 `style` 추가). **style은 advisory**: parse 경계에서 severity=warning·recommendation=needs_user_review로 고정(그 외는 `ValueError`)되고, **decision 우선순위 계산에서 제외**되어 findings가 style뿐인 후보는 여전히 `pass`다. 근거(D6): 어투 이탈은 저자가 알아차리면 충분하고 의도적 변경도 정당하므로 **차단하거나 재생성 루프를 태우면 안 된다** — 따라서 needs_user_review로 escalate해 accept를 막는 대신(그러면 재생성 강요) style을 decision에서 빼 `pass`를 유지하고 **최종 채택은 사용자**가 정한다(프론트 accept는 pass 조건, style advisory는 함께 표시). style은 auto-revise 대상 아님(continuity 전용, `_is_eligible_continuity_revise` 명시 잠금)·block 불가. **D6=A 우선순위**: `저자 설정 > canonical 관찰 > candidate 관찰`을 문체/어투에 적용, Gate는 설정 기준만 판정, 관찰→설정 자동 반영 없음(ProjectBrief append-only history가 저자 직접 수정 이력을 보존). 캐릭터 어투(D4 aspect) 설정 저장은 deferred라 이번엔 프로젝트 문체 설정 대조까지. Gate 프롬프트를 style 축으로 열되 advisory 규칙을 명시. backend **1250 passed/73 skipped/326 subtests**(신규 `GateStyleFindingTest` 5 + revise 회귀 style ineligible 케이스 1), frontend **162 passed/11 files**(WritingPanel +1: pass+style accept 유지·advisory 표시), `gen:api`는 `WritingGateFindingType` enum에 `style` 1개 additive, build 101 modules. LLM 미사용. **증분 3 전체(D4+D5+D6) 완료 → 문체/분량 슬라이스 종료.** | 오너 이어서 진행 지시, `plans/writing-style-and-length-control-decisions.md` (D5=A/D6=A), `services/application/app/writing/{models,gate,gate_prompt}.py`, `tests/test_writing_gate.py::GateStyleFindingTest`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.23 | 2026-07-21 | **문체/분량 슬라이스 증분 3(1/2) — character_observation optional aspect(D4=B, 오너=optional)**. `character_observation` payload가 캐릭터 어투를 식별하는 optional `aspect`(자유 문자열, 예: `voice`/`trait`)를 담을 수 있게 했다. 브리프 D4=B의 원 리터럴은 exact `(name, observation, aspect)` 필수였으나, 착수 시점 코드 스코핑에서 **candidate 생성 경로가 payload를 검증**(`record_candidate`→`validate_candidate_payload`)해 필수화하면 테스트 25개 파일 + 저장된 live candidate가 즉시 무효가 되고 마이그레이션이 필요함이 드러났고, 이 필드의 핵심 가치(캐릭터 어투 검증)는 브리프상 설정 저장이 deferred라 이번 증분에서 forward-defense임을 근거로 **오너가 optional을 선택**했다. `analysis/schema.py`가 required + optional 필드 모델로 바뀌어 character에 optional `aspect`를 허용하되(present 시 non-empty string, event/open_question엔 불허=알 수 없는 field), 기존 `{name, observation}`은 그대로 유효(**마이그레이션 불요**)다. taxonomy는 여전히 3종(2A D5=A 무변). 추출 프롬프트(extractor·prompt_templates)가 aspect를 optional로 안내하고, 저장된 payload는 `main.py`가 `dict(candidate.payload)`로 wholesale 직렬화하므로 review inbox/candidate detail/conflict diff에 **추가 코드 없이 surface**한다(색인·Gate 배선은 D5). backend **1245 passed/73 skipped/322 subtests**(신규 `CharacterAspectPayloadTest` 7: optional 하위호환·aspect 보존·free string·empty 거절·비-character 불허·기타 unknown 거절·required 누락 거절), frontend 무변, gen:api 무변(무타입 payload). LLM 미사용. **증분 3의 D5(Gate `style` finding)·D6(우선순위 계약)는 다음.** | 오너 결정(aspect=optional, exact-tuple 리터럴 대신 blast radius/deferred 근거), `plans/writing-style-and-length-control-decisions.md` (D4=B), `services/application/app/analysis/schema.py`, `tests/test_analysis_extractor_schema.py::CharacterAspectPayloadTest`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.22 | 2026-07-21 | **문체/분량 슬라이스 증분 2 — 생성 분량 프리셋(D3=A)**. 출력 길이를 서버 전역 고정값(`WRITING_GENERATE_MAX_TOKENS` 1024)에서 요청별 프리셋으로 승격한다. `POST .../writing/generate`에 신규 요청 필드 **`output_length=short\|medium\|long`**(생략 시 `short` — pre-slice 단일값과 동일해 하위 호환)을 노출하고 **서버가 프리셋→출력 토큰 매핑을 소유**한다(확정 기본 **1024/2048/4096**). 매핑은 env 조정 가능하며 fail-loud다(`WRITING_OUTPUT_LENGTH_SHORT\|MEDIUM\|LONG`, `short`는 미설정 시 기존 `WRITING_GENERATE_MAX_TOKENS`를 승계, 각 값이 1 미만이거나 정수로 파싱되지 않으면 기동 실패 — 증분 1 style-example 상한 선례 동형). 알 수 없는 프리셋은 조용한 기본값이 아니라 **400**이다. **`output_length`는 출력 토큰 축이며 기존 요청 필드 `max_tokens`(입력 ContextPackage 예산, 5개 endpoint 공유)와 독립**이다 — 의미·이름 무변. **`long`(4096, ≈91초)은 `WRITING_LOOP_MAX_WALL_CLOCK_MS=60000`을 초과하므로 단일 generate 전용**이다: `output_length`는 `/writing/revise-and-gate`의 요청 필드가 아니며(구조적으로 루프에 4096을 태울 수 없음), 프론트도 `long` 생성 뒤 자동 revise/gate 루프를 건너뛴다(Gate 지적은 작가가 직접 판단). 기존 loop 예산 10000tok/60000ms는 무변이다. backend **1238 passed/73 skipped/320 subtests**(신규 `WritingOutputLengthPresetTest` 9건), frontend **161 passed/11 files**(WritingPanel +2), `gen:api`는 generate 요청에 `output_length` 1개만 additive. LLM 미사용. 오너 독립 검증 PASS(조건 없음, `verifications/2026-07-21/increment2_d3_output_length_preset.md`) 후 비차단 hardening #1(short 경로 독립 잠금 회귀)·#2(비정수 env 기동 실패 명문화) 반영. | 오너 다음 작업 진행 지시, `plans/writing-style-and-length-control-decisions.md` (D3=A, 매핑 1024/2048/4096), `services/application/app/writing/models.py`·`main.py`, `tests/test_writing.py::WritingOutputLengthPresetTest`, `frontend/src/writing/WritingPanel.tsx`, `daily_logs/2026-07-21/work_log.md` |
| v1.7.21 | 2026-07-20 | **문체/분량 슬라이스 증분 1 계약 — ProjectBrief 문체 정본 통합(D1=A/D2=A)**. 런타임 도달 불가능했던 `WritingBrief`를 삭제하고 그 `style_rules`·`preferred_patterns`·`forbidden_patterns` 전부와 신규 자유 텍스트 `style_examples`를 append-only `ProjectBriefVersion`의 required 배열 key로 통합한다. 네 배열은 trim 후 blank·중복을 HTTP 422로 거부한다. `style_examples`는 기본 최대 **3개·항목당 1,000자**, `PROJECT_BRIEF_STYLE_EXAMPLES_MAX_ITEMS`·`PROJECT_BRIEF_STYLE_EXAMPLE_MAX_CHARS`로 조정 가능하며 두 값이 1 미만이면 app 기동 실패다. 환경별 상한의 authority는 HTTP **write** validator이고 schema catalog는 required shape·nonblank·uniqueItems를 잠근다. **runtime 상한을 낮춰도 이미 저장된 append-only version의 current/history/detail read에는 적용하지 않는다.** | 오너 보완 결정 1=A/2=B, 독립 검증 B-1 closure, `plans/writing-style-and-length-control-decisions.md`, `plans/writing-workspace-v2-w0-contract.md`, `schemas/writing-workspace-v2-w0.schema.json`, `verifications/2026-07-20/project_brief_style_integration.md`, `daily_logs/2026-07-20/work_log.md` |
| v1.7.20 | 2026-07-20 | **미채택 Writing candidate 복구 안전망(scratch) 계약 승격 — 잠정 정책 → 정본**. 오너 결정 **D0=B / D1=B / D2=A**(브리프 `plans/unaccepted-candidate-persistence-decisions.md`)로 구현된 복구 안전망의 보존/만료 정책이 구현자 잠정값이었는데, 오너 승인으로 정본 계약이 됐다. **승격 대상은 "상한 20"이라는 숫자가 아니라 "기본 20 + 운영자 조정 가능"이라는 계약이다**(오너: "실제로 어떻게 될지는 사람에 따라 달라서" — 숫자를 상수로 굳히면 dogfood에서 승격 근거를 만드는 관찰 자체가 코드 수정을 요구하게 된다). 계약 내용: scratch는 **Core SOT 외부**(`writing_drafts_scratch`)이며 정본이 아니고 복구 전용이다; 키는 `(project_id, draft_id)`이고 generate 요청에 `current_position`이 없으면 저장하지 않는다; generate 성공마다 append하고 per-draft 상한은 **기본 20 · `WRITING_SCRATCH_MAX_PER_DRAFT`로 조정 · 1 미만은 기동 실패**(0/음수 허용 시 안전망이 지켜야 할 초안을 조용히 삭제하므로 조용한 손실 대신 시끄러운 실패); **정본 version이 저장된 accept만 정리**하며 정상 200과 **analysis만 실패한 502 partial 양쪽**이 이에 해당하고 **비-PASS Gate(`accepted=false`)는 정리하지 않는다**(복구할 초안이 남아 있음); 시간 기반 자동 만료는 없다; scratch 쓰기/삭제 실패는 generate/accept를 실패시키지 않는다(best-effort 격리). public surface = `GET/DELETE /projects/{id}/writing/scratch?draft_id=`(plain dict, `response_model` 없음 — loop-audits 선례). D0=B는 Phase 7 P1을 각하한 것이 아니라 **향해 가는 방향으로 두고 좁게 시작**한 것이며, 이 계약과 함께 오너가 선언한 단계 전환(**"게이트 우선" 종료 → 게이트 ↔ UI/UX 동반 정합, SoT 변경이 상시화되는 구간**)도 정본이다. `next_unit`은 generate 시점에 부재하므로 scratch schema에서 제외하고 `intent`만 nullable seam으로 예약한다(Phase 7 `conversation_turn` 흡수 시 동반 추가). backend **1228 passed/70 skipped/299 subtests**, frontend **159 passed/11 files**. 오너 독립 검증 2회(조건부 합격→B-1 closure→**합격, 조건 없음**). | 오너 승격 승인 + 환경변수화 결정, `plans/unaccepted-candidate-persistence-decisions.md`, `verifications/2026-07-20/writing_scratch_recovery.md`, `services/application/app/writing/scratch.py`, `tests/test_writing_scratch.py`·`test_writing_scratch_mongo.py`, `daily_logs/2026-07-20/work_log.md` |
| v1.7.19 | 2026-07-19 | **export UI 옵션화 — include_archived 토글 + manifest 토글(frontend 슬라이스, backend 무변)**. v1.7.18 export UI에 오너 후속 (a)+(b)를 한 슬라이스로 얹었다. DraftList에 두 체크박스를 추가한다: **"보관된 원고 포함"**(default off, archived draft가 있을 때만 노출 — combined·bundle 양쪽 `include_archived` 적용)과 **"추적 정보(manifest) 함께"**(default off). manifest 토글 on이면 combined은 body 파일 + 별도 `{project_id}.manifest.json` 2파일, bundle은 zip에 `manifest.json`을 추가한다 — **bundle의 manifest.json을 v1.7.18 always-on에서 opt-in으로 변경**해 두 export가 한 토글로 일관된다. archived-only project gating을 재정의: v1.7.18의 "컨트롤 숨김"(H4) → **"컨트롤 표시 + 내보낼 unit 0이면 버튼 disable + 토글로 활성화"**(빈 export 방지 의도는 disable로 보존, 토글이 탈출구). `canExport = drafts.some(includeArchived || !archived)`. backend W4 계약(`?include_archived`/`?manifest`)을 그대로 소비하므로 신규 endpoint·schema 없음. frontend **155 passed/10 files**(+3: include_archived 쿼리·combined 2파일 manifest·bundle zip manifest; bundle/sanitize/archived-only 회귀는 opt-in/disable로 갱신), build JS 389.54 kB, `gen:api` byte-identical(backend/OpenAPI 무변), `tsc` clean. LLM 미사용. | 오너 후속 (a)+(b) 승인, `frontend/src/drafts/DraftList.tsx`·`DraftList.test.tsx`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.18 | 2026-07-19 | **프로젝트 export 프론트 UI 소비 + 회차별 개별 ZIP(bulk) — frontend 슬라이스, backend 무변**. W4 export 계약(v1.7.17)을 DraftList(`/projects/:projectId`)에서 소비한다. "전체 원고를 한 파일로"(TXT/Markdown)는 `GET /projects/{id}/export`의 body를 Blob 다운로드하고, **오너 결정으로 추가한 "회차별 개별 파일 (ZIP)"**(TXT/Markdown)는 `?manifest=true`로 포함 unit을 열거→각 unit을 기존 단일 version export로 verbatim 본문 취득→`jszip`으로 `{position2자리}-{제목}.{ext}` 항목 + `manifest.json`을 묶어 `{project_id}.zip` 다운로드한다. bulk는 **신규 backend endpoint 없이 기존 endpoint 재사용**이며 zip 항목명은 canonical 계약이 아니라 프론트 표현 결정(path-unsafe 문자만 `_` 치환). `exportingRef` 동시 실행 가드·진행 중 전 버튼 disable·**비보관 unit이 하나도 없으면 컨트롤 미표시**(archived-only project는 빈 결과라 감춤)·보관 원고 기본 제외. 오너 독립 검증 PASS(조건 없음, `verifications/2026-07-19/w4_export_frontend_zip.md`) 뒤 H2(combined blob 본문/content_type 단정)·H3(entry명 sanitize/fallback 회귀)·H4(archived-only 컨트롤 gating)를 보강, H1/H5는 선례/무해로 skip. frontend **152 passed/10 files**, build JS 388.63 kB(JSZip 포함), `gen:api` byte-identical(backend/OpenAPI 무변). `jszip@^3.10.1` 의존성 추가. LLM 미사용. | 오너 dogfood 전 기본 기능·bulk ZIP 결정, `frontend/src/drafts/DraftList.tsx`·`DraftList.test.tsx`·`src/api/client.ts`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.17 | 2026-07-19 | **Writing Workspace V2 W4 — 프로젝트 전체 ordered-latest export(D6=A)**. W0 §범위의 마지막 slice를 구현했다. 신규 `GET /projects/{id}/export?format=txt\|markdown&manifest=<bool>&include_archived=<bool>`가 project의 Draft를 `position` 순으로, 각 unit의 **latest version** snapshot `raw_text`를 이어 붙여 내보낸다. body는 단일 version export 선례를 이어 **AI metadata를 삽입하지 않는 verbatim**이며, unit 경계에만 제목을 넣는다(오너 결정: **Markdown은 `# {title}` heading, TXT는 제목 줄** — unit_kind별 heading 레벨 매핑 없음). archived unit은 **기본 제외 + `include_archived=true` 옵트인**(오너 결정), version이 없는 Draft는 snapshot이 없어 body/manifest에서 skip한다. export **delivery manifest는 요청 시에만**(오너 결정: 같은 endpoint `manifest=true` 쿼리 플래그, 미요청 시 `manifest=null`) 응답에 실리며 `project_id/format/include_archived`와 `units[]{draft_id,title,unit_kind,position,version_id,version_number,snapshot_id,content_hash}`로 body 순서와 동형인 traceability를 기록한다. archived project는 read이므로 export가 계속 200이다. saved publication manifest(version 선택 저장 정본)는 계속 Deferred다. W0 §4 matrix에 **EX-01~13** 양방향 회귀(fire 6/not-fire 7)를 추가했고 exact-key envelope·manifest omit/populate·빈 project 빈 body를 잠갔다. backend **1198 passed/73 skipped(Mongo-gated)/297 subtests**, frontend **146 passed/10 files**, `gen:api`가 export path/타입 추가(build 287.30 kB JS). LLM 미사용. 오너 독립 검증 PASS(조건 없음, `verifications/2026-07-19/w4_project_export.md`) 뒤 §6.3 빈-body clause 빈 셀을 EX-13으로 보강(잔여 hardening 후보는 선례 일치로 skip). **W1~W4 전체 완료** — 다음은 오너 dogfood/OPS-1 결정 또는 Deferred 항목이다. | 오너 다음 작업 진행 지시 + export contract 리터럴 3건 결정(heading·manifest·archived), `plans/writing-workspace-v2-w0-contract.md` §4·§6, `tests/test_core_sot.py::ProjectExportContractTest`·`test_application_api.py::ProjectExportApiTest`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.16 | 2026-07-19 | **Writing Workspace V2 W3 증분 2 — explicit Writing intent + W3 전체 closure(WI-01~22, SC-01/02)**. W0 §3의 `intent=append_current\|start_next_unit`을 generate/accept 계약에 구현했다. `WritingRequest/WritingCandidate`가 `intent`와 `NextUnit{title,unit_kind,goal}`를 싣고 candidate가 request의 값을 echo한다. accept는 provider/write 전에 intent/next_unit binding(append+next_unit·start+missing·candidate mismatch·blank title/goal)을 400으로 막고, replay lookup을 stale base·Gate보다 먼저 수행한다. **`start_next_unit`은 신규 Core SOT 6-surface 원자 write**(position shift[archived 포함]·새 active Draft[current+1]·version 1·snapshot·blocks·**accept receipt**)를 한 transaction으로 commit하고 실패 시 여섯 표면 전부 rollback한다(transaction·single-writer fallback 양쪽). append는 기존 3-surface 저장과 receipt 없는 save-key read-through replay를 그대로 유지한다. 두 intent 모두 Core SOT commit 뒤 `analyze:{snapshot_id}`로 Analysis job을 만들고 실패는 `502 + accepted=true + intent + saved` partial로 수렴한다. accept 응답에 `intent`, `saved`에 `draft_id/unit_kind/position`을 추가했다. `WritingAcceptReceipt`는 `(project_id, writing-accept:{key})` unique 경계로 durable 저장된다. **W3 전체 closure**: SC-01/02를 W2 ProjectBrief에 더해 W3 OU(draft-order)·Writing intent fragment(savedWritingTarget·writingAcceptResponseV2·writingAcceptAnalysisPartialV2·nextUnitSpec·writingIntent·unitKind)까지 W0 schema catalog와 동형 대조하고, accept·draft-order endpoint가 catalog root 대신 named component를 참조함을 확정했다. `nextUnitSpec`의 `goal`은 catalog대로 required-nullable 키다. 프론트는 채택 방식(현재 이어쓰기\|다음 유닛 시작) 선택과 새 유닛 title/kind/goal 입력을 accept에 배선한다. Mongo 미기동 시 backend **1181 passed/73 skipped(Mongo-gated)/297 subtests**, frontend **146 passed/10 files**, `gen:api` byte-identical·build 96 modules·`git diff --check` clean. **live replica-set 실증 완료**: `docker compose up -d mongo` 후 backend **1254 passed/4 skipped(Chroma 1+ES 3, W3 무관)**로 WI-11 six-surface transaction rollback을 실 transaction에서 통과. | 오너 다음 작업 진행 지시, `plans/writing-workspace-v2-w0-contract.md` §3~4, `schemas/writing-workspace-v2-w0.schema.json`, `tests/test_writing_accept.py`·`test_core_sot_mongo.py`·`test_project_brief.py`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.15 | 2026-07-19 | **W3 ordered-unit 독립 검증 B1 closure**. commit `56a73a3` 독립 감사는 OU-01~14 구현과 수치를 재현했으나 duplicate `ordered_draft_ids`가 W0 §2.2의 exact 409 대신 Pydantic 422가 되고 OU-08이 `(409,422)`를 허용하는 결함을 발견해 조건부 합격했다. 정본 §2.2가 status를 명시적으로 고정하므로 request validator를 제거하고 Core SOT의 full-permutation 검사로 중복을 처리해 missing/duplicate/foreign/unknown/set-change를 모두 409/write 0으로 통일했다. JSON Schema `uniqueItems:true`는 구조적 제약의 introspection이며 status authority가 아니므로 유지한다. OU-08은 전 case exact 409로 강화했고 migration artifact ID 보존을 직접 단정했다. 프론트 fixture를 required Draft shape로 정렬하고 canonical `position`을 visible ordinal로 오인하지 않도록 “정본 순서”라고 명시했다. 원 독립 검증 기록의 conditional verdict은 역사적으로 보존한다. | 오너 검증 보강·커밋 지시, `verifications/2026-07-19/w3_ordered_unit.md`, `tests/test_ordered_units.py`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.14 | 2026-07-19 | **Writing Workspace V2 W3 증분 1 — ordered unit 구현**. W0 §2의 Draft required `unit_kind=chapter\|scene\|other`와 project-wide archived-inclusive contiguous `position=1..N`을 Core SOT/in-memory/Mongo/public payload에 구현했다. create는 requested/default `other`와 `N+1`, list는 position 순, archive는 slot을 보존한다. 신규 `PUT /projects/{id}/draft-order`는 archived 포함 전체 ID 완전순열만 받아 한 repository write로 exact `1..N`을 교체하며 invalid 409/write 0, missing 404, archived project 409, same-order mutation 0을 보장한다. 명시 one-shot migration은 repository 기존 순서대로 legacy Draft를 `other/1..N`으로 보강하고 mixed/unknown/duplicate/gapped를 project 단위 fail-closed 처리하며 전체 성공 뒤에만 `(project_id,position)` unique index를 설치한다. transaction 및 local/test single-writer before-image fallback을 구현했고 실제 replica-set에서 fallback rollback/commit을 포함한 Mongo 37개를 통과했다. 프론트 생성 단위 선택·kind/position 표시·전체순열 위/아래 reorder를 연결했다. OU-01~14 named matrix, backend 1159 passed/60 skipped, frontend 144/10, fresh OpenAPI→build 96 modules를 통과했다. **W3는 미완료**이며 W0 §3의 `append_current\|start_next_unit` WI-01~22와 최종 SC-01/02 closure가 다음 증분이다. | 오너 W2 커밋 후 다음 작업 진행 지시, `plans/writing-workspace-v2-w0-contract.md`, `tests/test_ordered_units.py`, `scripts/migrate_ordered_units.py`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.13 | 2026-07-19 | **Writing Workspace V2 W2 ProjectBrief onboarding + canonical overview 구현·독립 검증 PASS/hardening closure**. W0의 `ProjectBriefVersion`을 Core SOT in-memory/Mongo에 append-only로 구현하고 project별 `version_number`·`idempotency_key` unique 경계, current-base 409, same-key 선행 replay, archived write 409/read 유지, all-null/empty clear 이력 보존을 HTTP 4개 endpoint와 exact Pydantic/OpenAPI schema로 노출했다. Writing `ContextPackage.project_brief`는 current brief를 candidate/memory와 섞지 않는 별도 authoritative item이며 prompt도 `<project_brief authority="canonical">`로 직렬화한다. 신규 `/projects/:id/overview`는 optional progressive onboarding/skip·versioned edit·“작품 정보 지우기(이력 보존)”와 canonical memory만의 인물/사건/미해결 질문 카드, pending 별도 count/link를 제공한다. 독립 검증은 PASS(조건 없음); 후속으로 OpenAPI `pattern:\S`, existing project/no brief service 회귀, collision→stale deterministic/live-Mongo test node를 보강했다. W3 ordered Draft/intent는 선행 구현하지 않았다. 최신 backend **1144 passed/52 skipped**, frontend **143 passed/10 files**, build 96 modules. Docker/Node 정렬·live Mongo concurrency는 테스트 머신 인계, LLM은 미사용. 다음은 W3다. | `verifications/2026-07-19/w2_project_brief_overview.md`, `plans/writing-workspace-v2-w0-contract.md`, `tests/test_project_brief.py`, `frontend/src/projects/ProjectOverview.tsx`, `daily_logs/2026-07-19/work_log.md` |
| v1.7.12 | 2026-07-18 | **Writing Workspace V2 W1 독립 검증 blocking closure**. v1.7.11을 조건부 합격시킨 독립 46-agent 검증이 cross-Draft source 이동의 dirty confirm 누락(미저장 본문 silent loss)과 회귀 empty cell 5개를 발견했다. 다른 Draft source `navigate()` 앞에 dirty confirm을 추가하고, 동일 root pattern sweep으로 editor의 SPA Link(목록·분석 검토함·rail 전체/detail)에도 공통 이탈 guard를 연결했다. 상태줄 저장/분석/pending, reject endpoint+재조회, same/cross-Draft dirty cancel→confirm, snapshot not found, quote/hash mismatch를 named 회귀로 추가했다. 검증 권장 C2도 반영해 text edit·save·version switch·reload 시 source notice를 clear하여 다른 version의 근거 표시가 잔류하지 않는다. 원 검증의 조건부 verdict은 `verifications/2026-07-18/w1_split_workspace.md`에 역사적으로 보존하며 closure 후 자체 검증은 frontend 139/9·build 95 modules다. backend/OpenAPI/W0 schema 무변, 다음은 W2다. | 오너 blocking closure 지시, `verifications/2026-07-18/w1_split_workspace.md`, `frontend/src/drafts/DraftEditor.tsx`, `frontend/src/drafts/DraftEditor.test.tsx` |
| v1.7.11 | 2026-07-18 | **Writing Workspace V2 W1 split workspace 구현**. 기존 DraftEditor route를 editor + docked right rail(`이어쓰기|분석|검토`)로 재구성하고 좁은 화면은 같은 tab 정보구조를 editor 위 단일 열로 유지한다. 작업 상태줄은 저장 상태·분석 실행 상태·검토 탭에서 조회한 project pending count를 구분한다. URL query `panel=writing|analysis|review`, review `candidate`, exact anchor `source`가 tab/candidate/detail/source 선택 복원의 정본이다. resolved source는 `snapshot_id`를 현재 또는 project 내 다른 Draft의 version 목록에 대조해 exact version을 연 뒤 raw Unicode code-point offset을 textarea UTF-16 selection으로 변환한다. quote/content_hash 불일치에는 이동하지 않고, 성공 시 최신/과거 version을 명시한다. Review rail의 confirm/reject는 서버 `{action,eligible,reason}`을 그대로 소비하고 고급 edit/conflict는 기존 전체 detail route를 유지한다. backend API·OpenAPI·W2/W3 runtime은 무변이며 다음은 W2 ProjectBrief onboarding+canonical overview다. | 오너 W1 진행 승인, `frontend/src/drafts/DraftEditor.tsx`, `frontend/src/review/WorkspaceReviewPanel.tsx`, `docs/plans/writing-workspace-v2-w0-contract.md` |
| v1.7.10 | 2026-07-18 | **Writing Workspace V2 W0 계약/migration 확정 + 독립 검증 blocking closure(runtime 구현 전)**. W0가 ProjectBrief exact version/API, ordered Draft의 `unit_kind=chapter\|scene\|other`+project-wide contiguous `position`, full-permutation reorder, legacy repository-order→`other`/`1..N` fail-closed migration, `append_current\|start_next_unit` request/accept binding과 next-unit Core SOT transaction/accept receipt를 잠갔다. `ProjectBrief`는 append-only version이며 GET null/PUT optimistic base+idempotency/history API를 사용한다. legacy Writing intent 생략은 `append_current`로 호환한다. next-unit accept는 position shift+Draft+v1+snapshot+blocks+receipt를 한 Core SOT transaction으로 commit하고 Analysis job은 기존 경계대로 commit 뒤 `analyze:{snapshot_id}`로 수렴한다. 독립 53-agent 검증의 조건부 합격에서 확인된 empty cell 7개를 모두 독립 named 행으로 닫고 정상 counterpart·schema catalog root 금지까지 보강해 matrix를 **50행(PB 12+OU 14+WI 22+SC 2; fire 22/not-fire 28)**으로 확장했다. fragment-only schema 소비, trim 후 constraint 중복 authority, migration maintenance/exclusive 실행·index 설치 gate, archived slot UI, W3 신규 6-surface 원자 범위, delivery manifest/publication manifest 구분도 명문화했다. 원 verification verdict은 이 closure 전 working tree에 대한 역사적 조건부 합격으로 보존한다. W0는 문서/schema slice라 runtime code/OpenAPI는 여전히 v1.7.8과 동일하며 다음은 W1 editor+docked right rail/source deep-link다. | 오너 승인 W0 착수·blocking 보강 지시, `plans/writing-workspace-v2-w0-contract.md`, `schemas/writing-workspace-v2-w0.schema.json`, `verifications/2026-07-18/w0_contract_migration.md` |
| v1.7.9 | 2026-07-18 | **Writing Workspace V2 구조 방향 확정(구현 대기)**. 오너 dogfood에서 작품 시작 정보 부재·현재 이어쓰기/다음 unit 모호성·editor↔review 왕복·작품 overview/전체 export 부재가 확인됐고, 이를 prompt 문구가 아니라 정본·명시 intent·작업공간 구조로 해결하기로 했다. 확정 조합은 **D1=A** 별도 `ProjectBrief`, **D2=A** 평면 ordered unit(`unit_kind=chapter|scene|other` + `position`), **D3=C** `append_current|start_next_unit` discriminator와 next-unit accept 원자 저장, **D4=A** editor+docked right rail(좁은 화면 tab/drawer), **D5=A** canonical-only overview+pending 분리, **D6=A** ordered-latest TXT/Markdown+별도 manifest, 전체 **C** 세로 슬라이스다. 구현 순서는 W0 계약/migration → W1 split workspace → W2 brief/overview → W3 ordered unit/next intent → W4 export다. 현재 runtime code/schema/API는 v1.7.8과 동일하며, 다음 작업은 W0에서 exact schema·migration·idempotency·원자성 경계를 양방향 회귀와 함께 확정하는 것이다. | 오너 권장안 전부 승인, `docs/live_review_briefs/2026-07-18/writing_workspace_ux_restructure.md`, `docs/plans/product-shell.md`, `docs/plans/frontend-writing-workspace-decisions.md` |
| v1.7.8 | 2026-07-18 | **실검수 C — 이어쓰기 accept 후 재분석 `source_ref not found` 예방+복구 계약**. 실제 dogfood에서 새 snapshot catalog 9개는 정상인데 accept job의 구 snapshot report pointer와 새 catalog가 함께 prompt에 들어간 뒤 strict `source_invalid`가 발생했고, D5=A 단일 failed job replay를 프론트가 후보 0 성공으로 오인했다. 오너 결정 **C**: 명시 same-job `failed→pending` retry API, failed envelope 판별, strict anchor를 유지한 prompt namespace 예방을 함께 적용한다. 초기 immutable `analysis_extract_v2` 주의 문구는 동일 job 라이브 재검수에서 재실패하여 보존하고, 정확한 output shape·advisory→authoritative payload 순서·report ID를 제외한 repair catalog를 잠근 새 기본 `analysis_extract_v3`로 승격한다. strict validation·자동 remap 금지·repair 1회 상한, succeeded replay, snapshot당 job 1은 유지한다. | 오너 dogfood·C 승인, `docs/live_review_briefs/2026-07-18/analysis_retry_after_accept.md`, `docs/plans/02-analysis-job-state-decisions.md`, `docs/plans/02-analysis-provider-wiring-decisions.md` |
| v1.7.7 | 2026-07-18 | **테스트베드 슬라이스 독립 검증 조건부 합격 → blocking 3건 closure + D5=A 정렬(D4=A analysis-key literal 개정)**. 오너 독립 검증(39-에이전트 adversarial, `docs/verifications/2026-07-18/testbed_abc_slice.md`)이 v1.7.6을 조건부 합격시키며 blocking 3건을 지적했고 오너 지시로 닫았다. **C-1/C-2(계약 의무 렌더 회귀 부재)**: SoT v1.7.6 C가 슬라이스 계약으로 규정한 progress coarse 단계·candidate summary 표시에 회귀가 없어 boundary 빈 칸이었다 → WritingPanel에 under-strict 회귀 2건(deferred fetch로 generate/Gate 단계 progress 문구 in-flight 관찰·claims 있는 후보 "근거 주장 N개" 표시), mutation bite로 각 렌더 제거 시 단독 실패 실증. **Q1(`ensureSourceRefCatalog` 부분실패 자가복구 불가)**: "refs 존재 시 스킵"이 부분 빌드 후 재시도에서 catalog를 영구 불완전하게 두던 gap → **coverage 기반 멱등 빌드**(블록 offset 매칭으로 누락 블록만 생성, 부분 실패는 재시도로 자가복구, mid-loop 실패는 throw해 partial catalog로 run 안 함, anchorable 블록 0이면 friendly 422 조기 차단), AnalysisTrigger에 **busyRef**(더블클릭 가드). 회귀 3건(self-heal·전부-degenerate·더블클릭), self-heal mutation bite 실증. **D5=A 정렬(오너 결정=완전 정렬)**: 검증이 확증한 "accept의 pending job orphan + 같은 snapshot 재분석 시 candidate 중복 적산"을 닫기 위해 **analysis job 멱등 key를 snapshot 유도(`analyze:{snapshot_id}`)로 개정**(원 D4=A `writing-accept:{key}` analysis literal 개정, **save key는 무변**). accept(`writing/accept.py::analysis_job_key`)와 프론트 `analyzeVersion`이 동일 literal을 파생 → `create_job`의 `(project,snapshot,key)` 멱등이 snapshot당 한 job으로 수렴(orphan 0·재클릭 중복 0·이미 SUCCEEDED면 재추출 없이 기존 후보 반환·accept가 실은 report도 run에서 소비). accept-replay 멱등 보존(같은 accept→같은 snapshot→같은 job). 회귀: 백엔드 `test_writing_accept.py` +1(snapshot-scope key·trigger 재사용·중복 job 없음), 프론트 기존 AnalysisTrigger 테스트에 deterministic key pin 단언 추가(random uuid 회귀 bite). 백엔드 **1119 passed/48 skipped**, 프론트 **121 passed/8 files**(blocking closure +5), `gen:api` diff 0. 상세 `docs/plans/05-writing-accept-decisions.md`(2026-07-18 개정), `docs/daily_logs/2026-07-18/work_log.md`. | 오너 독립 검증·결정(완전 D5=A 정렬), `docs/verifications/2026-07-18/testbed_abc_slice.md`, `services/application/app/writing/accept.py`, `frontend/src/api/client.ts`, `frontend/src/writing/WritingPanel.test.tsx`, `frontend/src/review/AnalysisTrigger.test.tsx` |
| v1.7.6 | 2026-07-18 | **테스트베드 사용가능화 슬라이스 A+B+C(오너 dogfood 발견 3건 대응) + report 2차 repair**. 오너가 브라우저 dogfood에서 발견: (1) 이어쓰기 502 "report field must be an array"(간헐적 12B 비-배열 report), (2) 검토 트리거 버튼 부재·집필↔검토 분리, (3) 진행/판정/에러가 안 보이는 블랙박스. 오너 결정으로 A+B+C를 한 슬라이스에 처리. **A(502 견고화, 오너 결정=UI 재시도+백엔드 2차 repair)**: `report.py`의 report enrich를 `first→1 repair→502`에서 **bounded repair loop(`MAX_REPORT_REPAIRS=2`)**로 변경 — 이전 계약 D4=A "strict+1 repair"를 **strict + 최대 2 repair**로 개정(각 repair usage는 계속 누적돼 aggregate budget 회계 무변, strict schema/literal 검증 무변). 진단 `report_live_diag`는 최종 repair raw를 노출(`caps[-1]`). 프론트는 `describeWritingError`로 알려진 실패(invalid_candidate_report/revision/gate·503/504/5xx)를 사람이 읽을 안내+재시도 가능 여부로 매핑하고 WritingPanel이 "다시 생성" 버튼을 노출. **B(검토 트리거+진입)**: 신규 `AnalysisTrigger`가 집필 화면에서 "이 원고 분석"으로 최신 snapshot을 분석해 needs_review candidate를 만들고 검토함으로 잇는다. 분석 추출은 snapshot에 **source_ref catalog**가 선행돼야 하므로(없으면 `source_ref catalog is required` 400), `analyzeVersion`이 catalog가 비어 있으면 **블록별 full-span source_ref를 먼저 생성**(offset만 필요, 서버가 quote/hash 도출)한 뒤 job create→run. DraftEditor 헤더에 "검토함 →" 링크. **C(관측성)**: WritingPanel이 서버측 파이프라인을 coarse 단계(근거 검색·초안 생성 → Gate 평가 → 자동 개선)로 표시하고 후보에 "근거 주장 N개" 요약을 노출. **공개 계약**: 백엔드 report는 내부 repair 횟수만 변경(OpenAPI schema·public literal 무변, `gen:api` diff 0); 프론트는 기존 엔드포인트(source-refs·analysis jobs·writing)만 소비(손선언). 회귀: 백엔드 report `MAX_REPORT_REPAIRS` 양방향(2차 repair 성공·3회 전부 실패·usage 합산, +진단 갱신), 전체 백엔드 **1118 passed/48 skipped/273 subtests**. 프론트 **116 passed/8 files**(AnalysisTrigger 7·WritingPanel report retry 1 추가), build 94 modules. **라이브 관통(실 12B, 브라우저 동등 경로)**: generate 정상(claims 3), analyzeVersion catalog 3 생성→run 200→candidate 3→검토함 3(어제 400 해소). 상세 `docs/daily_logs/2026-07-18/work_log.md`. | 오너 dogfood 발견·결정(A=UI재시도+2차repair, B+C 통합), `services/application/app/writing/report.py`, `frontend/src/review/AnalysisTrigger.tsx`, `frontend/src/writing/WritingPanel.tsx`, `frontend/src/api/client.ts`, `docs/daily_logs/2026-07-18/work_log.md` |
| v1.7.5 | 2026-07-17 | **B Review Inbox 두 번째 슬라이스 — candidate edit + conflict merge/split**(첫 슬라이스가 남긴 두 무거운 표면). v1.7.4와 동일하게 Phase 6 어포던스(v1.6.67)를 **순수 소비**한다 — backend·OpenAPI schema·공개 payload·회귀 무변, `gen:api` diff 0. `client.ts`에 `editCandidate`(`POST .../candidates/{id}/edit`, body `{payload}`)·`reconcileConflict`(`POST .../review-queue/{entry_id}/reconcile`, body `{action}`) 추가(둘 다 UI가 성공 시 목록으로 이동하므로 응답 body 미사용=void). **candidate edit**: ReviewInboxDetail의 read-only payload `<dl>`를 "수정" 버튼(edit 어포던스)으로 필드별 `<textarea>` 폼으로 토글한다 — taxonomy payload는 타입별 정확한 키 집합의 non-empty 문자열(character=name/observation·event=event·open_question=question)이라 기존 필드를 in-place 편집(키 추가/삭제 금지, 서버 재검증 400). 빈 필드가 있으면 "저장" disabled(UX 편의, 서버가 최종 authority — 400). 저장 성공→목록 이동, 400/409→폼 유지+error, "취소"→read-only 복귀. **conflict merge/split**: conflict card에 merge/split 버튼을 `conflict.actions` 어포던스로 렌더(`disabled`=`!eligible`, tooltip=`reason`; merge=character+matched·split=character 자격은 **서버가 선언**하고 프론트는 재계산하지 않음). 성공→목록 이동(서버 재조회). edit 모드 중에는 confirm/reject·merge/split 버튼을 숨겨 상태 혼선을 막는다. 회귀 순 +9(ReviewInboxDetail 8→17: 추가 10·삭제 1 — 구 'merge/split 미렌더' over-strict는 이제 렌더하므로 제거. 양방향: merge/split 렌더·**merge eligible=false→disabled+reason**·**split eligible=false→disabled+reason**[over-strict 어포던스 소비 대칭]·merge/split 각 reconcile endpoint+action body·**edit→edit endpoint+payload body**·빈 필드 저장 disabled·취소 복귀·edit 400 폼 유지·**edit 모드 중 confirm/reject·merge/split 숨김**). **mutation bite 실증 2종**: edit URL을 `/confirm`으로 변이→edit 회귀 단독 bite·reconcile action을 `"merge"` 하드코딩→split 회귀 단독 bite(각 write endpoint가 named 회귀에 pin, v1.7.4 검증 교훈 적용). 전체 프론트 **108 passed/7 files**(review 26), build 93 modules, backend/scope diff 0. 이로써 Review Inbox 핵심 검토 루프(candidate confirm/reject/edit·conflict merge/split·gate resolve/dismiss)가 프론트에서 완결된다. 남은 Phase 6 UI = memory card·미회수 foreshadowing view(별도 화면)·부분 승인/retry 일반화(오너 결정). 실 데이터 관통은 오너 풀스택 후속. | `plans/frontend-review-inbox-decisions.md`(follow-up), `frontend/src/review/ReviewInboxDetail.tsx`·`ReviewInboxDetail.test.tsx`, `frontend/src/api/client.ts`, `frontend/src/styles.css`, `docs/daily_logs/2026-07-17/work_log.md` |
| v1.7.4 | 2026-07-17 | **B Review Inbox 최소 action UI 구현(첫 B 슬라이스: 목록 + 근거 detail + 이진 action, 오너 D=B)**. Phase 6 어포던스 계약(v1.6.67 `{action,eligible,reason}`)을 **순수 소비**한다 — backend·OpenAPI schema·공개 payload·회귀는 무변이고 `gen:api` 재생성 schema diff 0이다. review-inbox/gate-finding read 엔드포인트는 여전히 `dict[str, object]` 무타입이라 응답 shape을 `client.ts`에 **손선언**했다(v1.6.94 첫 슬라이스 선례; 척추/Writing과 달리 이 구역은 아직 `response_model` 미적용, response_model 타입화는 additive 후속). 신규 route `/projects/:projectId/review`(목록)·`/projects/:projectId/review/:candidateId`(근거 detail), DraftList에 진입 링크. **프론트는 자격을 재계산하지 않는다** — 각 버튼의 `disabled`는 서버 `eligible`, tooltip은 `reason`(display text, pattern-match 금지)에서 렌더하고 이 슬라이스는 **이진 action 4종만**(candidate confirm/reject, gate finding resolve/dismiss) 배선한다. candidate `edit`·conflict `merge`/`split` 어포던스는 payload에 실려 오지만 **렌더하지 않는다**(다음 슬라이스). action 성공 후 목록을 **서버에서 재조회**(낙관적 패치 없음, 처리된 항목은 inbox에서 빠짐), detail의 confirm/reject 성공은 목록으로 복귀. detail은 candidate payload·원문 근거 quote·conflict diff를 **read-only**로 보인다. 회귀 +17(양방향: 목록/detail 렌더·단일 origin `/api` path·confirm/reject/resolve/dismiss endpoint+재조회·**어포던스 소비 over-strict**[`eligible=false`→disabled+reason, mutation bite 실증]·deferred edit/merge/split 미렌더·missing source_ref·action 실패 시 항목/근거 보존). 전체 프론트 **99 passed/7 files**(review 17), build 93 modules(CSS 11.85/JS 260.18 kB), backend/scope diff 0, `git diff --check` clean. 다음 B 슬라이스 = candidate edit(payload 폼) + conflict merge/split(reconcile). 실 데이터 관통(분석 candidate/gate finding 생성→검토)은 오너 풀스택 후속. | `plans/frontend-review-inbox-decisions.md`(D=B), `frontend/src/review/ReviewInbox.tsx`·`ReviewInbox.test.tsx`·`ReviewInboxDetail.tsx`·`ReviewInboxDetail.test.tsx`, `frontend/src/App.tsx`, `frontend/src/api/client.ts`, `frontend/src/drafts/DraftList.tsx`, `frontend/src/styles.css`, `docs/daily_logs/2026-07-17/work_log.md` |
| v1.7.3 | 2026-07-16 | **C2 자동 revise/retrieve loop UI 구현 — C0의 `WritingReviseGateResponse`/`WritingReviseGatePartial` 생성 타입을 소비해 bounded loop를 Writing 작업공간에 연결**. backend·OpenAPI schema·공개 payload는 무변이며 `schema.d.ts` 재생성은 IDENTICAL이다. generate→Gate 뒤 backend와 동형인 안전 자격(continuity finding, recommended `revise`, nonblank evidence, candidate 내 evidence 정확히 1회)을 만족할 때만 `/writing/revise-and-gate`를 자동 호출한다. 다수 finding은 error severity 우선, 동급은 Gate 순서로 선택한다. 최초 `retrieve_more` 진입은 만들지 않고, endpoint 계약대로 **기존 candidate+revise finding**으로 시작해 retrieval은 서버 loop 내부가 담당한다. 응답의 마지막 candidate·Gate·loop status·stage progress를 보존·표시하며 `pass|terminal_decision|not_eligible|budget_exhausted|no_change|failed` 6종을 각각 완료·사용자 검토·수동 처리·재시도/재생성 행동으로 매핑한다. partial 4xx/5xx도 candidate와 stage error discriminator를 보존하고, 5xx만 원 요청 exact body로 재시도하며 4xx는 확정 실패로 표시한다. persisted loop audit UI는 Deferred이므로 interactive request는 `persist_audit=false`를 명시한다. 최종 revised candidate를 accept할 때 C1 H1 exact-body idempotency guard가 새 key를 보장한다. 독립 검증 **PASS(조건 없음)**(`verifications/2026-07-16/c2_writing_loop_ui.md`) 후 비차단 H1~H4를 테스트로 보강(2회 evidence 차단·non-revise 미진입·gate/retrieval discriminator·성공 후 retry 숨김). 프론트 C2 +15, 전체 **82 passed/5 files**(WritingPanel 33), build 91 modules, `git diff --check` clean. H5(설정성 503 retry 문구)는 현 5xx 계약 의미를 바꿀 수 있어 후속 UX 검토. 실 12B 관통 smoke는 오너 풀스택 후속이며 그 전까지 `OPS-1`은 Waiting. | `plans/frontend-writing-workspace-decisions.md`(C2), `verifications/2026-07-16/c2_writing_loop_ui.md`, `frontend/src/writing/WritingPanel.tsx`·`WritingPanel.test.tsx`, `frontend/src/api/client.ts`, `frontend/src/styles.css`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.7.2 | 2026-07-16 | **C1 기본 Writing 작업공간 UI 구현 — editor에 generate→Gate 근거→accept/save 기본 루프(D1=A·D2=A·D4=A)**. C0(v1.7.1)가 낸 성공/partial 생성 타입을 **손선언 없이** 소비한다(backend/schema 무변, `schema.d.ts` 재생성 IDENTICAL). 신규 `frontend/src/writing/WritingPanel.tsx`가 editor 안에 렌더되고 `DraftEditor`가 D1 게이팅 상태(dirty/onLatest/hasVersions/readOnly)와 accept 후 reload를 배선한다. **D1=A(clean latest only)**: zero-version·과거 version·dirty·archived 각각에서 generate를 disabled로만 두지 않고 **왜 불가한지 + 해소 행동**을 설명 텍스트로 함께 보인다(첫 저장/최신 복귀/현재 변경 저장; display text는 localizable, machine literal 아님). generate 기준 base version id는 후보 생성 시점의 latest로 고정된다. **D2=A(기본 루프)**: instruction 공백-only 미전송·in-flight 중복 generate 차단, generate 성공 후 별도 `/writing/gate` 호출로 decision+findings(type/severity/message/evidence/recommended_decision) 표시, **pass만 accept 활성**, `200 accepted=false`는 실패가 아니라 재-Gate 결과로 표시. **D4=A(read-only candidate panel)**: 후보는 editor 본문과 분리된 read-only panel, accept 전 원문 무변. **accept intent 결박**: idempotency key를 exact accept request body(minus key)에 결박 — transport/5xx 재시도는 같은 key, candidate/base가 바뀌면 새 key. **outcome 계약**: `client.acceptWriting`이 `200 accepted=true`·`502 partial(saved)`를 "저장됨"으로, `200 accepted=false`를 "재-Gate 결과"로, 그 외를 error로 정규화 — **`502 + accepted=true + saved`는 저장 성공**으로 처리해 재조회+분석 재시도 안내(generic error로 삼켜 저장된 version을 잃지 않는다). `409 stale base`는 candidate 보존+최신 재조회 안내, `400/404/422`는 확정 거부, transport/5xx는 candidate·intent 보존. 프론트 회귀 +20(WritingPanel 단위 18 + DraftEditor seam 2, 양방향: D1 4-state 게이팅·pass-only accept·accepted t/f·502 partial·409 stale·5xx key 재사용·**검증 hardening 3: over-strict body 변경→새 key·400/422 definitive 거부**), 전체 프론트 **67 passed/5 files**, build 91 modules. backend·schema·회귀 무변. 독립 검증 **PASS(조건 없음)**(`verifications/2026-07-16/c1_writing_basic_ui.md`) 후 비차단 H1(idempotency over-strict pin — signature 가드를 직접 잠금, C2/D4=B candidate 가변화 전 선제)·H2(400/422 accept 전용 회귀) 반영. 실 LLM 관통(compose generate→Gate→accept smoke)은 12B 필요라 풀스택 후속. | `plans/frontend-writing-workspace-decisions.md`(C1), `frontend/src/writing/WritingPanel.tsx`·`WritingPanel.test.tsx`, `frontend/src/drafts/DraftEditor.tsx`·`DraftEditor.test.tsx`, `frontend/src/api/client.ts`, `frontend/src/styles.css`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.7.1 | 2026-07-16 | **C0 Writing HTTP contract 구현 — generate/gate/revise-and-gate/accept 응답 타입화(D3=A, `ARCH-1` 첫 발화)**. v1.7.0 D3=A가 계약만 잠근 것을 구현했다. **안전망 먼저(v1.6.95 절차 재사용)**: 4 endpoint 응답의 완전한 exact-key 회귀(`Writing*EnvelopeKeyTest` 9개 — generate 후보/중첩 4·gate·accept 성공+partial·revise-and-gate 성공+4 partial)를 **모델보다 먼저** 추가해 현 dict payload에서 통과시켰다(`response_model`은 미선언 필드를 조용히 삭제하므로). **`ARCH-1` 첫 단계**로 Writing HTTP 모델을 별도 모듈 `services/application/app/writing/http_models.py`로 분리(전 `main.py` router 일괄 분리는 하지 않음). **성공 경로**는 dict 반환이라 `response_model` 적용: generate→`WritingCandidatePayload`, gate→`WritingGatePayload`, revise-and-gate→`WritingReviseGateResponse`, accept→`WritingAcceptResponse`. **partial-failure 경로**(`JSONResponse` 직접 반환, runtime validation 우회)는 `responses={}`로 OpenAPI 문서화만: revise-and-gate 400/502/503/504 = `WritingReviseGatePartial | ErrorDetailResponse`(4 `*_error` discriminator를 optional로 union 표현), accept 502 = `WritingAcceptAnalysisPartial | ErrorDetailResponse`(**502 + `accepted=true` + `saved`** 보존). payload 빌더 무변(D2=A와 동형, 모델은 dict를 검증·직렬화만). **모델 = payload와 정확히 같은 너비 실증**: `WritingGatePayload`에서 `evaluated_by_model`을 빼면 gate·accept(중첩 gate) exact-key 회귀가 bite(모델 공유). `gen:api` 재생성으로 프론트 타입에 성공·partial literal 실림(성공 4 endpoint + partial union arm 5 = revise-and-gate 400/502/503/504 + accept 502; discriminator error 필드는 6개). 회귀 +9, 백엔드 **1117 passed/48 skipped/273 subtests**(이 환경 ES 미설치 기준선 1108). 프론트 build 90 modules·**47 passed**(소비 코드 무변, schema.d.ts만 재생성). 다음은 C1 기본 generate→Gate→accept/save UI. | `plans/frontend-writing-workspace-decisions.md`(D3=A), `services/application/app/writing/http_models.py`, `services/application/app/main.py`, `tests/test_writing.py`·`test_writing_gate.py`·`test_writing_accept.py`·`test_writing_revise.py`(`Writing*EnvelopeKeyTest`), `frontend/src/api/schema.d.ts`, `plans/product-readiness-backlog.md`(`ARCH-1`), `docs/daily_logs/2026-07-16/work_log.md` |
| v1.7.0 | 2026-07-16 | **Frontend Writing 작업공간 C 착수 계약 확정(구현 대기)**. 오너가 `plans/frontend-writing-workspace-decisions.md`의 **D1=A · D2=A · D3=A · D4=A · D5=A**를 승인했다. **D1=A**: Writing 기준은 clean latest 저장 version 하나이며 zero-version·과거 version·dirty editor에서는 generate/accept를 막는다. 이 제약은 disabled control로 숨기지 않고 **왜 실행 불가인지와 첫 저장·최신 복귀·현재 변경 저장 중 필요한 해소 행동을 사용자 설명 텍스트로 함께 제공**한다(display text는 localization 가능, machine literal 아님). **D2=A**: C1은 generate→별도 Gate 근거→pass candidate accept/save 기본 루프를 먼저 닫고 bounded revise/retrieve는 C2다. **D3=A**: generate/gate/revise-and-gate/accept의 현재 payload exact-key 회귀를 모델보다 먼저 추가한 뒤 Writing HTTP response/error 모델을 별도 모듈로 분리하고 성공 `response_model`+partial `responses={}`로 OpenAPI에 싣는다. `JSONResponse` partial은 runtime validation을 우회하므로 exact-key 회귀가 load-bearing이며, 특히 accept의 **502 + `accepted=true` + `saved`**를 generic error로 잃지 않는다. 이 선택으로 `ARCH-1` trigger가 발화하지만 전 `main.py` 일괄 분리는 하지 않는다. **D4=A**: 첫 후보는 read-only panel이며 accept 전 원문 무변. 다만 후보 일부 수정·저장 후 수정/재생성의 의미는 C1 실제 UX 뒤 editable candidate와 함께 재검토한다. **D5=A**: C0 HTTP contract→C1 기본 UI→C2 bounded loop의 세 slice. accept key는 exact request body에 결박하고 200 accepted=false·502 partial save·409 stale에서 candidate를 보존한다. 본 버전은 결정만 기록하며 runtime code/public payload는 무변. | 사용자 결정(D1~D5 전부 A, D1 설명 텍스트 동반, D4 부분 수정 UX 후속 재검토), `plans/frontend-writing-workspace-decisions.md`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.6.99 | 2026-07-16 | **Frontend editor/save A2 — version history·dirty 전환 확인·txt/Markdown export, Product shell A 완료**. 기존 `/projects/:projectId/drafts/:draftId` route 안에서 전체 version 목록을 최신순으로 표시하고 선택은 D2=A대로 component state에만 둔다. dirty 상태에서 다른 version을 누르면 최소 `window.confirm`: 취소는 현재 본문·선택·fetch 무변, 확인은 detail 조회 성공 후에만 `raw_text`/baseline/selected version을 함께 교체하며 실패 시 현재 편집값과 선택을 보존한다. 선택 조회 중 textarea는 잠시 read-only이고 저장/중복 선택을 막아 비동기 응답이 새 입력을 덮지 않게 한다. 과거 version 편집 후 Save는 append-only 새 latest를 mint해 history에 추가·선택하고 기존 version은 유지한다. 선택된 저장 version의 export envelope을 `Blob([body], {type: content_type})`로 만들고 서버 `filename` 그대로 txt/Markdown 다운로드하며, AI metadata를 본문에 넣지 않는다. 생성 OpenAPI 타입만 소비하고 backend route/model/Core SOT 무변. 독립 검증 PASS 후 export 회귀를 실제 backend filename·charset 포함 MIME으로 grounding하고 selection in-flight 잠금의 적용/해제를 전용 양방향 회귀로 보강했다. D2=B query·autosave·rich editor·DOCX/PDF/EPUB은 계속 후속이다. 프론트 **47 passed(4 files)**, build 90 modules, OpenAPI 타입 재생성 IDENTICAL. A 완료 체크포인트에서 ARCH-1은 backend HTTP 표면 무변이므로 미발화. | `frontend/src/api/client.ts`, `frontend/src/drafts/DraftEditor.tsx`, `frontend/src/drafts/DraftEditor.test.tsx`, `frontend/src/styles.css`, `docs/plans/frontend-editor-save-decisions.md`, `docs/verifications/2026-07-16/frontend_editor_save_a2.md`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.6.98 | 2026-07-16 | **Frontend editor/save A1 구현**(v1.6.97 결정의 code slice). 공개 UI spine에 `/projects/:projectId/drafts/:draftId`를 추가하고 원고 행을 editor deep link로 연결했다. editor는 project·draft·version list를 같은 project/draft path로 읽고, version이 없으면 빈 baseline, 있으면 가장 큰 `version_number`의 detail `snapshot.raw_text`를 verbatim 표시한다. 명시적 Save만 새 version을 mint하며 unchanged 상태(brand-new empty 포함)는 쓰지 않고, nonempty→empty 변경은 허용한다. 브라우저 save intent는 승인 계약대로 `{crypto.randomUUID(), exact rawText}`에 결박한다: in-flight 중복 POST 차단, transport/5xx/parse fault 뒤 동일 본문만 같은 key 재사용, 본문 변경은 새 key, 2xx와 400/404/409/422는 intent 종료, save 409는 입력을 보존한 채 read-only 전환한다. project/draft archive는 read-only이고 저장된 본문 read는 유지한다. **독립 검증 CONDITIONAL PASS의 B1 폐쇄**: 저장 응답은 저장된 baseline만 갱신하고 textarea의 최신 편집 state는 덮지 않아 in-flight 중 추가 입력을 보존한다. H1 replay fixture를 실가능 흐름으로 정정하고 H3 beforeunload 양방향 회귀도 추가했다. 생성 OpenAPI 타입만 소비하며 백엔드 route/model/Core SOT 무변. A2 history selection·dirty version 전환 확인·txt/Markdown export, D2=B query, autosave/offline journal/rich editor는 계속 후속이다. 프론트 회귀 **39 passed(4 files)**, build 90 modules, OpenAPI 타입 재생성 IDENTICAL. | `frontend/src/drafts/DraftEditor.tsx`, `frontend/src/drafts/DraftEditor.test.tsx`, `docs/verifications/2026-07-16/frontend_editor_save.md`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.6.97 | 2026-07-16 | **Frontend editor/save A1 구현 계약 확정(코드 구현 대기)**. 오너가 `plans/frontend-editor-save-decisions.md`의 **D1=A·D2=A·D3=A**를 승인했다. **D1=A**: 브라우저 저장 intent는 `crypto.randomUUID()` key와 exact `rawText`를 한 쌍으로 보존한다. transport/5xx 등 commit 여부가 모호한 실패 후 본문이 같을 때만 같은 key+body로 재시도하며, 본문 변경은 새 key다. 현재 API의 확정 400/404/409/422와 2xx 뒤 intent를 종료한다. 이는 같은 key+mutated body가 오류가 아니라 최초 snapshot replay가 되는 기존 Core SOT 계약에서 본문 유실을 막는 소비 규칙이다. 새로고침 횡단 journal은 보장하지 않는다. **D2=A**: editor route는 `/projects/:projectId/drafts/:draftId`, 최초 진입은 최신 version, version 선택은 component state다. 오너는 B(query parameter)의 공유·새로고침 복원 가치를 인정했지만 현재 기본 루프에는 추가 기능이므로 A를 먼저 채택하고 B를 additive 후속으로 유지한다. **D3=A**: A1 latest/empty load+textarea+명시적 save/read-only, A2 history selection+dirty confirmation+txt/Markdown export. autosave·rich editor·offline journal·전역 cache/data router 무변 보류. 백엔드 endpoint/model/Core SOT 계약 무변이며 본 버전은 결정만 기록한다. | 사용자 결정(D1=A·D2=A·D3=A, D2=B는 additive 후속), `plans/frontend-editor-save-decisions.md`, `docs/daily_logs/2026-07-16/work_log.md` |
| v1.6.96 | 2026-07-16 | **Product shell 두 번째 세로 슬라이스 — 프로젝트 상세 route + 원고 목록/생성**. 오너는 외부 의존성 없는 B(자체 History API)를 개인적으로 선호했지만, 곧 이어질 editor·Writing·Review의 project/draft 주소 계층 확장성을 우선해 **A(`react-router` Declarative BrowserRouter)**를 확정했다. 공개 UI route spine은 `/`(프로젝트 목록/생성)과 `/projects/:projectId`(선택 프로젝트의 원고 목록/생성)이며, Data/Framework mode·loader/action·route cache는 열지 않고 기존 component state + 얇은 fetch client를 유지한다. direct URL·뒤로가기·SPA fallback을 지원하고, 원고 생성 성공 후 서버 목록을 재조회한다. 공백-only와 in-flight 이중 제출을 막고 보관 프로젝트는 기존 원고를 읽되 생성 UI를 숨긴다. `react-router@8.2.0` 추가(React/DOM 선언 `^19.2.7`), 백엔드 endpoint/model/계약은 무변. editor route 후보 `/projects/:projectId/drafts/:draftId`와 본문·명시적 save·version·export는 다음 slice. 프론트 회귀 **22 passed(3 files)**, production build·OpenAPI 타입 재생성·실 API direct-route smoke PASS. **독립 검증 PASS(조건 없음)**(`verifications/2026-07-16/frontend_project_navigation.md`) — boundary matrix 16행 빈 셀 없음. 비차단 H3 공백 submit-level 회귀를 `trimmed === ""` 제거 mutation bite로 보강했고, H2 새 항목 강조는 실제 위치 상실 관측 시로 명확화했다. | 사용자 결정(A, 확장성 우선), `plans/frontend-project-navigation-decisions.md`, `frontend/src/App.tsx`, `frontend/src/drafts/DraftList.tsx`, `docs/daily_logs/2026-07-16/work_log.md`, `verifications/2026-07-16/frontend_project_navigation.md` |
| v1.6.95 | 2026-07-16 | **백엔드 공개 계약 조이기 — 척추 응답 모델(H1) + 이름 입력 검증(H2)**(브리프 `plans/frontend-api-contract-decisions.md` Resolved, v1.6.94 독립 검증 H1/H2에서 발단). 오너가 **H1 → H2 순서**로 진행을 지시하고 **D1=A / D2=A / D3=A**를 확정했다. **착수 전 1차 소스 실측이 브리프의 그림을 바꾼 사실**: 50 endpoint 중 **48개만 `response_model` 적용 가능**하고 **`/writing/revise-and-gate`·`/writing/accept` 2개는 `JSONResponse` 직접 반환이라 FastAPI가 response_model을 검증·문서화 모두 우회**한다(H1이 구조적으로 못 덮는 구멍이며 하필 Writing 트랙 표면). 요청 모델 5종은 전부 제약 없는 `str`이었다(`Field`/validator 사용처 0). **D1=A**: Product shell **척추 14 endpoint**(projects 5 + drafts 5 + versions 4)에만 적용 — 문제는 "50개에 타입이 없다"가 아니라 "**프론트가 손선언하는 자리**에 없다"이고 그 자리는 지금 척추뿐이며, Writing·Review envelope은 UI가 아직 만나지 않아 지금 고정하면 두 번 만지게 된다. 나머지 34개는 해당 UI 슬라이스에서. **D2=A**: `response_model=` 파라미터, 헬퍼는 계속 dict 반환 — payload 생성 코드(헬퍼 20개) 무변(§3). **핵심 안전 절차**: `response_model`은 미선언 필드를 **조용히 삭제**하는데 기존 회귀엔 exact-key set assertion이 **0**이었다 → **모델보다 안전망을 먼저 깔았다**(`SpineEnvelopeKeyTest` 5, 현 dict payload에서 통과 확인 후 모델 적용). 실증: `snapshot.project_id`를 모델에서 빼면 공개 API에서 사라지는데 **1104개 중 이 회귀 1개만** bite. **save surface(narrow)와 read surface(wide)가 같은 키 이름에 다른 shape**을 담아 모델 분리가 강제됐다(`SavedDraftVersionPayload` vs `DraftVersionMetaPayload` 등). **H1 성과 실증**: 백엔드 `archived`→`is_archived` rename + `gen:api` 재생성 시 **프론트 `tsc`가 정확한 소비 지점(`ProjectList.tsx:71`)을 짚어 실패** — v1.6.94의 "payload 변경이 컴파일 타임에 안 잡힌다"가 척추 구역에서 해소됐다. `client.ts` 손선언 `Project` 삭제 → 생성 타입 소비(v1.6.94 검증 H5도 자연 해소). **D3=A(H2)**: `NonBlankName = Annotated[str, StringConstraints(strip_whitespace=True, min_length=1)]`를 project/draft의 create·rename 4종 요청 모델에 적용 — **strip 후 min_length**라 `"  x  "`→`"x"` 저장, 공백-only는 **422**. **Core SOT 정본 계약 무변**: 검증자가 든 우회 시나리오(다른 클라이언트·trim 안 하는 미래 화면)가 전부 HTTP를 지나므로 경계에서 닫힌다; 서비스 계층 검증(D3=B)은 정본 계약을 여는 값에 비해 "scripts가 빈 이름 생성"만 추가로 사는데 실재 위험이 아니다. 회귀 +12(척추 exact-key 5 + blank name 7/subtest 13, 양방향: 제약 제거·strip 누락 mutation 양쪽 bite, padding 이름·일반 이름·내부 공백 보존 over-strict). 백엔드 1099→**1111 passed/45 skipped/273 subtests**, 프론트 10 passed. **live 관통**(격리 네트워크 frontend+application): padding strip·공백 422·척추 전 envelope 키 보존(가장 넓은 version detail 포함)·enum `"heading"` 직렬화·export body 원문 PASS. **(검증 후속 보강)** 오너 독립 검증 **PASS(조건 없음)**(`verifications/2026-07-16/backend_contract_tightening.md`) — mutation 4종(A 안전망 단독 bite / B 프론트 `tsc` rename 검출 / C·D H2 양방향)과 정량·live 키 집합을 전부 독립 재현. 비차단 4건 중 3건 반영(문서·주석만, 코드 무변): **H1-d2 정정** — 본 행의 "2개는 `JSONResponse` 직접 반환이라 response_model 우회"는 **부정확**했다. 실제로는 **두 endpoint의 성공 경로는 dict 반환이라 `response_model` 적용이 가능**하고, uncoverable한 것은 **partial-failure envelope**(JSONResponse)뿐이다. 결론(D1=A 척추 한정 + Writing 슬라이스 Deferred)과 fork 서술은 불변이며 위 "타입 계약 동기화" 절을 정확한 진술로 고쳤다. **H1-d1** 브리프 D1=A 행의 "척추 13개(projects 2+drafts 8+snapshots 3)"를 실제 구성 **14개(projects 5+drafts 5+versions 4)**로 정정(SoT·HANDOFF·코드는 처음부터 14로 정확). **H1-d3** `main.py` 모델 분리 주석의 mechanism 방향 정밀화(결론 불변). | 사용자 결정(H1→H2 순서, D1=A·D2=A·D3=A), `plans/frontend-api-contract-decisions.md`, `verifications/2026-07-16/frontend_first_slice.md`(H1/H2 발단), `verifications/2026-07-16/backend_contract_tightening.md`, `tests/test_application_api.py::SpineEnvelopeKeyTest`·`::BlankNameRejectionTest` |
| v1.6.94 | 2026-07-16 | **Frontend 첫 슬라이스 구현 — Product shell scaffold + 프로젝트 목록/생성(v1.6.93 계약의 code slice, D1=A/D2=B/D3=A)**. v1.6.93이 계약만 기록하고 "코드 구현은 다음 세션"으로 남긴 것을 착수했다. **오너 범위 결정**: D3=A 척추를 둘로 쪼개 이번 슬라이스는 **scaffold + 프로젝트 목록/생성**까지이고 **원고 목록→에디터는 다음 슬라이스**다(브리프 D3 각주 "A 자체도 한 슬라이스로 크면 둘로 쪼갠다"의 발화). **오너 결정(브리프가 "첫 슬라이스에서 결정"으로 남긴 항목): 프론트 테스트 도구 = Vitest + React Testing Library** — Vite와 설정/트랜스폼을 공유해 추가 빌드 체인이 없고, 이 저장소의 양방향 회귀 관례를 프론트에도 그대로 적용할 수 있다. **신설**: `frontend/`(Vite+React+TS SPA — `src/api/client.ts` 얇은 `fetch` 래퍼 + `ApiError`, `src/projects/ProjectList.tsx`), `services/frontend/Dockerfile`(node build → nginx 서빙 2-stage, deps 설치가 소스 복사보다 앞), `frontend/nginx.conf`, compose `frontend` 서비스(`${FRONTEND_PORT:-5173}:80`, `depends_on: application service_healthy`), `scripts/dump_openapi.py`(read-only OpenAPI 덤프). **단일 origin 실현(D2=B의 대가 상쇄)**: nginx `location /api/` → `proxy_pass http://application:8000/`(trailing slash가 prefix strip), dev는 Vite proxy가 같은 모양 — 브라우저가 cross-origin 호출을 하지 않으므로 **CORS 미개방 유지**. `proxy_read_timeout 120s`는 배포 Writing loop wall-clock 기본 60s(v1.6.89)보다 커서 nginx가 loop 자체 budget보다 먼저 끊지 않는다. **OpenAPI→TS 타입 생성 배선**(`npm run gen:api`): 50 endpoint 경로·**요청 바디**는 생성 타입이 실제로 잠그나, **응답 페이로드는 타입이 없다** — 엔드포인트 반환 주석이 `dict[str, object]`라 OpenAPI가 `additionalProperties: true`로 내보내기 때문이며, 이번 슬라이스는 사용하는 응답 shape만 `client.ts`에 손으로 선언하고 갭을 기록했다(닫으려면 백엔드 `response_model` 필요 = 별도 결정). **백엔드·계약·회귀 무변**(1099 passed/45 skipped/260 subtests, 프론트 착수 전과 동일). 프론트 회귀 9개(양방향: 목록 렌더·단일 origin `/api` 경로 over-strict·빈 상태·POST 후 서버 재조회·trim·공백-only 미전송 over-strict·list/create 오류 detail 노출·실패 시 입력 보존·성공 시 오류 해제). **live 관통 확인**: 격리 네트워크에서 frontend+application 컨테이너로 SPA 200·SPA fallback 라우트 200·`/api` prefix strip 후 GET/POST/health 왕복(한글 이름 포함) PASS. **(검증 후속 보강)** 오너 독립 검증 **PASS(조건 없음)**(`verifications/2026-07-16/frontend_first_slice.md`) — 손선언 응답 타입↔백엔드 payload literal 1:1 일치 재도출, 단일 origin 상쇄 구성+live(음성 404 포함) 입증, 정량 주장 전부 독립 재현(`schema.d.ts` 재생성 IDENTICAL 포함), 백엔드 git 무변경 확인. 비차단 hardening 6건 중 3건 반영(**프로덕션 코드 무변, 테스트/문서만**): **H6** 이중 제출 회귀(`fireEvent.submit`이 disabled 버튼을 우회해 `submit()` 내부 `saving` 가드를 직접 pin — `|| saving` 제거 mutation에서 정확히 1개만 bite 실증)·**H3/H4** doc 정정(브리프 관련 정본 v1.6.92→v1.6.94, compose 주석이 결정 v1.6.93과 구현 v1.6.94를 구분). **H2**(`create_project`에 빈/공백 이름 검증 부재 — 프론트 trim은 spec-silent UX 동작이라 다른 클라이언트가 우회 가능; 현 슬라이스 영향 없음)는 **백엔드 공개 계약 표면을 정하는 사안이라 코드 무변으로 두고 `response_model`(H1)과 묶어 오너 결정으로 상향**. **H5**(경로 타입 call-site 미소비)는 결함 아님(엔드포인트 배선 증가 시 자연 소비)이라 보류. 프론트 회귀 9→**10**. | 사용자 결정(슬라이스 범위 분할·테스트 도구=Vitest+RTL), `plans/frontend-kickoff-decisions.md`, `frontend/`, `services/frontend/Dockerfile`, `docs/daily_logs/2026-07-16/work_log.md`, `verifications/2026-07-16/frontend_first_slice.md` |
| v1.6.93 | 2026-07-15 | **Frontend 착수 계약 확정(구현 대기)**. 오너가 프론트엔드 착수를 지시(“다음은 프론트”)하고 `plans/frontend-kickoff-decisions.md`의 **D1=A / D2=B / D3=A**를 확정했다. 이로써 v1.1부터 유지되던 **“frontend framework 최종 선택 보류”가 해소**되고, HANDOFF Active Decisions의 “editor shell(frontend)은 현재 범위 밖”이 범위 안으로 들어온다(기록된 결정과의 충돌이 아니라 “필요해지면 검토”의 발화). **D1=A**: React + TypeScript + Vite SPA — 만들 화면이 원고 에디터·Writing candidate/Gate 흐름·Review Inbox diff라 클라이언트 상태가 두꺼워 서버 렌더링(Jinja+HTMX, D1=C)의 약점 지점과 정확히 겹친다. **D2=B(오너 선택, 추천 A와 다름)**: 프론트를 application 이미지에 섞지 않고 **별도 compose 서비스(nginx)**로 분리 — Gateway/Worker/embedding/ES가 모두 독립 서비스인 이 저장소의 monorepo+독립 서비스 경계 원칙과 같은 결. 대가인 “단일 origin 상실”은 **nginx가 `/api`를 application으로 reverse-proxy**해 상쇄하며 CORS는 열지 않는다(인증 없는 단일 사용자 API의 노출 확대 회피 — D2=C 각하 근거). **D3=A**: 첫 슬라이스는 Product shell 척추(프로젝트 목록/생성 → 원고 목록 → 에디터: 편집·명시적 저장·version). 의존성이 순서를 강제한다 — Writing은 `current_position`(draft_id+version_id)을, Review는 저장된 원고에서 파생된 candidate를 전제하므로 이후 A→C(Writing 작업공간)→B(Review Inbox). 기존 백엔드 50 endpoint·계약·회귀는 **무변**(프론트는 신규 백엔드 없이 조립). 기본값으로 진행(별도 결정 아님): OpenAPI→TS 타입 생성, 얇은 `fetch` 래퍼(캐시 라이브러리는 필요 시), 평문 `textarea` 에디터 + 명시적 저장(Core SOT “명시적 version save only” 계약 그대로). **본 버전은 계약 결정만 기록하며 코드 구현은 다음 세션**. 후속 결정: autosave 정책(“저장=version mint” 계약 변경 여부), Writing loop 진행 표시(최대 60s 동기 요청). | 사용자 결정(D1=A·D2=B·D3=A), `plans/frontend-kickoff-decisions.md`, `plans/product-shell.md`, `plans/06-review-ui.md` |
| v1.6.92 | 2026-07-15 | **Writing stable context pointer 구현(v1.6.91 계약의 code slice, D1=A/D2=A/D3=A + sub-decision P-i)**. 신규 immutable `ContextPointer{collection,document_id,version_id,content_hash}`(`writing/models.py`)와 projection/allowlist 코어(`writing/context_pointer.py`)를 추가했다. `CandidateClaim`에 `related_context_pointers`를 additive 추가하고 report wire·HTTP·Gate prompt·accept→Analysis advisory copy 네 표면에 동일 literal로 실었다(Gate decision schema·loop audit bodyless schema 무변). **pointer 노출은 report extractor 한 turn뿐**: `format_context_package(package, include_pointers=True)` opt-in이며 generation/revise 평문 prompt는 무변(D2=A, 계약 3). parser는 exact 4-key/string 검사 후 **동일 request package의 exact membership**를 검증하고 claim 내 중복을 거부한다 — hallucinated/other-package/rogue/missing/non-string pointer는 invalid→기존 1회 repair→실패 시 502. `project_id`는 pointer wire에서 제외하고 trusted package project로 검증한다(D1=A, 계약 2). **sub-decision P-i(오너 확정)**: 구현 착수 전 브리프 follow-up의 origin 불변식 재확인에서 원 잠김 계약 1("네 non-empty string")이 실경로와 모순임을 발견 — memory는 `content_hash`, candidate는 `version_id`·`content_hash`가 구조적 빈값(두 store에 필드 자체가 없음)인데 회귀 매트릭스는 세 origin 인용을 요구한다. 오너가 **origin별 불변식 테이블**을 채택해 계약 1을 개정: `source_blocks`=4필드 non-empty, `memory_entries`=`content_hash`만 `""`, `analysis_candidates`=`version_id`·`content_hash` `""`, 미지 collection은 pointable 아님. 빈값은 store에 필드가 없는 자리에만 허용하므로 source-block의 빈 version/hash(실 결함)는 fails-closed로 거부하며, 위반·cross-project item은 **provider 호출 전** 거부한다. 식별력은 유지된다(memory id=버전별 고유, candidate id=edit별 고유). D3=A대로 기존 pointer 없는 report JSON은 신 parser에서 invalid이며(report는 비영속·재추출 가능) fixture를 새 계약으로 정정했다. 쓰기 수·repair 횟수·public Gate literal 무변. 회귀 +31(양방향). **(검증 후속 보강)** 오너 독립 검증 **PASS(조건 없음)**(`verifications/2026-07-15/writing_stable_context_pointer.md`, 경계 매트릭스 빈 셀 없음·fails-closed 이중 방어 확인) → 비차단 hardening 2건 반영: **H2** generation/revise **service-seam** no-pointer tripwire 2건(formatter 단위 lock은 call-site의 `include_pointers=True` 주입을 못 잡음 — 두 call site mutation에서 정확히 이 2개만 bite 실증)·**H1** `related_context_pointers` 비-배열(str/dict/int) 매개변수화(`_list` 우회 mutation에서 bite). **H3**(실 12B pointer 복사 준수율)은 sandbox 검증 불가한 본질적 한계로 코드 무변 — 오너 풀스택 후속이며 실패 시 `scripts/diagnose_writing_report.py`가 exact 절을 노출한다. | 사용자 결정(P-i), `plans/05-writing-stable-context-pointer-decisions.md`, `writing/context_pointer.py`, `tests/test_writing_context_pointer.py`, `verifications/2026-07-15/writing_stable_context_pointer.md` |
| v1.6.91 | 2026-07-15 | **Writing stable context pointer 구현 계약 승인(→ v1.6.92에서 구현; 잠김 계약 1의 non-empty 문구는 sub-decision P-i로 origin별 테이블로 개정됨)**. 오너가 `plans/05-writing-stable-context-pointer-decisions.md`의 **D1=A / D2=A / D3=A**를 확정했다. 기존 structured report 결정의 **D2=A first→B**에서 pointer 없는 최소 schema(A)는 완료됐고, 이번 결정으로 full `candidate_claims[].related_context_pointers` schema(B) 확장을 승인한다. 신규 `ContextPointer` wire는 기존 `IndexPointer`의 `{collection,document_id,version_id,content_hash}` exact projection이며 `project_id`는 trusted candidate/package에서 유도해 모델 wire에서 제외한다(D1=A). **새 브리프의 D2=A는 A→B 단계 표기와 별도 축**으로, B 구현 시 pointer를 report extractor에만 표시하고 동일 request ContextPackage의 exact allowlist로 검증한다. unknown/rogue/cross-project/duplicate는 invalid→기존 1회 repair→계속 실패 502(D2=A). 각 claim은 required `related_context_pointers` array를 가지며 직접 package 근거가 없으면 `[]`; hint/risk와 Gate decision schema는 무변(D3=A). pointer는 grounding 연결일 뿐 source_ref mint·canon 승격·자동 save 권한이 없고 신규 registry/persistence/retention도 열지 않는다. 본 버전은 owner contract 결정만 기록하며 dataclass·formatter·parser·HTTP/Gate/accept advisory 확장과 양방향 회귀는 다음 code slice다. | 사용자 결정, `plans/05-writing-stable-context-pointer-decisions.md`, `plans/05-writing-self-report-decisions.md` |
| v1.6.90 | 2026-07-15 | **Writing Gate 판별 품질 라벨 벤치마크(프롬프트 튜닝 전치)**. B2b 라이브의 12B Gate 과민 `revise`/`not_eligible` 신호를 프롬프트 변경 전후에 비교할 수 있도록 정본 5 decision을 7개 라벨 fixture로 고정했다. 관측된 과민 경계의 over-strict guard로 **정상 역내 이동**·**정본과 양립하는 새 행동** 2건은 `pass`, continuity contradiction=`revise`, canon 근거 부족=`retrieve_more`, context 상충=`needs_user_review`, do_not_use/POV hard 위반=`block`으로 라벨링했다. `scripts/benchmark_writing_gate.py`(repeats 기본 3)는 `_default_writing_gate_service`를 재사용해 production prompt/model/thinking/max_tokens로 호출하고 expected/actual decision·finding type·token·error·accuracy JSON을 출력한다. case fault는 격리·fails-closed. **쓰기 0**, raw candidate/context 미출력, public literal·schema·runtime Gate·prompt 문구 무변. 회귀 +6. **실 12B baseline 완료**(`192.168.1.22:9080`, 7 case×3): `complete=true`, **21/21 match, accuracy 1.0**, parse/provider fault 0. 명확한 라벨 경계에서 과민 판정이 재현되지 않아 prompt 변경은 보류하며, 실패를 재현하는 새 fixture가 생길 때 동일 baseline에 추가한다. | HANDOFF Writing 잔여 Gate 튜닝 트랙, `writing/gate_quality.py`, `scripts/benchmark_writing_gate.py`, `tests/test_writing_gate_quality.py`, `docs/benchmarks/2026-07-15/writing_gate_quality_q4_baseline.json` |
| v1.6.89 | 2026-07-15 | **B2b aggregate budget production 기본값 default-on**(Option A 최종 단계, HANDOFF Owner Decisions). v1.6.87 M-i 측정 도구로 **실 12B(`gemma-4-12B-it-qat-q4_0`, 외부 llama `192.168.1.22:9080`) 라이브 per-stage 수집 완료**(3 passes, `complete=true`): per-stage MAX = revise 323tok/1018ms·report 766/5578·gate 815/3368·retrieve_plan 368/1435·context_search 0tok(제외)/27024ms. 합성 raw 최악경로(기본 정책 2/1/3) = **max_total_tokens 4991·max_wall_clock_ms 51755**. **context_search 27s는 측정 하네스(1회성 컨테이너)의 Chroma/embedding 콜드스타트 아티팩트** — 프로덕션 상시 app에선 warm(pass2/3 ~4s)이라 재현 안 됨 → steady-state wall-clock ≈ 28824ms. **오너 B4 결정: ~2x 여유율 → `WRITING_LOOP_MAX_TOTAL_TOKENS=10000`(raw 4991의 2x)·`WRITING_LOOP_MAX_WALL_CLOCK_MS=60000`(steady-state 28.8s의 2x), default-on**. `docker-compose.yml` application 기본값을 `:-}`(off)에서 `:-10000}`/`:-60000}`로 변경 → **배포 기본이 발화**(override 가능). **code 기본(env 미설정)은 계속 unbounded=off**이라 M6=A "off by default" 코드 회귀 무변(구조 상한만으로 off-deployment bound). raw 아티팩트 `docs/benchmarks/2026-07-15/writing_loop_per_stage_ceiling_q4.json`(+ 노트). Option A(측정 메커니즘 M-i → live 수집 → 합성 → B4)로 B2b 종결. | 오너 결정(B4 ~2x default-on), `docs/benchmarks/2026-07-15/`, `plans/05-writing-loop-ceiling-composition-decisions.md`, `docker-compose.yml` |
| v1.6.88 | 2026-07-15 | **Writing loop multi-finding revise**(브리프 `plans/05-writing-multi-finding-revise-decisions.md` Resolved, HANDOFF Next Tasks #1 Writing 후속). bounded loop의 `_eligible_revision_finding`이 자동 revise 대상을 **정확히 1개**로 제한해, Gate가 revise 분기에서 다수 continuity finding을 내면(Gate 계약상 revise 분기엔 retrieve_more/needs_review/block 추천 finding이 없고, 자격 함수가 recommended_decision=revise인 continuity finding만 선택) `len != 1` 이유만으로 `not_eligible` 종료하던 gap을 닫는다. 오너 결정 **D1=A continuity-only 유지**(각 선택 finding은 continuity+revise+evidence 후보 내 정확히 1회; do_not_use/pov는 revise 추천이어도 자동 splice 제외 — Gate가 block/needs_review로 라우팅)·**D2=A sequential**(자격 함수를 "정확히 1개"→"N개 중 최우선 1개 선택"으로 완화; loop 기존 re-gate가 다음 라운드 finding을 선택해 finding당 revision round 1 소비, 구조 상한이 총량 bound; reviser/splice/report/Gate/audit/budget 계약 무변)·**D3=A**(error severity 먼저, 동급이면 Gate 반환 순서 안정 정렬). 변경 표면 = 자격 함수 1개(`_is_eligible_continuity_revise` per-finding 추출 + `_eligible_revision_finding` 선택). **public literal·schema·서비스 경계 무변** — loop status/stages/HTTP envelope 동일, 유일한 동작 변화 = 다수 continuity finding이 not_eligible 대신 순차 소진. 양방향 회귀 +7(`EligibleRevisionFindingTest` 5: 자격 0→None·단일·2개 첫 선택·error 우선 order-independent·ineligible 혼재 시 eligible 선택 / `MultiFindingSequentialLoopTest` 2: 실 reviser 관통 2-finding 순차→pass·기본 상한 budget_exhausted bound). 기존 `test_revise_eligibility_rejects_every_broader_boundary`의 "2 continuity findings→not_eligible" case를 새 계약(eligible)으로 정정. Deferred: D2=B batch reviser(비용 측정 후), do_not_use/pov 자동 revise(D1=B, canon 정책 재검토), `max_revision_rounds` 기본값 상향(live 데이터 후). full 1062/45/240. | 사용자 결정(D1=A/D2=A/D3=A), `plans/05-writing-multi-finding-revise-decisions.md`, `services/application/app/writing/revise_gate.py`, `tests/test_writing_revise.py` |
| v1.6.87 | 2026-07-15 | **Phase 5.10 Option A (M-i) per-stage 측정 도구**. B2b 재측정에서 실 12B가 Gate 독립성(D3=A) 때문에 loop 최악경로(max_structural)를 걷지 못해 `WRITING_LOOP_MAX_TOTAL_TOKENS`/`MAX_WALL_CLOCK_MS` production 기본값의 근거 ceiling을 loop-level 직접 측정할 수 없다. 오너가 이미 **Option A(per-stage 합성)**을 선택(v1.6.86 합성 코어 `worst_case_stage_counts`/`compose_worst_case_ceiling` 구현·독립 검증 PASS)했고, 본 버전은 그 합성에 투입할 per-stage 실 비용을 얻는 **측정 메커니즘 M-i**(오너 확정)를 구현한다. 신규 `services/application/app/writing/per_stage_measure.py`(`run_per_stage_measurement` + `StageSample`/`PerStageMeasurement`)와 `scripts/measure_writing_stages.py` CLI가 revise/report/gate/retrieve_plan/context_search 각 stage를 production seam(`_build_revise_service`/`_build_report_service`/`_default_writing_gate_service`/`_build_writing_retrieval_planner`/`_default_context_search_service`)으로 조립해 실 gateway로 격리 호출, provider `TokenUsage`와 `perf_counter` 지연을 stage별로 기록한다. **`context_search`는 loop `metered()` 밖 호출이라 token 미포함(항상 0)·wall-clock만**(`revise_gate.py` 경계·`_TOKEN_STAGES` 불변식 동형). **`retrieve_plan`은 합성 `retrieve_more` Gate 결과**(코드 구성, 모델 미산출)를 planner에 먹여 Gate 독립성 문제를 원천 회피한다. `repeats`간 stage별 **보수적 MAX**를 취하고(합성 "관측 최댓값"), stage fault는 `error`/`incomplete_stages`로 surface해 gap에서 ceiling을 합성하지 못하게 한다. CLI는 측정 dict를 `compose_worst_case_ceiling`(env 정책 상한)에 투입해 raw 최악경로 ceiling JSON을 출력하되, **incomplete 측정에서는 fails-closed**(검증 H6): 어느 stage든 fault/미측정이면 `compose_worst_case_ceiling`이 누락 stage를 0으로 자동 기여해 ceiling을 과소 산출하므로, CLI `compose_ceiling`이 `ceiling.complete=false`+`incomplete_stages`를 노출하고 `max_total_tokens`/`max_wall_clock_ms`를 `null`로 비워 under-bound 숫자를 production default 근거로 오독하지 못하게 한다(`stage_counts`는 디버깅용 보존). **쓰기 없음**(Mongo/audit/file 0; gate/report 진단과 동형, 출력은 숫자만이라 raw prose 없음·persist 안전). 실행: `docker compose run --rm --no-deps application python scripts/measure_writing_stages.py --project-id <id> --repeats 3`. public literal·schema·서비스 경계 변화 없음. 회귀 +14(`tests/test_writing_per_stage_measure.py`: per-stage token/wall-clock 캡처·context_search token 제외 under/over-strict·synthetic retrieve_more gate·repeats MAX·stage fault incomplete·default 정책 ceiling round-trip·no-write call sequence·CLI wiring·**ceiling incomplete fails-closed·env→policy wiring**). full 1055/45/235. **live per-stage 수집 실행 → 합성 → B4 여유율/default-on 승인은 sandbox 밖 풀스택 후속(오너)**. | 사용자 결정(측정 메커니즘 M-i), `plans/05-writing-loop-ceiling-composition-decisions.md`, `scripts/measure_writing_stages.py`, `tests/test_writing_per_stage_measure.py` |
| v1.6.86 | 2026-07-14 | **잔존 4개 strict JSON parser fence-strip 스윕 완료(추적 부채 해소)**. gate(v1.6.83)·report(v1.6.85)와 동일 root-cause(`json.loads` 직접 호출, whole-content markdown fence strip 없음)가 남아 있던 `analysis/compare_judge.py::_json_object`·`analysis/extractor.py::_json_object`·`context_search/planner.py::_json_object`·`writing/retrieval.py::parse_writing_retrieval_plan` 4곳에 공유 `writing/json_extract.py::strip_code_fence`를 적용했다. 오너 D2=A 추출 프레이밍("fence 래핑은 정상 출력 포맷 변형, parser가 JSON을 추출") 그대로 — parser 정규화이며 strict schema/enum/literal 검증은 parsed dict에 동일 적용(public literal·schema·서비스 경계 무변). 4개 parser 모두 repair(1회 재호출)로 부분 완충돼 있었으나, fence-wrapped 유효 출력이 first·repair 양쪽에서 반복되면 동일 502/invalid가 가능했던 잔여 위험을 제거한다. `retrieval.TEMPLATE`에 fence 금지 문구 추가(나머지 3개는 이미 template/repair prompt에 fence 금지 존재). **SoT 정합**: Phase 2A extraction 계약 clause(위 "versioned provider extraction")가 "markdown-fenced JSON = first-parse 실패→repair"로 기술돼 있던 것을 fence 추출 정책에 맞게 정정(fence-wrapped 유효 JSON은 first call 통과, malformed/schema mismatch만 repair). 양방향 회귀 +8(각 parser: fenced valid first-call 통과 under-strict + fenced schema/object-invalid still rejected over-strict; compare/extractor/planner의 기존 fence→repair 테스트는 새 계약에 맞게 정정). tracked debt 4곳 전부 해소(잔존 0). full 1035/45/235. **동일 버전 별도 slice**: 이 버전(commit `18f0786`)에는 B2b ceiling Option A **합성 코어**(`worst_case_stage_counts`/`compose_worst_case_ceiling`, `scripts/benchmark_writing_loop.py`)도 포함됐다 — 정책 상한→최악경로 stage 카운트 재도출 + per-stage 비용 dict→raw ceiling 합성(context_search token 제외·wall-clock 포함). 독립 검증 PASS(`docs/verifications/2026-07-14/writing_loop_ceiling_and_fence_hardening.md`). v1.6.87 row가 이 합성 코어를 역참조한다. | 추적 부채 스윕(HANDOFF Next Tasks #1), gate/report D2=A 추출 프레이밍 계승, B2b ceiling Option A(`plans/05-writing-loop-ceiling-composition-decisions.md`), `writing/json_extract.py`, `tests/test_analysis_compare_judge.py`·`test_analysis_extractor_schema.py`·`test_context_search_planner.py`·`test_writing_retrieval.py` |
| v1.6.85 | 2026-07-14 | **Phase 5.10 D2=A report fence-strip remediation**. report 진단(v1.6.84) live 실행으로 D2=A evidence 확보 — report first output이 gate와 동일하게 ```` ```json ```` fence로 감싸져 있었고(1-call repair가 살림), `parse_report`가 fence strip 없이 `json.loads`해 first 실패 → repair 유발. 오너 결정 **D2=A (a)+(c)** + 프레이밍("이건 버그가 아니라 추출 과정 — 모델이 fence로 감싸는 건 정상 출력 포맷 변형이고 parser가 JSON을 추출하는 것"): (a) `report.py:parse_report`에 fence 추출 적용 + (c) `TEMPLATE`에 fence 금지. **first·repair 모두 `parse_report`를 거치므로 양쪽 fence 자동 커버**(repair도 ```` ```json ```` 내면 추출). gate v1.6.83과 동일 root cause·동일 정규화(parser 완화 아님, strict 4-field schema/item 검증은 parsed dict에 동일 적용). fence 추출을 공유 유틸리티 `writing/json_extract.py::strip_code_fence`로 추출(이전 gate_prompt의 private helper를 이동·공용화) — gate·report 양 parser가 같은 추출을 재사용. 양방향 회귀 7개(`ReportFenceStrippingTest`: first+repair fence 추출·fenced invalid still rejected·mutation bite). tracked debt: `report.py:113` 해소, `compare_judge`·`extractor`·`planner`·`retrieval` 4개 잔존(repair 완충). public literal·schema·서비스 경계 무변. | 사용자 결정(D2=A (a)+(c) + 추출 프레이밍), `plans/05-writing-report-live-diagnostics-decisions.md`, `tests/test_writing_report.py::ReportFenceStrippingTest` |
| v1.6.84 | 2026-07-14 | **Phase 5.10 D1=A operator-only Writing candidate report live diagnostics**. 오너 결정 **D1=A**(gate 진단과 동일 패턴)·**D2=A**(raw evidence 확보 뒤 별도 remediation 브리프). v1.6.83 gate fence fix로 `invalid_gate_result` 502가 0건이 됐으나 B2b 재측정(1/12 success)에 `invalid_candidate_report` 502가 남아("report field must be an array"), P1 bodyless audit으로 raw report output이 없어 exact 위반 clause(fence? schema? 어느 필드?)와 first/repair 중 어느 시점 실패인지 알 수 없었다. 신규 `services/application/app/writing/report_live_diag.py`와 `scripts/diagnose_writing_report.py`가 `/writing/revise-and-gate` 사전 report 파이프라인(context→revise→report)을 동일 production collaborators/prompt/model/`thinking=false`/`max_tokens`로 재현해 report의 **first + 1-call repair raw response**와 strict-parse error만 stdout에 출력한다(report는 repair가 있어 gate와 달리 두 raw를 모두 잡음). **쓰기 없음**: Mongo/audit/file 저장 0. report는 `_build_report_service(capture)`로 production config 재사용(이미 provider를 받는 선례). 회귀는 report request parity·first+repair raw capture(parse 실패 under-strict/성공 over-strict)·fence 미strip 관측·upstream 실패 분류·no-write spy를 양방향으로 잠갔다(11개). D2=A remediation(report fence strip tracked debt / prompt / parser)은 별도 브리프. public literal·schema·서비스 경계 변화 없음. | 사용자 결정, `plans/05-writing-report-live-diagnostics-decisions.md`, `scripts/diagnose_writing_report.py`, `tests/test_writing_report_live_diag.py` |
| v1.6.83 | 2026-07-14 | **Phase 5.10 D2=A Gate fence-strip remediation**. 독립 검증(v1.6.82)이 확정한 root cause — B2b 502의 `invalid_gate_result` exact 위반 clause는 **markdown code fence 래핑**이었다(`gate_prompt.py` `json_object()`가 fence strip 없이 `json.loads` → JSONDecodeError; JSON 자체는 유효, Gate 추론은 정상). 오너 결정 **D2=A (a)+(c)**: (a) `json_object`에 `_strip_code_fence`(whole-content code fence, 임의 lang tag 포함)를 추가해 parse 전 정규화 + (c) `WRITING_GATE_TEMPLATE`에 "Return raw JSON only (no markdown code fence)" 금지 추가. **parser 정규화이지 구조 완화가 아님** — strict schema/enum/priority/evidence 검증은 parsed dict에 그대로 적용되므로 public literal·schema·서비스 경계 무변(fence로 감싼 유효 JSON이 502에서 정상 parse로 바뀐 게 유일한 동작 변화). 양방향 회귀 9개(under-strict: fence-wrapped valid parse — strip 제거 시 B2b 502 재현 / over-strict: fence-wrapped invalid가 여전히 올바른 이유로 거부) 추가, mutation으로 bite 실증. prompt template은 fence 금지 문구를 추가했으나 version `writing_gate_v1` 유지(persisted prompt DB 없이 InMemory seed만). **패턴 스윕 부채**: 동일 root-cause(`json.loads` 직접 호출, fence strip 없음)가 `report.py`·`analysis/compare_judge.py`·`analysis/extractor.py`·`context_search/planner.py`·`writing/retrieval.py`에도 있으나 이들은 repair(1회 재호출)로 부분 완충돼 있어 Gate(유일한 미완충·실제 502 발생)만 우선 수정하고 나머지는 tracked debt로 기록. | 사용자 결정(D2=A (a)+(c)), `plans/05-writing-gate-live-diagnostics-decisions.md`, `tests/test_writing_gate.py::GateFenceStrippingTest` |
| v1.6.82 | 2026-07-14 | **Phase 5.10 D1=A operator-only Writing Gate live diagnostics CLI**. 오너 결정 **D1=A**(operator-only one-shot CLI)·**D2=A**(raw evidence 확보 뒤 별도 prompt/repair/parser 브리프 — 본 버전은 진단 표면만). B2b 502은 Gate stage `invalid_gate_result`로 좁혀졌으나 persisted audit은 P1 bodyless라 raw model output이 없어 exact JSON/enum/priority/evidence 위반 clause를 판별할 수 없었다. 신규 `services/application/app/writing/gate_live_diag.py`(`RawCaptureProvider` + `run_gate_diagnosis`)와 `scripts/diagnose_writing_gate.py`가 `/writing/revise-and-gate` 엔드포인트의 사전 Gate 파이프라인(context→revise→report→gate)을 동일 production collaborators/prompt template/model/`thinking=false`/`max_tokens`로 재현해 Gate의 raw response와 strict-parse error만 stdout에 출력한다. **쓰기 없음**: Mongo/audit/API response/file 어디에도 저장하지 않으며 민감 본문이 stdout에 나올 수 있어 출력은 operator terminal에만 둔다. Gate service는 `_default_writing_gate_service(provider=None)` 신규 seam(`_build_revise_service`/`_build_report_service`가 이미 provider를 받는 선례와 일치, 기본 동작 무변)으로 production config를 그대로 재사용한다. 회귀는 Gate request parity(production factory 기반 model/max_tokens/thinking/template)·ContextSearchRequest parity·raw capture(parse 실패 시 under-strict·성공 시 over-strict)·upstream 실패 분류·no-write call sequence를 양방향으로 잠갔다. 실행은 application 컨테이너(전체 env + scripts/ 보유): `docker compose run --rm --no-deps application python scripts/diagnose_writing_gate.py --project-id <benchmark-project>`(기본 benchmark context를 idempotent seed, `--current-position`로 기존 draft 재사용 가능). public literal·schema·서비스 경계 변화 없음. | 사용자 결정, `plans/05-writing-gate-live-diagnostics-decisions.md`, `scripts/diagnose_writing_gate.py`, `tests/test_writing_gate_live_diag.py` |
| v1.6.81 | 2026-07-14 | **Phase 5.10 B2b full-stack Writing loop benchmark harness**. 오너 결정 **B1=A/B2=A/B3=A/B4=A**: deployed `POST /projects/{id}/writing/revise-and-gate` HTTP를 측정하고 opt-in persisted audit에서 aggregate `total_tokens`·loop `wall_clock_ms`를 읽는다(audit GET은 POST latency에서 제외). 대표 fixture는 `terminal_pass`·`retrieve_more_then_pass`·`max_structural_path`(revision 2/retrieval 1/Gate 3)이며, expected status/stage trace가 다른 실모델 출력은 `unexpected_loop_trace` failure로 보존해 다른 branch 비용을 성공 p95에 섞지 않는다. 기존 Gemma Q4 선례대로 warmup 1회 성공은 제외·failure는 보존하고 measured success 3회로 case별 p95/max를 낸다. report는 required CLI provenance(`model`/`quant`/`compose_revision`), fixture hash·raw trace·HTTP latency·audit aggregate·failure code를 남긴다. **B1 closure**: warmup HTTP failure가 iteration 0 raw run으로 보존되고 measured iteration이 계속됨을 양방향 회귀로 잠갔다. **production default는 아직 off(`None`)**: 결과 p95/failure rate 뒤 별도 owner 승인으로 token/time 여유 ceiling과 default-on 여부를 확정한다. | 사용자 결정, `plans/05-writing-loop-benchmark-decisions.md`, `scripts/benchmark_writing_loop.py`, `tests/test_writing_loop_benchmark_script.py` |
| v1.6.80 | 2026-07-13 | **Phase 5.10 Writing loop aggregate token/wall-clock budget(B2 increment)**. 오너 결정 **M1=A**(usage plumbing·집계·configurable 강제는 지금, production 숫자는 B2b live 계측 후)·**M2=A**(`total-token`+`wall-clock`만; search/context budget은 Context Gate 유지)·**M3=A**(loop collaborator의 내부 `*_metered` 반환, public domain 결과 무변)·**M4=A**(`flat-loop-gate.md` 미러: token post-accounting, 누적 `> limit`인 논리 provider 단계 결과는 채택하지 않고 `budget_exhausted`; `== limit` 완료 허용; monotonic deadline은 다음 provider/search 단계 전 검사; component provider timeout은 provider error 유지)·**M5=A first→B**(run-level `total_tokens`/`wall_clock_ms`는 opt-in persisted audit summary/detail에만 additive, ephemeral `loop`/`stages` 무변)·**M6=A first**(두 aggregate limit 기본 `None`=off, production 숫자는 B2b). `WritingLoopPolicy.max_total_tokens|max_wall_clock_ms`와 env/Compose `WRITING_LOOP_MAX_TOTAL_TOKENS|WRITING_LOOP_MAX_WALL_CLOCK_MS`를 추가한다. revise/report(+repair)/Gate/retrieval planner(+repair) usage를 정확히 한 번씩 누적하며, 첫 Gate 전에 budget이 소진된 정상 200 결과는 마지막 완전 candidate를 보존하고 `gate=null`일 수 있다. 감사 Mongo 구문서는 신규 필드 부재 시 0으로 읽는다. save/accept/Analysis side effect와 standalone usage 노출은 없음. | 사용자 결정, `plans/05-writing-loop-budget-decisions.md`, `tests/test_writing_loop_budget.py` |
| v1.6.79 | 2026-07-13 | **Phase 5.9 L9 B 감사 opt-in 재개정 (P2=A→B)**. v1.6.78의 P2=A(모든 loop 종료 자동 감사)를 오너 재지시로 **opt-in**으로 되돌린다: "감사와 실제 loop 작업을 분리하고, 항상이 아니라 필요할 때만, on/off 토글로 loop 실행 시 바로 적용." 채택 **A(request 플래그) + 기본 off**: `/writing/revise-and-gate` 요청의 `persist_audit`(bool, 기본 null→env `WRITING_LOOP_AUDIT_DEFAULT`[기본 false], 요청 플래그가 override)가 true일 때만 persist. loop는 trail을 항상 서버 권위로 in-memory 계산하되 persist만 gate된다. **H3 흡수(격리)**: persist는 loop critical path 밖의 try/except-격리 단계라 **쓰기 실패해도 loop 결과를 그대로 반환**하고 `audit_id=null`+`audit_error:{type:"audit_persist_error",detail}`를 additive로 싣는다(감사 실패는 loop 결과를 죽이지 못함). 응답에 `audit_error`(성공·실패 5경로 모두, persist 미요청/성공 시 null) 추가. **불변**: P1=B bodyless trail·P3=A append-only uuid·P4=A list/detail·P5=A immutable(retention 운영과제)·side-effect 0·감사 레코드 구조/읽기 계약. pre-loop 요청 거부는 플래그 on이어도 미기록. Tradeoff: opt-in은 P2=A의 균일 관측 이점을 약화(로컬 1인 단계 loop별 제어·비용·격리 우선). | 사용자 재결정(감사 분리·opt-in·토글), `plans/05-writing-persisted-loop-audit-decisions.md`(P2 재개정), `tests/test_writing_loop_audit.py` |
| v1.6.78 | 2026-07-13 | **Phase 5.9 L9 B persisted Writing bounded-loop audit**. L9=A first→B의 후속으로 `/writing/revise-and-gate`의 모든 loop 종료(정상 6종 status + partial `failed`)를 durable append-only audit 레코드 1건으로 영속화한다. P1=B(bodyless per-stage trail: stage별 `candidate_hash`·revise `finding_fingerprint`·retrieval `pointer_ids`, run-level `trigger_finding_fingerprint`·`initial/final_candidate_hash`·`final_gate_decision`+finding fingerprints; 본문은 detail의 `final_candidate_text` 하나뿐), P2=A(모든 종료 자동 기록, pre-loop 요청 거부[400/502/504]는 loop 아님→미기록), P3=A(append-only, 재시도=새 uuid — 비결정 loop의 각 실행 보존), P4=A(`GET /projects/{id}/writing/loop-audits` 요약 목록[created_at desc·bodyless] + `GET .../{audit_id}` 전체 trail, project isolation·cross-project 404), P5=A(immutable, 상태 전이·자동 삭제 없음 — retention/TTL은 명시된 운영과제 후속, 오래된 run은 검증 보조자료로 보존). 응답은 기존 `{candidate,gate,loop,stages}`에 `audit_id`만 additive. side effect는 감사 쓰기 1건뿐(Core SOT/Analysis/memory 0). token/latency 집계는 B2 usage plumbing 후속. `CORE_SOT_MONGO_URI` 시 `writing_loop_audits` Mongo 어댑터, 없으면 in-memory. | 사용자 결정, `plans/05-writing-persisted-loop-audit-decisions.md`, `tests/test_writing_loop_audit.py` |
| v1.6.77 | 2026-07-13 | **Phase 5.9 G8 bounded revise/retrieve loop 결정**. L1/L2/L3=A(기존 `/writing/revise-and-gate`; pass·사람 decision 종료, 단일 continuity revise와 retrieve_more만 잔여 budget 안에서 자동 실행; revise→report→Gate, retrieve→merge→Gate), L4=A configurable(기본 총 revision=2·retrieval=1·Gate=3을 `WritingLoopPolicy`/환경 설정으로 변경 가능), L5=A(구조적 call cap first, aggregate token/time은 usage 계측 뒤 B2), L6=A first→C(additive 최소 `loop`+`stages`, 전체 중간 artifact 후속), L7=A(200 `pass|terminal_decision|not_eligible|budget_exhausted|no_change`; stage 실패는 `failed`), L8=A typed distinction(자동 후속 unchanged만 `UnchangedWritingRevision`을 200 `no_change`로 소비, 최초/standalone unchanged=502 유지), L9=A first→B(ephemeral first, persisted loop audit 후속). multi-finding·non-continuity auto-revise와 save/accept/Analysis는 없음. | 사용자 결정, `plans/05-writing-bounded-loop-decisions.md` |
| v1.6.76 | 2026-07-13 | **Phase 5.8 Writing `retrieve_more` 1회 lifecycle 결정**. T1=B/T2=B(기존 `/writing/revise-and-gate`가 같은 호출의 첫 GateResult를 내부 소비), T3=E/T4=E(별도 follow-up retrieval planner LLM이 strict JSON `query+needs` 선택; 기존 Phase 4 planner는 선택된 needs 안의 step/tool 계획), T5=B(이전 ContextPackage+targeted delta를 Application이 dedup·전체 budget 재적용해 단일 package로 merge), T6=B(candidate/report 유지, merged package로 Gate만 재평가), T7=A first→B(성공 `{candidate,gate}` 유지, retrieval 실패는 candidate+첫 Gate+`retrieval_error`; 후속 `stages`), T8=A first→B(`max_retrieval_rounds=1`, 두 번째 Gate가 retrieve_more여도 종료). follow-up 기본 허용 needs=`current_scene|recent_scenes|event_context|source_quote|canonical_memory`; `current_position` 부재 시 position-dependent `current_scene|recent_scenes` 제외; candidate_memory 금지. save/revise/generate/accept/Analysis/persistence 없음. | 사용자 결정, `plans/05-writing-retrieve-more-decisions.md` |
| v1.6.75 | 2026-07-13 | **Phase 5.7 G3 B partial revise→report→Gate 합성**. R1=A(기존 `/writing/revise-and-gate`를 목표 3단계로 승격)·R2=A(동일 ContextPackage 공유, 재검색 없음)·R3=A(report 실패 502/504 partial revised candidate+`report_error`, `gate=null`, Gate 미호출)·R4=A(기존 report taxonomy: timeout 504, provider/invalid/unexpected 502)·R5=A first→C(성공 `{candidate,gate}` 유지, 다회 합성 시 `stages` additive 확장)·R6=A(context/reviser/reporter/Gate 필수, 조용한 2단계 degrade 없음). 합성 service는 revise 1회→report enrich 1회→Gate 1회 순서를 Application에서 소유하고, report 내부 strict JSON repair 최대 1회는 기존 계약을 재사용한다. report 성공 후 Gate decision 5종(pass + non-pass 4종)은 200, Gate 실패 partial candidate에는 최신 report가 보존된다. save/accept/Analysis/재검색/두 번째 revise 없음. | 사용자 결정, `plans/05-writing-revise-report-gate-decisions.md`, `tests/test_writing_revise.py` |
| v1.6.74 | 2026-07-13 | **Phase 5.7 partial revise→Gate 1회 합성**. G1=A(별도 `POST /projects/{id}/writing/revise-and-gate`)·G2=A(동일 ContextPackage 공유; retrieve_more일 때만 후속 재검색)·G3=A first→B(첫 slice report 없음, 목표는 report 최신화 후 Gate)·G4=A(Gate 실패 partial candidate+gate_error: 입력/설정 검증 400, provider·invalid result·예기치 않은 평가 실패 502, timeout 504)·G5=A(Gate decision 5종 모두 200)·G6=A(`{candidate,gate}` nested)·G7=A(revise/gate/context 필수, 기존 mapping)·G8=A first→B(revise 1회+Gate 1회, 후속 정책/budget loop). revise validation/provider/context 실패는 Gate 미호출과 기존 400/502/504 mapping을 유지하며, Gate 실패는 성공한 비영속 revised candidate를 숨기지 않는다. save/report/accept/Analysis 자동 호출 없음. 원격 실모델에서 revise 성공 후 Gate invalid JSON이 재현돼 `partial_ok` candidate 보존을 확인했다; Gate JSON 품질/repair는 기존 Gate 별도 후속이다. | 사용자 결정, `plans/05-writing-revise-gate-decisions.md`, `tests/test_writing_revise.py` |
| v1.6.73 | 2026-07-13 | **Phase 5.6 finding evidence 기반 부분 revise**. D1=A(revise-only; retrieve_more 별도 브리프)·D2=A first→C(exact evidence 단일 발생→offset/hash)·D3=A(replacement 평문, 서버 splice)·D4=A first→C(단일 finding→비용 측정 후 multi-finding structured)·D5=A→B→C(Gate 별도→자동 1회→정책 잠금 후 bounded pass loop)·D6=C(inline first, persisted 감사 id API 후속)·D7=A first→C(LLM 1회→Application AgentLoop budget)·D8=A first→C(unchanged=502→후속 200 changed=false). `POST /projects/{id}/writing/revise`는 continuity+revise finding 하나의 evidence가 candidate에 정확히 한 번 있을 때 모델 replacement fragment만 받아 Application이 결정적으로 splice한다. evidence 밖 prefix/suffix는 그대로 보존하고 stale report는 비우며 저장/Gate/report/accept/Analysis 자동 호출 없음. **Writing 제어 원칙**: 모델/provider 서버는 생성 호출과 응답에 집중하고, anchor/offset 산술·schema validation·splice·decision priority·loop/budget 제어는 교체 가능한 모델 밖의 Application 서버가 소유한다. **독립 검증 조건부 합격 후 closure**: B1 context budget/backend 504/502, H1 빈 입력 pre-context, H2 planner 절약, H3 no-save spy, H4 Markdown fence unwrap을 회귀로 보강했다. 정상 prose 따옴표는 의미 보존을 위해 제거하지 않는다. | 사용자 결정, `plans/05-writing-partial-revise-decisions.md`, `tests/test_writing_revise.py`, `verifications/2026-07-13/writing_partial_revise.md` |
| v1.6.72 | 2026-07-13 | **Phase 5.5 Writing report 재평가 API**. 오너 결정 A=비영속 inline candidate를 지금 재평가하고 서버가 ContextPackage를 재구성, B=persisted candidate/report 감사 이력과 id 기반 API는 additive 후속으로 보존. `POST /projects/{id}/writing/report`는 request/instruction/candidate text를 받아 기존 strict extractor를 재사용하고 enriched WritingCandidate를 반환하며 저장·Gate·accept·Analysis를 실행하지 않는다. 빈 입력/unsupported task/invalid context request=400, missing project=404, dependency=503, malformed report/일반 provider/context backend=502, provider timeout/context budget=504. 실 LLM에서 발견한 prompt schema 누락을 exact JSON 골격+enum literal로 보강했다. **독립 검증 조건부 합격 후 closure**: B1(report 전용 unsupported task 400)과 H1(invalid context 400)을 직접 잠그고, H2(no-save spy)·H3(query override)·H4(current_position) 회귀를 보강했다. | 사용자 결정, `plans/05-writing-report-api-decisions.md`, `tests/test_writing.py`, `tests/test_writing_report.py`, `verifications/2026-07-13/writing_report_api.md` |
| v1.6.71 | 2026-07-12 | **Phase 5.4 structured candidate report**. D1=A(평문 후 별도 extractor)·D2=A first→B(pointer 없는 typed schema→stable pointer full schema)·D3=A then B committed(generate 합성→별도 report API 필수 후속)·D4=A(strict+1 repair)·D5=B(Gate+Analysis 소비)·D6=A first→C(AnalysisJob immutable advisory→별도 report entity). report는 snapshot extraction prompt 보조이며 direct candidate/memory mint 금지. | 사용자 결정, `plans/05-writing-self-report-decisions.md`, `tests/test_writing_report.py` |
| v1.6.70 | 2026-07-12 | **Phase 5.3 Writing accept→save→analysis 재진입**. D1=A(`draft_id+base_version_id`, latest 아니면 409)·D2=A first(빈 base=patch, trailing newline=exact concat, 그 외 `\n\n`; C client offset/separator 확장 고려)·D3=A(accept가 Gate 재평가, pass만 save)·D4=A(`writing-accept:{key}`로 save/job idempotency 파생)·D5=A first(pending Analysis job 생성, run 없음; C background run 확장 고려)·D6=A(non-pass=`200 accepted=false`)·D7=A(job write 실패=`502` partial-success saved artifact, same-key retry 수렴). `POST /projects/{id}/writing/accept` additive API. replay는 기존 save를 먼저 찾아 Gate/save 중복 없이 job을 재유도한다. | 사용자 결정, `plans/05-writing-accept-decisions.md`, `tests/test_writing_accept.py` |
| v1.6.69 | 2026-07-12 | **Phase 5.2 Writing Gate**. D1=A(생성과 분리된 별도 1-turn LLM Gate, 내부 호출 수보다 정확도 우선)·D2=B(decision+구조화 findings)·D3=A(side-effect-free 판정; 자동 전체 재생성 없음, finding evidence/recommended_decision으로 후속 부분 revise 연결)·D4=A first(별도 `POST /projects/{id}/writing/gate`, generate→gate 합성은 additive 확장). decision=`pass|revise|retrieve_more|needs_user_review|block`, 우선순위=`block > needs_user_review > retrieve_more > revise > pass`; finding type=`do_not_use|pov|continuity`, severity=`warning|error`. 같은 project의 request/candidate/ContextPackage만 평가하고 저장·검색·재생성을 수행하지 않는다. | 사용자 결정, `plans/05-writing-gate-decisions.md`, `tests/test_writing_gate.py` |
| v1.6.68 | 2026-07-12 | **Phase 5.1 Writing 생성 (첫 slice, 생성만)**(브리프 `plans/05-writing-generation-decisions.md` Resolved, HANDOFF 방향 결정 Phase 5). 순차 계획상 Phase 6보다 앞이나 미구현이던 Writing AI(글 생성)를 연다 — 입력 인프라(ContextPackage·Gateway)는 준비 완료. **오너 결정 Q1=생성만**(Writing Gate는 의미 있는 do_not_use/POV 검사가 LLM 기반이라 독립 slice)·**Q2=평문 프로즈**(모델은 프로즈만, 서비스가 WritingCandidate 래핑 — 로컬 Gemma의 긴 창작 프로즈를 JSON string에 넣는 fragility 회피; self-report 구조 필드는 계약에 정의하되 Gate slice가 채움). 파생: D3=continue_scene 한정·D4=직접 1-turn Gateway 호출(extractor/compare_judge 패턴, agent_loop runner 미사용)·D5=output_type=draft_patch·D6=결정적 안전선은 project isolation+task/instruction 검증만(do_not_use/POV 의미검증은 Gate slice)·D7=HTTP가 context_search→generate 오케스트레이션(핵심 흐름). **신설**: `services/application/app/writing/`(`models.py` WritingRequest/WritingBrief/WritingCandidate + WritingTaskType/WritingOutputType, `prompt.py` master system prompt[로컬변형]+ContextPackage 컴팩트 포맷[do_not_use/constraints 우선·candidate 라벨]+build_writing_request, `service.py` `WritingService.generate` 1-turn Gateway 평문 래핑+`WritingError`+`seed_writing_template`). **HTTP**: `POST /projects/{id}/writing/generate`(context_search 미구성/writing 미구성 503·unsupported task 400·WritingError 400·ProviderError 502·budget 504), 응답 `{request_id, project_id, task_type, output_type, text, status="candidate", self_reported_constraints, candidate_id, generated_by_model}`. `status`는 항상 candidate(canon 확정 안 함). 회귀 +19(`tests/test_writing.py`: prompt 조립 4[do_not_use 우선·candidate 라벨·instruction/excerpt/context 포함·brief]·generate 7[평문 래핑·ProviderError 전파·task/instruction/project isolation/brief project 검증·template 부재]·HTTP 8[200 orchestration·503×2·400·404·ProviderError 502·budget 504·context 502]). pytest 824 passed/48 skip(mongo env 제외), git diff --check clean. Writing Gate·accept→save 재진입·구조적 self-report·revise/outline/critique·실 LLM live smoke는 후속. **(검증 후속 보강)** 오너 독립 검증 합격(조건 없음, `docs/verifications/2026-07-12/writing_generation.md`, 매트릭스 10행 + mutation 6종 전부 bite) → 비차단 H2(orchestration `ContextSearchBudgetExceeded→504`·`ContextSearchFailed→502` 브랜치 회귀 부재)를 HTTP 회귀 +2로 보강(504 mapping mutation bite 실증). H1(self_reported_constraints 빈 값=Gate slice forward-defense)·H3(평문 보장 live 검증)·H4(budget 1차원)은 코드 무변. | 사용자 결정(Q1=생성만·Q2=평문), `plans/05-writing-generation-decisions.md`, `tests/test_writing.py` |
| v1.6.67 | 2026-07-12 | **Phase 6 Review Inbox 액션 어포던스**(브리프 `plans/06-review-inbox-affordances-decisions.md` Resolved, HANDOFF Next Tasks #1 Phase 6 UI 잔여). Review Inbox가 각 항목에 어떤 review action이 가능한지 선언하지 않아 프론트가 백엔드 자격 규칙을 재구현해야 하던 gap을 닫는다. **오너 결정 D1=자격 주석형 descriptor**(`{action, eligible, reason}` — 불가 이유까지)·**D2=candidate+conflict+gate finding 전부**·**D3=list+detail 둘 다**. 자격 규칙(read-time 재계산, write 엔드포인트가 authority): candidate(confirm/reject/edit 항상 eligible)·conflict(merge=character+matched canonical, split=character; 불가 이유 "merge/split is character-only"/"merge requires a matched canonical memory")·gate finding(resolve/dismiss=open일 때만, "gate finding is already terminal"). **read-only 어포던스 계산 + 기존 payload에 `actions` additive** — 신규 도메인 write·상태 모델·색인 무변. **신설**: `analysis/review_inbox.py` `ActionAffordance`(`{action,eligible,reason}`) + 순수 함수 `candidate_affordances`/`conflict_affordances`/`gate_finding_affordances`. **envelope 확장**: review-inbox list item·detail item에 `actions`(candidate), detail conflict에 `actions`(merge/split), gate finding payload(review-inbox·`/gate-findings` 공유)에 `actions`(resolve/dismiss). 회귀 +7(`tests/test_analysis_apply_api.py::ReviewInboxAffordanceTest` 5[candidate 전부 eligible·character-no-matched merge만 차단·비-character 둘 다 차단·gate open eligible·gate terminal 차단]·`ReviewInboxApiTest` 2[list/detail/gate 어포던스 노출·전용 gate 엔드포인트 actions]). pytest 805 passed/48 skip(mongo env 제외), git diff --check clean. 부분 승인·merge/split 일반화·frontend는 계속 미확정(Phase 6 UI slice). | 사용자 결정(D1=자격 주석형·D2=3섹션 전부·D3=list+detail), `plans/06-review-inbox-affordances-decisions.md`, `tests/test_analysis_apply_api.py` |
| v1.6.66 | 2026-07-12 | **Phase 6 candidate edit (백엔드 계약)**(브리프 `plans/06-candidate-edit-decisions.md` Resolved, HANDOFF Next Tasks #1 Phase 6 UI 잔여). review action 3종 중 마지막 edit을 연다(수용 기준 §60 approve/reject/**edit**). **오너 결정 D1=원 후보의 새 version**: 편집값으로 candidate의 새 version(append-only, 새 id)을 mint하고 그 version을 `confirmed`+canonical 승격, 원 후보는 `superseded`로 보존(감사)·de-index. D2=edit-and-confirm 단일 액션(편집 주체=검토자)·D3=신규 terminal `superseded`(edit 전용, confirm/reject 전이 채널 아님)·D4=idempotency(`logical_key=edit:{원본id}`가 기존 `(project,task,logical_key)` unique index 재사용 — replay·동시성 잠금, 클라이언트 key 불요, 원본 1회 edit)·D5=근거/provenance/confidence 보존(payload만 교정, schema 재검증)·D6=confirm 선례 재사용(promote 링크=successor id·원본 conflict resolve·CANDIDATE_REMOVED). **성격: 결정적 백엔드 로직, 라이브 불요**. 부분 승인·merge/split·frontend는 계속 미확정(Phase 6 UI slice). **신규 literal**: `AnalysisCandidateStatus.SUPERSEDED`, `AnalysisCandidate.supersedes_candidate_id`. **신설**: `AnalysisService.edit_candidate`+`CandidateEdit`, `CandidateReviewService.edit`+`CandidateEditResult`. **HTTP**: `POST /projects/{id}/analysis/candidates/{cid}/edit`{`payload`}(missing/cross-project 404·terminal 후보 409·invalid payload 400), 응답 `original_candidate_id`/`candidate_id`(successor)/`status`/`memory_id`/`idempotent_replay`. 신규 repo 메서드 없음(기존 index/promote/queue 재사용). 회귀 +15(`tests/test_candidate_review.py::EditTest` 9[version mint+supersede+promote·근거 보존·replay 무중복·one-shot 2차 payload replay·needs_review set 배제·invalid payload 원본 불변·terminal 409·optional deps·404]·`+transition` 1[superseded target 거부]·`tests/test_analysis_apply_api.py::EditCandidateApiTest` 5[200 flow+inbox drop·replay·400·409·404×2]). pytest 798 passed/48 skip(mongo env 제외), git diff --check clean. 실 Mongo edit round-trip은 sandbox 밖. **(검증 후속 보강)** 오너 독립 적대적 검증 조건부 합격(`docs/verifications/2026-07-12/candidate_edit_backend.md`, 매트릭스 13행 lock list + 6 mutation) → 차단 B1(매트릭스 행 7 "conflict resolve(dismiss 아님)" over-strict 미잠금 — `list_open()==()`가 RESOLVED/DISMISSED 구분 못 함, M3 mutation 미bite) closure: `_enqueue_conflict` 헬퍼 엔트리 반환 + edit 테스트에 `queue.get().status is RESOLVED` assertion 추가(M3 재bite 실증, 구현은 무변·correct). 비차단 H1(2-연산 원자성 live 후속)·H2(409/400 우선순위 계약 미명시)·H3(in-memory duplicate 미감지)는 코드 무변. | 사용자 결정(D1=원 후보의 새 version), `plans/06-candidate-edit-decisions.md`, `tests/test_candidate_review.py`, `tests/test_analysis_apply_api.py` |
| v1.6.65 | 2026-07-12 | **Context Gate finding 영속화 + Review Inbox 통합**. reject finding만 durable store, `/context-search` 필수 idempotency key, 결정적 finding id, persistence 실패 502, open→resolved/dismissed, candidate action 자동 연동 없음. 최소 재현 envelope에 request/result fingerprint와 안정 pointer ids 저장. 기존 inbox `items` 불변 + `gate_findings` additive, Gate list/detail/resolve/dismiss API. pass 감사 이력과 immutable GateRun manifest는 확장 가능 후속. | 사용자 D1~D8 결정, `plans/06-gate-finding-persistence-decisions.md` |
| v1.6.64 | 2026-07-12 | **Phase 6 Review Inbox 백엔드**. 미승격 `needs_review` candidate를 한 행으로 하고 open conflict를 중첩하는 read-only list/detail API. 상세은 candidate payload, source_ref 정본 pointer(유실은 `status=missing`), matched canonical과 결정적 field diff를 반환. legacy 직접 승격 candidate는 v1.6.60 dedup에 따라 억제. Gate finding persistence·부분 승인/edit·frontend route는 후속. | 사용자 승인, `plans/06-review-inbox-backend-decisions.md`, `tests/test_analysis_apply_api.py::ReviewInboxApiTest` |
| v1.6.63 | 2026-07-12 | (c-2): off-default same-name semantic 반증(`ANALYSIS_CHARACTER_HOMONYM_MATCH_THRESHOLD`), 라벨 기반 calibration CLI, open review entry 기반 append-only `merge|split` write. merge는 matched canonical payload 보존+evidence union version, split은 명시 candidate 별도 canonical. resolution 기록으로 same-action replay 멱등·cross-action 거부. | 사용자 승인, `plans/02b-7-character-alias-homonym-decisions.md` |
| v1.6.62 | 2026-07-12 | **(c) character 별칭 semantic 보강**(브리프 `plans/02b-7-character-alias-homonym-decisions.md` Resolved, HANDOFF Next Tasks #1 (c)). 2B.3 D2=A가 "별칭/동명이인은 merge/split review 후보(**자동 병합 없음**)"로 잠갔고 2B.6 D5=A가 "character semantic은 별도 결정"으로 남긴 경계를 연다. 결정적 name-key의 **false negative(별칭: `김철수`↔`철수`, name-key=0이라 중복 canonical 양산)**를 semantic으로 탐지. **오너 결정 D1=A 탐지만**(자동 병합 없음 — semantic 매칭을 `conflict`[review]로 surface, judge 미호출; D2=A 계약 존중·canon 오염 회피)·**D2=A 별칭만 먼저**(동명이인 false-positive 반증은 name-key=1 분기라 메커니즘 반대, 별도 후속 증분)·D3=A(conflict surface + matched id 실음→review_queue 풍부)·D4(별도 env)·D5=A(off 기본, 회귀 0)·D6=A(top-1)·D8(2B.6 wiring guard 재사용). **성격: 2B.6 semantic read 인프라 재사용, 계약 확장 + off 기본**. 동명이인 반증·threshold 실값 확정·merge/split write는 후속. **신규 env literal**: `ANALYSIS_CHARACTER_ALIAS_MATCH_THRESHOLD`(기본 `None`=off). **확장**: `AnalysisCompareService`에 `alias_matcher: SemanticMemoryMatcher | None` 주입 seam(2B.6 `EmbeddingSemanticMatcher` 재사용, character threshold 분리) — `_compare_candidate`가 결정적 name-key 매칭 0 + scope≠None(character)일 때만 alias 조회, 히트 시 `conflict`(matched_memory_id=canonical). name-key≥1은 종전 judge 경로 불변(alias 미호출). `main.py` `_build_memory_semantic_matcher(threshold_env)` 공유 빌더로 리팩터(2B.6 `_build_semantic_matcher` + 신규 `_build_character_alias_matcher`) — CHROMA_HOST 있고 EMBEDDING_SERVICE_URL 없으면 fail-fast(env명 포함). 회귀 +8(`tests/test_analysis_compare.py::CharacterAliasTest` 6[alias→conflict+matched id·judge 미호출 under/over-strict·below-threshold create·off 기본 create·same-name 결정적 우선(StubEmbedding 미기재 텍스트로 alias 미호출 증명)·**alias 경로 self-exclusion 같은 job**·**scope-less(event) 미영향**]·`tests/test_analysis_compare_api.py` +2[alias wiring fail-fast·off 기본 none]). pytest 757 passed/48 skip(mongo env 제외), git diff --check clean. **라이브 관통 PASS**(`scripts/phase2b7_character_alias_live_smoke.py`, 풀스택 머신): 실 BGE-m3-ko embedding + 실 Chroma `memory_vectors` query_similar로 `철수`(별칭)→conflict+canonical id·`영희`(타인)→create, threshold 0.7(관통·wiring 검증이지 **라벨 정확도·threshold 실값 캘리브레이션은 후속** — D5/D7). **(검증 후속 보강)** 오너 독립 적대적 감사 PASS(조건 없음, `docs/verifications/2026-07-12/character_alias_semantic.md`) — 비차단 관찰 Obs2(alias 경로 self-exclusion 직접 회귀)·Obs4(scope-less 미영향 회귀) 2건을 회귀로 실경로 보강(+6→+8, mutation bite 재실증); Obs1(SoT 산문 버전 순서)은 기존 부채 동반 건이라 §3 surgical 보류, Obs3(rationale id 참조)은 계약 미고정 자유도라 코드 무변. | 사용자 결정(D1=A·D2=A), `plans/02b-7-character-alias-homonym-decisions.md`, `tests/test_analysis_compare.py`, `tests/test_analysis_compare_api.py` |
| v1.6.61 | 2026-07-11 | **Phase 6 candidate 상태 전이 (백엔드 계약)**(브리프 `plans/06-candidate-state-transition-decisions.md` Resolved, HANDOFF Next Tasks #1 Phase 6). 여러 slice가 남긴 5개 forward-defense stub(candidate de-index·retriever needs_review 필터·drain self-heal·review queue resolve/dismiss·(e) v1.6.60 dedup)의 **수렴점**을 연다 — SoT §464(`confirmed`/`canonical` 미확정)·§465(review action idempotency 미확정)을 확정. **오너 결정 D1=분리 모델**(승인 = candidate `needs_review→confirmed` + canonical `MemoryEntry` promotion[`source_candidate_id` 링크], 거절 = `needs_review→rejected` promotion 없음; confirmed=검토 결정 기록·canonical=물질화)·**D2=`CANDIDATE_REMOVED` 대칭 이벤트**(색인 upsert-only stub 실경로화, worker composite delete drain)·**D3=review_queue `resolved`/`dismissed` 전이**(confirm→resolve·reject→dismiss, candidate 단위 idempotent)·**D4=idempotent 단건 전이**(재적용 no-op replay — 중복 status write·promotion·de-index enqueue·queue 전이 금지)·**D5=rejected 보존**(감사 이력, 색인만 제거). **성격: 결정적 백엔드 로직, 라이브 불요**. UI·source deep link·merge/split·부분 승인은 계속 미확정(Phase 6 UI slice). **신규 literal**: `AnalysisCandidateStatus.CONFIRMED`/`REJECTED`, `IndexSyncEvent.CANDIDATE_REMOVED`, `ReviewQueueStatus.RESOLVED`/`DISMISSED`. **신설**: `analysis/candidate_review.py`(`CandidateReviewService.confirm/reject` 오케스트레이션 — 상태 전이 + promote + de-index enqueue + queue 전이; optional removal_outbox/review_queue 미주입 시 전이만). **확장**: `AnalysisService.transition_candidate`(상태 머신, `_transition_job` 미러 — legal `needs_review→confirmed/rejected`만, cross-terminal/backward 거부, idempotent) + `CandidateTransition` + repo `update_candidate`(InMemory+mongo `$set status`). `IndexSyncOutboxService.enqueue_candidate_removed` + `_PER_SINK_EVENTS`에 CANDIDATE_REMOVED(worker `index_candidate` 재유도가 not-needs_review면 delete). `ReviewQueueService.resolve_for_candidate/dismiss_for_candidate` + repo `list_open_for_candidate`. **HTTP**: `POST /projects/{id}/analysis/candidates/{cid}/confirm`·`/reject`(missing 404, illegal 전이 409). 회귀 +22(`tests/test_candidate_review.py` 12[상태머신 legal/idempotent/illegal/scope·confirm promote+de-index+resolve·reject dismiss·replay 무중복·retriever 제외·optional deps]·`tests/test_review_queue.py` +4[resolve/dismiss/scope/idempotent]·`tests/test_memory_api.py` +6[confirm/reject/idempotent/409/404×2]; `tests/test_candidate_index.py` 2개 forward-defense stub→실경로 갱신). mutation 양방향 재실증(불법 전이 검증 제거→3 fail·idempotency 게이트 제거→2 fail, cp 정확 복원). pytest 748 passed/48 skip(mongo env 제외), git diff --check clean. 실 Mongo `update_candidate` round-trip·live de-index 관통은 sandbox 밖. resolve/dismiss 수동 UI·merge/split·부분 승인은 Phase 6 UI slice. **(검증 후속 보강)** 오너 독립 검토 CONDITIONAL PASS → 비차단 관찰 2건 closure(`docs/verifications/2026-07-11/candidate_state_transition.md`, 최종 합격): Obs1 `rejected→needs_review` 거부를 boundary matrix 명시 + 테스트 4번째 edge 추가·Obs2 `CANDIDATE_REMOVED`/`CANDIDATE_UPSERTED` 단일 reconcile 경로 공유를 `candidate_index.py` docstring 재작성 + worker routing 테스트 신규(M3 `_PER_SINK_EVENTS` 제거→routing FAIL bite). suite 748→749 passed. | 사용자 결정(D1~D5), `plans/06-candidate-state-transition-decisions.md`, `tests/test_candidate_review.py`, `tests/test_memory_api.py` |
| v1.6.60 | 2026-07-11 | (e) **canonical↔candidate 승격 dedup**(브리프 `plans/04-canonical-candidate-dedup-decisions.md` Resolved, HANDOFF Next Tasks #1 (e)). v1.6.50 D7이 수용한 한계("같은 지식이 **승격 후** canonical·candidate 양쪽 노출")를 닫는다. 원인은 결정적이다: 승격 시 canonical `MemoryEntry.source_candidate_id = candidate.id`(`memory/service.py`)가 박히지만 candidate status는 `needs_review`로 남아(`needs_review→confirmed` 전이는 Phase 6) candidate retriever가 승격된 candidate를 계속 반환 → package에 canonical·candidate 사본 동시 적재. **오너 결정 D1=승격됐으면 항상 억제**(store 권위 — `find_memory_by_candidate` 링크 존재 시 canonical이 같은 package에 있든 없든 candidate 노출 억제, `service.py:349` D5=A "승격 candidate는 canonical 경로로만 서빙"에 부합)·**D2=지금 retrieval-time interim 억제**(context_search 조립 단계 additive, 상태 모델·색인 무변; Phase 6 candidate de-index 도입 시 상위집합으로 흡수되는 forward-defense). **성격: embedding/semantic 아님 — `source_candidate_id` identity dedup이라 라이브·캘리브레이션 불요**. **구현**: `memory/service.py`에 `is_candidate_promoted(project_id, candidate_id)`(→`repo.find_memory_by_candidate is not None`) public 메서드 추가. `context_search/service.py`에 `PromotedCandidateResolver` structural Protocol(`MemoryService` 구조적 충족) + `ContextSearchService.__init__`에 optional `promoted_candidate_resolver` 주입(**미주입 시 억제 없음 = 종전 D7, 하위호환**). `_run_candidate_memory_step`이 retrieval 후 kept/suppressed 분할 — suppressed는 `ExcludedHit(record_id, reason="candidate_promoted")`로 trace 기록(hits_considered=전체·items_produced=kept); resolver 예외는 기존 candidate step `backend_error` degrade로 접힘. `main.py` `_default_context_search_service`가 `promoted_candidate_resolver=memory` 배선. **계약 literal·public 응답 envelope 무변**(억제 candidate가 애초에 package에 안 실림); trace `ExcludedHit.reason` 문자열 `"candidate_promoted"` 신규(내부 trace, schema/gate 대상 아님). 회귀 +6(`tests/test_context_search_candidate_memory.py::CanonicalCandidateDedupTest` 5[승격 억제+trace under-strict·혼합 부분억제 양방향·미승격 유지 over-strict·resolver 미주입 D7 하위호환 over-strict·resolver 예외 degrade]·`tests/test_memory_phase2b.py::IsCandidatePromotedTest` 1[승격 링크 양방향+project/candidate scope]). mutation 재실증: 억제 무력화 시 승격/혼합/degrade 3 test 재실패. pytest 724 passed/48 skip(mongo env 제외), git diff --check clean. **후속**: resolve/dismiss·candidate de-index 실경로는 Phase 6. **(검증 후속 보강)** 오너 독립 검토 조건부 합격 → 비차단 관찰 I1~I3 closure(`docs/verifications/2026-07-11/canonical_candidate_dedup.md`): I1 N>1 전부-승격 경계 회귀 신규·I2 project scope 배선 회귀 신규·I3 mutation 재실증 증거 보강(sha256 pre==post 정확 복원, 억제 무력화 시 5 failed/2 passed). 회귀 +2, suite 724→726 passed. | 사용자 결정(D1/D2), `plans/04-canonical-candidate-dedup-decisions.md`, `tests/test_context_search_candidate_memory.py`, `tests/test_memory_phase2b.py` |
| v1.6.59 | 2026-07-10 | (2B.4 후속) **conflict review queue 영속화**(브리프 `plans/02b-4-review-queue-persistence-decisions.md` Resolved, HANDOFF Next Tasks #1 (d)). v1.6.44 2B.4 apply가 `conflict`(review-only, D7)를 `SKIPPED_REVIEW`로 처리하지만 어디에도 영속화하지 않아 apply 응답 이후 조정 대기 conflict를 조회·추적할 수 없던 gap을 닫는다. **오너 결정 D1=최소 영속화만**(status=`open` 단일, resolve/dismiss 전이는 Phase 6 review 상태 전이 forward-defense — candidate `needs_review`-only와 동형)·**D2=GET list 엔드포인트**·**D3=결정적 id upsert**(멱등)·**D4=action generic 저장**(merge/split 미발화, 스키마만 열어둠). **신설**: `analysis/review_queue.py`(`ReviewQueueEntry`·`ReviewQueueStatus.OPEN`·`ReviewQueueRepository` Protocol·`InMemoryReviewQueueRepository`·`ReviewQueueService`·`derive_review_queue_id`=`(project_id,job_id,candidate_id,action)` canonical-JSON SHA-256, 2A `logical_key` 선례)·`analysis/review_queue_mongo_repository.py`(`MongoReviewQueueRepository`, collection `review_queue`, deterministic `_id` upsert, project+status index). **wiring**: `MemoryApplyService`에 optional `review_queue` 주입 — conflict 분기에서 enqueue(미주입 시 동작 불변, 하위호환). `create_app`이 `_default_review_queue_service`(Mongo 구성 시 Mongo repo)로 배선. **HTTP**: `GET /projects/{id}/analysis/review-queue`(open 항목 조회, missing project 404). 회귀 +13(`tests/test_review_queue.py` 5[enqueue·결정적 id·멱등 upsert under/over·project scope]·`tests/test_memory_apply.py` +4[conflict→enqueue under-strict·safe action 미enqueue over-strict·재적용 미중복·queue 미주입 하위호환]·`tests/test_analysis_apply_api.py` +4[conflict→GET 관통·재적용 미중복·빈 큐·missing 404]). pytest 717 passed/48 skip(mongo env 제외), git diff --check clean. resolve/dismiss 전이·merge/split 산출·큐 기반 재조정 write는 후속(Phase 6). 실 Mongo round-trip은 sandbox 밖. **(검증 후속 보강)** 오너 독립 감사(`docs/verifications/2026-07-10/review_queue_persistence.md`) 합격 → 비차단 관찰 3건 closure: I1 over-strict guard를 safe action 4종 전량 parametrize(update+add_evidence 추가)·I2 conflict+ghost candidate→`UnknownCandidate` 회귀 신규·I3 본문 §380 stale prose 정정(conflict 영속화 v1.6.59 closed 추적). mutation 2종 양방향 재bite, suite 717→718 passed. | 사용자 결정(D1~D4), `plans/02b-4-review-queue-persistence-decisions.md`, `tests/test_review_queue.py`, `tests/test_analysis_apply_api.py` |
| v1.6.58 | 2026-07-09 | (b-6 후속) **ES-lexical backfill 스크립트**(브리프 `plans/04-es-lexical-backfill-decisions.md` Resolved, HANDOFF Next Tasks #1). v1.6.57(b-6 증분2) accepted limitation #1("ES-lexical backfill 부재 → G4-B 아래 per-sink-max로 terminal drop된 ES 실패는 수렴 수단 없음")을 닫는다. **오너 결정 G1=A**(기존 `scripts/phase2b5_reindex_memory.py`에 ES leg 추가 — 같은 canonical 순회에서 memory vector+lexical 동시 backfill, worker `_build_memory_adapter` composite 대칭)·**G2=A**(candidate lexical+vector를 신규 `scripts/phase2b5_reindex_candidate.py`로 함께 — b-2 검증 관찰 (a) candidate vector backfill 부재까지 닫음)·**G3=A**(memory/candidate 분리 스크립트 — service/list/상태 상이)·**G4=A**(버전 bump, 새 public literal 없음). **성격: 순수 운영 스크립트 + 회귀 — 새 계약 literal 없음, forward-defense stub과 무관**(candidate는 needs_review 단일이라 upsert-only). 필요 helper(`list_memories`/`list_needs_review_candidates`/`build_*_lexical_record`/`connect_es_*`/text projection)는 v1.6.52/54/55에서 전부 구현·잠금됨. **구현**: `scripts/phase2b5_reindex_memory.py`에 `_build_memory_lexical_index`(ELASTICSEARCH_URL 시 `connect_elasticsearch_memory_index`, 아니면 InMemory fake dry-run) 추가 → `run_reindex`가 같은 canonical 순회에서 vector+lexical 동시 적재, summary를 `vector_backend`/`lexical_backend`/`vector_records_written`/`lexical_records_written`로 개환. 신규 `scripts/phase2b5_reindex_candidate.py` — `list_needs_review_candidates` 전수(needs_review만, 필터 불필요) → `candidate_index_text`+embed → `build_candidate_index_record`+`build_candidate_lexical_record` → vector+lexical 동시 적재. 둘 다 outbox 우회 일괄 재적재(D7 자세), ES 미구성 시 InMemory fake dry-run(vector backfill과 동일 자세). 회귀: `tests/test_phase2b5_reindex_memory_script.py` summary rename+lexical 양방향 guard 갱신, 신규 `tests/test_phase2b5_reindex_candidate_script.py`(needs_review 적재·project 격리·summary). mutation 양방향 4건(memory canonical 필터 under-strict·memory/candidate lexical leg over-strict·candidate vector leg over-strict) 재실증. pytest 704 passed/45 skipped(단 sandbox `elasticsearch` 패키지 부재 시 `ConnectElasticsearchTest` 3개 환경 의존 failed — b-5 도입·skip guard 없이 `from elasticsearch import` 직접 import·제 slice와 무관·HEAD에서도 동일 failed, 상세 work_log). 실 backfill live 실행(실 Mongo+ES)은 sandbox 밖 후속. | 사용자 결정(G1=A/G2=A/G3=A/G4=A), `plans/04-es-lexical-backfill-decisions.md`, `tests/test_phase2b5_reindex_memory_script.py`, `tests/test_phase2b5_reindex_candidate_script.py` |
| v1.6.57 | 2026-07-09 | (b-6) 증분2 — index-sync outbox **per-sink bookkeeping**(브리프 `plans/04-worker-compose-outbox-bookkeeping-decisions.md` Resolved, HANDOFF Next Tasks #1 (b-6) 증분2). v1.6.52/54가 두 번 미룬 per-target bookkeeping를 해소한다. 이전에는 outbox `targets` 필드가 존재하나 **inert**였다(성공=entry 삭제·실패=whole-event `$set`, 어느 쪽도 per-sink 전이 안 함) — vector(Chroma)·lexical(ES) 중 한 sink가 죽으면 건강한 sink까지 whole-event로 DLQ drop됐다. **오너 결정 G3=B**(full per-sink status + per-sink 재시도 예산)·**G4=B**(실패 sink만 재시도·성공 sink는 SUCCESS로 동결·all-terminal 시 entry 삭제)·**G6=A**(새 public literal 없음 — 내부 `IndexSyncStatus` + free-form target key `"vector"`/`"lexical"` 재사용). **구현**: `indexing/models.py` `IndexSyncTargetState`에 per-sink `attempt_count`/`last_error` 추가·`backend`를 `IndexSyncBackend`→`str` 완화(ES `"elasticsearch"` 허용, enum member 미추가)·신설 `SinkOutcome`(target/backend/ok/error). `indexing/service.py`: `_enqueue_event` placeholder targets → **빈 dict**(enqueue는 배포 sink 구성 모름 → sink-agnostic 유지, worker가 claim 시 materialize); `IndexSyncWorker`가 memory/candidate(`_PER_SINK_EVENTS`)는 `_drain_sinks`(composite `drain(entry, skip=SUCCESS targets)` → per-sink outcome → `_merge_target_states`[각 sink attempt_count 독립 증가·SUCCESS sink 동결] → `_classify_targets`[all-SUCCESS=삭제 / all-terminal(SUCCESS·per-sink-max FAILED)=삭제 / else=requeue])·archive(project/draft)는 종전 단일-sink whole-event 경로(`_drain_archive`) 그대로. `record_outbox_success`/`record_outbox_failure`에 `targets`/`terminal` optional 인자 추가(per-sink는 병합 targets 영속·terminal 전달, archive는 미전달로 후진호환). `indexing/memory_index.py`·`candidate_index.py`: composite가 `(target, backend, adapter)` named sink 튜플을 받고 `index_memory`/`index_candidate`(None 반환) → `drain(entry, *, skip)`(각 sink try/except·`SinkOutcome` tuple 반환·skip된 SUCCESS sink 미실행)으로 전환 — all-or-nothing raise 폐지, per-sink 격리. `indexing/mongo_repository.py`: `_target_state_doc`/`_to_target_state`가 str backend·attempt_count·last_error round-trip, 실패 requeue 시 `targets` `$set` 영속. `scripts/index_sync_worker.py` `_build_memory_adapter`/`_build_candidate_adapter`: ES 무관 **항상 composite**(단일 vector sink or vector+lexical) 반환 → worker가 per-sink를 균일 처리. **성격: 성공-경로 dedup 의미 이동**(성공=즉시 삭제 → all-terminal 삭제)·`IndexSyncRepository` Protocol per-target 표면 확장이 changelog 대상; **새 SoT public literal 없음**(G6=A). **수용 한계**: ES-lexical backfill 부재 → G4-B 아래 per-sink-max로 terminal drop된 ES 실패는 수렴 수단 없음(별도 후속). 회귀 재편(`tests/test_candidate_index.py` `test_sink_failure_propagates_not_swallowed` all-or-nothing → per-sink 격리 양방향 재lock + `PerSinkBookkeepingTest`[failing sink 자기만 requeue·healthy sink 미재색인·per-sink-max all-terminal 삭제·transient 복구 후 삭제] + skip guard; enqueue 빈 targets `test_indexing_phase3a`/`test_indexing_mongo`; composite drain `test_context_search_memory_lexical_retrieval`; builder 항상-composite `test_index_sync_worker_script`; worker construction `test_memory_vector_index`). pytest 702 passed/45 skip(mongo env 제외), git diff --check clean. 실 배포 per-sink 관통·ES-lexical backfill은 후속. **(오너 독립 감사 `docs/verifications/2026-07-09/outbox_per_sink_bookkeeping_b6_increment2.md` 합격 → 비차단 관찰 보강)** boundary matrix 15셀 전수 추적 확인·행동 회귀(archive DLQ·`DerivedIndexRecordNotFound` 국소화) 부재 확인. 관찰 #2(양-sink 동시 실패 경계) 명시 회귀 `PerSinkBookkeepingTest::test_both_sinks_fail_requeue_together_then_dlq_together` +1(양방향), 관찰 #3(per-event attempt_count가 per-sink 경로에서 backoff-only) `_drain_sinks` 주석 명시 → suite 703 passed/45 skip. | 사용자 결정(G3=B/G4=B/G6=A), `plans/04-worker-compose-outbox-bookkeeping-decisions.md`, `docs/verifications/2026-07-09/outbox_per_sink_bookkeeping_b6_increment2.md`, `tests/test_candidate_index.py`, `tests/test_index_sync_worker_script.py` |
| v1.6.56 | 2026-07-09 | (b-6) 증분1 — index-sync worker를 docker-compose **상시 서비스**로 전환(브리프 `plans/04-worker-compose-outbox-bookkeeping-decisions.md` Resolved, HANDOFF Next Tasks #1 (b-6) 증분1). v1.6.53(b-5) G6=A가 "worker는 compose 미추가(수동/out-of-band)"로 남긴 배포 gap(색인 적재가 수동 실행이라 배포에서 ES `memory_lexical`/`candidate_lexical`이 안 채워짐)을 닫는다. **오너 결정 G0=A**(in-script `--loop`/`--interval` + SIGTERM graceful)·**G1=A**(health port 없음 — per-call `request_timeout`이 hang을 bound)·**G2=A**(single-replica + graceful SIGTERM(finish-in-flight); atomic outbox claim `find_one_and_update`+lease(600s)이 이미 이중 실행을 막아 leader guard 불필요, N-replica 확장성 보존). **G5=A 2증분 중 증분1** — outbox 데이터 모델 미접촉; per-sink bookkeeping는 증분2(v1.6.57, G3=B/G4=B). **성격: 계약 literal·public 표면 변경 없음**(배포 인프라 + worker runtime loop). b-5 G6=A "worker out-of-band" → "worker compose 서비스" **posture 반전**만이 changelog 기록 대상(G6=A 새 literal 없음). **구현**: `indexing/service.py` `IndexSyncWorker.run_once`에 `stop_check: Callable[[],bool]\|None` 추가(for-loop 각 claim 직전 검사 → in-flight entry 완료 후 다음 claim 경계 종료; 기본 None→one-shot 후진호환). `scripts/index_sync_worker.py`: `--loop`/`--interval`·`_GracefulShutdown`(SIGTERM/SIGINT)·`_install_signal_handlers`·`run_loop`(worker 1회 build 후 run_once 반복, busy 즉시 재drain/idle sleep, stop_check 주입, per-pass JSON)·`_build_index_sync_worker`·`main` dispatch; exit-code map(0/2) 양 모드 보존. `docker-compose.yml` `worker` 서비스(application 이미지 공유, `restart: unless-stopped`, `stop_grace_period: 120s`, depends_on 4 sink, env 배선). `services/application/Dockerfile` `COPY scripts/` 추가, `.dockerignore` `scripts/` 제외 해제. 회귀 +10(`tests/test_indexing_phase3a.py` stop_check 양방향 + `tests/test_index_sync_worker_script.py` WorkerLoopTest/ParseArgsLoopTest/Increment1SignalAndEdgeTest — 실 SIGTERM→graceful flag flip end-to-end·退化·env override 포함). pytest 699 passed/45 skip(mongo env 제외), git diff --check clean. **(오너 독립 감사 `docs/verifications/2026-07-09/worker_compose_increment1_b6.md` 조건부 합격 → 전제 폐쇄)** 6 감사자+반박 21건 만장일치 CONFIRMED, 구현 결함 0; 브리프 G2 lease-release 자기모순 정정(canonical=finish-in-flight)·#8 정정(2026-07-08 "targets.chroma.status combined 반영" → inert 사실관계 정정)·HANDOFF 능동 모순 정정·signal wiring 회귀 락(2-1)·stop_grace_period 60→120s(3-1) 폐쇄. 실 Mongo end-to-end drain smoke PASS(격리 standalone mongo). 증분2 per-sink bookkeeping·실 배포 관통은 후속. | 사용자 결정(G0/G1/G2/G5/G6), `plans/04-worker-compose-outbox-bookkeeping-decisions.md`, `docs/verifications/2026-07-09/worker_compose_increment1_b6.md`, `tests/test_index_sync_worker_script.py`, `tests/test_indexing_phase3a.py` |
| v1.6.55 | 2026-07-09 | ⑤ §5 B 후속 — `needs_review` candidate retrieval의 **vector + lexical + hybrid(RRF)** 확장(브리프 `plans/04-writing-candidate-retrieval-decisions.md` Resolved, HANDOFF Next Tasks #1 (b-2) 증분2). v1.6.54가 candidate 색인 파이프라인을 세운 위에 canonical(v1.6.51/52)과 **대칭인** retriever를 얹는다. **오너 결정 G4=A**(vector+lexical+hybrid 한 slice). **구현**: `context_search/service.py`에 `VectorCandidateMemoryRetriever`(embed(query)→per candidate_type `query_similar`→hit `candidate_id`로 `get_candidate` **권위 재유도**→`status is NEEDS_REVIEW`만; `_merge_hits` 단일 풀 cosine 랭킹), `LexicalCandidateMemoryRetriever`(ES search→권위 재유도), `HybridCandidateMemoryRetriever`(vector+lexical RRF `1/(k+rank)` k=60, candidate id dedup) — 전부 canonical retriever와 동형, `.retrieve()` 반환 타입(`tuple[AnalysisCandidate, ...]`) 불변이라 `_run_candidate_memory_step`/`_item_from_candidate`/Gate `_gate_candidate_findings` **한 줄도 안 바뀜**(순수 주입 교체). `main.py`: `_build_candidate_memory_retriever(analysis)`가 canonical과 동일 env 스위치로 backend 선택(CHROMA_HOST+EMBEDDING_SERVICE_URL→vector, ELASTICSEARCH_URL→lexical, 둘 다→hybrid, 없으면 종전 Mongo-direct). index text는 근거 아님(contracts §1.3, Mongo 재유도 후 grounding). **계약 literal·public 표면 변경 없음**(retrieval 레이어 교체, item/Gate 계약 불변). 회귀 +9(`tests/test_context_search_candidate_retrieval.py`: vector 권위 재유도+relevance 순서·global limit·query-drives-ranking·stale(removed+transitioned) 격리[under-strict]·단일 풀 병합[over-strict]; lexical needs_review-only+ranking·stale skip; hybrid RRF 양신호 융합·id dedup; seam 불변 micro candidate item; needs_review 필터 mutation 양방향 재실증). pytest 682 passed/45 skip(mongo env 제외), git diff --check clean. candidate hybrid 튜닝·실 Chroma/ES live smoke는 후속. **(검증 후속 보강)** lexical retriever transitioned-candidate guard(needs_review 필터 mutation 재실증)·hybrid 단일 backend 저하 +2·SoT §3/§5B body·`CandidateMemoryRetriever` docstring 갱신(candidate retrieval 실현 반영) → 증분2 회귀 +9→+11, suite 최종 689 passed. | 사용자 결정(G4=A), `plans/04-writing-candidate-retrieval-decisions.md`, `tests/test_context_search_candidate_retrieval.py` |
| v1.6.54 | 2026-07-09 | ⑤ §5 B 후속 — `needs_review` candidate **색인 파이프라인** 신설(브리프 `plans/04-writing-candidate-retrieval-decisions.md` Resolved, HANDOFF Next Tasks #1 (b-2) 증분1). canonical은 `memory_vectors`/ES `memory_lexical`에 색인되지만 candidate는 색인 표면이 없어 vector/lexical retrieval로 바꿀 수 없었다 — 이 증분이 2B.5(canonical 색인)를 candidate에 대해 재현한다. **오너 결정 G0=A**(2 증분: 색인→retriever), **G1=A**(canonical과 **물리 분리** — `candidate_vectors` collection + `candidate_lexical` ES index, `IndexRecordKind.CANDIDATE`·`IndexSyncEvent.CANDIDATE_UPSERTED` 신설; 재유도 source(analysis store)·라이프사이클(needs_review 불변 vs superseded)이 상이), **G2=A**(`record_candidate(s)` extraction persist choke point에서 신규 candidate만 `CANDIDATE_UPSERTED` enqueue — `AnalysisService`에 `reindex_outbox` 주입, MemoryService 중앙화 선례 대칭; idempotent replay 미enqueue), **G5=A**(`derive_memory_index_text` 재사용 — 색인 text = write projection), **G6=A**(worker composite drain vector+lexical, worker는 compose 미추가 out-of-band, retriever는 index 비어도 graceful). **성격: candidate는 상태가 `needs_review` 하나뿐(confirmed/rejected는 Phase 6)이라 de-index 이벤트가 없다** — 색인은 upsert-only, retriever 권위 재유도의 needs_review 필터·drain의 self-heal delete는 Phase 6 전이 도입 시 도달하는 **forward-defense**(v1.6.50 Gate status-stale 분기와 동일 자세). **구현**: 신설 `indexing/candidate_index.py`(`CandidateIndexRecord`, `CandidateVectorIndexAdapter` Protocol, `InMemoryCandidateVectorIndexAdapter`, `CandidateIndexSyncAdapter`[needs_review→upsert / not-found·transitioned→delete], `CompositeCandidateIndexSyncAdapter`) + `indexing/candidate_lexical_index.py`(`CandidateLexicalRecord`, ES adapter[status: needs_review 필터, nori 매핑], `CandidateLexicalIndexSyncAdapter`, `connect_elasticsearch_candidate_index`) + `chroma.py` `ChromaCandidateVectorIndexAdapter`. `indexing/service.py`: `enqueue_candidate_upserted`·`CandidateIndexMutationAdapter` Protocol·worker `candidate_adapter` 주입·`CANDIDATE_UPSERTED` drain dispatch. `main.py`: `_default_analysis_service(reindex_outbox=sync_outbox)`. `scripts/index_sync_worker.py`: `_build_candidate_adapter`(Mongo analysis + embedding + Chroma/fake + ES composite), summary `candidate_backend`. **outbox per-target bookkeeping은 미룸**(v1.6.52 정정과 동일 — enqueue는 배포 sink 구성 모름, worker가 configured sink로만 fan-out). **계약 literal 신설**: `IndexRecordKind.CANDIDATE`·`IndexSyncEvent.CANDIDATE_UPSERTED`. 회귀 +19(`tests/test_candidate_index.py`: enqueue choke point[신규 enqueue/replay 미enqueue/absent no-op] + vector·lexical drain[needs_review upsert / not-found·transitioned delete forward-defense] + InMemory 랭킹·scope + Chroma round-trip·CRUD + ES 문서·needs_review 필터·멱등 delete + composite fan-out + worker dispatch·unconfigured; ES 필터 mutation 양방향 재실증). pytest 673 passed/45 skip(mongo env 제외), git diff --check clean. **(검증 후속 보강)** composite sink failure 전파 +1·worker `_build_candidate_adapter` 3분기(no-Chroma/CHROMA_HOST/ES-composite, G1 `candidate_vectors` collection mutation 재실증)·batch `record_candidates` enqueue(production runner.py 경로, N=1 singular 테스트가 못 잡는 iteration mutation 재실증) +1 → 증분1 회귀 +19→+24, suite 최종 689 passed. | 사용자 결정(G0/G1/G2/G5/G6), `plans/04-writing-candidate-retrieval-decisions.md`, `tests/test_candidate_index.py`, `tests/test_index_sync_worker_script.py` |
| v1.6.53 | 2026-07-09 | 배포 인프라 — v1.6.52 lexical/hybrid를 **compose 배포에서 발화**시키는 전용 ES 서비스 추가(브리프 `plans/04-compose-elasticsearch-service-decisions.md` Resolved, HANDOFF Next Tasks #1 (b-5)). **계약 literal·public 표면 변경 없음**(v1.6.52 코드 계약 그대로) — 배포 표면 실현 + 소폭 견고성 코드 터치. **오너 결정 G0~G6 전부 A**: **G0=A**(ES 서비스 + application `ELASTICSEARCH_URL` 배선, worker는 out-of-band 유지), **G1=A**(자체 `services/elasticsearch/Dockerfile` = 공식 ES + `elasticsearch-plugin install --batch analysis-nori`; 공식 이미지에 nori 미포함이라 필수), **G2=A**(`${ELASTICSEARCH_VERSION:-8.13.4}` — live smoke nori 검증 버전), **G3=A**(`discovery.type=single-node`+`xpack.security.enabled=false`; 코드가 무인증 plaintext·MVP 단일 사용자, 보안 ON은 인증 slice 선행 필요라 범위 밖), **G5=A**(`connect_elasticsearch_memory_index`에 `request_timeout` 기본 30 추가 — 앱/worker 부팅 시 cold nori create가 stock 10s timeout 초과하는 flake 방지, v1.6.52 smoke 견고화 대칭; 미지정 호출자는 기본 30 상속 = §2 최소 변경), **G6=A**(worker 미추가 — 기존 Chroma reindex와 동일 out-of-band, retriever는 인덱스 비어도 graceful). **구현**: `services/elasticsearch/Dockerfile`(nori), `docker-compose.yml`에 `elasticsearch` 서비스(single-node·보안 off·`ES_JAVA_OPTS` 기본 512m·`es_data` 볼륨·curl `_cluster/health` healthcheck) + application `ELASTICSEARCH_URL: http://elasticsearch:9200`·`depends_on: service_healthy` → 배포 canonical retriever 기본 = **hybrid(RRF)**(CHROMA+EMBEDDING 이미 구성). `connect_elasticsearch_memory_index(request_timeout=30)`. **실 bring-up 검증**: `docker compose build/up elasticsearch` → nori(`analysis-nori 8.13.4`) 로드·무인증 plaintext·cluster health green·앱 `connect_` 경로가 실 ES에 `korean`(nori) 분석 인덱스 생성 확인(ephemeral `ai_writte_smoke_b5` 생성→삭제, 잔여 0). 회귀 +3(`ConnectElasticsearchTest`: 기본 request_timeout=30 + nori 인덱스 생성/미재생성/param plumbing; hardcode mutation 양방향 재실증). pytest 654 passed/45 skip(mongo env 제외), git diff --check clean. (오너 독립 감사 `docs/verifications/2026-07-09/compose_elasticsearch_service_b5.md` **합격** → 비차단 관찰 보강: #1 single-node 영속 yellow → `ELASTICSEARCH_MEMORY_SETTINGS`에 `number_of_replicas: 0` 추가[파생 인덱스라 replica 불필요, 실 재현 green/unassigned 0 확인, connect_·live smoke 공유], #2 브리프 G5 논의 절 env-override 문구 정정.) worker compose 서비스·hybrid 튜닝·candidate 색인·outbox per-target bookkeeping은 후속. | 사용자 결정(G0~G6), `plans/04-compose-elasticsearch-service-decisions.md`, `docker-compose.yml`, `services/elasticsearch/Dockerfile`, `tests/test_context_search_memory_lexical_retrieval.py` |
| v1.6.52 | 2026-07-08 | ⑤ §5 B / §8 — Writing canonical memory retrieval의 **ES lexical + hybrid(RRF)** 확장(브리프 `plans/04-writing-memory-lexical-retrieval-decisions.md` Resolved). v1.6.51 vector leg에 이은 lexical leg. **오너 결정 E0/E1=A**(색인 파이프라인+retriever 한 slice), **E2=A**(lexical retriever는 vector 대칭: ES query→hit `memory_id`→`get_memory` 권위 재유도→canonical-only, contracts.md §1.3 "ES는 정본 아님, Mongo 재조회 후 grounding"), **E3=hybrid(RRF) 지금**(vector+lexical을 Reciprocal Rank Fusion으로 결합), **E4=A**(ES 문서 = mongo pointer + nori 분석 text), **E5=A**(fake 회귀 + 실 ES live smoke 실행), **E6=A**(env 배선). **구현**: 신설 `services/application/app/indexing/memory_lexical_index.py`(`MemoryLexicalRecord`, `MemoryLexicalIndexAdapter` Protocol, `InMemoryMemoryLexicalIndexAdapter` 토큰-overlap fake, `ElasticsearchMemoryIndexAdapter`[ES 8.x `query`/`size` kwargs, nori 매핑, delete 멱등], `MemoryLexicalIndexSyncAdapter` worker drain[canonical→index/superseded·deleted→delete, vector leg 대칭], `connect_elasticsearch_memory_index`). `context_search/service.py`: `LexicalCanonicalMemoryRetriever`(seam·반환 타입 불변 → step/item/Gate 무변경) + `HybridCanonicalMemoryRetriever`(각 sub-retriever의 canonical-resolved 순위 리스트를 RRF `1/(k+rank)` k=60로 융합, memory id dedup). `memory_index.py`: `CompositeMemoryIndexSyncAdapter`(worker memory drain을 vector+lexical 팬아웃). `main.py`: canonical retriever 배선을 `_build_vector_canonical_retriever`+`_build_lexical_canonical_retriever`로 분리, 둘 다 있으면 hybrid·하나면 그 backend·없으면 Mongo-direct. worker(`index_sync_worker.py`)는 `ELASTICSEARCH_URL` 있으면 composite drain. requirements에 `elasticsearch>=8,<9` 추가. **구현 중 정정(§1)**: 브리프 E1의 "outbox `targets.elasticsearch` 추가"는 **worker composite fan-out(substance)로 구현하고 persisted envelope의 per-target bookkeeping은 미룸** — enqueue(Mongo choke point)는 배포 ES 구성을 몰라 무조건 ES target을 넣으면 ES 없는 배포에서 영구 pending이 되므로, v1.6.26 outbox envelope·Mongo repo를 건드리지 않고 worker가 configured sink로만 fan-out한다(ES per-target status 추적은 outbox multi-target 추적 확보 시 후속). 회귀 14개(retriever/adapter/drain 13 + worker ES-composite 배선 1; `tests/test_context_search_memory_lexical_retrieval.py`: InMemory lexical 랭킹/project scope, ES adapter 문서·필터드 query·멱등 delete[fake client], lexical retriever 권위 재유도·canonical-only·query-drives-ranking, hybrid RRF 양신호 융합·dedup·단일 backend 저하, worker drain canonical/superseded/missing, composite fan-out; RRF·lexical canonical 필터 mutation 재실증). **실 ES live smoke 통과**(`scripts/phase4_lexical_memory_live_smoke.py`: 실 ES 8.13.4+nori에 한국어 "폭풍" query→canonical storm memory만 매칭·superseded 제외·권위 재유도·hybrid RRF, ephemeral 인덱스 정리). pytest 651 passed/45 skip(mongo env 제외), git diff --check clean. candidate lexical/vector·hybrid 가중치 튜닝·compose 전용 ES 서비스는 후속. (오너 독립 감사 `docs/verifications/2026-07-08/canonical_memory_lexical_hybrid_rrf.md` 합격 → 비차단 관찰 #1[worker ES-composite 배선 회귀 추가]·#2[live smoke request_timeout=30 + finally-delete 견고화] 보강.) | 사용자 결정(E0~E6), `plans/04-writing-memory-lexical-retrieval-decisions.md`, `tests/test_context_search_memory_lexical_retrieval.py`, `tests/test_index_sync_worker_script.py`, `scripts/phase4_lexical_memory_live_smoke.py` |
| v1.6.51 | 2026-07-08 | ⑤ §5 B 후속 — Writing canonical memory retrieval의 vector 확장(브리프 `plans/04-writing-memory-vector-retrieval-decisions.md` Resolved). v1.6.48/v1.6.50이 남긴 "retrieval 레이어 vector/검색엔진 교체"의 canonical leg. **오너 결정 D0=A**(이번 slice는 canonical vector만; candidate는 색인 index가 없어 Mongo-direct 유지, candidate vector는 색인 파이프라인 선행 별도 slice), **D1=A**(권위 재유도: 벡터에서 id 찾고 Mongo store로 재유도), **D2=단일 풀 병합**(3종 memory_type을 각각 query 후 cosine 유사도 하나의 풀로 병합·global limit; `_merge_hits`를 격리해 per-type 선발 전략으로 후속 분리 가능하게 둠), **D3=A**(배포 배선: `CHROMA_HOST`+`EMBEDDING_SERVICE_URL` 둘 다 있으면 vector, 아니면 Mongo-direct fallback), **D4=A**(계약+fake 회귀, 실 Chroma 관통은 sandbox 밖 후속), **D5=A**(ES lexical §8은 범위 밖 별도 브리프). **구현**: 신설 `VectorCanonicalMemoryRetriever`(embed(query)→`MemoryVectorIndexAdapter.query_similar` per-type→hit id로 `MemoryService.get_memory` 재유도→`status is CANONICAL`만 반환; 삭제된 memory의 잔존 벡터는 `MemoryNotFound`로 skip). `.retrieve()` seam·반환 타입 불변 → `_run_canonical_memory_step`/`_item_from_memory`/Gate `_gate_memory_findings` **한 줄도 안 바뀜**(retriever 순수 주입 교체). `create_app`은 `_build_canonical_memory_retriever(memory)`로 env 기반 선택(fallback = 종전 `MongoDirectCanonicalMemoryRetriever`, `_build_semantic_matcher`의 memory_vectors 배선 선례 재사용). candidate retriever·Gate·item 계약 전부 무변. 회귀 6개(`tests/test_context_search_memory_vector_retrieval.py`: 권위 재유도+relevance 순서·global limit·stale 벡터(삭제/superseded) 격리[under-strict]·단일 풀 병합이 per-type 아님[over-strict]·seam 불변 micro memory item·query가 embed→랭킹에 반영됨 lock; CANONICAL 필터·`_merge_hits`·embed(query) 무시 mutation 3방향 재실증). pytest 637 passed/45 skip(mongo env 제외), git diff --check clean. candidate vector(색인 선행)·ES lexical(§8)·실 Chroma live smoke·relevance 품질 spike는 후속. (오너 독립 감사 `docs/verifications/2026-07-08/canonical_memory_vector_retrieval.md` 합격 → 비차단 관찰 #1[HANDOFF 버전 주석]·#3[Phase 4 prose]·#4[query lock] 보강.) | 사용자 결정(D0~D5), `plans/04-writing-memory-vector-retrieval-decisions.md`, `tests/test_context_search_memory_vector_retrieval.py` |
| v1.6.50 | 2026-07-08 | ⑤ §5 B 후속 — `needs_review` candidate의 Writing 포함(브리프 `plans/04-writing-candidate-context-decisions.md` Resolved). v1.6.48이 남긴 "바로 다음 slice". v1.6.48 canonical inclusion machinery(retriever seam → step → item → Gate origin 분기)를 그대로 재사용한다. **오너 결정 D1=A/D2=A/D3=A/D5=A/D6=A/D7=A + D4=B**(review_status 필드 지금 신설) + **안전선=A**(Phase 6 §62 위장 금지를 라벨+권위필드 배제로 지킴). **구현**: 신설 `ContextNeed.CANDIDATE_MEMORY`(`NEED_ALLOWED_TOOLS`=(mongo,), micro). `ContextItem.review_status` 필드(candidate origin만 의미, 현재 `needs_review`; canonical/source-block은 빈값). `CandidateMemoryRetriever` seam + `MongoDirectCandidateMemoryRetriever`(project-wide `needs_review`-only, limit, query 무시=랭킹 후속). analysis 층에 project-wide `list_needs_review_candidates`(service/Protocol/in-memory/mongo) 신설. `_run_candidate_memory_step`가 retriever로 candidate를 받아 `_item_from_candidate`로 `ContextItem`(status=CANDIDATE, pointer.collection=`analysis_candidates`·document_id=candidate_id·version_id=""·content_hash="", text=`derive_memory_index_text(candidate_type, payload)` 재사용, review_status=candidate status) 변환(retriever 미주입 시 빈 step, 예외→BACKEND_ERROR step failure). **Gate 재편(v1.6.48 계약 보존하며 좁힘)**: `evaluate_context_gate`에 `analysis_service` param 추가. candidate 금지는 폐지가 아니라 **candidate origin(`analysis_candidates`)만 예외**로 좁혔다 — 그 origin은 `_gate_candidate_findings`가 analysis store `get_candidate`로 (존재+`status is NEEDS_REVIEW`+project) 재검증(없으면 `stale_item`, status≠needs_review면 `stale_item`[Phase 6 forward-defense], service 미주입이면 `candidate_gate_unconfigured`); memory/source-block origin의 candidate-status는 종전대로 `candidate_item_not_allowed`(v1.6.48 회귀 무변). `create_app`이 candidate retriever(analysis)·gate analysis_service를 배선. candidate item은 `micro_evidence`에만 실리고 macro/constraints/do_not_use 배제(§62). **정정(§1)**: `needs_review→confirmed/rejected` 전이는 Phase 6이라 현재 승격된 candidate는 needs_review 유지 → canonical·candidate 양쪽 노출 가능(D7=A no-dedup 수용). 회귀 9개(`tests/test_context_search_candidate_memory.py`: micro 라벨 배치·§62 권위필드 배제·retriever needs_review-only/limit·retriever 예외→BACKEND_ERROR·Gate 4방향[pass/missing-stale/status-stale/unconfigured]·비-candidate-origin candidate-status 여전히 거부; Gate status-check·candidate-origin 예외 mutation 양방향 재실증). pytest 630 passed/45 skip(mongo env 제외), git diff --check clean. vector/ES relevance retrieval·semantic canonical↔candidate dedup·Writing 소비 강제는 후속. | 사용자 결정(D4=B·안전선=A), `plans/04-writing-candidate-context-decisions.md`, `tests/test_context_search_candidate_memory.py` |
| v1.6.49 | 2026-07-08 | 추적 부채 #8(2026-07-06 이월, `ProviderError`→502 패턴을 `/context-search`·2A extraction 경로에 적용)을 조사·폐쇄했다. **조사 결과 부채는 stale**: 두 경로는 이미 502로 매핑되고 있었다(부채 노트의 "uncaught → HTTP 500"은 현재 코드와 어긋남). `/context-search`는 service `_build_plan`의 `except Exception → ContextSearchFailed(llm_error)`가 planner의 async await까지 포함해 provider 실패를 감싸고(엔드포인트가 `ContextSearchFailed`→502), 2A `/run`은 엔드포인트 `except Exception → 502` catch-all이 runner가 재던진 `ProviderError`를 받는다. 노트가 지목한 compare 엔드포인트만 catch-all이 없어 실제로 누출했고 그래서 명시적 `except ProviderError`(v1.6.34 taxonomy)가 필요했다. **오너 결정(명시적 분기 추가)**: 동작은 이미 502로 동일하나 compare와의 대칭·의도 가독·미래 refactor 방어를 위해 두 경로의 실제 provider 호출 지점에 명시적 `except ProviderError`를 추가했다 — `/run` 엔드포인트(catch-all 앞, `str(exc)`→502)와 context-search service `_build_plan`(generic catch 앞, `ContextSearchFailed(llm_error, "planner provider error: …")`). 2A extraction adapter에는 wrap하지 않는다(거기서 감싸면 `AnalysisExtractionError`→400으로 **오분류**되므로 502 경계에 둔다). 회귀 +2(`test_context_search_api.py::test_planner_provider_error_maps_to_502_llm_error` async 실 `ProviderError`→502+"provider error" detail; `test_application_api.py::test_analysis_run_endpoint_maps_real_provider_error_to_502` 실 `ProviderError` 재던지기→502+failure_reason=provider_error). mutation 양방향 재실증(context-search 명시 분기 무력화 시 detail assert 실패; `/run` 두 catch 무력화 시 uncaught 500). pytest 621 passed/45 skip(mongo env 제외), git diff --check clean. 계약(ProviderError→502) 변경 없음, 명시화·회귀 lock·부채 폐쇄만. | 오너 결정(명시적 분기), `tests/test_context_search_api.py`, `tests/test_application_api.py` |
| v1.6.48 | 2026-07-07 | Writing ContextPackage에 canonical memory 포함(⑤ §5 B)을 구현했다(브리프 `plans/04-writing-canonical-context-decisions.md` Resolved). **framing 전환**: 2026-07-05 `04-context-package-completion-decisions.md`가 `needs_review` candidate 포함을 canonical store 부재로 D1=B로 미뤘는데, Phase 2B(2B.1~2B.6)가 canonical store를 제공해 종속을 해소했다. Gate는 이미 candidate 금지·canonical 허용이라, 실제 작업은 (1) canonical memory를 Writing package에 micro로 넣기 + (2) memory item의 별도 Gate 검증(현 `_gate_stale_findings`가 source-block snapshot을 하드 가정해 memory를 무조건 reject하던 것). **오너 결정 D1=A**(canonical만, candidate는 종전대로 금지 — candidate 포함은 A의 machinery를 재사용하는 바로 다음 slice), **D2=A**(Mongo-direct 먼저; retrieval 레이어와 권위 재유도(항상 memory store)를 분리 설계해 후속에서 vector·검색엔진으로 retrieval 교체 가능). **구현**: 신설 `ContextNeed.CANONICAL_MEMORY`(`NEED_ALLOWED_TOOLS`=(mongo,), micro=`MACRO_NEEDS` 미포함). `CanonicalMemoryRetriever` seam + `MongoDirectCanonicalMemoryRetriever`(canonical-only, limit, query 무시=랭킹 후속). `_run_canonical_memory_step`가 retriever로 canonical `MemoryEntry`를 받아 `_item_from_memory`로 `ContextItem`(status=CANONICAL, pointer.collection=`memory_entries`·document_id=memory_id·version_id=str(version)·content_hash="", text=`derive_memory_index_text` 재사용) 변환(retriever 미주입 시 빈 step, 무실패). `evaluate_context_gate`가 `memory_service` param 추가 + item origin(pointer.collection==`memory_entries`) 분기 — memory item은 `get_memory`로 (존재+`status is CANONICAL`+project) 재검증(`_gate_memory_findings`), 아니면 source-block SOT 검증 유지. candidate 금지 유지(문구 "later slice"→"canonical only"). `create_app`이 retriever(memory)·gate memory_service를 배선. 회귀 8개(`tests/test_context_search_canonical_memory.py`: micro 배치·memory pointer·retriever canonical-only/limit·gate pass/superseded-stale/missing-stale/unconfigured/candidate-금지; gate·retriever guard mutation 재실증). pytest 618 passed/45 skip(mongo env 제외), git diff --check clean. relevance vector retrieval·candidate 포함은 후속 slice. **독립 검증(`docs/verifications/2026-07-07/writing_canonical_memory_inclusion.md`) 조건부 합격 → 폐쇄**: 차단(boundary matrix cell #10 — `_run_canonical_memory_step`의 retriever 예외→`BACKEND_ERROR` 분기(`service.py:311-332`) 회귀 부재; SoT §5 retriever 실패 taxonomy 계약-grounded·프로덕션 Mongo 장애로 도달 가능)를 `CanonicalMemoryRetrieverFailureTest`(raise retriever 주입→`BACKEND_ERROR` 기록 under-strict + step 격리로 search 비-crash over-strict)로 채우고(mutation 양방향 재실증), 비차단 #2(`_item_from_memory`의 `snapshot_id`/`sot_reloaded` inert placeholder)를 주석 문서화. 비차단 #3(`test_memory_mongo.py` sandbox fail)은 부모 커밋 재현·`memory/` 미변경으로 env artifact(무조치). 회귀 +1(8→9), pytest 619 passed/45 skip. | 사용자 결정(D1=A·D2=A), `plans/04-writing-canonical-context-decisions.md`, `tests/test_context_search_canonical_memory.py` |
| v1.6.47 | 2026-07-07 | Phase 2B.6(event/open_question 의미적 identity resolution)를 구현했다(브리프 `plans/02b-6-semantic-identity-resolution-decisions.md` Resolved). **헤드라인 정정**: HANDOFF/2B.2의 "`PriorMemoryBackend`를 semantic으로 교체" 표현은 seam 위치 오기였다 — event/open_question을 always-`create`로 만드는 실제 지점은 2B.3 compare의 `_find_matches`(`scope is None → ()`)다. **오너 결정 D1=A**: semantic은 compare `_find_matches`에 들어간다(`PriorMemoryBackend`는 coarse 패키징이라 무관). **D4=A off 기본**: threshold는 주입값·기본 `None`(off)이라, 설정 전까지 event/open_question은 종전대로 always-create(회귀 보존·안전); 실값은 실 embedding+실 데이터 fixture 캘리브레이션 후(auto-promotion threshold v1.6.39 D2=B와 정합 — 추측값으로 canon 병합 금지). **구현**: `MemoryVectorIndexAdapter`에 `query_similar(project_id, memory_type, vector, limit)` 추가(in-memory fake cosine + `ChromaMemoryVectorIndexAdapter` collection.query, memory_type $and 필터). `analysis/semantic_matcher.py`의 `EmbeddingSemanticMatcher`가 candidate를 **쓰기와 동일한** `derive_memory_index_text` projection + 동일 embedding으로 embed → `memory_vectors` query → cosine ≥ threshold인 top-1(D6=A, self-exclusion·canonical 필터 후)을 반환. `AnalysisCompareService`에 `semantic_matcher` 주입 seam(off 기본, `_find_matches`의 scope-None 분기가 소비). `_default_compare_service`가 env `ANALYSIS_SEMANTIC_MATCH_THRESHOLD`+`CHROMA_HOST` 있을 때만 matcher 배선(in-memory fake는 worker와 분리라 실 Chroma 필요). character 경로는 결정적 name key 유지(D5=A). read-after-write eventual consistency는 수용 경계(D8). **누적 오너 확인 폐쇄**: event/open_question always-create 누적을 이 slice가 해소(D4 캘리브레이션 완료 시 실발화). 회귀 +10(`tests/test_analysis_semantic_matcher.py` 8: threshold 양방향·top-1·memory_type scope·self-exclusion·superseded skip·judge 관통·off-기본 always-create; `tests/test_chroma_memory_adapter.py` +2: query_similar 랭킹/scope·limit; threshold·memory_type guard mutation 재실증). pytest 608 passed/45 skip(mongo env 제외), git diff --check clean. 실 embedding threshold 캘리브레이션은 sandbox 밖 후속. **독립 검증 합격**(`docs/verifications/2026-07-07/phase_2b_6_semantic_identity_resolution.md`, 차단 없음); 비차단 관찰 #1(운영 footgun) 보강으로 `_build_semantic_matcher`에 wiring guard 추가 — threshold+CHROMA_HOST 켜고 `EMBEDDING_SERVICE_URL` 누락 시 첫 query의 차원 불일치 대신 startup에서 `RuntimeError` fail-fast(회귀 2 + mutation 재실증, 610 passed). | 사용자 결정(D1=A·D4=A), `plans/02b-6-semantic-identity-resolution-decisions.md`, `tests/test_analysis_semantic_matcher.py`, `tests/test_chroma_memory_adapter.py` |
| v1.6.46 | 2026-07-07 | Phase 2B.5 **증분 2: 라이브 배선**(실 Chroma memory adapter + worker/create_app 배선 + backfill + live smoke). **오너 결정(2026-07-07)**: canonical을 만드는 모든 경로(2B.4 apply·2B.1 수동 promote·auto-promote)가 vector 재색인 enqueue한다(정기 backfill-only 아님). 이를 위해 enqueue를 **`MemoryService` 단일 choke point로 중앙화**했다 — `promote_candidate`/`_versioned_upsert`의 비-replay mint 시 `_enqueue_reindex`가 `enqueue_memory_upserted`를 호출(idempotent_replay는 미enqueue). 증분 1의 `MemoryApplyService.reindex_outbox` seam은 이 중앙화로 흡수·제거(apply는 promote/versioned를 호출하므로 자동 커버). memory→indexing 순환 회피 위해 `MemoryReindexOutbox` Protocol을 `memory/service.py`에 로컬 정의. **실 Chroma**: `indexing/chroma.py`의 `ChromaMemoryVectorIndexAdapter`(upsert/delete-by-`{project_id,memory_id}` $and/list) + `memory_record_to_chroma`/`memory_record_from_chroma`(metadata round-trip). collection `memory_vectors`(env `CHROMA_MEMORY_COLLECTION`), source_block collection과 분리. **배선**: `create_app`이 `_default_memory_service(reindex_outbox=sync_outbox)`로 3경로 enqueue를 켠다(주입 memory_service는 자체 wiring 유지). `scripts/index_sync_worker.py`가 `_build_memory_adapter`(Mongo MemoryService + embedding(`EMBEDDING_SERVICE_URL`/fake) + Chroma/fake memory collection)로 `MEMORY_UPSERTED`를 drain, summary에 `memory_backend`. **backfill** `scripts/phase2b5_reindex_memory.py`(project canonical 전수 직접 embed+upsert, superseded 제외, outbox 우회 D7). **live smoke** `scripts/phase2b5_memory_reindex_live_smoke.py`(promote→outbox→worker→실 Chroma record 확인, sandbox 밖). 회귀 +20(`test_memory_vector_index` promote-path 4·`test_chroma_memory_adapter` 11·`test_analysis_apply_api` HTTP enqueue wiring 1·`test_index_sync_worker_script` memory adapter 분기 2·`test_phase2b5_reindex_memory_script` 4; choke-point enqueue mutation 재실증). pytest 598 passed/45 skip(mongo env 제외), git diff --check clean. 실제 Chroma 관통 live 실행은 sandbox 밖 후속. | 사용자 결정(promote 경로 enqueue), `plans/02b-5-memory-vector-reindex-decisions.md`, `tests/test_chroma_memory_adapter.py`, `tests/test_index_sync_worker_script.py`, `tests/test_phase2b5_reindex_memory_script.py` |
| v1.6.45 | 2026-07-07 | Phase 2B.5(memory→vector 재색인) **증분 1: 계약+fake+회귀**를 구현했다(브리프 `plans/02b-5-memory-vector-reindex-decisions.md` Resolved). 착수 조사로 canonical memory가 vector index에 미색인(source_block만)임을 확인해 record kind·collection·트리거가 전부 신규임을 surface했다. **오너 결정**: **D3=B** async outbox→worker(skew 무관용, Phase 4 인프라를 memory kind로 확장), **D6=write-only**(semantic read=`PriorMemoryBackend` vector 교체는 후속 slice), D1=A(memory_type별 결정적 text projection), D2=A(별도 `memory_vectors` collection + `IndexRecordKind.MEMORY`), D4=A+교정, D5(memory 전용 record 신설), D7=A(backfill 스크립트, 후속). **신설 literal**: `IndexRecordKind.MEMORY`, `IndexSyncEvent.MEMORY_UPSERTED`, `MemoryIndexRecord`(id/kind/project_id/memory_id/memory_type/version/status/text/vector). **projection(D1=A)**: `indexing/memory_index.py`의 `derive_memory_index_text` — character=`name\nobservation`, event=`event`, open_question=`question`(계약 필드만 읽음, `analysis/schema.py` 검증 후). **D4 교정(구현 중 계약 교차검증, CLAUDE.md §1)**: 원 추천("record id=memory_id upsert 교체")은 2B.4 append-only(version별 새 id)와 모순 → 교정 계약: worker(`MemoryIndexSyncAdapter`)가 memory 로드해 (a) non-canonical이면 그 id 벡터 삭제 후 skip(순서 무관 self-healing), (b) canonical이면 새 version upsert + `supersedes` 있으면 이전 id 벡터 삭제. `MemoryStatus`(2B.4)·`supersedes`로 결정적·멱등. no_change/conflict=enqueue 없음. event/open_question은 항상 create(누적, 알려진 caveat). **트리거(D3=B)**: `IndexSyncOutboxService.enqueue_memory_upserted`(source=`memory_entries`/memory_id/version, dedup per memory_id) + `IndexSyncWorker`가 `MEMORY_UPSERTED`를 optional `memory_adapter`로 dispatch(미구성 시 RuntimeError) + `MemoryApplyService`가 optional `reindex_outbox`로 CREATED/VERSIONED 시 enqueue. **라이브 배선은 증분 2**(실 Chroma `memory_vectors` adapter + worker/create_app 배선 + backfill 스크립트 + live, sandbox 밖) — 증분 1의 create_app은 `reindex_outbox` 미배선(drain 측 부재 시 undrainable entry 미생성, 2B.3 judge-None seam 리듬과 동일). 회귀 15개(`tests/test_memory_vector_index.py`: projection 3 + enqueue 5(no_change/conflict over-strict·dedup·outbox-none noop) + worker 7(canonical-only update 양방향·self-heal 고립·missing→능동 삭제·**adapter 미구성 dispatch→BACKEND_ERROR+requeue**·idempotent·event 누적); 핵심 guard 4개 mutation 재실증). pytest 580 passed/45 skip(mongo env 제외), git diff --check clean. **독립 검증(`docs/verifications/2026-07-07/phase_2b_5_memory_vector_reindex_increment_1.md`) 조건부 합격 → 폐쇄**: 차단(worker `MEMORY_UPSERTED`+adapter 미구성→RuntimeError 분기 회귀 부재, boundary matrix 행16 빈 셀)을 `test_memory_upserted_without_adapter_records_backend_error`로 채우고(mutation 재실증), 비차단(missing-memory 삭제 경로 seed 강화·브리프 D1 prose `observation` 정정)도 보강. | 사용자 결정(D1~D7), `plans/02b-5-memory-vector-reindex-decisions.md`, `tests/test_memory_vector_index.py` |
| v1.6.44 | 2026-07-06 | Phase 2B.4(proposal→실제 memory versioned upsert) 첫 code slice를 구현했다(브리프 `plans/02b-4-memory-versioned-upsert-decisions.md` Resolved). **D1=A** compare와 분리된 명시 apply: `services/application/app/analysis/apply.py`의 `MemoryApplyService.apply_proposals`가 `ActionProposal`을 결정적 memory 쓰기로 반영한다(LLM 없음, 라벨은 이미 확정). `create`→새 canonical(2B.1 승격 재사용, version=1), `update`→새 version(payload=candidate 교체), `add_evidence`→새 version(payload 보존), `no_change`→쓰기 없음, `conflict`→review-only skipped(D7, merge/split은 2B.3 미산출). **D2=A** append-only: `MemoryStatus.SUPERSEDED` 도입 + `MemoryEntry.supersedes` 필드, versioned upsert는 새 canonical(version=prev+1, supersedes=prev.id)을 먼저 삽입하고 이전 entry를 `superseded`로 전이(이전 version 불변 보존). canonical-only prior 필터가 superseded 자연 제외 → **2B.2 O1 폐쇄**(`test_superseded_memories_excluded_from_prior_memory`, 필터 mutation 재실증). **D3** `update`=payload 교체·source_ref_ids union·confidence=candidate·provenance=candidate / `add_evidence`=payload 보존·source union·confidence=max(prev,candidate)·provenance=prev. **D4=A** memory→vector 재색인은 분리(2B.5) — memory 색인 자체가 미존재라 독립 설계 필요. **D5=A** idempotency `(project_id, source_candidate_id)`(한 candidate=한 반영, 재적용 replay). 타입 불일치·non-canonical target·candidate 부재는 거절. **D6=A HTTP** `POST /projects/{id}/analysis/jobs/{job_id}/apply`(검토한 proposal을 body로 받아 반영, action별 결과 요약; unknown action/candidate 400, missing matched memory·cross-project 404). 회귀 23개(`tests/test_memory_apply.py` 13 + `tests/test_analysis_apply_api.py` 9 + context O1 1; 독립 검증 조건부 합격 후 **non-canonical target 거절 회귀**(boundary matrix 빈 셀)·update conf over-strict(prior>candidate)·add_evidence replay·MissingMatchedMemory HTTP 보강, mutation 재실증) + live Mongo versioned upsert round-trip 1(throwaway mongo 통과). Mongo `update_memory`(replace_one)·`supersedes` round-trip 추가. pytest 565 passed/45 skip(mongo env 제외). | 사용자 결정(D1~D7), `plans/02b-4-memory-versioned-upsert-decisions.md`, `tests/test_memory_apply.py`, `tests/test_analysis_apply_api.py`, `tests/test_memory_mongo.py` |
| v1.6.43 | 2026-07-06 | Phase 2B.3.2(실제 Gateway 터미널-JSON compare judge)를 구현했다 — 2B.3의 `CompareJudge` seam을 실 adapter로 채웠다(4.1→4.2→2B.3→2B.3.2 리듬). `services/application/app/analysis/compare_judge.py`의 `TerminalJsonCompareJudge`(async)가 versioned prompt `analysis_compare_v1`(task_type `analysis_compare`, 기존 `prompt_templates` 저장소 재사용)로 매칭 (candidate payload + 기존 memory payload)을 Gateway `/v1/generate` 1-turn에 보내고 `{"action","rationale"}` JSON을 strict parse한다. malformed/fenced/out-of-set이면 1회 repair, 그래도 실패면 `InvalidJudgeResult`(→HTTP 502). 허용 action은 matched-pair 4종(update/add_evidence/no_change/conflict)뿐이고 **`create`는 judge 출력에서 거절**(no-match 결정적 전용) — `AnalysisCompareService`의 `JUDGE_ACTIONS` guard는 fake seam 방어로 유지. `create_app`은 `_default_compare_service(memory)`로 env `LLM_GATEWAY_BASE_URL` 있으면 이 judge를 wiring하고(`ANALYSIS_COMPARE_MAX_TOKENS` 기본 512), 없으면 종전대로 judge 미주입(매칭 pair 503). 판정 경계(update↔add_evidence↔no_change↔conflict)는 프롬프트 action 정의로 안내하고 fixture는 adapter parse/repair를 잠근다. live smoke `scripts/phase2b3_compare_judge_live_smoke.py`(sandbox 밖 실행). 회귀 13개(`tests/test_analysis_compare_judge.py` 11 + env 기본 factory wiring 2). pytest 541 passed/45 skip(mongo 제외). | 사용자 결정(D1=A), `plans/02b-3-analysis-compare-action-decisions.md`, `tests/test_analysis_compare_judge.py` |
| v1.6.42 | 2026-07-06 | Phase 2B.3(candidate↔canonical 대조 + action proposal) 첫 code slice를 구현했다(브리프 `plans/02b-3-analysis-compare-action-decisions.md` Resolved). **D2=A 결정적 scope key**: `services/application/app/memory/scope.py`의 `MemoryScope(scope_type, scope_id)` + `derive_scope(memory_type, payload)` — character만 `scope_type="character", scope_id=정규화(name)`(공백 collapse + casefold), event/open_question은 엔티티 id 부재로 `None`(identity 대조 제외, semantic은 후속 seam). `MemoryEntry`에 `scope` 필드 추가, 2B.1 승격이 candidate→memory 시 산출(D5=A, Mongo round-trip·`_memory_payload` 포함). `PriorMemoryItem`에 `scope` 추가로 **§8 ⑧ 완전 완성**(memory type/scope/status/version/검색 이유). **compare(D1=A 터미널 JSON seam·D3=A 하이브리드)**: `services/application/app/analysis/compare.py`의 `CompareAction`(create/update/add_evidence/no_change/conflict), `ActionProposal`, `CompareJudge` 주입 seam, `AnalysisCompareService`. 결정적 scope 매칭으로 후보를 좁혀 (a) 매칭 없음→`create`(결정적, event/question 포함), (b) 정확히 1개→주입 judge가 update/add_evidence/no_change/conflict 라벨(judge가 `create` 반환은 `InvalidJudgeResult`), (c) 복수 canonical 동일 identity(2B.1 허용 중복)→결정적 `conflict`. **D6 self-exclusion 확정**: `analysis_job_id == 대상 job`인 memory 제외(같은 job 승격분은 항상 no_change 노이즈라 유지). **D4=A proposal only**(memory 쓰기 없음, 2B.4). **D7=A HTTP** `POST /projects/{id}/analysis/jobs/{job_id}/compare`(job candidate별 proposal 반환, 매칭인데 judge 미구성 시 503, `InvalidJudgeResult` 502, missing project/job 404). 실제 Gateway 터미널-JSON judge adapter + versioned prompt + live smoke는 다음 증분(2B.3.2). 회귀 21개(`tests/test_memory_scope.py` 5 + `tests/test_analysis_compare.py` 9 + `tests/test_analysis_compare_api.py` 7, self-exclusion·judge-create 거절(service+HTTP 502)·중복 conflict·다른 identity→create 양방향). pytest 528 passed/45 skip(mongo 제외). | 사용자 결정(D1~D7), `plans/02b-3-analysis-compare-action-decisions.md`, `tests/test_memory_scope.py`, `tests/test_analysis_compare.py`, `tests/test_analysis_compare_api.py` |
| v1.6.41 | 2026-07-06 | Phase 2B.2(prior-memory 검색 + Analysis 비교용 ContextPackage, §8 ⑧) 첫 code slice를 구현했다(`services/application/app/context_search/prior_memory.py`, 브리프 `plans/02b-2-analysis-context-package-decisions.md` Resolved). 신설 literal: `ContextSearchPurpose.ANALYSIS_CONTEXT`, `ContextNeed.PRIOR_MEMORY`. **D1=A(검색+패키징, 판정은 2B.3)**: 이 slice는 coarse 후보군(같은 project·같은 `memory_type`의 canonical `MemoryEntry`)을 결정적으로 조회·패키징만 하고, 동일성/action 판정과 scope key 정밀화는 2B.3다. **D2=A + semantic seam**: `PriorMemoryBackend` Protocol 주입 seam(현재 `DeterministicPriorMemoryBackend`=결정적 key 조회, 후속 semantic 검색이 같은 인터페이스로 교체). **D3=A**: 단일 ContextPackage schema에 `PriorMemoryItem`(memory_id/memory_type/value/status/version/source_ref_ids/match_reason) 추가 — taxonomy 5필수(값·상태·source·version·비교 이유) 충족. `value`는 MemoryEntry의 `payload`(Mapping, F3). `scope`는 MemoryEntry 부재라 담지 않음 → §8 ⑧ 추적은 2B.3까지 열림(D1=A 위임). ContextPackage에 `prior_memories` field 추가, `trace`는 optional(analysis_context는 planner/plan 없어 `None`). **D4=B(A 포함 hybrid)**: 검색 primitive는 `memory_types` 파라미터화(`AnalysisContextRequest`), 진입면은 job-aware(HTTP가 job candidate types → coarse memory_type 집합 유도). 빈 `memory_types`=빈 package(비교 대상 없음, 절대 전체 아님). **F4 self-exclusion**: `exclude_job_id`로 그 job이 승격한 memory 제외(HTTP는 `exclude_job_id=job.id`) — 오너 승인 잠정값, 2B.3 compare 상호작용 관찰 후 확정. **D5=A(purpose별 Gate 분기)**: `evaluate_analysis_context_gate`는 analysis_context 전용 — candidate 금지는 무적용(대상이 canonical), Writing item(macro/micro) 누출만 차단. cross-project 격리는 project-scoped 조회 계약이 보장(F5, PriorMemoryItem에 project_id 없음). MemoryStatus가 canonical 단일이라 non-canonical guard는 미도입(불가능 시나리오 방어 회피). **D6=B**: HTTP `POST /projects/{project_id}/analysis/jobs/{job_id}/context`. `/context-search`(Writing 전용)는 이제 `analysis_context` purpose를 400으로 거절(purpose 표면 분리). 회귀 17개(`tests/test_analysis_context.py` 10 + `tests/test_analysis_context_api.py` 7 — 독립 검증 후속 O6 다중-type 합집합/중복제거 1개 포함). 검증 후속으로 HTTP endpoint의 도달 불가 `InvalidAnalysisContextRequest→400` dead-code를 제거했다(request 검증은 service layer 소유). | 사용자 결정, `plans/02b-2-analysis-context-package-decisions.md`, `tests/test_analysis_context.py`, `tests/test_analysis_context_api.py` |
| v1.6.40 | 2026-07-05 | Phase 2B.1(canonical `MemoryEntry` store + candidate 승격) 첫 code slice를 구현했다(`services/application/app/memory/`). `MemoryEntry`는 canonical 저장 단위로 status `canonical`(단일 literal), 승격된 candidate의 `payload`/`provenance`/`source_ref_ids`/`confidence`를 보존하고 첫 `version=1` + 감사 필드(`analysis_job_id`/`source_candidate_id`/`promotion_mode`/`applied_threshold`)를 가진다. `memory_type`/`provenance`는 2A enum을 재사용한다(D5=A: 2A 3종 유지). 승격 두 경로: (1) 수동 승인 `promotion_mode="manual"`은 confidence 무관 항상 `canonical`; (2) 결정적 threshold gate `promotion_mode="auto_threshold"`는 `threshold is not None and confidence >= threshold`일 때만 승격하고 미만은 candidate `needs_review` 유지(수동 경로 보존). 자동 승격 threshold는 주입값 `MEMORY_AUTO_PROMOTION_THRESHOLD`이며 기본 `None`(off) — 품질 fixture 전까지 추측값으로 canon 미양산(D2=B). 승격 idempotency는 `(project_id, source_candidate_id)` unique index(`uniq_memory_candidate_promotion`, 같은 candidate 재승격=동일 memory). HTTP: `POST /projects/{id}/analysis/candidates/{cid}/promote`(수동), `POST /projects/{id}/analysis/jobs/{jid}/auto-promote`(gate 적용), `GET /projects/{id}/memory[/{mid}]`(조회, project 격리). **2B.1 slice 경계(작업자, 오너 확인 대상)**: D3 entity/scope key 매칭·충돌 해소는 compare(2B.3) 소관이라 여기서 구현하지 않는다 — 2B.1은 create-only/`version=1`이고 유일성은 `source_candidate_id`로만 잡으며, scope key 산출·같은 entity 중복 canonical의 update/merge는 2B.3에 위임한다(브리프 "이후 update는 2B.3 upsert 연결"과 정합). `memory_entries`는 운영 collection에 추가. | `tests/test_memory_phase2b.py`, `tests/test_memory_api.py`, `tests/test_memory_mongo.py`, `plans/02b-analysis-compare-kickoff-decisions.md` |
| v1.6.39 | 2026-07-05 | Phase 2B(기존 기억 대조·canonical memory) 착수 결정 브리프를 승인했다(`plans/02b-analysis-compare-kickoff-decisions.md`). **D1=A**: 첫 sub-slice(2B.1)는 canonical memory store(`MemoryEntry`) + `needs_review` candidate 승격을 세우고 compare/action은 후속(대조 상대를 먼저 확보, Phase 4 ⑤/⑧ 종속의 토대). **D2=B**: confidence 기반 자동 승격을 채택하되, **AI가 아니라 결정적 시스템 threshold gate가 승격**한다(Analysis AI 경계 "canon 확정 금지" 유지, 문구 개정 없음) — threshold 이상만 자동 `canonical`, 미만은 `needs_review` 유지 + 수동 승인 경로 보존. threshold는 품질 fixture/benchmark 근거(SoT v1.6.13 budget 선례)이며 그 전까지 보수적 주입 설정값(추측값으로 canon 양산 금지). 승격 MemoryEntry는 승격 근거(confidence/threshold/source_refs/provenance/job_id) 기록. **D3=A**: entity resolution은 결정적 key(`memory_type + scope_type + scope_id` + 정규화 name) 완전일치만, 별칭/동명이인은 `merge/split` review 후보(자동 병합 없음). **D4=A**: action literal `create/update/add_evidence/no_change/conflict` + `merge/split`(review-only); 판정 경계는 2B.3 fixture로 확정. **D5=A**: taxonomy는 2A 3종 유지. ⑧ Analysis 비교용 package(`analysis_context` purpose)와 ⑤ Writing canonical 포함은 각 후속 slice 종속으로 명문화. 이 브리프는 결정만이며 코드는 2B.1부터. | 사용자 결정, `plans/02b-analysis-compare-kickoff-decisions.md`, `plans/02-analysis-pipeline.md` §Phase 2B, `plans/analysis-memory-taxonomy.md` |
| v1.6.38 | 2026-07-05 | Phase 4 ContextPackage 완성(⑤ candidate 포함 §5 B / ⑧ Analysis 비교용 뷰 §8 C)이 Phase 2B에 종속됨을 오너 결정으로 확정했다(브리프 `plans/04-context-package-completion-decisions.md`, D1=B). `needs_review` candidate 포함은 지금 하지 않고 승인/canonical 승격 경로가 생기는 Phase 2B로 미룬다 — canonical store 부재 상태의 "지금 포함"은 미검증 후보를 Writing 근거로 흘려보내는 것이라 `evaluate_context_gate`의 candidate 라벨 금지(Writing-안전성 방어선)를 근거 없이 완화하게 된다. 따라서 D2/D3/D4(candidate 검색 경로·`prior_memory` need 신설·Gate 완화)는 열지 않는다. ⑧ Analysis 비교용 확장 필드는 착수 브리프 §8대로 Phase 2B 착수 브리프가 확정한다. Phase 4는 현재가 합리적 정지점이다(Writing용 ContextPackage는 Phase 5 MVP에 충분, candidate 미포함, Gate가 candidate 금지 유지). 코드/public literal 변경 없음 — 미확정 항목의 상태만 "Phase 2B 종속"으로 확정. | 사용자 결정, `plans/04-context-package-completion-decisions.md`, `plans/04-agentic-search-kickoff-decisions.md` §5·§8 |
| v1.6.37 | 2026-07-05 | Phase 3B index sync worker를 real Chroma archive mutation에 배선했다(Next Tasks worker→real Chroma). `ChromaArchiveIndexMutationAdapter`(`services/application/app/indexing/chroma.py`)는 archive event를 real Chroma delete로 처리한다: `project_archived`는 `{project_id}` 매칭 derived source-block record 전부, `draft_archived`는 project-scoped `{project_id, draft_id}` 매칭 record만 삭제한다(`entry.source.mongo_id`가 draft id). derived record는 SOT에서 rebuild 가능하므로 cleanup은 tombstone이 아니라 **delete**다. 삭제 대상이 이미 없으면 목표 상태(archived 콘텐츠가 derived index에 없음)가 달성된 것으로 보고 `DerivedIndexRecordNotFound`를 raise → worker가 idempotent success로 처리한다(브리프 §8.2). 삭제 전 `get(where, include=[])`로 존재를 확인하고, `ids` 길이가 0이면 delete를 호출하지 않는다(numpy-like truthiness 회피 위해 truthiness 대신 `len()` 사용). `ChromaCollection` protocol에 `delete(where)`가 추가됐다. worker command `scripts/index_sync_worker.py`는 `CHROMA_HOST` 설정 시 `ChromaArchiveIndexMutationAdapter`(`connect_chroma_collection`, `CHROMA_PORT`/`CHROMA_COLLECTION` env는 create_app B.4 규약과 동일)를, 미설정 시 종전 `RecordingArchiveIndexMutationAdapter`를 쓴다. worker summary JSON에 `archive_backend`(`chroma`/`in_memory_fake`)가 추가됐다. claim/retry/backoff/terminal-move lifecycle은 v1.6.29 그대로다. 실제 Chroma 서버 관통 live smoke는 후속이다. | Next Tasks(worker→real Chroma), `plans/03-index-worker-retry-decisions.md` §8, `tests/test_chroma_adapter.py`, `tests/test_index_sync_worker_script.py` |
| v1.6.36 | 2026-07-05 | Phase 4 real 영속 vector 백엔드(후보 B)를 wiring했다(브리프 `plans/04-real-vector-backend-decisions.md`, Approved). `create_app`은 env 기반으로 vector 백엔드를 선택한다: `CHROMA_HOST`가 있으면 real 영속 `ChromaVectorIndexAdapter`(재시작 생존, rebuild summary `backend="chroma"`), 없으면 종전 `InMemoryVectorIndexAdapter`(`backend="in_memory_fake"`)다. embedding은 `EMBEDDING_SERVICE_URL`이 있으면 real `RemoteEmbeddingProvider`(별도 embedding 서비스 컨테이너의 `dragonkue/BGE-m3-ko`, 1024-dim, `expected_dimensions=1024` 차원 guard armed), 없으면 `DeterministicFakeEmbeddingProvider`다. 주입된 `vector_index`(테스트)는 항상 fake backend label을 유지한다. embedding 서비스는 llama gateway와 분리된 컨테이너라 vector 백엔드는 LLM 게이트와 독립이다. stale guard는 backend 무관하게 SOT를 재조회하므로 Chroma hit도 정본 재확인 후에만 ContextItem이 된다. `backend` literal enum에 `chroma`가 추가됐다. 실제 Chroma 서버/embedding 모델 관통(1024-dim assert, 재시작 vector hit 생존)은 B.5 live 검증이다. | 사용자 결정, `plans/04-real-vector-backend-decisions.md`, `tests/test_real_vector_backend_wiring.py` |
| v1.6.35 | 2026-07-05 | Phase 4 공유 in-process vector index를 도입했다. `create_app`이 단일 `InMemoryVectorIndexAdapter`(+ `DeterministicFakeEmbeddingProvider`)를 소유하고, source-block rebuild endpoint는 여기에 write하며 기본 wiring된 context search는 여기서 read한다 — 같은 프로세스에서 rebuild 후 context search를 하면 `source_quote` 등 vector need가 stale guard + SOT 재조회를 통과한 실제 hit을 반환한다. 이 공유 index는 프로세스 수명 in-memory이고 비durable이며(재시작 시 소실) `backend` literal은 여전히 `in_memory_fake`다. 공유 index는 `LLM_GATEWAY_BASE_URL` 유무와 무관하게 생성돼 rebuild가 채우고, planner 미구성 시 `/context-search`만 종전대로 503이다. rebuild HTTP/CLI summary count(`records_indexed`/`records_query_visible`/`records_archived`)는 해당 rebuild의 `snapshot_id`로 scope해 per-rebuild(누적 없음) 의미와 v1.6.22/v1.6.23 계약을 그대로 유지한다 — 누적은 뒤에서만 일어난다. real ChromaDB/ES persistent backend는 계속 후속이다. | 사용자 결정, `plans/04-shared-vector-index-decisions.md`, `tests/test_context_search_shared_index.py` |
| v1.6.34 | 2026-07-04 | Phase 4 Slice 4.3 context search HTTP API + async wiring을 구현했다. `ContextSearchService.build_context_package`가 async가 됐고(planner 결과를 `inspect.isawaitable`로 await — Slice 4.1 sync fake planner와 Slice 4.2 async 터미널 JSON planner가 같은 seam에 꽂힌다), `evaluate_context_gate`는 sync 유지다. `POST /projects/{project_id}/context-search`가 ContextPackage(macro/micro/constraints/do_not_use/trace)와 독립 Context Gate 결정을 직렬화해 반환한다. 오류 매핑: 미지원 purpose/need literal·미요청 need 등 invalid request는 400, wall-clock 초과는 504, `ContextSearchFailed`(llm_error/backend_error/system_error/sot_error)는 502, missing project는 404, planner 미구성(`LLM_GATEWAY_BASE_URL` 부재)은 503이다. create_app이 env 기반 `_default_context_search_service`로 TerminalJsonSearchPlanner를 wiring한다. deployed vector adapter는 여전히 non-persistent fake라 vector need는 hit이 없고 Mongo-direct need(current/recent scene)만 서빙한다(real Chroma 후속). | 사용자 결정, `plans/04-agentic-search-kickoff-decisions.md` §9.3, `tests/test_context_search_api.py` |
| v1.6.33 | 2026-07-04 | Phase 4 Slice 4.2 터미널 JSON LLM planner adapter를 구현했다. `services/application/app/context_search/planner.py`가 versioned prompt template `context_search_plan_v1`(task_type `context_search_plan`, 기존 `prompt_templates` 저장소 재사용)과 `TerminalJsonSearchPlanner`를 제공한다. planner는 Gateway `/v1/generate` 1-turn 호출로 SearchPlan JSON을 strict parse하고, malformed/out-of-set output이면 1회 repair, 그래도 실패하면 `ContextSearchFailed(llm_error)`다. adapter는 enum literal(need/tool) 멤버십만 검증하고, plan 의미 검증(미요청 need, need별 불허 tool, project 일치)은 `ContextSearchService._validate_plan`이 계속 소유한다. `project_id`는 모델이 아니라 request에서 주입한다. provider가 async라 adapter도 async이며(Phase 2A extraction 패턴), Slice 4.1의 sync `SearchPlanner` Protocol/`build_context_package`는 fake 주입 seam으로 유지된다 — async planner의 service 통합은 HTTP wiring slice로 미룬다. | 사용자 결정, `plans/04-agentic-search-kickoff-decisions.md` §9.2, `tests/test_context_search_planner.py` |
| v1.6.32 | 2026-07-03 | Phase 4 Slice 4.1 독립 검증(조건부 합격)의 차단 조건을 폐쇄했다. `sot_error`의 범위를 명문화: SOT reload 호출(position reload, vector stale-guard 검증, hit 재조회, Gate 재검증)에서 탈출하는 모든 non-NotFound 예외(실가동 pymongo 장애 포함)는 원형 전파 없이 `ContextSearchFailed(sot_error)` 전체 실패다. NotFound는 경로별 의도 분기다 — vector hit snapshot NotFound는 `snapshot_missing` soft stale 제외, Mongo position NotFound는 `sot_error` 전체 실패. `system_error`는 발화 경로가 생길 때 회귀와 함께 여는 예약 literal이다. toggle repo로 진짜 백엔드 예외를 주입하는 양방향 회귀 4개를 추가했고 변이 증명(catch 축소 시 5개 재실패)으로 잠갔다. | `docs/verifications/2026-07-03/context_search_slice_4_1.md`, `plans/04-agentic-search-kickoff-decisions.md`, `tests/test_context_search.py` |
| v1.6.31 | 2026-07-03 | Phase 4 Slice 4.1 context search를 구현했다. `services/application/app/context_search/`가 purpose/need/tool/status/error literal, `ContextSearchRequest`/`SearchPlan`/`ContextItem`/`ContextPackage`/`GateDecision` 계약, planner 주입형 `ContextSearchService`, 독립 `evaluate_context_gate()`를 제공한다. vector hit는 Phase 3A stale guard와 Mongo SOT 재조회를 거친 뒤에만 ContextItem이 되고 index text는 근거로 쓰지 않는다. `current_scene`/`recent_scenes`는 SOT block kind 기반 deterministic 경계다. retriever step 실패는 `degraded=true` + 계열 error type으로 trace에 남고, SOT reload 실패는 `sot_error` 전체 실패다. package는 persist하지 않고 응답 단위 trace를 포함한다. `InMemoryVectorIndexAdapter.query_similar()`가 project-scoped cosine 유사도 query 표면으로 추가됐다. LLM planner adapter(Slice 4.2), HTTP surface, ES lexical, candidate 포함은 후속이다. | 사용자 결정, `plans/04-agentic-search-kickoff-decisions.md`, `tests/test_context_search.py` |
| v1.6.30 | 2026-07-03 | Phase 4 agentic search 착수 브리프를 승인했다. purpose는 `writing_context` 1종, need는 `current_scene`/`recent_scenes`/`event_context`/`source_quote` 4종으로 시작하고 후속 확장 가능하다. planner는 터미널 JSON LLM planner를 즉시 채택한다 — LLM이 versioned prompt 기반 1-turn 호출로 SearchPlan JSON을 생성하고(strict parse + 1회 repair), tool-call flat loop planner는 Gateway tool-call wire 계약 해소 후 전환 계획(브리프 §2.1)으로 추적한다. retrieval은 Phase 3A fake vector + Mongo direct 순차 실행, ranking/budget은 deterministic 최소 규칙이다. `needs_review` candidate는 첫 slice에서 제외하되 `candidate`/`canonical` status 라벨 필드는 처음부터 계약에 연다. retriever step 실패는 degraded + 계열 구분 error taxonomy(`backend_error`/`system_error`/`llm_error`/`sot_error`, enum 확장 가능)로 기록하고 Mongo SOT reload 실패는 전체 실패다. ContextPackage는 첫 slice에서 persist하지 않는다. package는 단일 schema + purpose literal로 시작하되 이후 slice에서 Writing용/Analysis 비교용 모두 완성해야 한다(추적 의무). | 사용자 결정, `plans/04-agentic-search-kickoff-decisions.md`, `plans/04-agentic-search.md` |
| v1.6.29 | 2026-07-03 | Phase 3B one-shot index sync worker 첫 slice를 구현했다. Worker는 `pending` 또는 10분 이상 지난 stale `running` outbox entry를 claim해 recording-only fake archive mutation을 실행하고, attempt result를 `index_sync_logs`에 append한다. Claim lease는 `claimed_at` UTC datetime/BSON Date로 저장한다. Backoff는 `max_attempts=3` 기준 1분 → 5분 → terminal `failed`다. Terminal-location/index 전략은 terminal 이동을 채택해 `success|failed`가 되면 active outbox entry를 제거하고 terminal history는 `index_sync_logs`가 소유한다. Archive worker-time `not_found`는 idempotent success로 처리한다. | 사용자 결정, `plans/03-index-worker-retry-decisions.md`, `tests/test_indexing_phase3a.py`, `tests/test_index_sync_worker_script.py` |
| v1.6.28 | 2026-07-03 | Phase 3B worker/retry 실행 경계 브리프를 조건부 승인 상태로 추가했다. 첫 worker는 one-shot command로 구현하고 장기적으로 UI-triggered background/daemon이 같은 service를 재사용할 수 있게 둔다. Claim timeout은 10분이며, stale running 판정을 위해 `claimed_at` lease timestamp가 필요하다. Backoff는 `max_attempts=3` 기준 1분 → 5분 → terminal `failed`다. `backend_error`와 query-time `not_found`는 둘 다 최대 3회 시도한다. Dedup은 `pending|running` active entry에만 적용한다. 독립 검증 후 terminal-location/index 전략과 archive worker-time `not_found` 처리는 구현 전 오너 결정으로 분리됐다. | 사용자 결정, `plans/03-index-worker-retry-decisions.md`, `docs/verifications/2026-07-03/phase3b_worker_retry_brief.md` |
| v1.6.27 | 2026-07-03 | Phase 3B archive outbox 독립 검증 후속 보강을 반영했다. `index_sync_outbox`를 `mongo_collections.md` 운영 collection 레지스트리에 추가하고, `index_sync_logs`와 `sync_request_id`로 조인되는 pending request collection임을 명시했다. Mongo repository의 outbox document 직렬화/역직렬화는 fake collection round-trip 회귀로 잠갔고, `analysis_completed` event가 아직 code enum에 열리지 않았음을 명시 회귀로 고정했다. | `docs/verifications/2026-07-03/phase3b_archive_outbox_slice.md`, `tests/test_indexing_mongo_indexes.py`, `tests/test_indexing_phase3a.py` |
| v1.6.26 | 2026-07-03 | Phase 3B archive outbox 첫 code slice를 구현했다. `IndexSyncOutboxEntry`는 `project_id`, nullable `user_id`, event(`project_archived`, `draft_archived`), source(`mongo_collection`, `mongo_id`, optional `mongo_version`), canonical `targets.chroma.status`, `targets.chroma.backend="in_memory_fake"`, status(`pending|running|success|failed`), retry metadata(`attempt_count`, `max_attempts=3`, `next_attempt_at`, `last_error`)를 가진다. 서버/backend 계열 오류와 데이터 없음/not-found 계열 오류는 각각 `backend_error`, `not_found`로 분리한다. Application archive endpoint는 Core SOT archive 성공 후 `index_sync_outbox` pending entry를 idempotent하게 생성한다. Dedup key는 `(project_id, event, source.mongo_collection, source.mongo_id)`이고 outbox/log는 `sync_request_id`로 조인 가능하다. Worker execution, retry 실행/backoff 숫자, actual ChromaDB/Elasticsearch mutation, `analysis_completed` wiring은 후속이다. | 사용자 결정, `tests/test_indexing_phase3a.py`, `tests/test_application_api.py`, `tests/test_indexing_mongo_indexes.py` |
| v1.6.25 | 2026-07-03 | Phase 3B automatic sync/outbox 브리프를 승인·수정했다. 첫 automatic event source는 archive events(`project_archived`, `draft_archived`)로 시작하되, 오너가 장기적으로 더 맞는 흐름으로 본 `analysis_completed`를 후속 확장 경로로 열어 둔다. Delivery는 로컬 1인 프로젝트 기준 외부 queue 없이 Mongo outbox/polling을 사용한다. 저장 단위는 단일 `index_sync_logs`가 아니라 `index_sync_outbox` + `index_sync_logs` 분리를 채택하고, 두 collection은 `sync_request_id`로 조인한다. 첫 slice는 archive event outbox entry 생성만 다루며 worker/adapter execution, ChromaDB/Elasticsearch mutation, retry 실행은 후속이다. Persistent envelope는 canonical `targets` shape와 `backend="in_memory_fake"`를 사용하고, `user_id`는 user model 확정 전까지 nullable이다. | 사용자 결정, `plans/03-index-sync-outbox-decisions.md` |
| v1.6.24 | 2026-07-02 | Phase 3A source-block index hit의 explicit stale validation을 추가했다. `SourceBlockIndexingService.validate_source_block_record(record)`는 Core SOT를 재조회해 hit 사용 가능 여부와 stale reason literal(`project_archived`, `draft_archived`, `snapshot_missing`, `draft_mismatch`, `content_hash_mismatch`, `block_missing`)을 반환한다. `snapshot_missing`은 정본 snapshot이 없어 후속 draft/hash/block 검사를 진행할 수 없으므로 단독 reason으로 short-circuit한다. Drift 판정은 `version_id`가 아니라 snapshot `content_hash`와 block/draft pointer 정합성 기준이다. 이 검증은 archive 이후 기존 materialized record를 자동으로 숨기는 sync가 아니라, query/Context Gate 계층이 hit 사용 전에 정본 상태를 재확인하는 방어선이다. Automatic sync/outbox와 persistent vector backend는 후속이다. | `tests/test_indexing_phase3a.py` |
| v1.6.23 | 2026-07-02 | Phase 3A explicit source-block index rebuild를 Application HTTP API로 노출했다. `POST /projects/{project_id}/snapshots/{snapshot_id}/index/source-blocks/rebuild`는 현재 deterministic fake vector adapter를 사용해 rebuild summary(`project_id`, `snapshot_id`, `target`, `backend`, `records_attempted`, `records_written`, `records_indexed`, `records_query_visible`, `records_archived`)를 반환한다. `backend`는 `in_memory_fake`이고, missing/cross-project snapshot은 404다. Persistent vector backend와 automatic sync는 후속이다. | `tests/test_application_api.py` |
| v1.6.22 | 2026-07-02 | Phase 3A explicit source-block index rebuild script를 추가했다. `scripts/phase3a_rebuild_source_block_index.py --project-id ... --snapshot-id ...`는 `CORE_SOT_MONGO_URI` 또는 `--mongo-uri`로 Core SOT MongoDB를 읽고 deterministic fake vector adapter로 rebuild를 실행한 뒤 JSON summary(`project_id`, `snapshot_id`, `target`, `records_attempted`, `records_written`, `records_indexed`, `records_query_visible`, `records_archived`)를 출력한다. Exit code는 full write 성공 0, partial write 1, usage/config/domain error 2다. Application HTTP API endpoint와 persistent vector backend는 후속이다. | `tests/test_phase3a_rebuild_source_block_index_script.py` |
| v1.6.21 | 2026-07-02 | Phase 3A source block indexing 독립 검증 후속 보강을 반영했다. Phase 3A `IndexSyncRequest(project_id, snapshot_id, target)`와 `IndexSyncResult(request, records_attempted, records_written)`는 explicit rebuild용 in-process 축소 계약이며, `contracts.md` §7.3의 persistent sync log/outbox envelope(`sync_result_id`, `sync_request_id`, target별 결과, timestamps)는 후속 sync log slice 전까지 구현 대상이 아니다. Archived project/draft query exclusion은 explicit rebuild가 materialize한 record metadata 기준으로 적용되며, archive 이후 stale record를 즉시 숨기려면 재build 또는 후속 automatic sync가 필요하다. Draft-only archive 제외 분기는 회귀로 잠갔다. | `docs/verifications/2026-07-02/phase3a_source_block_index.md`, `tests/test_indexing_phase3a.py` |
| v1.6.20 | 2026-07-02 | Phase 3A source block indexing 첫 slice를 승인·구현했다. 첫 index target은 Core SOT source block only, backend는 Chroma-like vector contract with deterministic fake adapter, embedding은 fake provider only, delivery는 explicit snapshot rebuild, archive/delete 반영은 status/version filter다. Index record는 `project_id`, collection, document/block id, version id, content hash를 가진 Mongo pointer를 포함하며, archived project/draft record는 query 결과에서 제외한다. Adapter failure는 Core SOT save를 rollback하지 않는다. | 사용자 승인, `plans/03-indexing-kickoff-decisions.md`, `tests/test_indexing_phase3a.py` |
| v1.6.19 | 2026-07-01 | Phase 2A repair retry가 valid JSON이지만 source_ref catalog anchor literal을 보존하지 못한 출력도 1회 repair 대상으로 삼도록 보강했다. Adapter는 parsed candidate의 `source_ref_id`, span, quote, content_hash를 입력 catalog와 대조하고 mismatch가 있으면 parser/schema repair와 같은 prompt 경로로 재호출한다. repair 후에도 catalog mismatch가 남으면 성공으로 보정하지 않고 기존 runner/source validation 경계가 `source_invalid`를 보존한다. | `tests/test_analysis_extractor_schema.py`, `scripts/phase2a_provider_live_smoke.py` |
| v1.6.18 | 2026-07-01 | Phase 2A source_ref catalog 준비를 위한 Application HTTP surface를 추가했다. `POST /projects/{project_id}/snapshots/{snapshot_id}/source-refs`는 immutable snapshot span으로 source_ref를 non-idempotent 생성하고, `GET /projects/{project_id}/snapshots/{snapshot_id}/source-refs`와 `GET /projects/{project_id}/source-refs/{source_ref_id}`는 같은 project의 catalog/ref만 읽는다. invalid span은 400, missing/cross-project snapshot/ref는 404이며, archived project에서도 source_ref 생성·조회는 허용된다. | `tests/test_application_api.py`, `scripts/phase2a_provider_live_smoke.py` |
| v1.6.17 | 2026-07-01 | Phase 2A live provider output 원인 분석 뒤 Application-side JSON repair retry를 추가했다. `/v1/generate` 경로는 유지하고, `VersionedPromptAnalysisExtractionAdapter`가 첫 provider content를 strict parser로 검증한 뒤 실패 시 원문 output과 parser error, 원래 prompt payload를 포함한 repair prompt를 1회만 재호출한다. repair 출력도 같은 strict parser/source validation/candidate schema를 통과해야 하며, 실패하면 기존처럼 `schema_invalid`로 job failure가 보존된다. | 사용자 결정, `tests/test_analysis_extractor_schema.py`, `scripts/phase2a_provider_live_smoke.py` |
| v1.6.16 | 2026-07-01 | Phase 2A provider/Gateway runner factory wiring의 첫 구현 slice를 추가했다. Core SOT는 snapshot별 source_ref catalog read surface를 제공하고 Mongo required index `source_refs_by_project_snapshot`을 설치한다. Prompt template은 `prompt_templates` 저장소에 `task_type + version` unique contract로 seed/fetch하며 `analysis_extract_v1`을 기본 seed한다. Prompt builder는 snapshot과 source_ref catalog를 Gateway `ChatCompletionRequest`로 조립한다. Application은 `LLM_GATEWAY_BASE_URL`이 설정된 경우 `/v1/generate` 기반 `GatewayGenerateProvider`와 `VersionedPromptAnalysisExtractionAdapter`로 기본 analysis runner를 구성하고, env가 없으면 기존처럼 503을 반환한다. `{"candidates":[]}`는 유효한 빈 extraction으로 처리한다. | `tests/test_core_sot.py`, `tests/test_prompt_templates.py`, `tests/test_analysis_prompt_builder.py`, `tests/test_analysis_gateway_provider.py`, `tests/test_analysis_extractor_schema.py`, `tests/test_analysis_runner.py`, `tests/test_application_api.py` |
| v1.6.15 | 2026-07-01 | Phase 2A provider/Gateway wiring pre-implementation 결정을 승인했다. 첫 실제 provider wiring은 tool-call 없는 terminal JSON extraction으로 진행하고, 모델은 새 source_ref를 생성하지 않고 입력 source_ref catalog의 id를 선택한다. Prompt template은 DB에 저장해 versioned 관리하며 첫 literal은 `analysis_extract_v1`, task_type은 `analysis_extract`다. Gateway 호출 surface는 구현 전 비용 확인으로 `/v1/generate` 임시 사용과 `/v1/generate-structured` 최소 구현 중 선택한다. | 사용자 결정, `plans/02-analysis-provider-wiring-decisions.md` |
| v1.6.14 | 2026-06-30 | 첫 export 형식을 plain text + Markdown으로 확정하고 Slice 1 draft version export 계약을 추가했다. `GET /projects/{project_id}/drafts/{draft_id}/versions/{version_id}/export?format=txt\|markdown`은 선택 version snapshot의 `raw_text`를 verbatim body로 내보내고(AI metadata 미주입, Markdown 합성/제거 없음, 두 형식 body 동일), `format`은 content_type/확장자만 가른다. payload에 version 추적 필드 포함. unsupported format 400, missing/cross-project version 404, archived도 export 허용. 사용자 결정: 막힌 Next Tasks 대신 export 진행, 형식은 plain text + Markdown. | 사용자 결정, `tests/test_core_sot.py`, `tests/test_application_api.py` |
| v1.6.13 | 2026-06-30 | Gemma Q4_0 llama.cpp live benchmark를 실행하고 AgentLoopRunner task profile의 초기 production budget/retry 숫자 기본값을 확정했다. Report는 `docs/benchmarks/2026-06-30/gemma_q4_llama_cpp_repeats3_warmup1.json`이며, `flat-loop-gate.md`가 `analysis_compare`, `context_search`, `writing_generate` 기본 policy를 소유한다. | 사용자 요청, `scripts/benchmark_llm_provider.py`, `docs/benchmarks/2026-06-30/gemma_q4_llama_cpp_repeats3_warmup1.json` |
| v1.6.12 | 2026-06-30 | Phase 2A analysis run endpoint 계약을 승인·구현했다. `POST /projects/{project_id}/analysis/jobs/{job_id}/run`은 pending job만 runner dependency로 실행하고 요청 안에서 await해 terminal job과 candidate 목록을 반환한다. `running`/`succeeded`/`failed` job은 재실행하지 않고 `idempotent_replay=true`로 현재 job과 저장된 candidate를 반환한다. pending job에서 runner가 구성되지 않았으면 503, missing/cross-project 및 `snapshot_not_found`는 404, `schema_invalid`/`source_invalid` 계열은 400, `duplicate_conflict`는 409, provider/기타 실행 오류는 502로 표면화한다. source_ref 자동 생성과 Gateway runtime wiring은 제외다. | 사용자 요청, `tests/test_application_api.py`, `tests/test_analysis_runner.py` |
| v1.6.11 | 2026-06-30 | Phase 2A analysis job/candidate HTTP read surface를 추가했다. Application API는 `POST /projects/{project_id}/analysis/jobs`로 job을 idempotent 생성/replay하고, `GET /projects/{project_id}/analysis/jobs/{job_id}`와 `GET /projects/{project_id}/analysis/jobs/{job_id}/candidates`로 job 상태와 candidate를 조회한다. 이 API는 runner/gateway 실행을 시작하지 않으며, 존재하지 않는 project 또는 cross-project job/candidate 접근은 404다. | `tests/test_application_api.py` |
| v1.6.10 | 2026-06-29 | Phase 2A job all-or-nothing 범위를 명확화했다. all-or-nothing은 candidate write에 한정되며, job/task 생성은 idempotent setup이라 실패 후에도 남을 수 있다(롤백 대상 아님). 동작 변경 없음, runner slice 2 검증의 비차단 오해 여지 해소. | `verifications/2026-06-29/analysis_job_state_runner_slice2.md` |
| v1.6.9 | 2026-06-29 | Phase 2A job 상태 전이와 실패 상태 저장 계약을 승인했다. 상태는 `AnalysisJob`에만 두고(Task 무상태), `pending→running→succeeded|failed` 전이만 허용하며 terminal은 불변이다. runner는 새 job(`pending`)만 실행하고 기존 job은 replay로 반환하며, `failed`는 terminal이라 재실행은 새 `idempotency_key`로 한다. `failed`는 닫힌 `failure_reason` enum(`snapshot_not_found`, `source_invalid`, `schema_invalid`, `provider_error`, `duplicate_conflict`) + free-text `failure_detail`로 저장한다. | 사용자 결정, `plans/02-analysis-job-state-decisions.md` |
| v1.6.8 | 2026-06-29 | Phase 2A candidate write의 write-error 분류를 정밀화했다. duplicate-key(code `11000`) 충돌만 `DuplicateAnalysisCandidateRequest`로 표면화하고, 그 외 `BulkWriteError`/`PyMongoError`는 원본 타입을 보존해 인프라 오류 오표기를 막는다. fallback은 매핑 여부와 무관하게 이번 시도 candidate `_id`를 먼저 정리한다. | `verifications/2026-06-29/analysis_mongo_persistence_hardening.md` |
| v1.6.7 | 2026-06-29 | Phase 2A service batch API의 intra-batch idempotency를 명시했다. 같은 batch 안의 동일 `project_id + task_id + logical_key` request는 첫 candidate를 만들고 이후 항목은 idempotent replay로 정규화하며, 다른 logical_key는 별도 candidate로 유지한다. Mongo bulk duplicate는 stable `DuplicateAnalysisCandidateRequest`로 표면화한다. | `verifications/2026-06-29/analysis_mongo_persistence.md` |
| v1.6.6 | 2026-06-29 | Phase 2A Analysis Mongo persistence 계약을 명시했다. `analysis_jobs`, `analysis_tasks`, `analysis_candidates`를 저장하고 job/task/candidate idempotency는 unique index로 강제한다. Candidate batch write는 transaction 경로에서 한 트랜잭션으로 commit하고, non-transaction fallback은 single-writer local/test 경로로 실패 시 이번 시도 candidate만 rollback한다. | `plans/02-analysis-pipeline.md` |
| v1.6.5 | 2026-06-29 | Phase 2A runner는 source validation이 구성된 `AnalysisService`만 받도록 명시하고, 같은 run의 duplicate `(task_id, logical_key)` draft는 result/write에서 1개로 정규화한다. Mongo persistence slice에서는 runner all-or-nothing의 transaction/fallback 보존을 별도 검증한다. | `verifications/2026-06-29/analysis_phase2a_slice4.md` |
| v1.6.4 | 2026-06-29 | Phase 2A extraction runner 계약을 명시했다. Runner는 job을 idempotent 생성/재사용하고, snapshot 로드→provider extraction→task 생성/재사용→전체 draft 사전 검증→candidate 저장 순서로 실행한다. Task는 `project_id + job_id + candidate_type`으로 재사용하며, candidate write는 모든 draft의 logical_key/source/schema 검증 뒤 시작한다. | `plans/02-analysis-pipeline.md` |
| v1.6.3 | 2026-06-29 | Phase 2A `source_anchors` identity를 unordered set으로 명확화했다. 같은 anchor 중복은 identity와 parsed draft에서 하나로 정규화하며, ordered evidence chain이 필요해지면 순서 의미를 별도 필드(`sequence`/`evidence_order` 등)로 계약화한다. | `verifications/2026-06-29/analysis_phase2a_slice3.md` |
| v1.6.2 | 2026-06-29 | Phase 2A logical_key derivation에서 같은 `source_anchors` set은 순서와 무관하게 같은 key를 만든다고 명시했다. Anchor 내용이 다르면 별도 candidate identity다. | `verifications/2026-06-29/analysis_phase2a_slice2.md` |
| v1.6.1 | 2026-06-29 | Phase 2A source validation 이후 최소 taxonomy schema와 fake-provider extraction adapter 계약을 명시했다. Payload는 `character_observation {name, observation}`, `event_observation {event}`, `open_question_observation {question}`이고 모든 field는 non-empty string이다. Provider extraction output은 top-level `{candidates: [...]}` JSON object이며, logical_key는 `candidate_type + payload + source_anchors` canonical JSON SHA-256으로 파생한다. | `plans/02-analysis-kickoff-decisions.md`, `plans/02-analysis-pipeline.md` |
| v1.6 | 2026-06-29 | Phase 2A 착수 최소 계약 승인: taxonomy 3종(`character_observation`, `event_observation`, `open_question_observation`), provenance `source_observed`/`ai_inferred`, candidate action `create` only, status `needs_review`, confidence range만 강제, `create_source_ref` primitive non-idempotent 유지 + candidate/job 저장층 retry idempotency 소유, 2A/2B milestone 분리. 같은 날 검증 보강으로 confidence NaN 거절, action≠`create` 회귀, `logical_key` 임시 identity 계약을 명시했다. | 사용자 결정, `plans/02-analysis-kickoff-decisions.md`, `verifications/2026-06-29/analysis_phase2a_slice1.md` |
| v1.5.1 | 2026-06-28 | Core SOT Mongo adapter setup 계약 명확화: query path를 지탱하는 required index는 `uniq_save_request`와 `blocks_by_snapshot`이며, MongoDB가 required index 생성을 거부하면 `MongoRepositorySetupError`로 표면화한다. 현재 query path가 없는 `source_refs_by_snapshot` 인덱스는 required contract에서 제외한다. | `docs/verifications/2026-06-28/mongo_index_setup.md` O1/O2 |
| v1.5 | 2026-06-28 | archive를 읽기 전용 상태로 명문화: 읽기 허용, 본문 쓰기(draft 생성·version 저장)+메타데이터 수정(rename) 차단(409), SOT 본문은 archive 무관 불변, 상태 전이(unarchive)와 source_ref/분석 후보 archived 정책은 범위 밖. 기존 구현·회귀와 정합하는 문서 명문화. (같은 날 비의미 명확화: archived project 하위 draft를 archive하는 것은 상태 전이이므로 하위 draft 쓰기 차단의 예외임을 명시 — `archive_api_endpoint.md` Issue #1.) | 사용자 결정, `docs/verifications/2026-06-28/rename_api.md` R1 |
| v1.4 | 2026-06-28 | non-transaction fallback을 single-writer 전용으로 명시. 동시성 안전은 transaction 기본 경로가 담당하고, fallback의 orphan cleanup/retry guard는 같은 writer의 순차 재시도에만 정의됨. | 사용자 결정(R2 option b), `docs/verifications/2026-06-28/mongo_adapter_recheck.md` |
| v1.3 | 2026-06-26 | Core SOT persistence/retention 계약 승인: Mongo transaction 기본, 제한적 non-transaction fallback, explicit version save only, save idempotency key 필수, project/draft archive와 snapshot/version/source_ref 보존. | 사용자 승인 |
| v1.2 | 2026-06-26 | Core SOT text/reference 계약 승인: raw snapshot 기준, Unicode code point offset, raw UTF-8 SHA-256 content hash, deterministic MVP source block split. Adaptive/length-based chunking은 파생 index layer 후속 후보로 분리. | 사용자 승인 |
| v1.1 | 2026-06-26 | Slice 1 실행 경계 승인: monorepo+독립 LLM Gateway, FastAPI Application API, 느슨하게 분리 가능한 Worker 경계. frontend framework 최종 선택은 보류. | 사용자 승인 |
| v1.0 | 2026-06-26 | SoT를 정본 계약 인덱스로 승인. 미확정 항목은 계속 추측 구현 금지. | 사용자 승인, `docs/verifications/2026-06-25/system_contract_sot.md` |

## 문서 역할

| 문서 | 역할 | 지위 |
|---|---|---|
| 이 문서 | 서비스/계약 SoT 인덱스와 우선순위 | Approved SoT v1.6.43 |
| [`plans/README.md`](plans/README.md) | 계획 문서 진입점과 Phase/MVP 관계 | Draft |
| [`plans/00-foundations.md`](plans/00-foundations.md) | 전역 원칙과 제품 경계 | Draft |
| [`plans/implementation-plan.md`](plans/implementation-plan.md) | 구현 순서, slice 상태, 검증 gate | Draft |
| [`plans/llm-gateway.md`](plans/llm-gateway.md) | LLM Gateway 계약과 Gemma Q4 검증 | Proposed |
| [`plans/flat-loop-gate.md`](plans/flat-loop-gate.md) | AgentLoopRunner decision/tool/budget/completion 계약 | Draft, 일부 구현 검증됨 |
| [`plans/product-shell.md`](plans/product-shell.md) | 사용자 제품 표면 | Draft |
| [`plans/01-core-sot.md`](plans/01-core-sot.md) | MongoDB 정본 저장 계약 | Draft |
| [`plans/02-analysis-kickoff-decisions.md`](plans/02-analysis-kickoff-decisions.md) | Phase 2A 착수 최소 결정 | Approved for Phase 2A kickoff |
| [`plans/02-analysis-job-state-decisions.md`](plans/02-analysis-job-state-decisions.md) | Phase 2A job 상태 전이 결정 | Approved for Phase 2A job-state slice |
| [`plans/02-analysis-provider-wiring-decisions.md`](plans/02-analysis-provider-wiring-decisions.md) | Phase 2A provider/Gateway wiring 결정 | Approved for Phase 2A provider wiring pre-implementation |
| [`plans/02-analysis-pipeline.md`](plans/02-analysis-pipeline.md) | 분석 후보와 Analysis Gate | Draft |
| [`plans/03-indexing.md`](plans/03-indexing.md) | Chroma/ES 파생 인덱스 | Draft |
| [`plans/04-agentic-search.md`](plans/04-agentic-search.md) | ContextPackage와 Context Gate | Draft |
| [`plans/05-writing-ai.md`](plans/05-writing-ai.md) | WritingCandidate와 Writing Gate | Draft |
| [`plans/06-review-ui.md`](plans/06-review-ui.md) | 후보 검토와 상태 전이 UI | Draft |
| [`contracts.md`](contracts.md) | 초기 계약 아이디에이션 | Reference only |

## 시스템 경계

처음에는 monorepo 안에서 서비스 경계를 나눈다. 본격 MSA는 전제하지 않는다.

| 구성요소 | 소유 책임 | 소유하지 않는 것 |
|---|---|---|
| Product Shell | 프로젝트/원고 작업 공간, 처리 상태, 내보내기 | 기억 정본, AI 판정 |
| Application API | 제품 API, domain service, Gate 합성, request context | 모델 lifecycle, 벡터/lexical 정본 판정 |
| Worker | 분석/색인/검색 job 실행, AgentLoopRunner 실행 | 사용자 UI, LLM Gateway 내부 transport |
| MongoDB | 원문, snapshot, source_ref, 상태, version, 구조화 기억의 정본 | 의미 검색 또는 lexical ranking |
| ChromaDB | semantic retrieval cache | canonical memory |
| Elasticsearch | lexical/metadata retrieval index | canonical memory |
| LLM Gateway | model load, inference, provider error, usage/timing | Mongo/Chroma/ES 접근, project memory lookup, domain tool 실행 |
| AgentLoopRunner | bounded flat loop, tool allowlist, budget, terminal decision | domain Gate 통과 여부, memory update |
| Gate | 후보 검증과 처분 | 사용자 의도 최종 확정, loop 종료 원인 |
| Review UI | 승인/거절/수정 UX | 자동 canon 승격 규칙의 독자 소유 |

## 구현 기술 결정

- 저장소 구조는 monorepo를 유지한다.
- LLM Gateway는 같은 repo에서 계약을 맞추되 독립 프로세스/컨테이너 경계를 유지한다.
- Application API의 backend framework는 FastAPI를 사용한다.
- frontend framework는 **React + TypeScript + Vite**로 확정한다(v1.6.93, 오너 결정 D1=A — 종전 "보류(React/Vue 후보)" 해소). 서빙 경계는 **별도 compose 서비스(nginx)**이며(D2=B), nginx가 `/api`를 application으로 reverse-proxy해 단일 origin을 유지하고 CORS를 열지 않는다(인증 없는 단일 사용자 API라 CORS 노출을 늘리지 않는다). 근거·각하된 대안은 `plans/frontend-kickoff-decisions.md`.
- **v1.6.94로 첫 슬라이스가 구현됐다**(`frontend/` + compose `frontend` 서비스 + `services/frontend/Dockerfile`): scaffold + 프로젝트 목록/생성까지이며 원고 목록→에디터는 다음 슬라이스다(오너 범위 분할). 프론트 테스트 도구는 **Vitest + React Testing Library**로 확정한다(브리프가 "첫 슬라이스에서 결정"으로 남긴 항목). 단일 origin은 nginx `location /api/` → `proxy_pass http://application:8000/`(trailing slash prefix strip)로 실현하고, dev는 Vite proxy가 같은 모양을 만든다 — 프론트는 **절대 API base를 쓰지 않는다**(쓰는 순간 CORS가 필요해져 D2=C 각하 근거를 되살린다). nginx `proxy_read_timeout`은 배포 Writing loop wall-clock 기본(60s, v1.6.89)보다 커야 한다.
- **타입 계약 동기화의 실제 범위(v1.6.94 → v1.6.95 척추 → v1.7.1 Writing)**: `npm run gen:api`(=`scripts/dump_openapi.py` → `openapi-typescript`)가 생성하는 타입은 원래 **경로와 요청 바디만** 잠갔다 — 엔드포인트가 `dict[str, object]`를 반환하도록 주석돼 OpenAPI 응답 schema가 `additionalProperties: true`(빈 object)로 나왔기 때문이다. **v1.6.95(D1=A)로 Product shell 척추 14 endpoint는 `response_model`이 붙어 응답 타입이 실재한다** — 프론트는 이 구역에서 손선언을 쓰지 않으며 백엔드 payload 변경이 컴파일 타임에 잡힌다(실증: 백엔드 필드 rename → `tsc` 실패). 척추 이후 34개가 무타입으로 남았고, **그중 Writing 4 endpoint를 v1.7.1(C0)로 타입화**했다: generate/gate/revise-and-gate/accept의 **성공 경로**에 `response_model`이 붙어 프론트가 실제 성공 타입을 소비한다(`services/application/app/writing/http_models.py`). **partial-failure 응답**(`candidate`/`gate`/`loop`/`stages`/`audit_*`/`*_error`, 502/504 등)은 **`JSONResponse`로 직접 반환되어 FastAPI가 `response_model` 검증·직렬화를 우회**하므로 runtime 검증은 못 받지만, `responses={}`로 **OpenAPI 문서화는 된다**(revise-and-gate 400/502/503/504 = `WritingReviseGatePartial | ErrorDetailResponse`, accept 502 = `WritingAcceptAnalysisPartial | ErrorDetailResponse`). 즉 partial envelope의 유일한 runtime lock은 exact-key 회귀(`Writing*EnvelopeKeyTest`)이며, 특히 accept의 **502 + `accepted=true` + `saved`**를 generic error로 잃지 않는다. Writing 외 나머지 30 endpoint는 여전히 무타입(해당 UI 슬라이스에서 확장). **review-inbox/gate-finding read 2 endpoint는 v1.7.4 B 슬라이스가 소비하지만 `response_model` 없이 `client.ts` 손선언으로 소비**했다(v1.6.94 첫 슬라이스와 동일 interim; response_model 타입화는 additive 후속) — 이 구역은 백엔드 payload 변경이 컴파일 타임에 잡히지 않는다.
- **`response_model`의 필드 필터링 위험(v1.6.95)**: FastAPI `response_model`은 **모델이 선언하지 않은 필드를 조용히 삭제**한다(오류·경고 없음). 따라서 응답 모델은 payload와 정확히 같은 넓이여야 하며, **모델을 붙이기 전에 해당 envelope의 exact-key 회귀를 먼저 깐다**(`tests/test_application_api.py::SpineEnvelopeKeyTest`). 실증: `snapshot.project_id`를 모델에서 빼면 공개 API에서 그 필드가 사라지는데 **1104개 중 이 exact-key 회귀 1개만** 그것을 잡았다. 또한 **save surface와 read surface는 같은 키 이름(`draft_version`/`snapshot`/`blocks`)에 다른 shape**을 담으므로 모델을 공유하면 안 된다.
- **project/draft 이름 제약은 HTTP 경계에서 강제한다(v1.6.95, D3=A)**: `name`/`title`은 `NonBlankName = Annotated[str, StringConstraints(strip_whitespace=True, min_length=1)]` — **strip 후 min_length**라 `"  x  "`는 `"x"`로 저장되고 공백-only는 **422**다. 적용 대상은 project/draft의 create·rename 4종. **Core SOT 정본 계약은 무변**(`core_sot`는 계속 검증하지 않는다) — 모든 클라이언트가 HTTP를 지나므로 경계에서 막으면 빈 이름이 정본 store에 닿지 않는다. 프론트의 trim은 UX 편의일 뿐 계약이 아니며, 계약은 이 경계 제약이다. `raw_text` 빈 문자열·`idempotency_key` 형식 제약은 아직 없다(에디터 슬라이스 후속).
- Worker는 Application과 코드·계약을 공유하되, 나중에 별도 entrypoint나 프로세스로 분리할 수 있게 느슨하게 연결한다.
- 초기 local/personal runtime에서는 외부 queue 제품을 전제하지 않고 단순한 in-process/background boundary로 시작한다. 더 강한 queue가 필요하다는 증거가 생기면 별도 결정으로 승격한다.

## 확정된 전역 계약

### 제품과 프로젝트 경계

- MVP는 계정/인증이 없는 단일 사용자 시스템이다.
- 그래도 모든 저장·검색·Gate·tool handler는 `project_id`를 강제한다.
- 향후 다중 사용자를 위해 `user_id`를 지금 억지로 넣지 않는다.
- Product Shell은 프로젝트/원고 작업 표면이며 AI 기억의 정본을 별도로 소유하지 않는다.

### Source of Truth

- MongoDB가 원문과 구조화 기억의 SOT다.
- 원문 snapshot은 primary SOT다.
- 원문 snapshot의 `raw_text`는 저장 후 변경하지 않는다.
- `source_ref` offset은 `raw_text` 기준 Unicode code point index로 계산한다.
- MVP `source_ref` span은 하나의 `source_block` 안에 포함되어야 한다. 여러 block을 가로지르는 인용은 후속 후보/review 계약에서 별도로 다룬다.
- `content_hash`는 `raw_text`의 UTF-8 bytes에 대한 SHA-256이다.
- `normalized_text_hash`는 v1 필수 계약이 아니다. 정규화 기반 dedupe/search가 필요해질 때 별도 계약으로 추가한다.
- MVP `source_blocks`는 deterministic source reference 단위다. Markdown heading, 명시 scene marker, paragraph boundary 기반으로 만들며 AI 추론으로 block을 나누지 않는다.
- 사용자가 승인한 설정은 canonical SOT다.
- 분석 결과는 저장되더라도 derived SOT이며 상태와 근거가 필요하다.
- ChromaDB와 Elasticsearch는 MongoDB로 재생성 가능한 파생 인덱스다.
- adaptive chunking, semantic chunking, 길이 기반 episode/section chunking은 검색 품질을 위한 파생 index 후보이며 MongoDB raw snapshot/source_ref 정본을 대체하지 않는다.
- 검색 hit는 MongoDB pointer/version/hash로 재조회하기 전까지 정본 사실이 아니다.
- draft save는 명시적 version save만 지원한다. autosave는 MVP 범위가 아니며, 필요성이 확인될 때 별도 사용자 결정으로만 추가한다.
- draft save request는 `idempotency_key`를 필수로 가진다. 같은 `project_id + draft_id + idempotency_key` 재시도는 같은 `draft_version`을 반환해야 한다.
- Mongo adapter는 required query indexes `uniq_save_request`(`project_id`, `draft_id`, `idempotency_key`, unique)와 `blocks_by_snapshot`(`snapshot_id`, `block_index`)를 설치해야 한다. MongoDB가 required index 생성을 거부하면 setup failure는 `MongoRepositorySetupError`로 표면화한다. 현재 `source_refs` 조회는 `_id` 기준이므로 by-snapshot source_ref index는 required contract가 아니다.
- Docker 기반 정상 runtime은 MongoDB transaction을 기본으로 사용한다. non-transaction fallback은 transaction을 사용할 수 없는 local/test 환경의 제한적 경로이며, write order, idempotency lookup, orphan cleanup/retry guard를 요구한다.
- non-transaction fallback은 **single-writer 전용**이다. 같은 `(project_id, draft_id, idempotency_key)`에 대한 동시 draft save는 fallback에서 보장하지 않으며(orphan cleanup이 동시 writer의 committed dependents를 지울 수 있음), 동시성 안전이 필요한 runtime은 transaction 기본 경로를 사용한다. fallback의 orphan cleanup/retry guard는 같은 writer의 순차 재시도에 대해서만 정의된다.
- project/draft 삭제는 MVP에서 archive로 처리한다. `source_snapshots`, `draft_versions`, `source_blocks`, `source_refs`는 보존한다.
- archive는 보존이자 **읽기 전용 상태**다. archived project/draft에 대해:
  - 읽기(project/draft get·list, version/snapshot/block 재조회)는 허용한다.
  - 본문 쓰기(draft 생성, version 저장)와 메타데이터 수정(project/draft rename)은 차단한다(409 Archived). project archive는 그 하위 draft 쓰기까지 차단한다.
  - SOT 본문(`source_snapshots`, `draft_versions`, `source_blocks`)은 archive 여부와 무관하게 생성 후 불변이다. 따라서 "archived 본문 수정"이라는 연산은 존재하지 않는다.
  - archive/unarchive 같은 상태 전이는 본 쓰기 차단의 대상이 아니다. 여기에는 archived project 하위 draft를 archive하는 것도 포함되므로(상태 전이), "하위 draft 쓰기 차단"의 예외로서 archived project에서도 허용한다. unarchive는 MVP 범위가 아니므로 본 계약은 "archived인 동안 차단"으로 한정하며 영구 불변을 규정하지 않는다.
  - `source_refs` 생성은 보존된 immutable snapshot에 대한 파생 주석이므로 archived 상태에서도 **허용한다**(사용자 결정). 이는 본문/메타데이터 쓰기 차단의 예외다.
- `create_source_ref` primitive는 non-idempotent다. 같은 span 재호출은 새 ref를 만들 수 있다. Phase 2A candidate/job 저장층이 같은 logical candidate retry 중복 방지 idempotency를 소유한다.
- Phase 2A candidate retry identity는 `project_id + task_id + logical_key`다. `logical_key`는 비어 있지 않은 문자열이어야 한다. schema/extraction slice의 기본 derivation은 `candidate_type + payload + source_anchors` canonical JSON의 SHA-256이며, 같은 provider retry payload는 같은 key가 되고 payload 또는 anchor set이 다른 관찰은 별도 candidate가 된다. `source_anchors`는 identity에서 unordered set으로 취급하므로 provider 출력 순서와 동일 anchor 중복은 같은 identity로 정규화한다. 순서 의미가 필요한 ordered evidence chain은 후속 schema에서 `sequence`/`evidence_order` 같은 명시 필드를 추가해 계약화한다.
- archive/delete 이후 파생 인덱스는 stale 처리, version filter, rebuild 대상으로 다루며 MongoDB 정본 보존을 되돌리지 않는다.
- 분석 후보의 부분 승인, 부분 저장, 나머지 retry는 Phase 2/6 review action idempotency 계약에서 다룬다. Slice 1 draft save idempotency와 섞지 않는다.
- **미채택 Writing candidate 저장소(`writing_drafts_scratch`)는 Core SOT 외부이며 정본이 아니다**(v1.7.20 승격, 브리프 `plans/unaccepted-candidate-persistence-decisions.md` D2=A). 정본은 여전히 `source_snapshots`/`draft_versions`뿐이고, scratch는 두 용도의 **low-stakes tier**다: (1) accept 전 소실을 막는 **복구**, (2) **비동기 생성 결과 보관**(async-pad, v1.7.26 — worker가 긴 프리셋 생성 결과를 여기 append하고[증분 2b], 패드 UI가 읽어 표시한다[증분 3]). scratch 내용은 정본 사실이 아니며 canonical UI·검색·ContextPackage에 정본으로 노출하지 않는다.
  - 키는 `(project_id, draft_id)`이며 `draft_id`는 generate 요청의 `current_position`에서 온다. `current_position`이 없으면 저장하지 않는다(키가 없으면 복구 대상이 아니다).
  - generate 성공마다 append한다(D1=B 이력). per-draft 보존 상한은 **기본 20**이며 **`WRITING_SCRATCH_MAX_PER_DRAFT`로 조정 가능**하다. 초과분은 오래된 것부터 삭제한다. **1 미만 값은 거부하고 기동에 실패한다** — 0/음수를 허용하면 저장 직후 스스로를 trim해 안전망이 지켜야 할 초안을 조용히 삭제하므로, 조용한 데이터 손실 대신 시끄러운 실패를 택한다.
  - **정본 version이 저장된 accept는 그 accept의 `request_id`와 일치하는 scratch 항목만 정리한다**(async-pad D2=A, v1.7.25). 정상 200 accept와 **analysis job만 실패한 502 partial**(version은 저장됨) 양쪽이 정리 대상이며, 두 경우 모두 **채택된 항목(`request_id` 일치)만** 삭제하고 같은 draft의 다른 생성 결과는 **보존한다** — accept 뒤에도 여전히 복사·복구 가치가 있어 비동기 결과 패드가 재사용할 기반이다. 대응 항목이 없으면 no-op이다. **비-PASS Gate 결과(`accepted=false`, 저장된 것 없음)는 정리하지 않는다** — 사용자에게 아직 복구할 초안이 남아 있으므로 여기서 지우면 안전망이 초안을 죽인다. (v1.7.20 승격 당시 이 조항은 whole-draft 삭제였고 granularity가 문구로 명시되지 않았다 — v1.7.25가 항목 단위로 축소하며 명시한다. 검증 H2.)
  - 명시적 버리기(DELETE)는 해당 draft의 scratch 전체를 삭제한다. **시간 기반 자동 만료는 없다**(자동 삭제 없음 정신); 상한·accept 정리(항목 단위)·명시 삭제(draft 전체)만이 수렴 수단이다.
  - scratch 쓰기/삭제 실패는 generate/accept를 실패시키지 않는다(best-effort 격리). 안전망이 정본 경로를 막지 않는다.
  - schema는 candidate 식별 중 `request_id`를 실값으로 싣고, `intent`와 `version_id`를 nullable seam으로 둔다. `version_id`(async-pad D7)는 generate의 `current_position.version_id`에서 오며 패드가 "이 version 기준으로 생성됨"을 표시할 근거다. HTTP generate 경로는 `current_position`이 있을 때만 저장하고 `ContextPositionBody.version_id`가 필수 문자열이므로 이 경로로 저장된 레코드는 항상 실값을 싣는다. None은 이 필드 이전에 쓰인 legacy 레코드(및 필드를 생략한 직접 호출자)를 위한 nullable seam이다 — 기존 레코드는 `intent`·`version_id`를 None으로 읽는다(마이그레이션 불요). `next_unit`은 generate 시점에 존재하지 않으므로 제외하며, Phase 7 `conversation_turn` 흡수 시 `intent`와 함께 추가한다.
- **비동기 생성 job 저장소(`writing_generation_jobs`) + 실행 worker는 Core SOT 외부이며 정본이 아니다**(async-pad D3=B/D4=A; v1.7.26 = 증분 2b 저장소·worker, v1.7.27 = 증분 2c endpoint 생산자 배선). 긴 프리셋(`medium`=2048/`long`=4096)은 요청을 블록하기엔 느려 **worker가 백그라운드로 실행**하도록 한다(1024=`short`는 계속 동기) — generate endpoint가 2c에서 이들을 enqueue한다(아래 endpoint 배선 항). **별도 worker 서비스**(`scripts/generation_job_worker.py`, 색인 sync outbox와 무관 — 성격이 다른 CDC vs 사용자 요청 job, 브리프 검증 H3)가 **atomic claim**(`find_one_and_update` + lease)으로 job을 집어 동기 경로와 같은 context-build + generate 파이프라인을 돌린 뒤 결과를 scratch에 append하고 job을 종료 상태로 만든다. **claim은 원자적이라 동시/replica worker가 한 job을 이중 실행하지 않고**, crash한 worker의 in-flight job은 lease(기본 600초, `WRITING_GENERATION_CLAIM_TIMEOUT_SECONDS`) 만료 뒤 재claim된다. 상태는 Analysis 선례 `pending/running/succeeded/failed`이며 전이는 강제된다(PENDING→RUNNING claim, RUNNING→{SUCCEEDED,FAILED}; **FAILED→PENDING은 명시 재시도**로 `POST .../writing/generation-jobs/{job_id}/retry`가 구동한다 — v1.7.28, 아래 endpoint 항). 실패 taxonomy는 generate 파이프라인 예외를 매핑한 6종 + **catch-all `internal`**(매핑 안 된 infra/버그를 종료 상태로 보내 RUNNING livelock 방지, 검증 H-2). job은 async를 draft에 묶으므로 `draft_id`/`version_id`가 필수다. enqueue는 `(project_id, request_id)` 멱등(재시도가 두 번째 생성을 만들지 않음). worker의 결과 저장은 job 기준 멱등(reclaim 재실행 시 이전 scratch를 `request_id`로 지우고 다시 써 중복 방지, 검증 H-3). worker 실행/claim 실패는 격리되어 다른 job이나 정본 경로를 막지 않는다.
  - **endpoint 배선(D5) 완료(v1.7.27, 증분 2c)**: `POST .../writing/generate`는 `output_length`로 분기한다 — **`short`(1024)는 동기**(200 + `WritingCandidatePayload`, 무변)이고 **`medium`(2048)/`long`(4096)은 job을 enqueue하고 `202 Accepted`로 `{job: <WritingGenerationJobPayload>, idempotent_replay}`를 즉시 반환**한다(비블로킹; worker가 claim해 실행). endpoint는 async 분기에서 writing/context_search/scratch를 부르지 않고(전부 worker 역할) 동기 전용 503 검사도 우회한다. **async 프리셋에 `current_position`이 없으면 400**이다(패드는 per-draft 키라 표시할 곳이 없다; short는 여전히 positionless를 허용). enqueue는 `(project_id, request_id)` 멱등이라 재POST는 같은 job에 `idempotent_replay=true`로 수렴한다. **상태 read = `GET .../writing/generation-jobs/{job_id}`**(200 + `WritingGenerationJobPayload`; 미발견·타 프로젝트는 404)이며, 종료 시 `result_scratch_id`(성공)·`failure_reason`/`failure_detail`(실패)가 실린다. (async 응답은 candidate가 아니라 job이므로, 동기 후보 응답의 "status는 항상 candidate"는 동기 경로에만 해당한다.)
  - **재시도 endpoint(D4=A, v1.7.28 = 재시도 UI 슬라이스)**: `POST .../writing/generation-jobs/{job_id}/retry`(200 + `WritingGenerationJobPayload`)가 **FAILED job을 PENDING으로 되돌려** failure_reason/detail·claim lease를 지우고, worker의 claim 루프가 이를 다시 집어 재실행한다(Analysis retry endpoint 미러 — 단 Analysis와 달리 별도 `run` 호출이 없다; 생성 worker가 PENDING을 자동 claim하므로 retry만으로 재실행된다). **FAILED만 재시도 가능**하고 그 외 상태(pending/running/succeeded)는 `InvalidJobStateTransition`→**409**, 미발견·타 프로젝트는 **404**다. 프론트 결과 패드는 실패 job에 "다시 시도" 버튼을 노출해 이 endpoint를 호출하고, 성공하면 job이 PENDING으로 돌아가 폴링이 재개된다.

### Candidate 원칙

- 모든 AI 출력은 candidate다.
- Writing AI 출력은 `draft_candidate`다.
- Analysis AI 출력은 `analysis_candidate`다.
- Agentic Search 출력은 `context_candidate`다.
- candidate는 Gate 또는 사용자 승인 없이 canonical 상태가 되지 않는다.
- 후보 상태와 loop 종료 decision은 다른 층위다. 예: Analysis candidate `needs_review`와 Loop decision `awaiting_review`는 같은 뜻이 아니다.

### 추적성

- 구조화 기억에는 `project_id`, 상태, version, `source_ref`가 필요하다.
- `source_ref`는 snapshot/block/span/quote/hash로 원문을 다시 찾을 수 있어야 한다.
- 검색 결과와 ContextPackage 항목은 MongoDB pointer로 SOT를 다시 읽고 version/hash를 확인해야 한다.
- trace에는 provider error detail, loop decision, tool call, Gate finding의 원인을 보존한다.

### HTTP 에러 응답 계약

H3 페이즈(브리프 `plans/api-error-response-contract-decisions.md`, D1~D4=A)의 S1. H1 성공 `response_model`(v1.6.95)·H2 입력 검증이 남긴 **에러 응답**의 전역 의미론을 정본화한다. 계기는 v1.7.28 시점 dogfood의 레거시-데이터 `/drafts` 500→503 수정에서 "503이 새 public 상태코드인데 정본·OpenAPI 어디에도 없어 다음 검증자가 로그에서 유추해야 한다"가 드러난 것이다.

- **에러 본문은 균일한 단일 키 `{"detail": <string>}`다**(D1=A). 모델은 `ErrorDetailResponse{detail: str}`(`writing/http_models.py`)이며 상태코드별 별도 에러 스키마는 두지 않는다.
- 계약은 **3층**이다: **상태코드 = 기계용 의미론**, **`detail` = 사람용 메시지**, (후속) **`reason` = 기계용 코드**. **`detail` 문자열 자체는 계약이 아니다** — 클라이언트는 표시·로깅에만 쓰고 문자열 패턴 매칭으로 분기하지 않는다. 기계용 분기가 필요해지면 detail을 파싱하는 대신 `reason`을 **additive**로 도입한다(실사용 근거가 생길 때. D1=B의 자리를 이 3층 구조가 열어 둔다).
- **요청 스키마 검증 실패(422)는 이 계약 밖이다** — FastAPI/Pydantic이 자동 생성하며 본문이 `{"detail": [ …오류 배열… ]}`로 형태부터 다르다. 자동 문서화되므로 endpoint별로 선언하지 않는다.
- **endpoint별 realistic 코드 집합은 OpenAPI(`responses=`)가 기계적 진실이다**(D3=A). 본 SoT는 코드의 **의미론만** 고정하고 endpoint×코드 표를 중복 유지하지 않는다(이중 관리 drift 방지). 새 endpoint나 새 실패 분기를 만들면 그 endpoint의 `responses=`에 반영하고 `python3 scripts/dump_openapi.py` 재덤프로 실제 노출을 확인한다(코드가 emit할 의도만 믿지 않는다).
- **선언은 런타임 동작을 바꾸지 않는다**: `responses=` 추가는 문서·타입만 넓히며 상태코드·`detail`·분기는 그대로다. 프론트 에러 UX 변경도 이 페이즈 밖이다(D4=A).

#### 상태코드 의미론

| 코드 | 의미 | 대표 원인(실코드) |
|---|---|---|
| 400 | 요청이 이 자원 상태에서 성립하지 않음(도메인 검증 실패). 클라이언트가 요청을 고치면 성공할 수 있다. | `ValueError`/`CoreSotError`, `UnsupportedExportFormat`, `InvalidContextSearchRequest`, Writing intent/next_unit binding 위반, async 프리셋 + `current_position` 없음 |
| 404 | 대상이 없거나 **다른 project 소유**다. project isolation 위반은 존재를 알리지 않고 404로 수렴한다. | `NotFound`/`AnalysisNotFound`/`MemoryNotFound`/`GateFindingNotFound` 계열 |
| 409 | 대상은 있으나 **현재 상태**가 그 연산을 허용하지 않음. | `Archived`, stale base(`StaleProjectBriefBase`/`StaleWritingBase`), job/candidate/finding 상태 전이 위반, 잘못된 reorder 순열(`InvalidDraftOrder`) |
| 422 | 요청 스키마 검증 실패(FastAPI 자동, 본문 형태 다름) | Pydantic 제약(`NonBlankName` 등) |
| 502 | **상류**(LLM provider·gateway·검색)가 실패했거나 AI 출력이 계약 schema를 만족하지 않음. | `ContextSearchFailed`, `InvalidWritingGateResult`/`InvalidWritingRevision`/`InvalidCandidateReport`/`InvalidJudgeResult` |
| 503 | 서버가 **지금** 이 연산을 수행할 수 없음. 요청을 고쳐서는 해결되지 않는다(아래 두 얼굴). | 협력자 미구성, `DraftOrderIntegrityError` |
| 504 | 서버가 소유한 **예산**이 소진됨(소켓 timeout이 아니라 계약된 wall-clock/token budget). | `ContextSearchBudgetExceeded` |
| 동적 | Gateway `ProviderError` 매핑을 그대로 전달하는 지점(9곳). 가능한 코드 집합이 상류에 달려 있어 endpoint별 realistic 집합만 선언하고 전체 열거는 하지 않는다. | `raise HTTPException(status_code=status, …)` |

#### 503의 두 얼굴

1. **협력자 미구성(configuration)** — LLM/검색이 필요한 endpoint에 그 협력자가 조립되지 않은 배포. detail은 `"<X> is not configured"` 형태이며 writing 6종·context search·analysis runner·compare judge가 여기 속한다. **배포 구성**이 바뀌어야 해결된다.
2. **저장 데이터 마이그레이션 필요(data integrity)** — `DraftOrderIntegrityError`. pre-v1.7.14 legacy draft(`unit_kind`/`position` 없음)나 비연속 position 집합을 읽을 때 발생하며, 해결책은 요청 수정이 아니라 **one-shot `scripts/migrate_ordered_units.py` 실행**이다. 현재 방어 지점은 `GET …/drafts`(list)·`POST …/drafts`(create)·`GET /projects/{id}/export` 3곳이다.

**공통 규칙**: 503은 "클라이언트가 요청을 고쳐 성공시킬 수 없고 서버 측 조치(구성 변경 또는 마이그레이션)가 선행돼야 한다"를 뜻한다. 그 조치 전의 단순 재시도는 무의미하다.

**`InvalidDraftOrder`의 의도적 비대칭**: 이 예외 계열은 **서버 데이터 무결성**(`DraftOrderIntegrityError` → 503)과 **클라이언트 입력 오류**(잘못된 `unit_kind`, 불완전한 reorder 순열 → 409/400) 양쪽에 쓰인다. `PUT …/draft-order`(reorder)는 서브클래스가 기존 `(Archived, InvalidDraftOrder)` 절에 잡혀 **409를 유지한다**. 다만 그 이유가 "reorder에서는 무결성 위반이 클라이언트 순열로 판별되기 때문"은 **아니다** — `reorder_drafts`도 저장 집합을 `_require_ordered_drafts`로 **순열 검사보다 먼저** 검사하므로, legacy 데이터에서는 입력과 무관하게 같은 무결성 에러가 발생하고 그것이 기존 절에 흡수되어 409가 된다. 즉 reorder의 409는 **입력 오류 face에 대해서만 정확한 의미론**이고 무결성 face에서는 2026-07-22 수정이 500 누수만 닫는 수술적 범위를 지킨 **무변경의 결과**다. 이 상태는 의도적이며 회귀로 잠겨 있고, 503으로의 재분류는 본 계약의 별도 개정 사항이다.

#### 알려진 결손 — `start_next_unit`의 500 누수

`POST …/writing/accept`의 `intent=start_next_unit` 경로는 `CoreSotService.start_next_unit`가 기존 ordered draft 집합을 읽으므로 legacy 데이터에서 `DraftOrderIntegrityError`를 던지는데, accept endpoint에 이를 잡는 절이 없어 **500이 샌다**(list/create/export 3곳과 같은 근본 원인이며 같은 마이그레이션이 해결책이다). H3 **S5**에서 `except DraftOrderIntegrityError → 503`을 추가해 닫는다. 그때까지 이 500은 **알려진 결손**이며 본 정본이 승인한 동작이 아니다.

## 서비스별 계약 SoT

### LLM Gateway

정본 세부 문서: [`plans/llm-gateway.md`](plans/llm-gateway.md)

확정된 구현 계약:

- Gateway는 같은 repo에 두되 독립 프로세스/컨테이너로 실행한다.
- Application은 모델 파일 경로, CUDA 설정, inference engine 세부를 알지 않는다.
- Gateway는 MongoDB/ChromaDB/Elasticsearch에 접근하지 않는다.
- Gateway는 domain tool registry나 AgentLoopRunner terminal decision을 소유하지 않는다.
- 현재 provider error literal은 다음 5종이다.

```text
provider_unavailable
provider_timeout
provider_overloaded
provider_invalid_response
provider_request_rejected
```

- HTTP/text completion 성공 응답은 `model`, 첫 choice `message.content`, `finish_reason`, `usage.prompt_tokens`, `usage.completion_tokens`를 모두 유효값으로 가져야 한다.
- `usage` 또는 token count 누락/invalid는 0으로 보정하지 않고 `provider_invalid_response`다.
- 명시적 token count 0은 유효하다.
- `HttpxJsonTransport`는 `trust_env=false`가 기본이며, proxy는 필요할 때만 opt-in한다.
- 실제 tool-call parsing은 아직 미구현이다.

검증 근거:

- Slice 0.1~0.6 unit/contract 회귀
- [`verifications/2026-06-24/llm_gateway_f1_f2_closure.md`](verifications/2026-06-24/llm_gateway_f1_f2_closure.md)
- [`verifications/2026-06-24/llm_gateway_slice_0_6_httpx.md`](verifications/2026-06-24/llm_gateway_slice_0_6_httpx.md)

### AgentLoopRunner

정본 세부 문서: [`plans/flat-loop-gate.md`](plans/flat-loop-gate.md)

확정된 구현 계약:

- Loop는 Application/Worker 소유다.
- sub-agent spawn, delegate tool, nested AgentLoopRunner 호출, 임의 code/shell tool은 지원하지 않는다.
- 한 run은 정확히 하나의 terminal decision으로 종료한다.

```text
completed
awaiting_review
blocked
budget_exhausted
invalid_tool_arguments
tool_error
provider_error
```

- Gateway의 5 provider literal은 Loop decision으로 승격하지 않고 `provider_error` trace detail로 보존한다.
- domain Gate 결과와 Loop decision은 직교한다.
- `completed`는 loop 종료 상태일 뿐 domain Gate 통과가 아니다.
- `budget_exhausted`는 성공이 아니다.
- provider 응답 content는 JSON object이며 loop 종료 채널은 top-level `self_report` field다.
- `self_report` 허용값은 정확히 `finalize` 또는 `defer`다. 누락·오타·대소문자 변형·non-string·산출물 내부 nested `self_report`는 provider output 오류다.

Budget 계약:

- 필수 차원은 `max_iterations`, `max_wall_clock_ms`, `max_total_tokens`, `max_tool_calls`, `max_repeated_calls`다.
- `max_iterations`, `max_wall_clock_ms`, `max_total_tokens`는 1 이상이다.
- tool 사용 profile의 `max_tool_calls`, `max_repeated_calls`는 1 이상이다.
- tool 없는 `writing_generate`는 tool budget 2종을 0으로 둔다.
- token은 post-accounting 차원이다. 누적 `== limit`은 완료 가능, `> limit`은 `budget_exhausted`다.
- retry는 free path가 아니며 기존 budget을 소비한다.

Tool registry 계약:

- v1 public tool literal은 6종이다.

```text
search_memory
load_memory
load_snapshot
compare_memory
validate_candidate
validate_context
```

- `analysis_compare` allowlist: `search_memory`, `load_memory`, `load_snapshot`, `compare_memory`, `validate_candidate`
- `context_search` allowlist: `search_memory`, `load_memory`, `validate_context`
- `writing_generate` allowlist: 없음
- 모델 arguments는 `project_id`, task/trace identity, deadline을 소유할 수 없다.
- raw arguments는 JSON으로 정확히 한 번 parse한다. parse 실패를 `{}`로 바꾸지 않는다.
- A2 validator 범위는 `required`, type, `additionalProperties: false`, array `items`다.
- `enum`/bounds(`minimum`/`maximum`)는 이 keyword를 선언하는 tool schema가 처음 등록될 때 검증과 양방향 회귀를 추가한다. 그전까지 명시적 deferral이다.
- valid tool call signature는 `tool name + canonical JSON arguments`다. canonical JSON은 key sort와 JSON type/value 보존을 사용한다.

검증 근거:

- [`verifications/2026-06-24/agent_loop_a1_decision_budget.md`](verifications/2026-06-24/agent_loop_a1_decision_budget.md)
- [`verifications/2026-06-25/agent_loop_a2_registry.md`](verifications/2026-06-25/agent_loop_a2_registry.md)
- [`verifications/2026-06-25/agent_loop_a3_completion_resolution.md`](verifications/2026-06-25/agent_loop_a3_completion_resolution.md)
- [`verifications/2026-06-25/self_report_parser.md`](verifications/2026-06-25/self_report_parser.md)
- [`verifications/2026-06-25/agent_loop_provider_runner.md`](verifications/2026-06-25/agent_loop_provider_runner.md)

## Gate 합성 계약

Loop Gate, Analysis Gate, Context Gate, Writing Gate는 같은 decision으로 합치지 않는다.

| Gate | 질문 | 대표 결과 |
|---|---|---|
| Loop Gate | loop run이 왜 끝났나 | terminal decision 7종 |
| Analysis Gate | 분석 후보를 어떻게 처분할까 | `create/update/add_evidence/no_change/conflict` 및 job 상태 |
| Context Gate | package를 AI에 전달해도 되나 | project/SOT/pointer/version/stale/budget 검사 |
| Writing Gate | writing candidate를 editor에 제안해도 되나 | hard constraint/POV/continuity/finding |

합성 순서:

```text
AgentLoopRunner terminal decision
→ 산출물 존재 시 domain Gate 실행
→ Gate finding과 후보 상태 저장
→ 사용자 검토 또는 후속 service action
```

Loop decision이 `completed`여도 domain Gate가 reject할 수 있다. 반대로 domain candidate가 `needs_review` 상태여도 loop는 완결된 산출을 제출했다면 `completed`일 수 있다.

## Phase 계약 인덱스

### Product Shell

정본 세부 문서: [`plans/product-shell.md`](plans/product-shell.md)

- 단일 사용자 제품 표면이다.
- 프로젝트 CRUD, 원고 작업 공간, 처리 상태, 내보내기를 제공한다.
- 첫 export 형식은 plain text와 Markdown으로 확정했다(v1.6.14). DOCX/PDF/EPUB은 후속 검토다.
- 보관/삭제 정책, draft/chapter/scene 계층은 미확정이다.

### Phase 1. Core SOT

정본 세부 문서: [`plans/01-core-sot.md`](plans/01-core-sot.md)

- `projects`, `drafts`, `draft_versions`, `source_snapshots`, `source_blocks`, `source_refs` 계약을 만든다.
- snapshot은 생성 후 수정하지 않는다.
- text/reference와 persistence/retention 계약은 v1.2~v1.3에서 승인됐다.
- Application API는 `GET /projects/{project_id}/drafts/{draft_id}/versions/{version_id}/export?format=txt|markdown`으로 선택한 draft version을 내보낸다. export `body`는 그 version snapshot의 `raw_text`를 verbatim으로 내보내며 AI 분석 metadata를 본문에 주입하지 않고 Markdown을 합성·제거하지 않는다(두 형식 body 동일). `format`은 `content_type`(`text/plain`/`text/markdown`)과 filename 확장자(`.txt`/`.md`)만 가른다. payload는 `version_id`/`version_number`/`snapshot_id`/`content_hash` 추적 필드를 포함한다. 지원하지 않는 format은 400, missing/cross-project version은 404다. archive는 read를 막지 않으므로 export는 archived에서도 가능하다.

### Phase 2. Analysis Pipeline

정본 세부 문서: [`plans/02-analysis-pipeline.md`](plans/02-analysis-pipeline.md)

- Phase 2A는 prior memory 없이 snapshot 근거 기반 후보를 만든다.
- Phase 2A 최소 taxonomy는 `character_observation`, `event_observation`, `open_question_observation` 3종으로 시작하며 후속 확장을 막지 않는 구조로 구현한다.
- Phase 2A provenance literal은 `source_observed`와 `ai_inferred`만 허용한다. `user_declared`는 ProjectBrief/Product Shell 입력 계약 이후로 미룬다.
- Phase 2A candidate action은 `create` only이고 status는 `needs_review`로 고정한다.
- Phase 2A confidence는 `0.0 <= confidence <= 1.0`만 강제하고 자동 reject threshold는 후속 품질 fixture 이후 결정한다. NaN은 이 범위 밖이므로 거절한다.
- Phase 2A source validation은 `CandidateSourceAnchor(source_ref_id, start_offset, end_offset, quote, content_hash)`를 같은 project의 Core SOT `SourceRef`와 대조한다. source_ref가 없거나 다른 project에 속하거나 span/quote/hash가 다르면 candidate 저장을 거절한다.
- Phase 2A Snapshot Loader는 같은 project의 Core SOT snapshot raw text, content hash, source block ids를 analysis 입력으로 로드한다. 다른 project의 snapshot은 찾을 수 없는 것으로 처리한다.
- Phase 2A 최소 payload schema는 `character_observation {name, observation}`, `event_observation {event}`, `open_question_observation {question}`이다. 모든 payload field는 non-empty string이며 누락된 required field와 알 수 없는 추가 field는 malformed payload로 거절한다. **`character_observation`은 optional `aspect`(자유 문자열, 예: `voice`/`trait`)를 추가로 담을 수 있다(v1.7.23, 증분 3 D4=B, 오너=optional)** — 캐릭터 어투를 식별해 Writing Gate 문체 대조(D5)의 기준을 세우기 위함이다. taxonomy는 여전히 3종(2A D5=A 무변)이며, optional이라 기존 `{name, observation}` payload는 그대로 유효하다(마이그레이션 불요). aspect가 있으면 non-empty string이어야 하고, `event`/`open_question`에는 허용되지 않는다(알 수 없는 field).
- Phase 2A fake-provider extraction adapter는 provider content를 top-level `{candidates: [...]}` JSON object로 파싱하고, 각 candidate의 approved type/provenance/confidence/source_anchors/payload를 검증한 뒤 candidate draft를 만든다.
- Phase 2A extraction runner는 source validation이 구성된 `AnalysisService`만 받는다. Runner는 `AnalysisJob`을 `project_id + snapshot_id + idempotency_key`로 idempotent 생성/재사용하고, Snapshot Loader → provider extraction → `AnalysisTask` 생성/재사용 → 전체 draft 사전 검증 → candidate 저장 순서로 실행한다. Task는 `project_id + job_id + candidate_type`으로 재사용한다. 한 run의 candidate write는 모든 draft가 logical_key/source/schema 검증을 통과한 뒤 시작한다. 같은 run의 duplicate `(task_id, logical_key)` draft는 1개로 정규화한다. Job 상태 전이와 실패 상태 저장 계약은 아래 job-state 조항(v1.6.9)에서 정의했으며 구현은 job-state slice다.
- Phase 2A service batch API는 같은 batch 안의 동일 `project_id + task_id + logical_key` candidate request를 idempotent replay로 정규화한다. 첫 request는 candidate를 만들고 이후 동일 request는 같은 candidate를 반환한다. 같은 batch 안에서도 logical_key가 다르면 별도 candidate로 유지한다.
- Phase 2A Mongo persistence는 `analysis_jobs`, `analysis_tasks`, `analysis_candidates`를 저장한다. Required idempotency indexes는 `uniq_analysis_job_request`(`project_id`, `snapshot_id`, `idempotency_key`, unique), `uniq_analysis_task_request`(`project_id`, `job_id`, `candidate_type`, unique), `uniq_analysis_candidate_request`(`project_id`, `task_id`, `logical_key`, unique)다. Candidate list query는 `analysis_candidates_by_job`(`project_id`, `job_id`) index를 사용한다. MongoDB가 required index 생성을 거부하면 setup failure는 `MongoAnalysisRepositorySetupError`로 표면화한다.
- Phase 2A candidate batch write는 transaction 경로에서 한 트랜잭션으로 commit한다. Non-transaction fallback은 Core SOT fallback과 같이 **single-writer local/test 전용**이며, 실패하면 이번 시도에서 새로 쓴 candidate `_id`만 삭제해 candidate 부분 저장을 남기지 않는다. 동시성 안전이 필요한 runtime은 transaction 경로를 사용한다.
- Phase 2A candidate write의 duplicate 충돌만 stable `DuplicateAnalysisCandidateRequest`로 표면화한다. `insert_many`가 unique index를 위반하면 pymongo는 duplicate-key code `11000`을 담은 `BulkWriteError`를 던지므로 양 경로(transaction/fallback)는 이를 `DuplicateAnalysisCandidateRequest`로 매핑한다. duplicate가 아닌 다른 `BulkWriteError`/`PyMongoError`(예: document validation, write concern)는 원본 예외 타입을 보존해 인프라 오류가 duplicate request로 오표기되지 않게 한다. fallback은 매핑 여부와 무관하게 이번 시도 candidate `_id`를 먼저 정리한다.
- Phase 2A job 상태는 `AnalysisJob`에만 둔다. `AnalysisTask`는 status가 없다(candidate_type 파티션이며 독립 lifecycle 없음). job 상태는 `pending`(생성, 미실행) → `running`(추출/검증/저장 진행) → `succeeded`(이번 run candidate 모두 저장) | `failed`(실패, candidate 미저장)다. 허용 전이는 `pending→running`, `running→succeeded`, `running→failed`뿐이고 그 외는 `InvalidJobStateTransition`이다. terminal 상태(`succeeded`/`failed`)는 불변이다.
- Phase 2A runner는 새로 생성한 job(`pending`)일 때만 추출을 실행한다. `find_job_request`가 기존 job(상태 무관)을 찾으면 idempotent replay로 그대로 반환하고 재실행하지 않는다. `failed`는 terminal이며 같은 snapshot 재분석은 새 `idempotency_key`(새 job)로 한다. crash 등으로 비terminal에 멈춘 stale job의 자동 복구/재개는 MVP 범위 밖이다.
- Phase 2A `failed` job은 닫힌 `failure_reason` enum과 free-text `failure_detail`을 저장한다. `failure_reason`은 `snapshot_not_found`(snapshot 로드 실패), `source_invalid`(source_ref/anchor 검증 실패), `schema_invalid`(payload/logical_key/provider content malformed), `provider_error`(provider extraction 호출 실패, Gateway `provider_error` umbrella와 정렬), `duplicate_conflict`(candidate 저장 `DuplicateAnalysisCandidateRequest`)다. `succeeded`/비terminal 상태에서는 `failure_reason`/`failure_detail`이 비어 있어야 한다. 실패는 성공으로 위장하지 않으며 candidate를 저장하지 않는다. all-or-nothing은 candidate write에 한정되고, job/task 생성은 idempotent setup이라 실패 후에도 남을 수 있다(롤백 대상 아님).
- Phase 2A Application API는 `POST /projects/{project_id}/analysis/jobs`로 job을 `project_id + snapshot_id + idempotency_key` 기준 idempotent 생성/replay하고, `GET /projects/{project_id}/analysis/jobs/{job_id}`로 job 상태를 읽고, `GET /projects/{project_id}/analysis/jobs/{job_id}/candidates`로 저장된 candidate를 읽는다. 존재하지 않는 project 또는 다른 project의 job/candidate 접근은 404로 처리한다.
- Phase 2A Application API는 `POST /projects/{project_id}/analysis/jobs/{job_id}/run`으로 기존 job 실행을 시작할 수 있다. 이 endpoint는 `pending` job만 실행하며, runner dependency를 주입받아 요청 안에서 async runner를 await한다. `running`/`succeeded`/`failed` job은 재실행하지 않고 현재 job과 저장된 candidate를 `idempotent_replay=true`로 반환한다. pending job에서 runner가 구성되지 않았으면 503이다. 실패 HTTP mapping은 `snapshot_not_found` 404, `schema_invalid`/`source_invalid` 계열 400, `duplicate_conflict` 409, provider/기타 실행 오류 502다. 실패 job은 이후 `GET`으로 조회 가능해야 한다. source_ref 자동 생성과 Gateway runtime wiring은 이 endpoint 범위가 아니다.
- Phase 2A Application API는 `POST /projects/{project_id}/snapshots/{snapshot_id}/source-refs`로 source_ref catalog 항목을 생성하고, `GET /projects/{project_id}/snapshots/{snapshot_id}/source-refs`로 snapshot catalog를 source order로 조회하며, `GET /projects/{project_id}/source-refs/{source_ref_id}`로 단일 ref를 조회한다. create request는 `start_offset`, `end_offset`만 받으며 Core SOT가 quote/block/content_hash를 snapshot에서 계산한다. invalid span은 400, missing/cross-project snapshot/ref는 404다. source_ref는 immutable snapshot의 파생 주석이므로 archived project에서도 생성·조회가 허용된다.
- Phase 2A 실제 provider wiring 첫 slice는 tool-call 없는 terminal JSON extraction으로 진행한다. source_ref 후보는 Application/Worker가 static/mechanical anchor catalog로 준비하고, 모델은 새 source_ref를 생성하지 않고 입력 catalog의 `source_ref_id`만 선택한다. Core SOT는 `project_id + snapshot_id`로 source_ref catalog를 source order로 읽는 surface를 제공하며, Mongo required index는 `source_refs_by_project_snapshot`이다. Prompt template은 DB에 저장해 versioned 관리하며, `prompt_templates` 저장소는 `task_type + version` unique contract를 가진다. 첫 prompt version literal은 `analysis_extract_v1`, task_type은 `analysis_extract`다. Prompt builder는 snapshot metadata/raw_text와 source_ref catalog를 Gateway `ChatCompletionRequest`로 조립한다. Application runtime은 `LLM_GATEWAY_BASE_URL`이 설정된 경우 `/v1/generate` 기반 provider adapter와 versioned prompt extractor로 기본 analysis runner를 구성한다. env가 없으면 기존처럼 pending `run`은 runner 미구성 503이다. `{"candidates":[]}`는 유효한 빈 extraction 결과다.
- Phase 2A versioned provider extraction은 strict parser를 기본으로 유지한다. 첫 provider content가 malformed JSON 또는 candidate schema mismatch로 실패하면 Application adapter가 원문 output, parser error, 원래 prompt payload를 포함한 repair prompt를 같은 provider에 1회만 재호출한다. **markdown code fence로 감싼 유효 JSON은 실패가 아니라 추출 대상이다** — v1.6.86부터 `_json_object`가 whole-content fence(임의 lang tag)를 strip한 뒤 parse하므로, fence-wrapped 유효 extraction은 first call에서 그대로 통과한다(gate v1.6.83·report v1.6.85와 동일 `strip_code_fence` 정규화이며 strict schema 완화가 아님). fence를 벗겨도 malformed거나 schema mismatch면 위 repair 경로가 그대로 적용된다. 첫 provider content가 JSON/schema는 통과했지만 `source_ref_id`, span, quote, content_hash가 입력 source_ref catalog와 정확히 일치하지 않는 경우도 같은 1회 repair 대상이다. repair output도 top-level `{candidates:[...]}` JSON object와 Phase 2A candidate/source schema를 통과해야 한다. repair 실패 또는 output truncation은 성공으로 보정하지 않고 기존 runner failure mapping에 따라 보존한다. repair 후에도 source_ref catalog mismatch가 남으면 기존 source validation 경계가 `source_invalid`로 실패를 보존한다.
- Phase 2A와 2B는 별도 milestone이다.
- Phase 2B는 Phase 3~4 이후 prior memory를 검색해 `create/update/add_evidence/no_change/conflict` 후보를 만든다(action literal + `merge/split` review-only는 v1.6.39 D4로 확정).
- Analysis AI는 canon을 확정하지 않고 기존 기억을 직접 덮어쓰지 않는다. v1.6.39 D2=B로 자동 승격을 도입하되 **AI가 아니라 결정적 시스템 threshold gate**가 승격하므로 이 경계는 유지된다.
- v1.6.39로 착수 결정 확정: 첫 sub-slice(2B.1)는 canonical memory store(`MemoryEntry`) + candidate 승격(D1=A). 자동 승격은 confidence threshold gate(threshold 이상 자동 `canonical`, 미만 `needs_review`+수동), threshold는 fixture/benchmark 근거·그 전까지 보수적 주입 설정값(D2=B). entity resolution은 결정적 key만(D3=A). taxonomy는 2A 3종 유지(D5=A). **미확정으로 남은 것**: 자동 승격 threshold 실제 수치(품질 fixture 후), `needs_review` candidate의 Writing 포함(⑤ canonical은 v1.6.48로 닫힘 — candidate는 A의 machinery 재사용하는 다음 slice + review 지위 Phase 6), compare judge live smoke 실행(sandbox 밖)·판정 경계 심화 fixture, event/open_question 의미적 resolution **threshold 실 캘리브레이션**(v1.6.47로 seam+계약+off-기본 닫힘; 실값은 실 embedding+데이터 fixture로, sandbox 밖 후속), 중간 status(`confirmed`)·taxonomy 확장, conflict review queue 영속화(v1.6.59로 닫힘 — apply가 `conflict`를 durable `review_queue`에 영속화 + `GET .../review-queue`; merge/split 산출·resolve/dismiss 전이는 Phase 6 잔여), character 별칭 semantic 보강(2B.3 D2=A 확장 — 별칭 false-negative 방향은 v1.6.62로 닫힘[탐지만·자동 병합 없음], **동명이인 false-positive 반증·threshold 실 캘리브레이션은 후속 잔여**), Writing canonical retrieval의 vector/검색엔진 확장(v1.6.48 D2=A Mongo-direct의 retrieval 레이어 교체). (⑧ `scope`는 v1.6.42, compare judge adapter+prompt는 v1.6.43, versioned upsert 실쓰기는 v1.6.44, memory 재색인은 v1.6.45(증분1)·v1.6.46(증분2 라이브 배선), event/open_question 의미 resolution seam은 v1.6.47, ⑤ Writing canonical 포함은 v1.6.48로 닫힘. memory→vector live 실행만 sandbox 밖 잔여.)
- v1.6.40으로 2B.1이 구현됐다(`services/application/app/memory/`). `MemoryEntry`(status `canonical` 단일 literal)는 승격된 candidate의 payload/provenance/source_ref_ids/confidence를 보존하고 `version=1` + 감사 필드(analysis_job_id/source_candidate_id/promotion_mode/applied_threshold)를 가진다. 수동 승격(`manual`)은 confidence 무관 항상 canonical, 결정적 threshold gate(`auto_threshold`)는 `threshold is not None and confidence >= threshold`만 승격하고 미만은 candidate `needs_review` 유지. threshold는 `MEMORY_AUTO_PROMOTION_THRESHOLD` 주입값·기본 `None`(off). 승격 idempotency는 `(project_id, source_candidate_id)` unique index. HTTP는 candidate promote/job auto-promote/memory 조회. job auto-promote 응답의 `promoted[]`는 **이번 호출에서 신규 승격된 memory만** 담는다(같은 job 재호출은 idempotent replay라 재보고하지 않으며 저장 memory 수는 불변). **2B.1 경계**: D3 scope key 매칭·충돌 해소(같은 entity 중복 canonical의 update/merge)는 compare(2B.3)에 위임하고 2B.1 유일성은 source_candidate_id로만 잡는다(create-only/version=1). 이 slice 경계는 오너 확인 대상이다.
- v1.6.41로 2B.2가 구현됐다(`services/application/app/context_search/prior_memory.py`). prior-memory 검색+패키징 slice로 판정(2B.3)은 켜지 않는다(D1=A). 신설 literal `ContextSearchPurpose.ANALYSIS_CONTEXT`/`ContextNeed.PRIOR_MEMORY`. `DeterministicPriorMemoryBackend`가 같은 project·같은 `memory_type`의 canonical `MemoryEntry`를 결정적 조회하고(`PriorMemoryBackend` 주입 seam=후속 semantic 검색 교체점, D2=A), `AnalysisContextService`가 `ContextPackage`(purpose=analysis_context, `prior_memories`=`PriorMemoryItem[]`, macro/micro/trace 비움)로 묶는다. `PriorMemoryItem`은 taxonomy 5필수(value=payload/status/source_ref_ids/version/match_reason)를 담고 scope는 미포함(2B.3, §8 ⑧ 추적 유지). 진입면은 job-aware(HTTP `POST /projects/{id}/analysis/jobs/{job_id}/context`가 job candidate types→coarse memory_type 집합 유도, D4=B), 빈 memory_types=빈 package. F4 self-exclusion(`exclude_job_id`=그 job)은 오너 승인 잠정값(2B.3 관찰 후 확정). Gate는 purpose 분기(`evaluate_analysis_context_gate`): candidate 금지 무적용, Writing item 누출만 차단, cross-project는 조회 격리가 보장(D5=A/F5). `/context-search`는 Writing 전용으로 `analysis_context` purpose를 400 거절.
- v1.6.42로 2B.3(compare→action proposal + D3 scope key)이 구현됐다. `memory/scope.py`의 `derive_scope`가 character만 결정적 `MemoryScope(character, 정규화 name)`을 내고 event/open_question은 None(D2=A). `MemoryEntry.scope`·`PriorMemoryItem.scope` 추가(승격 시 산출, §8 ⑧ 완전 완성, D5=A). `analysis/compare.py`의 `AnalysisCompareService`가 결정적 scope 매칭으로 후보를 좁혀 매칭 없음→`create`, 1개→주입 `CompareJudge`(터미널 JSON seam, D1=A)가 update/add_evidence/no_change/conflict 라벨(judge의 `create` 반환은 거절), 복수 canonical 동일 identity→결정적 `conflict`(2B.1 중복 canonical 경계 표면화). D6 self-exclusion(같은 job 승격분 제외) 확정. proposal only(D4=A, 쓰기는 2B.4). HTTP `POST .../jobs/{job_id}/compare`(D7=A, 매칭인데 judge 미구성 503).
- v1.6.43로 2B.3.2(실제 Gateway 터미널-JSON compare judge)가 구현됐다. `analysis/compare_judge.py`의 `TerminalJsonCompareJudge`가 versioned prompt `analysis_compare_v1`로 매칭 pair를 Gateway `/v1/generate` 1-turn에 보내 `{action, rationale}` JSON을 strict parse(+1회 repair, 실패 `InvalidJudgeResult`→502)한다. matched-pair 4종만 허용하고 `create`는 judge 출력에서 거절. `create_app`은 `_default_compare_service`로 env `LLM_GATEWAY_BASE_URL` 있을 때 wiring, 없으면 종전 503. live smoke는 `scripts/phase2b3_compare_judge_live_smoke.py`(sandbox 밖). 판정 경계 fixture 심화·live 실행은 후속.
- v1.6.47로 2B.6(event/open_question 의미적 identity resolution)이 구현됐다. 오너 결정 D1=A(semantic은 compare `_find_matches`, `PriorMemoryBackend` 아님 — HANDOFF 오기 정정)·D4=A(threshold 주입값·기본 off, 캘리브레이션 후 발화). `MemoryVectorIndexAdapter.query_similar`(memory_type $and 필터) + `analysis/semantic_matcher.py`의 `EmbeddingSemanticMatcher`(쓰기와 동일 projection·embedding → top-1 ≥ threshold, self-exclusion·canonical 필터) + `AnalysisCompareService.semantic_matcher` seam(off 기본). character는 결정적 유지(D5=A). event/open_question always-create 누적 오너 확인 항목 해소(D4 캘리브레이션 시 실발화). 실 embedding 캘리브레이션은 sandbox 밖 후속.
- v1.6.62로 (c) **character 별칭 semantic 보강**이 구현됐다(브리프 `plans/02b-7-character-alias-homonym-decisions.md`, D1=A 탐지만·D2=A 별칭만 먼저). 2B.6 semantic read 인프라(`EmbeddingSemanticMatcher`·`query_similar`)를 재사용해, character candidate가 결정적 name-key 매칭 0일 때(scope≠None) 다른 이름의 canonical character와 의미적으로 가까우면(별칭 false-negative) `conflict`(review)로 surface한다 — **자동 병합 없음**(judge 미호출, 2B.3 D2=A 존중). `AnalysisCompareService.alias_matcher` seam(off 기본, 별도 env `ANALYSIS_CHARACTER_ALIAS_MATCH_THRESHOLD`). name-key≥1은 종전 judge 경로 불변. 동명이인 false-positive 반증(name-key=1 분기)·threshold 실값 캘리브레이션·merge/split write는 후속. 라이브 관통 PASS(실 BGE-m3-ko + Chroma).
- v1.6.46으로 2B.5 **증분 2**(라이브 배선)가 구현됐다. 오너 결정(2026-07-07)으로 canonical 생성 3경로(apply·수동 promote·auto-promote)가 모두 재색인 enqueue하도록 **`MemoryService`에 enqueue를 중앙화**(비-replay mint 시 `_enqueue_reindex`; 증분 1 apply-seam은 흡수). 실 Chroma `ChromaMemoryVectorIndexAdapter`(`memory_vectors` collection) + `create_app` 기본 배선 + worker `_build_memory_adapter`(Mongo/embedding/Chroma) drain + `phase2b5_reindex_memory.py` backfill + `phase2b5_memory_reindex_live_smoke.py`(sandbox 밖). 미배선이던 증분 1 gap 폐쇄.
- v1.6.45로 2B.5(memory→vector 재색인) **증분 1**(계약+fake+회귀)이 구현됐다(D3=B async, D6=write-only). `indexing/memory_index.py`: `derive_memory_index_text`(D1=A projection), `MemoryIndexRecord`(D2/D5, 별도 `memory_vectors` collection·`IndexRecordKind.MEMORY`), `InMemoryMemoryVectorIndexAdapter`, `MemoryIndexSyncAdapter`(D4 교정 canonical-only self-heal). `IndexSyncEvent.MEMORY_UPSERTED` + `IndexSyncOutboxService.enqueue_memory_upserted` + `IndexSyncWorker`의 memory dispatch(optional `memory_adapter`) + `MemoryApplyService`의 optional `reindex_outbox`(CREATED/VERSIONED enqueue). **라이브 배선(실 Chroma `memory_vectors` adapter + worker/create_app 배선 + backfill 스크립트 + live)은 증분 2**(sandbox 밖) — 증분 1 create_app은 drain 측 부재로 `reindex_outbox` 미배선(2B.3 judge-None seam 리듬). semantic read(event/open_question resolution)는 `PriorMemoryBackend` vector 교체로 후속 slice.

### Phase 3. Indexing

정본 세부 문서: [`plans/03-indexing.md`](plans/03-indexing.md)

- ChromaDB와 Elasticsearch는 MongoDB pointer/version/status를 가진 파생 인덱스다.
- index hit는 SOT 재조회 전까지 정본이 아니다.
- Phase 3A 첫 slice는 Core SOT source block only를 Chroma-like vector contract에 색인한다. `IndexPointer`는 `project_id`, Mongo collection/document id, version id, content hash를 가진다. `IndexSyncRequest(project_id, snapshot_id, target)`와 `IndexSyncResult(request, records_attempted, records_written)`는 explicit rebuild용 in-process 축소 계약이다. `contracts.md` §7.3의 persistent sync log/outbox envelope(`sync_result_id`, `sync_request_id`, target별 결과, timestamps)는 후속 sync log slice 전까지 구현 대상이 아니다. Embedding은 deterministic fake provider만 사용하고 실제 embedding model/dimension은 후속 quality spike 뒤 확정한다. Sync delivery는 explicit snapshot rebuild only이며 자동 outbox/polling은 후속이다. Archive/delete 반영은 hard delete가 아니라 explicit rebuild가 materialize한 `project_archived`/`draft_archived` metadata status filter로 query 결과에서 제외한다. Archive 이후 기존 stale record를 즉시 숨기려면 재build 또는 후속 automatic sync가 필요하다. Adapter failure는 MongoDB/Core SOT 저장 결과를 rollback하지 않는다.
- Phase 3A source-block hit는 `validate_source_block_record(record)`로 Core SOT 정본을 재조회해 사용 가능 여부를 확인할 수 있다. Stale reason literal은 `project_archived`, `draft_archived`, `snapshot_missing`, `draft_mismatch`, `content_hash_mismatch`, `block_missing`이다. `snapshot_missing`은 snapshot을 조회할 수 없어 다른 stale check를 진행할 정본이 없으므로 단독 reason으로 short-circuit한다. `draft_archived`는 조회된 snapshot의 현재 owning draft 상태 기준이며, record의 stale `draft_id`가 다르면 별도로 `draft_mismatch`도 반환한다. Drift 판정은 `version_id` 비교가 아니라 snapshot `content_hash`, draft pointer, block pointer 정합성 기준이다. 이 검증은 기존 materialized vector record를 자동 삭제하거나 자동 숨김 처리하지 않으며, query/Context Gate 계층이 hit 사용 전 정본 상태를 확인하기 위한 explicit guard다.
- Phase 3A는 `scripts/phase3a_rebuild_source_block_index.py`로 explicit rebuild를 실행할 수 있다. 이 script는 `--project-id`, `--snapshot-id`, `CORE_SOT_MONGO_URI`/`--mongo-uri`를 받아 Core SOT MongoDB에서 snapshot blocks를 읽고 current fake vector adapter에 rebuild한 뒤 JSON summary를 출력한다. JSON summary field는 `project_id`, `snapshot_id`, `target`, `records_attempted`, `records_written`, `records_indexed`, `records_query_visible`, `records_archived`다. Exit code는 full write 성공 0, partial write(`records_attempted != records_written`) 1, usage/config/domain error(no Mongo URI, missing snapshot 등) 2다. Application HTTP API endpoint와 persistent vector backend는 후속이다.
- Phase 3A는 Application HTTP API `POST /projects/{project_id}/snapshots/{snapshot_id}/index/source-blocks/rebuild`로도 explicit rebuild를 실행할 수 있다. JSON summary field는 `project_id`, `snapshot_id`, `target`, `backend`, `records_attempted`, `records_written`, `records_indexed`, `records_query_visible`, `records_archived`다. missing/cross-project snapshot은 404다. v1.6.35부터 이 endpoint는 `create_app`이 소유한 공유 vector index에 write하고(같은 프로세스 context search가 read), summary count(`records_indexed`/`records_query_visible`/`records_archived`)는 해당 rebuild의 `snapshot_id`로 scope해 per-rebuild 의미를 유지한다(누적 없음). v1.6.36부터 `backend`는 wiring에 따라 `chroma`(env `CHROMA_HOST` 설정 시 real 영속 Chroma) 또는 `in_memory_fake`(미설정)다. automatic sync는 후속이다.
- Phase 3B archive outbox 첫 code slice가 구현됐다. Application archive endpoint는 `project_archived`/`draft_archived` event를 Core SOT archive 성공 후 `index_sync_outbox`에 pending entry로 남긴다. Dedup key는 `(project_id, event, source.mongo_collection, source.mongo_id)`라 재archive가 중복 outbox entry를 만들지 않는다. Entry는 nullable `user_id`, canonical `targets.chroma.status`, `targets.chroma.backend="in_memory_fake"`, `status="pending"`, `attempt_count=0`, `max_attempts=3`, `next_attempt_at=null`, `last_error=null`을 가진다. Outbox와 future log는 `sync_request_id`로 조인한다. `index_sync_outbox`는 `mongo_collections.md` 운영 collection 레지스트리에 등록됐다. 오류 타입은 서버/backend 계열 `backend_error`와 데이터 없음/not-found 계열 `not_found`를 분리한다. `analysis_completed`는 오너가 장기적으로 가장 맞는 흐름으로 본 후속 event candidate지만, 아직 code enum에는 열지 않는다.
- Phase 3B one-shot worker 첫 slice가 구현됐다. Worker는 `pending` 또는 10분 이상 지난 stale `running` outbox entry를 claim하고, archive mutation을 실행한 뒤 `index_sync_logs`에 attempt result를 append한다. Terminal-location/index 전략은 terminal 이동을 채택해 `success|failed`가 되면 active outbox entry를 제거하고 terminal history는 `index_sync_logs`가 소유한다. Archive worker-time `not_found`는 idempotent success다. Query-time `not_found`는 후속 LLM orchestration/query selector retry loop의 error type으로 남는다.
- v1.6.37부터 worker command는 `CHROMA_HOST` 설정 시 `ChromaArchiveIndexMutationAdapter`로 real Chroma를 mutate한다: archive event는 매칭 derived source-block record를 **delete**하고(`project_archived`=project 전체, `draft_archived`=project-scoped draft), 대상이 없으면 `DerivedIndexRecordNotFound`→idempotent success다. `CHROMA_HOST` 미설정 시 종전 recording-only fake로 폴백한다. worker summary JSON에 `archive_backend`(`chroma`/`in_memory_fake`)가 포함된다. 실제 Chroma 서버 관통 live smoke는 후속이다.
- `analysis_completed` sync wiring만 미확정이다. ES analyzer(nori)·actual Elasticsearch mutation은 v1.6.52로(§8 ES lexical/hybrid), analysis candidate indexing은 v1.6.54로(`IndexSyncEvent.CANDIDATE_UPSERTED` outbox → worker composite drain이 `candidate_vectors`/`candidate_lexical`에 적재) 구현됐다. Chroma archive delete mutation은 v1.6.37로 구현됐다(worker→real Chroma).

### Phase 4. Agentic Search

정본 세부 문서: [`plans/04-agentic-search.md`](plans/04-agentic-search.md), [`plans/04-agentic-search-kickoff-decisions.md`](plans/04-agentic-search-kickoff-decisions.md)

- 목적에 맞는 ContextPackage 후보를 만든 뒤 Context Gate를 통과시킨다.
- 착수 브리프가 v1.6.30으로 승인됐고 Slice 4.1(domain 계약 + orchestration + Context Gate, planner 주입)이 v1.6.31로, Slice 4.2(터미널 JSON LLM planner adapter)가 v1.6.33으로, Slice 4.3(HTTP API `POST /projects/{id}/context-search` + service async wiring)이 v1.6.34로 구현됐다. 첫 slice literal: purpose `writing_context`, need `current_scene`/`recent_scenes`/`event_context`/`source_quote`. build_context_package는 async이고 Gate는 sync다. HTTP 오류 매핑: invalid 400 / wall-clock 504 / ContextSearchFailed 502 / missing project 404 / planner 미구성 503.
- v1.6.35로 공유 in-process vector index가 도입됐다. `create_app`이 단일 `InMemoryVectorIndexAdapter`(비durable, 재시작 시 소실)를 소유하고, source-block rebuild endpoint가 write하고 기본 wiring된 context search가 read해서 같은 프로세스의 rebuild → context search가 실제 vector hit(stale guard + SOT 재조회 통과)을 서빙한다. 공유 index는 `LLM_GATEWAY_BASE_URL` 유무와 무관하게 생성되고, planner 미구성 시 `/context-search`만 503이다. v1.6.36로 이 공유 vector 백엔드가 env 기반으로 real 영속 Chroma(`CHROMA_HOST` 설정 시 `ChromaVectorIndexAdapter`, 재시작 생존)와 real embedding(`EMBEDDING_SERVICE_URL` 설정 시 `RemoteEmbeddingProvider`, `dragonkue/BGE-m3-ko` 1024-dim)으로 승격 가능해졌고, 미설정 시 종전 fake로 폴백한다. embedding은 llama gateway와 분리된 컨테이너라 vector 백엔드는 LLM-독립이다. real ES lexical 경로는 계속 후속이다.
- planner는 터미널 JSON LLM planner다(1-turn SearchPlan JSON, strict parse + 1회 repair). `TerminalJsonSearchPlanner`가 versioned prompt template `context_search_plan_v1`(task_type `context_search_plan`)로 Gateway `/v1/generate`를 호출하고, enum literal(need/tool) 위반은 repair 1회 후 남으면 `llm_error`다. adapter는 literal 멤버십만 검증하고 plan 의미 검증은 service `_validate_plan`이 소유한다. provider가 async라 planner도 async이며 service 통합은 HTTP wiring slice로 미룬다. tool-call flat loop planner는 Gateway tool-call wire 계약 해소 후 전환한다(브리프 §2.1).
- `context_search` profile은 flat-loop allowlist 3종만 쓴다(전환 후 적용).
- tool success와 `validate_context` success는 Context Gate 통과가 아니다.
- retriever step 실패 error taxonomy는 `backend_error`/`system_error`/`llm_error`/`sot_error`로 시작하고 enum 확장 가능하다. `sot_error`(Mongo SOT reload 실패)는 degraded가 아니라 전체 실패이며, NotFound뿐 아니라 SOT reload 호출에서 탈출하는 모든 non-NotFound 예외(pymongo 장애 포함)를 원형 전파 없이 매핑한다(v1.6.32). vector hit의 snapshot NotFound만 index drift로 보고 `snapshot_missing` soft stale 제외한다. `system_error`는 예약 literal이다.
- package는 단일 schema + purpose literal로 시작한다. 이후 slice에서 Writing용/Analysis 비교용 모두 완성해야 하며(오너 추적 의무), analysis 비교용 필드는 Phase 2B 착수 브리프에서 결정한다. v1.6.38로 이 완성(⑤ candidate 포함 §5 B / ⑧ Analysis 비교용 §8 C)이 **Phase 2B에 종속됨**이 확정됐다(오너 결정 D1=B, `plans/04-context-package-completion-decisions.md`). Writing용 뷰는 현재 package로 Phase 5 MVP에 충분하고, candidate 포함은 canonical/승인 경로가 없어 지금 하지 않으며 `evaluate_context_gate`의 candidate 라벨 금지를 유지한다. v1.6.41로 ⑧ Analysis 비교용 뷰(`analysis_context` purpose + `prior_memory` need + `PriorMemoryItem` 5필수)가 구현됐고, **v1.6.42로 `PriorMemoryItem.scope`가 추가돼 ⑧이 완전 완성**됐다(memory type/scope/status/version/검색 이유). scope는 D2=A로 character만 결정적(name), event/open_question은 None이다. **v1.6.48로 ⑤ Writing canonical 포함이 구현됐다**(브리프 `plans/04-writing-canonical-context-decisions.md`, D1=A canonical만·D2=A Mongo-direct): `ContextNeed.CANONICAL_MEMORY` + `CanonicalMemoryRetriever` seam + Gate memory-item 재검증 분기(memory store로 canonical 재확인). **v1.6.50으로 `needs_review` candidate 포함이 구현됐다**(브리프 `plans/04-writing-candidate-context-decisions.md`): `ContextNeed.CANDIDATE_MEMORY` + `CandidateMemoryRetriever` + Gate candidate-origin 재검증(candidate 금지를 폐지 않고 candidate origin `analysis_candidates`만 예외로 좁힘; `micro`·`status=candidate`/`review_status` 라벨·권위필드 배제로 §62 준수). **v1.6.51로 canonical retrieval이 vector로 확장됐다**(브리프 `plans/04-writing-memory-vector-retrieval-decisions.md`): `VectorCanonicalMemoryRetriever`(env 구성 시 `memory_vectors` per-type query→cosine 단일 풀 병합→`get_memory` 권위 재유도→canonical-only, 아니면 Mongo-direct fallback) — retrieval 교체는 step/item/Gate 불변(D0=A canonical만). **v1.6.52로 §8 ES lexical + hybrid(RRF)가 추가됐다**(브리프 `plans/04-writing-memory-lexical-retrieval-decisions.md`): `LexicalCanonicalMemoryRetriever`(ES nori query→`memory_id`→`get_memory` 권위 재유도→canonical-only) + `HybridCanonicalMemoryRetriever`(vector+lexical RRF 융합) + worker composite drain(vector+lexical). env 배선이 vector/lexical/hybrid/Mongo-direct를 택하고 seam은 불변. **v1.6.53으로 이 lexical/hybrid가 배포에서 실동작하도록 compose 전용 ES 서비스(nori)가 추가됐다**(브리프 `plans/04-compose-elasticsearch-service-decisions.md`): `services/elasticsearch/Dockerfile`(공식 ES + analysis-nori) + `docker-compose.yml` `elasticsearch` 서비스(single-node·보안 off) + application `ELASTICSEARCH_URL` 배선 → 배포 canonical retriever 기본 = hybrid. 계약 literal 변경 없음(배포 표면 실현 + `connect_` request_timeout=30 견고화). **v1.6.54로 candidate 색인 파이프라인이 신설됐다**(브리프 `plans/04-writing-candidate-retrieval-decisions.md`): `IndexRecordKind.CANDIDATE`·`IndexSyncEvent.CANDIDATE_UPSERTED` + `record_candidate(s)` choke point → worker composite drain이 canonical과 물리 분리된 `candidate_vectors`/`candidate_lexical`에 적재(candidate는 needs_review 단일 상태라 색인 upsert-only, de-index·needs_review 필터는 Phase 6 전이 도입 시 도달하는 forward-defense). **v1.6.55로 candidate retrieval이 vector/lexical/hybrid(RRF)로 확장됐다**: `Vector`/`Lexical`/`HybridCandidateMemoryRetriever`가 canonical과 동형 env 스위치로 backend를 택하고 `get_candidate` 권위 재유도→needs_review-only(seam·item·Gate 불변). **v1.6.60으로 (e) canonical↔candidate 승격 dedup이 구현됐다**(브리프 `plans/04-canonical-candidate-dedup-decisions.md`, D1=승격 시 항상 억제·D2=retrieval-time interim): 승격된 candidate는 status가 `needs_review`로 남아(전이는 Phase 6) candidate retriever가 계속 반환하는데, 그 canonical 사본이 같은 지식을 이미 grounding하므로 candidate step이 `PromotedCandidateResolver`(`source_candidate_id` identity 링크, `MemoryService.is_candidate_promoted`)로 억제한다(canonical이 같은 package에 있든 없든, `ExcludedHit reason="candidate_promoted"` trace). resolver 미주입 시 억제 없음(종전 D7 하위호환). Phase 6 candidate de-index 도입 시 이 억제는 상위집합으로 흡수된다.

### Phase 5. Writing AI

정본 세부 문서: [`plans/05-writing-ai.md`](plans/05-writing-ai.md)

- Writing AI는 DB/검색 tool에 직접 접근하지 않는다.
- MVP `writing_generate` profile은 tool 없음이다.
- 검증된 ContextPackage와 append-only ProjectBrief 문체 설정으로 WritingCandidate를 만든다. 별도 `WritingBrief` 계약은 v1.7.21에서 삭제됐다.
- 사용자가 accept하기 전에는 draft version이나 canon이 바뀌지 않는다.
- **accept 전 candidate는 소실되지 않는다**(v1.7.20). generate가 만든 candidate는 Core SOT 외부 복구 저장소에 남아 새로고침/이탈 뒤에도 복구할 수 있다. 이는 정본을 만들지 않으며(위 [Source of Truth](#source-of-truth)의 `writing_drafts_scratch` 계약), "accept 전에는 canon이 바뀌지 않는다"는 위 계약과 충돌하지 않는다 — scratch는 정본이 아니라 복구 tier다. public surface는 `GET/DELETE /projects/{id}/writing/scratch?draft_id=`다.
- **v1.6.68로 첫 생성 slice가 구현됐다**(브리프 `plans/05-writing-generation-decisions.md`, 생성만): 오너 Q1=생성만(Gate 다음 slice)·Q2=평문 프로즈(서비스가 WritingCandidate 래핑). continue_scene 한정, 직접 1-turn Gateway 호출(`WritingService`, extractor/compare_judge 패턴), output_type=draft_patch, 결정적 안전선=project isolation+task/instruction 검증. HTTP `POST .../writing/generate`가 context_search→generate 오케스트레이션. status는 항상 candidate(**동기 후보 응답 한정** — v1.7.27 증분 2c부터 `medium`/`long` 프리셋은 candidate가 아니라 job을 반환한다; 위 `writing_generation_jobs` endpoint 배선 항). **Writing Gate(pass/revise/retrieve_more/needs_user_review/block, LLM 기반)·accept→save 재진입·구조적 self-report(candidate_claims/new_memory_hints/risk_notes)·revise/outline/critique/rewrite_style task는 계속 미확정(후속 slice).**
- **Gate `style` finding + 문체 우선순위(v1.7.24, 증분 3 D5=A/D6=A)**: Writing Gate가 후보 산문을 **저자의 ProjectBrief 문체 설정**(tone·`style_rules`·`preferred_patterns`·`forbidden_patterns`·`style_examples`, v1.7.21)과 대조해 이탈 시 `style` finding을 낸다. **style은 advisory다**: severity=warning·recommendation=needs_user_review로 고정되고(parse 경계에서 강제), **decision 우선순위에서 제외**되어 findings가 style뿐인 후보는 여전히 `pass`다(경고이지 차단 아님) — 저자가 알아차리고 최종 채택 여부를 결정한다(D6). style은 **자동 revise 대상이 아니고**(auto-revise는 continuity 전용, 명시적으로 잠금) `block`이 될 수 없다. **우선순위(D6=A)**: `저자 설정 > canonical 관찰 > candidate 관찰`(Phase 7 D7 트리)을 문체/어투에도 적용한다 — Gate는 **설정을 기준으로만** 판정하고 분석 관찰은 설정을 덮지 않으며, 관찰→설정 자동 반영은 만들지 않는다(ProjectBrief는 append-only version+history라 저자가 직접 수정하면 이력 보존). 캐릭터 어투(D4 `aspect`) 기반 대조는 캐릭터-어투 **설정** 저장이 deferred라 이번엔 프로젝트 문체 설정 대조까지이며, aspect 관찰은 forward-defense다. mood는 키가 없어 Phase 7. 프론트는 `pass`+style이면 accept를 유지하고 style finding을 advisory 안내와 함께 보인다.
- **생성 분량 프리셋(v1.7.22, D3=A)**: generate 요청 필드 `output_length=short|medium|long`이 출력 토큰 프리셋을 고른다. **서버가 프리셋→토큰 매핑을 소유**한다(기본 1024/2048/4096; `WRITING_OUTPUT_LENGTH_SHORT|MEDIUM|LONG` env 조정 가능·**값이 1 미만이거나 정수로 파싱되지 않으면 기동 실패**, `short` 미설정 시 기존 `WRITING_GENERATE_MAX_TOKENS` 승계). 생략 시 `short`(하위 호환), 알 수 없는 값은 400. 이 필드는 **출력 토큰 축이며 입력 예산 `max_tokens`(ContextPackage budget)와 독립**이다. **`long`(4096≈91초)은 loop wall clock(60000ms)을 초과하므로 단일 generate 전용** — `output_length`는 `/writing/revise-and-gate` 요청 필드가 아니고, 프론트도 `long` 뒤 자동 루프를 건너뛴다. loop 예산(10000tok/60000ms)은 무변.

### Phase 6. Review UI

정본 세부 문서: [`plans/06-review-ui.md`](plans/06-review-ui.md)

- 사용자가 후보의 원문 근거, 기존 기억 diff, Gate finding을 보고 approve/reject/edit/defer 한다.
- 승인 전 candidate가 canonical UI나 검색 constraint로 위장되지 않는다.
- 승인 후 MongoDB가 먼저 갱신되고 인덱스는 그 결과를 따른다.
- **v1.6.61로 candidate 상태 전이 백엔드 계약이 구현됐다**(브리프 `plans/06-candidate-state-transition-decisions.md`, 이 slice의 forward-defense stub 수렴점): 오너 결정 **D1=분리 모델**(`confirmed`=candidate 검토 결정 기록[`needs_review→confirmed`] + `canonical`=promotion 물질화[`source_candidate_id` 링크], 승격 주체=confirm; `rejected`는 promotion 없이 보존[D5]). `AnalysisService.transition_candidate`(legal `needs_review→confirmed/rejected`만·cross-terminal/backward 거부·idempotent) + `CandidateReviewService`(confirm=전이+promote+de-index+queue resolve / reject=전이+de-index+queue dismiss, D4 idempotent 단건). de-index=`CANDIDATE_REMOVED`(D2, worker 재유도 delete). review_queue `open→resolved/dismissed`(D3). `POST .../candidates/{cid}/confirm|reject`(404/409).
- **v1.6.66으로 candidate edit 백엔드 계약이 구현됐다**(브리프 `plans/06-candidate-edit-decisions.md`, review action 3종의 마지막 edit): 오너 결정 **D1=원 후보의 새 version**. edit은 편집 payload로 candidate의 새 version(append-only, 새 id)을 mint하고 그 version을 `confirmed`+canonical 승격하며(promote 링크=successor id), 원 후보는 신규 terminal `superseded`(D3, edit 전용 — `transition_candidate`의 legal target 아님)로 보존·de-index한다. edit-and-confirm 단일 액션(D2), 근거/provenance/confidence 보존·payload만 schema 재검증(D5), idempotency는 `logical_key=edit:{원본id}`가 기존 candidate unique index를 재사용(D4, 클라이언트 key 불요·원본 1회 edit), 원본 conflict는 resolve(D6). `AnalysisService.edit_candidate`+`CandidateReviewService.edit`, `POST .../candidates/{cid}/edit`{`payload`}(404/409/400).
- **v1.6.67로 Review Inbox 액션 어포던스가 구현됐다**(브리프 `plans/06-review-inbox-affordances-decisions.md`): 오너 결정 D1=자격 주석형 `{action, eligible, reason}`·D2=candidate+conflict+gate finding 전부·D3=list+detail 둘 다. inbox가 각 항목의 가용 review action과 자격(불가 이유 포함)을 선언 — candidate(confirm/reject/edit 항상 eligible)·conflict(merge=character+matched, split=character)·gate finding(resolve/dismiss=open일 때만). 권위는 read-time 재계산이고 write 엔드포인트가 실제 authority(어포던스는 선언만, 상태·색인 무변 additive). `ActionAffordance` + `candidate_affordances`/`conflict_affordances`/`gate_finding_affordances`, review-inbox list/detail·conflict·gate finding payload에 `actions` 필드. **machine contract = `action`+`eligible`이고 `reason`은 display text**(안정 literal 아님 — 소비자는 `reason` 문자열을 pattern-match하지 않는다; localization은 계약 bump 불요). **부분 승인/부분 retry·merge/split 일반화·source deep link route·frontend는 계속 Phase 6 UI slice로 미확정.**

## 현재 구현 상태

| Slice | 상태 | 근거 |
|---|---|---|
| LLM Gateway 0.1~0.6 | 구현·검증 완료 | `services/llm_gateway/`, `tests/test_llm_*`, `test_httpx_transport.py` |
| AgentLoopRunner A1 | 구현·검증 완료 | `decision.py`, `budget.py`, A1 verification |
| AgentLoopRunner A2 | 구현·검증 완료 | `registry.py`, A2 verification |
| AgentLoopRunner A3 | 구현·독립 검증 합격(보강) | `completion.py`, `resolution.py`, budget F1 방어 + retry cap, `InvalidBudgetPolicy.decision` |
| AgentLoopRunner provider composition | 구현·독립 검증 합격(보강) | `parser.py`, `runner.py`, `test_agent_loop_runner.py`; I2 forward-lock(provider usage budget before completion, retry non-free), provider runner verification |
| AgentLoopRunner production budget defaults | benchmark 기반 확정 | `docs/benchmarks/2026-06-30/gemma_q4_llama_cpp_repeats3_warmup1.json`, `plans/flat-loop-gate.md` |
| Core SOT minimal skeleton | 구현·조건부 검증 보강 완료 | `services/application/app/core_sot/`, `services/application/app/main.py`, `tests/test_core_sot.py`, `tests/test_application_api.py`; C1/C2/C3 보강 |
| Product Shell UI | A editor/save/history/export 구현 완료. Writing 작업공간 **C0 HTTP contract + C1 기본 UI(generate→Gate→accept/save) + C2 bounded revise/retrieve loop UI 구현 완료**. 다음은 B Review Inbox | `frontend/`, `frontend/src/drafts/DraftEditor.tsx`, `frontend/src/writing/WritingPanel.tsx`, `services/application/app/writing/http_models.py`, 프론트 회귀 |
| Phase 2~6 UI | 미구현 | 백엔드 계약과 계획 문서만 존재 |

## 다음 구현 경계

- agent_loop 계약층은 A1/A2/A3, self-report parser, provider composition까지 구현·독립 검증됐다.
- runner의 domain tool-call branch는 지금 구현하지 않는다. 선행 계약은 (1) Gateway tool-call response parsing, (2) model tool-call wire format, (3) Phase payload와 실제 tool handler다.
- `ProviderTurnResult`는 현재 terminal provider content와 usage만 표현한다. tool-call 수신 구조는 wire 계약이 생긴 뒤 확장한다.
- `artifact_present`는 현재 runner에 주입되는 결정 함수다. Slice 2A AnalysisCandidate, Slice 4 ContextPackage, Slice 5 WritingCandidate schema가 확정되면 profile별 구조 조건 평가로 교체한다.

## 미확정 결정 목록

다음 항목은 구현자가 임의로 채우지 않는다.

- ~~frontend framework 최종 선택~~ (v1.6.93 확정 — D1=A React+TS+Vite · D2=B 별도 nginx compose 서비스(/api reverse-proxy, CORS 미개방) · D3=A Product shell 척추 우선. `plans/frontend-kickoff-decisions.md`). Writing loop 진행 표시는 v1.7.3 C2로 구현 완료했고, 자동 저장(autosave) 정책만 후속 결정으로 남는다.
- 외부 queue 제품 선택. 초기 local/personal runtime은 단순한 in-process/background boundary로 시작한다.
- Gateway/model tool-call response wire format
- ~~`confirmed`와 `canonical`의 의미 및 승격 주체~~ (v1.6.61 확정 — D1=분리 모델: confirmed=candidate 검토 결정, canonical=promotion 물질화, 승격 주체=confirm 전이)
- Phase 2/6 review action idempotency(v1.6.61 확정 — 단건 전이 idempotent no-op replay; v1.6.66 candidate edit도 idempotent 단건)와 **부분 승인/부분 retry 정책**(미확정 유지 — Phase 6 UI slice). candidate edit 반영 위치는 v1.6.66으로 확정(원 후보의 새 version)
- adaptive chunking 또는 길이 기반 episode/section chunking을 Phase 3 파생 index에 도입할지 여부
- Phase 2B taxonomy 확장과 confidence threshold
- Analysis `update/add_evidence/no_change/conflict`의 정확한 public envelope
- Chroma embedding model, ES analyzer, 자동 sync delivery 방식
- ContextPackage의 Analysis 비교용 확장 필드(단일 schema + purpose literal은 v1.6.30으로 확정, Writing용/Analysis 비교용 모두 완성은 추적 의무, 필드는 Phase 2B 착수 브리프에서 결정; v1.6.38로 ⑤ candidate 포함과 함께 Phase 2B 종속 확정 — D1=B; v1.6.41로 ⑧ `analysis_context`/`prior_memory` + `PriorMemoryItem` 5필수 구현; v1.6.42로 `PriorMemoryItem.scope` 추가로 ⑧ 완전 완성; **v1.6.48로 ⑤ Writing canonical 포함 구현**(canonical만, D1=A/D2=A Mongo-direct); `needs_review` candidate 포함은 바로 다음 slice)
- WritingCandidate 출력 단위(full text/patch) — v1.6.68로 continue_scene은 평문 프로즈(output_type=draft_patch, 서비스 래핑)로 확정; 다른 task type/editor 적용 단위는 후속
- Writing Gate decision literal은 v1.6.69로 `pass|revise|retrieve_more|needs_user_review|block` 확정. continue_scene accept 적용 단위는 v1.6.70의 latest-base paragraph append로 확정; finding-evidence 기반 부분 revise/retrieve와 client offset/separator patch는 후속으로 미확정 유지
- enum/bounds를 쓰는 첫 tool schema 등록 시 validator 확장 방식

## 변경 규칙

1. 계약 literal을 바꿀 때는 해당 테스트와 검증 기록을 함께 갱신한다.
2. spec과 구현이 충돌하면 둘 중 하나를 조용히 선택하지 않는다.
3. 미확정 항목을 구현해야 하면 사용자 결정 또는 좁은 spike로 먼저 계약을 만든다.
4. 문서-only 변경도 링크, 우선순위, status, 관련 HANDOFF를 확인한다.
5. 상세 구현 이력은 daily work log에 기록하고, 현재 actionable 상태만 `HANDOFF.md`에 남긴다.
