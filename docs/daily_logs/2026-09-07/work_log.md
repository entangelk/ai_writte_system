# 2026-09-07 작업 로그

## 목표

- HANDOFF Next Tasks **4번(장면 메모 후속)** 을 닫는다 — 오너 결정 2026-09-06 이 남긴 두 자리.
  - ① 목록 행에 **수정 시각**(`updated_at` 은 payload 에 이미 있다).
  - ② **절단 표시("더 보기")** — `truncated` 를 화면이 처음 읽는다. SoT v1.8.37 이 이 자리에 붙여 둔 조건은 *"열 때 true/false 양방향 셀과 함께 연다"* 이므로, 셀이 조건이지 부록이 아니다.

---

## 세션 24 — 장면 메모 후속: 수정 시각 · 더 보기 (SoT v1.8.38)

### 1. 착수 전 — 이것은 결정 브리프 자리가 아니다

두 자리 모두 오너가 *무엇을* 여는지 이미 정했고(2026-09-06), 남은 것은 배치뿐이었다. 그중 하나는 이미 답이 있었다 — HANDOFF 4번이 *"전문은 단건 GET 이 주므로 '더 보기'는 그 호출이다(목록이 전문을 기대하게 만들지 말 것)"* 라고 못박아 두었다. 남은 판단은 **"더 보기"를 별도 화면에만 둘 것인가**였고, 선례로 답했다: `SceneNoteSearch` 는 두 화면이 공유하는 한 벌이고 완료 기준 1 이 *"두 화면이 같은 검색 결과를 읽는다"* 이다. 행이 하는 일(링크 vs 선택)만 갈리는 것이 종전 설계이므로, 목록 자신의 성질인 펼침을 화면마다 다르게 두면 그 축이 하나 더 늘어난다. **양쪽에 똑같이 두었고, 모드 분기가 없어 코드도 그쪽이 적다.**

### 2. 구현 (커밋 `f46a462`)

`frontend/src/notes/SceneNoteSearch.tsx` 한 파일 + 스타일 2규칙이 전부다. 백엔드·API 계약·`schema.d.ts` 무변(두 필드 모두 Slice 1 부터 payload 에 있었다).

- **행 꼬리줄** `.note-row-meta` 를 새로 두고 거기에 수정 시각과 "더 보기"를 실었다. 제목 줄에 얹지 않은 이유는 좁은 드로어에서 제목·장 배지·보관 배지와 같은 줄에 서면 **제목이 먼저 줄바꿈**되기 때문이다.
- 수정 시각은 `<time dateTime={note.updated_at}>수정 {…toLocaleString("ko-KR")}</time>`. 선례는 `AdminConsole` 의 `요청 {…}` 이다. 서버 값은 UTC-aware 라(`core_sot/mongo_repository.py::_aware`) 브라우저가 로컬로 옮긴다 — naive 함정이 없다.
- **펼침은 한 번에 한 행**(`expandedId`·`expandedBody`)이고, 전문은 `getSceneNote` 단건 GET 이 준다. 조회 실패는 **미리보기를 걷어내지 않는다**(걷어내면 사용자는 메모가 사라진 것으로 읽는다) — 목록 조회 실패의 종전 처방과 같은 모양이다.
- **목록이 새로 오면 접는다.** 저장 뒤 갱신(`refreshKey`)이나 새 검색에서 펼친 전문만 옛 본문으로 남으면 같은 행의 미리보기와 다른 사실을 말한다.
- `.note-expand` 는 **링크형 버튼**(`.rail-back` 과 같은 모양)이라 accent 면을 쓰지 않는다 → 기본 동작 버튼의 겉모습 자리(`buttonAppearance.test.ts`)에 등재하지 않는다. 등재가 필요한 쪽은 **타이포 축**이었다(아래 Issues 1).

### 3. 셀 (신규 6)

| 파일 | 셀 | 잠그는 것 |
|---|---|---|
| `SceneNotesPage.test.tsx` | shows each row its own last-modified time | 행마다 **자기** `updated_at`. 실행 머신 시간대에 기대 문자열이 달리므로 같은 변환을 기대값으로 쓴다 — 잠그는 것은 문자열이 아니라 출처다 |
| " | offers 더 보기 only where the server said the preview was cut | `truncated` **양방향** — 참인 행에만 |
| " | pulls the full body from the single-note GET and folds back to the preview | 펼침이 **단건 GET** 이라는 것 + 접기 |
| " | keeps the preview on screen when the full body cannot be read | 실패가 미리보기를 지우지 않는다 |
| " | folds an expanded row back when a new list arrives | 목록 갱신에서 접기(변이 MN-8 이 드러낸 자리) |
| `SceneNotePanel.test.tsx` | expands a cut preview in place without moving the editing target | 드로어 목록에도 같은 자리가 있고, **펼침 ≠ 선택**(편집 대상 불변) |

### 4. 변이 (커밋 → 변이 → 원복, 매회 `git status --short` 로 바이트 대조)

| 변이 | diff | 재실패 셀 |
|---|---|---|
| MN-1 신호 무시(over) | `{note.truncated && (` → `{true && (` | 화면 truncated 셀 1 + 드로어 펼침 셀 1 |
| MN-2 신호 안 읽음(under) | `{note.truncated && (` → `{false && (` | 4(truncated · 단건 GET · 실패 유지 · 드로어) |
| MN-3 시각 오귀속(over) | `note.updated_at` → `notes[0].updated_at` | 수정 시각 셀 1 |
| MN-4 시각 제거(under) | `<time>` 블록 → `{null}` | 수정 시각 셀 1 |
| MN-5 목록이 전문을 준다(over) | `await getSceneNote(…)` → `setExpandedBody(note.body_preview)` | 3(단건 GET · 실패 유지 · 드로어) |
| MN-6 펼침을 선택에 물림(over) | `onClick` 에 `onSelect?.(note)` 추가 | 드로어 펼침 셀 1 |
| MN-7 실패가 목록을 지움(under) | catch 에 `setNotes([])` 추가 | 실패 유지 셀 1 |
| MN-8 갱신에서 안 접음 | `load` 성공부의 `setExpandedId(null)`·`setExpandedBody(null)` 삭제 | **처음엔 0 → 셀 보강 후 1**(커밋 `9262f27`) |

### Issues found

1. **타이포 축 가드가 새 규칙을 잡았다** — `.note-row-meta` 가 `var(--type-micro)` 를 쓰는데 `typeScale.test.ts` 의 이관 목록에 없어 전수에서 1실패(*"keeps the migration list identical to what the stylesheet actually migrated"*). 이것이 **가드가 설계대로 작동한 것**이다(4번 셀은 목록을 스타일시트에서 유도해 대조한다 — 목록이 뒤처지는 것 자체가 불일치다). 목록에 한 행 더해 닫았다. `.note-expand` 는 `font-size: inherit` 이라 유도 집합에 안 들어가고, 그래서 목록에도 없다.
2. **MN-8 이 처음에 안 물었다** — 접는 동작에 셀이 없었다. 사용자 관점의 손상은 *"새 목록이 준 매치 중심 스니펫 대신 옛 전문이 그 행에 남는다"* 이므로, 펼친 뒤 검색을 제출해 스니펫이 이기는지 단정하는 셀로 닫았다. 세션 21 의 MN-8·MN-10 과 같은 처방(무잠금을 드러내면 셀을 새로 넣고 재실패 확인)이다.

### Decisions

- **"더 보기"는 두 화면 모두에 둔다** — 위 1절. 드로어에서 행 제목(선택)과 "더 보기"(펼침)는 다른 일이고, 그 구분 자체를 셀이 잠근다.
- **펼침은 한 번에 한 행이다.** 여럿을 동시에 펼치게 하면 상태가 맵이 되고 목록 갱신마다 정리 규칙이 하나 늘어난다 — 지금 요구에 없다.
- **접기 버튼은 조회 중 `불러오는 중…` 으로 잠근다.** 응답 전 접으면 뒤늦게 도착한 전문이 접힌 행에 붙는다.
- 세션 21 의 *"목록 행에 `updated_at` 을 싣지 않았다 … 오너 판단 자리라 적어 둔다"* 는 이 세션으로 닫혔다.

### Verification

- 신규·기존 focused: `src/notes/` **36 passed**(페이지 12 · 패널 19 · 드로어 5).
- 스타일 가드 3파일 16 passed(`typeScale`·`buttonAppearance`·`pageLayout`).
- `tsc --noEmit` 0.
- 프론트 전수 **451 → 457 passed / 38 files**(캡처 실행 `EXIT=0`, 38파일 전건).
- **★ 그 앞 실행 하나가 1실패를 보고했는데 정체를 못 남겼다** — `tail` 파이프에 실패 블록이 잘렸다(운영 실수). 같은 트리를 전체 출력 캡처로 **두 번 연속** 다시 돌려 둘 다 **457/457 초록**(`EXIT=0`)이고, 파일을 읽는 가드 10개(`typeScale`·`buttonAppearance`·`pageLayout`·`designTokens`·`navigationLinks`·`disabledState`·`scratchPadCss`·`adsense`·`productName`·`chartColors`)도 따로 돌려 35 passed 다. 그 실행 창(09:30~09:47)에 SoT·CHANGELOG·HANDOFF·README 를 편집하고 있었지만 **그 가드들이 저장소 문서를 읽지 않는다는 것은 확인했으므로 원인으로 지목하지 않는다**. ~~머신 부하가 극단적이었다(같은 실행 `environment 1778s`)~~ — **이 해석은 2026-09-07 독립 검증 F7 이 반증했다**: 실패 실행의 `environment 1778s` vs 검증자 **초록** 실행의 **1712s**, 겨우 4% 차이라 그 실행이 특별히 무거웠다는 근거가 없다. 부하를 원인으로 적은 것은 **측정 없이 고른 설명**이었다(같은 축의 다른 실행과 대조했어야 했다). 남는 가설은 **재현율이 낮은 타이밍 플레이크** 하나뿐이다. **정체 미상으로 남긴다** — 다음 전수에서 재현되면 그때 잡는다. **★ 2026-09-07 세션 38 추가**: 같은 모양(전수에서만 1실패, 단독은 초록)이 다시 났고 **그때는 정체를 잡았다** — `me/PersonalHubPage.test.tsx > gives the remaining request budget a home`(전수에서 **5739ms**, 단독 12/12 초록). **이 셀이 그때의 미상 1실패의 유력한 후보다.** 확정은 아니다(그때 블록이 잘려 이름을 못 남겼다). 덧붙여 검증자가 별건 함정 하나를 실측했다: **vitest 를 저장소 루트에서 돌리면** jsdom 환경이 없어 `ReferenceError: document is not defined` 로 **전멸**한다(2회 재현). 실패 모양이 "1개 실패"와 달라 그 실행의 정체는 아니지만, 전수 실행의 **cwd 도 변인**이다 — `frontend/` 안에서 돌린다.
- 백엔드는 돌리지 않았다 — 이 슬라이스가 `services/`·`tests/`·`schemas/` 를 한 바이트도 건드리지 않았다(`git show --stat`).

### Next steps

- 육안 확인 누적 목록에 두 자리를 더한다(좁은 드로어에서 꼬리줄 줄바꿈 · 긴 전문 펼침의 스크롤).
- 독립 검증 대기.

---

## 세션 25 — 독립 검증 하드닝 H1~H3 폐쇄 (SoT v1.8.39)

독립 검증([`scene_note_follow_up.md`](../../verifications/2026-09-07/scene_note_follow_up.md)) 판정 **합격**, 차단 0. 비차단 하드닝 셋과 기록 정정 하나가 이 세션의 범위다. **검증 기록 자체는 고치지 않는다** — 검증자 소유이고, 선례(Slice 3·4 검증 기록)도 폐쇄 세션이 손대지 않았다.

### 1. H1 — 진짜 결함이었다(셋 중 유일하게 동작이 틀렸다)

H2·H3 는 잠금 공백이지만 **H1 은 코드가 틀린 자리**다. 펼침은 행마다 따로 나가는 요청인데 `expand()` 가 응답의 **주인을 확인하지 않았다**:

- A 를 펼치는 중에 B 를 누르면, A 의 늦은 **성공**이 `expandedBody` 에 A 본문을 싣는데 펼친 행은 B 다 → **B 행이 A 본문을 말한다**(B 응답이 오면 자기 교정되지만 그 사이가 거짓이다).
- A 의 늦은 **실패**는 더 나쁘다 — `setExpandedId(null)` 로 **열려 있던 B 를 접고** A 의 오류를 배너로 띄운다. 사용자는 자기가 떠난 행 때문에 지금 보던 것을 잃는다.

처방은 기다리는 대상을 `useRef` 로 들고 응답마다 대조하는 것이다. **`expandedId` 상태로는 판정할 수 없다** — handler 가 닫아 둔 옛 값이라 늘 자기 자신과 같다. 패널의 `let active = true` 정리와 같은 계열이고, 접기·목록 갱신도 그 ref 를 비운다.

### 2. ★ H1 셀이 처음에 새고 있었다 — 변이가 아니었으면 못 봤다

늦은 실패 셀이 `resolve` **직후에** 단정하고 있었다. 상태 갱신이 아직 안 붙은 시점이라 **가드를 걷어낸 변이(MN-10)가 35셀 전건 초록**으로 통과했다. `act` 로 흘려 보내 닫았다(커밋 `8e415ab`). 늦은 성공 셀은 뒤에 `findAllByRole` 이 있어 **우연히** 물고 있었는데, 같은 이유로 `deliver()` 를 태우고 `getAllByRole` 로 바꿨다 — 우연한 대기에 기대지 않는다.

이것이 이 세션에서 가장 값이 큰 발견이다: **하드닝을 닫으면서 새 셀을 넣으면 그 셀도 변이로 재야 한다.** 안 그러면 "닫았다"는 기록만 남고 잠금은 없다.

### 3. 변이 (커밋 `fdb4c69`·`8e415ab` → 변이 → 원복, 매회 `git status --short` 대조)

| 변이 | diff | 재실패 셀 |
|---|---|---|
| MN-9 늦은 성공 가드 제거 | 성공부 `if (awaiting.current !== target) return;` 삭제 | drops a late expand success … 1 |
| MN-10 늦은 실패 가드 제거 | catch 의 같은 줄 삭제 | **처음엔 0 → 셀 보강 후 1**(drops a late expand failure …) |
| MN-11 로딩 잠금 제거 | 접기 버튼 `disabled={expandedBody === null}` 삭제 | locks the fold control … 1 |
| MN-12 드로어에서만 시각 끄기 | `<time>` 을 `onSelect === undefined` 뒤로 | carries the row metadata into the drawer list too 1 |

MN-12 는 검증자가 예고한 모양 그대로다 — **컴포넌트 공유는 잠금이 아니다**(v1.8.37 B1 과 같은 종).

### 4. 기록 정정 — 부하 해석은 반증됐다

세션 24 가 미확인 1실패에 *"머신 부하가 극단적이었다"* 를 붙였는데, 검증 F7 이 실패 실행 `environment 1778s` vs 초록 실행 **1712s**(4% 차)로 반증했다. **측정 없이 고른 설명**이었다 — 같은 축의 다른 실행과 대조했으면 그 자리에서 무너졌다. 세션 24 의 해당 문장을 취소선으로 정정하고 남는 가설(낮은 재현율의 타이밍 플레이크)만 남겼다. 검증자가 실측한 별건 함정(**루트에서 vitest 실행 시 `document is not defined` 전멸**, 2회 재현)도 같은 자리에 박았다.

### Issues found

1. 위 2절 — 새 셀 자신이 무잠금이었다(MN-10).

### Decisions

- **검증 기록은 고치지 않는다.** 폐쇄는 SoT·work_log 에 남긴다(선례: Slice 3·4 검증 기록은 조건 폐쇄 뒤에도 단일 커밋).
- **`awaiting` 은 ref 다.** 상태로 두면 같은 경쟁이 판정 쪽으로 옮겨 갈 뿐이다.

### Verification

- focused `src/notes/` **40 passed**(페이지 15 · 패널 20 · 드로어 5).
- 프론트 전수 457 → **461 passed / 38 files**(전량 캡처, `EXIT=0`) · `tsc --noEmit` 0.
- 백엔드 무변(`services/`·`tests/`·`schemas/` 0건) — 문서 가드만 별도 확인.

### Next steps

- 배포 대기(프론트 이미지 재빌드 선행) · 육안 확인 누적.

---

## 세션 26 — 서비스 정책 계약 문서 (SoT v1.8.40)

오너 지시(2026-09-05, HANDOFF Next Tasks 9). 지시가 *"검증은 너의 몫이 아니다"* 를 함께 못박아 6차 승격 재검증(2번)은 이 세션 밖이다.

### 1. 지시 안의 긴장을 어떻게 풀었나

지시문이 두 가지를 함께 말한다 — *"**코드가 실제로 시행하는 정책값**을 모아 그 근거를 준다"* 와 *"값을 여기 복사하면 **두 번째 정본**이 된다"*. 값을 안 적으면 첫째를 못 지키고, 적으면 둘째가 일어난다.

**가드로 풀었다.** 문서의 각 행이 자기 정본을 `모듈::상수` 로 가리키고, [`test_service_policy_contract.py`](../../../tests/test_service_policy_contract.py) 가 그 상수를 **실제로 import 해서** 문서에 적힌 수와 대조한다. 그러면 문서는 정본이 아니라 **파생본**이고, 상수를 고치고 문서를 안 고치면 전수가 빨개진다. 선례가 같은 모양이다 — README 절차 표의 SoT 버전을 `test_docs_indexes` 가 SoT 헤더에 못박아 두고 있고, 그 자리는 **가드를 넣기 전에 실제로 두 버전 뒤처져 있었다**(v1.7.89 동안 v1.7.87). 이것은 결정 브리프를 낼 자리가 아니라 **선례가 이미 답한 자리**였다.

### 2. ★ 쓰는 동안 실측 셋이 서술의 전제를 바꿨다

문서의 값은 브리프에서 베끼지 않고 코드에서 다시 쟀다. 세 가지가 다르게 나왔다.

1. **원고는 "나갈 수 있다"가 아니라 이미 나간다.** 지금 배포 조합(`docker-compose.external-embedding.yml`)이 `EMBEDDING_SERVICE_URL` 을 **필수**(`:?`)로 만들고, 임베딩 요청은 본문을 그대로 싣는다(`indexing/embedding.py` — `{"input": text}`). 핸드오프가 *"원고가 외부 LLM API로 나간다는 고지"* 라고 적은 공백은 **LLM 축보다 임베딩 축이 먼저 확정적**이다. 그 주소가 오너 LAN 인지 제3자인지는 저장소 밖 `.env` 가 정하므로 — **약관이 답해야 하는 "제3자에게 가는가"의 답이 배포 설정에 달려 있다**는 것 자체가 결정 대상이다.
2. **보존 기간의 현재 답은 "영원히"다.** `expireAfterSeconds` 는 저장소 전체에 **넷뿐**이고(세션·가입 가드·요청 중복 잠금·quota replay) 전부 운영용이다 — **원고·활동 로그·사용량 원장·감사 기록에는 하나도 없다.** 무기한이 나쁘다는 게 아니라 **선택된 적이 없다**는 것이 문제다.
3. **셀프 계정 삭제 경로가 없다.** `DELETE` operation 넷을 전부 확인했고(원고·프로젝트·스크래치 둘) 사용자 축은 없다. 있는 것은 관리자의 `deactivate_user` 뿐이다(마지막 활성 관리자는 거부).

### 3. 나온 것

- [`docs/service-policy-contract.md`](../../service-policy-contract.md) — §1 가입·계정 · §2 로그인 상태 · §3 요청 한도 · §4 길이 상한 · §5 열람 권한 · §6 삭제 · §7 광고 · **§8 아직 정해지지 않은 셋**. §8 을 문서 안에 남긴 것은 의도다 — **비어 있다는 사실이 약관의 입력**이라 지우면 정보가 사라진다.
- [`tests/test_service_policy_contract.py`](../../../tests/test_service_policy_contract.py) — 3셀/28 subtests.
- [`docs/plans/service-policy-decisions.md`](../../plans/service-policy-decisions.md) — 미결 셋의 결정 브리프(각각 선택지 표 + 추천 + 후속 고려). **추천은 셋 다 "작게"** 다: 고지는 제품 안 한 줄(C) · 보존은 *선택된* 무기한(A) · 계정 삭제는 현행 유지를 명시(A). 이유는 셋 다 같다 — 지금 규모에서 파기 그래프·동의 축·스케줄러를 새로 여는 것이 값보다 비싸다.

### 4. 변이 (커밋 `10a6c26` → 변이 → 원복, 매회 `git status --short` 대조)

| 변이 | diff | 결과 |
|---|---|---|
| MP-1 상수만 고침 | `DEFAULT_DAILY_LIMIT` 20 → 25 | 값 대조 셀 재실패(`하루 요청 한도`) |
| MP-2 문서만 고침 | 세션 유지 기간 7일 → 30일 | 값 대조 셀 재실패(`세션 유지 기간`) |
| MP-3 포인터 파괴 | `DEFAULT_SESSION_TTL` → `SESSION_TTL_DEFAULT` | 포인터 생존 셀 + 값 대조 셀 2 재실패 |
| MP-4 **over-strict** | 행 라벨만 `세션 유지 기간` → `로그인 유지 기간` | **통과(의도)** — 재는 것은 문장이 아니라 셀 안의 수다 |

### Issues found

1. **가드가 내 문서의 오류 둘을 첫 실행에서 잡았다** — ① `api/env.py::DRAFT_RAW_TEXT_MAX_CHARS` 는 틀린 경로다(실제는 `env.py`, 앱 루트). 앞선 `ls` 출력 두 개를 한 덩어리로 읽어 생긴 오독이다. ② `가입 요청 빈도 상한` 을 *"3600초당 5건"* 으로 적었는데 포인터 순서는 `MAX_REQUESTS`·`WINDOW_SECONDS` 라 수 순서가 뒤집혀 있었다 → *"5건 / 3600초 창"* 으로 고쳤다. **문서를 손으로 검수했으면 둘 다 통과했을 것이다.**
2. **인덱스 등재 가드가 신규 브리프를 잡았다**(예상된 자리 — 메모리의 등재 체크리스트). `docs/plans/README.md` 행 + 카운트 주장 **3곳**(plans README 1 · 최상위 README 2 — 절차 표 셀과 문서 표 셀이 **따로** 있다) 갱신.

### Decisions

- **문서에 값을 적되 가드로 잠근다**(위 1절). 대안은 값을 안 적는 것인데, 그러면 *"한 장에서 답한다"* 는 목적 자체가 무너진다.
- **§8(빈 칸)을 문서 안에 남긴다.** 브리프로 옮기고 문서에서 지우면, 문서만 읽는 사람은 *"정책이 다 있다"* 로 읽는다.
- **미결 셋은 추측으로 채우지 않았다.** 보존 기간·계정 삭제·고지 문구는 오너가 고를 자리다(CLAUDE.md §1 결정 브리프).

### Verification

- 신규 가드 **3 passed / 28 subtests**; 양방향 변이 4종(위 표).
- 문서 인덱스 가드 `test_docs_indexes` **15 passed / 299 subtests**, `test_repo_hygiene` 포함 24 passed / 880 subtests.
- 프런트 무변(이 세션은 `frontend/` 를 건드리지 않았다) — 전수 미실행이 아니라 **범위 밖**이다.

### Next steps

- **오너 결정 셋** — 브리프에서 고르면 §8 의 행이 §1~7 로 올라온다.
- 광고(AdSense) 화면별 제어는 Phase S-2 와 같은 축이라 그 슬라이스에서 함께 연다.

---

## 세션 27 — 정책 미결 셋, 오너 결정 (구현 미착수)

세션 26 이 낸 브리프에 오너가 답했다. **구현은 하지 않았다** — 이 세션은 결정 기록이고, 셋 중 둘은 코드 작업이 새로 생긴다.

### User Decisions and Rationale

| 결정 | 고른 것 | 오너의 근거 · 내 관측 |
|---|---|---|
| **1. 고지** | **B + 가입 시 약관 확인**(브리프 B 에 동의 게이트를 더한 조합). 동의 **시각 + 약관 버전 저장**. 기존 계정 **소급 동의 없음** | 오너: *"LLM 을 사용하는 순간 자신의 데이터가 외부로 나간다는 걸 인지해야지."* 내 추천은 C(제품 안 상시 문구)였는데 **오너가 더 강한 자리를 골랐다** — 고지를 화면 구석이 아니라 **들어오는 문**에 둔다. C 보다 비싸지만(필드 2개 + 가입 검증) *"인지했는가"* 에 답할 수 있는 것은 이쪽뿐이다 |
| **2. 보존** | **A — 무기한**, 삭제 요청 전까지 저장 | 추천대로. 코드는 안 바뀐다 — 바뀐 것은 무기한이 **선택된** 값이 됐다는 사실이다 |
| **3. 계정 탈퇴** | **셀프 탈퇴 → 유예 기간 뒤 파기.** 문서는 지우고 **원장은 남기며 사용자명 한 값 보존** | 내 추천은 A(현행 유지 명시)였는데 **오너가 C 를 골랐다.** 근거가 강하다 — 아래 |

### ★ 오너가 옳았고 내 조사가 얕았다

오너 질문: *"이건 논의 되었을텐데? 계정 탈퇴되는 순간 문서는 다 삭제되고 로그에만 남겨둔다고?"*

**맞았다 — 다만 프로젝트 축이다.** 내가 세션 26 에서 *"셀프 삭제 경로가 없다"* 고 적은 것은 **코드만 보고 내린 결론**이었고, 그래서 절반만 맞았다. 실제로는:

- **D8-6**(2026-08-02)·**08-2c**(2026-08-05)가 *"원고·기억·색인·감사는 지우고 **사용량 원장 행은 남기며**, 원장을 사람이 읽을 수 있도록 **프로젝트 이름 한 값이 파기를 살아남는다**"* 를 확정했다. 오너가 말한 모양이 **정확히 이 계약**이다.
- 같은 D8-6 의 `Deferred / out of scope` 에 **"사용자 자신의 hard delete"** 가 한 줄로 있다 — **계정 축만 미뤄 둔 것**이지 원칙이 없던 게 아니다.

**교훈:** *"없다"* 를 코드 부재로 결론짓지 말 것. 이 저장소는 **결정이 코드보다 먼저 있고 유예 목록에 적혀 있는** 경우가 많다 — `Deferred / out of scope` 절을 먼저 읽었으면 세션 26 에서 잡았다. 브리프의 선택지 표도 *"새로 정한다"* 가 아니라 *"유예를 닫는다"* 로 썼어야 했다.

그리고 그 확장은 프로젝트에서 이미 푼 문제를 사용자 축에서 반복시킨다 — 계정이 사라지면 원장 행의 소유자가 고아가 된다. 그래서 **사용자명 한 값 보존**을 함께 물었고 오너가 골랐다(`project_name_history` 와 같은 모양).

### 남은 것 둘 — 값 하나, 선행 하나

1. **유예 기간 `N` 일이 비어 있다.** *"유예를 둔다"* 까지만 정해졌다. **구현자가 임의로 고르면 안 되는 계약 리터럴**이라 브리프·정책 문서·HANDOFF 세 곳에 그렇게 적었다.
2. **약관 본문이 아직 없다.** 동의 게이트는 동의받을 문서가 있어야 완성된다 — 약관·방침은 오너 정본이고 HANDOFF 오너 병목 표에 이미 있다. 순서는 **약관 본문 → 버전 체계 → 동의 게이트**다. 코드 축(필드·확인 단계)은 먼저 지을 수 있으나 버전 문자열이 약관과 함께 온다.

### 기록

- 브리프 상태 `Proposed` → `Resolved`, 오너 결정 표와 근거를 머리에 얹었다.
- 정책 문서 §8 을 *"아직 정해지지 않은 것"* → **"정해졌지만 아직 시행되지 않은 것"** 으로 바꿨다. **§1~7 과 지위가 다르다는 것을 절 머리와 문서 머리에 둘 다 적었다** — 섞이면 문서가 시행되지 않는 약속을 시행 중인 것처럼 말한다. 보존(결정 2)만은 *"이미 그렇다"* 로 적었다(코드가 안 바뀐다).

### Verification

- 정책 가드 3 passed / 28 subtests(§8 은 정본 포인터가 없어 가드 대상이 아니다 — 시행 전이므로 상수가 없는 것이 옳다).
- 문서 인덱스 가드 재실행.

### Next steps

- **구현 슬라이스 둘이 새로 생겼다**(HANDOFF): 가입 동의 게이트(약관 본문 선행) · 셀프 탈퇴 + 유예 파기(유예 기간 N 오너 확인 선행).

---

## 세션 28 — 유예 30일 확정 + 약관·방침 초안 (SoT v1.8.42)

오너 지시: 유예는 **30일**, 약관 본문은 *"일단 대충 써놔줘. 첫삽 뜬다 생각하고... 지금 포트폴리오용이야. 중요한건 우리가 정해놓은걸 제대로 담았는가?"*

### 1. 합격 기준을 그대로 받았다

*"대충 써달라"* 와 *"제대로 담았는가"* 는 충돌하지 않는다 — **문장 다듬기는 대충, 커버리지는 정확히**라는 뜻으로 읽었다. 그래서 문체에 시간을 쓰지 않고, **정책 문서 §1~§8 을 한 줄씩 짚으며** 썼다.

그 기준을 [`docs/legal/README.md`](../../legal/README.md) 의 **대조표**로 만들고 가드로 잠갔다. 정책 문서의 표 축 **17개**가 각각 어느 조항으로 갔는지 적고, 축이 늘었는데 약관이 안 따라가면 전수가 빨개진다. **조항 내용이 맞는지는 재지 않는다**(문장이 정책을 옳게 옮겼는지는 사람이 읽는다) — 가드 docstring 에 그 한계를 명시했다.

표에 안 잡히는 **문장형 정책 18개**(승인제 · 1회용 비밀번호 · 통합 401 · 정지 · 목록 조회는 접근 아님 · 이름 한 값 보존 …)도 같은 대조표의 둘째 표에 손으로 넣었다.

### 2. 초안이 일부러 안 한 것

- **`[운영자]`·`[문의 연락처]`·`[시행일]`·`[추론 서비스 사업자]` 를 지어내지 않았다.** 없는 법인명·주소를 채우면 그 문서 자체가 허위가 된다. 마지막 값은 **배포 설정(`.env`)이 정하므로 코드에서 유도할 수도 없다** — 방침 제4조 3항이 그 사실을 문장으로 적었다.
- **"미시행" 표식을 문서 머리와 가드 양쪽에** 뒀다. 초안이 시행 중인 문서로 읽히면 안 된다.
- **§8(정해졌으나 미시행) 넷은 "시행될 약속"으로 적되 대조표에 `시행 전제` 열을 뒀다** — 가입 동의 게이트와 셀프 탈퇴가 구현되기 전에는 그 조항이 참이 아니다.

### 3. 초안이 정책 문서보다 더 말한 것 (기록해 둔다)

법적 문서에 필요하지만 코드 정책에 대응이 없는 조항들이라, **어디서 왔는지**를 대조표 마지막 표에 남겼다. 그중 하나는 실제 공백을 메운다 — **정지(`suspended`) 상태가 코드에 있는데 "무엇을 하면 정지되는지"가 어디에도 없었다**(약관 제10·11조가 그 자리다).

### 4. 변이 (커밋 `cb704eb`·`bde03b5` → 변이 → 원복, 매회 `git status --short` 대조)

| 변이 | diff | 재실패 |
|---|---|---|
| ML-1 대조표 행 이름 변경 | `계정 탈퇴` → `계정 탈퇴(미기재)` | **처음엔 0 → 가드 보강 후 1** |
| ML-2 정책 축 추가·대조표 미갱신 | 정책 문서에 행 하나 추가 | 커버리지 셀 1(+값 대조 셀 1) |
| ML-3 미시행 표식 제거 | 약관 상태를 `시행 중` 으로 | 표식 셀 1 |
| ML-4 **over-strict** | 대조표의 조항 번호만 `제99조` 로 | **통과(의도)** — 재는 것은 축 이름이지 조항 번호가 아니다 |

### Issues found

1. **★ ML-1 이 처음에 안 물었다 — 가드가 부분 문자열을 보고 있었다.** `axis not in mapping_text` 라 행 이름을 `계정 탈퇴(미기재)` 로 바꿔도 `계정 탈퇴` 가 부분 문자열이라 초록이었다. **본문 어디든 그 글자가 있으면 통과**하는 셈이라, 대조표가 아니라 우연을 재고 있었다. 표의 **첫 칸을 정확히 대조**하도록 고쳤다(커밋 `bde03b5`). 세션 25 의 교훈이 그대로 반복됐다 — **닫으면서 넣은 가드도 변이로 재야 한다.**

### Decisions

- **유예 30일을 리터럴로 세 곳에 박았다**(브리프·정책 문서 §8·HANDOFF 11번). 구현 때 상수 한 곳에 두고 정책 문서가 그것을 가리키게 한다 — §1~7 과 같은 방식이라 그때 가드가 자동으로 잠근다.
- **약관은 `docs/legal/` 에 뒀다.** 제품이 서빙하는 자리가 아니라 오너가 검토할 초안이라 `docs/` 안이 맞다. 가드가 등재를 요구하지는 않지만 **최상위 README 문서 지도와 `docs/README.md` 에 연결**했다 — 연결 안 된 문서는 없는 문서다.

### Verification

- 정책 가드 **5 passed / 30 subtests**(값 대조 3 + 커버리지 2).
- 문서 인덱스·위생 가드 24 passed / 883 subtests.
- 백엔드 코드 무변 · 프런트 무변.

### Next steps

- 오너 검토 — 특히 **약관 제10조(금지 행위)** 는 코드에 대응이 없어 순전히 초안 판단이다.
- 구현 슬라이스 둘(HANDOFF 10·11번)이 끝나야 §8 조항이 참이 된다.

---

## 세션 29 — `[문의 연락처]` 의 자리 확정 + ★ 내 인계 요약이 불완전했다

### 오너 결정

- **`[문의 연락처]` 는 랜딩 페이지 푸터에 들어간다.** 약관·방침 초안의 대괄호 넷 중 이것 하나는 **자리가 정해졌고**, 값은 **랜딩 기획 때** 채운다. 초안 둘과 대조표에 그렇게 적었고, HANDOFF 에 **12번(랜딩 페이지 기획)** 을 세웠다.

### ★ 사고 — "남은 작업" 을 물었을 때 내가 절반만 셌다

오너 지적: *"랜딩페이지 작업하는거 진행 안되었는데 왜 남은거 없다고 했어!"*

**맞다.** 앞선 답에서 나는 `## Next Tasks` 절의 0~9번만 열거하고, **그 위에 있는 `## 열린 것 — 부채 · 결정 대기` 의 표 둘을 통째로 빠뜨렸다.**

| 빠뜨린 것 | 크기 |
|---|---|
| **⚠️ 오너 결정이 있어야 움직이는 것** 표 | **9행** — 랜딩 기획이 **첫 줄**이었다 |
| **🔧 미수리 — 알고 있고 아직 안 고친 것** 표 | **13행** |

**원인은 검색 방식이다.** `grep -n '^[0-9]\.'` 로 번호 매긴 항목만 뽑았고, 그것이 곧 "남은 일 전부"라고 가정했다. HANDOFF 는 **남은 일을 세 곳에 나눠 싣는다** — 번호 목록(착수 순서), 오너 결정 표(막힌 것), 미수리 표(아는 결함). 번호 목록만 보면 **오너 병목이 통째로 안 보인다.**

**더 나쁜 것은 내가 그 사실을 말하지 않은 것이다.** *"핸드오프 기준으로 9건"* 이라고 단정했지 *"Next Tasks 절 기준"* 이라고 범위를 밝히지 않았다. 범위를 밝혔으면 오너가 그 자리에서 고칠 수 있었다.

**교훈:** 인계 문서에서 "남은 것"을 셀 때는 **절 목록을 먼저 뽑고**(`grep '^## '`) 각 절이 무엇을 담는지 확인한 뒤 센다. 그리고 **집계의 범위를 답에 함께 적는다.**

### 참고 — 그때 함께 보고했어야 할 것

미수리 표 13행 중 **하나는 오늘 내 작업으로 더 틀어졌다**: `docs/README.md` 의 브리프 수 주장이 **100개**로 적혀 있는데 실제는 **105개**다(원래 104 로도 이미 틀렸고, 내가 브리프를 더해 105 가 됐다). **가드가 안 잡는 자리다** — `test_docs_indexes` 의 카운트 대조는 최상위 `README.md` 와 `docs/plans/README.md` 만 본다. 고치지 않고 보고만 한다(범위 밖 · 오너 판단).

### Verification

- 정책 가드 5 passed / 30 subtests.

---

## 세션 30 — 계정 탈퇴 구현 계획 (데몬·취소 포함)

오너 지적: *"탈퇴 요청에서 탈퇴 데몬이랑 취소하는 기능도 안되어있잖아."* 맞다 — 세션 27 이 오너 결정을 받아 적으면서 HANDOFF 11번을 *"셀프 탈퇴 + 30일 유예 파기 구현"* 한 줄로 뒀는데, **탈퇴는 버튼 하나가 아니다.** 요청 자리·유예 중 상태·**30일 뒤 실제로 지우는 데몬**·**취소**가 각각 있어야 성립하고, 그 셋이 어디에도 안 적혀 있었다.

[`plans/account-withdrawal-implementation-phases.md`](../../plans/account-withdrawal-implementation-phases.md) 로 정리했다(Slice 0~5 + 오너 결정 D1~D6).

### ★ 착수 전 실측 셋 — 이것이 계획의 값이다

계획을 쓰기 전에 파기 경로를 직접 읽었고, 셋이 설계를 좌우한다.

1. **파기 본체는 한 벌뿐이다.** `routers/admin.py::execute_project_purge` 를 admin purge 와 소유자 purge 가 공유한다(D8-6d: *"두 벌이 되면 어느 한쪽만 새 서비스를 받아 조용한 고아를 만든다"*). 탈퇴 파기가 사용자 축 그래프를 새로 쓰면 **세 벌째**다. **다만 그 함수는 `HTTPException` 을 던져** 데몬에서 그대로는 못 쓴다 — 경계 하나가 필요하다.
2. **파기 재시도는 멱등이 아니다.** core_sot 을 **먼저** 지우므로 derived 단계에서 실패하면 재시도가 **404 로 끝나 derived 에 도달하지 못한다**(남는 것이 무해하지 않다 — `llm_call_audits` 의 프롬프트 본문, scratch 의 원고 후보). → **데몬을 "실패하면 재시도" 로 짜면 안 된다.** 부분 파기를 표시하고 멈춰야 하며 수습은 `purge_reconciler` 다.
3. **★ 사용량 원장이 `user_id` 를 쓰는데 우리는 원장을 남기기로 했다.** 이 저장소에서 **파기 대상의 표식은 필드 이름**이다 — `purge_reconciler` 가 `project_id` 필드를 가진 컬렉션을 발견해 고아를 쓸어 가고, 그래서 활동 로그는 반드시 `project_id` 여야 하며(`activity/log.py:31`) **남아야 하는 원장은 `target_project_id` 로 개명**해 그 쓸이를 피했다(8.2 L1=B). **그런데 원장의 사용자 축은 아직 `user_id` 다**(`quota/ledger.py:54`). 사용자 축 reconciler 를 `user_id` 기준으로 만드는 순간 **원장이 쓸려 가고 오너 결정과 정면으로 어긋난다.** 지금은 그 reconciler 가 없어 잠재적이지만 **이 계획이 그 문을 연다** → D4.

### 계획의 모양

- **Slice 0 상태 축**(30일을 상수 한 곳에 — 정책 문서 §6 이 그것을 가리키면 값 대조 가드가 자동으로 잠근다) → **1 요청·취소 API**(마지막 활성 관리자 거부는 `LastActiveAdmin` 선례 재사용) → **2 유예 중 접근 규칙** → **3 파기 데몬**(`index_sync_worker` 의 `--loop --interval` 선례) → **4 화면** → **5 문서 승격**(§8 → §6, 약관 제8조가 그때 참이 된다).
- **오너 결정 D1~D6**: 유예 중 무엇이 되나 · 데몬을 새 워커로 둘까 · 실패 처리 · **원장 `user_id` 개명** · 취소 시점 · 마지막 관리자. 각각 추천을 달았고 **D3 은 선택지 ⓑ(자동 재시도)가 실측 2 때문에 애초에 동작하지 않는다**는 것까지 적었다.

### Issues found

1. **세션 27 의 기록이 얇았다.** 오너 결정을 *"셀프 탈퇴 + 유예 뒤 파기"* 로 받아 적고 끝냈는데, **결정을 받아 적는 것과 그 결정이 무엇을 필요로 하는지 적는 것은 다른 일**이다. 결정 직후에 *"이걸 하려면 무엇이 있어야 하나"* 를 한 번 훑었으면 데몬·취소가 그때 나왔다.
2. 계획 문서는 브리프(`*-decisions.md`)가 아니라 **전체 수만 오른다**(126) — 브리프 수(105)는 그대로다. 인덱스 카운트 가드가 둘을 따로 세므로 처음에 106/106 으로 적었다가 잡혔다.

### Verification

- 문서 인덱스·위생 가드 24 passed / 884 subtests. 코드 무변.

---

## 세션 31 — 계정 탈퇴 D1~D6 확정 + D4 선행 구현 (SoT v1.8.43)

### User Decisions and Rationale

| # | 오너 결정 | 메모 |
|---|---|---|
| **D1** | **ⓒ — 유예 중에는 로그인·조회·취소만**(쓰기·유료 차단) | **★ 내 선택지 문구가 헷갈렸다는 지적을 받았다.** 오너: *"d1이 탈퇴 신청 후 유예기간이라면 C, 유예기간중 취소를 눌러 일반 상태로 된것을 뜻하는 유예라면 A (구현자 추천에 네가 햇갈리게 써놔서 알려줌)."* 내 추천 ⓐ 의 근거가 *"취소한 사람이 원래대로 쓸 수 있어야"* 였는데 그것은 **취소 이후** 이야기다 — **유예 중**과 **취소 후**를 한 선택지에 뭉쳐 놓은 것이 오독의 원인이다. 바로잡은 계약: 유예 중 = 로그인 O·조회 O·쓰기 X·유료 X·취소 O / 취소 후 = 계정이 다시 활성이라 **제한이 전부 사라진다**(별도 규칙이 아니다) |
| **D2** | ⓐ 새 워커 컨테이너 | 추천대로 |
| **D3** | ⓐ + **관리자 화면에서 재실행 통제** | 오너 추가. **★ 그 버튼은 파기 재호출이 아니다** — `execute_project_purge` 재호출은 core_sot 이 비어 **404 로 끝나고 derived 에 도달하지 못한다**. 부를 것은 **reconciler 경로**이고, 대상 컬렉션을 **DB 에서 발견**하는 성질을 그대로 가져와야 한다 |
| **D4** | ⓐ `target_user_id` 개명 | **이 세션에서 구현했다**(아래) |
| **D5** | ⓐ 파기 실행 전까지 언제나 취소 | 추천대로 |
| **D6** | ⓐ 마지막 활성 관리자 거부 | 추천대로 |

### D4 구현 — 왜 이것만 먼저인가

나머지 결정은 **아직 없는 기능**이라 늦어도 손해가 없다. D4 만 **지금 이미 위험**하다: 계정 탈퇴 Slice 3 이 사용자 축 reconciler 를 만드는 날, 원장이 `user_id` 를 들고 있으면 **통째로 사라진다**(오너 결정 *"원장은 남긴다"* 와 정면 충돌).

**범위를 저장면으로 좁혔다.**

- **옮긴 것**: 저장 필드(`UsageEntry`·`AdjustmentEntry`) · 인덱스 키 3종 · 질의 필터.
- **안 옮긴 것 ①** 서비스 층의 **메서드 인자 이름**(`has_usage(user_id, …)`) — 저장되지 않는 값이라 개명해도 **안전이 늘지 않고 변경 면만 넓어진다.**
- **안 옮긴 것 ②** `admin_user_id` — **행위자**이지 원장의 소유 축이 아니고, 이름이 달라 정확 일치 판정에 안 걸린다(활동 로그 `actor_user_id` 선례).
- **안 옮긴 것 ③ — 이게 중요하다.** 세션·색인·생성 job·회원 한도 정책도 `user_id` 를 쓰지만 **개명하지 않았고 그것이 옳다**: 전부 **탈퇴 시 지워져야 하는 계정 데이터**라 쓸이 대상인 것이 맞다. 활동 로그가 `project_id` 를 **일부러** 유지해 파기에 쓸리는 것과 같은 구조다 — **필드 이름은 파기의 opt-in/opt-out 스위치**이고 원장만 opt-out 이다. 이 관점을 계획 문서와 SoT 에 함께 적었다.

**마이그레이션**(`scripts/migrate_ledger_user_axis.py`): 멱등 · `--dry-run` · **옛 인덱스 3개 제거**(키가 바뀌어 같은 이름으로 재생성할 수 없다. 새 인덱스는 어댑터가 기동 때 만들므로 생성 지점을 두 벌로 두지 않는다). **배포 전에 돌려야 한다** — 안 돌리면 옛 행이 새 질의에 안 잡혀 **사용량이 0 으로 보이고**, 나중에 그 행들만 쓸려 간다.

### 변이 (커밋 `6eeedf5` → 변이 → 원복, 매회 `git status --short` 대조)

| 변이 | diff | 재실패 셀 |
|---|---|---|
| MD-1 저장 키 되돌리기 | `_usage_doc` 의 키를 `user_id` 로 | 7(사용자 축 계약 · 키 집합 핀 · 왕복 · 조회 등) |
| MD-2 옛 인덱스 미제거 | `for name in _STALE_INDEXES` → `for name in ()` | 인덱스 제거 셀 1 |
| MD-3 **over-strict** 행위자 오개명 | `$rename` 대상을 `admin_user_id` 로 | 4(행위자 보존 셀 포함) |
| MD-4 dry-run 무력화 | `if dry_run or pending == 0` → `if pending == 0` | dry-run 셀 1 |

### Issues found

1. **선택지 문구가 오너를 헷갈리게 했다**(D1). 원인은 선택지 자체가 아니라 **추천의 근거가 다른 구간을 가리킨 것**이다. 구간이 둘인 결정("유예 중" vs "취소 후")은 **선택지에서 구간을 먼저 못박아야** 한다. 표에 그 사실과 바로잡은 계약을 함께 남겼다.
2. `pytest --timeout` 은 이 저장소에 없다(플러그인 미설치). 전수 첫 시도가 그 인자로 죽었다 — 재실행했다.

### Verification

- 백엔드 전수 **2903 passed / 1 skipped / 3839 subtests · EXIT=0**(2327초). 종전 기준선 2892/1/3798 → **+11 passed / +41 subtests**(신규 6셀 + subtest).
- ★ **소요 2327초는 종전 기록 333초와 비교하지 말 것** — 머신-로컬 값이고 이 세션 내내 다른 작업이 함께 돌았다.
- 변이 4종 전부 기명 셀 재실패(위 표).

### Next steps

- **배포 시 마이그레이션 선행**(HANDOFF 5번에 명시).
- 계정 탈퇴 Slice 0~5 착수(D1~D6 확정 완료).

---

## 세션 32 — D4 검증 하드닝 H1·H2 폐쇄 (SoT v1.8.44)

독립 검증([`account_withdrawal_d4_ledger_axis.md`](../../verifications/2026-09-07/account_withdrawal_d4_ledger_axis.md)) 판정 **합격 · 차단 0**. 비차단 둘 중 **H1 은 실제 결함이었다.**

### 1. H1 — 내 마이그레이션에 크래시 창이 있었다

`update_many`(문서 개명)와 `drop_index` 사이에 프로세스가 죽으면, 재실행이 `pending == 0` **조기 반환**에 걸려 **옛 인덱스를 영원히 안 지운다.** 그 상태로 앱을 기동하면 어댑터의 같은-이름-다른-키 `create_index` 를 MongoDB 가 **code=86(IndexKeySpecsConflict)** 으로 거부해 **기동이 깨진다.**

**실패 모양이 v1.8.43 이 경고한 것보다 나쁘다.** 나는 *"안 돌리면 사용량이 0 으로 보인다"* 라고 적었는데, 실제로는 **앱이 뜨지 않는다.** 검증자가 code=86 을 실측하면서 *"마이그레이션 선행은 권고가 아니라 기동의 필요조건"* 임이 서버 동작 수준에서 확정됐다.

**처방은 게이트를 없애는 것이다.** 지울 대상을 `pending > 0` 이 아니라 **실제 인덱스 키**로 고른다 — `list_indexes` 에서 키에 `user_id` 가 든 것만. 이렇게 하면 ① 크래시 창이 닫히고 ② **어댑터가 새 키로 다시 만든 동명 인덱스는 대상이 아니게 된다**(이름으로 지우면 그것까지 지운다). 목록을 손으로 들지 않는 성질은 `purge_reconciler` 가 컬렉션을 DB 에서 발견하는 것과 같다.

겸하여 `except Exception: continue` 를 걷었다. 이름이 `list_indexes` 에서 오므로 존재가 보장되고, 남는 실패는 **권한 오류 같은 진짜 오류**라 삼키면 안 된다.

### 2. H2 — 멱등 셀에 다리가 하나 없었다

`test_running_it_twice_changes_nothing_the_second_time` 이 문서 무변만 단정해서, 재실행이 drop 을 *시도*해도(그리고 예외가 삼켜져도) 통과했다. `second["dropped_indexes"] == []` 한 줄로 닫았다.

### 3. 실 mongo 로 다섯 단계를 재현했다

가짜 컬렉션만으로는 code=86 을 못 본다. 27020 rs-test 에서 직접 쟀고 **스크립트를 기록 옆에 커밋했다**([`repro_migration_crash_window.py`](../../verifications/2026-09-07/repro_migration_crash_window.py)) — `/tmp` 에 두면 재부팅 한 번에 재현 불가다(저장소 규범).

| 단계 | 실측 |
|---|---|
| 1 | 같은 이름·다른 키 `create_index` → **code=86 IndexKeySpecsConflict** 거부 |
| 2 | 크래시 창 재현 — 옛 필드 문서 **0건**인데 옛 인덱스 **2개 잔존** |
| 3 | 재실행이 그 둘을 지운다(**종전 구현은 못 지웠다**) |
| 4 | 그 뒤 어댑터의 새 키 인덱스 생성 **성공** — 기동 복구 |
| 5 | 재실행 멱등, `dropped=[]` — 새 키 인덱스를 안 건드린다 |

### 4. 변이 (커밋 `285205b` → 변이 → 원복, 매회 `git status --short` 대조)

| 변이 | diff | 재실패 셀 |
|---|---|---|
| MH-1 조기 반환 복원 | `if dry_run:` → `if dry_run or pending == 0:` | 크래시 창 복구 셀 1 |
| MH-2 **over-strict** 이름으로 지우기 | 키 판정 → `index["name"] != "_id_"` | 새 키 인덱스 보존 셀 1 |
| MH-3 drop 목록 비우기 | `report["dropped_indexes"] = []` | 2(크래시 창 · 인덱스 제거) |

### Issues found

1. **내가 실패 모양을 과소평가했다.** v1.8.43 에 *"사용량이 0 으로 보인다"* 라고 적었는데 실제 최악은 **기동 불가**다. 원인은 마이그레이션을 **문서 관점**으로만 보고 인덱스 제약(같은 이름·다른 키)을 서버에 물어보지 않은 것이다 — **실 mongo 로 한 번 재 봤으면 그때 나왔다.**

### Verification

- 마이그레이션 셀 5→**7 passed**, quota 계열 합계 97 passed / 11 subtests.
- 실 mongo 5단계 재현(위 표) · 변이 3종 기명 재실패.
- 문서 인덱스 가드 15 passed / 300 subtests.

### Next steps

- 배포 시 마이그레이션 선행(이제 **크래시해도 다시 돌리면 된다**) + 프론트 재빌드.
- 계정 탈퇴 Slice 0~5 착수 가능.

---

## 세션 33 — 미수리 표의 문서 부패 5건 폐쇄 (하루 마감)

오너: *"가볍게 가자... 오늘은 정리하고 끝내자. 다음 작업자를 위해 메모 잘 남겨주고."*

### 다섯 다 "거짓 단언이 아니라 갱신 미추종"이었다

표에 적힌 값을 그대로 믿지 않고 **다시 쟀다** — 그리고 셋은 표 자신이 낡아 있었다.

| 항목 | 표의 주장 | 실측 | 처리 |
|---|---|---|---|
| SoT "현재 구현 상태" 자기모순 | 표가 구현·미구현을 함께 말한다 | *"Phase 2~6 UI 미구현"* 인데 **같은 문서의 변경 이력**이 Review Inbox(v1.6.67)·정체성 그룹(v1.8.34)·장면 메모 화면(v1.8.36)을 구현으로 기록 중 | **화면별 정본은 이 표가 아니라 변경 이력**임을 행에 명시. 화면 목록을 두 벌로 만들지 않는다 |
| `00-foundations` 체크박스 | 확정된 항목이 미체크 | 6개 전부 닫혀 있었다 | **체크만 바꾸지 않고 각 줄에 어디서 닫혔는지**를 붙였다. 다시 열리지 않는 관문이라는 것도 적었다(새 전역 결정은 브리프로) |
| `actions.py` "기록하지 않는 21" | 실제 29 | **29 맞음. 그런데 절 주석도 갈라져 있었다** — 인증 2(실제 3) · 관리자 4+승인 2(실제 11) | 헤더·docstring·절 주석 넷 다 교정 + **재발 방지 가드** |
| `docs/README.md` "브리프 96개" | 실측 100 | **지금은 105**(표가 96, 어제 100, 오늘 105 — 표 자신이 두 번 뒤처졌다) | 105 로 교정 |
| 문서 지도 누락 2건 | — | 여전히 없었다 | `dogfood-checklist.md`(운영 관찰) · `chat-revision-ideation.md`(아이디에이션 원본)로 등재 |

### ★ 재발 방지를 하나 넣었다

`docs/README.md` 의 브리프 수가 **96 → 100 → 105 로 두 번 뒤처진 것**이 이 계열의 성질을 보여 준다: **아무도 안 보는 수는 반드시 뒤처진다.** `actions.py` 도 21 로 적힌 채 29 가 됐고, 기존 가드는 **등재 여부만** 보고 개수를 안 봤다.

`test_activity_actions.py` 에 3셀을 넣었다 — 헤더 수 · 절별 수 · 합계(절이 통째로 빠지면 절별 셀이 조용하므로). 변이 2종 재실패 확인(절 수 되돌리기 · 헤더 수 부풀리기). **재는 것은 주석의 수 ≡ 실제 수뿐**이고, 어느 절에 무엇이 들어가는지는 사람의 분류라 건드리지 않는다.

### 하루 마감 메모

- HANDOFF **282줄** — 마지막 자가 검수가 276줄(2026-09-05)이라 다음 트리거(~376줄)는 아직이다.
- **"코드를 만질 때의 규칙"에 오늘 신설된 가드 3종을 적었다** — 다음 작업자가 빨간 줄을 만났을 때 *가드가 이상한 게 아니라 값이 뒤처진 것*임을 알 수 있게.
- 미수리 표 13 → **8행**(남은 것은 코드 결함 6 + 테스트 공백 2).

### Verification

- `test_activity_actions`·`test_activity_log`·`test_docs_indexes`·`test_repo_hygiene`·`test_billable_actions` **67 passed / 1091 subtests**.
- 변이 2종 기명 재실패.

---

## 세션 34 — 배포 (원장 소유 축 개명 + 오늘치 슬라이스)

오너가 배포 호스트에서 직접 실행했다. **주소·계정·키 경로는 저장소에 적지 않는다**(공개 저장소 보안 규칙) — 아래는 결과만이다.

### 절차와 실측

| 단계 | 결과 |
|---|---|
| `git pull --ff-only` | **Already up to date** — 오너가 앞서 push 해 둔 상태였다 |
| `build`(두 override) | app(4서비스 공유 태그)·frontend·gateway·elasticsearch **Built**, 15.1초 |
| 마이그레이션 `--dry-run` | `documents_with_old_field: **14**` · `stale_indexes: 3` · `renamed: 0`(dry-run 이므로) |
| 마이그레이션 적용 | **`renamed: 14`** · `dropped_indexes: 3`(dedupe_unique · by_user_day · by_user_week) |
| `up -d` | 9 컨테이너 — **application·admin·gateway·mongo·elasticsearch·chroma healthy**, worker·generation_worker·frontend Started |

**★ `application` 이 healthy 로 떴다는 것이 마이그레이션 성공의 증거다.** 어댑터는 기동 때 `target_user_id` 키로 인덱스를 만드는데, 옛 인덱스가 남아 있었다면 같은-이름-다른-키라 **code=86 으로 거부돼 기동이 깨졌을 것**이다(v1.8.44 실측). 즉 healthy 자체가 *"옛 인덱스가 지워졌다"* 를 말한다.

### ★ 배포가 한 번 멈췄다 — 진입점 부트스트랩 누락

첫 시도가 `ModuleNotFoundError: No module named 'services'` 로 죽었다. 원인·처방·전수 정렬은 세션 35 에 있다. **오너는 `-e PYTHONPATH=/app` 우회로 계속 진행했고 배포 자체는 성공했다.**

이번 배포에 **부트스트랩 수정은 안 실렸다** — 그 두 커밋(`5759d9e`·`a207a7c`)이 배포 시점에 아직 origin 밖이었기 때문이다. 서버 스크립트에 H1 수정(`stale_indexes` 필드)은 있는데 부트스트랩만 없던 것이 그 증거다. **다음 배포부터는 `-e PYTHONPATH=/app` 없이 그냥 된다.**

### 남은 확인 (선택)

- 마이그레이션 적용과 `up -d` 사이에 **옛 앱이 돌던 짧은 창**이 있었다. 그 사이 유료 요청이 있었다면 그 행만 옛 이름으로 남는다 — 마이그레이션을 **한 번 더** 돌려 `documents_with_old_field: 0` 이면 깨끗하다(멱등이라 안전).
- 사용량이 화면에서 정상으로 보이는지(14행이 옮겨졌으므로 0 으로 보이면 안 된다).

---

## 세션 35 — 진입점 부트스트랩 (배포를 멈춘 결함)

### 무엇이 일어났나

배포 중 오너가 컨테이너 안에서 마이그레이션을 실행하자 **`ModuleNotFoundError: No module named 'services'`** 로 죽었다.

**원인은 운영이 아니라 내 스크립트다.** `python scripts/x.py` 는 `sys.path[0]` 에 **CWD 가 아니라 스크립트의 디렉터리**를 넣는다. 그래서 저장소 루트를 손수 얹어야 하고, 이 디렉터리의 **23/29 가 이미 그러고 있었다**(`index_sync_worker.py` 선례 — compose 가 `python scripts/…` 로 실행하기 때문에 그 둘은 갖고 있다). 내 스크립트가 그 관례를 빠뜨렸다.

### ★ 왜 테스트가 못 잡았나

**단위 셀 5개가 전부 초록이었다.** 셀은 `migrate()` 를 **import 해서 부르지 진입점을 밟지 않는다** — 그리고 pytest 는 저장소 루트가 이미 `sys.path` 에 있는 상태로 돌아서 이 결함이 원리적으로 안 보인다. HANDOFF 미수리 표의 *"`scripts/` 를 pytest 가 실행하지 않는다"* 가 **정확히 문 자리**다. 오늘 아침에 그 표를 읽고도 *문서* 항목만 닫았다.

### 처방

- 내 스크립트에 부트스트랩 추가(다른 cwd 에서 `--help` 실행으로 확인).
- **낙오돼 있던 6개도 맞췄다** — `create_user`·`migrate_ordered_units`·`migrate_chapter_scene_hierarchy`·**`purge_reconciler`**·스모크 2. 하필 **사람이 배포 중에 손으로 치는 것들**이라 같은 자리에서 같이 멈춘다(특히 `purge_reconciler` 는 파기 실패의 수습 경로다 — 계정 탈퇴 계획이 기대는 자리).
- `tests/test_script_entrypoints.py` — `from services.` 를 쓰는 스크립트가 그 import **앞에** 루트를 얹는지 **정적으로** 잠근다. 실행해서 재지 않는 이유는 다수가 실행 즉시 mongo·LLM 에 붙기 때문이다(이 자리가 오래 비어 있던 이유이기도 하다).

### Issues found

1. **가드가 처음에 틀렸다.** 인라인 형태만 인정해 **멀쩡한 8개**를 잡았다 — `REPO_ROOT` 변수 경유도 정당한 형태다. *"어떤 모양이냐"* 가 아니라 **"services import 전에 0번 자리에 루트를 얹는가"** 만 재도록 고쳤다. 가드를 쓸 때 **기존 코드를 먼저 훑어 형태의 폭을 재야** 한다.
2. **★ CLAUDE.md §6 을 어겼다(이 저장소 열 번째).** 부트스트랩 수정을 **커밋하기 전에** 변이를 걸었고, `git checkout -- <file>` 이 HEAD 로 되돌리면서 **미커밋 수정을 지웠다.** 그 상태를 그대로 커밋해 `5759d9e` 는 정작 고치려던 파일만 안 고친 채 나갔다. 순서는 **커밋 → 변이 → 원복**이고 첫 변이 전에 `git status --short` 가 비어야 한다. **방금 만든 가드가 즉시 잡아** 다음 커밋(`a207a7c`)에서 복구했다 — 가드가 없었으면 배포용 스크립트가 안 고쳐진 채 나갔다.

### Verification

- `test_script_entrypoints` **2 passed / 29 subtests**(스크립트 29개 전수) · 마이그레이션 셀 7 · `test_purge_reconciler` 포함 9 passed.
- 변이: 부트스트랩 제거 → 그 파일 하나가 기명 SUBFAILED.
- 다른 cwd(`/tmp`)에서 `python3 …/migrate_ledger_user_axis.py --help` 실행 확인.

### Next steps

- **다음 push 뒤 배포부터 `-e PYTHONPATH=/app` 이 필요 없다.**

---

## 세션 36 — 오너 실사용 관측 2건 인계 (하루 마감)

오너가 배포된 빌드를 둘러보다 발견한 것 둘. **고치지 않고 인계만 했다**(오너 지시). 다만 *"확인해봐야 한다"* 로만 남기면 다음 사람이 처음부터 헤매므로 **원인 후보를 코드에서 좁혀** 함께 적었다.

### 1. 메모 읽기면 (HANDOFF 13번)

오너: *"메모작성 중인 페이지에서 저장된 메모를 눌렀을 때 메모가 안보였던거같아. 그리고 메모가 보이는 곳에 그냥 흰바탕에 텍스트라서 보기 힘들어."*

- **안 보인다**: 드로어 읽기면이 `setOtherBody(note.body ?? "")` 뒤 `<p className="note-body">{otherBody}</p>` 라, **`body: null`(메모 없음)과 `""`(빈 메모)가 둘 다 빈 `<p>`** 가 된다 — 아무 문구도 안 뜬다. **편집면은 그 둘을 구분하는데**(`missing ? "아직 메모가 없습니다."`) **읽기면만 안 한다.** SoT v1.8.11 이 `null`/`""` 구분을 계약으로 박아 둔 자리라 **계약 위반**이다.
- 부수 후보: **현재 장면의 행**을 누르면 `selected` 가 `null` 이 돼 편집 상자로 돌아가는데, 이미 거기 있었으면 **아무 변화가 없어 보인다**(의도된 동작이지만 피드백이 없다).
- **흰 바탕**: `.note-body` 가 면·테두리 없는 맨 텍스트다. 같은 패널의 편집 상자(`.rail-note-editor`)는 면이 있어 **읽기면만 어긋난다** — 내가 Slice 4 에서 편집면만 보고 읽기면의 시각을 안 맞춘 것이다.

### 2. ★ 이어쓰기 채택 (HANDOFF 14번)

오너: *"채택 기능이 아직도 아무동작도 하지 않아. 애초에 채택을 눌렀을 때 어떤 동작이 되는지도 모르겠네."*

**원인이 코드에서 잡힌다 — 버튼의 `disabled` 와 핸들러의 가드가 어긋난다.**

| | 조건 |
|---|---|
| 버튼 `disabled` | `!canAccept` = 후보 있음 · gate `pass` · `nextUnitReady` |
| `accept()` 핸들러 | 위 셋 **+ `contextRef.current === null` 이면 조용히 `return`** |

즉 `contextRef` 가 비어 있으면 **버튼은 활성인데 눌러도 아무 일도 안 나고 메시지조차 없다.** `contextRef` 는 **생성 경로에서만** 채워지고(`:376`), **자동 개선(revise) 경로는 후보·gate 만 갈아 끼우고 채우지 않으며**(`:475` 부근), 채택 성공 뒤에도 비워진다(`:575`).

**★ 오너의 두 번째 문장이 더 큰 지적이다.** 버튼 라벨이 *"채택하고 저장"* 인데 **무엇이 저장되는지**가 화면에 없다 — 채택은 **생성 시점에 고정된 base + 후보**를 저장하고 **그 뒤 편집기에 친 글은 버린다**(핸들러 주석에만 있다). 그 사실이 확인 대화상자에만 있고 버튼 옆엔 없다.

**다음 사람에게 남긴 주의**: 스크래치 패드의 `채택`(`ScratchRecovery.tsx:313`)은 **라벨이 정확히 "채택"인 다른 버튼**이다. 오너가 본 것이 어느 쪽인지 **재현이 먼저**다.

### Decisions

- **처음엔 인계만 하려 했으나 오너가 바로 고치라고 했다** — *"찾아버렸으면 지금 작업안하는게 더 비효율적이잖아."* 맞는 판단이다: 원인을 좁혀 둔 맥락이 살아 있을 때가 가장 싸고, 다음 사람이 같은 추적을 반복하지 않는다. 세션 37 에서 고쳤다.

---

## 세션 37 — 관측 2건 수정 (오너 지시: *"찾아버렸으면 지금 작업안하는게 더 비효율적이잖아"*)

### 13 메모 읽기면 — 둘 다 확정 결함이었다

- **`null`/`""`/내용 셋을 각각 다르게 말한다.** 종전엔 앞의 둘이 **같은 빈 문단**이라 화면이 아무 말도 안 했다. 편집면은 이미 갈랐고 **SoT v1.8.11 이 그 구분을 계약으로 박아 둔 자리**라 — 읽기면만 계약을 안 지킨 것이다.
- **`.note-body` 에 편집 상자와 같은 면**(`--surface-raised` + hairline + 여백)을 줬다. 긴 전문은 상자 안에서 스크롤한다. **내가 Slice 4 에서 편집면만 보고 읽기면의 시각을 안 맞춘 것**이다.

### 14 채택 — 어포던스는 고쳤고, 재현은 남았다

**정직하게 적는다: 오너가 본 *"아무 동작도 하지 않는다"* 의 재현 경로를 코드에서 못 찾았다.** `contextRef` 는 성공 분기에서만 비고 그때 후보도 함께 사라진다. 그래서 아래 ①을 *"오너 증상의 원인"* 이라고 단정하지 않는다 — **원리적 가능성을 없앤 것**이다.

1. **`disabled` 가 `accept()` 의 모든 조건을 덮게 했다.** 종전 `canAccept` 는 `contextRef.current === null` 을 안 봤다 — 어긋나면 **활성인데 조용히 return 하는 죽은 버튼**이다. ref 는 렌더를 다시 돌리지 않으므로 같은 사실을 상태로도 든다.
2. **조용한 return 을 없앴다.** 그 자리에 닿으면 이유를 말한다. → **이제 무반응이면 화면이 이유를 말하므로, 오너가 다시 겪으면 그 문구가 다음 단서다.**
3. **★ 오너의 두 번째 문장이 더 큰 지적이었다** — *"어떤 동작이 되는지도 모르겠네."* 채택이 **생성 시점 본문 + 후보**를 저장하고 **그 뒤 편집기에 친 글은 버린다**는 사실이 **확인 대화상자에만** 있었다. 즉 **누르기 전에는 어디에도 없었다.** 버튼 옆 상시 문구로 올렸다.

스크래치 패드의 `채택`(`ScratchRecovery.tsx:313`)은 **다른 버튼이고 `disabled` 와 핸들러 가드가 일치한다** — 결함이 아니다. 오너가 본 것이 어느 쪽인지는 여전히 확인 대상이라 HANDOFF 14번에 남겼다.

### 변이 (커밋 `ac382cf` → 변이 → 원복)

| 변이 | diff | 재실패 셀 |
|---|---|---|
| MV-1 빈 문단으로 되돌리기 | 읽기면의 3분기 제거 | 메모 2 |
| MV-2 없음 표시 끄기 | `setOtherMissing(note.body === null)` → `false` | 메모 1 |
| MV-3 죽은 버튼 재현 | `canAccept` 에서 `acceptContextReady` 제거 + `contextRef` 를 null 로 | **14**(가드 일치 셀 포함) |
| MV-4 **over-strict** 상시 문구 제거 | `{canAccept && (` → `{false && (` | 채택 설명 셀 1 |

### Issues found

1. **채택 셀을 문구로 잠그면 안 된다.** 처음엔 문구를 단정할 뻔했는데, 그러면 문구를 다듬을 때마다 셀이 깨진다. **가드 일치**를 잠그는 쪽으로 썼다 — *"채택이 활성일 때 누르면 반드시 요청이 나간다"*(조건이 어긋나면 요청 0건이라 실패한다).
2. 화면 문구에 마크다운 `**` 를 넣을 뻔했다(JSX 는 그대로 렌더한다). 즉시 걷었다.

### Verification

- 프런트 전수 **461 → 466 passed / 38 files**(전량 캡처 `EXIT=0`) · `tsc --noEmit` 0.
- 변이 4종 기명 재실패(위 표).
- **미배포** — 다음 배포에 실린다(프론트 재빌드 필요).

---

## 세션 38 — 채택 "무반응"의 실제 원인 확정 (오너 콘솔 증거)

### ★ 세션 37 의 의심은 빗나갔다

오너가 콘솔을 붙여 줬다: **`POST …/writing/accept 429`**.

**버튼은 처음부터 동작하고 있었다.** 요청이 나갔고 서버가 거절했다. 그리고 이 시스템에서 **429 는 한도 초과가 아니다** — `api/errors.py` 가 계약으로 적어 뒀듯 *"같은 요청이 이미 진행 중이거나 방금 보냈다"* 이고 `X-Confirm-Duplicate` 재전송으로 **지금 바로** 풀린다(한도 초과는 **402** 다).

화면은 429 에서 확인 대화를 띄우게 돼 있었다(`describeQuotaError` → `kind: "locked"`, `confirmable: true`). **그런데 그 대화는 패널 맨 위에 그려지고 채택 버튼은 맨 아래다.** 아래에서 누른 사용자에게는 **화면 밖에서 열렸다.**

**교훈: 원인은 로직이 아니라 피드백의 위치였다.** 세션 37 에서 나는 `canAccept` 와 핸들러 가드의 불일치(죽은 버튼)를 의심했는데 — 그 정리 자체는 옳았지만 **오너 증상의 원인이 아니었다.** 그때 내가 *"재현 경로를 못 찾았다"* 고 정직하게 적어 둔 것이 여기서 값을 했다: 단정했으면 틀린 원인이 기록에 남았을 것이다.

### 고친 것

1. **확인 대화가 열리면 그 자리로 시선을 옮긴다** — `scrollIntoView` + `focus`. **포커스가 본체이고 스크롤은 보조**다(jsdom 에 `scrollIntoView` 가 없어 `?.()` 로 부른다 — 없다고 포커스까지 못 주면 안 된다).
2. **확인 버튼이 그 동작의 말을 쓴다** — 생성=`하나 더 만들기` · 검사=`다시 검사하기` · 개선=`다시 개선하기` · **채택=`그래도 채택하기`**. 종전엔 넷 다 `하나 더 만들기` 라, **보더라도** 무엇을 승인하는지 알 수 없었다.

### 변이

| 변이 | diff | 재실패 |
|---|---|---|
| MC-1 포커스 제거 | `confirmRef.current?.focus();` 삭제 | 확인 도달 셀 1 |
| MC-2 **over-strict** 라벨 되돌리기 | 채택 라벨을 `하나 더 만들기` 로 | 같은 셀 1 |

### ★ 전수 플레이크의 정체를 잡았다

전수 한 번이 **1실패**로 났고(467 중), 이번에는 **이름이 남았다** — `me/PersonalHubPage.test.tsx > gives the remaining request budget a home`, 전수에서 **5739ms**. **단독 실행은 12/12 초록**이고 재전수도 **467/467 초록**이다. 부하에서 느려지는 타이밍 플레이크다.

**세션 24 의 미상 1실패와 모양이 같다**(전수에서만 · 1건 · 단독은 초록). 확정은 못 한다 — 그때는 `| tail` 로 블록이 잘려 이름이 없다. 그래도 **유력한 후보**이므로 세션 24 기록에 상호 참조를 달았고, HANDOFF 미수리 표에 **알려진 플레이크**로 올렸다.

### 별건 — 콘솔의 CSP 경고

오너 콘솔에 `connect-src 'none'` **report-only** 위반이 찍힌다. **저장소의 nginx 에는 CSP 헤더가 없으므로**(Phase S-2 가 지적한 바로 그 공백) **앞단 프록시나 브라우저 확장이 넣은 것**이다. 지금은 report-only 라 차단하지 않지만 **enforcing 으로 바뀌면 API 호출이 전부 막힌다.** 출처 확인이 필요해 HANDOFF 미수리에 올렸다.

### Verification

- 프런트 전수 **467 passed / 38 files · EXIT=0**(전량 캡처, 재확인 실행) · `tsc` 0.
- 변이 2종 기명 재실패.

