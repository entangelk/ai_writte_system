# 2026-09-11 work log

## 세션 56 — 계정 탈퇴 Slice 4(화면) 독립 검증 (오너 지시 "핸드오프 확인해서 다음 작업 진행해줘. 독립 검증작업이야. 검증하고 의심하고 또 의심해줄래")

세션 55 산출(Slice 4 구현 `b68d923`·SoT v1.8.54)을 반증 시도로 뜯었다. 기록 [`docs/verifications/2026-09-11/withdrawal_slice4_screen.md`](../verifications/2026-09-11/withdrawal_slice4_screen.md) — 판정 **조건부 합격(조건 둘)**.

### Completed work

- **계약 스코프 정독 → 경계 행렬**: SoT v1.8.54 · 브리프 ⓐ표/후속 고려 · 계획서 §Slice 4 · 백엔드 라우터의 409 의미 셋까지. **409 분할의 전제를 백엔드에서 재확증** — POST 409=`LastActiveAdmin`·DELETE 409=`WithdrawalNotRequested` 가 각각 하나뿐이라 "동작별로 가른다"에 빈틈이 없다([auth.py:294·314](../../services/application/app/routers/auth.py)).
- **구현·셀 대조 — 구조 주장 전부 실측 일치**: `AuthUserContext` 프로덕션 소비자 = `useAuthenticatedUser` 뿐(grep) · `schema.d.ts` 무변 + `gen:api` 재실행 무차분 · 백엔드 무변(operation 105 무변) · 배너 프로덕션 마운트 = `AuthGate.tsx:161` 유일.
- **변이 재적용 6종 — 5종 일치, MV-2 재현 불가(→갭 B1)**: MV-1 6 · MV-3 3 · MV-4 1 · MV-5 3 · MV-6 1 전부 세션 55 기록 그대로. **MV-2("배너 /me 한정, 5실패")는 프로덕션 장착 지점(AuthGate)에서 0실패** — 초점 셀들이 `AuthGate` 없이 배너를 직접 마운트하는 복제 셸을 쓰기 때문. 컴포넌트 내부 변이(MV-2c)로는 2실패 — 어느 형태로도 "5"가 나오지 않는다.
- **신규 반증 5종 — 갭 둘 실증**: **MV-2b**(AuthGate 배너 통째 제거 → withdrawal·PersonalHubPage·App 3파일 52셀 **전건 초록** = B1) · **MV-7**(취소 409 재조회를 로컬 합성 치환 → **11/11 초록** = B2, SoT "서버의 지금 상태를 다시 읽어" 무셀) · MV-8(ceil→floor) 4실패 · MV-9(Math.max 제거) 1실패 · MV-10(원색 리터럴) 프런트 `designTokens` 1실패 — **백엔드 provenance 가드는 `:root`만 봐서 이 변이를 잡지 못한다**(가드 분업 실측).
- **전수·기준선 재현**: 프런트 전수 **460 passed / 39 files · EXIT=0**(756초, 파일 캡처) · tsc 0에러 · build 성공 · 문서 가드 넷 HEAD `0cf184a` **36 passed / 1018 subtests**(세션 55 기록과 정확히 일치).
- **기록·등재**: 검증 기록 + 인덱스 단면 **298건**(분포 197/**96**/5 · 32% 불변 — round(96/298)=32) + 루트 README 분포 문장·기준선 ②행(**4,128** — *2026-09-11 세션 57 정정: 이 줄이 처음 적은 4,127 은 증분 +2 예측값이었다. 아래 §Verification 의 양단 실측이 +3(`test_repo_hygiene` 609→611 · `test_docs_indexes` 310→311)을 냈고 README·HANDOFF 는 4,128 로 나갔다 — 이 두 자리만 예측값으로 남아 있었다*) + HANDOFF(Slice 4 검증 절·기준선 4125→**4128** 유도·플레이크 표 셋째 계열·Next Tasks 1번) + CHANGELOG.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| **B1 — 배너 장착 지점(AuthGate) 무셀** | 배너 셀들이 복제 셸을 쓴다 — SoT "셀이 잠근다" 서술이 장착 지점에 대해 거짓 | **조건으로 인계**(처방: 진짜 `AuthGate` 렌더 셀 하나 — `App.test.tsx` 선례 + 시드 인프라 있음) | 차단 |
| **B2 — 취소 409 재조회 무셀** | 기존 셀이 결과만 잠그고 수단을 안 잠금 · 재조회 실패 문구 경로도 무테스트 | **조건으로 인계**(처방: 409 셀에 fetch 호출 단정 한 줄) | 차단 |
| 초점 첫 실행 2실패("남은 기간 3일"→"4일") | **WSL2 부팅 창 시계 뒤점프** — 모듈 스코프 `Date.now()+3일` 픽스처가 렌더 시각의 역행에 취약(journal "Clock change detected" 실패 창 11회·부팅 15분 전 실측) | 재실행 11/11 초록 + 메커니즘 산술 귀속 → **미수리 표 플레이크 셋째 계열 등재** | 플레이크 |
| 세션 55 변이 표가 diff 없이 개수만 기록 | MV-2 재유도가 위치에 따라 0/2/5 로 갈라지는 것이 증명 | 검증 기록에 내 diff 전부 기재(가이드 §"write down the diff" 준수) | 기록 |

### Decisions

- **판정 = 조건부 합격.** 구현·주장 대부분이 재현됐지만 "계약이 이름 붙인 분기에 셀이 없으면 초록이어도 차단"이 가이드의 규칙이고, B1·B2 둘 다 무셀 변이(MV-2b·MV-7)로 실증됐다. 각각 처방이 한 셀이라 조건 폐쇄 비용이 낮다.
- **플레이크를 고치지 않고 등재만 했다.** 재현 1회·재실행 초록이므로 S13 선례(트리거: 재발 누적)와 같은 처리 — 고칠지는 오너/다음 슬라이스 판단.
- **백엔드 전수를 돌리지 않고 통제 대조로 갔다.** 이 세션은 백엔드 소스·테스트 무변(문서만 추가)이라 세션 55가 오너 승인받은 방법(문서 가드 양단)을 그대로 따랐다. 기준선 **4128**은 문서 둘의 유도이고 커밋 트리에서 문서 가드로 직접 확인한다 — **증분은 +2 가 아니라 +3 이었다**(검증 인덱스 행이 subtest 를 하나 더 낸다. 이 줄에 처음 적힌 4127 은 예측값이었고 2026-09-11 세션 57 이 정정했다).

### Verification (세션 56)

- 변이 12회(MV-1·2·2b·2c·3·4·5·6·7·8·9·10) — 매번 프리플라이트 `git status --short` 무출력 → 편집 → 초점 → `git -C <절대경로> checkout --` → 무출력 확인. 마지막 `git diff --stat HEAD` 무출력.
- 초점 재실행(플레이크 판정 뒤) 11/11 초록 · 프런트 전수 460/39 EXIT=0 · tsc/build 초록 · `gen:api` 무차분.
- 문서 가드(등재 뒤): 넷 초록 — **36 passed / 1021 subtests**. 증분은 양단 실측(`git worktree add --detach /tmp/wt_v56base 0cf184a`)으로 분해: `test_repo_hygiene` 609→**611**(새 문서 둘) · `test_docs_indexes` 310→**311**(검증 인덱스 행 — 예측 +2 를 넘어서는 셋째). 첫 실행에서 `docs/README.md` 의 "297건" 갱신 누락을 **가드가 물었다**(SUBFAILED — `test_every_stated_count_matches_the_files_on_disk`).

### Next steps

- **Slice 4 조건 B1·B2 폐쇄**(셀 둘, 구현 세션) → **승격 재검**(변이 재적용 MV-2·2b·7 포함, 다음 검증 세션).
- 랜딩+동의 게이트는 그 뒤(HANDOFF Next Tasks 2번).
- 개발 스택 재생성(`withdrawal_worker`)·배포 대기는 그대로.

## 세션 57 — 계정 탈퇴 Slice 4 검증 조건 B1·B2 폐쇄 (오너 지시 "검증기록 확인해서 보강할 부분 보강해줄래?")

세션 56 검증 기록([`withdrawal_slice4_screen.md`](../../verifications/2026-09-11/withdrawal_slice4_screen.md))의 차단 조건 둘을 처방대로 닫았다. 커밋 `b52633d` — **프로덕션 코드 무변, 셀만 11→13**. SoT **v1.8.55**.

### Completed work

- **B1 — 배너 장착 지점을 지나는 셀 하나.** `/projects/p1` 에서 **진짜 `AuthGate`** 를 세우고(세션 확인 응답 하나면 셸이 선다) 화면이 그려진 뒤 배너 존재·문구·취소 버튼을 단정한다(`hangs the banner off the real app shell`). 기존 배너 셀들의 `renderShell` 은 `<WithdrawalBanner />` 를 직접 마운트하는 **복제 셸**이라 장착 지점이 비어도 초록이었다 — **테스트가 프로덕션 배선을 복제하면 그 배선 자체가 무잠금이 된다**는 것이 이 조건의 일반형이고, 파일 머리말에 그 문장을 박았다.
  - **남은 일수는 일부러 안 잰다.** 그 축은 위 순수함수 셋이 고정 시각으로 잠그고 있고, 여기서 또 재면 H1(모듈 스코프 `Date.now()+3일`)의 시계 의존을 장착 지점 셀로 끌어들이게 된다.
- **B2 — 409 재조회의 *수단* 을 잰다 + 처방이 지목한 무테스트 경로 한 셀.** 기존 409 셀에 호출 단정 셋(**2회** · 둘째가 `/api/me/withdrawal` · **GET**)을 넣었다. 결과(배너 소멸)는 로컬 `null` 합성으로도 같아서 수단을 안 잠그면 계약 문장이 비어 있다. 이어 **재조회까지 실패하는 경로**를 셀로 덮었다 — 문구(`탈퇴 상태를 다시 읽지 못했습니다`) + **배너 유지**(못 읽은 상태를 지어내면 유예 중인 사람이 저장을 눌렀을 때 403 이 다시 정체불명이 된다).
  - 기존 409 셀이 `stubFetch(...)` 의 반환을 **안 받고 있었다** — 단정을 넣으며 `const fetchMock =` 로 받았다(첫 실행이 `fetchMock is not defined` 로 실패해 드러났다).
- **기록·등재**: 검증 기록에 **§폐쇄 보고**(v1.8.52 선례 형식 — 조건별 처방 대조 + 변이 재확인 표) · 검증 인덱스 요약 칸(판정 열은 **안 건드렸다** — 승격은 다음 검증 세션 몫) · SoT v1.8.55 행 + **v1.8.54 행의 잠금 주장에 정정 포인터** · README SoT 버전 칸 · HANDOFF(절 제목·기준선 462·계약 주의 ⓐ·Next Tasks 1번) · CHANGELOG.
- **계획서 §Slice 4 범위 문언 정정**(검증 H-목록의 마지막 항목): *"설정 화면의 탈퇴 요청"* → `/me`(개인 허브). 결정 전에 쓰인 문언이 남아 있던 것으로 모순은 아니었고, 정정 표식을 달았다.
- **세션 56 work_log 의 낡은 수 정정**: 이 파일이 두 자리에서 기준선을 `4127`(증분 +2 예측)로 적고 있었는데, 같은 파일 §Verification 의 양단 실측(`test_repo_hygiene` 609→611 · `test_docs_indexes` 310→311)과 README·HANDOFF 는 **4128**(+3)이다. 두 자리를 4128 로 고치고 왜 갈렸는지를 그 자리에 적었다.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| B1 — 배너 장착 지점 무셀 | 배너 셀들이 `AuthGate` 없는 **복제 셸**을 씀 — 계약이 사는 자리를 셀이 안 지난다 | 진짜 `AuthGate` 를 세우는 셀 하나 | **MV-2b 1실패**(종전 3파일 52셀 전건 초록) · **MV-2 1실패**(종전 0실패) |
| B2 — 취소 409 재조회 무셀 | 셀이 **결과**(배너 소멸)만 봄 — 로컬 합성과 구별되지 않는다 | 둘째 호출을 직접 세는 단정 + 재조회 실패 경로 셀 | **MV-7 2실패**(종전 11/11 초록) · **MV-11 1실패** |
| 기존 409 셀이 `stubFetch` 반환을 안 받음 | 그 셀은 호출을 셀 일이 없어 변수도 없었다 | `const fetchMock =` 으로 받음 | 첫 실행 실패 → 즉시 수정, 13/13 |
| 계획서 §Slice 4 "설정 화면" 문언 | 결정(브리프 D1=ⓐ, `/me`) 전에 쓰인 줄이 갱신 안 됨 | 문언 정정 + 정정 표식 | 기록 |
| 세션 56 work_log 두 자리의 `4127` | 증분 **예측값**(+2)이 먼저 적히고, 뒤에 실측(+3)이 README·HANDOFF 에만 반영됨 | 4128 로 정정하고 사유 병기 | 기록 |

### Verification (세션 57)

- **프리플라이트**: 매 변이 전 `git status --short` 무출력. **폐쇄 커밋(`b52633d`)이 변이보다 먼저다** — 변이는 커밋된 트리 위에서만 했고, 매번 `git -C <절대경로> checkout -- <절대경로>` 뒤 무출력 확인. 마지막 `git diff --stat HEAD` 무출력.
- **변이 6종, 양방향**:

| 변이 | diff | 기대 | 실측 |
|---|---|---|---|
| **MV-2b** (under) | `AuthGate.tsx` 에서 `<WithdrawalBanner />` 줄 통째 제거 | 새 B1 셀만 실패 | **1실패**(3파일 54셀 중) — 종전 52셀 전건 초록 |
| **MV-2** (under) | `AuthGate.tsx` → `{location.pathname === "/me" && <WithdrawalBanner />}` | 새 B1 셀만 실패 | **1실패** — 종전 프로덕션 지점에서 0실패 |
| **MV-2d** (over) | 배너를 `<header>` **위로** 이동(셸 안, 줄 위치만 다름) | 초록이어야 한다(줄 위치를 잠그면 과잉) | **13/13 초록** |
| **MV-7** (under) | 409 처리 두 곳: `apply(await getMyWithdrawal())` → `apply({withdrawal_requested_at: null, purge_due_at: null})` | 409 셀 + 재조회 실패 셀 실패 | **2실패** — 종전 11/11 초록 |
| **MV-11** (under) | 재조회 실패 분기(try/catch) 제거 → 일반 오류 문구로 떨어짐 | 재조회 실패 셀만 실패 | **1실패** |
| **MV-12** (over) | 재조회 실패 catch 에서 상태를 `null` 로 **합성**(못 읽었는데 지어냄) | 재조회 실패 셀만 실패 | **1실패** |

- **전수**: 프런트 **462 passed / 39 files · EXIT=0**(757초, 파일 캡처 — `frontend/` 안에서). 세션 56 기준선 460/39 대비 **+2 = 이번 폐쇄의 두 셀**(B2 의 수단 단정은 기존 셀 안이라 수를 안 움직인다). `npx tsc --noEmit` 0에러 · `npm run build` 성공.
- **백엔드**: 소스·테스트 **무변**(문서만) — 세션 55·56 이 오너 승인받은 통제 대조를 그대로 따른다. 문서 가드 `test_docs_indexes` **17 passed / 311 subtests**(문서 파일을 안 늘렸으므로 세션 56 값 그대로 — 기준선 4128 무변).

### Next steps

- **Slice 4 승격 재검은 검증 세션 몫**(변이 재적용 MV-2·2b·7 + 신규 2d·11·12). 조건을 닫은 이 세션은 판정을 못 올린다.
- 구현 세션이 다음에 집는 것은 **랜딩 + 동의 게이트**(HANDOFF Next Tasks 2번).
- **H1 플레이크는 안 고쳤다** — 등재 상태 그대로다(고칠지는 오너 판단). 고친다면 처방은 렌더 셀의 시각 고정(`vi.setSystemTime`)이나 픽스처 여유 하루 폭.

## 세션 58 — 랜딩 축 착수 순서 ② : `/terms`·`/privacy` + 공개 푸터 (오너 지시 "핸드오프 확인해서 다음 작업 진행해줄래?")

HANDOFF Next Tasks 2번(랜딩 + 동의 게이트)의 **착수 순서 ②** 를 구현했다. 브리프 [`landing-page-scope-decisions.md`](../../plans/landing-page-scope-decisions.md) **D2=ⓐ**(앱 안 정적 페이지 + 대조 가드). 커밋 `d8bae4a`(구현) · `95f2592`(렌더러 종료 결함 폐쇄). SoT **v1.8.56**.

### Completed work

- **라우트를 두 구간으로 갈랐다.** `AuthGate` 가 앱 전체를 감싸던 것을 **보호 구간(`path="*"`)만** 감싸게 바꾸고 그 앞에 공개 라우트 둘(`/terms`·`/privacy`)을 놓았다. 9.2 **P6=ⓐ** 가 *"`AuthGate` 를 보호 구간에만 적용해 공개 route 를 끼울 수 있게"* 라고 예고한 자리이고, F10(공개 랜딩)의 트리거가 온 것이다.
  - **보호 구간은 한 자리로 남긴다** — 라우트마다 `AuthGate` 를 두르면 이동마다 세션 확인이 다시 나가 화면마다 *"세션을 확인하는 중…"* 이 번쩍인다.
  - **공개 라우트가 느는 것은 API 가 느는 것이 아니다**(브리프가 기록을 요구한 구분): 공개 API 표면은 그대로 `/health`·`/auth/login`·`/auth/signup`·`/auth/logout` 넷이고 operation 105 무변이다.
- **본문은 옮겨 쓰지 않고 사본을 싣는다.** `docs/legal/*.md` 를 `frontend/src/legal/` 로 복사해 `?raw` 로 번들하고, 읽을 때 `renderLegalDocument` 가 변환한다. **본문을 TSX 로 옮겨 쓰는 길은 처음부터 닫혀 있었다** — 그러면 바이트 대조 가드가 성립하지 않는다.
  - 렌더러는 **두 문서가 실제로 쓰는 문법만** 그린다(h1·h2·문단·인용·표·번호 목록·중첩 불릿·수평선 + 강조·코드·링크). 일반 마크다운 라이브러리를 들이면 번들과 위생 처리를 함께 떠안는다.
  - **링크 둘만 앱 주소로 바꾼다**(`terms-of-service-draft.md`→`/terms` · `privacy-policy-draft.md`→`/privacy`). 저장소 문서끼리의 참조(`README.md`·`../service-policy-contract.md`)는 **문자로만** 남긴다 — 독자가 따라갈 수 없는 주소를 링크로 보여 주는 것이 브리프가 ⓑ 의 단점으로 적은 *"약관이 개발 문서처럼 보인다"* 의 실현이다.
  - **머리말 `근거:` 줄은 걷고 `상태`·`작성`·`버전` 은 남긴다**(검토 전이라는 사실과 판본은 독자에게 필요하다). 셋은 **한 줄씩** 세운다 — 마크다운 기본 접합대로면 *"상태: … 작성: … 버전: …"* 한 문장이 되어 판본이 문장 속에 숨는다.
- **대조 가드**(D2=ⓐ 를 고르며 인정한 단점의 값): [`legalSource.test.ts`](../../../frontend/src/legal/legalSource.test.ts) 4셀 — ① 사본 ↔ 정본 **바이트 동일** ② **목록 자체를 디스크와 대조**(정본이 셋째 문서를 얻었는데 안 실으면 ①은 아는 짝만 보고 조용히 통과한다 — `typeScale.test.ts` 4번이 닫은 것과 같은 맹점) ③ **버전 문자열 `1.0` 핀**(머리말 + 부칙 두 자리 — 동의 게이트가 저장할 계약 리터럴) ④ 푸터의 연락처·운영자 표기가 문서와 같은 값인가.
- **공개 푸터**(약관 · 방침 · 문의 · 운영자) — 오너 2026-09-07 *"`[문의 연락처]` 는 랜딩 푸터에 들어간다"*. 붙는 자리는 **정문 세 얼굴**(세션 확인/오류 · 로그인·가입 · 가입 접수)과 **약관·방침 페이지**다. **작업 화면(앱 셸)에는 달지 않는다** — 매일 쓰는 도구의 화면마다 법적 링크 줄이 따라다닐 이유가 없고(10.4 의 "작업 도구 문법"), 그 경계는 셀이 양방향으로 잠근다.
- **셸은 앱 셸의 규칙을 빌린다**(`app-shell`·`app-header`) — 폭·밑줄을 다시 정하면 10.4 가 닫은 *"화면마다 폭"* 결함이다. 다만 **계정 메뉴도 유예 배너도 없다**(세션을 모르는 자리다). 읽는 글이라 `.legal-document` 가 **스스로** 46rem 로 제한한다(`.page-heading` 42rem 선례 — 컨테이너 폭 규칙은 건드리지 않는다).
- **약관·방침은 진입 번들 밖이다**(`95f2592` 뒤 `83e335e`) — 본문 사본 둘이 **14 kB 의 원문**이고 매일 쓰는 집필 세션은 그 페이지를 열지 않는다. `ObservabilityDashboard`·`AdminConsole` 이 이미 그 규칙으로 갈라져 있다. 실측 **474.26 → 457.22 kB**(gzip 145.23 → 139.18) · 새 청크 `LegalPage` 17.40 kB(gzip 6.37). **빌드 산출물이 그것을 보여줘서 알았다** — 첫 구현은 진입 청크에 실려 있었다.
- **방침 부칙에서 내부 메모를 걷었다** — `- 개인정보 보호 문의: kdtyohan@gmail.com — **랜딩 페이지 푸터에도 싣는다**(오너 2026-09-07)`. 공개 문서에 **편집 지시가 실려 있었고** 이제 그것이 화면에 그려진다. 같은 사실은 [`legal/README.md`](../../legal/README.md) 조달 표가 이미 적으므로 정보 손실은 없다.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| **렌더러가 부분집합 밖에서 무한 루프** — 탭이 멈춘다 | `startsBlock` 이 블록이라고 말하지만 **받아 줄 분기가 없는 줄**(지원하지 않는 `### …`)에서 문단 수집기가 현재 줄을 안 먹어 `cursor` 가 제자리 | 문단 수집기가 **현재 줄을 무조건 먹는다** + 종료 셀 하나(`degrades an unsupported construct to text…`) | **변이 ML-4 가 기명 실패가 아니라 OOM 으로 죽어 드러났다**(커밋 `95f2592`). 폐쇄 후 ML-4 는 기명 1실패 |
| 공개 문서에 편집 지시 | 초안이 *"랜딩 푸터에도 싣는다"* 를 부칙 항목 안에 적어 둠 — 저장소 안에서만 뜻이 있는 문장 | 부칙에서 제거(정본은 `legal/README.md` 조달 표) | 공개 페이지가 운영 메모를 안 보여준다 |
| 푸터와 본문이 같은 이름의 링크를 든다 | 약관 본문이 방침을 **세 자리**에서 가리키고 푸터도 같은 이름 | 본문 단정은 `<article>` 안에서, 셋 **전부** 센다 | 첫 실행 `Found multiple elements` → 범위를 가른 뒤 초록 |
| `버전:` 단정이 부칙 줄에도 걸림 | 부칙이 `- 이 약관 버전: \`1.0\`` 으로 같은 토큰을 쓴다 | 메타 세 줄을 **클래스로 걷어 순서까지** 단정 | 판본 줄이 붙었는지/갈라졌는지 둘 다 잡는다 |
| **프런트 전수 1실패 — 신규 플레이크 계열** | `App.test.tsx > keeps the scene-notes screen behind the session gate` 가 제목만 기다리고 **fetch 시퀀스를 `waitFor` 없이** 단정한다(목록 요청이 아직 안 나간 시점) | 회귀 아님 판정(단독 29 passed) · 미수리 표에 등재 | 전수 831초 실행에서 1실패 · **단독 재실행 초록** · 같은 트리의 앞선 전수는 41파일 전건 초록 |

### Decisions

- **푸터는 공개 표면에만.** 로그인한 사용자가 약관에 닿는 길은 주소뿐이 된다 — 그 대가를 알고 고른 것이고(작업 화면 문법 보존), 동의 게이트(④)가 가입 흐름 안에서 링크를 다시 세운다. 뒤집고 싶으면 `MO-1` 이 그 자리를 가리킨다(앱 셸 `<main>`).
- **머리말 메타 세 줄은 남긴다.** *상태(검토 전)* 와 *버전* 은 독자에게 필요한 고지이고, `근거:` 만 저장소 경로라 걷었다.
- **렌더러를 직접 쓴다**(라이브러리 도입 안 함) — 문법이 문서 둘에 닫혀 있고, 번들·위생 처리보다 이 파일을 고치는 편이 싸다. 경계는 종료 셀이 지킨다.

### Verification (세션 58)

- **프리플라이트**: 변이 전 `git status --short` 무출력 — **구현 커밋(`d8bae4a`)·폐쇄 커밋(`95f2592`)이 모든 변이보다 먼저**다. 매 변이 뒤 `git -C <절대경로> checkout -- .` + 무출력 확인(스윕 스크립트가 원복 실패 시 멈춘다).
- **변이 11종, 양방향** — 초점은 `src/legal` + `src/App.test.tsx`(42~43셀):

| 변이 | diff | 기대 | 실측 |
|---|---|---|---|
| **ML-1** (under) | `frontend/src/legal/terms-of-service.md` 제3조 `**7일**` → `**14일**`(사본만) | 바이트 대조 셀 | **1실패** — `keeps every bundled copy byte-identical…` |
| **ML-2** (under) | `App.tsx` 공개 라우트 둘을 **보호 구간 안으로** 이동 | 인증 밖 셀 | **7실패** — 약관·방침 5셀 + 푸터 2셀(세션 확인이 끼어 문서가 안 뜬다) |
| **ML-3** (under) | `AuthGate.tsx` 의 `<LegalFooter />` **세 줄 전부** 제거 | 정문 푸터 셀 둘 | **2실패** |
| **ML-4** (under) | `markdown.tsx` 표 분기 비활성(`if (false && …)`) | 마크다운 모양 셀 | **폐쇄 전: OOM**(무한 루프 — 위 Issues) → **폐쇄 후 1실패** `renders the markdown shapes…` |
| **ML-5** (under) | `IN_APP_LINKS` 를 빈 객체로 | 링크 셀 | **1실패** |
| **ML-6** (under) | `METADATA_LINE` 무력화(`/^(상태\|작성\|버전):(?!)/`) | 메타 줄 셀 | **1실패** |
| **ML-7** (under) | 가드의 `BUNDLED` 에서 방침 제거(= 감시 축소) | 목록 대조 셀 | **1실패** — `bundles every canonical document…` |
| **ML-8** (under) | `markdown.tsx` 문단 수집기의 `[line]`+`cursor += 1` 제거(폐쇄 되돌리기) | 종료 셀 | **워커 OOM — 기명 실패가 아니다**(동기 렌더의 무한 루프는 `testTimeout` 이 못 끊는다). 신호는 *전수가 완주하지 못한다* 이고 셀 docstring 이 그 한계를 적는다 |
| **MO-1** (over) | 앱 셸 `<main>{children}<LegalFooter /></main>` | 작업 화면 경계 셀 | **1실패** — `leaves the working screens alone…` |
| **MO-2** (등가) | 약관 페이지에서 푸터를 `<main>` **밖으로** 이동(페이지에는 여전히 있다) | 초록이어야 한다(줄 위치를 잠그면 과잉) | **42/42 초록** |
| **MO-3** (over) | `IN_APP_LINKS[href] ?? href` — 저장소 경로까지 링크로 | 링크 셀 | **1실패** |

- **전수**: 프런트 **476 passed / 41 files · EXIT=0**(823초 — 신규 2파일 14셀 = 기준선 462/39 대비 +14/+2). ★ **같은 트리를 두 번 돌렸다**: 첫 실행(831초)이 1실패였고 그것이 위 신규 플레이크(단독 29 passed), 재실행이 **전건 초록**이다. 13셀 시점(`95f2592` 직전)의 전수도 **475/41 EXIT=0** 이었다. `npx tsc --noEmit` 0에러.
- **백엔드 기준선은 전수 재실행이 아니라 통제 대조로 유도했다**(HANDOFF 가 규정한 조건 — *"백엔드 소스 무변"*): `git diff --name-only b895f98..HEAD -- services scripts tests` **빈 출력** · 그리고 **새 문서 파일이 없다**(`git diff --name-status b895f98..HEAD -- docs` 는 방침 한 건 `M` 뿐 — work_log 는 이미 있던 파일이다). 문서·프런트를 읽는 가드 전부를 돌렸다: `test_docs_indexes`·`test_repo_hygiene`·`test_design_token_provenance`·`test_activity_ui_labels`·`test_typecheck`·`test_service_policy_contract`·`test_product_name` → **58 passed / 1110 subtests · EXIT=0**. 따라서 기준선 **3025 passed / 1 skipped / 4128 subtests 무변**이고 README 절차 표도 그대로다.

### Next steps

- **③ 랜딩 본문**(D1=ⓒ — 소개 + 정직한 한계: 승인제 가입 · 원고 외부 전송 · 개인 프로젝트). 문구는 [`product-overview.md`](../../product-overview.md) §5 를 쓰고 `abstract.md` 를 인용하지 않는다. **스크린샷은 육안 확인이 닫힌 뒤**(오너 2026-09-08).
- **④ 가입 동의 게이트**(HANDOFF 10번) — 동의 시각 + **버전 `1.0`** 저장. 백엔드 축이 함께 열린다(가입 요청 payload·저장·관리자 화면 노출 여부). 이 게이트가 상수를 만드는 날 **양쪽 핀 셀**(`legalSource.test.ts` · `test_service_policy_contract.py`)이 그 상수를 가리키게 바꾼다.
- **Phase S-2(nginx 보안 헤더)·AdSense 화면별 제어는 같은 창**이 싸다 — 광고가 이제 약관·방침 페이지에도 뜬다(`index.html` 이 모든 페이지에서 로드한다).
- 신규 플레이크 셀(`App.test.tsx` scene-notes 게이트)의 처방은 **fetch 시퀀스를 `waitFor` 로 감싸는 것**이다(같은 파일의 다른 셀들이 이미 그 모양이다). 이번 슬라이스 밖이라 등재만 했다.

## 세션 59 — 랜딩 축 ② 독립 검증 + 계정 탈퇴 Slice 4 승격 재검 (오너 지시 "다음 작업 검증해줘")

두 축을 한 세션에서 다뤘다: ① 세션 58 산출(랜딩 ②, SoT v1.8.56)의 독립 검증 ② 세션 57 폐쇄분(Slice 4 조건 B1·B2)의 승격 재검 — 검증 세션 몫으로 남아 있던 것. 기록 [`landing_l2_terms_privacy.md`](../verifications/2026-09-11/landing_l2_terms_privacy.md)(**조건부 합격**) · [`withdrawal_slice4_promotion.md`](../verifications/2026-09-11/withdrawal_slice4_promotion.md)(**합격** — 선행 기록 승격).

### Completed work

- **랜딩 ② — 계약·구현 대조 전축 일치**: 라우팅 두 구간(공개 라우트 마운트 조회 무발사 셀 확인) · 백엔드 무변(공개 API 넷·operation 105) · 번들 수치 SoT 와 소수점 일치(457.22/139.18 · 청크 17.40/6.37) · `gen:api` 무차분 · 방침 부칙 메모 제거 최소diff + `legal/README.md` 조달 표 사실 보존 확인.
- **★ 바이트 가드 양방향 신규 실증(ML-9)**: 세션 58 은 사본 측(ML-1)만 시험했다 — **정본만 고쳐도** 첫 셀이 문다(1실패). D2=ⓐ의 가드가 양쪽으로 하중을 받는다.
- **세션 58 변이 재적용 6종 전건 일치**(ML-1 1·ML-2 7·ML-3 2·ML-7 1·MO-1 1·MO-2 등가 초록) — 보고 정직.
- **갭 LB1 실증(MO-6a·6b)**: SoT *"정문 세 얼굴"* 중 **AuthStatus(세션 확인·오류)와 가입 접수** 얼굴에서 각각만 푸터를 지워도 legal 14 + App 29 전건 초록. ML-3 의 "2실패"(둘 다 로그인 폼 얼굴)가 신호였다. **세션 56 B1(배너 장착 지점)과 같은 형태가 한 슬라이스 만에 재발** — 조건으로 인계(처방: 셀 하나).
- **Slice 4 승격 재검 6종 전부 세션 57 기록과 일치**(MV-2b 1 · MV-2 1 · MV-2d 등가 초록 · MV-7 2 · MV-11 1 · MV-12 1) → 선행 기록 판정 **조건부 합격 → 합격 승격**(원문 인용 보존).
- 전수·기준선: 프런트 **476/41 EXIT=0**(908초) · tsc 0 · 문서 가드 7종 등재 전 **58/1110**(세션 58 유도와 일치) → 등재 후 **58/1114**(증분 +4 측정: 문서 둘·인덱스 행 둘) → 기준선 **4132**.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| LB1 — 푸터 두 얼굴 장착 무셀 | 셀이 로그인 폼 얼굴만 지나감 — "세 얼굴" 열거의 둘에 경로 없음 | **조건으로 인계**(셀 하나) | 차단 |
| MV-2 첫 측정 3실패(기록 1) | **H1 시계 플레이크 재발** — 2셀이 "4일"로 깨짐(25분 창 journal Clock change **50회**) | 재측 정확히 1 — 플레이크 2회째 등재 갱신 | 플레이크 |
| 편집 메모 재유입 무가드(ML-10) | 바이트 가드는 "두 사본이 같은가"만 알지 내용을 모름 | 하드닝 H2 로 기록(패턴 금지 셀은 오너 값 필요) | 비차단 |
| 등재 중 카운트 넉 자리 미갱신 | 측정 먼저 하려던 순서 탓 | 가드가 물었다(SUBFAILED 4) → 300건으로 일괄 갱신 | 폐쇄 |

### Decisions

- **두 축을 한 세션에서**: 승격 재검은 검증 세션의 몫이었고 랜딩 ② 검증이 같은 날 열렸으므로 같이 담았다. 승격 재검은 별도 기록으로 남겼다(세션 52 선례).
- **MV-2 불일치(3 vs 1)를 플레이크로 분류**: 실패 메시지 "남은 기간 4일"이 H1 의 서명과 정확히 같고 journal 이 시계 스텝 50회를 증명하며 재측이 기록과 일치 — 세 지점의 교차 확인이다.

### Verification (세션 59)

- 변이 11회(랜딩: ML-1·2·3·7·9·10, MO-1·2·6a·6b·7) + 6회(Slice 4: MV-2·2b·2d·7·11·12) — 매번 프리플라이트 무출력 → 편집 → 초점 → 절대경로 원복 → 무출력 확인.
- 기준 초점: legal 14/14 · withdrawal 13/13. 전수 476/41 EXIT=0 · build/tsc/gen:api 초록.
- 문서 가드(등재 뒤): 7종 **58 passed / 1114 subtests · EXIT=0**.

### Next steps

- **LB1 폐쇄**(셀 하나, 구현 세션) → 승격 재검(다음 검증 세션).
- 랜딩 ③ 본문 · ④ 동의 게이트 — LB1 과 무관하게 진행 가능.
- H1 플레이크 재발 2회 — 처방 적용 여부 오너 판단.

## 세션 60 — 랜딩 ② 검증 조건 LB1 폐쇄 + 하드닝 H3 (오너 지시 "검증기록 확인해서 보강할 부분 보강해줘")

세션 59 검증 기록([`landing_l2_terms_privacy.md`](../../verifications/2026-09-11/landing_l2_terms_privacy.md))의 차단 조건 하나와 하드닝 하나를 닫았다. 커밋 `7be433e` — **셀 14→16**, 프로덕션 변경은 H3 범위 교정 한 군데뿐이다. SoT **v1.8.57**.

### Completed work

- **LB1 — 푸터의 세 장착 지점 중 둘을 지나는 셀 하나.** SoT v1.8.56 이 *"정문 세 얼굴과 약관·방침 페이지"* 로 **열거**했는데 셀이 지나는 얼굴은 **로그인 폼 하나**였다(검증 MO-6a·6b 각각 전건 초록). 새 셀은 **세 얼굴을 한 셀에** 담고 사이를 `unmount()` 로 끊는다 — ① 세션 확인 중(**영원히 pending 한 fetch** 로 그 얼굴에 머문다) ② 세션 확인 실패(**500** — 401 이 아닌 실패의 얼굴) ③ 가입 접수(201 뒤 머무는 얼굴). 단정은 얼굴마다 `법적 고지` nav + 그 얼굴의 고유 문구다.
  - **얼굴마다 셀을 쪼개지 않았다** — 같은 계약이 세 자리로 갈라지면 하나가 빠지는 것이 다시 조용해진다. 실패 메시지에 얼굴 이름을 실어(`"세션 확인 얼굴에 법적 고지가 없다"`) 한 셀이어도 어디가 깨졌는지 바로 읽힌다.
  - **★ 세션 56 B1 과 같은 실패 형태가 한 슬라이스 만에 재발했다**(검증의 지적). *"계약이 사는 자리를 셀이 지나는가"* 는 슬라이스마다 되묻는 질문이고, **열거가 셋이면 셀도 셋을 봐야 한다** — 이 문장을 새 셀 머리말에 박았다.
- **H3 — 접두 필터를 머리말 안에 가뒀다.** `근거:` 걷기와 메타 줄 분리가 문서 **전체**에 적용돼, 본문 한가운데 같은 모양의 줄이 오면 **조용히 사라졌다**(약관 조항이 없어지는데 화면도 전수도 아무 말을 안 한다). 머리말 경계를 유도하고(`---`, 없으면 첫 `## `) 규칙을 그 범위로 제한했다. 셀은 **머리말 줄은 사라지고 본문의 같은 모양은 그려진다**를 한 자리에서 단정한다.
- **검증 기록에 §폐쇄 보고**(v1.8.52·v1.8.55 선례 형식 — 항목별 처방 대조 + 변이 재확인 표 + Reproduction 에 폐쇄 확인 절차). **판정 열은 안 건드렸다** — 승격은 다음 검증 세션 몫이다.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| LB1 — 정문 세 얼굴 중 둘이 무셀 | 푸터 셀 둘이 **로그인 폼 얼굴만** 본다(ML-3 의 "2실패" 가 그 신호였다) | 세 얼굴을 지나는 셀 하나 | **MO-6a 1실패**(종전 전건 초록) · **MO-6b 1실패**(종전 전건 초록) · **ML-3b 2실패**(기존 셀이 폼 얼굴을 계속 지킨다 — 세 얼굴이 셀로 분할 점유됨) |
| H3 — 접두 필터가 문서 전체 | 머리말 전용 규칙을 문서 전체에 적용 — 본문 줄이 **조용히 삭제** | 머리말 경계 유도 + 범위 제한 | **MH-3a 1실패** · **MH-3b over 2실패** · **MO-8 등가(경계를 첫 `## ` 로만) 45/45 초록** |
| H2(ML-10)는 못 닫았다 | 바이트 가드는 *"같은가"* 만 알고 *"무엇이 실렸는가"* 를 모른다. 기계화 후보인 금지 어휘가 **머리말 고지 상자의 일부러 실은 문장**(*"오너 결정(2026-09-09)으로 …"*)과 충돌 | **오너 결정 대기** — 갈래 셋을 폐쇄 보고에 적었다(ⓐ 금지+예외 허용목록 · ⓑ 고지에서 오너·날짜 서술 제거 후 금지 · ⓒ 기계화 포기, 개정 체크리스트) | 공개 문서의 문장을 바꾸는 결정이라 구현자가 고를 자리가 아니다 |

### Verification (세션 60)

- **프리플라이트**: 변이 전 `git status --short` 무출력 — **폐쇄 커밋 `7be433e` 이 모든 변이보다 먼저**다. 매 변이 뒤 `git -C <절대경로> checkout -- .` + 무출력 확인(스윕 스크립트가 원복 실패 시 멈춘다).
- **변이 6종, 양방향** — 초점 `src/legal` + `src/App.test.tsx`(45셀):

| 변이 | diff | 기대 | 실측 |
|---|---|---|---|
| **MO-6a** (under) | `AuthGate.tsx` `AuthStatus` 안 `<LegalFooter />` 한 줄 제거 | 새 LB1 셀만 실패 | **1실패** — 검증 실측은 legal 14 + App 29 **전건 초록** |
| **MO-6b** (under) | 가입 접수 얼굴의 `<LegalFooter />` 한 줄 제거 | 새 LB1 셀만 실패 | **1실패** — 종전 43/43 초록 |
| **ML-3b** (under) | 로그인 폼 얼굴의 `<LegalFooter />` 한 줄 제거 | **기존** 두 셀이 실패 | **2실패** — 세 얼굴이 셀로 **분할 점유**됨을 실증 |
| **MH-3a** (under) | `markdown.tsx` 머리말 스코프를 문서 전체로 되돌림 | H3 셀만 실패 | **1실패** |
| **MH-3b** (over) | `근거:` 걷기 제거(편집 메타가 공개면에 뜬다) | H3 셀 + 링크 셀 | **2실패** |
| **MO-8** (등가) | 머리말 경계를 `---` 없이 첫 `## ` 로만 구한다 | 초록이어야 한다(유도 방식을 잠그면 과잉) | **45/45 초록** |

- **전수**: 프런트 **478 passed / 41 files · EXIT=0**(839초, 파일 캡처 — `frontend/` 안에서). 검증 기준선 476/41 대비 **+2 = 이번 폐쇄의 두 셀**. `npx tsc --noEmit` 0에러 · `npm run build` 성공 — **진입 번들 457.22 kB 불변**, `LegalPage` 청크 17.40 → **17.61 kB**(늘어난 것은 머리말 경계 유도 코드다).
- **백엔드**: 소스·테스트 **무변**이고 **새 문서 파일도 없다**(검증 기록은 기존 파일에 절을 더했고 work_log 도 기존 파일이다) → 기준선 **3025 / 1 / 4132 무변**. 커밋 트리에서 문서·프런트를 읽는 가드 7종으로 확인: **58 passed / 1114 subtests · EXIT=0** — 세션 59 가 등재 뒤 잰 1114 와 **같은 수**다(문서 파일이 안 늘었으므로 같아야 하고, 실제로 같다).

### Next steps

- **승격 재검은 다음 검증 세션 몫**(MO-6a·6b·ML-3b + MH-3a·3b·MO-8 재적용). 조건을 닫은 이 세션은 판정을 못 올린다.
- **H2 는 오너 답을 기다린다**(갈래 셋 — 폐쇄 보고 §남은 것).
- 구현 세션이 다음에 집는 것은 **랜딩 ③ 본문**과 **④ 가입 동의 게이트**다 — LB1 과 무관하게 열려 있다.
