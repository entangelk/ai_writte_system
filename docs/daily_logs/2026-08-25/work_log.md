# 2026-08-25 작업 로그 (알파)

> **[세션 2] README 화면 스크린샷 — 5장 정규화 파이프라인 (문서·에셋, 프로덕션 코드 0줄 · SoT 무변).**
> 오너가 리드미/소개용으로 찍은 내부 화면 5장(창 크기 제각각 — 비율 1.25~1.82)의 비율 통일을
> 요청(*"가로세로 화면 비율 맞춰서 남는 부분은 검은색 처리하든 해서 동일하게"*)했다. 구현자
> 제안(검은 레터박스 대신 **페이지 배경색 패딩**)을 오너가 승인(*"오케이 그럽시다 … 전부 내부
> 페이지니까 톤 자체는 똑같아"*), README 삽입분 선별은 *"꼭 다 넣을 필요는 없고"*로 구현자
> 판단에 위임됐다.
> - **측정이 규칙을 정했다**: 다섯 장 전수 PIL 계측 — 가장자리 최다색이 전부 **#f1f8ff**
>   (99%+ 단색 → 브라우저 크롬 부재 증거), 알파 전부 불투명. 패딩을 검정이 아니라 이 색으로
>   하면 **이음새가 원리적으로 없다**(라이트 테마 README 흰 배경 위 검은 띠는 역효과 — 레터박스는
>   어두운 영상 관행). 비율은 후보 3종 중 패딩 총량 최소인 **3:2**(트림 기준 평균 24.9% vs
>   16:10 25.8% · 16:9 29.2%), 폭은 **1200px**(GitHub 본문폭 ~980보다 넉넉 → 레티나 선명).
> - **파이프라인** [`docs/img/normalize.py`](../../img/normalize.py): 오토크롭(배경색 여백 24px만
>   남김 — 프로젝트목록 아래 457px 죽은 여백 등 제거) → 3:2 캔버스 배경색 패딩(중앙) →
>   1200×800 리샘플. 원본 보존·`*_3x2.png` 별도 출력·재실행 멱등(`_3x2` glob 제외). 재현
>   스크립트 커밋 관례 준수 — 나중에 스크린샷을 다시 찍으면 한 명령으로 재생성.
> - **Issues — bbox 임계 초판 실패**: 채널별 편차 **최댓값 ≥ 8**로 잡았더니 그림자·안티앨리어싱의
>   옅은 꼬리(편차 합 ~20)까지 콘텐츠로 분류돼 bbox가 화면 전체로 확장(5장 전부 trim≈원본).
>   사전 수동 측정과 같은 **합 > 30** 기준으로 교체해 정상. 흰 카드 내부(배경과 편차 합 ~21)는
>   여전히 직접 잡지 못하지만 카드 테두리·그림자가 잡히므로 bbox 용도에 충분하다는 근거를
>   docstring에 남겼다.
> - **README**: 정문(세 축 앞)에 2×2 표 4장 — 프로젝트 목록 → 원고 작업공간 → 기억 후보
>   목록·검토(작업 흐름 순). **로그인은 제외** — 정보량이 가장 적은 화면인 데다 콘텐츠가 중앙
>   카드(544px)라 **×1.48 업스케일**이 유일하게 크다(나머지 4장은 ×0.76~1.06). 정규화 규칙과
>   나머지 1장 위치는 표 아래 `<sub>`로 안내.
> - **검증**: 출력 5장 전수 — 치수 1200×800·모서리 (241,248,255)·대비 픽셀 존재(2.6~4.4%); 트림
>   bbox가 사전 수동 측정(4px 그리드 샘플링)과 ±수십 px 정합(전수 스캔이라 더 빡빡하게 잡힘);
>   README 링크 5종(이미지 4·스크립트 1) 파일 존재 확인; 임계 수정 후 재실행이 초판 출력물을
>   입력으로 삼지 않은 것으로 멱등 확인.
> - **다음 후보**: GitHub 렌더 육안 확인(2열 폭·한글 파일명 렌더) — 오너; 로그인 화면 재사용
>   시에는 큰 창에서 재스샷 권장(업스케일 회피). 소개 문서에서 5장 전부 재사용 가능.

> **[세션 1] 포트폴리오 설명문 신설 (문서만, 코드 0줄 · SoT 무변).**
> 오너 지시: *"퍼블릭 전환했으니까 일단 포트폴리오 설명문 작성해봐"* — 08-23 예고("설명문은
> 내일")의 시행이고, GitHub public 전환(오너 설정, 같은 날) 직후 첫 작업.
> 산출물 [`docs/portfolio.md`](../portfolio.md)(9절 · 203줄) + 정문 연결 둘.

## Goals

- 공개된 저장소를 **평가자**(채용·코드 리뷰)가 시간 예산 안에서 훑을 수 있게 한다 —
  08-02 오너 요청(*"이 레포를 포트폴리오 용으로도 쓸 거니까 내 결정·검증 계획 등을 평가자가
  보기 깔끔하게"*)이 만든 README 정문의 **위에 읽기 안내·해설 층**을 얹는다.
- README(`세 축 = 저장소 지도`)와 분업한다 — 설명문은 **서사**(무엇이 어려웠고 어떻게 판단했나)
  와 **증거 링크**를 담당하고, 살아 있는 숫자는 README·SoT가 정본인 채로 둔다.

## User Decisions and Rationale

- **오너(2026-08-25)**: "일단 작성해봐" — 초안을 먼저 보고 반응하는 흐름. 위치·언어·분량은
  구현자 판단으로 위임됐다(아래 Decisions).

## Decisions (구현자 판단)

- **위치 = `docs/portfolio.md`.** 최상위는 정문 다섯(README·HANDOFF·CHANGELOG·CLAUDE·LICENSE)이
  쓰는 자리고, 문서의 집은 `docs/`다. 도달성은 정문 연결로 해결한다(아래).
- **구성 = 9절**: ①무엇을 ②어떻게(작업 방식 — 1인 오너+AI 세션, 검증 문화의 *이유*) ③규모
  ④시간 예산별 경로(5분·30분·2시간) ⑤하이라이트 6 ⑥스토리 셋 ⑦스택 ⑧정직한 한계 ⑨증거 지도.
  ⑤⑥이 이 문서의 고유 가치다(README에 없는 해설), ③⑦은 정문 요약의 링크 환류.
- **숫자는 전부 기준 시점(2026-08-25) 실측**을 붙이고 살아 있는 정본을 README·SoT로 지정한다.
  검증 기록 수처럼 가드가 묶는 값도 이 문서에서는 **날짜 스냅샷**으로만 쓴다 — 스냅샷 성격의
  문서를 `_COUNT_CLAIMS`에 묶으면 기록마다 여기까지 고쳐야 한다(HANDOFF가 여러 차례 기록한
  "세는 사람이 셋이면 갈라진다"의 예방이 목적이지 확산이 아니다). 재현 명령 3종을 본문에 뒀다.
- **스토리 셋 선정** — README "평가자를 위한 짧은 경로"가 이미 보여주는 D8-7 사례와 겹치지
  않게: 8.2b 잠금(회귀가 결함을 정상으로 고정) · 인증 두 겹(관측 불가능한 한 겹 제거) ·
  R-e 다이어트(−72.8%). 셋 다 SoT 해당 행(v1.7.87·v1.7.61)·HANDOFF와 대조했다.

## Completed work

- **`docs/portfolio.md` 신설** — 위 구성 그대로. 작업 방식 절(§2)이 "AI가 쓴 코드를 어떻게
  신뢰하는가"에 대한 이 저장소의 답(양방향 가드·뮤테이션·독립 검증)을 서사로 놓은 자리다.
- **정문 연결 둘**: 최상위 `README.md` 세 축 표 바로 아래에 평가자용 안내줄(링크) ·
  `docs/README.md` 문서 지도에 "포트폴리오" 행(맨 위).
- **선례 준수**: `test_product_name.py`의 정문 스캔 범위 밖이지만 제품명 "에-라잇"을 그대로
  썼다. LICENSE 링크의 채용·평가 환영 문구와 §8 한계가 같은 사실을 말한다.

## Verification

- **링크 전수**: `docs/portfolio.md`의 `.md` 링크 38건 + 디렉터리 링크 3건 전부 해석
  (깨진 링크 0 — 스크립트로 실측). README·docs/README.md 편집분도 0.
- **문서 인덱스 가드**: `tests/test_docs_indexes.py` **13 passed**(stdlib unittest로 직접
  실행) — 최상위 README 링크 해석·숫자 주장 4종·판정 분포·SoT 버전 칸 전부 무변 확인.
- **사실 대조**: 아래 전부 그 자리에서 재측정·원문 대조 — 커밋 794(`git rev-list --count`) ·
  첫 커밋 2026-06-24 · plans 111/브리프 93 · 검증 256건/56일 · SoT v1.8.4 · compose 서비스 10 ·
  Python 35,818줄·TS 16,585줄(테스트 제외) · 회귀 2,506/338(HANDOFF 기준선) · operation 87은
  SoT v1.8.4 행("operation 87 무변")에서(77은 낡은 수 — HANDOFF "지금 상태" 절 일부 행이 아직
  77을 적고 있어 다음 인계 때 실측 갱신 대상) · 스토리 수치는 SoT v1.7.87·v1.7.61 행과 대조.
- **오타 2건 자체 수정**(줔→줄·젱용→저장) — 게이트 전에 통독으로 잡았다.

## 다음 세션 후보

- 오너 초안 검토(문서 방향·톤 확인) 후 보정.
- HANDOFF "지금 상태" 절의 낡은 operation 수(77) 갱신 — 이번에 발견했으나 본 슬라이스
  범위 밖으로 뒀다(무관 줄 손대지 않음).
