# 2026-09-12 작업 로그

## 세션 63 — Phase W1a: 모바일 도구가 원고를 가리던 것 해소 (오너 지시 "핸드오프 읽고 프론트엔드 스타일 변경 다음 작업 진행해줄래?")

### Goals

- 핸드오프의 프론트 스타일 다음 작업인 Phase W1a를 구현하고, 모바일 원고 가림과 도구 접근성을 실제 React 앱에서 확인한다.

### Completed work

- `frontend/src/drafts/DraftEditor.tsx`: 같은 도구 선택 nav를 원고 앞에 옮겼다. 도구를 복제하거나 드로어를 재마운트하지 않는다. 라벨·이벤트·배지·ref는 유지한다.
- `frontend/src/styles.css`: 48rem 이하에서 도구가 일반 문서 흐름을 차지한다. 42rem 이하 2열, 그 위 4열이며 기존 버튼 최소 높이 4.25rem을 유지한다. 데스크톱 fixed 우측 배치·블루 토큰·페이지 폭은 유지한다.
- Phase W 계획과 HANDOFF의 다음 작업을 W2로 갱신하고 CHANGELOG에 사용자에게 보이는 배치 변경을 기록했다. API·저장 계약은 바뀌지 않아 SoT 버전은 올리지 않는다.

### Issues found

- 원인: 고정 우측 도구는 모바일의 좁은 원고와 같은 좌표를 차지했다. 가로 넘침이 없어도 가림은 발생하므로 브라우저에서 두 요소의 사각형 교차를 검사했다.
- 패턴 스윕: `styles.css`의 `position: fixed`·`right: 0` 전수 검색과 blame을 확인했다. 도구는 `42e91cd2`에서 fixed, `ca1a833e`에서 버튼 확대. 다른 fixed인 `.workspace-rail`(980줄, `42e91cd2`)은 사용자가 열 때만 원고를 덮는 의도된 드로어다. `.session-panel`(263줄, `444b1b45`)은 계정 메뉴의 absolute 배치라 같은 상시 원고 가림 패턴이 아니다. 추가 동일 결함은 발견하지 못했다.
- 이 환경의 Vite 파일 감시가 CSS 수정 뒤 옛 모듈을 제공했다. HTTP 응답에서 확인하고 서버를 재시작해 최종 2열/4열 배치를 검증했다.
- 검증 환경에 한글 폰트가 없어 처음에는 네모 글리프가 나왔다. 기존 로컬 Windows 맑은 고딕을 임시 fontconfig로 검증용 Chrome에만 제공했다. 앱의 서체 제공 문제는 해결된 것이 아니며 W2 범위다. 폰트 파일은 저장소에 넣지 않았다.

### Decisions — User Decisions and Rationale

- 사용자 요청: 핸드오프를 읽고 프론트엔드 스타일 변경의 다음 작업을 진행한다. 기록된 순서에 따라 W1a를 이번 슬라이스로 삼고 W2~W4까지 범위를 확대하지 않았다.
- 구현 선택: 모바일 도구를 원고 앞의 일반 흐름에 둔다. 스티키 도구는 스크롤 중 원고를 다시 가릴 수 있다. 대가로 모바일에서는 긴 원고 아래에서 도구를 쓰려면 페이지 위쪽으로 돌아가야 한다. 데스크톱은 계속 고정 우측 도구다.

### Verification

실제 Vite React 앱 `/projects/p1/drafts/d1`을 headless Chrome CDP로 검사했다. API는 테스트용 프로젝트·원고·버전 fixture로 대체하고 외부 요청은 차단했다. 긴 원고는 한글/영문/숫자 혼용 2,220자다. 운영 데이터·유료 생성·저장 API는 호출하지 않았다. 보조 예산/쿼터 API는 503 fixture여서 생성 성공 흐름의 실사용 검증은 아니다.

| 화면 | 도구 폭 | 본문 폭 | 버튼 크기(첫 버튼) | 결과 |
|---|---|---|---|---|
| 320×844 긴 원고 | 284.8px | 250.8px | 135.4×68px | 2열, 겹침·가로 넘침 없음 |
| 390×844 긴 원고 | 339.8px | 305.8px | 162.9×68px | 2열, 겹침·가로 넘침 없음 |
| 768×1024 긴 원고 | 689px | 625.6px | 166.8×68px | 4열, 겹침·가로 넘침 없음 |
| 1440×900 긴 원고 | 98.6px | 1054px | 89.6×68px | fixed 우측 유지, 겹침·가로 넘침 없음 |
| 390×844 빈 원고·읽기 전용 | 339.8px | 305.8px | 162.9×68px | 두 상태 모두 정상 배치 |

- 브라우저 가드는 under 방향으로 모바일 도구/본문 교차·넘침·도구의 본문 앞 배치를, over 방향으로 데스크톱 fixed 유지·버튼 존재·드로어 열림/Escape 닫힘/포커스 복귀를 확인했다. 저장 버튼은 스크롤 후 `elementFromPoint`로 가리지 않는지 검사한다. 읽기 전용에는 저장 버튼이 없다.
- 완료 배지와 패널 상태 유지의 실제 생성 흐름은 기존 `DraftEditor.test.tsx` 회귀로 확인한다. 스타일값만 복사하는 새 단위 테스트와 프로덕션 코드 변이는 추가하지 않았다.
- 한계: 실제 모바일 기기/소프트 키보드, 운영 데이터, 장시간 피로도는 미확인이다. 한글 렌더링은 위 로컬 검증 서체 환경의 결과이며 모든 기기에서 같은 서체를 보장하지 않는다. 독립 검증 판정은 부여하지 않는다.

자동 검증 결과(2026-09-12, 이어받은 세션에서 실시):

- 초점 회귀 `designTokens`·`typeScale`·`pageLayout`·`scratchPadCss`·`buttonAppearance`·`DraftEditor`: **6파일 87 passed**(EXIT=0).
- 프런트 전수 `npx vitest run --reporter=basic`(파일 캡처): **478 passed / 41 files · EXIT=0** — 기준선과 같은 수이고 알려진 플레이크 재발 없음. 셀 수를 움직인 변경이 없어 기준선 갱신 대상도 아니다.
- `npx tsc --noEmit` EXIT=0 · `npm run build` 성공.
- 문서 가드 `test_docs_indexes`·`test_repo_hygiene`·`test_design_token_provenance`: **31 passed / 1019 subtests passed** · `git diff --check` 이상 없음.

### Next steps

- Phase W2: 한글 서체 로딩·fallback과 원고/UI 역할 점검. 웹폰트 의존성 갈림길은 계획의 결정 브리프 지침을 따른다.
- W3 서재, W4 보조 표면·마감. 배포와 푸시는 오너 소관이다.

## 세션 64 — Phase W2: 한글 타이포 — 원고 명조 폴백과 UI 고딕 고정 (오너 지시 "작업 AI가 작업하다 지쳐서 진행하던 작업 마무리좀 해줄래? 핸드오프 읽고 프론트엔드 스타일 변경 다음 작업 진행해줄래?")

세션 63의 마무리(전수·기록·커밋 `bc78ed6`)를 이어받아 계획의 다음 슬라이스 W2를 구현했다.

### Goals

- 원고 본문의 한글이 명조로, UI의 한글이 고딕으로 결정론적으로 렌더링되게 한다. 라틴·숫자 외형은 바꾸지 않는다.

### Completed work

- `frontend/src/styles.css` `.editor-form textarea`(원고 본문 — 편집·읽기 전용이 같은 규칙): serif 스택에 한글 명조 후보(바탕·AppleMyungjo·나눔명조·Noto Serif KR/CJK KR)를 generic `serif` 앞에 추가했다. 라틴·숫자는 Georgia(설치 환경)에서 먼저 매치되어 그대로다.
- `:root` UI 스택: `system-ui` 뒤에 한글 고딕(맑은 고딕·Apple SD Gothic Neo·나눔고딕·Noto Sans CJK KR)을 추가했다. 라틴은 system-ui(Segoe/SF)에서 먼저 매치되므로 외형이 변하지 않고, 한글 음절만 위 이름들에서 해소된다.
- 제목 등 나머지 serif 표면(~20곳, 같은 리터럴)은 이번에 바꾸지 않았다 — 계획 지침 "제목까지 무조건 명조로 바꾸지 않는다. 원고 중심의 작은 표면부터 비교한다".
- 웹폰트는 도입하지 않았다(파일 출처·라이선스·전송량·자체 호스팅은 오너 결정 갈림길). 외부 폰트 서비스 호출은 추가하지 않았다.

### Issues found

- 원인: serif 스택 `Georgia, "Times New Roman", serif`에 한글 서체가 없어 원고 맥락의 한글 음절이 무작위 OS 폴백으로 떨어졌다. 세션 62(W1) 검증 환경에서 제목 한글이 나눔고딕 — 고딕 — 으로 렌더링된 것이 이 결함의 관측이었다(명조 맥락에 고딕 한글).
- 패턴 스윕: `styles.css`의 `font-family` 선언 31곳 전수 확인. serif 리터럴 ~20곳 중 이번에 고친 것은 원고 본문 1곳뿐이고 나머지는 제목·인용·후보본 등 "안 바꾼 표면"이다. `system-ui, sans-serif` 리터럴 7곳은 serif 표면 안의 메타 라벨용 의도된 UI 리셋이라 그대로 뒀다.
- 세션 63 함정의 변형 재발: 5173 포트에 죽은 세션의 Vite가 남아 **옛 CSS**를 서빹하고 있었다. 새 서버(5174)의 응답에 신규 스택(`Batang`)이 있는지 대조해 가려내고 낡은 서버를 정리한 뒤 5174로 검증했다.
- 한글 폰트가 아예 없는 시스템에서는 두자(tofu)가 된다 — 시스템 폰트 접근의 근본 한계이고 옛 스택과 거동이 같다(아래 미설치 실측). 모든 기기에 동일 외형을 보장하려면 자체 호스팅 웹폰트뿐이다.

### Decisions — User Decisions and Rationale

- 구현 선택: 계획의 W2 착수 지침이 "로컬 시스템 서체 보완은 의존성 없이 검토 가능"이라고 명시했으므로 시스템 서체 스택 완성으로 슬라이스를 닫았다. 웹폰트 도입은 기기 간 원고 외형 일관성이 필요해질 때 오너 결정 브리프로 연다.
- 제목의 한글은 이번에 고딕 폴백 그대로 뒀다. 원고 본문의 명조 전환을 먼저 보는 것이 계획의 "작은 표면부터 비교"에 맞다.

### Verification

실제 Vite React 앱 `/projects/p1/drafts/d1`(390×844 기준)을 CDP로 검사했다. API fixture·외부 요청 차단은 세션 63과 같다(하네스는 이어받아 적응). 폰트 환경을 둘로 나눠 측정했다: **한글 폰트 제공 환경**(바탕+맑은 고딕을 임시 fontconfig로 검증용 Chrome에만 제공 — 폰트 파일은 저장소에 넣지 않음)과 **무한글 폰트 환경**(시스템에 한글 폰트 0개, `fc-list :lang=ko` 실측).

탐침 비교 — 같은 텍스트를 신·구 스택으로 렌더해 `CSS.getPlatformFontsForNode`로 실제 사용 글꼴을 읽었다(한글 폰트 제공 환경):

| 탐침 | 실측 | 뜻 |
|---|---|---|
| 원고 한글, 새 스택 | **Batang 8자** + Liberation Serif 2자(공백) | 한글 → 명조 전환 |
| 원고 한글, 옛 스택 | Malgun Gothic 8자 | 옛 결함(명조 맥락에 고딕 폴백) 실증 |
| 원고 라틴·숫자, 신/구 | Liberation Serif 11자로 동일 | 라틴·숫자 불변 |
| UI 한글(도구 버튼 스택) | Malgun Gothic 7자 | 고딕 결정론적 해소 |
| 제목(H1) 한글 | Malgun Gothic 5자 | 안 바꾼 표면 무변 |

- Georgia는 이 검증 환경에 없어 라틴이 fontconfig 대체(Liberation Serif)로 렌더링됐다. 라틴 불변 단정은 신·구 스택 간 동일성으로 세웠다.
- 줄바꿈: 280px 폭 탐침에서 한글/영문/숫자 혼용 문장이 3줄로 래핑(100px > 행 높이 33.3px). 문서 가로 넘침은 320/390/768/1440 전부 없음, W1a 배치(모바일 static·데스크톱 fixed) 유지.
- 미설치(무한글 폰트) 환경: 신·구 스택의 거동이 동일(한글이 Liberation Serif/DejaVu Sans로 귀속 = 두자 렌더링)이고 레이아웃 깨짐·넘침 없음 — 회귀 없음. 두자 해소는 웹폰트 몫이다.
- 스크린샷 육안(한글 폰트 환경, 390px): 원고는 명조(획 대비 있는 바탕), UI·제목은 고딕 — 역할 대비 성립. 두자·겹침·넘침 없음.
- 한계: 실제 모바일 기기·소프트 키보드 미확인. 바탕·맑은 고딕에 굵은 변형이 없어 `font-synthesis: none` 하에서 굵은 한글 제목은 기본 굵기로 난다(기존과 같음). 독립 검증 판정은 부여하지 않는다.

자동 검증 결과:

- 스타일 가드 `designTokens`·`typeScale`·`pageLayout`·`scratchPadCss`·`buttonAppearance`·`disabledState`·`observability/chartColors`: **7파일 30 passed**(EXIT=0).
- `npm run build` 성공(8.08s).
- 프런트 전수 `npx vitest run --reporter=basic`(파일 캡처): **478 passed / 41 files · EXIT=0** — 기준선과 같은 수, 플레이크 재발 없음.

### Next steps

- Phase W3: 작품 서재(프로젝트/원고 목록의 제목·행 간격·행동 위계).
- (오너 갈림길로 남김) 자체 호스팅 웹폰트 — 기기 간 원고 외형 일관성이 필요해질 때 결정 브리프로 연다. 한글 폰트가 없는 기기의 두자도 이 갈림길에서만 해소된다.
- 제목 등 나머지 serif 표면의 한글 서체는 원고 명조가 자리 잡은 뒤 별도 비교로 정한다(Phase W 마감 무렵).

## 세션 65 — Phase W3: 작품 서재 — 행동 위계와 좁은 화면 목록 행 (오너 지시 "다음작업 이어서 진행해줘.")

### Goals

- 프로젝트 목록과 장/장면 목록의 제목·행 간격·행동 위계를 개선한다. 계획 지침 준수: 있는 데이터(작품명·보관 여부·장면 상태)만 쓰고 없는 데이터 꾸미기·API 확장은 하지 않는다.

### 사전 조사(변경 전 실측, 2026-09-12)

- CDP 브라우저 조사(긴 제목·보관 상태 fixture): ① **320px에서 장/장면 목록 가로 넘침**(문서 scrollWidth 322 > 뷰포트) ② 행마다 ↑↓/삭제 버튼과 상태·순서 배지가 제목과 경쟁 ③ "장 순서 N"/"장면 순서 N" 배지는 순서가 목록 순서·화살표로 자명한 잡음 ④ 보관 상태가 "(보관됨)" 괄호 텍스트라 형태 구분이 없음.
- 스크린샷 육안: 모바일에서 행이 협소하고 제목 위계가 상실, 보관 행이 활성 행과 같아 보임.

### Completed work

- `frontend/src/drafts/DraftList.tsx`: "장 순서 N"·"장면 순서 N" 배지 제거. 순서 정보는 `<ol>` 순서·↑↓ 컨트롤이 이미 전달한다.
- `frontend/src/projects/ProjectList.tsx`: "(보관됨)" → "보관됨"(구분은 칩 형태가 담당).
- `frontend/src/styles.css` `.status-badge`: 텍스트에서 **칩**으로(옅은 accent 배경·원형 모서리·`--type-micro`) — 색만이 아니라 형태로 상태를 전달한다. `0.74rem` 리터럴을 계단으로 옮기며 `typeScale.test.ts` 이관 목록에 등재했다.
- `styles.css` 모바일(≤48rem): `.resource-row:not(.note-row)` 두 줄 배치 — 첫 줄 제목(링크·장 제목), 둘째 줄 상태·배지·컨트롤. 메모 행은 자체 레이아웃이라 제외.
- `styles.css` `.scene-creation-form input` `min-width: 0`: flex 항목 기본 `min-width:auto`가 320px에서 폼을 17px 넘치게 하던 것(요소 사각형 실측으로 특정)을 해소.

### Issues found

- 패턴 스윕: `.resource-row`를 4개 화면이 공유한다(프로젝트·내 허브·장/장면·장면 메모). 메모 행(`.note-row`)은 장면 메모 슬라이스가 검증한 자체 레이아웃이므로 `:not(.note-row)`로 제외해 무변을 지켰다.
- **검증 서버 신선도 검사법 정정**: dev 서버의 `/src/styles.css`는 **한 줄짜리 HMR JS 모듈**로 서빙돼 `grep -c`(줄 수)로는 항상 1이 나온다 — 발생 수(`grep -o | wc -l`)로 세야 한다. 세션 63·64가 만난 "옛 서버" 의심은 이 셈법 오류였을 수 있다(첫 사례는 W1a 세션이 응답 내용으로 확인했고, 이번 세션의 5174 판정은 셈법 오류였다). HANDOFF 함정 줄을 이 셈법으로 보강했다.
- 육안 검토가 지적한 "폼과 목록 사이 이중 가는 선"은 2rem 간격의 평행선인 기존 패턴이라 쌓인 결함이 아니다 — W4(마감)에서 볼 미관 사항으로 남긴다.
- 데스크톱 ↑↓/삭제 버튼 스타일은 유지했다. 모바일 협소함의 원인은 한 줄 배치였고 해소됐으며, 버튼 재디자인은 이 슬라이스 완료 조건 밖이다.

### Decisions — User Decisions and Rationale

- 배지 제거·칩화·두 줄 배치는 Phase W 방향("보조 정보는 작고 차분하게"·"색만으로 상태 전달 금지")과 완료 조건 안에서 구현자가 정했다. 수정 시각·진행률 같은 없는 데이터는 계획 지침대로 꾸미지 않았다.

### Verification

실제 Vite React 앱을 CDP로 검사했다(API fixture·외부 차단, 한글 폰트 제공 환경).

- 가로 넘침: 프로젝트 목록·장/장면 목록 모두 **320/390/1440 전부 없음**(변경 전 320 넘침 요소 실측 목록이 빌 때까지 반복).
- 키보드: Tab 포커스 순서는 변경 전과 동일(배지는 포커스 대상이 아니었다).
- 빈 목록: 빈 프로젝트 목록 렌더 정상·넘침 없음.
- 스크린샷 육안: 모바일 두 줄 행으로 제목이 지배적이 되고 상태·컨트롤이 둘째 줄에 정돈, 칩 배지가 보관 상태를 형태로 구분, 겹침·잘림 없음(1440 데스크톱 포함).
- 한계: 실기기·소프트 키보드 미확인. 장면 메모 화면은 제외 선택자로 무변을 가정(회귀 43셀 초록, 시각 확인은 W4 스윑 몫).

자동 검증 결과:

- 초점 회귀 `typeScale`·`buttonAppearance`·`pageLayout`·`designTokens`·`disabledState`·`DraftList`·`ProjectList`: **7파일 45 passed** · 장면 메모 3파일 **43 passed**.
- `npm run build` 성공.
- 프런트 전수 `npx vitest run --reporter=basic`(파일 캡처): **478 passed / 41 files · EXIT=0** — 기준선과 같은 수(단정을 줄였지 셀 수는 무변), 플레이크 재발 없음.

### Next steps

- Phase W4: 보조 표면과 마감(드로어·설정·계정, 데스크톱/모바일 시각 스윕, 폼-목록 사이 평행선 미관).

## 세션 66 — Phase W4: 보조 표면·마감 — 페이즈 종결 (오너 지시 "마지막도 해줘")

### Goals

- 보조 표면(드로어 3종·설정 탭·계정·검토함·메모·약관·로그인·관리·관측)에 W1~W3 공통 변경이 무엇을 깨뜨렸는지 실측 스윕하고, 유예됐던 육안 항목(비활성 버튼 색·표 넘침 등)을 이 자리에서 판정한다. 모든 화면을 같은 카드로 감싸는 일은 하지 않는다(계획 지침).

### Completed work

- `frontend/src/styles.css`: 모바일(≤48rem)에서 **데이터 표(관측 `.kpi-table`·검토 `.diff-table`)이 자기 상자 안에서 가로 스크롤**하게 했다(`display:block; overflow-x:auto`). 9열 KPI 표가 375px 뷰포트를 215px 넘겨 문서 전체가 흔들리던 실측 결함. 문서용 `.legal-table`(약관)은 열이 좁고 스윕 통과라 제외.
- `frontend/src/me/PersonalHubPage.tsx`: "(보관됨)" → "보관됨". W3이 ProjectList만 고쳐 허브가 괄호 리터럴로 남아 있던 형제 표면 어긋남("한쪽만 고치면 다시 어긋난다" 패턴의 W3 실수)을 바로잡았다. 테스트는 괄호 문자를 고정하지 않았다.

### 스윕 결과(변경 전 → 변경 후)

14개 화면 × 1440/768/390/320 넘침 측정(CDP·한글 폰트 환경·fixture):

- **변경 전**: 관측 화면만 320·390 넘침(9열 KPI 표 590px). 나머지 13개 화면(편집기+드로어 3종·설정 3탭·계정 허브·검토함·메모·약관 2·로그인·관리) 전 폭 무결.
- **변경 후**: **14개 전부 넘침 없음**.
- fixture가 503으로 떨어진 보조 표면(관리 감사·가입 신청·quota 정책, 설정-활동 탭, 편집기 memory/note 단건)은 오류 면만 스윑했다 — 내용 행은 회귀로 덮인다.
- 스크린샷 육안: 메모 드로어의 칩 배지(장 제목)가 메타데이터로 자연스럽다 · 설정-개요 폼 390px 정상 · 관측 표가 상자 안 스크롤로 바뀌고 문서가 흔들리지 않는다.

### Issues found — 유예 육안 항목 판정

- **비활성 버튼 색(Phase 10 ④)**: 육안 확인 완료 — 흐린 청회색으로 "누를 수 없음"은 분명히 전달되나 "농도가 아니라 색"이라는 종전 지적과 일치하는 외형이다. 이 축은 `docs/plans/10_disabled_contrast.py`로 색별 대비를 실측하는 **별도 추적 부채**다(`--disabled-opacity: 0.45` 토큰 주석에 그렇게 지정돼 있다) — W4에서 서둘러 고치지 않았다.
- **좁은 화면 표 넘침(프론트 스타일 나머지 축)**: 실체는 관측 KPI 표였고 이번에 닫았다. `.diff-table`은 같은 해부학이라 같은 규칙으로 묶었다(검토 상세 화면은 이번 스윕 범위 밖 — 패턴 스윕 규칙에 따른 선제 적용).
- 메모 드로어 닫기 버튼이 텍스트형이라 터치 타깃이 작아 보인다는 관찰이 있으나 차단 사항은 아니다 — 별도 요청이 있을 때 본다.
- 폼-목록 사이 평행선(서재 화면, 2rem 간격)은 기존 패턴이라 그대로 뒀다 — 위계를 해치지 않는다.

### Decisions — User Decisions and Rationale

- 표 스크롤·괄호 제거 외에 화면을 바꾸지 않았다: 스윕 실측이 결함을 1건(KPI 표)만 밝혔고 "모든 화면을 같은 카드로"는 계획이 금지한 일이다.

### Verification

- 스윕: 위 표(14화면 × 4폭). 자동: 초점 회귀 `PersonalHubPage`·`typeScale`·`buttonAppearance`·`pageLayout`·`designTokens`·`chartColors`·`ReviewInboxDetail` **7파일 55 passed** · `npm run build` 성공.
- 프런트 전수 `npx vitest run --reporter=basic`(파일 캡처): **478 passed / 41 files · EXIT=0** — 기준선 유지, 플레이크 재발 없음.

### Next steps

- **Phase W 전 슬라이스(W1·W1a·W2·W3·W4)의 시각 확인을 마쳤다 — 페이즈를 완료로 올린다.** 남는 것: 웹폰트 갈림길(오너) · 제목 등 serif 표면의 한글 서체 비교 · 비활성 버튼 색 실측 부채.
- 배포·푸시는 오너 소관.

## 세션 67 — 랜딩 ② 승격 재검(판정 승격) · 백엔드 회귀 기준선 전수 확인 (오너 지시 "핸드오프 읽고 다음작업 진행해줘")

HANDOFF Next Tasks 1번(랜딩 ② 승격 재검 — 검증 세션 몫)과 회귀 기준선 절의 "다음 세션의 첫 일"(백엔드 3026/1/4154 유도값 전수 확인)을 한 세션에서 닫았다. **프로덕션 코드 무변.**

### Completed work

- **백엔드 전수 재실측**: test-mongo PRIMARY 확인 후 `python3 -m pytest -q` — **3026 passed / 1 skipped / 4156 subtests · EXIT=0**(373초). **skip 1 = live Chroma 뿐** (판정 규칙). 선행 유도값 4154 와 subtest +2 차이를 귀속했다:
  - 유도 시점(세션 60) 이후 커밋이 새 문서 둘을 만들었다 — `docs/plans/frontend-writing-studio-phase.md`(`dede62b`) · `docs/daily_logs/2026-09-12/work_log.md`(`bc78ed6`) — `git log --diff-filter=A --name-only 33b67e2..HEAD -- 'docs/**' '*.md'` 실측 둘뿐.
  - `test_repo_hygiene` 는 문서당 1 subtest 를 내므로 +2 가 정확히 설명된다. 교차 확인: 문서 가드 7종 **59 passed / 1138 subtests** = 세션 60 실측 58/1114 + 정책 가드 +1셀/+22(`d5c63fb`·`9ca0e8b`) + 문서 둘 +2.
  - **결론: 회귀가 아니라 유도값이 낡은 것 — 기준선은 4156.** HANDOFF 회귀 줄 정정(유도 주석 → 전수 실측).
- **랜딩 ② 승격 재검**: 폐쇄 커밋 `7be433e`(LB1+H3)의 변이 6종을 재적용해 전건 일치를 확인하고 선행 기록 판정 **조건부 합격 → 합격 승격**. 기록 [`verifications/2026-09-12/landing_l2_promotion.md`](../../verifications/2026-09-12/landing_l2_promotion.md) · 검증 인덱스 등재(301건 · 합격 201/조건부 95/불합격 5 — 분포 문장 갱신).
- 절차는 매 변이마다 프리플라이트 `git status --short` 무출력 → 편집 → 초점(`src/legal` + `src/App.test.tsx`, 파일 캡처) → `git -C <절대경로> checkout -- <절대경로>` → 무출력 확인. 기준 초록 45/45 선확인.

### Issues found

| 문제 | 원인 | 해결 | 결과 |
|---|---|---|---|
| 유도 기준선 4154 가 전수와 +2 어긋남 | 유도를 만든 세션(60) 이후 커밋이 문서 둘을 추가 — 유도 규칙("백엔드 소스 무변")은 지켜졌지만 **유도 시점 이후의 문서 증가는 반영 못함** | 새 문서 열거 + 문서 가드 7종 교차 측정으로 귀속 | 기준선 4156 정정 — 회귀 아님 |
| MO-8 첫 측정이 Startup Error(reporter 로드 실패) | 백엔드 가드 실행으로 셸 cwd 가 저장소 루트로 옮겨진 채 `npx vitest` — 루트엔 vitest 설정이 없어 npx 가 다른 vitest를 잡음(HANDOFF 함정 "프런트는 frontend/ 안에서"의 초점 버전) | `frontend/` 안에서 재적용·재측 | 유효 측정 45/45 초록 |

### Verification

| 변이 | diff | 세션 60 기록 | 실측 |
|---|---|---|---|
| MO-6a (under) | `AuthStatus` 의 `<LegalFooter />` 제거 | 1실패 | **1실패**(LB1 셀) |
| MO-6b (under) | 가입 접수 얼굴의 `<LegalFooter />` 제거 | 1실패 | **1실패**(같은 셀) |
| ML-3b (under) | 로그인 폼 얼굴의 `<LegalFooter />` 제거 | 2실패 | **2실패**(기존 두 셀) |
| MH-3a (under) | 머리말 스코프를 문서 전체로 되돌림 | 1실패 | **1실패**(H3 셀) |
| MH-3b (over) | `근거:` 걷기 제거 | 2실패 | **2실패**(H3+링크 셀) |
| MO-8 (등가) | 경계를 첫 `## ` 로만 | 초록 | **45/45 초록** |

- 백엔드 전수·문서 가드는 위 표와 같고, 프런트는 이번 세션에서 소스 무변이라 세션 66 실측 478/41 이 유효(재실측 불요 — 유도 정당 조건의 프런트 판).
- **기록 추가 뒤 문서 가드 7종 재실측: 59 passed / 1140 subtests(+2 — 검증 기록 파일 1 · 인덱스 행 1)** → 인계 기준선은 **4158**(전수 4156 + 2 유도)로 HANDOFF·README 에 반영했다. 가드가 처음에 7실패를 낸 것도 같은 축이었다 — 루트 README 분포 문장(합격 199·96·5 → 201·95·5)·건수(300건/71일치 → 301건/72일치)·②행(4,154 → 4,158)·`docs/README.md` 건수·백분율 서식(`round()` 정수라 32% 무변)을 함께 고쳤다.

### Next steps

- **랜딩 ③(본문)·④(가입 동의 게이트)** 가 구현 세션의 다음 축이다(NEXT 표 2번) — "한 창에서 본다"(S-2 nginx 헤더·AdSense 화면별 제어 포함).

## 세션 68 — 랜딩 ③ 본문 · ④ 가입 동의 게이트 · S-2 nginx 헤터 (오너 지시 "핸드오프 읽고 다음작업 진행해줘")

HANDOFF Next Tasks 2·4번(한 창)을 구현했다. **같은 저장소에서 다른 작업 AI가 11번 Slice 5(정책 §8 → §6 승격)를 동시 진행했다** — 법률 문서 축(방침 머리말 고지·`legal/README.md` §8 표)은 그쪽에 양보하고 내 기록에만 넘겼다. 커밋 4개: `58a8ac9`(백엔드 게이트) · `86a4de7`(프런트 게이트) · `f36c7bd`(랜딩) · `b6fd464`(nginx 헤더).

### Goals

- 방침 제3조(가입 절차 확인·동의 시각·버전 기록)의 게이트를 백엔드·프런트 양축으로 구현하고, 랜딩(D1=ⓒ)을 루트의 익명 얼굴로 세운다. S-2의 nginx 헤더 축을 같은 창에서 닫는다.

### Completed work

- **백엔드**: `TERMS_VERSION = "1.0"` 계약 상수(`auth/users.py`) · `User` 에 `terms_agreed_at`·`terms_version_agreed`(None 기본 — 게이트 이전 행·관리자 생성 계정 마이그레이션 무변, 소급 동의 금지) · `request_signup` 이 `agreed_terms_version` 을 시행 상수와 대조(누락·옛 판본 → 400 단일 얼굴, 해셔 앞)하고 **서버 시계·서버 상수로만** 스탬프 — 신규·거절 재요청(새 동의) 두 갈래. `SignupRequest` 는 모델에서 관대(S-3 선례). `users_mongo.py` 매핑 동반.
- **핀 셀 상수화**: `tests/test_service_policy_contract.py` `_ENFORCED_VERSION = TERMS_VERSION`(docstring 예고 이행) · `frontend/src/legal/legalSource.test.ts` `ENFORCED_VERSION = TERMS_VERSION`.
- **프런트 게이트**: 가입 폼에 동의 확인란(약관·방침 새 탭 링크 — 폼 입력 보존), 미체크 제출 잠금, 요청 바디에 `agreed_terms_version`. `LegalFooter.tsx` 에 `TERMS_VERSION` 상수. `.consent-check` CSS(텍스트 입력 규칙 상속 차단).
- **랜딩**: `frontend/src/auth/Landing.tsx`(소개 블록 넷 — 원고 구조·기억·Gate·관측 + 정직한 한계 셋 — 승인제·원고 외부 전송·개인 프로젝트 + LegalFooter). `LoginMode` 에 `"landing"` 추가, `AuthGate` 가 `pathname === "/" && !sessionExpired` 일 때만 랜딩 — 라우팅 무변. 로그인 폼 h1 "쓴 것을 기억하는 집필 작업실"→"로그인", 랜딩 버튼 "로그인"(제출 버튼 "작업실 입장"과 이름 충돌 방지). typeScale 이관 목록에 랜딩 규칙 등재.
- **nginx**: server 레벨에 X-Frame-Options DENY·X-Content-Type-Options nosniff·Referrer-Policy 셋 `always`. 가드 `tests/test_frontend_nginx_headers.py`(server 레벨 배치 — depth 파서로 location 추출, always).

### Issues found

- **세션 만료가 랜딩에 안내를 삼키는 결함을 셀이 잡았다** — 만료 셀 셋이 "세션이 만료되었습니다" 문구를 못 찾아 재실패. 랜딩에 그 배너가 없으므로 만료 상태는 initialMode 에서 signin 직행시켰다(설계 수정).
- **버튼 이름 충돌**: 랜딩 입장 버튼을 처음 "작업실 입장"으로 지었다가 로그인 제출 버튼과 이름이 같아 역할 질의가 흔들렸다 — "로그인"으로 교체하고 Landing.tsx 주석에 근거를 남김.
- **정규식 location 제거가 server 레벨까지 삼켰다**(nginx 가드 초안) — greedy 매치가 location 사이 지시문을 먹어 add_header 3건이 사라짐. 중첩을 아는 depth 파서로 교체.
- 파이썬 일괄 삽입이 `userEvent.type(...)` 안의 anchor 에 끼어 5곳의 문법을 깨고 전역 치환이 제출 클릭 문장까지 바꿨다 — tsc·실행으로 발견해 수리(자동화 편집은 기계 검증 없이 쓰지 않는다).
- **다른 작업 AI(Slice 5)가 같은 tests 파일을 편집 중**(`test_auth_users.py` 헬퍼 리팩터·`test_service_policy_contract.py` 앵커 추가) — 백엔드 전수 카운트에 그 트리가 섞여 내 슬라이스 단독 귀속이 안 된다(아래 Verification 주석).

### Decisions — User Decisions and Rationale

- 오너: 핸드오프의 다음 작업(랜딩 ③·동의 게이트 ④, S-2 같은 창) 진행. 슬라이스 분할·범위는 구현자가 계획대로 나눴다(백엔드→프런트 게이트→랜딩→nginx).
- 오너: 다른 작업 AI가 11번 Slice 5 진행 예고 — 법률 문서 축(방침 머리말·§8 표)을 그쪽에 넘겨 충돌을 피했다.
- 오너: 완료 후 독립 세션 검증(서브에이전트)·검증기록 보강 지시 — 세션 말미에 수행(아래).
- 구현 선택: 동의 검증을 서비스 계층 400 으로(모델 422 금지 — signup 정책 거부의 단일 얼굴). 저장 값을 서버 상수로 통일(클라이언트 청구 저장 금지). 랜딩을 라우팅 무변으로(딥링크·관리자 리다이렉트 계약 보존). CSP 미포함(AdSense 심의 전 안전 지시문 작성 불가 — 오너 결정 대기 항목으로 명시).

### Verification

- 초점: 백엔드 인증 축 `test_auth_users`·`test_signup_throttle`·`test_service_policy_contract`·`test_auth_api` **261 passed / 1275 subtests** · nginx 가드 **2 passed / 6 subtests** · 프런트 초점(랜딩·게이트·스타일 가드) **70 passed** · `tsc --noEmit`·`npm run build` 초록.
- **전수**: 백엔드 **3034 passed / 1 skipped / 4166 subtests · EXIT=0**(340.7초 · skip 1 = live Chroma 뿐). 프런트 **482 passed / 41 files · EXIT=0**(파일 캡처). ★ 백엔드 수치는 Slice 5 동시 진행 트리 포함(내 확정 기여는 동의 게이트 +6셀·스로틀 +1셀·nginx +2셀이지만 합이 +8 — 나머지 ±는 Slice 5의 `tests` 편집).
- **뮤테이션 8종 기명 재실패**(프리플라이트 게이트·절대경로 원복 매번 확인): 게이트 검증 제거 → 3실패 · 신규 행 스탬프 제거(over) → 1 · 프런트 잠금 조건 제거 → 1 · 바디 필드 제거(over) → 1 · 랜딩 signin 고정(under) → 2 · 랜딩 전경 확장(over) → 2 · nginx 헤더 제거 → 2.
- **실브라우저(CDP)**: 랜딩 4폭(1440/768/390/320) 넘침 0 · h1·한계 셋·버튼 둘·푸터 실측 확인. dev 서버 신선도는 발생 수 셈법(5174 신규 선택자 3·5173 옛 0)으로 판정. 가입 폼 확인란 존재·제출 잠금 확인(체크 후 풀림은 vitest 셀이 잠금 — CDP 동기 타이밍 한계).
- 한계: 랜딩 실기기(모바일 스크롤·소프트 키보드)·배포 화면 육안 미확인 — 오너 확인 목록에 남긴다.

### Next steps

- 독립 세션 검증(서브에이전트) → 검증기록 보강.
- AdSense 화면별 제어는 방침 제6조 2항("모든 화면") 수정을 동반하는 오너 결정 대기.
- 랜딩 스크린샷은 육안 확인 목록 닫힌 뒤(브리프 지침).

## 세션 69 — 계정 탈퇴 Slice 5: 정책 문서 §8 → §6 승격 (오너 지시 "핸드오프읽고 백엔드 쪽으로 작업할거 있는지 확인해줄래? 다른 AI가 프론트엔드쪽 작업중이니까 놀라지말고 확인먼저 해줘")

**같은 저장소에서 프런트 작업 AI(세션 68)와 동시 진행했다.** 오너가 양쪽을 조율해 법률 문서 축(두 문서 머리말 고지 · `legal/README.md` §8 표 · 정책 문서 §6/§8)을 이쪽으로 넘겼다. 커밋 셋: `e169e96`(승격 본체) · `311616d`(SoT 번호 v1.8.59→v1.8.60 정정) · 기록(이 커밋).

### Goals

- 백엔드 쪽에 지금 집을 수 있는 일이 있는지 먼저 **확인해 보고**(오너가 요청한 순서), 고른 것을 끝까지 닫는다. 고른 것은 HANDOFF 11번 **Slice 5 — 정책 문서 §8 의 *계정 탈퇴* 행을 §6 으로 승격**이고, 범위는 오너 선택에 따라 **탈퇴 행만**(같은 표의 *고지* 행은 세션 68 의 슬라이스라 건드리지 않는다).

### Completed work

- **착수 전 확인(오너가 요청한 것)**: 동의 게이트의 **백엔드는 이미 세션 68 이 끝냈고**(`58a8ac9`), 랜딩 ③·S-2 도 그쪽이 집었다. 남은 백엔드 후보를 충돌 위험과 함께 네 줄로 정리해 오너에게 제시했다 — Slice 5 · `/writing/generate` TIMEOUT 502/504 정렬(결정 브리프 선행) · `llm_call_audits` 절단 축 · 8번 계정 정리. **뮤테이션이 필요한 후보(2번 6차 재검증)는 그 시점에 차단 상태였다** — 남의 미커밋 트리가 있어 CLAUDE.md §6 사전 게이트가 성립하지 않고, MV-D 가 프런트 셀이다.
- **승격 본체**: 정책 문서 §6 에 **표 한 행**(`계정 탈퇴 유예 기간` 30일 → `auth/users.py::WITHDRAWAL_GRACE_PERIOD`) + 네 문장(유예 중 권한 · 재요청 멱등 · 취소 경계 · 잔존 둘). §8 에서 행 제거. `legal/README.md` 는 축을 §1~§7 표와 문장형 표로 옮기고 §8 을 넷 → 셋으로 줄였다.
- **회원이 읽는 거짓 문장 둘을 걷었다**: 약관 머리말의 *"제8조 3·4항은 아직 기능이 제공되지 않습니다"* 줄 전체(그 문서의 유일한 미제공 조항이었다) · 방침 머리말에서 **탈퇴 축만**(제3조 안내 상자는 세션 68 몫이라 그대로). **프런트 사본 둘 동반 이동** + 바이트 가드 4/4.
- **신규 가드**: 약관 제8조 3항·방침 제5조 3항의 `30일` 을 상수와 대조(제5조 1항 선례 — 앵커는 수가 없는 부분, 비교는 수의 집합).
- **정정 셋**: 핀 셀 docstring · 계획서 Slice 0 절 · 정책 문서 §8 말미 — 셋 다 *"§6 승격 뒤에는 대조 가드가 핀을 이어받는다"* 고 예고했는데 **틀렸다**(아래).

### Issues found

- **★ 종전 예고를 그대로 따르면 값 잠금이 사라졌다.** 핀 셀 docstring과 계획서가 *"§6 이 상수를 가리키면 `test_service_policy_contract.py` 가 그 일을 이어받는다"* 고 적어 핀 셀 삭제를 예고했지만, 대조 가드는 **문서와 상수가 갈라지는 것**만 막는다 — 둘을 **함께** 60일로 바꾸면 초록이다. 선례가 답을 갖고 있었다(`MIN_PASSWORD_LENGTH`·`SCENE_NOTE_MAX_CHARS` 는 승격된 축인데 **문서 행과 핀을 둘 다** 든다). 핀을 남기고 세 문서의 예고를 정정했다. **MU-A 가 이것을 실증했다** — 핀이 혼자 `FAILED` 를 냈다.
- **★ 승격이 드러낸 기계 공백**: 약관·방침 본문의 `30일` 은 아무 셀도 안 봤다(v1.8.52 가 *조항 ↔ 상수 표* 부채로 남긴 자리). 닫았다.
- **★ 못 닫은 공백 하나 — 오너 몫**: **유예 중 일반 쓰기·유료 경로 403** 을 약관도 방침도 적지 않는다. 코드가 시행하는 제한인데 고지가 없어, 탈퇴 신청 회원은 **말 없이 저장이 막히는 것**을 만난다. 조항 문장을 지어내지 않고 대조표에 **`미기재` 행**으로 세웠다.
- **프런트 사본 파일명을 잘못 짚었다** — `cp` 로 `frontend/src/legal/*-draft.md` 두 개를 **새로 만들었다**(사본의 실제 이름은 `-draft` 가 없다). 곧바로 지우고 올바른 이름으로 옮긴 뒤 `cmp` 로 확인했다. **교훈: 사본 동반 이동은 `ls` 로 이름을 먼저 보고 한다.**
- **SoT 행 번호가 경합했다** — 세션 68 이 `v1.8.59` 를 가져가 이쪽은 `v1.8.60` 이 됐고, 이미 커밋한 계획서·인덱스 두 참조를 정정했다(`311616d`). **같은 창에 두 작업자가 있으면 버전 번호는 선점 대상이다 — 쓰기 직전에 파일을 다시 본다.**
- **`git add` 와 `git commit` 사이에 HEAD 가 움직여 커밋이 거부됐다**(`cannot lock ref 'HEAD'`). 세션 68 의 커밋이 그 사이에 들어온 것이고, **스테이징은 살아남았다**. 재실행으로 끝났지만 **공유 파일(SoT·HANDOFF·work_log·CHANGELOG)은 그쪽 커밋이 떨어질 때까지 손대지 않고 기다렸다** — 스테이징하면 남의 미커밋 작업을 함께 커밋하게 된다.
- **세션 68 기록이 이쪽 변경을 한 줄 잘못 적었다**(*"`test_auth_users.py` 헬퍼 리팩터"*) — 실제로는 **핀 셀 docstring 한 곳**만 고쳤다(프로덕션·헬퍼 무변). 남의 세션 기록은 고치지 않고 여기 적는다.

### Decisions — User Decisions and Rationale

- 오너: *"백엔드 쪽으로 작업할 거 있는지 확인"* — 후보 넷을 충돌 위험과 함께 제시하고 **오너가 "Slice 5 — 탈퇴 행만 먼저 승격"을 골랐다.** 근거: 값이 가장 크고, 같은 §8 표의 *고지* 행(동의 게이트)은 동시 진행 슬라이스의 것이라 그 절반만 집으면 충돌이 0 이다.
- 오너: 완료 후 **독립 세션 검증(서브에이전트)** 과 검증기록 보강 지시 + *"검증 AI한테 저쪽 다른 AI가 프론트 작업하고 있다는 거 말해주는 거 잊지 말고"* — 브리프에 `git checkout`·`stash` 금지, `cp` 백업 + 역복원, 프런트·공유 기록 파일 편집 금지, 프런트 전수 금지를 명시해 띄웠다.
- 구현 선택: **조항 문장은 쓰지 않는다**(법률 문언은 오너 정본) — 발견한 공백은 대조표의 `미기재` 행으로만 남겼다. **핀 셀은 지우지 않는다**(선례 + MU-A 실측).

### Verification

- 초점: 정책 가드 **9셀/66 → 10셀/70 subtests**(`b6fd464` 양단 `git worktree` 실측 — 행 +2 · 신규 셀 +2) · 문서·정책·auth 초점(`test_docs_indexes`·`test_repo_hygiene`·`test_product_name`·`test_service_policy_contract`·`test_auth_users`) **110 passed / 1008 subtests** · 프런트 바이트 가드 `legalSource.test.ts` **4 passed · EXIT=0**. **백엔드 소스 무변**(`services/` 0줄).
- **전수는 이 세션이 돌리지 않았다** — 세션 68 이 같은 창에서 **3034 passed / 1 skipped / 4166 subtests · EXIT=0** 을 재면서 *"Slice 5 동시 진행 트리 포함"* 이라고 적었고, HANDOFF 함정(**backend 전수와 frontend 전수를 겹쳐 돌리지 말 것**) + 검증 서브에이전트가 도는 중이라 중복 실행을 피했다. **절대 기준선 갱신은 그 수를 움직인 두 슬라이스의 몫이고, 이 세션의 확정 기여는 +1셀 / +4 subtests 다.**
- **변이 3종 양방향** — **MU-A**(`WITHDRAWAL_GRACE_PERIOD` 30→60일, 문서 무변): 정책 행 `SUBFAILED(axis='계정 탈퇴 유예 기간')` 1 + 신규 셀 **2 SUBFAILED(두 문서 기명)** + **핀 `FAILED` 1** + 경계 3 · **MO-B**(약관 제8조 3항만 60일, over): 신규 셀 **1 SUBFAILED(terms 기명)** — 같은 조항 안에서 수가 갈린 것도 문다 · **MO-C**(수를 유지한 문장 다듬기, over): **10/70 초록 = 거짓양성 없음**.
- **★ 변형 절차는 `git checkout` 을 쓰지 않았다.** 트리에 다른 작업자의 미커밋 변경이 있어 사전 게이트(`git status --short` 가 빈다)가 성립하지 않았고, 가이드의 *남의 트리 감사* 분기대로 **`cp` 백업 → 역방향 복원 → `cmp` 바이트 동일 확인**을 매번 했다. 변형 전 본체는 이미 커밋돼 있었다(`e169e96`).
- 알려진 한계: 조항 **내용**의 정오는 기계가 안 본다(대조표는 *어느 조항이 받는지* 만 잠근다) · **§6 표 행을 통째로 지우면 아무것도 실패하지 않는다**(가드 docstring 이 적은 기존 한계 — 행 삭제는 사람이 읽는다).

### Next steps

- 독립 세션 검증(서브에이전트) 진행 중 → 검증기록이 나오면 **조건·하드닝을 보고 보강**한다.
- **오너 결정 대기로 남긴 것**: 유예 중 쓰기 차단 고지를 약관·방침에 넣을지(문언은 오너 몫, 대조표 `미기재` 행이 그 자리를 지킨다).
- Slice 4b(관리자 잔여 정리)는 **브리프가 먼저**이고 트리거가 없다 — 계정 탈퇴 축에서 남은 것은 그 하나다.

### 검증 폐쇄(같은 세션, 오너 지시 "검증기록 나오면 봐서 보강할 부분 보강까지")

- 독립 검증(서브에이전트) 판정 **조건부 합격** — 기록 [`verifications/2026-09-12/landing_l3_gate_and_landing.md`](../../verifications/2026-09-12/landing_l3_gate_and_landing.md)(변이 6종 신규 설계·MV-A 가 Mongo 영속 무셀을 실측해 조건을 엶).
- **C1 폐쇄**(`cbd88f7`): `test_auth_users_mongo.py` 셀 둘(쓰기면 왕복 + legacy None). 뮤테이션 재실증 — MV-A 재현 1 재실패 · MV-B 재현에서 동의 이름 셀 기명 포함(검증 시점 0개 → 1개).
- **C2 폐쇄**(같은 커밋): 확인란 문구 버전·새 탭 링크 단정 셀. `target="_blank"` 제거 변이 1 재실패.
- 판정 승격은 다음 독립 검증 세션 몫 — 검증 기록에 폐쇄 보고만 덧붙였다(v1.8.52·v1.8.57 선례).
- 하드닝 셋(h1 무핀·벤더 금지 부정단정·gen:api 드리프트)은 비차단으로 검증 기록에 남았다.

### 검증 조건 폐쇄 (같은 세션 · SoT v1.8.61)

독립 검증 서브에이전트가 **조건부 합격**(조건 셋 · 하드닝 다섯)을 냈고 같은 세션에서 전부 닫았다. 기록: [`verifications/2026-09-12/account_withdrawal_slice5_promotion.md`](../../verifications/2026-09-12/account_withdrawal_slice5_promotion.md) §폐쇄 보고.

- **B1 — 내 §6 문장이 코드보다 앞서 나갔다.** *"취소는 파기가 시작되기 전까지"*(D5)를 **코드는 시행하지 않는다** — `cancel_withdrawal` 이 `purge_started_at` 을 안 봐서 파기가 청구된 계정, **파기가 실패해 표식만 남은 계정**도 취소가 통과한다. 검증의 갈래 중 **ⓑ**(문장을 코드에 맞추고 거리를 부채로)를 택했다 — ⓐ(코드 수정)는 **거부의 얼굴**을 고르는 오너 결정을 품는다. 브리프를 올려 **HANDOFF 오너 결정 표가 2026-09-09 이후 처음 다시 찼다**. 무셀이던 분기는 **특성 셀**이 받는데, docstring 이 *이 동작을 승인하지 않는다* 고 적고 ⓐ 선택 시 셀과 문장이 함께 뒤집히는 자리임을 표시한다.
- **B2 — 남겨 둔 절반이 내 몫이었다.** 방침 머리말의 *"제3조 안내 상자는 아직 제공되지 않습니다"* 는 동의 게이트가 `58a8ac9`(내 슬라이스보다 **앞선** 커밋)에서 시행돼 이미 거짓이었는데, 나는 *"그쪽 슬라이스 몫"* 으로 넘기고 Slice 5 를 닫았다. **HANDOFF 는 그 정리를 Slice 5 몫이라 적고 있었다** — 양쪽이 서로에게 넘긴 자리다. 줄을 걷고 사본을 함께 옮겼다.
- **B3 — 내가 쓴 §8 절 머리가 자기 표와 모순했다**(*"남은 전제는 한 줄"* ↔ 그 행의 전제 *"없음"*). 열린 전제는 실제로 **0** 이었다. 남은 두 행이 *아직 시행되지 않은 것* 이 아니라 **코드가 따로 할 일이 없던 것** 임을 적고, **§8 을 비울지는 오너 판단**으로 남겼다.
- **하드닝 다섯**: H1 유예 중 **재로그인 200** 셀 · H2 `미기재` 행을 **코드 가드와 양방향** 결합 · H3 행 수 하한 10 → **14** · H4 §6 잔존 **둘 → 셋**(`admin_audit_events`) · H5 두 진단(값 불일치 vs 앵커 실종)을 함정에 등재.
- **★ 보강이 내 새 결함을 하나 만들고 가드가 잡았다** — §8 행의 상태 칸에 `모듈.py::상수` 꼴을 적었더니 그 행이 **정본 포인터 행으로 파싱**돼 `_rows()` 가 15 가 됐다(§8 에 포인터를 달지 않는다는 **그 문서 자신의 규칙** 위반이고, 방금 올린 H3 하한의 근거까지 흔들렸다). 표기를 끊고 규칙을 행 안에 적었다. **교훈: 문서에 `모듈::상수` 꼴을 쓰는 순간 그 줄은 가드의 입력이 된다 — §8 에서는 백틱을 둘로 끊는다.**
- **검증이 조용히 통과한다고 실측한 변이 둘(MX-D 행 삭제 · MX-I `미기재` 행 삭제)이 이제 기명 실패한다** — 같은 변이 재적용으로 확인(`cp` 백업 → 역복원 → `cmp`, `git checkout` 미사용).
- 수치: 정책 가드 **11셀/70** · `test_docs_indexes` **17/316**(브리프 등재 + 건수 139/117) · `SelfWithdrawalApiTest` **21/169** · 백엔드 소스 무변.
- **판정 승격은 하지 않았다** — 조건을 닫은 세션이 자기 판정을 올리면 독립성이 사라진다(v1.8.52 선례). 승격 재검은 다음 검증 세션 몫이다.

## 세션 70 — 계정 탈퇴 Slice 4b: 관리자 잔여 정리 (오너 지시 "핸드오프 읽고 다음작업 진행해줘. 진행한다음 서브 에이전트 하나 스폰해서 독립검증 맡기고 결과 나오면 보강할 부분 보강까지 해줘")

**브리프 선행 → 오너 결정 → 구현 → 독립 검증(서브에이전트) → 보강까지 한 세션에서.** 커밋: `73ae7f7`(착수 결정 브리프) · `2aa9e3f`(백엔드 구현) · 프런트·기록(이 커밋).

### Goals

- 핸드오프 Next Tasks 가 명시한 **다음 구현 축 Slice 4b(관리자 잔여 정리 — 브리프 선행)** 를 여는 것. 갈래 셋(범위·dry-run·감사)은 오너 결정 사항이라 브리프를 먼저 쓰고 물었다.

### Completed work

- **착수 결정 브리프** [`plans/slice4b-admin-residual-purge-decisions.md`](../../plans/slice4b-admin-residual-purge-decisions.md): 갈래 셋의 선택지 표 + 추천(①ⓐ·②ⓐ·③ⓐ). **오너가 셋 다 추천안 채택**(2026-09-12). plans 인덱스 등재 + 개수 주장 갱신(139/117 → 140/118 — `README.md`·`docs/README.md`·`plans/README.md` 세 곳).
- **본체 한 벌**: 스크립트가 직접 돌던 `stalled_user_ids`·`leftover_projects`·`reconcile` 을 `deletion/account_reconcile.py`(신규)로 옮기고 **스크립트와 라우터가 같은 모듈**을 쓴다(Slice 3 계약 주의 ⓕ "두 벌째 금지" 와 같은 정신). 스크립트 출력 키는 그대로(dry-run/apply 호환). 조건 넷(프로젝트 잔여 시 행 유지 · 묘비 없으면 행 유지 · 조사는 파괴 없음 · stalled 정의)을 이관 전 ReconcilerTest 셋이 그대로 잠근다(가리키기만 새 모듈로).
- **operation 둘** (`routers/admin.py`): `GET /admin/users/{id}/reconcile`(dry-run 조사 — 파괴 없음) · `POST …/reconcile`(실행). 대상 계약: 404(없음)·409(**파기 미시작** — 프로젝트 purge 의 "archived 선행 409" 와 대칭)·400(빈 사유). 사유는 프로젝트 purge·quota 정지 선례처럼 필수.
- **감사(③ⓐ)**: `AdminAuditEvent.action` 에 `account_reconcile` 추가 · `record_account_reconcile_requested`(2단계 선례 — fail-closed 요청 행 → `record_purge_outcome` 결과 행) · **`record_purge_outcome` 이 요청 행의 `target_user_id` 를 상속**하게 함(프로젝트 purge 는 None 이라 무변) · 감사 화면 `/admin/audit-events` 가 `list_destructive_events`(purge+reconcile) 로 — 회원 정책(member_quota_policy)은 파괴가 아니라 계속 밖.
- **읽기 표면**: `AdminUserPayload` 에 탈퇴 축 두 스탬프(`withdrawal_requested_at`·`purge_started_at` — **세 번째 축**, 승인 `status`·활성 `is_active` 와 별개). `AdminAuditEventPayload.target_project_id` 를 `str | None` 로(계정 축 이벤트는 None — 감사 화면이 계정 이벤트를 처음 싣는 자리라 응답 검증이 잡았다).
- **등재 전부**: tier 핀 **105→107 · admin 17→19** · `responses=` 신규 상수 `_ERRORS_ADMIN_400_404_409` · `activity/actions.py` EXCLUDED 등재(31→32, 절 주석 11→12) · 프런트 `gen:api` 재생성.
- **프런트**: 상세 화면 `AdminUserDetail` — 탈퇴 축 표시 + stalled 일 때만 **잔여 조사 → 결과(잔여 프로젝트·묘비) → 사유 + 사용자명 입력 확인(`.admin-danger-zone`·`.danger-button` 선례) → 실행 → 결과(지운 컬렉션·계정 행 처분 + 목록 링크)**. 목록 `AdminConsole` — `adminWithdrawalLabel`(세 번째 축 라벨, 기존 라벨 순서 계약 밖) 옆 표시. 기존 fixture 전부에 스탬프 둘 추가(undefined 가 `!== null` 로 읽혀 섹션이 잘못 뜨는 것을 막음).
- **셀**: 백엔드 신규 `test_admin_account_reconcile.py` **13셀**(dry-run 무변·404·409 둘(미청구·유예만)·실행 조건 셋·감사 2단계+target 상속+failed 경로+감사 화면 노출·빈 사유 400·읽기 스탬프·401/403) · payload 핀 셀 둘 갱신 · ReconcilerTest 4셀 이관. 프런트 **+5셀**(섹션 조건부·조사→사용자명 확인→실행 파이프·잔여 보고·409·목록 라벨 축).

### Issues found

- **감사 화면이 계정 이벤트를 처음 싣는 순간 응답 검증이 실패했다** — `AdminAuditEventPayload.target_project_id` 가 `str` 필수였는데 account_reconcile 은 None. 스키마(`AdminAuditEvent`)와 같은 폭(`str | None`)으로 고쳤다. member_quota 이벤트도 None 이라 이 화면이 그 필드를 채우는 일은 앞으로 없다(계속 밖).
- **프런트 집합 가드가 새 링크를 잡았다**(`navigationLinks.test.ts` — Slice 4 가 배운 정확-일치 집합). "사용자 목록으로" 링크는 정당한 보조 이동이므로 **가드의 기대 집합에 추가**했다(링크를 빼는 Slice 4 때와 결론이 다르다 — 그때는 링크의 일이 아니었다).
- **InMemory 조립에서 reconcile 저장소와 users 저장소가 갈라진다** — Mongo 배포는 둘이 같은 `users` 컬렉션을 보지만 InMemory 는 별개 상태라 "계정 행 삭제"를 `deleted_rows` 관측으로 잠갔다(셀 주석에 그 이유를 적음). 테스트 픽스처 세팅에서 reconcile repo 의 stalled 목록에도 대상을 심는다.

### Decisions — User Decisions and Rationale

- 오너(2026-09-12): 갈래 셋 **①ⓐ 계정 하나 · ②ⓐ 두 액션(조사→확인→실행) · ③ⓐ admin_audit 남긴다** — 셋 다 구현자 추천안 채택. 브리프 "✅ 오너 결정" 절에 근거와 함께 기록.
- 구현 선택: **전역 스윕 op 은 만들지 않는다**(①ⓐ) — 도그푸드에서 stalled 다발을 만나면 그때 얹는다(스크립트 경로 유지). **dry-run 을 GET 으로** — 부수효과 없는 조사는 읽기 계약이 맞다. **실행 시점 재확인**(`_stalled_user_or_error` 를 조사·실행 양쪽이 호출) — dry-run 과 실행 사이 데몬 청구가 끼어들어도 409 로 걸린다.

### Verification

- 백엔드 초점(구현 직후): `test_auth_api`(tier 핀 포함) · `test_admin_account_reconcile` · `test_account_purge` · `test_account_withdrawal_worker` · `test_admin_audit` · `test_admin_surface_separation` · `test_activity_actions` — **전부 초록**. 앱 조립 스모크: reconcile route GET/POST 등록 확인.
- 프런트: `tsc --noEmit` 통과 · `npm run build` 통과 · 전수 **488 passed / 41 files · EXIT=0**(재실행 — 첫 전수는 집합 가드 1실패를 냈고 그 원인을 닫은 뒤 재측정). 기존 482 대비 +6: **+5 는 이 슬라이스**(AdminUserDetail 4 · AdminConsole 1), **+1 은 세션 69 조건 폐쇄(`cbd88f7` 의 확인란 문구·새 탭 링크 셀)가 핸드오프 기준선에 아직 반영 안 된 것**.
- 백엔드 전수: **3053 passed / 1 skipped / 4213 subtests · EXIT=0**(2차 실측 355.9초). 1차 전수(3052/1/4200 · EXIT=1)의 유일한 실패는 **선언 잠금 지도 누락**(`test_application_api` 의 `AdminErrorContractDeclarationTest` 가 새 operation 둘을 못 찾은 것)이었고 — **내 슬라이스 결함**이라 닫고(지도 두 행 + 17→19) 재실측했다. 이전 기준선 3034/1/4166(세션 68) 대비 **+19 passed / +47 subtests**의 귀속: **이 슬라이스 +13 passed(신규 파일)·+2 subtests(선언 지도)**, 나머지 +6/+44 는 **Slice 5 조건 폐쇄·하드닝 커밋들**(`cbd88f7`·`566ea4b`·`dfc455a` — 세션 68 기준선에 아직 반영 안 된 백엔드 셀·정책 가드 확장)이다.
- **독립 검증(서브에이전트)**: 구현 커밋 뒤 별도 세션으로 반증 시도 — 결과는 검증 기록 참조.

### Next steps

- 검증 결과에 따른 보강(조건 폐쇄) → SoT·HANDOFF·CHANGELOG 갱신으로 슬라이스 닫기.
