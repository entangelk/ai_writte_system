# Work Log — 2026-09-05 세션 1 (HANDOFF 자가 검수 · 문서 전용)

## Goals

- 오너 지시(*"핸드오프 정리좀하자. 너무 길어졌다. 검수해줘"*) — `HANDOFF.md` 의 자가 검수를
  [`docs/guides/records-and-handoff.md`](../../guides/records-and-handoff.md) §HANDOFF.md 절차대로 실행.
- 착수 시점: HEAD `10177e0` clean, `HANDOFF.md` **742줄 · 222KB**(줄당 평균 300자, 최장 2,193자).
- 성공 기준: ① 완료 서술을 걷어내되 **그 안에 섞인 "지금도 구속하는 것"을 잃지 않는다**
  ② 남기는 수치 주장은 **그 자리에서 재실측**한다 ③ 링크 전수 해석 + 문서 가드 통과.

## Completed work

커밋 **`21e79d7`** (문서 1파일, 프로덕션 코드 0줄). **742 → 354줄 · 222KB → 148KB.**

### 절차 — 삭제 전에 안전망을 먼저 세웠다

가이드가 *"편집은 대부분 삭제"*라고 규정하지만, 이 저장소의 HANDOFF 는 완료 서술 **안에**
구속 사실을 섞어 두는 형태로 자라 있었다(마감 메모가 계약·함정·오너 결정을 같은 블록에 담았다).
그래서 삭제를 기계적으로 검산하는 순서로 진행했다:

1. 전체 742줄 정독 → 절별 판정.
2. 유지 구간은 **원문 바이트 그대로** 옮기는 조립 스크립트로 새 파일을 만들었다(재타이핑 오류 차단).
   재작성 구간만 새 텍스트, 수치 정정은 해당 줄에 **문자열 치환**으로 적용(앵커 부재 시 assert 실패).
3. **삭제 안전망**: 신·구 파일에서 마크다운 링크 대상과 백틱 코드참조를 집합으로 뽑아 차집합을 냈다.
   사라진 링크 54건·경로형 코드참조 77건을 한 건씩 판정해, **버려선 안 되는 5건을 되살렸다**(아래).
4. 설치는 `cp` 후 **md5 대조**(WSL DrvFs 제자리 쓰기 손상 이력 때문). 동일 확인.

### 걷어낸 것

| 대상 | 분량 | 정본이 어디 있는가 |
|---|---|---|
| Next Tasks 의 마감 메모 12개(2026-08-14~09-01) | 336줄 | `daily_logs/` · SoT 변경이력 · `verifications/` |
| 추적 부채의 `[닫힘]·[해소됨]·[폐쇄]` 12항목 | — | 각 검증 기록 · SoT |
| D8 진행표 20행(1a~7, 전부 완료) | 21줄 | "완료 슬라이스" 절 + SoT |
| 머신 관측치 6블록(08-08~08-21) | 6줄(장문) | — (관측치는 재측정이 정본) |
| 완료된 0~3번 구현 목록 | 13줄 | 검증 기록 |

머신 관측 블록은 **셋이 서로 "지금 작업 중인 머신"이라고 적어** 충돌하고 있었다 — 가이드가 금지한
"머신-로컬 관측을 프로젝트 사실로 기록" 의 전형이라 절 전체를 걷고, "이 절에 쌓지 않는다"는 한 줄로
대체했다.

### 코드로 재실측해 정정한 수치·단언 6건

| 자리 | 종전 | 실측 | 근거 |
|---|---|---|---|
| 총 operation | 99 · 77 · 76(세 곳이 각각) | **102** | `tests/test_auth_api.py:1910-1911` tier 핀(project 76) |
| `/admin` operation | 8 | **17** | 같은 파일 `ADMIN` 집합 |
| 활동 분류표 | "20 경로" · "mutating 40 전수" | **개수 삭제** | `activity/actions.py` 가 정본(실측 logged 29 · EXCLUDED 29) |
| 컨테이너 폭 | 68rem | **76rem** | `styles.css:226·310·318` |
| 403 생산자 | "소유권·관리자 정확히 둘" | **넷** | `dependencies.py:132`·`:291`·`:163`, `routers/auth.py:115`·`:119` |
| `unit_kind` 존치 판단 | 오너 몫으로 열림 | **이미 해소** | 공개 스키마·프론트 0건, `core_sot` legacy migration 입력으로만 잔존 |

개수는 슬라이스마다 낡으므로, 활동 분류표는 값을 적는 대신 **정본 파일을 가리키게** 바꿨다
(이 저장소가 이미 여섯 번 겪은 *"개수는 구조적으로 낡는다"* 교훈의 적용).

### 되살린 것 — 삭제 대상 메모에만 있던 구속 사실 5건

안전망이 잡은 것들이다. 이것이 없었으면 조용히 사라졌다.

1. **`/docs`·`/redoc`·`/openapi.json` 비공개**(오너 2026-08-23, SoT v1.7.98) — 코드 확인
   `main.py:1777-1779`. Active Decisions 로 승격했다.
2. **Phase 9 활동 화면 가드 넷** — 특히 *"최근 100건" 문구의 두 겹 잠금*: 백엔드
   `list_for_project(limit=…)` 기본값을 바꾸면 `test_activity_ui_labels.py` 가 실패한다(F1 커서
   페이징 작업이 여기서 걸린다).
3. **`:disabled` 농도의 의도된 예외 둘**(`.session-menu button:disabled`·`.login-form input:disabled`)
   — 접으면 over-strict 셀이 문다.
4. **AdSense 가드 위치**(`adsense.test.ts` 가 `index.html` `<head>` 를 핀 · `ads.txt` 는 무가드,
   침묵 실패 모드 명시) + SRI 미부착이 의도라는 근거.
5. **`docs/portfolio.md` 링크**와 그 문서의 "숫자는 날짜 스냅샷" 규칙.

### 부수 정정

- **`llm_gateway/app/client.py` 줄 앵커 재실측.** HANDOFF 두 자리가 같은 세 경로를 각각
  `71/216/253/243` 과 `75/224/262/237` 로 적고 있었고 **둘 다 드리프트**해 있었다. 실측
  `/props` = `_probe_context_window:80`, `/tokenize` = `:236`·`:273`, `/apply-template` = `:263`.
  "이 파일은 줄 번호가 아니라 함수 이름으로 찾는다"는 주의를 함께 적었다.
- **`LLAMA_CTX_SIZE=16384` 함정의 사유가 낡아 있었다.** 본문이 R-e 이전 증상(*"프롬프트만으로 창을
  넘어 4/4 실패"*)을 현재형으로 서술했는데, R-e(v1.7.61) 이후 이유는 **"K-3 가드에 400 으로
  거부당하지 않으려고"**다. 같은 파일 안의 다른 항목(추적 부채)이 이미 후자를 적고 있어 자기모순이었다.
- **T1 부채는 줄지 않고 커졌다** — 근거였던 68rem 이 76rem 이 됐으므로 본문을 그렇게 고쳤다.
- **CSS 절 항목 수** 여덟 → 열(2건 이관).

## Issues found

- **문제**: `activity/actions.py:173` 의 주석이 *"기록하지 않는 21"* 이라고 적는데 실제
  `ExcludedOperation` 은 **29건**이다.
  **원인**: 슬라이스마다 행이 늘었는데 주석 수치를 안 따라갔다. 가드는 등재 여부만 보고 개수를 안 본다.
  **처리**: 이번 정리의 범위(문서)를 벗어나므로 **고치지 않고 HANDOFF 에 명시**했다.
  **결과**: 코드를 고칠 다음 세션의 첫 항목 후보. 같은 병이라 처방도 같다 — 주석에서 개수를 빼거나
  가드가 세게 한다.
- **문제**: `frontend/src/pageLayout.test.ts` 의 docstring 이 여전히 `68rem` 이라고 적는다.
  **원인**: 그 가드는 *값이 아니라 규칙성*을 재도록 설계돼 있어 68 → 76 변경을 물지 않았다.
  설계 의도대로 동작한 것이고 결함이 아니다.
  **처리**: 문서만 정정하고 테스트는 손대지 않았다. HANDOFF 에 *"값을 인용할 때는 `styles.css` 를
  본다"* 를 적었다.
- **문제(미해결, 오너 확인 필요)**: 이 머신의 `nvidia-smi` 는 **RTX 3060 12GB** = 표의 **알파**인데,
  HANDOFF 의 최신 회귀 기준선은 2026-09-04 를 **"베타 실측"** 이라 적는다.
  **원인 미상** — 오너가 머신을 옮겼거나, 09-04 세션의 라벨이 틀렸다.
  **처리**: 어느 쪽인지 단정하지 않고 **양쪽을 병기**하고 "머신 이름을 근거로 판단하기 전에 그 자리에서
  다시 잰다"를 적었다.

## Decisions

- **길이 상한을 새로 만들지 않았다.** 가이드가 *"길이는 증상이지 규칙이 아니다"* 라고 명시하므로,
  줄 수를 목표로 삼지 않고 **"오늘 할 일을 바꾸는가"** 한 기준으로만 잘랐다. 결과적으로 절반 이하가
  됐지만 장문 항목(인증 경계·quota·리랭커·CSS)은 **길어야 할 이유가 있어 그대로 뒀다**.
  트레이드오프: 148KB 는 여전히 크다. 다음 검수에서 볼 자리는 "지금 상태"의 1,500자대 항목 둘이다.
- **삭제 대신 이관을 우선했다.** 완료 서술이라도 그 안에 계약·함정이 있으면 "완료 슬라이스"·"함정"·
  "Active Decisions" 로 옮겼다. 근거: 이 저장소의 반복 실패 모양이 *"닫으면서 그 자리가 남긴 규칙까지
  버리는 것"* 이었다.
- **개수 주장은 가능한 한 없앴다.** 남긴 것은 테스트가 핀하는 값(operation 102/76/17)뿐이고, 나머지는
  정본 파일을 가리킨다. 근거: 낡은 개수 6건이 이번 검수의 최대 오염원이었다.
- **코드 결함은 고치지 않았다.** `actions.py` 주석과 `pageLayout.test.ts` docstring 둘 다 문서 정리
  범위 밖이라 기록만 했다(CLAUDE.md §3 surgical changes).

## Verification

- 링크 전수 해석 — 신 파일의 마크다운 링크 대상 전부 디스크 존재 확인, **깨진 링크 0건**.
- `python3 -m pytest tests/test_docs_indexes.py -q` → **13 passed / 290 subtests**.
- `python3 -m pytest tests/test_product_name.py tests/test_typecheck.py -q` → **13 passed / 11 subtests**.
- 설치 후 `md5sum` 대조 일치(DrvFs 손상 없음), 연속 빈 줄 0건, 헤딩 11개 전부 보존.
- 전수 회귀는 돌리지 않았다 — **프로덕션 코드 0줄**이고 변경 파일이 `HANDOFF.md` 하나다.

## Next steps

- HANDOFF Next Tasks 1번(정체성 그룹 Slice 5 독립 검증)부터.
- 오너 확인: ① 지금 머신이 알파인가 베타인가 ② `actions.py` 주석 수치 정정을 다음 코드 세션에
  포함할지.
