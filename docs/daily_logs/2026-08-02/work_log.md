# 2026-08-02 작업 로그

## Task — 인증 D8-7 G1=C: 저장소 노출면 축소 (SoT v1.7.75)

### Goals

- 어제 낸 착수 브리프([`plans/auth-d8-7-infra-auth-decisions.md`](../../plans/auth-d8-7-infra-auth-decisions.md))의
  **G1을 오너가 확정**했다 → **C**(노출면 축소를 지금, 자격증명은 원격 배포 시점). 구현자 추천과 같다.
- 성공 기준: ① 저장소가 LAN에서 보이지 않는다(실측) ② 컨테이너 간 통신·호스트 툴링 무영향
  ③ 회귀가 **양방향**으로 문다 ④ 정본의 "외부 노출 금지" 문구가 개정된다.

### User Decisions and Rationale

- **G1 = C 확정 (오너, 2026-08-02).** 브리프의 4지선다(A 축소 / B 자격증명 / C 둘을 시간축으로 분리 /
  D 하지 않음)에서 C. 근거는 브리프 §G1 추천 그대로다 — 위험의 형태(LAN 무인증 노출은 축소로 완전히
  사라진다) · 단계(로컬 1인·머신 3대, `--auth --replSet`이 강제하는 keyfile이 "compose up만으로 뜬다"를
  깬다) · 되돌릴 수 있음(A는 B를 막지 않고 둘은 겹쳐 쓴다).
- **G2~G6은 묻지 않았다** — 브리프가 "G1=A/C면 불필요"라고 명시했고 실제로 그렇다. 각하가 아니라
  **유예**이며, B를 여는 계기는 원격/다중 호스트 배포다.
- **D8-5 C-1~C-6은 이번에 다루지 않는다**(오너: "지금은 D8-7만"). 5-b·5-d는 계속 차단 상태.

### Completed work — 구현 (코드 변경)

- **`docker-compose.yml`** — 5개 서비스의 호스트 게시를 `127.0.0.1:` 바인드로 바꿨다:
  `mongo`(27520) · `chroma`(8523) · `elasticsearch`(9520) · `gateway`(8521) · `embedding`(8522).
  각 자리에 **왜 그 서비스인지**를 주석으로 남겼다(정본 데이터 / 파생이지만 본문 조각을 가짐 /
  무인증 lexical 인덱스 / LLM을 쓰는 무인증 경로 / 내부 모델 서버).
- **`docker-compose.test.yml`** — `test-mongo`도 loopback. 게시는 유지해야 한다(pytest가 호스트에서
  돈다) 그러나 **호스트만** 필요하다.
- **`docker-compose.llama.yml`** — `llama`(9080)는 **일부러 유지**. 이 스택의 유일한 크로스머신
  의존이며(GPU를 가진 머신이 12B를 못 올리는 머신에 모델을 준다 — 베타가 `LLAMA_BASE_URL`을 LAN으로
  가리키는 것이 정상 구성) 모델 서버라 노출되는 것은 **내용이 아니라 연산**이다. 주석으로 명시.
- **`application`·`frontend`** — 공개 유지. 주석을 새로 달아 **의도임**을 못박았다(제품 표면이고
  세션 뒤에 있다, v1.7.52/53/55). 이 둘을 loopback으로 묶는 것은 별개 결정이다.
- **`.env.example`** — 바인드 주소는 env로 못 바꾸며 여기 값은 **번호만** 바꾼다는 것을 머리말에 추가.
  저장소가 여전히 무인증이라 바인드가 유일한 방어라는 사실도 함께.
- **애플리케이션 코드 변경 0줄.** 컨테이너 간 통신은 compose 네트워크 이름(`mongo:27017`·
  `gateway:8001`)이라 호스트 게시와 무관하다 — 브리프 §1이 예측한 그대로다.

### Verification

- **실측(docker 자신이 답한 값)**: `docker compose config` → **host_ip 127.0.0.1이 5건**
  (chroma 8523 · elasticsearch 9520 · embedding 8522 · gateway 8521 · mongo 27520),
  `application` 8520·`frontend` 5520은 host_ip 없음(= 0.0.0.0). 세 compose 파일 전부
  `config --quiet` exit 0.
- **라이브**: `docker compose -f docker-compose.test.yml up -d` 후
  `docker port ai_writte_system-test-mongo-1` → **`27020/tcp -> 127.0.0.1:27020`**.
  그 컨테이너에 호스트 pytest가 그대로 붙어 전량 회귀가 돌았다(아래) — **"loopback 바인드가
  호스트 툴링을 깨지 않는다"의 직접 증거**다.
- **회귀 전량**(test-mongo ON): 아래 "회귀 결과" 절.
- **양방향 뮤테이션 3건 실증**(전부 커밋 없이 역방향 Edit으로 원복, `git status` 청결 확인):

  | 뮤테이션 | 기대 | 실측 |
  |---|---|---|
  | mongo의 `127.0.0.1:` 접두 제거(= 종전 상태 복귀) | under-strict 재실패 | `SUBFAILED(service='mongo')` — 원 결함의 재현 |
  | `application`을 loopback으로 묶음(과잉 교정) | over-strict 실패 | `SUBFAILED(service='application')` |
  | `worker`에 포트 게시 추가(미분류 신규 서비스) | 디스커버리 실패 | `test_every_publishing_service_is_classified` 실패 |

### Decisions (구현자 판단)

- **"게시를 없앤다"가 아니라 "127.0.0.1로 바인드한다"를 골랐다.** 브리프 A는 둘 다 허용한다.
  바인드 쪽이 **LAN 노출은 똑같이 0으로 만들면서** 호스트에서 붙는 정당한 작업
  (mongosh 디버깅 · `scripts/migrate_ordered_units.py` 기본 URI 27520 · 호스트 `pytest`의
  test-mongo · runbook의 `curl localhost:8521`)을 하나도 깨지 않는다. 게시를 통째로 없앴다면
  그 작업들이 전부 `docker compose run` 안으로 옮겨가야 했다(브리프 §2가 예상한 비용) — 그
  비용을 낼 이유가 없다.
- **gateway·embedding까지 넣었다.** 브리프 §2 Follow-up이 "데이터가 없지만 LAN에 열려 있다 —
  노출면 축소 대상으로는 같은 취급이 맞다"고 적었고, 특히 gateway는 **무인증으로 LLM을 쓰는 경로**다.
  호스트에서 쓰는 두 지점(runbook 헬스체크·R-a 예산 리그)은 localhost라 무영향임을 확인했다.
- **llama를 닫지 않은 것은 판단이지 누락이 아니다.** 닫으면 12B를 못 올리는 머신이 스택을 못 돌린다
  (HANDOFF 머신 표의 베타가 정확히 그 경우). 회귀에 **"LAN에서 보여야 한다"는 셀을 따로 두어**
  나중에 "일관성"을 이유로 묶는 과잉 교정을 막았다.
- **가드가 목록을 믿지 않게 만들었다.** 어제 스크립트 로그인 슬라이스의 교훈(문자열 하나로 스윕하면
  같은 일을 다른 철자로 하는 호출부를 놓친다)을 그대로 적용해, **게시하는 서비스 집합을 compose에서
  읽어** 분류 리터럴과 대조한다. 새 서비스가 포트를 게시하면 분류가 **규칙이 아니라 강제**다.
- **파서는 stdlib만 쓴다.** `yaml`은 이 repo의 백엔드 의존이 아니라(호스트에 우연히 있을 뿐)
  넣으면 `argon2-cffi`와 같은 "없으면 수집 실패" 함정을 하나 더 만든다. compose 3파일은 repo가
  소유하고 형식이 고정이라 `ports:` 리스트만 읽는 좁은 정규식으로 충분하고, **못 읽으면 리터럴
  대조에서 실패**하므로 조용한 skip이 되지 않는다.

### 정본 개정 (SoT v1.7.75)

- **"외부 노출 금지"의 해제 조건이 *인증*에서 *노출 없음*으로 바뀌었다.** 이것이 이 슬라이스의
  진짜 계약 변경이다 — 브리프 §G1의 C 단점("D8-7 완료의 의미가 달라진다")이 실제로 청구된 것.
- **함께 적은 것(가장 오독하기 쉬운 자리)**: 저장소는 **여전히 무인증**이고 v1.6.53 G3=A의 위험은
  사라진 게 아니라 **도달 불가능해진 것**이다. 포트를 다시 0.0.0.0으로 열면 그 위험이 그대로 돌아온다.
- 본문 2곳(§"제품과 프로젝트 경계") + 변경이력 1행. operation 카운트 무변(71).

---

## Hardening — 독립 검증(2026-08-02) 반영

독립 검증 **합격**(`docs/verifications/2026-08-02/d8_7_g1c_loopback_exposure.md`, `f3c255d`).
Blocking 0건. 검증자가 뮤테이션 3종·회귀 1836/103.30s·compose 리터럴 ↔ docker 렌더링 일치를
독립 재현했고, `network_mode`·`expose:` 같은 **다른 노출 경로가 전무**함도 확인했다(내가 안 본 축이다).
아래 3건을 반영했다.

### #1 [비차단, 실제 blind spot] 인라인 `ports:` 표기를 파서가 못 읽었다

- **검증자의 반증이 성공한 유일한 지점**이다. `ports: ["8599:8000"]` 한 줄 표기를 worker에 넣으면
  **docker는 게시로 수용하는데**(`published: "8599"`) 가드는 5 passed로 통과했다 — `_PORTS_RE`의
  `^    ports:\s*$`가 뒤에 내용이 있으면 매칭에 실패해 그 서비스를 **집합에서 통째로 누락**시켰다.
- 즉 내가 work_log에 쓴 **"못 읽으면 리터럴 대조에서 실패하므로 조용한 skip이 되지 않는다"는
  멀티라인 표기에만 성립하는 주장**이었다. 현재 compose 3파일이 전부 멀티라인이라 결함은 아니었지만,
  **"분류가 규칙이 아니라 강제"라는 성질 자체가 표기 하나에 의존**하고 있었다.
- **수리**: `_PORTS_RE`를 `^    ports:(?P<inline>.*)$`로 넓혀 인라인 리스트를 항목까지 파싱하고,
  항목 정규도 큰따옴표 전용에서 **작은따옴표·무따옴표까지** 받게 했다(compose가 셋 다 받는다).
  파서를 `_published_ports_in(text)`(본문) + `_published_ports(path)`(파일)로 갈라 **본문을 직접
  먹일 수 있게** 했다 — 이 갈라짐의 목적은 아래 회귀 하나뿐이다.
- **회귀 `ComposeParserTest`**: 네 표기(멀티라인 큰따옴표·작은따옴표·무따옴표 + 인라인 리스트)를
  전부 읽고, **`ports`가 없는 서비스는 집합에 넣지 않는다**는 것까지 한 셀에서 단정한다(넓히다가
  아무 리스트나 삼키면 분류 리터럴이 무의미해지는 과잉 교정 방향).
- **검증자의 우회를 그대로 재현해 닫힌 것을 확인**: 같은 인라인 뮤테이션에서 docker는 여전히
  수용(`published: "8599"`)하고 **가드는 이제 `test_every_publishing_service_is_classified`로
  실패한다**. 원복 후 `git diff` 청결 확인.

### #2 [비차단, 슬라이스 범위 밖] SoT tier 카운트가 낡아 있었다

- 검증자 지적대로 정본 §"제품과 프로젝트 경계"가 **"69개가 네 tier(public 4 · 인증 전용 2 ·
  관리자 4 · project-scoped 59)"**라고 적고 있었다. **직접 실측**해 확인했다 —
  `tests/test_auth_api.py`의 `test_every_operation_lands_in_exactly_one_named_tier`가
  `len(tiers)==71` · `len(by_tier["project"])==60`을 단정하고, 리터럴은 `PUBLIC` 4개 ·
  `AUTH_ONLY` 2개 · `ADMIN` **5개**(D8-5a 3 + 5-c KPI + 6d purge)다. 4+2+5+60 = **71**로 합치한다.
- 즉 **세 숫자가 모두 낡았다**(69→71 · 관리자 4→5 · project 59→60). D8-5c·D8-6d가 카운트를
  올렸는데 changelog에만 반영되고 본문에 안 내려왔다. G1=C와는 무관한 문서 부채다.
- **정본·HANDOFF 3곳 정정**하면서 **"이 숫자의 권위 소스는 문서가 아니라 코드"**를 함께 적었다 —
  같은 drift가 재발할 때 어느 쪽을 믿을지가 정해져 있어야 한다. HANDOFF의 "69개 tier 분할"·
  "`/health` 제외 69개(전체 70)"도 각각 71·70(전체 71)로 고쳤다.

### #3 [★ 가장 중요] "시행 완료"는 파일 수준에서만 참이었다

- 검증자가 `docker ps -a`로 잡았다: `mongo-1`·`gateway-1`·`elasticsearch-1`·`chroma-1`·`embedding-1`이
  `Exited (255) 25h` 상태로 **여전히 `0.0.0.0:27520->27017` 등 옛 매핑을 보유**한다. **포트 매핑은
  컨테이너 생성 시점에 고정**되므로 compose 파일 수정은 기존 컨테이너에 적용되지 않는다.
- **지금 노출은 없다**(전부 Exited). 그러나 **옛 컨테이너를 그대로 살리면 LAN 노출이 복귀한다.**
  내가 "시행까지 끝냈다"고 보고한 것은 **파일 수준에서 참이고 런타임 수준에서 거짓**이었다.
- **기록으로 닫았다**: HANDOFF 함정 절에 **"compose 포트 매핑 변경은 이미 만들어진 컨테이너에
  적용되지 않는다 — `up -d`로도 안 된다. 확인법은 파일이 아니라 `docker port`다"**를 신설하고,
  "지금 상태" ★항목과 SoT v1.7.75 엔트리에도 시행 범위가 compose 파일임을 명시했다.
  같은 성질이 env·command 변경 전반에 적용되므로 D8-7 한정 문구로 쓰지 않았다.
- **재생성 자체는 하지 않았다** — 오너의 머신 상태이고 스택 전체 재기동을 수반한다. 오너 결정 대기
  항목으로 올렸다. 함께 보고된 크래시 루프 고아(`frontend`·`worker`·`generation_worker`,
  `restart: unless-stopped`)도 같은 `down` 한 번으로 정리되는 같은 맥락의 상태다.

### Verification (hardening 후 재실측)

- `tests/test_compose_exposure.py` **6 passed / 7 subtests**(신규 `ComposeParserTest` 1셀 추가).
- 인라인 우회 뮤테이션 재현 → 이제 분류 가드가 실패(닫힘 확인), 원복 후 트리 청결.
- **전량 회귀(hardening 후 최종)**: **1837 passed / 4 skipped / 1556 subtests / 103.02s**, exit 0.
  직전 1836 대비 **+1 = `ComposeParserTest`**, 회귀 0건. 08-01 기준선 1831 대비 +6/+7.

### Next steps

- **D8-7의 남은 반(G2~G6 = 자격증명)은 원격/다중 호스트 배포 시점**이다. 브리프에 실행 계획으로 남았다.
- 지금 결정 없이 열 수 있는 잔여는 어제와 같다 — **purge 감사 로그**(작은 브리프 선행 권장:
  저장 위치·필드·조회 표면이 사실상 결정 사항) · **완전 멱등 재시구/reconciler**.
- **D8-5 C-1~C-6은 여전히 오너 결정 대기**(5-b·5-d 차단).
