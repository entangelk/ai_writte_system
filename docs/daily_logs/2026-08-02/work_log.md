# 2026-08-02 작업 로그

## Task — 인증 D8-7 G1=C: 저장소 노출면 축소 (SoT v1.7.75)

### Goals

- 어제 낸 착수 브리프([`plans/auth-d8-7-infra-auth-decisions.md`](../../plans/auth-d8-7-infra-auth-decisions.md))의
  **G1을 오너가 확정**했다 → **C**(노출면 축소를 지금, 자격증명은 원격 배포 시점). 구현자 추천과 같다.
- 성공 기준: ① 저장소가 LAN에서 보이지 않는다(실측) ② 컨테이너 간 통신·호스트 툴링 무영향
  ③ 회귀가 **양방향**으로 문다 ④ 정본의 "외부 노출 금지" 문구가 개정된다.

### User Decisions and Rationale

- **G1 = C 확정 (오너, 2026-08-02).** 브리프의 4지선다(A 축소 / B 자격증명 / C 둘을 시간축으로 분리 /
  D 하지 않음)에서 C. 근거는 브리프 §G1 추천 그대로다 — 위험의 형태(LAN 무인증 노출은 축소로 완전히
  사라진다) · 단계(로컬 1인·머신 3대, `--auth --replSet`이 강제하는 keyfile이 "compose up만으로 뜬다"를
  깬다) · 되돌릴 수 있음(A는 B를 막지 않고 둘은 겹쳐 쓴다).
- **G2~G6은 묻지 않았다** — 브리프가 "G1=A/C면 불필요"라고 명시했고 실제로 그렇다. 각하가 아니라
  **유예**이며, B를 여는 계기는 원격/다중 호스트 배포다.
- **D8-5 C-1~C-6은 이번에 다루지 않는다**(오너: "지금은 D8-7만"). 5-b·5-d는 계속 차단 상태.

### Completed work — 구현 (코드 변경)

- **`docker-compose.yml`** — 5개 서비스의 호스트 게시를 `127.0.0.1:` 바인드로 바꿨다:
  `mongo`(27520) · `chroma`(8523) · `elasticsearch`(9520) · `gateway`(8521) · `embedding`(8522).
  각 자리에 **왜 그 서비스인지**를 주석으로 남겼다(정본 데이터 / 파생이지만 본문 조각을 가짐 /
  무인증 lexical 인덱스 / LLM을 쓰는 무인증 경로 / 내부 모델 서버).
- **`docker-compose.test.yml`** — `test-mongo`도 loopback. 게시는 유지해야 한다(pytest가 호스트에서
  돈다) 그러나 **호스트만** 필요하다.
- **`docker-compose.llama.yml`** — `llama`(9080)는 **일부러 유지**. 이 스택의 유일한 크로스머신
  의존이며(GPU를 가진 머신이 12B를 못 올리는 머신에 모델을 준다 — 베타가 `LLAMA_BASE_URL`을 LAN으로
  가리키는 것이 정상 구성) 모델 서버라 노출되는 것은 **내용이 아니라 연산**이다. 주석으로 명시.
- **`application`·`frontend`** — 공개 유지. 주석을 새로 달아 **의도임**을 못박았다(제품 표면이고
  세션 뒤에 있다, v1.7.52/53/55). 이 둘을 loopback으로 묶는 것은 별개 결정이다.
- **`.env.example`** — 바인드 주소는 env로 못 바꾸며 여기 값은 **번호만** 바꾼다는 것을 머리말에 추가.
  저장소가 여전히 무인증이라 바인드가 유일한 방어라는 사실도 함께.
- **애플리케이션 코드 변경 0줄.** 컨테이너 간 통신은 compose 네트워크 이름(`mongo:27017`·
  `gateway:8001`)이라 호스트 게시와 무관하다 — 브리프 §1이 예측한 그대로다.

### Verification

- **실측(docker 자신이 답한 값)**: `docker compose config` → **host_ip 127.0.0.1이 5건**
  (chroma 8523 · elasticsearch 9520 · embedding 8522 · gateway 8521 · mongo 27520),
  `application` 8520·`frontend` 5520은 host_ip 없음(= 0.0.0.0). 세 compose 파일 전부
  `config --quiet` exit 0.
- **라이브**: `docker compose -f docker-compose.test.yml up -d` 후
  `docker port ai_writte_system-test-mongo-1` → **`27020/tcp -> 127.0.0.1:27020`**.
  그 컨테이너에 호스트 pytest가 그대로 붙어 전량 회귀가 돌았다(아래) — **"loopback 바인드가
  호스트 툴링을 깨지 않는다"의 직접 증거**다.
- **회귀 전량**(test-mongo ON): 아래 "회귀 결과" 절.
- **양방향 뮤테이션 3건 실증**(전부 커밋 없이 역방향 Edit으로 원복, `git status` 청결 확인):

  | 뮤테이션 | 기대 | 실측 |
  |---|---|---|
  | mongo의 `127.0.0.1:` 접두 제거(= 종전 상태 복귀) | under-strict 재실패 | `SUBFAILED(service='mongo')` — 원 결함의 재현 |
  | `application`을 loopback으로 묶음(과잉 교정) | over-strict 실패 | `SUBFAILED(service='application')` |
  | `worker`에 포트 게시 추가(미분류 신규 서비스) | 디스커버리 실패 | `test_every_publishing_service_is_classified` 실패 |

### Decisions (구현자 판단)

- **"게시를 없앤다"가 아니라 "127.0.0.1로 바인드한다"를 골랐다.** 브리프 A는 둘 다 허용한다.
  바인드 쪽이 **LAN 노출은 똑같이 0으로 만들면서** 호스트에서 붙는 정당한 작업
  (mongosh 디버깅 · `scripts/migrate_ordered_units.py` 기본 URI 27520 · 호스트 `pytest`의
  test-mongo · runbook의 `curl localhost:8521`)을 하나도 깨지 않는다. 게시를 통째로 없앴다면
  그 작업들이 전부 `docker compose run` 안으로 옮겨가야 했다(브리프 §2가 예상한 비용) — 그
  비용을 낼 이유가 없다.
- **gateway·embedding까지 넣었다.** 브리프 §2 Follow-up이 "데이터가 없지만 LAN에 열려 있다 —
  노출면 축소 대상으로는 같은 취급이 맞다"고 적었고, 특히 gateway는 **무인증으로 LLM을 쓰는 경로**다.
  호스트에서 쓰는 두 지점(runbook 헬스체크·R-a 예산 리그)은 localhost라 무영향임을 확인했다.
- **llama를 닫지 않은 것은 판단이지 누락이 아니다.** 닫으면 12B를 못 올리는 머신이 스택을 못 돌린다
  (HANDOFF 머신 표의 베타가 정확히 그 경우). 회귀에 **"LAN에서 보여야 한다"는 셀을 따로 두어**
  나중에 "일관성"을 이유로 묶는 과잉 교정을 막았다.
- **가드가 목록을 믿지 않게 만들었다.** 어제 스크립트 로그인 슬라이스의 교훈(문자열 하나로 스윕하면
  같은 일을 다른 철자로 하는 호출부를 놓친다)을 그대로 적용해, **게시하는 서비스 집합을 compose에서
  읽어** 분류 리터럴과 대조한다. 새 서비스가 포트를 게시하면 분류가 **규칙이 아니라 강제**다.
- **파서는 stdlib만 쓴다.** `yaml`은 이 repo의 백엔드 의존이 아니라(호스트에 우연히 있을 뿐)
  넣으면 `argon2-cffi`와 같은 "없으면 수집 실패" 함정을 하나 더 만든다. compose 3파일은 repo가
  소유하고 형식이 고정이라 `ports:` 리스트만 읽는 좁은 정규식으로 충분하고, **못 읽으면 리터럴
  대조에서 실패**하므로 조용한 skip이 되지 않는다.

### 정본 개정 (SoT v1.7.75)

- **"외부 노출 금지"의 해제 조건이 *인증*에서 *노출 없음*으로 바뀌었다.** 이것이 이 슬라이스의
  진짜 계약 변경이다 — 브리프 §G1의 C 단점("D8-7 완료의 의미가 달라진다")이 실제로 청구된 것.
- **함께 적은 것(가장 오독하기 쉬운 자리)**: 저장소는 **여전히 무인증**이고 v1.6.53 G3=A의 위험은
  사라진 게 아니라 **도달 불가능해진 것**이다. 포트를 다시 0.0.0.0으로 열면 그 위험이 그대로 돌아온다.
- 본문 2곳(§"제품과 프로젝트 경계") + 변경이력 1행. operation 카운트 무변(71).

---

## Hardening — 독립 검증(2026-08-02) 반영

독립 검증 **합격**(`docs/verifications/2026-08-02/d8_7_g1c_loopback_exposure.md`, `f3c255d`).
Blocking 0건. 검증자가 뮤테이션 3종·회귀 1836/103.30s·compose 리터럴 ↔ docker 렌더링 일치를
독립 재현했고, `network_mode`·`expose:` 같은 **다른 노출 경로가 전무**함도 확인했다(내가 안 본 축이다).
아래 3건을 반영했다.

### #1 [비차단, 실제 blind spot] 인라인 `ports:` 표기를 파서가 못 읽었다

- **검증자의 반증이 성공한 유일한 지점**이다. `ports: ["8599:8000"]` 한 줄 표기를 worker에 넣으면
  **docker는 게시로 수용하는데**(`published: "8599"`) 가드는 5 passed로 통과했다 — `_PORTS_RE`의
  `^    ports:\s*$`가 뒤에 내용이 있으면 매칭에 실패해 그 서비스를 **집합에서 통째로 누락**시켰다.
- 즉 내가 work_log에 쓴 **"못 읽으면 리터럴 대조에서 실패하므로 조용한 skip이 되지 않는다"는
  멀티라인 표기에만 성립하는 주장**이었다. 현재 compose 3파일이 전부 멀티라인이라 결함은 아니었지만,
  **"분류가 규칙이 아니라 강제"라는 성질 자체가 표기 하나에 의존**하고 있었다.
- **수리**: `_PORTS_RE`를 `^    ports:(?P<inline>.*)$`로 넓혀 인라인 리스트를 항목까지 파싱하고,
  항목 정규도 큰따옴표 전용에서 **작은따옴표·무따옴표까지** 받게 했다(compose가 셋 다 받는다).
  파서를 `_published_ports_in(text)`(본문) + `_published_ports(path)`(파일)로 갈라 **본문을 직접
  먹일 수 있게** 했다 — 이 갈라짐의 목적은 아래 회귀 하나뿐이다.
- **회귀 `ComposeParserTest`**: 네 표기(멀티라인 큰따옴표·작은따옴표·무따옴표 + 인라인 리스트)를
  전부 읽고, **`ports`가 없는 서비스는 집합에 넣지 않는다**는 것까지 한 셀에서 단정한다(넓히다가
  아무 리스트나 삼키면 분류 리터럴이 무의미해지는 과잉 교정 방향).
- **검증자의 우회를 그대로 재현해 닫힌 것을 확인**: 같은 인라인 뮤테이션에서 docker는 여전히
  수용(`published: "8599"`)하고 **가드는 이제 `test_every_publishing_service_is_classified`로
  실패한다**. 원복 후 `git diff` 청결 확인.

### #2 [비차단, 슬라이스 범위 밖] SoT tier 카운트가 낡아 있었다

- 검증자 지적대로 정본 §"제품과 프로젝트 경계"가 **"69개가 네 tier(public 4 · 인증 전용 2 ·
  관리자 4 · project-scoped 59)"**라고 적고 있었다. **직접 실측**해 확인했다 —
  `tests/test_auth_api.py`의 `test_every_operation_lands_in_exactly_one_named_tier`가
  `len(tiers)==71` · `len(by_tier["project"])==60`을 단정하고, 리터럴은 `PUBLIC` 4개 ·
  `AUTH_ONLY` 2개 · `ADMIN` **5개**(D8-5a 3 + 5-c KPI + 6d purge)다. 4+2+5+60 = **71**로 합치한다.
- 즉 **세 숫자가 모두 낡았다**(69→71 · 관리자 4→5 · project 59→60). D8-5c·D8-6d가 카운트를
  올렸는데 changelog에만 반영되고 본문에 안 내려왔다. G1=C와는 무관한 문서 부채다.
- **정본·HANDOFF 3곳 정정**하면서 **"이 숫자의 권위 소스는 문서가 아니라 코드"**를 함께 적었다 —
  같은 drift가 재발할 때 어느 쪽을 믿을지가 정해져 있어야 한다. HANDOFF의 "69개 tier 분할"·
  "`/health` 제외 69개(전체 70)"도 각각 71·70(전체 71)로 고쳤다.

### #3 [★ 가장 중요] "시행 완료"는 파일 수준에서만 참이었다

- 검증자가 `docker ps -a`로 잡았다: `mongo-1`·`gateway-1`·`elasticsearch-1`·`chroma-1`·`embedding-1`이
  `Exited (255) 25h` 상태로 **여전히 `0.0.0.0:27520->27017` 등 옛 매핑을 보유**한다. **포트 매핑은
  컨테이너 생성 시점에 고정**되므로 compose 파일 수정은 기존 컨테이너에 적용되지 않는다.
- **지금 노출은 없다**(전부 Exited). 그러나 **옛 컨테이너를 그대로 살리면 LAN 노출이 복귀한다.**
  내가 "시행까지 끝냈다"고 보고한 것은 **파일 수준에서 참이고 런타임 수준에서 거짓**이었다.
- **기록으로 닫았다**: HANDOFF 함정 절에 **"compose 포트 매핑 변경은 이미 만들어진 컨테이너에
  적용되지 않는다 — `up -d`로도 안 된다. 확인법은 파일이 아니라 `docker port`다"**를 신설하고,
  "지금 상태" ★항목과 SoT v1.7.75 엔트리에도 시행 범위가 compose 파일임을 명시했다.
  같은 성질이 env·command 변경 전반에 적용되므로 D8-7 한정 문구로 쓰지 않았다.
- **재생성 자체는 하지 않았다** — 오너의 머신 상태이고 스택 전체 재기동을 수반한다. 오너 결정 대기
  항목으로 올렸다. 함께 보고된 크래시 루프 고아(`frontend`·`worker`·`generation_worker`,
  `restart: unless-stopped`)도 같은 `down` 한 번으로 정리되는 같은 맥락의 상태다.

### Verification (hardening 후 재실측)

- `tests/test_compose_exposure.py` **6 passed / 7 subtests**(신규 `ComposeParserTest` 1셀 추가).
- 인라인 우회 뮤테이션 재현 → 이제 분류 가드가 실패(닫힘 확인), 원복 후 트리 청결.
- **전량 회귀(hardening 후 최종)**: **1837 passed / 4 skipped / 1556 subtests / 103.02s**, exit 0.
  직전 1836 대비 **+1 = `ComposeParserTest`**, 회귀 0건. 08-01 기준선 1831 대비 +6/+7.

---

## 런타임 반영 — 스택 재생성 (오너 결정: "down 후 즉시 up -d까지")

검증 Outstanding §1(옛 컨테이너가 옛 매핑 보유)을 오너가 **재생성**으로 닫기로 했다.

### 절차와 실측

- `docker compose -f docker-compose.yml -f docker-compose.llama.yml down` — llama override를 함께 준
  이유는 `llama-1`을 **정의된 서비스로** 처리하기 위함이다(base만 주면 orphan이 된다).
  named 볼륨 **5개 전부 보존** 확인(`mongo_data`·`es_data`·`chroma_data`·`embedding_cache`·`llama_models`).
- `docker compose up -d` — **base만**. llama override를 뺀 것은 판단이다: `-hf`가 리비전을 고정하지
  않아 **6.5 GB 재다운로드 위험**이 있고(추적 부채), 창 기본값이 8192라 report 경로가 K-3 가드에
  400으로 막힌다(★ 함정). 이 머신에 `.env`는 **없다**.
- **런타임 포트 매핑(`docker port` 실측)** — G1=C가 런타임에서 닫힌 것의 직접 증거:

  | 서비스 | 매핑 | |
  |---|---|---|
  | mongo · chroma · elasticsearch · gateway · embedding | `127.0.0.1:27520/8523/9520/8521/8522` | loopback ✓ |
  | application · frontend | `0.0.0.0:8520` · `0.0.0.0:5520` (+ `[::]`) | 의도된 공개 ✓ |

- **★ LAN 도달성 직접 측정**(호스트 LAN IP `172.30.135.149`로 TCP 연결 시도) — 파일도 `docker port`도
  아닌 **네트워크 수준의 최종 증거**:
  - 저장소·내부 5종: **loopback OPEN / LAN closed** (5/5)
  - `application`·`frontend`: **LAN OPEN** (의도)
- health: **healthy 7 + 워커 2**(healthcheck 없는 by design) = 문서화된 정상 상태.

### ★★ 재생성이 드러낸 것 — 실행 중이던 이미지가 *인증 없는 제품*이었다

- `application` 이미지가 **2026-07-22 빌드**로 **auth 슬라이스(D8-1~6) 전체보다 앞선다**. 실측:
  **`POST /auth/login` → 404**, **`GET /projects` → 세션 없이 200**. 즉 HTTP 인증 시행이 통째로 없다.
- **이것이 왜 이 슬라이스의 문제인가**: G1=C가 `application`·`frontend`의 LAN 공개를 유지한 근거가
  **"이 둘은 세션 뒤에 있다"**였다. 오래된 이미지에서는 **그 근거가 성립하지 않는다.** 재생성으로
  스택을 올리는 순간 나는 LAN에 **무인증 API를 게시**한 것이고, 발견 즉시 `docker compose stop
  application frontend`로 닫은 뒤(LAN 8520·5520 closed 확인) 두 이미지를 재빌드했다.
- **HANDOFF 부채 서술을 정정했다.** 종전 항목은 "임포트 체인이 `ModuleNotFoundError`로 **죽는다**"고
  적었는데 **실측은 반대**다 — 그 시점 코드에는 `argon2` import 자체가 없어 **앱이 정상 기동해
  healthy를 보고한다**. `import argon2`가 실패한다는 관측은 맞았고 **거기서 추론한 증상이 틀렸다**.
  죽으면 즉시 알지만 healthy는 안 보이므로 **종전 서술이 위험을 과소평가**하고 있었다.
- **규칙으로 남겼다**: 스택을 올린 뒤 **`curl :8520/projects`가 401인지 확인**한다. 200이면 이미지가
  낡은 것이다. 이것은 파일→`docker port`→**앱 행동**으로 이어지는 세 번째 확인 층이며, 검증자가
  잡은 "파일 수준에서만 참" 함정의 같은 계보다.

### 재빌드 후 최종 실측 (런타임 확인 3층 전부)

- 이미지: `application`·`frontend` 둘 다 **2026-08-02 빌드**, 새 이미지에 **`argon2 23.1.0`** 포함.
- **앱 행동**: `GET /projects` **401**(종전 200) · `POST /auth/login` **존재**(종전 404) · `/health` 200.
  = D8-3 인증 시행이 런타임에 실제로 섰다.
- **포트 매핑**: 저장소·내부 5종 `127.0.0.1` / `application`·`frontend` `0.0.0.0` (재확인).
- **LAN 도달성**: 저장소 5종 closed / 제품 표면 2종 OPEN (재확인).
- **health**: healthy 7 + 워커 2. 볼륨 5개 보존.
- **정리**: 이제 `application`·`frontend`의 LAN 공개가 **세션 뒤에 있다**는 G1=C의 전제가 런타임에서도
  참이다. 재빌드 전에는 그 전제가 거짓이었고, 그 간극이 이 세션에서 가장 위험한 발견이었다.

---

## Task — `docs/plans/` 인덱스 부채 해소 (문서만, SoT 무변)

### Goals

- 오너 지시(2026-07-28) "`docs/plans/`가 너무 커져서 정리가 필요하다"의 첫 조각. **이번 세션 첫
  발화가 그 증상 자체였다** — "브리프 내가 선택하는 게 있었던 거 같은데 문서 정리가 하나도 안 돼서
  내가 찾아볼 수가 없네."
- 결정이 필요 없는 부분만 고른다: **도달 가능성**(인덱스에서 모든 문서에 닿는다). 디렉터리 재편은
  미결정이므로 손대지 않는다.

### 착수 전 실측

- 문서 **90개**(README 포함), 그중 **73개가 `*-decisions.md` 브리프**.
- README에 링크된 것 **41개**, **미등재 51개**. HANDOFF에 적힌 기록(88개 중 50개 미등재)보다
  **더 나빠져 있었다** — 부채가 방치되는 동안 계속 자란다는 증거다.
- 미등재 51개는 대략 2026-07-20 이후 전부다(`auth-d8-*`·`observability-*`·`context-budget-*`·
  `external-api-*`·`02b-*`·`04-*` 다수). 즉 **최근 것일수록 안 보인다** — 가장 필요한 것이 안 보이는
  형태였다.

### Completed work

- **가드를 먼저 썼다**(`tests/test_plans_index.py`) — 목표 우선. 처음 돌렸을 때 정확히 **51건**을
  나열하며 실패했고, 그것이 인덱스를 고칠 작업 목록이 됐다.
- **README를 트랙별 표로 재구성**해 **89개 전부 등재**. 파일명 접두 체계가 이미 무너져 있어
  (`00`~`07` 계열 + 접두 없는 최근 것들) **접두가 아니라 트랙**으로 묶었다: 기반·횡단 / Phase 1 /
  2A / 2B / 3 / 4 / 5 / 6 / 7 / 프론트엔드 / 공개 API 계약 / 생성 제어·컨텍스트 예산 / 관측 KPI /
  다중 사용자 인증 / 외부 확장. 각 행은 `문서 | 무엇 | 상태`이며 상태는 각 파일의 `상태:` 줄에서 뽑았다.
- **"읽는 순서"는 6개로 줄였다** — 종전 39항목 목록은 순서이자 인덱스라 둘 다 실패하고 있었다.
  순서는 처음 오는 사람용으로 짧게, 인덱스는 전체를 덮게 갈랐다.
- **파일 이동·병합·삭제 0건.** 브리프는 오너 결정의 근거 기록이라 통폐합하면 "왜 그렇게 정했는가"가
  사라진다(HANDOFF 부채 항목의 경고). 이 슬라이스가 회복한 것은 **정리가 아니라 도달 가능성**이다.

### Verification

- `tests/test_plans_index.py` **2 passed**. 등재 89 = 디스크 89(README 제외).
- **양방향 뮤테이션**:
  - [under-strict] 더미 브리프 `zz-mutation-probe-decisions.md` 생성 → 실패(`'zz-mutation-probe-decisions.md'`).
    파일 삭제로 원복.
  - [over-strict] 인덱스 링크 하나를 없는 파일명으로 변경 → 실패(`인덱스의 깨진 링크 1건`).
    **역방향 치환으로 원복**(아래 사고 참조).
- **전량 회귀**: **1839 passed / 4 skipped / 1556 subtests / 102.80s**, exit 0. 직전 1837 대비
  **+2 = 인덱스 가드 2셀**, 회귀 0건.

### 사고 — `git checkout --`로 미커밋 README 재작성을 날렸다 (HANDOFF 함정 그대로)

- over-strict 뮤테이션을 원복하며 `git checkout -- docs/plans/README.md`를 썼는데, **그 시점 README
  재작성이 아직 커밋 전**이라 HEAD로 되돌아가 **89개 인덱스가 통째로 사라졌다.**
- **HANDOFF 함정 절에 이미 적혀 있는 사고다**("뮤테이션 테스트 원복에 `git checkout -- <file>`을 쓰면
  미커밋 작업이 사라진다 … 원복은 역방향 Edit이나 사전 `cp` 백업으로 한다"). 문서로 알고 있었는데
  실행에서 어겼다.
- 복구는 됐다(작성 내용을 그대로 갖고 있었다). **그 뒤 순서를 바꿨다 — 먼저 커밋하고, 뮤테이션은
  커밋 위에서 돌리고, 원복은 역방향 치환으로 했다.** CLAUDE.md §6이 "체크포인트 커밋은 노이즈가
  아니라 이 사고에 대한 안전장치"라고 적은 이유를 실측으로 재확인한 셈이다.

---

## Task — 포트폴리오 진입 경로 (문서만, SoT 무변)

### Goals

- 오너 요청: **"이 레포를 포트폴리오 용으로도 쓸 거니까 내 결정·검증 계획 등을 평가자가 보기
  깔끔하게 README에 연결"**. 함께 물은 것 — plans 디렉터리 재편도 지금 할지, 기록만 하고 나중에 할지.
- 오너가 판단을 위임했으므로(“생각해보고 네가 괜찮다는 방향으로”) **비용을 재고 갈랐다**.

### User Decisions and Rationale

- **오너 위임(2026-08-02)**: 재편 시점·범위를 구현자 판단에 맡겼다. 아래 실측이 근거다.

### 착수 전 실측 — 두 작업의 비용이 정반대였다

| 후보 | 비용 | 평가자에게 주는 가치 |
|---|---|---|
| **디렉터리 재편**(브리프를 하위 폴더로) | 브리프 경로 인용 **1,083건 / 253개 파일**. 그중 **183개가 `docs/verifications/` 과거 검증 기록** | **사실상 0** — 브리프가 어느 폴더에 있는지는 평가 대상이 아니다 |
| **포트폴리오 진입 경로** | 파일 이동 0건, 신규 문서 1개 + README 1개 절 | **큼** — 아래 공백을 메운다 |

- **공백 실측**: 최상위 `README.md`가 **47줄 · docs 링크 3개**뿐이다. 그런데 저장소에는
  **결정 브리프 73개**와 **독립 검증 기록 202건(39일치, 전부 `## Verdict` 보유)**이 있다.
  즉 이 저장소에서 **가장 특징적인 것이 정문에서 안 보인다**.

### Decisions (구현자 판단)

- **디렉터리 재편은 하지 않는다.** 이유는 취향이 아니라 위 표다 — 옮기면 **지나간 검증 기록의
  본문을 대량 수정**하게 되는데, 검증 기록은 "그 시점에 무엇을 확인했는가"의 원본이다. 그리고
  **아프던 증상(도달 불가)은 앞 슬라이스의 인덱스로 이미 닫혔다.** 남은 동기는 미학뿐이다.
  HANDOFF 부채 항목에 **이 숫자를 적어** 다음 사람이 비용을 알고 열도록 했다.
- **대신 검증 기록에 인덱스가 없다는 것을 발견해 함께 닫았다.** plans와 정확히 같은 부채가
  202건 규모로 있었고, 아무도 세지 않고 있었다.
- **평가자용 문서는 "자랑"이 아니라 "절차 설명"으로 썼다.** 결과 나열보다 **어떻게 결론에
  도달하는가**(브리프 → 결정 → 양방향 가드 → 반증 시도 → 정본 개정)가 재현 가능한 신호다.

### Completed work

- **`docs/verifications/README.md` 신설**(453줄) — ① 이 저장소의 검증이 *반증 시도*라는 것과
  기록 골격(Subject metadata/Scope/Methodology/Findings/Issues/Verdict/Outstanding) ②
  **뮤테이션 기법** 설명("테스트가 통과한다"로는 그 테스트가 무엇을 잡는지 알 수 없다) ③
  **판정 분포 합격 134 · 조건부 합격 54 · 서술형 14** — **조건부가 27%**라는 것이 형식적 통과가
  아니라는 증거다 ④ **대표 기록 4건**(런타임 괴리·프론트 플레이크·`git checkout` 사고·비차단
  지적의 오너 결정 승격) ⑤ **전체 202건 최신순 표**(기록·대상·판정).
  제목과 판정은 각 파일의 `# ` 제목과 `## Verdict` 절에서 뽑았다(202/202 추출 성공).
- **`README.md`에 "어떻게 만들어졌는가" 절** — 5단계 파이프라인 표(각 단계의 산출물 + 실측 규모)와
  **평가자용 3단계 짧은 경로**(결정이 내려지는 방식 → 검증이 잡는 것 → 함정이 쌓이는 방식).
  "문서" 절에도 검증 인덱스·CHANGELOG를 추가했다.
- **가드 일반화**: `tests/test_plans_index.py` → **`tests/test_docs_indexes.py`**. plans 인덱스에
  더해 **검증 기록 인덱스 커버리지**와 **최상위 README 링크 해석**을 잠근다 — 진입점의 깨진
  링크가 가장 비싸기 때문이다.

### Verification

- `tests/test_docs_indexes.py` **5 passed**. 검증 인덱스 링크 202건 전부 해석됨(깨진 링크 0).
- **양방향 뮤테이션**(전부 커밋 뒤에 돌리고 역방향 치환으로 원복 — 앞 슬라이스 사고의 교훈):
  - [under-strict] 새 검증 기록 `2026-08-03/probe.md` 생성 → 실패.
  - [over-strict] 최상위 README 링크를 없는 파일로 변경 → `README.md 의 깨진 링크 1건` 실패.
- 대표 기록 4건의 경로를 **실제 파일로 대조**했고 1건이 어긋나(`r_e_report_pointer_removal.md` →
  실제 `r_e_citation_numbers_audit.md`) 본문까지 열어 서술이 맞는지 확인한 뒤 고쳤다.
  **README에 쓴 주장은 전부 해당 파일에서 확인한 것이다.**
- **전량 회귀**: **1842 passed / 4 skipped / 1556 subtests / 104.67s**, exit 0.
  직전 1839 대비 **+3 = 가드 일반화(2셀 → 5셀)**, 회귀 0건.

---

## Task — README 3축 재구성: 기획 · 개발 · 서비스 (문서만, SoT 무변)

### Goals

- 오너 요청: **"KPI 설정이라든지 시스템 구성이라든지 개발이 아닌 기획쪽 문서도 있을 거다.
  README에서 기획/개발/서비스 식으로 종합적인 걸로 하고 싶다. 잘 표현돼 있는지 봐 달라."**
- 앞 슬라이스가 만든 진입 경로는 **개발 축만** 있었다. 나머지 두 축을 찾아 붙인다.

### 착수 전 실측 — 기획·서비스 자료는 있는데 정문에서 0개다

- 직전 README가 링크하던 docs **8개가 전부 개발 축**이었다(SoT·plans·verifications·HANDOFF 등).
- **기획 축 자료는 실재한다**:
  - **[`observability-kpi-rationale.md`](../../observability-kpi-rationale.md)** — 운영/제품 기획
    관점의 KPI 문서. 본문에 *"포트폴리오에서 오히려 강점으로 쓸 부분"*이라고 **이미 포트폴리오를
    의식해 쓰여 있는데** README·`docs/README.md` 어디에서도 링크되지 않았다. **가장 큰 누락.**
  - `abstract.md` §1 핵심 제품 컨셉 · §2 핵심 설계 원칙 · §15 MVP 범위 제안 · §16 핵심 위험과 대응
  - `plans/00-foundations.md`(제품 목표·불변 원칙) · `plans/product-readiness-backlog.md`
  - `plans/README.md`의 **Phase ↔ MVP 매핑 표**(기술 순서 ≠ 가치 묶음이라는 기획적 구분)
- **서비스 축 자료도 실재한다**: `runbooks/`(1) · `benchmarks/`(1, 실 12B 단계별 실측) ·
  **`live_review_briefs/`(2, 실사용 검수 → 계약 재협상)** · `guides/`(2, 작업 절차) ·
  compose 3파일 + HANDOFF의 머신 구성.
- 즉 **자료 부족이 아니라 배선 부족**이었다.

### Completed work

- **`README.md`를 3축으로 재구성**. 맨 위에 "이 저장소를 읽는 세 축" 표(축 → 답하는 질문 → 시작점)를
  두고 각 축을 절로 폈다.
  - **기획** — 풀려는 문제(장편 창작에서 무너지는 것은 문장력이 아니라 일관성) · 제품 원칙 3개
    (AI 출력은 정본이 아니다 / 기억은 append-only / 모든 주장에 근거 포인터) · Phase↔MVP ·
    **운영 KPI 기획**을 별도 소절로 올렸다(지표를 새로 만들지 않고 Gate 판정을 재활용했다는 점,
    그리고 **한계를 함께 적었다는 점**을 강조 — 후자가 이 문서의 진짜 강점이다).
  - **개발** — 기존 파이프라인 절을 그대로 두되 제목을 축에 맞췄다.
  - **서비스** — 구성 표 · **노출 정책**(저장소는 loopback, LAN에는 인증 뒤 표면 둘 + llama) ·
    관측(8개 호출부 전부 계측, 실패도 센다) · **실사용 되먹임**(실검수 브리프 = 코드 이슈가 아니라
    계약 재협상 기록 · 벤치마크 = 기본값이 추정이 아니라 측정에서 왔다) · 운영 절차 · 함정 축적.
  - 하단에 **문서 지도** 표를 남겨 기존 "문서" 절의 역할을 흡수했다.
- **`docs/README.md`도 3축과 맞췄다.** 종전 머리말이 "두 영역으로 구분한다"였는데 실제로는 8개
  하위 디렉터리가 있어 **사실과 어긋나 있었다**. 성격별 지도 표(기획·계약·결정·검증·실행 이력·
  서비스/운영·작업 절차)로 바꾸고 최상위 README를 진입점으로 가리켰다.
- **가드 보강**: 링크 정규를 `\]\(([^)#]+\.md)(?:#[^)]*)?\)`로 넓혀 **앵커가 붙은 링크의 파일
  경로도 검사**하게 했다. 종전 정규는 `#` 포함 링크를 통째로 건너뛰어, 앵커를 달면 파일이 없어도
  조용히 통과했다(이번에 `README.md#phase와-mvp의-관계`를 쓰면서 발견).

### Decisions (구현자 판단)

- **"로컬 1인 운영(dogfood 직전)"이라고 명시했다.** 서비스 축을 쓰면서 실사용자 지표가 있는 것처럼
  보이게 쓸 수 있었지만, GATE-1(dogfood 착수)은 아직 오너 결정 대기다. **단계를 정확히 적는 것이
  과장보다 강하다** — 이 저장소의 다른 문서들이 전부 그렇게 쓰여 있다.
- **KPI 문서의 "한계 명시"를 앞세웠다.** 지표를 자랑하는 것보다 *"이 점수는 거친 1차 근사이고
  지적의 개수·조합은 버려진다"*를 적어 둔 것이 기획자로서 더 신뢰 가는 신호다.
- **앵커 안정성**: `### 운영 KPI 기획 ★`처럼 제목에 기호를 넣으면 GitHub 앵커가 흔들려 ★를 뺐다.

### Verification

- **링크 전수 검사**: `README.md`·`docs/README.md`의 `.md` 링크(앵커 포함) **깨진 것 0건**,
  디렉터리 링크도 전부 해석됨.
- **주장 대조**: README에 쓴 사실을 원본에서 확인했다 — `abstract.md`의 §1/§2/§15/§16 존재,
  `live_review_briefs`·`benchmarks`·`runbooks`·`guides` 파일 실재, 프론트의
  `review`·`observability`·`writing` 디렉터리 실재, 8개 호출부 계측·SoT v1.7.75·브리프 73개.
- `tests/test_docs_indexes.py` **5 passed**.
- **전량 회귀**: **1842 passed / 4 skipped / 1556 subtests / 105.70s**, exit 0. 문서 변경이라
  셀 수는 그대로이고 **회귀 0건**이다.

---

## Task — purge reconciler: 파기된 project 의 잔류 데이터 정리 (D8-6 잔여, SoT 무변)

### Goals

- D8-6 잔여 둘(감사 로그 · 완전 멱등 재시구) 중 **결정이 필요 없는 쪽**을 골랐다. 감사 로그는
  저장 위치·필드·조회 표면이 사실상 결정이라 브리프가 선행돼야 한다.
- 성공 기준: ① 고아를 찾는다 ② **살아 있는 project 를 안 건드린다**(삭제 도구라 이 방향이 더 위험)
  ③ 기본이 안전하다(dry-run) ④ 컬렉션 로스터가 낡아도 동작한다.

### 결함의 정확한 형태 (착수 전 코드 실측)

`POST /admin/projects/{id}/purge`([`main.py:2801`](../../../services/application/app/main.py))의 순서는
**core_sot(8컬렉션) → derived 8종 → enqueue** 다. core_sot 이 성공하고 derived 에서 mongo 장애가
나면 전역 handler 가 503 을 내는데 — **재시도가 불가능하다.** 두 번째 호출에서
`core_sot.purge_project` 가 `NotFound` 를 던져 **404** 로 끝나고, derived 는 영원히 남는다.

- endpoint docstring 과 SoT 는 "부분 실패 시 재시도(멱등)"라고 적었다. **그 주장은 derived 단계
  실패에는 성립하지 않는다** — 재시도할 방법 자체가 없다.
- 08-01 기록은 잔류물을 **"ghost(무해)"**로 판단했다. **동의하지 않는다**: `llm_call_audits` 에는
  그 project 의 **프롬프트 본문**이, `writing_drafts_scratch` 에는 **원고 후보**가 남는다.
  파기를 요청받은 데이터가 남는 것이므로 D5 "부분 삭제는 조용한 고아" 금지 위반이다.
  다만 **매우 드물다**는 판단에는 동의하므로, endpoint 를 고치는 대신 **정리 도구**를 만들었다.

### Decisions (구현자 판단)

- **endpoint 순서를 바꾸지 않았다.** derived 를 먼저 지우도록 뒤집으면 재시도는 되지만, 실패를
  방치했을 때 **"살아 있는데 기억만 지워진 project"**가 남는다 — 지금의 "사라졌고 잔류물이 있다"
  보다 나쁘다. 순서 변경·`core_sot.purge` 멱등화는 **404 계약의 의미를 바꾸는 오너 결정**이라
  아래 Next steps 로 올렸다. reconciler 는 그 결정과 무관하게 유효하다(어느 쪽을 고르든 필요).
- **컬렉션 목록을 코드에 적지 않았다.** `project_id` 를 쓰는 컬렉션을 **DB 에서 발견**한다. 새
  derived 컬렉션이 생겨도 자동으로 덮이므로 이 스크립트가 로스터와 함께 낡지 않는다
  (2026-08-01 교훈: 목록을 믿지 말고 디렉터리/DB 를 읽어라).
- **기본은 dry-run.** 파기는 비가역이라 안전한 쪽이 기본이어야 한다. `--apply` 명시 필요.
- **앱 route 를 안 친다** → 세션 로그인 불필요(워커와 같은 Mongo 직접 접근). 스크립트 로그인
  디스커버리 가드에도 걸리지 않음을 확인했다.

### Completed work

- **[`scripts/purge_reconciler.py`](../../../scripts/purge_reconciler.py)** — 발견 → 대조 → (선택)
  파기 → enqueue. `projects` 에 행이 없는 `project_id` 가 고아다. 삭제 후 **마지막에**
  `PROJECT_PURGED` 를 enqueue 해 worker 가 vector/lexical 5백엔드를 지우게 한다(6c drain).
- **회귀 6셀**(`tests/test_purge_reconciler.py`, 실 Mongo) — 고아 발견 · **살아 있는 project 보존** ·
  모르는 새 컬렉션도 스윕 · dry-run 무해 · CLI 배선 · enqueue 순서.

### Verification

- **CLI 관통**(test-mongo, 시드 후 실행): dry-run 은 데이터를 그대로 두고 조사만 출력, `--apply` 는
  고아 2건 삭제 + `live` 보존 + outbox 에 `project_purged` PENDING 1건 생성. 요약 JSON 확인.
- **양방향 뮤테이션 3종**(전부 커밋 위에서 돌리고 원복):

  | 뮤테이션 | 실측 |
  |---|---|
  | enqueue 를 삭제보다 먼저 | 순서 셀만 실패 |
  | 살아 있는 판정 제거(`project_id in live` 삭제) | 고아 판정 셀 + 순서 셀 실패 = **살아 있는 원고를 지우는 방향이 잡힌다** |
  | 컬렉션 발견을 하드코딩 목록으로 | 목록에 `index_sync_outbox` 를 **넣으면** "모르는 컬렉션" 셀만, **빼면** 순서 셀까지 2건 실패 (2026-08-02 검증 hardening #2 로 정밀화 — 처음엔 내 뮤테이션(포함)만 보고 "1건"이라 적었다) |

- **전량 회귀**: **1848 passed / 4 skipped / 1559 subtests / 104.20s**, exit 0.
  1842 대비 **+6 = reconciler 6셀**, subtests +3, 회귀 0건.

### ★ 첫 판 가드가 거짓이었다 (뮤테이션이 잡은 것)

- 순서 셀을 처음 썼을 때 **뮤테이션이 통과했다**. 원인: `_collections_scoped_by_project` 는 apply
  루프 **전에** 스캔하는데, 그 시점 `index_sync_outbox` 가 비어 있으면 "project_id 를 쓰는
  컬렉션"으로 **발견되지 않아** 삭제 대상이 아니고, 따라서 순서가 무의미해진다.
- 즉 내가 스크립트 주석에 적은 함정은 **outbox 에 기존 항목이 있을 때만** 성립한다(실 배포에서는
  흔한 조건이지만 빈 DB 에서는 아니다). 셀은 통과하되 **아무것도 잠그지 않고 있었다.**
- setUp 에 **죽은 project 를 향한 기존 sync 작업**을 넣어 실제 조건을 복원했고, 그 뒤 뮤테이션이
  정확히 그 셀만 물었다. **"테스트가 통과한다"로는 그 테스트가 무엇을 잡는지 알 수 없다**의 실례다.

---

## Hardening — purge reconciler 독립 검증(2026-08-02, `1032b16`) 반영

**조건부 합격.** 검증자가 내 결함 진단 둘(503→404 재시도 불가 · D5 위반)을 **코드로 확증**했고
반증에 실패했으며, **거짓 가드 자기 고백도 재현**했다 — `66f884e`(setUp 의 outbox entry)를 되돌리자
순서 뮤테이션이 PASSED 로 통과하고, 되돌리지 않으면 잡힌다. 즉 그 정정이 셀을 진짜로 살렸다.
회귀 1848 재현, 뮤테이션 3종 전부 re-fail.

### Blocking #1 — 권위 소스가 여전히 거짓을 단언하고 있었다

- 내가 결함을 발견·확증·수습하고 HANDOFF 에 정정까지 적었지만, **거짓 단언의 원천을 안 고쳤다**:
  SoT v1.7.74 changelog("부분 실패 시 **멱등 재시도**")와 endpoint docstring([`main.py:2806`]
  (../../../services/application/app/main.py), "클라이언트 **재시도(멱등)**"). 이번 슬라이스 3커밋이
  SoT·main.py 를 전혀 안 건드렸기 때문이다.
- 내가 오너 결정(ⓐⓑⓒ) 이후로 정정을 미룬 것은 "결정에 따라 정정 내용이 달라진다"는 판단이었는데,
  **그 사이 미래 독자는 거짓을 읽는다.** 검증자 제언대로 **결정과 무관한 즉시 정정**이 가능했다 —
  지금 코드가 무엇을 하는지는 결정과 무관하게 확정된 사실이기 때문이다.
- **정정 3곳**:
  - **endpoint docstring** — "재시도(멱등)"를 지우고 **재시도가 왜 불가능한지**(core_sot 선행 →
    404 → derived 미도달), **잔류물이 왜 무해하지 않은지**, **수습은 `purge_reconciler.py`** 임을 적었다.
  - **SoT v1.7.76 신설** — 거짓 단언을 정정하고 실제 한계·수습 경로·오너 결정 3지선다를 계약에 올렸다.
    v1.7.74 행은 **다시 쓰지 않고**(이력이다) 취소선 + `[정정 v1.7.76 …]` 포인터만 달았다.
  - **SoT 본문 §"Product Shell"** — 패턴 스윕에서 같은 부류를 하나 더 찾았다: **"영구 삭제 정책은
    여전히 미확정이며 D5에서 결정한다"**가 D5=A 확정·D8-6 출시(v1.7.69~v1.7.74)를 못 따라가 낡아
    있었다. 검증자가 지적하지 않은 항목이다.
- **★ 정정을 산문으로 끝내지 않고 실행 가능한 사실로 만들었다** —
  `AdminProjectPurgeTest::test_a_second_purge_is_404_and_never_reaches_the_derived_services` 가
  재시도의 **404** 와 **derived 미도달**(spy 로 단정)을 잠근다. **뮤테이션 실증**: 오너 결정 ⓒ
  (`core_sot.purge` 멱등화)를 흉내내 `except NotFound: pass` 로 바꾸자 이 셀이 실패한다. 즉
  나중에 ⓑ·ⓒ 를 구현하면 **문서 정정을 강제한다** — 같은 거짓이 다시 쌓이지 않는다.

### #2 [비차단] 뮤테이션 표가 실제보다 좁게 적혀 있었다

- 검증자: "컬렉션 발견 하드코딩 → 모르는 컬렉션 셀만"이라 적었으나 순서 셀도 잡힌다.
- **직접 재현해 사실을 확정했다**: 하드코딩 목록에 `index_sync_outbox` 를 **포함하면 1건**(내 원래
  뮤테이션), **빼면 2건**(검증자의 변형)이 실패한다. 둘 다 맞고 **내 표가 일반 진술로는 약했다**.
  표를 조건까지 적는 형태로 고쳤다.

### #3 [비차단] dry-run 요약에 살아 있는 쪽의 규모가 없었다

- 삭제 도구인데 `orphan_project_ids` 만 보이고 "안 건드리는 live project 가 몇 개인지"는 안 보였다.
  운영자가 실행 전에 확인할 근거가 요약만으로는 부족하다는 지적이 옳다.
- `live_project_count` 를 요약에 추가하고, CLI 셀이 **stdout 을 파싱해 값을 단정**한다(출력 계약을
  잠그지 않으면 다음 사람이 조용히 지운다).

### Verification (hardening 후)

- `AdminProjectPurgeTest` **5 passed**(신규 한계 셀 포함) · `tests/test_purge_reconciler.py`
  **7 passed / 3 subtests**(신규 요약 셀 포함).
- ⓒ 흉내 뮤테이션 → 신규 한계 셀 + 기존 404 셀 2건 실패, 원복 후 green.
- **전량 회귀**: **1850 passed / 4 skipped / 1559 subtests / 107.28s**, exit 0, 회귀 0건.
  1848 대비 **+2 = purge 한계 셀 + reconciler 요약 셀**.
- **부수 발견**: 검증자가 추가한 `verifications/2026-08-02/purge_reconciler.md` 가 검증 인덱스에
  미등재라 **오늘 만든 인덱스 가드가 실제로 잡았다**(1 failed). 등재하고 판정 분포도
  재실측해 맞췄다(합격 134 · 조건부 **55** · 서술형 14 = **203건**). 가드가 하루 만에 값을 했다.

### Next steps

- **[오너 결정 후보] endpoint 자체의 재시도 가능성.** 선택지는 ⓐ 현행 유지 + reconciler 로 수습
  (지금) ⓑ derived 를 먼저 지워 재시도 가능하게(대신 실패 방치 시 "살아 있는데 기억만 없는
  project") ⓒ `core_sot.purge` 를 멱등화(대신 없는 project 도 204 — **404 계약 변경**).
  ⓑ·ⓒ 는 계약·데이터 안전성 트레이드오프라 브리프감이다.
- **purge 감사 로그**는 여전히 미착수이며 작은 브리프 선행을 권한다.

### Next steps (앞 슬라이스들)

- **D8-7의 남은 반(G2~G6 = 자격증명)은 원격/다중 호스트 배포 시점**이다. 브리프에 실행 계획으로 남았다.
- 지금 결정 없이 열 수 있는 잔여는 어제와 같다 — **purge 감사 로그**(작은 브리프 선행 권장:
  저장 위치·필드·조회 표면이 사실상 결정 사항) · **완전 멱등 재시구/reconciler**.
- **D8-5 C-1~C-6은 여전히 오너 결정 대기**(5-b·5-d 차단).

---

## Task — 기획 축 "제품 한 장 요약" (문서 부채, 오너 지시 2026-08-02 · SoT 무변)

### Goals

- README를 3축으로 재구성하면서 드러난 공백을 닫는다 — **제품 컨셉·MVP 범위·핵심 위험이 전부
  `abstract.md`(48KB) 안에** 있어서 평가자든 새 작업자든 §1·§2·§15·§16을 직접 뽑아 읽어야 했다.
- 성공 기준: ① 한 자리에서 "무엇을·누구를 위해·어디까지"가 답해진다 ② **원본 요약이 아니라
  현재 상태 기준**이다 ③ 원안과 달라진 지점이 명시된다 ④ 링크·수치가 실측과 일치한다.

### 착수 전 판단 — 왜 "요약"이 아니라 "다시 쓰기"인가

HANDOFF가 경고한 그대로다. `abstract.md`는 **2026-06 초안**이고 그 뒤 D0(다중 사용자 전환)·MVP
범위 조정이 있었다. 초안을 압축하면 **낡은 그림이 정문에 걸린다.** 그래서 초안을 뼈대로만 쓰고
**코드를 실측해** 현재 상태로 다시 썼다. 실측에서 드러난 원안 대비 차이 5건이 문서의 §5가 됐다.

| 축 | 원안(2026-06) | 실측된 현재 | 근거 |
|---|---|---|---|
| 사용자 | 단일 사용자, 계정·로그인 없음 | 다중 사용자 — 계정·세션·소유권 403·관리자 tier·파기 | `auth/`, `plans/multi-user-auth-cms-decisions.md` |
| 추출 | 5종(Character·Event·Location·Foreshadowing·Relation) | **관찰 3종** — 장소·관계 미착수, 떡밥은 `open_question_observation` | [`analysis/models.py:12`](../../../services/application/app/analysis/models.py) |
| Gate | Continuity·POV·떡밥 Gate 각각 | **Writing Gate 하나** — 지적 유형 4종·판정 5종. `style` 지적은 판정을 안 바꾼다 | [`writing/gate_prompt.py:26`](../../../services/application/app/writing/gate_prompt.py) · `:30` |
| 문체 | 과거 글에서 **학습**(voice_samples·style_profiles) | 사용자가 **선언** — 프로젝트 브리프의 `style_rules`·`preferred_patterns`·`forbidden_patterns`·`style_examples` | [`main.py:1575-1590`](../../../services/application/app/main.py) |
| 관측 | 초안에 없음 | 8개 호출부 전량 계측 + KPI 화면(프로젝트별·전역) | `observability/` |

MVP 4(Memory Console)도 실측했다 — **승인 UI만 섰다**(Review Inbox: 승인·거절·수정·병합·분할이
`reconcileConflict`까지 배선됨). 인물 카드·타임라인·관계 그래프 화면은 **0건**(`frontend/src/`에
`timeline|character card|relation` 매칭 파일 없음).

### Completed work

- **[`docs/product-overview.md`](../../product-overview.md)** 신설 — 9절: 한 문장 · 풀려는 문제 ·
  제품 원칙 4 · **MVP 범위(원안 ↔ 현재 도달점)** · **원안에서 달라진 것 5** · 일부러 안 하는 것 ·
  **위험과 현재 대응**(초안 §16의 5종 + 실사용에서 나온 2종, 각각 "남은 공백"까지) ·
  **정직한 공백**(로그인 표면 없음·dogfood 미착수·로컬 1인) · 더 읽을 곳.
- **진입 경로 배선** — 최상위 README 기획 축의 시작점을 `abstract.md` → `product-overview.md`로
  바꾸고(초안은 "원본"으로 유지), 문서 지도·`docs/README.md` 기획 행에 등재.

### 함께 정정 — 패턴 스윕에서 나온 낡은 단언 5건

작성하려면 권위 소스를 전부 읽어야 했고, 그 과정에서 **현재와 어긋난 단언**이 나왔다. 어제
Blocking #1의 교훈(*결정을 기다리는 동안 독자는 거짓을 읽는다*)을 그대로 적용해 **사실 확정이
가능한 것은 즉시** 고쳤다.

| # | 자리 | 무엇이 거짓이었나 | 조치 |
|---|---|---|---|
| 1 | [`plans/00-foundations.md`](../../plans/00-foundations.md) 불변 원칙 | **"MVP는 단일 사용자 제품이다 · 계정·로그인·권한은 구현하지 않는다"** — 인증 전체가 서 있는데 **불변 원칙**으로 단언. README가 "확정된 제품 경계"라며 여기를 가리킨다 | 취소선 + 정정 블록. **원문은 지우지 않았다**(왜 그렇게 계획했는지가 사라진다 — SoT v1.7.74 행 처리와 같은 방식). `project_id` 경계는 **유효**임을 함께 못박음 |
| 2 | [`plans/README.md:14`](../../plans/README.md) | 가드 이름 `tests/test_plans_index.py` — 같은 날 뒤 슬라이스에서 `test_docs_indexes.py`로 일반화돼 **존재하지 않는 파일** | 이름 정정 |
| 3 | 최상위 `README.md` | SoT **v1.7.75**(현재 v1.7.76) | 정정 |
| 4 | `docs/README.md` | 검증 기록 **202건**(현재 203건) | 정정 |
| 5 | [`frontend/src/api/client.ts:527`](../../../frontend/src/api/client.ts) | `ReviewConflict` 주석 **"shown read-only in this slice (no merge/split yet)"** — 그 인터페이스가 `actions`를 들고 있고 UI가 `reconcileConflict`로 제출한다 | 주석 정정(코드 무변) |

### Verification

- **링크 전수 해석**: 손댄 5개 문서의 `.md` 링크(앵커 제외 경로) **깨진 것 0건**.
- **문서 인덱스 가드**: `tests/test_docs_indexes.py` **5 passed**.
- **주장 대조**: 새 문서가 단언한 것을 전부 원본/코드에서 확인했다 — 추출 3종 리터럴 · Gate 지적
  4종과 "style은 판정을 안 바꾼다" · 브리프 문체 필드 4종 · Review Inbox 병합/분할 배선 ·
  타임라인/인물카드 화면 부재 · plans 89 + 브리프 73 + 검증 203 · SoT v1.7.76.
- **전량 회귀**: **1850 passed / 4 skipped / 1559 subtests / 106.34s**, exit 0. 문서 + 주석
  변경이라 **셀 수 무변**, 회귀 0건.
- **프론트**: `npx tsc --noEmit` exit 0(주석만 바뀌었으므로 타입 영향 없음을 확인).

### Next steps — 고치지 **않은** 낡은 단언 3건 (권한 밖이거나 결정 사안)

즉시 정정한 5건과 달리 아래는 **내 판단으로 고치면 안 되는** 것들이다. 전부 file:line까지 실측했다.

- **★ 정본 `system-contract-sot.md` §"현재 구현 상태"(L698~710)가 낡았다.** `Product Shell UI` 행이
  *"다음은 B Review Inbox"*라고 적었지만 Review Inbox는 이미 있고, 바로 아래 행은
  **`Phase 2~6 UI | 미구현`**이라고 단언한다 — Review Inbox·관측 대시보드·Writing 작업공간이
  전부 서 있으므로 **거짓**이다. 정본 본문 정정은 **버전 개정(v1.7.77) 사안**이라 손대지 않았다.
  이 표는 v1.6.x 이후 갱신이 끊긴 것으로 보이며, 실제 현재 상태의 권위 소스는 **변경이력과 코드**다.
  선택지: ⓐ 표를 현재로 갱신 ⓑ **"이 표는 이력이며 현재 상태는 변경이력을 본다"**로 성격을 명시
  ⓒ 표 자체를 걷어낸다. **ⓑ 추천** — 갱신하면 같은 부채가 다시 쌓인다(이미 두 번 그랬다).
- **[`observability-kpi-rationale.md`](../../observability-kpi-rationale.md) §6 "로드맵(지금 범위 밖)"의
  4항목 중 2항목이 이미 완료됐다** — "호출부 계측 확대"(8개 전량 완료)·"집계·대시보드"(API+화면 완료).
  기획 톤 문서라 표현 손질이 곧 논조 변경이고, 포트폴리오 정문 문서이므로 오너 취향 사안이다.
- **[`plans/00-foundations.md`](../../plans/00-foundations.md) "전역 착수 전 결정사항" 체크박스**가
  대부분 미체크인데 실제로는 확정된 것이 있다(에러 envelope=H3 · 삭제/보존 정책=D5=A ·
  monorepo 경계). 위 #1과 달리 **거짓 단언이 아니라 미갱신 체크리스트**라 급하지 않다.

---

## Hardening — 제품 한 장 요약 독립 검증(`bfd4690`) 반영

**조건부 합격**(`verifications/2026-08-02/product_overview.md`). 검증자가 문서의 사실 단언을
**코드로 직접 실측**해 대부분 확증했고("실측했다"는 보고가 진짜였음), 회귀 1850도 재현했다.
그러나 **반복 등장하는 단언 하나가 반박**됐다.

### ★ 확증된 Blocking급 지적 — 근거 인용이 틀렸다 (H-1)

`product-overview.md` §5-⑤가 "8개 호출부 전부가 감사 레코드를 남긴다"고 단언하면서 근거로
`observability-kpi-rationale.md`를 인용했다. **그 파일에는 "8"도 "8개 호출부"도 없다.** 매칭된
3줄(L19·L43·L53)은 "호출부"라는 단어가 있을 뿐이고, **L53은 오히려 "호출부 계측 확대"를
§6 로드맵(= 미완료)으로 나열**한다. 오너가 직접 반박을 시도했으나 실패했다.

- **가장 아픈 부분**: 같은 슬라이스가 **바로 그 §6 로드맵이 낡았다고 work_log에 적어 놓고**,
  그 낡은 문서를 근거로 인용했다. 자기가 stale이라 판정한 것을 근거로 쓴 것이다.
- 교훈: **"낡았다"고 판정한 문서는 그 순간 근거 목록에서도 빠져야 한다.** 판정과 인용이
  같은 세션 안에서 갈라졌다.

### 구현자 재실측 — 검증자 주장 2건은 반박됐다

검증자의 보고도 사실로 받지 않고 다시 물었다(적대적 검증은 양방향이다).

| 검증자 주장 | 재실측 | 결론 |
|---|---|---|
| **"8의 기준이 어디에도 없다"** (scope 10·endpoint 9·TASK_TYPE 5) | `LlmCallSite` enum 멤버 **8** + `ObservedProvider(` 감싸기 지점 **8** + 같은 파일 L56 주석 *"eight real LLM adapters"* | **반박.** 기준은 코드에 **이중으로** 있다. 검증자가 센 10·9·5는 *다른 것*의 개수라 일치할 이유가 없다. **진짜 결함은 "기준 부재"가 아니라 "문서가 기준을 안 밝힘"** |
| **"worker가 미기록이라 '전부'는 거짓"** | 워커 엔트리포인트 [`generation_job_worker.py:110`](../../../scripts/generation_job_worker.py)의 기본 `build_fn` → [`main.py:2148`](../../../services/application/app/main.py) **`llm_call_audit=_default_llm_call_audit_service()`** 배선됨 | **절반 반박.** `= None`은 **테스트 collaborator 기본값**이고 프로덕션 워커는 기록한다. 다만 **script·diagnostic은 계약상 미기록**이므로 "전부"에 한정이 필요하다는 결론은 유지 |

### 수정한 것

| 항목 | 조치 |
|---|---|
| H-1·H-3 "8개 호출부 전부" | `product-overview.md`·`README.md`·`HANDOFF.md` 세 곳 모두에서 **① "8"의 기준 명시**(`LlmCallSite` enum 리터럴 = LLM 어댑터 수, 8개 이름 열거) **② "전부"는 호출부 단위이지 모든 LLM 호출이 아님**(요청 경로 + 생성 워커는 기록, script·diagnostic은 계약상 미기록) **③ 틀린 인용 교체**(계측 범위 근거는 코드·정본 §"LLM 파이프라인 관측", `observability-kpi-rationale.md`는 *지표 선정 이유*의 근거로만) |
| H-4 시점 버전 | `00-foundations.md` 정정문의 "SoT v1.7.57 이후" → **v1.7.49**(D0=A 단계 전환, 2026-07-26). **출처도 함께 고쳤다** — HANDOFF Active Decisions가 v1.7.57이라 적고 있었고 내가 그것을 복사했다(패턴 스윕: 거짓의 원천을 고친다) |
| H-5 SoT 표 판정 과잉 | HANDOFF의 "거짓이다" → **"자기 자신과 모순된다"**(L696·L709 구현 완료 ↔ L710 미구현). Phase 2/3/4 UI(카드·타임라인·관계 그래프)는 **실제로 미구현**이라 그 행은 부분적으로 성립한다 — 내 "전부 서 있다"가 과했다 |
| 검증 기록 | `verifications/2026-08-02/product_overview.md` 신설(검증자 보고 + 구현자 재실측 양쪽). 인덱스 등재 + **203 → 204건 · 조건부 55 → 56**을 세 인덱스에서 재실측 갱신 |

**정본 SoT는 손대지 않았다** — §"현재 구현 상태" 표 처리(ⓐ 갱신 / ⓑ 이력 표로 성격 명시 /
ⓒ 걷어내기)는 버전 개정 사안이고 오너 결정 대기다.

### Verification (hardening 후)

- 손댄 8개 문서 `.md` 링크 **깨진 것 0건** · `tests/test_docs_indexes.py` **5 passed**
  (신규 검증 기록 등재를 가드가 요구했고 등재로 해소).
- 재실측 근거 명령: `LlmCallSite` 멤버 8 · `ObservedProvider(` 8 · `llm_call_scope(` 10(호출부와
  다른 축임을 확인) · SoT 변경이력에서 v1.7.49 = D0=A 단계 전환 확인.
- **전량 회귀**: **1850 passed / 4 skipped / 1559 subtests / 121.34s**, exit 0. 문서 전용이라
  셀 수 무변, 회귀 0건.

---

## Task — D8-5e 관리자 승격(access grant): 소유권을 지나는 유일한 통로 (SoT v1.7.77)

### User Decisions and Rationale

오너가 **C-1~C-6을 한 번에 결정**했다(브리프 `plans/auth-d8-5-admin-decisions.md` §7).
"이 여섯은 서로 얽혀 있어 한 번에 정하는 편이 낫다"는 브리프 권고대로다.

| # | 결정 | 근거 |
|---|---|---|
| C-1 | **고정 TTL, 1시간** | 구현자 추천은 30분이었고 **오너가 1시간을 골랐다**. 재발급 번거로움을 줄이는 쪽. "해제까지"·1회용은 각각 영구 권한화·재발급 폭증이라 기각 |
| C-2 | **읽기 전용** | 지원 시나리오는 "보고 확인"이 대부분이고, 관리자가 남의 원고를 고칠 수 있으면 정본 보존 정책과 충돌 |
| C-3 | **발급 + 그 아래 요청의 operation 단위 감사** | 발급만 남기면 "무엇을 봤는가"가 안 남아 C를 고른 이유가 반감 |
| C-4 | **사후 조회 가능** | 통지 채널이 없으므로 소유자가 자기 project 접근 이력을 볼 수 있는 데까지가 현실적 |
| C-5 | **사유 필수** | 감사 기록의 가치는 "왜"에 있고 한 필드로 얻는다 |
| C-6 | **최초 로그인 시 변경 강제** | 채널 없이 얻는 가장 큰 개선. 초대 토큰은 전달 채널이 없으면 같은 문제의 이름만 바꾼 것. **별개 축이라 이 슬라이스 범위 밖** |

**슬라이스를 쪼갰다**(오너 지시 "한 번에 큰 덩어리 금지"). 이번은 **승격 저장소 + 발급 endpoint
+ 인가 인지(읽기 전용)** 까지. C-3의 operation 단위 감사 · C-4 사후 조회 · 5-b · 5-d · C-6은 후속.

### Decisions (구현자 판단)

- **읽기 전용의 판정을 HTTP method 로 했다**(`GET`/`HEAD`). 대안은 "읽기 operation 목록"인데,
  목록은 **다음에 추가되는 endpoint 를 조용히 오분류**하고 그 방향이 열리는 쪽이다. method 판정은
  모르는 것에 대해 **닫히는 쪽으로 실패**한다(fail-closed).
- **`is_admin` AND 승격**으로 검사한다. 승격만 보면 강등이 무의미해진다.
- **append-only + TTL 인덱스 없음.** `sessions`는 TTL 인덱스로 Mongo 가 reap 하지만, 승격 행은
  **C-3 의 발급 감사 기록 자체**라 reap 하면 접근이 있었다는 증거가 사라진다. 만료는 **판정**이다.
  이 의도적 차이를 회귀가 단정한다(`test_there_is_no_ttl_index`) — 안 그러면 누군가 "일관성"을
  이유로 TTL 을 붙인다.
- **D8-6 파기에 배선했다.** 새 project-scoped 컬렉션을 파기에 안 물리면 **어제 닫은 "조용한 고아"
  금지(D5)를 내가 다시 여는 것**이다.
- **404 를 발급 전에 낸다** — 없는 project 에 대한 승격은 아무것도 아닌 것에 대한 감사 기록이고,
  201 이 project id 존재를 알려 주는 probe 가 된다.

### Completed work

- [`auth/access_grants.py`](../../../services/application/app/auth/access_grants.py) — 모델·저장소·서비스
- [`auth/access_grants_mongo.py`](../../../services/application/app/auth/access_grants_mongo.py) — naive BSON 재부착(sessions 선례), TTL 인덱스 **없음**
- `POST /admin/projects/{project_id}/access-grants`(201, ADMIN tier) — **operation 71→72**
- `require_project_owner` = 소유자 **OR** (관리자 AND `GET`/`HEAD` AND 살아 있는 승격)
- 회귀 **24셀**: `tests/test_auth_access_grants.py` 13 · `AdminAccessGrantTest` 11
- 계약 가드 갱신: tier 리터럴(ADMIN +1, 72) · `{project_id}` 예외 집합(`_ADMIN_PROJECT_ROUTES`) ·
  admin 에러 선언 lock list(+1) · `_declared` 의 성공 코드에 **201 추가**

### Verification

- **뮤테이션 6종 — 전부 정확히 물었다**:

  | 뮤테이션 | 실측 |
  |---|---|
  | 읽기 전용 해제(method 조건 제거) | 쓰기 3종 subtest 실패 |
  | `is_admin` 검사 제거 | 비관리자 승격 셀 + 인증 격리 셀 2건 |
  | 승격 검사 무력화(F1=C 미시행) | 읽기 개방 셀 1건 |
  | 만료 판정 제거(영구 권한화) | 3건(HTTP·서비스·append-only) |
  | TTL 1시간 → 24시간 | 리터럴 셀 포함 4건 |
  | **over-strict**: 소유자 경로에도 GET 제한 | **소유자 전수 셀 33건** — 정상 경로가 깨지는 방향도 잡힌다 |

- **전량 회귀**: **1874 passed / 4 skipped / 1579 subtests / 125.56s**, exit 0.
  1850 대비 **+24 = 신규 24셀**, subtests +20, **회귀 0건**.
- **프론트**: `npm run gen:api` 재생성(schema.d.ts +118줄) · `tsc --noEmit` exit 0 ·
  `vitest` **227 passed / 15 files**.

### ★ 사고 — `git checkout --` 로 미커밋 슬라이스를 날렸다 (HANDOFF 함정 그대로 밟음)

첫 뮤테이션 원복에 `git checkout -- services/application/app/main.py` 를 썼다. 그 시점
**슬라이스가 미커밋**이라 main.py 의 변경(임포트·팩토리·dependency·모델·endpoint·purge 배선)이
**통째로 HEAD 로 되돌아갔다**. 다른 파일은 무사했다(그 파일만 checkout 했으므로).

- **HANDOFF 함정 절에 이미 적혀 있는 사고다** — "뮤테이션 테스트 원복에 `git checkout -- <file>`
  을 쓰면 미커밋 작업이 사라진다. 이 프로젝트는 슬라이스가 아직 커밋되지 않은 상태가 흔하다."
  2026-07-30 독립 검증에서 한 번 났고, 문서에 남겼는데도 **같은 손이 다시 밟았다**.
- **복구**: 대화 이력에서 10개 편집을 재작성해 복원, 82 passed 로 동일성 확인.
- **교훈은 "조심하자"가 아니다** — CLAUDE.md §6 이 규정한 대로 **뮤테이션 전에 체크포인트 커밋**을
  했어야 한다. 커밋 후에는 `git checkout --` 가 **정확히 옳은 도구**가 된다(실제로 그 뒤 헬퍼가
  깨져 파일이 두 번 오염됐을 때 체크포인트로 1초 만에 복구했다). 순서를 지키면 이 함정은 존재하지 않는다.

### Next steps

- **C-3 나머지**(승격 아래 개별 요청의 operation 단위 감사) · **C-4**(소유자 사후 조회) — 다음 슬라이스.
- **5-b**(전 프로젝트 목록) · **5-d**(관리자 화면) — 승격이 섰으므로 이제 차단 해제됐다.
- **C-6**(최초 로그인 비밀번호 변경 강제 + 비밀번호 정책) — 오너 확정, 별개 축, 미착수.

---

## Hardening — D8-5e 독립 검증(`c62944b`+`7381596`) 반영

**조건부 합격**(`verifications/2026-08-02/d8_5e_access_grants.md`). 검증자가 뮤테이션 6종·회귀
1874 를 독립 재현해 구현을 확증했으나, **Blocking 1건**을 잡았다.

### 🔴 Blocking — 승격이 무소유 project 를 입양하고 있었다 (E1=A 위반)

정본이 **세 곳에서** "`owner_id=None` 은 승격으로도 열리지 않는다"고 단언하는데
(v1.7.77 changelog · §경계 · docstring) **코드가 안 지켰다.** `require_project_owner` 의
두 번째 분기(승격)에 `owner_id` 검사가 없었다 — 첫 분기에는 있고 승격 분기에만 빠졌다.

**세 경로가 같은 결론에 도달했다.**

| 경로 | 실측 |
|---|---|
| 검증자(임시 테스트) | 무소유 project + 승격 + GET → **200** |
| 별도 에이전트(**강화 뮤테이션** — 고칠 검사를 미리 넣어 봄) | **82 passed, 아무 셀도 안 깨짐** = 빈 셀의 직접 증거 |
| **구현자 재실측**(반증 시도) | 스크립트로 재현 → **200**(승격 없이는 403). 반증 실패, 확증 |

- **왜 안 잡혔나 — 빈 셀.** `AdminAccessGrantTest` 11셀이 **전부 alice 소유 project** 만 써서
  "무소유 + 승격" 조합이 **0건**이었다. 가드가 촘촘해 보였고 실제로 그 축에는 아무것도 없었다.
- **★ 교훈**: 지난 슬라이스가 "테스트가 통과한다로는 무엇을 잡는지 알 수 없다"였다면, 이번은
  **"가드가 많다가 그 축이 덮였다를 뜻하지 않는다"** 이다. 빈 셀은 통과하는 테스트로 안 보이고,
  **강화 뮤테이션**(고칠 것을 미리 넣어 아무것도 안 깨지는지 본다)이 그것을 드러낸 기법이다.
  내가 쓴 뮤테이션 6종은 전부 *결함을 넣는* 방향이라 **없는 셀은 볼 수 없었다.**
- **수정**(`8f3c52e`, 테스트 선행): 회귀 셀을 먼저 써서 결함 재현 확인 → 승격 분기에
  `project.owner_id is not None` 추가 → green → 뮤테이션(수정 되돌림)에서 **그 셀만** 재실패.

### 비차단 — 뮤테이션 2의 라벨이 과장이었다 (그리고 원인이 더 나빴다)

- 내가 "`is_admin` 제거 → 비관리자 승격 셀 **+ 인증 격리 셀**"이라 적은 두 번째 실패는 실제로
  **크래시**였다: `AttributeError: 'State' object has no attribute 'access_grants'`.
  건수(2)는 맞고 **라벨이 틀렸다.** 표를 정정했다(아래).
- **재실측에서 한 걸음 더 갔다** — 이건 라벨 오류일 뿐 아니라 **잠재 취약점**이다. 격리 셀
  (`test_the_ownership_dependency_cannot_run_without_authentication`)의 probe 앱이
  `access_grants` 를 state 에 안 넣는데, **`is_admin` 단락평가가 그 접근을 막아 주는 덕에**
  통과하고 있었다. 분기 순서를 바꾸는 리팩터가 들어오면 그 셀은 성질을 단정하는 대신 죽는다.
- **수정**: probe 앱에 `access_grants` 를 배선했다. 같은 뮤테이션이 이제 **크래시 없이 정확히
  1셀**(비관리자 승격)만 문다 — 실측 확인.

### 뮤테이션 표 정정 (앞 절 표를 이것으로 읽는다)

| 뮤테이션 | 실측(정정 후) |
|---|---|
| `is_admin` 검사 제거 | **1건** — 비관리자 승격 셀. (종전 표의 "인증 격리 셀"은 오독이었다: 그것은 probe 앱의 `AttributeError` 크래시였고, 이제 그 원인 자체를 없앴다) |

나머지 5종(읽기 전용 해제 3 · 승격 무력화 1 · 만료 판정 제거 3 · TTL 24h 4 · over-strict 33)은
검증자가 독립 재현했고 값이 일치한다.

### Verification (hardening 후)

- `tests/test_auth_api.py` **70 passed / 744 subtests** · `tests/test_auth_access_grants.py` 13 passed.
- Blocking 뮤테이션(수정 되돌림) → 신규 셀 1건만 재실패, 원복 청결.
- 라벨 뮤테이션 재측정 → 크래시 없이 1셀.
- **전량 회귀**: 아래 최종 절.
- 검증 기록 등재 + **204 → 205건 · 조건부 56 → 57**(세 인덱스).

### ★ `git checkout --` 를 같은 슬라이스에서 두 번 밟았다

Blocking 수정 직후 뮤테이션 원복에 또 `git checkout -- main.py` 를 썼고, **그 시점 수정이
미커밋**이라 수정이 날아갔다(앞 절의 사고와 같은 형태, 이번엔 손실이 한 줄이라 즉시 복구).

- 앞 절에서 "순서를 지키면 이 함정은 존재하지 않는다"고 적어 놓고 **바로 다음 슬라이스에서 다시
  밟았다.** 규칙을 아는 것과 지키는 것은 다르다는 실례다.
- **그 뒤로는 수정을 먼저 커밋(`8f3c52e`)하고 뮤테이션을 돌렸다.** 그 뒤 두 번의 뮤테이션 원복은
  `git checkout` 이 **정확히 옳은 도구**였고 실제로 안전했다.
- 규칙을 다시 적는다: **미커밋 상태에서는 역방향 Edit 으로만 원복한다. 커밋한 뒤에는 checkout 이
  안전하다. 그러므로 뮤테이션 전에 커밋한다.**
