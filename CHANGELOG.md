# CHANGELOG

| Date | Change | Detail |
|---|---|---|
| 2026-08-21 | **제품명 통일이 화면 밖까지 닫혔다 — H2 폐쇄(오너 결정 · backend 3줄).** 10.0(D5)이 화면을 "에-라잇" 으로 통일했지만 **FastAPI `title=` 세 자리**가 옛 작업 제목으로 남아 `/docs`·`/redoc` 과 OpenAPI `info.title` 에 그대로 떴다. **오너 결정**: 제품명만 한글로 통일하고 서비스 구분자는 영문 유지(`"에-라잇 Application"`·`"에-라잇 Embedding Service"`·`"에-라잇 LLM Gateway"`) — 전부 한글로 쓰면 코드·로그·compose 서비스명과 글자가 끊겨 검색이 안 이어지고, 로마자 통일은 **"에-라잇" 의 로마자형이 없어 새 명명 결정을 하나 더 낳는다.** **★ 진짜 결함은 세 줄이 아니라 그것이 열 달을 green 으로 살아남은 이유였다** — 프론트에는 `productName.test.ts` 스윕이 있는데 **백엔드에는 짝이 없었다**(렌더·행위 테스트의 사정거리 밖이라는, `<title>` 때와 **같은 병**). 신규 [`tests/test_product_name.py`](tests/test_product_name.py) 3셀이 **세 서비스의 실제 `app.title`**(소스 grep 이 아니라 OpenAPI 에 나가는 값) · **옛 이름의 재유입**(스캔 범위를 하드코딩하지 않아 네 번째 최상위 패키지도 덮는다) · **식별자 불변**(over-strict — `ai_writing_system` 은 표시명이 아니라 **Mongo DB 이름**이고 개명하면 앱이 조용히 빈 DB 를 가리킨다)을 잠근다. 뮤테이션 5종 전부 셀 페어링까지 실측(M1~M3 각 2셀 · M4·M5 각 1셀). `npm run gen:api` 재생성 시 `schema.d.ts` **diff 0**(openapi-typescript 가 `info.title` 을 안 옮긴다 — 2026-08-11 실측이 오늘도 유효). LLM 미사용. | 오너 결정 D-2026-08-21-a, [`daily_logs/2026-08-21/work_log.md`](docs/daily_logs/2026-08-21/work_log.md) |
| 2026-08-20 | **리랭커 독립 검증 — 조건부 합격, 조건 폐쇄까지 완료. 그리고 그 조건은 구현자가 스스로 넘긴 의심이었다.** 슬라이스를 인계하며 *"볼 만한 축"* ②에 **"fail-open 이 정말 모든 실패를 덮는가 — `text_of` 가 던지면 데코레이터가 안 잡는다"** 를 적었고, 검증이 그것을 **런타임으로 실증**해 조건 C1 이 됐다. 투영은 이 슬라이스가 검색 경로에 **새로 넣은 호출**이라 그 예외로 죽는 길도 이 슬라이스가 만든 것이었다. **폐쇄는 단계 전체 fail-open** — 투영·프로바이더 호출·순열 검사를 한 `try` 안에 두고 `except Exception` 으로 받는다. **★ 조용히 삼키지 않는다**: `logging.WARNING` + `exc_info` 로 남긴다. **fail-open 이 조용하면 리랭킹이 영원히 no-op 인 채로 아무도 모르고**, 그것이 이 저장소가 **오늘 네 번 만난 실패 모양**(*"green 이 말하지 않은 것"*)이다 — 선례는 `activity/log.py` 의 A4=A 격리 경계. over-strict 쪽도 잠갔다(정상 경로가 경고를 내면 로그가 소음이 되고 아무도 안 본다). **★ 문언 축소 대신 검사 확대를 고른 것이 오늘 네 번째다**(정본 산출물 문언 · 셀 실패 메시지 · 임베딩 조립 가드 · 이것) — 문언을 줄이면 계약이 **약해진 채 합의**된다. 비차단 둘도 닫았다: **유일-생성자 가드에 별칭·모듈 속성 인식**(같은 날 임베딩 B1 폐쇄에서 배운 `asname` 인식을 **몇 시간 뒤 같은 형태의 가드에 안 옮긴** 것이 그 자체로 교훈이다) · **동률 문언 정정**(안정 정렬이 보존하는 것은 요청 순서가 아니라 **응답 순서**다). **★ 재현 스크립트의 R3b 블록은 폐쇄로 리터럴이 낡았다** — `count=0` 은 *"안 물었다"* 가 아니라 뮤테이션이 낡은 것이고, 같은 의도를 새 형태로 다시 돌려 확인했다(오늘 *"뮤테이션이 안 물면 뮤테이션부터 의심한다"* 가 **세 번** 났다). 전수 `2354/1/2586`. | [검증](docs/verifications/2026-08-20/reranker_slice.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-20 | **뉴럴 리랭커 seam·외부 어댑터·평가 하네스 — 외부 API 확장 세 축(LLM·임베딩·리랭커)이 전부 코드로 섰다.** 2026-07-27 D5 로 결정만 돼 있고 **기능이 0줄**이던 축이다. `RerankProvider` seam + 외부 API 어댑터 + **retriever 데코레이터**(결정 3=A)로 붙였고 **도메인 코드는 0줄** 바뀌었다 — 두 융합 자리(정본 memory · 검토 대기 candidate)의 seam 이 이미 같아서 한 데코레이터가 둘 다 덮고, **vector-only·lexical-only 구성에서도 걸린다**(Hybrid 클래스 안에 넣는 선택지는 hybrid 에만 걸려 구성에 따라 동작이 갈렸다). **★ 기본은 꺼짐이다**(결정 2=A): `RERANK_API_URL` 이 비면 조립이 감싸지 않고 검색은 **RRF 융합까지만**으로 지금과 똑같이 동작한다 — **끄기가 조립에서 끝난다.** **★ 실패는 열려 있다**: 리랭커가 죽거나 **순열이 아닌 응답**(부분 `top_n` 포함)을 주면 융합 순서가 그대로 나간다 — 부분 응답을 받으면 그것은 재정렬이 아니라 **조용한 필터링**이고 이 seam 이 약속한 일이 아니다. **★ 정본의 글자와 현실이 어긋난 자리를 하나 발견했다** — D2=A 가 *"generic **OpenAI-호환**"* 이라 적었지만 **OpenAI 에는 rerank 엔드포인트가 없다.** 그 결정이 겨눈 성질은 *"벤더 전용이 아닌 형식"* 이었고 리랭킹에서 그 자리는 Cohere 가 낸 `POST /v1/rerank`(Jina·Voyage·TEI 가 같은 모양)다 — **글자를 그대로 따랐으면 존재하지 않는 계약을 구현할 뻔했다.** seam 은 점수가 아니라 **순서**를 돌려준다(점수를 흘리면 그것으로 자르는 두 번째 정책이 곧 붙는데 top-k 는 지연 실측 뒤로 미룬 결정이다). 조립 가드는 `ObservedProvider` 선례의 처방인데 **리랭커가 더 조용하다** — 계측이 사라지면 대시보드가 비지만 **재정렬이 사라지면 아무것도 안 비고 순위만 예전으로 돌아간다.** **평가 하네스는 선작성했고 정답은 채우지 않았다**(결정 4-②): 구현자가 채점표를 만들면 *"리랭커가 좋은가"* 가 아니라 *"리랭커가 구현자 생각과 같은가"* 를 재게 되므로, **정답은 dogfood 실사용 질의에 오너가** 붙인다 — 그 경계를 셀로 잠갔다(저장소에 평가셋이 들어오면 실패한다). 하네스 포맷은 *"리랭킹 전/후"* 가 아니라 **"구성 이름 → 순위 목록"** 이라 임베딩 모델 교체·RRF `k` 조정 비교에도 그대로 쓰인다. **★ 표기는 셋으로 갈라 적는다**: *"외부 API 리랭커 붙일 수 있음(기본 꺼짐) · self-host 미구현 · **품질 평가 미실시**"* — 하네스가 있다는 사실은 "평가했다" 가 아니다. 전수 `2350/1/2582`, 뮤테이션 7종 전부 재실패. | [브리프](docs/plans/reranker-slice-decisions.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-20 | **임베딩 어댑터 독립 검증 — 조건부 합격, 조건 폐쇄까지 완료.** 검증자가 지목 축 넷을 실측했고 셋은 참(헬퍼가 env→provider 만 · 경로 안 `v1` 무영향 · 기본값 `native` 무영향을 **코드·셀·compose 렌더 세 층**에서), **①이 실존**이었다 — `import RemoteEmbeddingProvider as REP` 뒤의 `REP(…)` 를 가드가 **침묵으로 통과**시켰다. **★ 지적의 핵심은 또 문언이었다**: 브리프·셀·HANDOFF 셋 다 *"생성자를 직접 부르는 자리가 **하나라도** 있으면 실패"* 라 단정했는데 별칭 호출이 **그 "하나" 이면서 실패하지 않았다.** **오늘 같은 병이 세 번째**(mypy 정본 산출물 문언 · 재검의 셀 실패 메시지 · 이 가드)이고 **세 번 다 폐쇄는 "문언 축소" 가 아니라 "검사를 넓힌다" 였다** — 문언을 줄이면 계약이 **약해진 채 합의**되기 때문이다. 폐쇄는 파일마다 `ImportFrom` 의 `asname` 을 원이름 집합에 더하는 것(**별칭은 파일 스코프**)이고, **잔여를 문언에 명시했다** — 할당 별칭(`P = Remote…; P(…)`)은 안 잡는다(이름 재결합 추적은 타입체커의 일). 검증자 재현 스크립트를 그대로 돌려 **V1 이 `16 passed` → 가드 실패로 뒤집히는 것**을 확인했다. 비차단 셋도 함께: **스캔 범위 하드코딩 → 분류 강요**(새 최상위 파이썬 디렉터리는 스캔하거나 이유와 함께 범위 밖 · **반대쪽도** — 목록만 남고 디렉터리가 사라지면 스캔이 0파일이 되어 조용히 통과한다) · **external env 3종 표기 셀**(FORMAT=콜론 · MODEL/KEY=대시) · **`if __name__` 블록 위치**. **★ 마지막 것이 가장 조용한 결함이었다** — 그 블록 뒤에 덧붙인 클래스 **7셀이 직접 실행에서 아예 수집되지 않았고**, pytest 로는 돌기 때문에 **전수도 CI 도 아무 말을 안 했다**(오늘 만난 *"green 이 말하지 않은 것"* 의 네 번째 얼굴 — argon2 33건 · mongo 11셀 skip · mypy 88 vs 111 에 이어). 전수 `2322/1/2556`, 뮤테이션 V1~V5 전부 의도대로. | [검증](docs/verifications/2026-08-20/embedding_adapter_slice.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-20 | **임베딩이 외부 OpenAI 형식 API 에 붙는다 — 어댑터 슬라이스(1=A·2=A·3=A·4=A) 구현.** `docker-compose.external.yml` 이 *"임베딩은 아직 진짜 외부 API 에 못 붙는다"* 고 적어 두던 제약이 사라졌다. `OpenAIEmbeddingProvider` 가 `RemoteEmbeddingProvider` 옆에 서고(경로·요청 키·응답 구조·인증 **넷이 달라서** 분기가 아니라 두 번째 클래스), **Protocol 세 곳·새 컨테이너 0·배치 없음**(결정 2=A — 형식이 배열을 받아 *"어차피 받으니 지금"* 충동이 드는 자리이고 브리프가 미리 막아 뒀다). **★ 이 슬라이스의 본체는 어댑터가 아니라 조립 헬퍼다.** 조립 지점이 **여섯**이었고 각자 env 를 읽었는데, 실측해 보니 브리프가 적은 것보다 나빴다 — `EMBEDDING_DIMENSIONS` 기본값 **5벌** · 타임아웃 읽는 법 **2종** · **`trust_env` 는 `main.py` 만 넘기고 워커 셋은 안 넘겼다** · 주소 없음 처리 **3종**. `build_embedding_provider_from_env()` 하나로 모으고, 가드는 **등재 목록이 아니라 AST** 로 *"생성자를 직접 부르는 자리가 하나라도 있으면 실패"* 를 단정한다(목록 방식은 새 자리 등재를 잊으면 침묵한다 — 브리프가 선택지 B 를 기각한 이유). **그 여섯 중 하나가 한 달 넘게 `TypeError` 로 깨진 채 green 이었고**, 이관이 **시그니처만이 아니라 `sys.path` 부트스트랩까지** 닫아 이제 그냥 돈다(`--help` rc=0). **★ 구현이 정한 것 — 형식은 `EMBEDDING_API_FORMAT` 으로 명시하고 키 유무로 추론하지 않는다.** 추론이 더 짧지만 **키를 잠깐 지우면 형식이 조용히 바뀌고** 그 실패는 `404` 로 **원인에서 가장 먼 자리**에 떨어진다(뮤테이션 E3 이 그 설계를 물게 해 뒀다 — 더 짧고 편해서 다음 사람이 실제로 그렇게 고칠 만한 방향이다). 기본이 `native` 라 기존 배포 무영향 · `EMBEDDING_API_MODEL` 은 openai 에서 **필수**(모델을 요청마다 보내므로 기본값이 있을 수 없다) · base 는 **호스트 루트**이고 벤더 문서가 인쇄하는 접미 `/v1` 하나는 벗긴다(경로 **안**의 `v1` 은 안 건드린다). **README 에 "외부 임베딩 API에 붙이기" 절**을 재색인 절 뒤에 붙였고(오너 조건대로 독자는 내부 구조를 모르는 사람), **외부 API 로 바꾸는 것도 "모델이 바뀌는 것" 이라 재색인이 따라온다**는 연결과 **비용 주의**(재색인 = 건당 1회 호출)를 함께 적었다. 전수 `2319/1/2544`, 뮤테이션 6종 전부 재실패. | [브리프](docs/plans/embedding-adapter-slice-decisions.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-20 | **mypy 가드 독립 검증 — 조건부 합격, 조건 폐쇄까지 완료.** 검증자가 **보고 7종 뮤테이션을 셀 페어링까지 그대로 재현**했고 전수 `2296/1/2519`·정정 둘(88→111 · 12-skip)·미설치 거동(`FAILED (failures=3)`, 메시지가 설치법을 말함)·Dockerfile 3종의 이미지 순수성까지 실측으로 확인했다. **핵심 주장 M4(원래 결함을 복원하면 저장소 초록 셀이 재실패)는 참.** **★ 조건은 검증자 자체 공격 4종이 전부 뚫린 것이었다** — 무공백 `#type:ignore` · 파일 1행 `# mypy: ignore-errors` · ini 퍼모듈 `ignore_errors` · **`files` 범위 축소**. 넷 다 *요약줄 초록 + 표적 결함 생존* 을 만들며, **지적의 핵심은 벡터가 아니라 문언이었다**: 산출물 계약 *"억제 주석 0건이며 그것을 잠그는 셀이 따로 있다"* 가 실제 잠금(정준형 `# type: ignore` 하나)보다 넓었다. **폐쇄는 문언을 좁히는 대신 검사를 넓혀서 했다** — 문언을 줄이면 계약이 **약해진 채 합의**되고, 그것이 이 슬라이스가 막으려는 모양 그대로다. **벡터를 하나씩 막지 않고 허용된 것만 남겼다**(주석 정규식 `#\s*(type:\s*ignore\|mypy:)` + 설정 무결성: 섹션은 `[mypy]` 하나 · 키는 넷 · `files` 는 정확히 `{services, scripts}`) — 다섯째·여섯째 벡터(`exclude`·`follow_imports`)가 선제로 함께 닫힌다. **★ 그리고 `disable_error_code` 는 부분집합만 단정한다**: 더하면(약화) 실패, **빼면(강화) 통과** — 고정 일치로 잠갔으면 **셀이 브리프 §후속 고려의 확장 트리거 자체를 막았을 것**이다. **가드를 조이다가 개선 경로를 잠그는 것이 새 실패 모양**이라, 재검에 검증자 목록에 없던 **과잉교정 점검 2종(M12 강화 허용 · M13 평범한 주석 오탐 0)** 을 더했다. 셀 7 → 8. | [검증](docs/verifications/2026-08-20/mypy_guard_slice.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-20 | **저장소가 "돌지 않는 코드" 를 스스로 잡는다 — mypy 가드 도입(브리프 1=B · 1-b=가 · 2=D).** 발단은 오너 질문 *"에러인데 어떻게 그린이 떴던거지? 다른 것도 있나?"* 였다. `scripts/calibrate_character_identity_threshold.py:20` 이 키워드 전용 생성자를 위치 인자로 불러 **한 달 넘게 `TypeError` 로 깨진 채 green** 이었고, 원인은 검출 층이 **하나도 없었던 것**이다(테스트가 스크립트를 안 부름 · CI 없음 · 타입체커·린터 없음). **★ 브리프 초판 추천이 실측으로 뒤집힌 것이 이 슬라이스의 핵심이다** — 초판은 AST 가드 셀(A)을 추천하고 타입체커(B)를 *"초기 에러 수를 아무도 모른다"* 는 이유로 유예했는데, **재는 데 든 비용이 venv 하나에 8.6초**였다. 재 보니 좁힌 설정(`call-arg`+`misc`)에서 **5건**이라 억제 목록 없이 시작할 수 있었고, 무엇보다 **mypy 가 AST 가드로는 원리적으로 못 보는 잠재 결함 둘을 프로덕션 코드에서 찾아냈다**: `writing/accept.py:134` 가 `_replay()`(반환 `tuple\|None`)를 **None 검사 없이 언팩**(같은 함수 `:106` 은 제대로 방어한다 — 레이스에서 fail-closed 대신 `TypeError`) · `quota/lock_mongo.py:99` 의 `raise conflict` 가 `DuplicateKeyError\|None`. **오너 결정 — 처음 방향은 "A 에 B 까지 얹어 최대한 정확하게" 였으나, B 가 A 를 진부분집합으로 포함하고 A 쪽만 오탐 허용목록(= 브리프 자신이 지목한 M5 침묵 함정)을 갖는다는 대조를 보고 B 단독으로 정정했다** — *"가드를 하나 더 얹으면 더 안전하다" 가 항상 참은 아니다.* 배치는 `requirements-dev.txt` 신설 + **미설치 시 skip 이 아니라 실패**(CI 가 없어 가드는 pytest 안에 있어야 돌고, skip 은 가드를 조용히 없앤다). **★ 함정 둘을 셀로 잠갔다**: `misc` 를 끄면 표적 `call-arg` 가 함께 사라진다(코드 8종 bisect) · **`call-arg` 를 꺼도 저장소는 초록인 채 가드만 사라지고**, `# type: ignore` 한 줄이면 mypy 자체가 통과한다 — *"초록이다" 를 단정하는 셀과 "무엇을 보고 있다" 를 단정하는 셀은 다른 셀이다.* **★ 그리고 확정 때 쓴 수치가 구현 중에 정정됐다**: 오전 측정이 런타임 의존성 없는 venv 였던 탓에 전체는 88 이 아니라 **111건**이 맞다. **좁힌 집합은 두 환경에서 똑같이 5건** 이었고 — `call-arg` 는 우리 코드끼리의 호출이라 소스에서 풀린다 — **그래서 좁힘은 비용 타협이 아니라 재현 가능성의 조건이다**(넓은 설정은 의존성이 덜 깔린 머신에서 더 조용해지고 그 침묵이 "깨끗하다" 로 읽힌다). 뮤테이션 7종 전부 재실패. | [브리프](docs/plans/script-rot-guard-decisions.md) · [work log](docs/daily_logs/2026-08-20/work_log.md) |
| 2026-08-13 | **잔여 미검증 구간 독립 검증 합격(Blocking 0) — 그리고 "미검증 5" 라는 전제 자체가 틀렸다.** 검증자가 인계받은 목록을 git 역사와 대조하니 **`e5c0fac`·`b7c6453` 은 이미 `e124879`(어제 차트 검증)의 조상이자 그 기록의 범위 안**이었다(Subject 가 `b7c6453` 을 명시하고 본문이 `e5c0fac` 을 *"M6 재탐침 → cell 1 FAIL"* 로 다룬다). **진짜 미검증은 셋**(`444ab1b`·`f724fce` 차트 H1/H2 보강 · `2c6eb9e` 정문 버튼 정정)이었고 전부 합격. **★ 어제 마감 메모가 사실을 틀린 것은 이것으로 두 번째다**(첫째는 부채 ① 전제 *"관측된 피해 0건"*) — **두 번 다 다음 세션이 입력으로 삼아 옮겨 적었고 두 번 다 재측정이 잡았다.** 그래서 *"미검증 목록은 인계 문구에서 베끼지 말고 git 에서 다시 유도하라"* 를 함정으로 박았다(확인법은 `merge-base --is-ancestor` **와** 기록 Subject·본문 대조 **둘 다** — 조상이라는 것만으로는 검증이 아니다). **★ 가장 값진 발견 — `2c6eb9e` 가 손으로 닫은 구멍을 오늘 부채 ① 가드가 구조적으로 이어받았다**: 10.4 가 하네스 오독으로 넣었던 거짓 규칙(`.login-form button`, 특이도 `(0,1,1)` 이 `.auth-submit` `(0,1,0)` 을 조용히 덮어 패딩·모서리·굵기를 바꾸고 hover lift 를 없앴다)을 **그대로 재도입하면 `buttonAppearance.test.ts` cell 1·2 가 실패한다.** 즉 *"하네스 오독 → 거짓 규칙 투입"* 이라는 어제의 사고 유형까지 같은 셀이 덮는다. 검증자는 **자기 기록의 비차단 H1 도 정정**했다(`c08b0c2` — cursor/input-link 가정이 틀렸음을 독립 재확인. verdict·건수·분포 무변). 부수: **cwd 함정이 하루에 4번**(구현자 1·검증자 3) — 복원 명령은 저장소 루트 절대경로에서 시작하고 뮤테이션 *사이*에도 `git status --short` 를 찍는다. **미검증 구간 0.** | [검증](docs/verifications/2026-08-13/chart_h1_h2_hardening.md) · [검증](docs/verifications/2026-08-13/login_button_false_finding_correction.md) · [work log](docs/daily_logs/2026-08-13/work_log.md) |
| 2026-08-13 | **부채 ①② 폐쇄 독립 검증 합격(Blocking 0) — 그리고 비차단 지적 하나를 재측정해 정정했다.** 검증자가 보고를 사실로 받지 않고 다시 잰 것 셋: 캐스케이드를 grep 이 아니라 **7 선택자를 건드리는 9규칙 전수 추출 + ghost 특이도 계산**으로 · **자체 설계 뮤테이션 6종**(구현자 것과 독립)으로 셀별 단독 물림 · **CSS-pre 31.42 를 cp-swap 빌드로 디스크에서 직접** 재측정. 전부 일치. **★ 비차단 H1 은 그대로 옮기지 않았다** — 기록은 `cursor: not-allowed` 흩어짐을 *"input/link 계열의 다른 표면"* 이라 적었는데, `:disabled` 규칙을 전수로 뽑아 보면 **셋 다 `button:disabled`** 이고(비버튼은 `.login-form input` 하나뿐) 진짜 갈라진 것은 cursor 가 아니라 **불투명도 네 값(0.35·0.42·0.45·0.5)** 이다. **범위 밖이라는 결론은 옳지만 성격 규정이 틀렸고, 그대로 옮기면 다음 사람이 input·link 를 뒤지다 못 찾는다** — 어제 폐쇄한 "라벨 오귀속" 부채와 같은 자리(숫자는 맞고 귀속만 틀려도 재현이 불가능해진다). 정정한 실측으로 **추적 부채 ④**를 트리거와 함께 등재했고(Phase 10 끝 육안 · 새 비활성 버튼 표면), **가드는 세우지 않았다** — 네 값을 하나로 모으는 것은 네 표면의 농도를 실제로 바꾸는 **오너 결정**이고, 미해결 디자인 판단 위에 가드를 세우면 틀린 전제를 코드로 굳힌다. 부수 확인: 넓힌 hover lift 는 `prefers-reduced-motion` 전역 규칙(10.5 **이전부터** 존재)의 사정권 안이라 **a11y 계약을 새로 만들지 않았다**. | [검증 기록](docs/verifications/2026-08-13/debt_buttons_typescale_m5.md) · [work log](docs/daily_logs/2026-08-13/work_log.md) |
| 2026-08-13 | **부채 ①: 기본 동작 버튼의 겉모습을 한 자리로 모았다 — 그리고 부채 문서의 전제가 틀렸음이 드러났다.** 어제 기록은 *"관측된 피해 0건 · 시각을 바꾸지 않고 닫는 길이 있다(패딩만 자리마다 남긴다)"* 였다. 다섯 블록을 실제로 재 보니 **패딩만 다른 것이 아니었다** — `.editor-actions button` 만 `transition` 이 없어 배경이 툭 바뀌었고, hover 시 뜨는 효과(`translateY(-1px)`)는 다섯 중 **둘**에만 있었다. 즉 *"언젠가 갈라질 수 있다"* 가 아니라 **이미 갈라진 채였고 어떤 테스트도 겉모습을 안 보기 때문에 아무도 못 봤을 뿐**이다(`toHaveClass` 0 · `ByTestId` 0). 처방이 그 자리를 말하지 않아 **오너에게 물어 "다섯 곳 전부 뜨게" 로 확정**했다(D-10.5-b — 모션까지 한 자리에서 정해야 *"겉모습을 정하는 자리가 하나"* 라는 주장이 절반만 참이 되지 않는다). `main` 뒤에 **base·hover·disabled 세 규칙**을 세우고 다섯 블록에는 패딩과 그 자리 고유 속성만 남겼다 — **크기는 자리가 정하고 정체성은 공통 규칙이 정한다.** **시각 변화는 감이 아니라 대조로 쟀다**: 7 선택자 × base/hover/disabled = 21자리의 유효 선언을 이전/이후로 diff 해, `.auth-submit`·`.form-controls button` 은 **차이 0**, 나머지 5곳은 hover lift·transition 만 바뀌고 **색·테두리·커서·패딩·disabled 불투명도는 한 자리도 안 움직였음**을 실측했다. 신규 가드 `buttonAppearance.test.ts` 4셀(자리가 하나인가 · hover/disabled 가 같은 집합을 덮는가 · **패딩은 자리마다 남았는가**=over-strict)이고 목록을 손으로 들지 않고 스타일시트에서 유도한다. 뮤테이션 4종 전부 **단독 물림**. build **704 modules · 진입 421.78 kB · lazy 무변** · CSS 31.42 → **30.79 kB**(-0.63 = 사본 5벌이 실제로 사라진 값). | [work log](docs/daily_logs/2026-08-13/work_log.md) |
| 2026-08-13 | **부채 ②: `typeScale` 이관 목록이 자기 밖을 보게 됐다.** 종전에는 `MIGRATED` 에서 행을 지우면 그 선택자가 검사 대상에서 **조용히 빠졌다**(어제 뮤테이션 M5 실측 — 아무 셀도 실패하지 않았다). 넷째 셀이 목록의 **키 집합 ≡ 스타일시트에서 `var(--type-*)` 로 font-size 를 선언한 규칙 집합**을 단정해 그 자리를 닫았다 — 유도 기준을 *"이관의 정의"* 그 자체로 잡았기 때문에 새 화면을 옮기는 사람이 통과시킬 방법은 **목록에 더하는 것 하나뿐**이다. 착수 전 실측에서 두 집합이 이미 **43 ≡ 43** 이라 **목록 조정 0건**이고 가드만 늘었다. 양방향(행 삭제 · 옮기고 등재 누락) 둘 다 그 셀 하나가 문다. 10.4 `pageLayout.test.ts` 가 마크업에서 유도해 같은 계열의 한계를 피한 것과 같은 처방이며, **남기는 일반 규칙은 "목록형 가드는 목록을 손으로 들지 말고 유도한다"** 이다. 부채를 사실로 적던 브리프 문장도 함께 정정했다. | [work log](docs/daily_logs/2026-08-13/work_log.md) |
| 2026-08-12 | **★ 정정 — 10.4 가 보고한 "정문 버튼이 브라우저 기본값" 은 사실이 아니었다.** H1(버튼 규칙 흩어짐)을 가드로 닫으려고 근거를 다시 재다가 발견했다. 진짜 마크업은 `<button className="auth-submit">` 이고 **`.auth-submit` 은 처음부터 `--action-primary` 로 칠해져 있었다** — 실측하면 정정 전후 **둘 다 `rgb(0,110,190)`**, 회색이었던 적이 없다. **원인은 하네스의 마크업을 손으로 쓰면서 실제 클래스를 빠뜨린 것**이다: 렌더는 진짜인데 **입력이 가짜**라 *"실측 스크린샷"* 이라는 가장 믿음직한 형태로 **없는 결함**이 나왔다. **피해**: 그것을 고치겠다고 넣은 `.login-form button`(특이도 0,1,1)이 멀쩡하던 `.auth-submit`(0,1,0)을 덮어써 패딩·모서리(0→4.8px)·굵기(400→700)를 바꾸고 hover 의 `translateY(-1px)` 를 없앴다 — **요청도 근거도 없는 restyle**. 규칙 셋을 전부 되돌렸다. **★ 독립 검증도 함께 통과시켰다** — 부모 커밋에서 *규칙의 부재*(0줄)를 확인했는데, **규칙이 없다는 것과 그 요소가 안 그려진다는 것은 다른 명제다.** 하네스 머리말에 이 함정을 사례와 함께 박았다. **H1 부채는 남기되 등급을 내렸다** — 겉모습이 다섯 곳에 흩어진 것은 사실이나 **관측된 피해 0건**이라, 검증자가 제안한 통합 가드는 **만들지 않았다**(틀린 전제 위에 가드를 세우게 되고 시각이 바뀌는 통합을 강제한다). frontend 318/26 무변(그 규칙을 무는 셀이 애초에 없었다) · CSS 31.76 → **31.42 kB** · 진입·lazy 무변. | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-12 | **Slice 10.4 페이지 배치 통일 독립 검증 합격 — 작업자가 커밋한 headless-chromium 하네스를 내가 직접 돌려 픽셀 수치를 PRE·POST 양쪽 열에서 재현했다.** "실측으로 시작"의 근거 수치를 보고가 아니라 하네스(`docs/plans/10_layout_probe.sh`, chromium 1440×900) 로 재도출: POST workspace **244·39·38%·1225** / admin **138·39·25%·1225** / editor **247·39·38%·1225** · PRE(부모 CSS 로 교체 측정) workspace **380·83·55%·1033** / admin **274·83·42%·1193** / editor **240·31·37%·1225** — 한 픽셀 안 틀림 없이 일치. 세 규칙 코드로 확인(① 폭 `min(100%,68rem)` 한 곳 == `main` max-width 68rem ② 블록 자기제한 42/34rem ③ 제목 한 계단, 로그인 `--type-display`=`round(1.125**9,3)`=2.887 램프 안 예외). 렌더로 찾은 둘 재현: `.login-form button` 이 부모엔 **0줄**(회색 기본값) · `.overview-page` 62rem override 잔존(가드가 잡은 누락). **★ 뮤테이션 LM1·LM2·LM3 단독 물림(스크립트 커밋)** — 가드가 값이 아니라 **"평을 정하는 자리가 하나"** 임을 증명. **★ 가드 뿌리 분류 건전**(작업자가 두 번 틀린 자리): `access-log-page`는 `<ul>` 안쪽 블록으로 올바르게 제외 · 모든 라우트가 workspace-page/admin-page/auth-shell 안(가드 밖 폭 0건). 회귀 frontend **318/26** · 백엔드 prod **0줄** · 번들 **CSS 31.76 · 진입 421.78 무변** 직접 빌드. 차단 결함 0. 비차단: H1 기본 버튼 규칙 흩어짐(추적 부채) · T1 목록 행 간격(육안 확인). | [verification](docs/verifications/2026-08-12/slice_10_4_layout.md) |
| 2026-08-12 | **Phase 10 Slice 10.4 — 배치를 편집기 기준으로 전 화면 통일.** **오너가 순서를 바꿨다** — *"폰트사이즈나 뭐 이런 거는 나중에 하고 화면 전체에서 배치나 디자인 스타일이 변경되는 것들을 작업"* → 토큰 이관을 미루고 배치부터, 범위는 선택지 넷 중 **"편집기 기준으로 전 화면 통일"**. **실측이 근거다**(headless chromium 1440×900, 하네스를 커밋했다): `workspace-page` 는 **첫 화면의 55% 를 콘텐츠 전 chrome 으로** 쓰고 있었다(콘텐츠까지 380px) → **244px · 38%**. `admin-page` 274 → 138px. 그리고 **폭이 52/62/68rem 세 종류인데 규칙이 적힌 곳이 없어** 오른쪽 끝이 1033·1193·1225 로 제각각이고 **헤더 밑줄만 한쪽으로 삐져나와** 있었다(왼쪽 32 · 오른쪽 224px) → 셋 다 **1225**. 큰 디스플레이 제목 + 넉넉한 여백은 *랜딩 페이지* 문법이지 매일 쓰는 **작업 도구**의 문법이 아니며, **10.1 의 크림+테라코타와 같은 계열의 AI 기본값**이다. 확정한 규칙 셋: ① 페이지 폭은 `main` 의 상한(68rem) **한 곳에서만** ② 좁은 측정폭은 **블록이 스스로** 제한한다(컨테이너를 좁혀서 얻지 않는다 — 정렬과 가독성이 서로를 깎지 않게) ③ 화면 제목은 `--type-title` 한 계단, 예외는 로그인뿐이고 그것도 램프 안(`--type-display` — **작업 화면이 아니라 정문**이라서). 인증 화면은 `main.auth-shell`(`max-width: none`)이라 **소속이 다르므로** 규칙 밖. **★ 새 가드가 이 슬라이스 자신의 누락을 잡았다** — 통일해 놓고 `.overview-page` 폭 override 를 남겨 개요 화면만 62rem 이었다. 그래서 `pageLayout.test.ts` 가 재는 것은 값이 아니라 **"폭을 정하는 자리가 하나인가"** 이다(값은 디자인 판단이라 바뀔 수 있다). **★ 그리고 렌더 스크린샷이 가드가 원리적으로 못 보는 것을 찾았다** — `.login-form button` 규칙이 **아예 없어서** 제품 정문의 로그인 버튼이 브라우저 기본값(회색 상자)으로 그려지고 있었다. 색 리터럴도 토큰 오타도 아니라 CSS·TS 가드가 전부 조용했고 렌더 테스트는 스타일을 안 본다. 근본 원인은 **기본 동작 버튼 규칙이 네 곳에 흩어져 있어 새 표면은 어디에도 안 들어가는 것이 기본값**이라는 것이며, 빠진 자리만 채우고 통합은 추적 부채로 남겼다. 뮤테이션 3종 전부 단독 물림. frontend 315/25 → **318/26** · CSS 31.70 → **31.76 kB** · 진입 421.78 kB · 관측 lazy 387.43 kB 무변 · 백엔드 0줄. | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-12 | **10.3 차트 보강 — 검산 결과가 가드가 아니었다는 것을 닫았다.** 독립 검증 H1: `chartColors.test.ts` 가 계열 셋이 *서로 다른지*만 봐서 **검증기가 FAIL 하는 조합도 전부 green 이었다**(실측 — `--chart-parse-error` 를 `#7b5100` 로 바꾸면 채도 바닥 미달로 검증기 FAIL 인데 가드 7셀 모두 통과, `styles.css` 주석만 조용히 낡았다). **ΔE 를 셀에 다시 구현하지는 않았다** — 검증기와 갈리는 두 번째 정본이 생기기 때문이며 그 판단은 유지했다. 대신 **"지금 값이 그때 검산한 바로 그 값인가"** 를 잠갔다(10.1 팔레트·10.3 타이포와 같은 출처 연결 처방): **`:root` 계열색 ↔ 주석의 검산 명령 팔레트 인자 ↔ `.chart-frame` 이 실제로 그리는 배경** 셋을 한 셀이 묶는다. **★ 세 번째 연결이 새 힘이다** — 브리프의 *"표면을 바꾸면 검산을 다시 돌린다"* 를 **처음으로 집행한다**(종전에는 그 문장을 아무도 강제하지 않았다). 함께 **H2 — `<Tooltip>`/`<Legend>` 를 계통에 넣었다**(recharts 기본 흰 배경·`#ccc` 테두리 → 카드면 토큰. 떠 있는 표면이라 융기면이 아니다). **★ 여기서 배운 것: *"색을 안 쓴 것"* 과 *"라이브러리 색을 쓴 것"* 은 다른데 리터럴을 세는 가드는 그 둘을 구별하지 못한다** — 어제 넓힌 TS 리터럴 가드가 이 자리에 조용했던 이유이며, 소스를 읽어 **모든** overlay 가 스타일 prop 을 갖는지 전수로 세는 셀을 따로 뒀다. 보강 뮤테이션 **5종 전부 단독 물림**(검증기 FAIL 조합 · 주석만 수정 · 표면 교체 · 차트 둘 중 한쪽만 스타일 누락 · Legend prop 제거). frontend 313/25 → **315/25** · 관측 lazy 387.03 → **387.43 kB** · 진입 421.78 kB · CSS 31.70 kB 무변. | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-12 | **Slice 10.3 관측 차트 독립 검증 합격 — "재보고 뒤 예상과 달라진 결론"의 CVD/ΔE 수치를 같은 검증기로 전부 재계산했다.** 보고를 사실로 받지 않고 dataviz 스킬 `validate_palette.js`(`--surface "#f6f9fc"`=`--surface-raised`) 를 직접 돌려 네 케이스를 잼: 채택안 `#006ebe,#8c1f4a,#9a6a24` PASS(최악 CVD **15.4**·정상 **19.7**) · 종전 `#1a6d99`안 PASS(CVD **12.6**) → 성공만 blue-600 으로 옮긴 개선 **12.6→15.4 재현** · 상태색 통일안 F1(blue·danger-600·warn-700) 정상 **13.9**·protan **4.5** FAIL · F2(blue·danger-700·warn-700) 정상 **12.2**·deutan **3.3** FAIL. **★ 네 수치 전부 보고와 정확 일치** — "계열 팔레트는 UI 상태색과 다른 물건, 통일하면 색각 이상에 한 덩어리" 핵심 논리가 실측 위에 선다. chrome 결함(막대 stroke `#f4f0e7` 크림 테두리) 종전 커밋에서 실존 확인·현재 0 리터럴·간격 `--surface-raised`. **★ 뮤테이션 C1–C5 전부 재현(스크립트 커밋), C3·C4·C5 단독 물림을 두 가드 동시실행으로 증명, C5=이 슬라이스가 고친 결함의 재발.** 회귀 frontend **313/25** · 백엔드 prod **0줄** · provenance **5/90** · 번들 **CSS 31.70 · lazy 387.03 · 진입 421.78 무변** 직접 빌드로 재생산. 이전 검증 폐쇄도 확인 — `e5c0fac`(내 H1) M6 재탐침 **cell1 FAIL** 로 맹점 닫힘 · `11f8bb6`(내 측정 니트 0.78rem 17→11·19→18 정정 + 인덱스 가드) green. **★ 이번엔 내 검증 커밋이 README 헤더 수치(건수 4곳·판정분포 표/문장)를 안 올려 `VerificationCountClaimsTest` 가 깨지는 일이 없도록, 7곳을 동기화하고 가드로 확인**했다(지난 4f67779 가 같은 자리를 실수해 작업자가 11f8bb6 으로 닫았다). 차단 결함 0. 비차단 2: H1 ΔE/CVD 자체는 자동 가드 아님(palette 바꾸면 재실행이 리뷰 필수) · H2 Tooltip/Legend recharts 기본 스타일. | [verification](docs/verifications/2026-08-12/slice_10_3_observation_chart.md) |
| 2026-08-12 | **Slice 10.3 관측 차트 — 색을 `:root` 로 되돌리고 chrome 을 앱 토큰에 맞췄다.** 브리프 §Follow-up 이 *"10.1 이 관측 차트를 깨뜨린다"* 로 남겨 둔 자리이며, **dataviz 검증기로 먼저 재보고 나서 손댔더니 결론이 예상과 달랐다.** ① **계열색 셋은 새 배경에서도 6검사 전부 통과** — 배경이 바뀌어 검증이 무효라는 지적은 맞았지만 다시 돌린 결과는 통과였고, **접근성 때문에 바꿀 이유는 없었다.** ② **★ 진짜 발견은 앱 상태색으로 갈아끼우면 떨어진다는 것** — `--warn-700`↔`--danger-600` 이 정상 시야 ΔE **13.9**(바닥 15) · protan **4.5** 로 뭉치고 채도 바닥도 미달한다(후보 3종 전부 FAIL). 2026-07-26 검증의 *"앰버를 어둡게 하면 벽돌색으로 끌려간다"* 가 다른 경로로 재현됐다 — **계열 팔레트는 UI 상태색과 다른 물건이고, "일관성"이라며 통일하면 색각 이상 사용자에게 두 계열이 한 덩어리로 보인다.** ③ **성공 슬롯만 `--blue-600` 으로 옮겼다 — 취향이 아니라 측정**: 최악 CVD 분리가 ΔE 12.6 → **15.4** 로 좋아진다. ④ **실제로 눈에 보이던 결함은 계열색이 아니라 chrome 이었다** — 막대 사이 간격이 *옛 페이지 배경*(`#f4f0e7`)이라 새 배경 위에서 **막대마다 크림색 테두리**로 보였다. 격자·축·간격을 앱 일반 토큰으로 옮겼고, **간격은 `--surface-page` 가 아니라 `--surface-raised`** 다(차트가 `.chart-frame` 안에 앉는다). ⑤ recharts 는 색을 SVG **속성**으로 받아 `var(--)` 가 안 닿으므로 `chartColors.ts` 가 `getComputedStyle` 로 읽는다 — **fallback 없음**(8.4 `.writing-confirm` 구조가 되기 때문). **★ jsdom 은 스타일시트를 로드하지 않아 읽기가 전부 빈 문자열이어도 recharts 는 `fill=""` 로 조용히 그리고 그 화면 15셀은 전부 green** — 그래서 토큰 실재를 보는 `chartColors.test.ts` 가 따로 있다. 함께 **`designTokens.test.ts` 의 색 리터럴 금지를 TS 소스까지 확장**했다(그 셀이 `styles.css` 만 읽어서 이 결함에 장님이었다. 현재 0건). 뮤테이션 5종 전부 작동하며 셋은 단독 물림. frontend 309/24 → **313/25** · CSS 31.60 → **31.70 kB** · 관측 lazy 386.70 → **387.03 kB**(lazy 경계 안, 진입 421.78 kB 무변). | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-12 | **10.3 보강 — 가드가 자기 정규식 밖의 토큰을 못 보던 맹점을 닫았다.** 독립 검증 H1(비차단, M6 탐침): 램프 검사 셀이 `1.125^n` **주석이 붙은 줄만** 잡으므로 **주석 없이 선언한 `--type-*` 은 검사를 조용히 통과**했다 — *"값은 계산 결과다"* 라는 그 파일의 주장이 새 토큰에는 성립하지 않는데 아무도 모르는 상태였다. **선언된 토큰 전부가 출처를 달고 있는지**를 먼저 단정하도록 고쳤다(종전 `steps.length > 0` 보다 강하다 — 0건뿐 아니라 일부 누락도 잡는다). M6 재실행 보강 전 **3 passed** → 보강 후 **실패** ✓, 정상 확장 탐침 M7′(주석 단 새 계단 + 이관 목록 밖 새 규칙)은 **3 passed** 로 over-strict 아님을 확인. **★ M5(이관 목록에서 행을 지우면 안 문다)는 여전히 열려 있다** — 같은 계열의 맹점 둘 중 하나만 닫혔다는 뜻이고, 목록을 줄이는 diff 는 리뷰에서 봐야 한다. 함께 **측정 니트 2건 정정**(정본 5곳): 패턴 스윕 `0.78rem` **17곳 → 11곳** — 17 은 **선언 수가 아니라 콤마로 갈린 선택자 이름 수**였다(그룹 선택자 하나가 이름 넷일 수 있다. 17+8 이 총합 45 와 안 맞는 내부 모순이 남아 있었고 검증자가 그것으로 잡았다) · 출발점 **19종 → 18종** — `0.84rem` 이 역할에 따라 `small`·`meta` **두 계단으로 갈려** 흡수표를 세로로 세면 19행이지만 서로 다른 값은 18개다. **셀 수 무변**(기존 셀 보강이라 309/24 그대로). | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-12 | **Slice 10.3 독립 검증 합격 — 타이포 축은 계산대로 정확하고 가드는 양방향으로 문다.** 보고를 사실로 받지 않고 코드·수치·뮤테이션으로 재도출했다. 스케일 8계단 전부 `round(1.125**n,3)` 수기 계산과 정확 일치하고 정본 표↔`styles.css:140–147` 이 무결점. **★ 이관 충실성: `var(--type-*)` 사용처 42곳이 `MIGRATED` 목록 42행과 단계별로 정확히 같다**(숨은 사용처 0 — 목록이 파일 전체를 빠짐없이 커버). 위계 편집 3곳 코드에 존재, "3.7배"=4.2/1.12=3.75 성립. **★ 뮤테이션 M1–M5 전부 독립 재현**(스크립트 커밋): M1(값 고침)·M2(지수만 고침)→같은 cell 1 양방향 · M3→cell 3 단독 · M4(죽은 계단)→cell 2 · **M5(이관 행 삭제)→안 물음 = 정본 L420–422 가 이미 못 박은 공식 한계**. frontend **309/24** green · 백엔드 prod **0줄** · CSS-읽기 가드 provenance **5/89** green. **★ "의심하고 또 의심"에 찾아낸 둘 — 둘 다 비차단**: ① 측정 정밀도 — 패턴스윕 "0.78rem 17곳"은 실측 **11곳**(17 이면 총합이 45 가 아니라 51) · "19종"은 흡수열 distinct **18종**. ② **H1 — cell 1 정규식이 `1.125^n` 주석 있는 줄만 잡아서 주석 없는 토큰은 램프 검사를 안 한다**(M6 탐침으로 실존 확인, 현재 8 토큰은 전부 주석이라 즉시 위험 0, 보강 셀 후보 기록). 차단 결함 0. | [verification](docs/verifications/2026-08-12/slice_10_3_typography_scale.md) |
| 2026-08-12 | **Phase 10 Slice 10.3 — 타이포 축을 열고 `DraftEditor` 를 스케일에 올렸다.** 10.1 이 미룬 축이며 브리프의 트리거대로 열었다 — *"화면 하나를 실제로 다시 그릴 때 그 화면의 font-size 부터. 전역 스케일을 먼저 정하지 않는다."* **출발점 실측: 그 화면이 그리는 42 선언이 font-size 18종을 쓰고 있었고 그중 12종이 0.72~0.92rem 한 뼘 안에 몰려 있었다** — 0.84 와 0.85 는 0.16px 차이라 **위계가 아니라 위계인 척하는 잡음**이었다. **값은 고른 것이 아니라 계산했다**(10.1 팔레트와 같은 규율): 1.125 major second 등비 램프를 1rem 에 앵커한 `round(1.125**n, 3)`, 8계단, 이름은 크기가 아니라 자리. **1.125 를 고른 결정적 이유는 `1.125^1 = 1.125rem` 이 현행 원고 본문 1.12rem 과 사실상 같다는 것** — 가장 많이 읽는 글자가 안 움직인다. 선언 42곳을 스냅(18종 → 8종). **함께 위계도 바꿨다**: 종전 편집기는 위쪽 chrome(여백 7rem + 제목 4.2rem)이 **원고보다 컸다**(제목이 본문의 3.7배) — 제목을 `--type-title`(2.027rem)로 내리고 상단 여백을 `clamp(2rem, 4vw, 3rem)` 로 줄여 **첫 화면에 원고가 들어오게** 했다. **오너 결정이 이 범위를 정했다**(2026-08-12): 색감 수준의 육안 확인은 이미 끝났고, **세세한 UI 는 도그푸드 하며 실제로 써 보면서 잡는 것이 맞다 — 지금은 큰 틀만.** 그래서 미세 조정은 하지 않았다(도그푸드 전에 고치면 두 번 고치게 된다). 신규 가드 `typeScale.test.ts` 3셀 양방향 — 램프 출처(값 ↔ 지수 주석) · 죽은 계단 · 이관 목록의 리터럴 복귀. 뮤테이션 4종 작동. **★ 5번째가 안 물었고 그것이 이 가드의 한계다** — 이관 목록에서 행을 지우면 그 선택자는 가드 밖으로 나가고 아무 셀도 실패하지 않는다(Phase 9 의 *"전수 가드는 배선의 존재만 보고 분기는 못 본다"* 와 같은 계열). **남은 리터럴 45곳 · 22종은 다른 12개 표면에 있고 화면 슬라이스가 자기 차례에 옮긴다** — D1=ⓒ 점진의 알려진 중간 상태다. frontend 306/23 → **309/24**(전부 신규 가드. 프로덕션 CSS 는 회귀를 한 셀도 안 물었다) · CSS 30.94 → **31.60 kB** · 703 modules · 진입 421.78 kB 무변. | [work log](docs/daily_logs/2026-08-12/work_log.md) |
| 2026-08-11 | **10.2 보강 — 시간대에 따라 세기가 달라지던 가드를 닫았다.** 독립 검증(합격)이 괄호로 *"M22 의 4 라는 수는 실행 머신 시간대에 민감하다"* 고만 적은 것을 파 보니 **`TZ=UTC` 에서는 그 뮤테이션이 0 failed** 였다 — UTC 머신에서는 `getFullYear` 와 `getUTCFullYear` 가 같은 값을 내므로 잡을 것이 없다. **즉 로컬/UTC 불변식이 UTC 머신에서는 전혀 안 잠겼고**, UTC 컨테이너·CI 에서 로컬→UTC 리팩터링이 전부 green 으로 통과한 뒤 버그는 시간대가 다른 사용자에게서만 터졌을 것이다(이 저장소는 머신 셋을 옮겨 다니고 컨테이너 기본이 UTC 다). 로컬 달력 셀을 **시간대 고정 + 자정 양쪽 끝**으로 바꿔 닫았다 — 실측 Asia/Seoul 3 failed · UTC **0 → 1** · America/New_York 3. **★ 일반화: 뮤테이션이 무는 수가 환경에 따라 달라지면 그 환경 중 하나에서는 0일 수 있다.** 시간대·로케일처럼 머신이 주는 값에 기대는 가드는 테스트가 그 값을 고정해야 하고, 안 그러면 가장 약한 머신이 CI 일 수 있다. 함께: 날짜 머리글 시각 강조(본문 잉크 + 하단 실선 + 점) — 오너가 *"머릿글이 안 접히는 것 같다"* 고 한 자리이며 **접힘은 정상이었고 강조가 약했다.** 10.3~ 로 미루지 않은 이유는 오너가 실제로 기능을 못 알아본 자리여서다. frontend 305 → **306/23** · CSS 30.94 kB · 진입 무변. | [verification](docs/verifications/2026-08-11/slice_10_2_activity_date_groups.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Phase 10 Slice 10.2 — 활동을 날짜로 묶는다(D3=ⓓ, backend 0줄).** 두 화면이 최대 100건을 **한 덩어리로 주르륵** 쌓던 것을 날짜 머리글로 나눴다(오너 육안 확인 지적). 행에서는 날짜를 빼 **시각만** 남긴다 — 같은 정보를 두 번 쓰지 않는 것이 그룹핑의 이유다. **★ 커서 페이징이 아니다**: 상한을 없애려면 operation 78 이 움직여야 하는데 **100건이 부족하다는 증거가 없다** — 커서는 유예이고 트리거는 *실사용자가 100건을 채워 "더 이전"을 필요로 할 때*(9.1 F1 이 정본). 상한 문구는 그대로 두고 셀이 잠근다(그룹핑이 상한을 올린 것처럼 보이면 안 된다). 공유 모듈이 지키는 것 셋 — **서버 순서를 다시 정렬하지 않고**(정렬 정본은 `log_mongo.py`, 여기서 또 정렬하면 순서를 정하는 곳이 둘이 된다) · **그룹 경계는 브라우저 로컬 날짜**(행이 로컬 시각을 찍으므로 머리글만 고정 시간대면 "오늘" 아래 어제 시각이 앉는다 — `quota/policy.py` 의 KST 와 **다른 판단이고 이유도 다르다**: 그쪽은 시행 창=서버 계약, 여기는 표시=보는 사람의 달력) · **날짜를 못 읽는 행을 버리지 않는다**(행이 조용히 사라지는 것이 잘못된 머리글보다 나쁘다). **★ 테스트가 시계에 의존하면 안 된다는 것을 두 번 헛디디며 배웠다** — 8/10 을 "8월 10일" 로 기대한 셀이 그날 "어제" 라 깨졌고, `vi.useFakeTimers()` 로 고치려니 `findBy*` 의 `waitFor` 가 멈춰 세 셀이 타임아웃됐다. **연도가 다른 날짜**로 화면 셀을 결정적으로 만들고 "오늘"·"어제" 는 `now` 를 주입받는 모듈 셀로 내렸다 — **재는 것을 그 자리에서 잴 수 있는 층으로 내리면 시계 의존이 사라진다.** 뮤테이션 4종 작동. frontend 294/22 → **305/23** · build 702 → 703 modules · 진입 421.78 kB. | [decision](docs/plans/10-frontend-design-system-decisions.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Slice 10.1 독립 검증 합격 + 잔여 니트 폐쇄 — 문서 드리프트를 가드로 묶었다(프로덕션 코드 무변).** 검증은 **조건부 합격**으로 발행됐고(C1 브리프 §D2 표가 구현과 갈림 · H1 짝 수를 세 곳이 다르게 말함) **같은 사이클에 폐쇄돼 합격으로 승격**됐다. 폐쇄하며 검증자 권고를 받아 **prose 짝 수를 `len(PAIRS)` 에 묶는 셀**을 더했고, 이어 남은 니트(semantic 표 누락)를 닫으면서 **§4 패턴 스윕으로 검증자가 짚은 2행이 아니라 9행 누락 + 매핑 오류 1건**(`--border-hairline` slate-300 → 실제 slate-200)을 찾아 표를 `:root` 에서 재생성했다. **★ 그리고 그 드리프트 자체를 가드로 묶었다** — primitive hex·짝 수 prose 에 이은 **같은 병의 세 번째**였고, 부주의가 아니라 *사람이 두 곳을 동시에 기억해야 하는 구조*가 원인이라 판단했다. 이제 브리프 표와 `:root` 는 어느 한쪽만 고쳐도 실패한다. **★ 그리고 그 가드가 즉시 값을 했다** — 뮤테이션 원복에 `git checkout` 을 써서 커밋 안 한 편집을 날린 §6 사고(저장소 열 번째)를 새 prose 가드가 그 자리에서 잡았다. 뮤테이션 M16~M20 작동. provenance 3 → **5 cells / 89 subtests**. **오너 결정 대기 하나** — 타이포 스케일(font-size 29종)을 10.1 에서 빼고 10.3~ 로 미룬 스코프 축소의 bless. | [verification](docs/verifications/2026-08-11/slice_10_1_palette.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Phase 10 Slice 10.1 — 잉크블루 토큰 체계 + 이 저장소 최초의 CSS 가드(backend 0줄).** 색 **178곳**의 토큰 참조와 하드코딩 **45곳**(hex 30·rgba 15)을 전부 semantic 토큰 뒤로 보냈다 — 화면은 semantic 만 보고 primitive 는 `:root` 안에서만 등장한다. **★ 구현이 브리프 가정 셋을 반증했다**: ① 이 앱에는 침강면보다 **융기면**이 많아 `--surface-raised` 를 신설 ② 브리프가 정한 테두리·placeholder 값은 **카드에서만** 맞고 페이지에서 미달(2.95/3.97) ③ 그 수정도 틀렸다 — **침강면이 페이지보다 어둡다**. **교훈: 최악 배경은 "주로 쓰는 면"이 아니라 정의된 표면 전체의 최소다.** 최종 **30짝 전수 실패 0**(본문 4면 AAA). placeholder 는 이분 탐색이 낸 4.50 이 **hex 양자화로 4.49** 가 돼 여유를 더했다. **★ CSS 는 지금까지 무엇을 바꿔도 회귀가 침묵했다**(`toHaveClass` 0·`ByTestId` 0) — 스타일시트를 파싱하는 가드를 만들었고 **첫 실행에서 8.4 가 남긴 실제 결함을 잡았다**: `.writing-confirm`(quota 확인 대화)이 정의된 적 없는 `var(--border)`·`var(--surface-muted, transparent)` 로 **2026-08-04 부터 테두리도 배경도 없이** 렌더되고 있었다(HANDOFF 가 "렌더 미검증" 이라 적어 둔 그 화면). 두 번째 가드는 **CSS hex 와 생성 스크립트가 갈리는 것을 막는다** — 주석에 "손으로 고치지 말 것" 이라 적어 두는 것만으로는 아무도 막지 못한다. **★ 타이포 축은 의도적으로 범위 밖에 뒀다** — font-size 29종을 스케일에 스냅하는 것은 토큰 이관이 아니라 시각 재설계이고 팔레트 교체와 겹치면 diff 를 검토할 수 없다(10.3~ 첫 항목). 뮤테이션 7종 전부 작동. backend 2268→**2271/1/2430** · frontend 291/21→**294/22** · build 702 modules·진입 420.81 kB 무변. | [decision](docs/plans/10-frontend-design-system-decisions.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Slice 10.0 독립 검증 조건 폐쇄 — 탭 타이틀 통일 + 렌더 밖을 보는 가드.** 독립 검증이 **조건부 합격**을 내며 잡은 것: 헤더·로그인 화면은 "에-라잇" 인데 **브라우저 탭 `<title>` 만 옛 이름**이었고, 작업자가 쓴 `queryByText` 가드는 그것을 **원리적으로 볼 수 없었다**(vitest 는 `index.html` 을 로드하지 않고 빈 jsdom 문서에 마운트한다 — 289셀 전부 green 인 채로 갭이 살아 있었다). 검증자 권고는 `document.title` 가드였으나 **그것도 런타임 값이라 같은 맹점이 남는다** — 대신 **파일을 읽는** 가드로 만들었다(`index.html` 직접 확인 + 소스 전수 스윕으로 `og:title`·manifest 등 아직 없는 자리까지 선제 커버). 양방향 실측 M7 → 2 failed · M8 → 1 failed. 검증의 H3(M1 실패 셀 라벨 오기)도 재현해 정정했다. **교훈: 렌더 테스트의 사정거리 밖에 있는 사용자 노출면(`<head>`·manifest·정적 파일)은 DOM 이 아니라 파일을 읽어 잠근다.** frontend 289/20 → **291/21** · build 702 modules · 진입 420.81 kB 무변. | [verification](docs/verifications/2026-08-11/slice_10_0_account_menu.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Phase 10 Slice 10.0 — 계정 메뉴 + 제품명 "에-라잇"(D4 ⓐ+ⓒ · D5, backend 0줄).** 9.2 가 만든 `/me` 는 **route 만 있고 진입 링크가 저장소 전체에 0개**여서 주소를 직접 쳐야 도달했다 — 만든 화면이 도달 불가였다. username 을 누를 수 있는 자리로 만들고(ⓐ) 그 클릭이 **내 작업·관리·로그아웃**을 연다(ⓒ). **★ 브리프가 적은 `role="menu"` 를 쓰지 않고 disclosure 로 갔다** — ARIA menu 는 애플리케이션 명령 메뉴용이라 화살표 탐색·타입어헤드를 약속하는데 여기 담긴 것은 내비게이션 링크 둘 + 액션 하나다. 부수 효과로 `<a>` 의 link 역할이 보존돼 기존 셀이 살았다(`role="menuitem"` 이었으면 덮였다). **로그아웃은 옮긴 것이지 다시 쓴 것이 아니며, 누를 때 패널을 닫지 않는다** — 닫으면 "나가는 중…"·`disabled` 가 사라져 진행 중이라는 유일한 신호를 잃는다. **★ 뮤테이션 6종 중 M2(Esc 포커스 복귀)가 처음에 안 물었고 그것이 신호였다** — 트리거를 클릭한 직후엔 포커스가 이미 트리거에 있어 복귀 코드를 지워도 통과했다. 포커스를 패널 안으로 옮긴 뒤 누르도록 셀을 강화하니 물었다. frontend 285 → **289/20**(+4) · 진입 420.08 → 420.81 kB · 702 modules 무변. | [decision](docs/plans/10-frontend-design-system-decisions.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-11 | **Phase 10 착수 결정 — 프론트 디자인 시스템(D1~D6 확정, 코드 0줄)** + **`/me/activity` 500 수정.** ① **육안 확인이 회귀가 못 보는 결함을 잡았다** — 재빌드 후 `GET /me/activity` 가 500 이었다(`register_auth` 만 `create_app` 의 **해석 전** `core_sot`(=None)을 받고 있었고, 나머지 13개 호출은 전부 해석된 값을 넘긴다). **회귀 2266셀이 green 이었던 것은 우연이 아니라 구조**다 — 모든 테스트가 `create_app(service=…)` 로 주입해 두 값이 같은 객체가 되고, 배포만 아무것도 주입하지 않는다. **주입 없이 조립한 앱을 구동하는 가드 2셀**을 함께 넣었다. ② **Phase 10 브리프 확정.** **★ 오너가 구현자 권고를 기각했고 그것이 옳았다** — 구현자는 현행 팔레트(크림 `#f4f0e7` + 테라코타 `#a4452f`)를 *"글쓰기 도구에 어울리는 방향이 이미 잡혀 있다"* 며 유지를 권고했으나, 오너의 *"현행이 이상해서"* 가 정확했다: 그 좌표는 **생성형 AI 디자인이 수렴하는 대표 기본값**이었고 구현자는 기본값을 선택된 방향으로 오독했다. **교훈 — "이미 방향이 잡혀 있다"는 그 방향이 *선택된 것*인지 *기본값*인지 확인하기 전에는 근거가 못 된다.** 오너 선호(*"블루계열 파스텔… 대비성있게"*)를 받아 **잉크블루 팔레트를 고르지 않고 계산했다** — 3계층 토큰(값이 아니라 의도로 명명) · OKLCH 지각 균등 램프(hue 250·L 등간격) · **WCAG 2.2 AA 18짝 전수 검산 실패 0**(본문 3짝은 AAA ≥7:1). 파스텔과 대비의 충돌은 **면은 파스텔, 잉크·강조는 같은 hue 의 깊은 쪽**으로 역할을 갈라 풀었다. 검산 스크립트를 함께 커밋해 재현 가능하다. ③ 실측이 HANDOFF 경고를 정정했다 — 회귀가 잠그는 것은 **시각이 아니라 의미**다(`toHaveClass` **0**·`ByTestId` **0** → 색·간격·배치는 비용 0 / 문구 311곳·접근성 semantics 478곳이 진짜 비용). 회귀 backend +2 cells. ④ **compose 앱 이미지를 하나로 묶었다** — 오너가 *"별도 태그를 한 특별한 이유가 있나? 서로 필요한거잖아"* 라고 물어 다시 봤고 **이유가 없었다**: `admin`·`worker` 주석이 이미 *"Shares the application image"* 라고 적었는데 **설정이 그것을 구현하지 않아** 네 서비스가 각자 태그를 받고 있었다. **그것이 기능을 죽이고 있었다** — 실행 중 `worker` 이미지가 07-27 로 15일 뒤처져 **`PROJECT_PURGED` drain 이 아예 없었다**(D8-6 파기의 워커 절반. 컨테이너 안 `_drain_purge` **0**, 현행 코드는 2. outbox 0건이라 잠복 상태였다). `generation_worker` 는 8.3 워커 차감이 빠져 있었다. 넷에 `image: ai_writte_system-app` 을 명시 — 빌드가 **1회만** 돌고 네 컨테이너 이미지 ID 가 같아졌다. **교훈: 주석이 말하는 의도를 설정이 구현하는지 확인한다.** | [decision](docs/plans/10-frontend-design-system-decisions.md) · [work log](docs/daily_logs/2026-08-11/work_log.md) |
| 2026-08-10 | **Phase 9 Slice 9.2 — 개인 허브 `/me` + 통합 활동(v1.7.96, operation 77 → 78)** — 9.1 이 프로젝트별로 연 활동을 **개인 페이지가 통합**한다. 오너가 *"개인 페이지잖아? 프로젝트 페이지가 아니고? 그러면 **전체 통합이어야지**"* 로 못박았고, 그 한 문장이 **backend 새 표면**을 불렀다 — 활동도 관측도 전부 project-scoped 라 **사용자 전역 조회가 이 저장소에 없었다**. 새 `GET /me/activity` 는 **project id 를 받지 않는다**(경로에 식별자가 없으면 IDOR 표면도 없다) — 범위는 **소유 프로젝트**이고 주체는 세션에서만 유도한다. **★ 다중 사용자가 되어도 범위는 소유 기준이고, 그때 바뀌는 것은 범위가 아니라 표시다**(행위자 열을 켠다). 개인 페이지는 **잔여 사용량이 처음 집을 얻는 자리**이기도 하다(`useMemberQuota` 가 화면 없이 훅으로만 떠돌았다). 관리자는 로그인 직후 `/admin` 으로 가되 **루트에서 로그인했을 때만** — 이 게이트는 URL 을 안 바꾸고 제자리에서 로그인 화면을 그려 **딥링크가 이미 보존**되므로, 무조건 옮기면 의도한 도착지를 삼킨다. **★ 그래서 브리프가 적은 `?next=` 를 일부러 만들지 않았다**: 목적지가 이미 주소에 있는데 쿼리로 한 번 더 실어 나르면 **없던 open redirect 표면을 만드는 것**이다. **제품명이 생겼다 — "에-라잇"**(오너 명명). **★ 착수 직전 독립 검증이 이 브리프의 핵심 논거를 반증했다** — 초판이 *"fan-out 은 정확성이 깨진다"* 를 새 endpoint 의 유일한 "치명적" 사유로 썼는데 **거짓이다**(N ≤ per-project 상한이면 병합 top-N 이 정확하다. 증명 + 무작위 20,000회 ×2 실측). 결론은 유지하되 **근거를 저울질로 다시 썼고**, ⓑ 가 1인·소수 프로젝트에서 훨씬 실행 가능함을 오너가 **알고** 골랐다. 회귀 backend +10 · frontend +12 cells. | [decision](docs/plans/09-2-personal-hub-and-ia-decisions.md) · [work log](docs/daily_logs/2026-08-10/work_log.md) |
| 2026-08-10 | **Phase 9 Slice 9.1 — 활동 타임라인 화면(v1.7.94, operation 무변 77, backend 프로덕션 0줄)** — 9.0 이 연 조회 통로를 소유자가 **처음으로 실제로 본다**. 화면은 **최신 100건 상한을 문장으로 말하고**(응답에 총 건수도 "더 있음"도 없어서, 말하지 않으면 사용자가 **전부**로 읽는다 — purge UI 문구가 부분적으로 거짓이 되어 값을 치른 8.2c 와 같은 형태) **행위자 열은 만들지 않는다**(관리자 행위가 이 컬렉션 밖이라 행위자가 **항상 보는 사람**이다 — A2/A8 의 부수 효과이지 설계가 아니다). **★ 오너가 이 슬라이스에서 원칙 하나를 세웠다**: 초판 브리프가 UI 라벨표를 두는 것의 단점으로 *"정본이 둘이 된다"* 를 적자 **그 프레이밍을 기각했다** — *"서로 다른 섹션의 정본이면(중복이 아니라면) 정본은 몇 개가 되든 상관없어. **인덱싱만 제대로 되어 있고 연결만 되어 있으면**."* 판단 기준은 개수가 아니라 **①중복인가 ②인덱싱됐는가 ③연결됐는가**이고, **그래서 전수 가드는 벌칙이 아니라 분할을 성립시키는 연결선**이다(나쁜 것은 분할이 아니라 **끊긴 분할**). 그 연결선은 **pytest 에 산다** — 프론트는 백엔드 리터럴 목록을 알 방법이 없고 `schema.d.ts` 는 `action` 을 `string` 으로만 준다. **구현이 브리프 전제 하나를 반증했다** — `draft_version` 행은 payload 에 `draft_id` 가 없어 링크를 만들 수 없고, 넣으려면 계약 변경이라 **구현자가 그 자리에서 넓히지 않고** 유예로 올렸다. | [decision](docs/plans/09-1-activity-timeline-screen-decisions.md) · [work log](docs/daily_logs/2026-08-10/work_log.md) |
| 2026-08-09 | **Phase 9 A2 추가 확정 — 활동 로그가 `writing/accept` 를 포함한다(19 → 20, v1.7.93)** — 구현 중 실측이 브리프 안의 **기준 충돌**을 드러냈다: §0.2 는 accept 를 *성격*으로 "AI 요청"에 넣었는데 A2 의 기준은 *"무엇을 바꿨는가"* 이고 accept 는 **정본 draft version 을 실제로 저장한다**. 그것도 **주 저작 흐름의 저장 경로**라, 빼면 "이 원고가 마지막으로 저장된 것은 언제인가"가 수동 저장에만 답해진다. **구현자는 임의로 넓히지 않고**(오너 승인은 "19") 분류표 그 행에 근거를 달아 물었고, 독립 검증도 같은 판단을 냈다 — **오너가 넓히기로 결정**했다. **A8(중복 없음)은 개정되지 않는다**: 남기는 것은 AI 요청이 아니라 *정본 저장*이라 관측·원장이 담는 것과 다른 사실이며, **C 확장과는 다른 축**이다. 기록 조건은 상태코드가 아니라 **정본이 바뀌었는가** — Gate 거부(200·saved=null)는 안 남기고, 502 partial(version 은 저장됨)은 남긴다. | [decision](docs/plans/09-0-service-activity-log-decisions.md) · [work log](docs/daily_logs/2026-08-09/work_log.md) |
| 2026-08-09 | **Phase 9 Slice 9.0 — 서비스 활동 로그(v1.7.92, operation 76 → 77)** — 제품이 *"누가 · 언제 · 무엇을 바꿨는가"* 에 처음으로 답한다. **발원은 8.2c 를 결정하다 드러난 실측 공백**: 이 저장소에 사용자 활동 로그가 **한 줄도 없었다** — 문서에만 있던 `system_events` 는 코드 0줄, **개명은 덮어쓰기라 흔적이 전혀 없고**, `draft_versions` 조차 `created_at` 도 `user_id` 도 없었다. 이제 `activity_events` 에 **정본 변경 10 + 검토 결정 9 = 19 경로**가 한 행씩 남고 소유자가 `GET /projects/{id}/activity` 로 읽는다. **★ 이 컬렉션은 8.2c 이름 이력과 정확히 반대 방향이다** — `project_id` 필드를 써서 **파기 reconciler 에 발견되어야** 하며(프로젝트 자식), 그쪽의 `_id` 트릭을 복사하면 개명 이력 전체가 삭제 예외로 승격돼 **D8-6 이 무너진다**. 실 Mongo 셀 둘이 두 방향을 나란히 잰다. **AI 요청은 담지 않는다** — 이미 관측·원장이 담고 세 번째 사본은 두 정본이다. 다만 오너가 *"C 까지 열어야 할 수 있다"* 고 해서 **분류표를 mutating 40 전수**로 두어 **확장이 행 값 하나 바꾸기**로 끝나게 했다. 기록 실패는 요청을 죽이지 않고(격리) **파기 실패는 삼키지 않는다**. `before`/`after` 는 **라벨만**(200자 상한) — 본문 이력은 이미 정본에 있다. 회귀 +36 cells, 뮤테이션 8종 전부 재실패. | [decision](docs/plans/09-0-service-activity-log-decisions.md) · [work log](docs/daily_logs/2026-08-09/work_log.md) |
| 2026-08-09 | **라우터 정리 Slice 2 — 관리자 표면을 다른 주소로(v1.7.91, operation 무변 76, A1=ⓑ)** — Slice 1 이 76 operation 을 `routers/` 로 옮겨 놓았고, 이번에는 **파일 배치가 아니라 네트워크 토폴로지**가 바뀐다. `/admin` 8 operation 은 이제 **같은 이미지·다른 command 의 네 번째 compose 서비스**(`worker`·`generation_worker` 와 같은 패턴)가 서빙하고 **호스트 포트를 게시하지 않는다** — 도달 경로는 nginx `location /api/admin/` 하나다. **왜**: 제품 포트는 D8-7 G1=C 에 의해 **일부러 LAN 에 게시**되고 그 근거가 "세션 뒤에 있다"이므로, `/admin` 이 거기 있는 한 방어가 `require_admin_user` **한 겹**이었다. 이제 LAN 에서 치면 **가드가 아니라 라우터가 404** 이고 가드는 두 번째 겹으로 남는다. **★ 배포 앱은 둘인데 계약은 하나다** — `create_product_app()`(68)·`create_admin_app()`(admin 8 + `/health`)이 `create_app()`(76)과 **같은 함수 본문**을 `include_*` 로 갈라 쓴다. 합집합 앱을 남긴 이유는 브라우저가 nginx 뒤에서 **한 origin** 만 보기 때문이다(프론트 TS 계약이 그 origin 의 OpenAPI 다). 그리고 한 본문인 것이 곧 **H-2(shim drift) 의 처방**이다 — 검증자가 요구한 것은 감시였지만, "테스트가 보는 앱"과 "배포되는 앱"이 갈라질 수 있는 코드를 감시하는 것보다 **갈라질 수 없게 만드는 편**이 강하다(이 저장소는 `ObservedProvider` 계측 누락으로 이미 *fake green·배포에서만 틀림* 에 데었다). 새 가드 10 cells 가 합집합 = 제품 ∪ 관리자 · 교집합 = `/health` · operation 별 가드·상태코드·에러 선언 동일을 단정한다. 공개 표면 무변은 지문 **IDENTICAL**(route 76 · order-pairs 0 · openapi sha `f8b42ef1…`)이라 `schema.d.ts` 도 그대로다. 뮤테이션 7종(그중 하나는 **배포 관리자 앱에만 route 를 더하는** 정확히 H-2 형 drift) 전부 재실패. | [decision](docs/plans/router-split-and-admin-separation-decisions.md) · [work log](docs/daily_logs/2026-08-09/work_log.md) |
| 2026-08-05 | **Phase 8 Slice 8.2c — 프로젝트 이름 이력 + D8-6 삭제 계약 개정(v1.7.90, operation 무변 76)** — **3일 전 확정한 "purge는 이름을 남기지 않는다"를 오너가 개정했다.** 8.2 원장 행의 축이 `target_project_id` 하나뿐이라, 이름이 없으면 삭제된 프로젝트의 사용 기록이 영영 **id로만** 답해진다("이 id에서 37회"). 이제 purge가 **파괴 전에** 이름 한 값을 `project_name_history`에 스냅샷하고, 그 행은 삭제를 살아남는다. **★ `_id`가 project id이고 `project_id` 필드는 없다** — reconciler가 그 필드로 sweep 대상을 발견하므로, 필드를 쓰면 **이 슬라이스가 남기려던 이름을 스스로 지운다**(8.2의 `target_project_id` 개명과 같은 뿌리). 남기는 것은 **프로젝트 이름 최신 한 값뿐**이다 — draft 제목은 원장에 조인할 상대가 없고 장·절 제목은 줄거리를 드러낸다. **쓰기는 파기 시점에만**(살아 있는 이름의 정본은 `projects`, 복제하면 두 정본) 하고 **fail-closed**다(못 남기면 파기가 시작되지 않는다). **화면도 함께 고쳤다** — "전체가 삭제되며"는 이제 부분적으로 거짓이라 "…다만 **사용 기록 조회를 위해 프로젝트 이름은 보관됩니다**"를 명시한다. 무엇이 예외인지 말하지 않는 경고는 관리자가 확인할 수 없다. 오너 결정에서 특히 값진 지점: **"수정 기록 로그가 있으면 이름 이력도 넓혀야 하지 않나"라는 질문이 실측으로 반박됐다** — 활동 로그는 project 자식이라 purge와 함께 사라지므로 이름 이력의 소스가 될 수 없고, 반대로 살려 두면 D8-6이 무너진다(두 축이 직교한다). 그 실측이 **Phase 9(서비스 활동 로그)** 를 낳았다. 회귀 backend +13 cells · 뮤테이션 5종 전부 재실패. | [decision](docs/plans/08-2c-project-name-history-decisions.md) · [work log](docs/daily_logs/2026-08-05/work_log.md) |
| 2026-08-04 | **Phase 8 Slice 8.4 — 제품 경로 배선(v1.7.89, operation 75 → 76)** — 시행은 8.3에서 켜졌는데 **화면이 그것을 볼 수 없었다**(프론트에 quota 인지 0건, 한도에 걸리면 `"402: …"` 원문이 떴다). **★ 착수 실측이 제품 판단을 바꿨다: 한 번의 "생성" 클릭이 유료 요청 2~3건을 연쇄로 부른다** — `generate` → `gate` → (지적 있으면) `revise-and-gate`, `accept`까지 하면 흐름당 최대 4회다. 8.0 B1=A는 지켜지고 있고(요청 1건 = 1회), 다만 **체감 단위(클릭)와 과금 단위(요청)가 1:1이 아니라는 사실이 화면에 처음 드러나는 자리**다. 새 것 셋: **`GET /me/quota`**(통합 잔여 `min(일,주)`, 분자·분모 모두 **시행이 쓰는 함수**를 지난다 — 두 번째 계산 자리가 생기면 "3회 남음" 직후 402가 난다) · **429 블로킹 확인 대화**("하나 더 만들까요? 사용량이 1회 더 듭니다") · **잔여 타일**. **확인은 사용자 행동에서만 나온다** — 유료 호출 함수의 명시 인자뿐이고 자동 재전송·세션 플래그는 8.2b G4=A의 방어를 지운다(상시 확인 = 상시 통과). **연쇄 중간의 429는 그 단계만 되묻는다**(전체 재전송은 이미 성공한 생성을 재과금한다). **면제는 신분이 아니라 정책 행**이다 — `enforce_quota`에 tier 분기가 0줄이고 오너 결정대로 **첫 부트스트랩 관리자만** `create_user.py --admin`이 `limit=None` 행을 함께 쓴다. 프론트는 **`status`로만 분기**하며(H3), 소유권과 코드가 겹치는 정지(403)는 `/me/quota`의 `status`가 정본이다. 회귀 backend +23 · frontend +26 cells, 뮤테이션 5종 전부 재실패(그중 하나가 H3 가드의 결함 — 동등성만 단정하던 셀 — 을 드러내 강화했다). **독립 검증 합격(Blocking 0) 뒤 비차단 3건을 닫았다**: **정지(403) 판정을 스냅샷 확정 뒤로** 옮기고(로드 전이면 정지가 소유권 거절로 위장된다 — 403에서만 재조회, 402·429는 안 한다), 뮤테이션-셀 매핑을 표로 기록하는 것을 기록 규칙에 넣고, **최상위 README의 서술형 판정 분포·조건부 비율·SoT 버전을 가드에 묶었다**(넣자마자 살아 있던 거짓 2건을 잡았다). | [decision](docs/plans/08-4-product-wiring-decisions.md) · [work log](docs/daily_logs/2026-08-04/work_log.md) |
| 2026-08-04 | **Phase 8 Slice 8.3 — quota 시행 구현(v1.7.88)** — 8.0~8.2b가 만든 재료를 요청 경로에 **조립했다**. 시행은 유료 9경로의 `Depends(enforce_quota)`이고 **소유권 뒤에 선언**하므로 `404`·`403`은 구조적으로 무과금이다. **차감은 성공차감**이며 "성공"은 `2xx` **그리고** provider 호출이다 — 그래서 `analysis_extract` replay처럼 **아무 일도 안 한 200은 과금되지 않는다**. **비동기 202는 워커가 센다**(job에 `user_id` 비정규화 + `(user_id, status)` 인덱스). **초과는 구조적으로 불가능하다** — 유효 사용량 = 원장 + 조정 + 진행 중 잠금 + 대기·실행 중 job이고, 그 계산과 잠금 차지를 **회원 단위 입장 뮤텍스** 안에서 한다(뮤텍스를 지우면 실 Mongo 20-way에서 실제로 초과가 통과한다 — 뮤테이션 실측. 건수는 race라 실행마다 다르다). 새 상태코드 `402`(초과)·`429`(잠금·진행 중 생성), 확인은 헤더 `X-Confirm-Duplicate`, 유료 9경로 전수 가드 5종. **정산은 `yield` dependency가 아니라 route wrapper다** — partial envelope과 202는 `JSONResponse` **반환**이라 dependency가 상태코드를 볼 수 없고, 그 자리에서 정산하면 일하고도 실패한 응답이 과금된다. 회귀 +88 cells, 뮤테이션 10종 전부 재실패. **독립 검증 합격**(Blocking 0) 뒤 비차단 3건을 닫았다 — 정산이 성공 응답을 뒤집지 않게 하고(잠금 해제 실패는 로그), 미분류 유료 동작을 500 대신 **503**으로, 확인 헤더는 **내용**을 본다(빈 값은 확인이 아니다). | [decision](docs/plans/08-3-quota-enforcement-decisions.md) · [work log](docs/daily_logs/2026-08-04/work_log.md) |
| 2026-08-04 | **Phase 8 Slice 8.3 — quota 시행 결정 확정(Q1~Q9 + Q1-a·Q1-b·Q3-a)** — **차감은 성공차감**이다(오너: *"실패에는 과금하지 않는다 — 서비스적인 정책"*, 구현자 추천 선차감을 기각). 그래서 **"성공"의 정의가 계약**이 됐다: `2xx` **그리고** provider를 실제로 부른 경우만 센다 — 상태코드만 보면 **양쪽으로 틀리기 때문**이다(partial envelope 6곳은 일하고도 4xx/5xx, `analysis_extract` replay는 provider를 안 부르고 200). **비동기 202는 접수일 뿐이라 워커가 생성 성공 시 차감**하며, 이는 job에 `user_id` 비정규화를 강제해 **8.4의 "워커 주체 전달"을 8.3으로 당긴다**. **초과는 두 겹으로 막는다** — 진행 중 잠금(8.2b)을 사용량에 포함하고, 그 계산과 잠금 차지를 **회원 단위 입장 뮤텍스** 안에서 하여 초과를 **구조적으로 불가능**하게 만든다(오너 추가 요구: "그 ms를 블로킹한다든지"). **Mongo 트랜잭션은 이 문제를 못 푼다** — 스냅샷 격리라 count 술어를 직렬화하지 않는다. 나머지: 저장소 장애 fail-closed · 상태코드 셋(잠금 `429`/초과 `402`/정지 `403`) · 확인은 헤더 `X-Confirm-Duplicate`(쿼리 선례가 전부 GET/DELETE라 POST 의도를 URL에 안 싣는다) · `yield` dependency · `dedupe_key` 경로별 매핑표. 구현은 다음 슬라이스. | [decision](docs/plans/08-3-quota-enforcement-decisions.md) · [work log](docs/daily_logs/2026-08-04/work_log.md) |
| 2026-08-03 | **Phase 8 Slice 8.2 — 사용량 원장(L1=B·L2~L5=A, v1.7.85)** — 유료 동작 1회 = `request_usage_ledger` 행 하나. 관리자 조정은 같은 컬렉션의 다른 `kind`이고 두 종류의 **필드 구성이 겹치지 않는다**. **프로젝트 축은 `target_project_id`** — `project_id`면 purge reconciler가 과금 기록을 지운다(오너 결정: 삭제돼도 사용 기록은 남는다). **중복 방지 키에 `action`이 들어간다** — 프론트가 한 흐름에서 uuid를 공유하므로 그것 없이는 유료 동작 4개가 1행으로 접힌다. 유니크 인덱스는 조정 행 때문에 **부분 인덱스**여야 한다. 창 키는 8.1에서 가져오고 TTL은 없다. 기록·집계까지이며 차감·차단은 8.3, 5초 DB 잠금은 8.2b, 프로젝트 이름 이력은 8.2c로 분리했다. | [decision](docs/plans/08-2-usage-ledger-decisions.md) · [work log](docs/daily_logs/2026-08-03/work_log.md) |
| 2026-08-03 | **Phase 8 Slice 8.1 — quota 정책 저장 계약(P1~P8, v1.7.84)** — 오너 지적으로 **사용량 초기화 주기와 구독 이용기간을 다른 축으로 분리**했다. 사용량 창은 **일(KST 자정) + 주(가입일로부터 7일, 회원별)** 이중 창이고 두 창을 모두 통과해야 한다. 경계는 KST(저장은 UTC), 창은 **파생**이라 리셋 작업이 없다. 한도는 창별 `int|None`(None=무제한) + `status` enum이며 `0회`와 정지는 다른 상태다. 행이 없으면 코드 기본값이고 기본값은 **잠정**(일 20/주 100) — 호출부가 상수 대신 해석 함수를 지나 나중에 계산식이 들어와도 안 바뀐다. 정책 변경은 **필드별로 유리하면 즉시·불리하면 현재 주기 끝에** 발효하며(오너: 정지 1주 지연은 사용감 우선으로 수용, 즉시 차단은 계정 비활성화가 세션까지 끊는다) 해석이 순수 함수라 발효 worker가 없다. 저장·시행 분리 유지 — 차감·차단은 8.3. | [decision](docs/plans/08-1-request-quota-policy-decisions.md) · [work log](docs/daily_logs/2026-08-03/work_log.md) |
| 2026-08-03 | **Slice 8.0 독립 검증 합격(`3b7afc8`) + 비차단 보강 4건** — Blocking 0. 검증자가 scope 스윕·정규식 재파싱·전체 suite·뮤테이션을 독립 재도출해 구현자 주장과 전부 일치했다. 보강: **H1** "provider를 부르는 route는 반드시 잡힌다"가 아니라 "**scope를 여는** route까지"임을 4곳에 명시하고(과장 정정), 관습이던 동작별 관측 셀 대응을 `BillableActionObservabilityCoverageTest`로 강제했다 — 새 유료 동작을 관측 셀 없이 추가하면 실패한다. **H2** 워커 셀이 상관키(`job.request_id`)까지 단정한다. **H3** `VerificationCountClaimsTest`가 "N일치" 일수도 디스크에서 유도한다(종전 `39` 리터럴 고정이라 **고치는 쪽이 깨졌다**); 같은 병인 `docs/plans/` 문서 수·브리프 수 주장 4곳도 가드에 넣고 실측값으로 정정했다. **H4** 최상위 README의 기준선·SoT 표기 갱신. 계약 변경 없음. | [verification](docs/verifications/2026-08-03/slice_8_0_billable_boundary.md) · [work log](docs/daily_logs/2026-08-03/work_log.md) |
| 2026-08-03 | **Phase 8 Slice 8.0 — billable request 경계 확정(B1~B6=A, v1.7.83)** — 회원 사용량 **1회 = 유료 endpoint 요청 1건**으로 고정했다. 오너 결정의 근거는 원가 정확성이 아니라 제품 성격("사용자에게 쉬운 서비스")이며, **동작별 원가 차이는 요금 단위가 아니라 내부 BM에서 흡수**한다. 유료 경로 9개의 판정 기준은 `llm_call_scope`를 여는가이고, 내부 JSON repair와 설계된 루프 라운드는 그 1회에 포함하되 **관측에서는 전부 보이게** 유지한다(오너 단서). 비동기 생성의 워커 실행과 실패 job 재시도는 같은 논리 요청이라 재차감하지 않는다. 분류 정본은 `quota/billable_actions.py`이고 전수 가드가 양방향으로 강제한다. 차감 시점·기간·한도·원장은 8.1~8.3으로 넘겼고 카운터 코드는 만들지 않았다. | [decision](docs/plans/08-0-billable-request-boundary-decisions.md) · [work log](docs/daily_logs/2026-08-03/work_log.md) |
| 2026-08-02 | **D8-6 독립 검증 합격·트랙 종료(`c2ca946`)** — Blocking/Hardening 0. 구현자가 5축으로 요약했던 가드는 실제 **7축**이며 archive 409, requested fail-closed, outcome best-effort, 사유 공백, 정확한 이름, 503 no-retry, `target_project_id` 분리를 모두 mutation으로 재실패시켰다. 실제 test-mongo에서 감사 tombstone과 진성 고아를 함께 넣어 reconciler가 `admin_audit_events`를 구조적으로 건너뛰고 감사가 살아남는 것도 실증했다. backend 1911/4/1625, frontend 236/17, build exit 0. | [verification](docs/verifications/2026-08-02/d8_6_purge_ui.md) · [work log](docs/daily_logs/2026-08-02/work_log.md) |
| 2026-08-02 | **D8-6 purge UI·최소 감사(v1.7.82)** — 오너가 D1~D5=A를 확정해 archive-only backend 409, 사유 필수, project graph 밖 `admin_audit_events`, requested fail-closed/결과 best-effort, 정확한 이름 확인과 503 재시도 금지 UI를 시행했다. D4는 현행 core-first+reconciler를 유지하며, D4-D operation journal/saga는 원격 저장소·다중 worker 또는 수동 수습 부담이 생기는 시점의 후속으로 명시 추적한다. | [work log](docs/daily_logs/2026-08-02/work_log.md) · [decision](docs/plans/auth-d8-6-purge-ui-decisions.md) |
| 2026-08-02 | **Phase 8 회원별 요청 횟수 제한·Billing Readiness 계획 추가.** 오너 결정으로 회원 quota 기준은 토큰량이 아니라 **서비스 요청 횟수**로 고정했다. 요청별 토큰은 생성 상한에서 통제하고 회원 quota로 누적하지 않는다. 구현은 8.0 사용 동작 인벤토리부터 정책·원장·quota 시행·제품 경로 배선·관리자 CMS 백엔드·결제 entitlement seam·독립 검증까지 분리한다. 기간·차감 시점·실패 처리 등 세부 정책은 지금 선결하지 않고 각 슬라이스 착수 시 별도 결정 브리프로 확정한다. 실제 결제/환불/정산과 CMS 프론트는 범위 밖이다. | [phase plan](docs/plans/08-member-request-quota.md) · [work log](docs/daily_logs/2026-08-02/work_log.md) |
| 2026-08-02 | **D8-5d 독립 검증 합격 + vacuous 무소유 UI 가드 폐쇄**. 검증은 frontend 234/17·build·backend 1900과 관리자 guard/C-6/12자 양방향/무소유 승격/lazy chunk를 재현했다. 유일한 hardening: 무소유 버튼의 `disabled` 단정이 빈 사유 조건 때문에 owner 조건을 직접 잠그지 못했다. `01e97d2`에서 무소유 project의 승격·접근 이력 버튼을 아예 렌더하지 않고, 회귀가 두 버튼 부재를 단정하도록 바꿨다. under-strict(무소유 분기 제거)와 over-strict(정상 소유 project도 차단)가 각각 대응 셀을 실패. 전체 **234 passed / 17 files**, build 698 modules. D8-5 검증·hardening 종료. | [verification](docs/verifications/2026-08-02/d8_5d_admin_console.md) · [work log](docs/daily_logs/2026-08-02/work_log.md) |
| 2026-08-02 | **D8-5d 관리자 화면 완료 — D8-5 종료(SoT v1.7.81, backend operation 무변 74)**. `/admin`에 전역 KPI·사용자 생성/비활성화·전 프로젝트 메타데이터·사유 필수 1시간 승격·접근 이력을 연결했다. 비관리자는 화면 렌더 전에 차단돼 관리자 API를 호출하지 않고, 무소유 project는 승격으로도 열리지 않는 E1=A를 UI에서도 유지한다. 프로젝트 소유자는 작업공간의 별도 접근 이력 화면에서 관리자의 method/path/사유/시각을 확인한다. C-6이 만들었던 브라우저 로그인 공백도 닫았다: 로그인 409일 때만 새 비밀번호 설정 단계가 나타나며, 기존 비밀번호와 **12자 이상·확인 일치** `new_password`를 함께 보낸다. 일반 로그인/401 의미는 무변. 관리자 화면은 lazy chunk로 분리. frontend **234 passed / 17 files**, build 698 modules. 뮤테이션 3축(관리자 guard·무소유 승격·12자 경계)이 각각 회귀에 잡힘. | [work log](docs/daily_logs/2026-08-02/work_log.md) · `frontend/src/admin/` · `frontend/src/auth/AuthGate.tsx` |
| 2026-08-02 | **C-6 독립 검증 합격 + 비차단 2건 폐쇄(테스트 전용, 코드 0줄)**. 뮤테이션 4종이 주장과 정확히 일치했고 열거 방어도 확인됐다. **비차단 2건 모두 "구현자 주장이 실제보다 강했다"** 는 쪽이며 둘 다 직접 재현해 확정했다. **① [우선] 정본이 단언한 방어가 빈 셀이었다** — v1.7.80 이 "C-6 이전 계정은 안 잠긴다"고 적는데 `_entry` 의 `.get(..., False)` 를 **하드 서브스크립트로 바꿔도 1898 전부 통과**했다(필드 없는 문서를 먹이는 셀이 0건). **결과는 조용하지 않다**: 하드 서브스크립트면 배포에서 **C-6 이전 계정 전부가 로그인 시 KeyError→500** 인데 fake collection 이 늘 새 필드를 가져 **스위트는 green** — `sessions` 의 naive-datetime 함정과 **같은 형태**다. 필드 없는 문서를 읽는 셀 + 과잉 교정(항상 False) 짝을 넣어 **양방향**으로 잠갔다. **② 디스커버리 가드가 부분문자열 그렙이었다** — `assertIn("must_change_password=True")` 는 **주석에 글자만 있어도 통과**한다(실제 인자를 지우고 주석만 남겨 재현). 즉 "실제 호출부 집합"이 아니라 "파일 어딘가에 그 글자가 있다"를 잠그고 있었다. **AST 로 `create_user(...)` 호출의 키워드 인자를 직접 읽게** 고쳤고, 검증자가 뚫은 우회와 과잉 교정 양쪽이 이제 잡힌다. **★ 오늘 빈 셀이 두 번 나왔다**(D8-5e `owner_id=None` Blocking · 여기 pre-C-6 행) — 둘 다 *결함을 넣는* 뮤테이션으로는 안 보이고 **방어를 없애 보는** 뮤테이션이 잡았다. **규칙으로 적었다: 계약에 "…는 잠기지 않는다" 류의 방어적 단언을 쓰면 그 단언을 지나는 셀이 있는지 그 자리에서 확인한다.** **1900 passed / 4 skipped / 1608 subtests**(+2, 회귀 0). 검증 208건. | 독립 검증 [`verifications/2026-08-02/`](docs/verifications/README.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `tests/test_auth_users_mongo.py` · `tests/test_auth_api.py` |
| 2026-08-02 | **D8-5 C-6 — 관리자가 정한 비밀번호는 1회용이 됐다(SoT v1.7.80, 비밀번호 정책 신설)**. D8-5a 독립 검증 H-c 가 남긴 위험(*관리자가 사용자의 비밀번호를 아는 상태*)을 오너 결정 "최초 로그인 시 변경 강제"로 닫는다. **★ 강제 지점을 세션 발급으로 잡은 것이 설계의 전부다** — 대안(모든 operation 검사)은 73개 operation 에 새 상태코드를 선언하게 만들고 **"403 생산자는 정확히 둘" 불변식을 깬다**. 로그인에서 막으면 세션이 아예 안 생겨 **파급 0**이고 우회할 표면도 없다. `POST /auth/login` 이 교체 대기에 **409**(쿠키 없음), `new_password` 를 함께 받으면 교체 후 200+세션. **자격증명 검증이 먼저**라 틀린 비밀번호는 교체 대기 여부와 무관하게 401 — **409 가 열거 신호가 되지 않는다**. **대기가 아닌데 `new_password` 를 주면 409**(무시하면 "바꿨다"고 믿는 클라이언트에게 거짓말). **정책 = 최소 12자, 구성 규칙 없음**이며 **`create_user` 가 아니라 교체 경로에 걸었다** — 관리자 초기 비밀번호는 1회용이라 강도가 의미를 갖는 것은 durable 한 쪽이고, 덕분에 **기존 fixture 88곳을 안 건드린다**. 플래그는 **남의 비밀번호를 정하는 두 표면만** 세우고 자기 계정을 발급하는 live-smoke 는 안 세운다(디스커버리 가드로 양방향 고정). Mongo 는 `.get(..., False)` 라 **C-6 이전 계정은 안 잠긴다**. 뮤테이션 4종: 강제 무력화 → 7 · **over-strict(플래그 미삭제) → 2** · 정책 1자 → 2 · 불필요한 new_password 무시 → 1. **★ 이 슬라이스가 만든 제품 공백(정직하게)**: 프론트가 `new_password` 를 못 보내므로 **관리자가 만든 계정은 브라우저로 로그인할 수 없다**(API 로는 가능) — C-6 이전엔 됐으므로 그 경로에 한해 일시 후퇴다. D8-5d 화면의 필수 항목으로 올렸다. **1898 passed / 4 skipped / 1608 subtests**(+9, 회귀 0) · frontend 227 passed. **남은 D8-5 는 5-d 하나**. | 오너 결정 C-6(2026-08-02), `verifications/2026-07-28/auth_d8_5a_admin_boundary.md` H-c, [work log](docs/daily_logs/2026-08-02/work_log.md) · 회귀 `tests/test_auth_api.py::ForcedPasswordChangeTest` |
| 2026-08-02 | **D8-5b 검증 합격 + 문서 수치 드리프트를 가드로 강제(테스트 전용)**. 검증 Blocking 0·hardening 0. 검증자가 **"모델 vs 핸들러" 뮤테이션 통찰을 독립 증명**했다(핸들러 dict 에만 필드를 더하면 `response_model` 이 걸러 내 4셀 전부 통과, 모델에 더해야 잡힌다). **실제로 보강한 것은 검증자가 지적하지 않은 드리프트다** — 검증 기록 등재 때 **최상위 README 만 206건·합격 135 에 멈춰 있었다**(검증자는 자기 인덱스를, 구현자는 자기 것을 고친다). **오늘만 세 번째이고 매번 같은 파일이 뒤처졌다** — 원인이 구조적이라 숫자를 고치는 대신 갈라질 수 없게 만들었다: `VerificationCountClaimsTest` 가 ① 네 곳의 "N건" 주장이 **디스크 파일 수와 같은지** ② **판정 분포가 총계와 맞는지**(README 의 "조건부 27%" 주장의 분모/분자) 를 단정한다. **링크는 가드가 보고 있었는데 숫자는 아무도 안 보고 있었다.** 뮤테이션 3종: README 만 옛 숫자 → 1 · 분포만 어긋남 → **분포 셀만** 1 · over-strict(기록 파일 추가) → 6. **★ `git checkout --` 로 미커밋 수정을 날리는 함정을 오늘 세 번째로 밟았고**, 이번엔 그것이 **뮤테이션 표를 거짓으로 만들었다**(오염된 기준에서 분포 뮤테이션이 과장되게 실패). 커밋 후 재측정해 값을 정정했다. **1889 passed / 4 skipped / 1606 subtests**(+2, 회귀 0). 검증 207건 · 합격 136. | 독립 검증 [`verifications/2026-08-02/d8_5b_admin_project_list.md`](docs/verifications/2026-08-02/d8_5b_admin_project_list.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `tests/test_docs_indexes.py` |
| 2026-08-02 | **D8-5b — 전 프로젝트 *메타데이터* 목록(`GET /admin/projects`, SoT v1.7.79, operation 73→74)**. 관리자가 "무엇이 존재하고 누구 것인가"에 답하는 목록. **경계가 이 슬라이스의 전부다** — id·name·archived·owner_id 만 주고 **내용은 주지 않는다**. 내용을 읽는 것은 여전히 D8-5e 승격(사유·1시간 만료·감사)을 거치며, 목록이 내용을 흘리면 그 승격을 우회하는 뒷문이 된다. `owner_id` 는 **raw** 로 준다(콘솔이 `GET /admin/users` 를 이미 부르므로 join 은 N+1 만 만든다). **archive 포함 + 플래그**(soft delete 라 "무엇이 존재하나"에서 빠지면 안 된다). 공개 `_project_payload` 는 **여전히 owner_id 를 노출하지 않는다**(D8-2c 는 별개의 유예 결정). 목록 조회는 **승격 감사에 기록되지 않는다**(접근이 아니다 — 기록하면 접근 이력이 오염된다). **★ 뮤테이션이 내 셀의 겨냥이 빗나갔음을 알려 줬다**: "내용 필드를 흘린다"를 핸들러 dict 에 필드 추가로 시도했더니 **4셀이 전부 통과** — `response_model` 이 여분을 걸러 내 애초에 응답이 안 바뀌었다. **모델**에 추가하자 정확히 물었고, 그 사실을 셀 주석에 적었다(안 적으면 다음 사람이 핸들러만 고쳐 보고 "가드가 없다"고 오판한다). **교훈: 뮤테이션이 안 물면 셀이 약한 것이 아니라 뮤테이션이 관측 가능한 변화를 못 만든 것일 수 있다 — 후자를 먼저 배제한다.** 뮤테이션 3종(소유자로 좁힘·모델 필드 추가·감사 기록) 전부 1셀씩. **함께 정정**: `Project` 모델 주석이 *"owner_id does NOT restrict access yet"* 이라 단언했는데 D8-3b(v1.7.53)부터 시행 중이라 거짓이었다. **1887 passed / 4 skipped / 1602 subtests**(+4, 회귀 0). **남은 D8-5**: 5-d 관리자 화면 · C-6. | `plans/auth-d8-5-admin-decisions.md` §4, [work log](docs/daily_logs/2026-08-02/work_log.md) · `services/application/app/main.py` · 회귀 `tests/test_auth_api.py::AdminProjectListTest` |
| 2026-08-02 | **D8-5f 독립 검증 합격 + 비차단 2건 폐쇄(테스트 전용, 코드 무변)**. 검증자가 회귀 1881·뮤테이션 3종을 독립 재현했고 Blocking 0건. **직전 D8-5e Blocking(`owner_id=None`)의 완전 폐쇄**와 그것이 이번 감사와 맞물리는 방식도 확인됐다 — 무소유 project 는 승격 분기 진입 전 403 이라 `record_use` 에 도달하지 못하고 따라서 기록도 없다(거부된 접근은 기록하지 않는다는 원칙과 일관). 비차단 2건은 **"성질은 참인데 잠그는 셀이 없다"** 형태였고, 둘 다 **먼저 실측해 참임을 확인한 뒤** 셀로 잠갔다: ① `record_use` 경로의 **503 face** — fail-closed 셀이 `RuntimeError` 로 "통과하지 않는다"만 보였고 배포의 실제 얼굴은 전역 handler 에만 기대고 있었다(`AutoReconnect` 실측 → 503). 뮤테이션(예외를 502 로) → **2셀** ② **access-log 자기기록** — 승격 관리자의 로그 조회도 기록되어 감사에 사각지대가 없다. **실측에서 성질이 하나 더 나왔다**: 기록이 dependency 에서 일어나므로 **응답이 자기 자신의 조회를 포함한다**(로그를 연 사실이 로그에 남는다). 뮤테이션(access-log 만 기록 제외) → **1셀**. **1883 passed / 4 skipped / 1591 subtests**(+2, 회귀 0). 검증 206건 · 합격 135. | 독립 검증 [`verifications/2026-08-02/d8_5f_access_grant_audit.md`](docs/verifications/2026-08-02/d8_5f_access_grant_audit.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `tests/test_auth_api.py` |
| 2026-08-02 | **D8-5f — C-3 operation 단위 감사 + C-4 소유자 사후 조회(SoT v1.7.78, operation 72→73). F1=C의 하위 결정 C-1~C-5가 전부 시행됐다**. v1.7.77이 승격을 세웠지만 **"무엇을 봤는가"는 안 남았다** — 오너가 C-3 에서 "발급만 남기면 C를 고른 이유가 반감된다"고 한 그 절반이다. **C-3과 C-4를 한 슬라이스로 묶었다**: C-3만 하면 *읽는 코드 없는 저장*이 되는데 이 저장소가 `is_admin`으로 한 번 후회한 패턴이다. **시행**: 승격 아래 요청마다 `access_grant_uses`에 한 행(승격·관리자·project·method·경로·시각·**사유**), 기록 지점은 `require_project_owner`의 승격 분기 **하나뿐**이라 endpoint를 추가해도 우회 불가. 사유는 **use 행에 denormalize** — 승격 행은 재발급·파기로 바뀌므로 감사 행이 그 시점 사실을 자족적으로 담아야 한다. **★ fail-closed**: 기록 실패는 **요청 실패**(저장소 503 face). **LLM 호출 감사와 정반대이며 의도적** — 그쪽은 보안 하중이 없어 격리가 옳고, 이쪽은 하중을 받아 격리가 틀리다. 같은 계층에 정반대 정책 둘이 사는 것이라 **정본·주석 양쪽에 왜 다른지**를 적었다(안 적으면 다음 사람이 "일관성"으로 맞춘다). 추가 코드는 **0줄** — 감싸지 않으면 된다. **기록 대상은 승격 아래 접근뿐**(소유자 정상 사용을 섞으면 접근 이력이 활동 로그로 오염된다). **C-4**: `GET /projects/{id}/access-log`(project tier, 최신순) — 통지 채널이 없으므로 "소유자가 알게 된다"의 현실적 형태는 조회다. **뮤테이션 4종**: 기록 제거 → 3셀 · fail-closed 해제 → 1셀 · **over-strict(소유자까지 기록) → 2셀**. **1881 passed / 4 skipped / 1591 subtests**(+6, 회귀 0). **남은 D8-5**: 5-b · 5-d · C-6. | 오너 결정 C-3·C-4(2026-08-02), [work log](docs/daily_logs/2026-08-02/work_log.md) · `services/application/app/auth/access_grants.py` · 회귀 `tests/test_auth_api.py::AdminAccessGrantTest` |
| 2026-08-02 | **D8-5e 검증 Blocking 폐쇄 — 승격이 무소유 project 를 입양하고 있었다(E1=A 위반, 코드 수정)**. 독립 검증 **조건부 합격**: 뮤테이션 6종·회귀 1874 는 독립 재현으로 확증됐으나, **정본이 세 곳에서 단언한 계약을 코드가 안 지키고 있었다** — `require_project_owner` 의 **승격 분기에만** `owner_id is not None` 검사가 빠져 무소유 project 가 관리자 승격으로 **200** 이었다(첫 분기에는 있었다). **세 경로가 같은 결론**: 검증자 임시 테스트 · 별도 에이전트의 **강화 뮤테이션**(고칠 검사를 미리 넣었더니 82 passed — 아무 셀도 안 깨짐 = **빈 셀의 직접 증거**) · 구현자 재실측(반증 시도 실패). **왜 안 잡혔나**: `AdminAccessGrantTest` 11셀이 전부 alice **소유** project 만 써서 "무소유 + 승격" 조합이 **0건**이었다. **★ 교훈 — "가드가 많다"가 "그 축이 덮였다"를 뜻하지 않는다.** 내 뮤테이션 6종은 전부 *결함을 넣는* 방향이라 **없는 셀은 원리적으로 볼 수 없었고**, 강화 뮤테이션이 그것을 드러냈다. **수정**(`8f3c52e`, 테스트 선행): 회귀 셀로 결함 재현 → 검사 추가 → green → 되돌리면 그 셀만 재실패. **비차단 1건도 닫았다**: 내가 "인증 격리 셀"이라 라벨한 뮤테이션 실패는 실제로 probe 앱의 `AttributeError` 크래시였고, 재실측에서 **그것이 잠재 취약점**임이 드러났다(격리 셀이 `is_admin` 단락평가 덕에 우연히 통과 중 — 분기 순서를 바꾸면 성질 단정 대신 죽는다). probe 앱에 `access_grants` 를 배선해 원인을 없앴고 같은 뮤테이션이 이제 크래시 없이 정확히 1셀만 문다. **★ 사고**: `git checkout --` 로 미커밋 수정을 날리는 함정을 **같은 날 두 번** 밟았다(앞 슬라이스에서 교훈을 적어 놓고도). 그 뒤 수정을 먼저 커밋하고 뮤테이션을 돌렸다. **1875 passed / 4 skipped / 1579 subtests**(+1, 회귀 0). 검증 205건 · 조건부 57. | 독립 검증 [`verifications/2026-08-02/d8_5e_access_grants.md`](docs/verifications/2026-08-02/d8_5e_access_grants.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `services/application/app/main.py` |
| 2026-08-02 | **D8-5e — 관리자 승격(access grant): 소유권을 지나는 유일한 통로가 섰다(SoT v1.7.77, operation 71→72)**. 오너가 **C-1~C-6을 한 번에 결정**했다(브리프 §7의 구현자 추천 일괄 채택, **C-1 수명만 1시간** — 추천 30분보다 길게). **계약**: 관리자 tier를 넓혀 project 내용을 여는 대신, 관리자가 **사유를 적고** 한 project에 승격을 발급하면 **1시간 동안 그 project를 읽을** 수 있다. 관리자는 여전히 소유권 403을 받으며 이것이 **유일한** 통로다. **읽기 전용(C-2) 판정은 HTTP method**(`GET`/`HEAD`) — "읽기 operation 목록"을 손으로 두면 다음 endpoint를 조용히 오분류하고 그 방향이 열리는 쪽이라, 모르는 method에 **닫히는 쪽으로 실패**하게 했다. **사유는 도메인 경계에서 강제**(C-5)해 스크립트도 우회 못 한다. **발급 기록 = 저장소 행이고 append-only**(C-3 발급 축): **만료는 판정이지 삭제가 아니라서** 만료 행이 남고, 그래서 **TTL 인덱스를 두지 않는다**(`sessions`와 의도적으로 다르다 — 세션은 reap해도 잃을 게 없지만 승격은 감사 증거다. 회귀가 이 부재를 단정한다). **`is_admin` AND 승격**이라 강등된 관리자의 승격이 역할보다 오래 살지 않고, `owner_id=None`은 승격으로도 안 열린다(E1=A). **D8-6 파기에 배선** — 새 project-scoped 컬렉션을 안 물리면 전날 닫은 "조용한 고아"(D5)를 다시 여는 것이다. **뮤테이션 6종 전부 물었다**(읽기 전용 해제·`is_admin` 제거·승격 무력화·만료 판정 제거·TTL 24시간 + **over-strict: 소유자 경로 제한 → 소유자 전수 33건**). **★ 사고 기록**: 첫 뮤테이션 원복에 `git checkout --`를 써 **미커밋 main.py를 통째로 날렸다** — HANDOFF 함정 절에 이미 적힌 사고를 그대로 밟았다. 복구 후 **뮤테이션 전 체크포인트 커밋**(CLAUDE.md §6)을 하고 나니 같은 도구가 안전해졌다. **1874 passed / 4 skipped / 1579 subtests**(+24, 회귀 0) · frontend 227 passed · `tsc` exit 0. **후속**: C-3 operation 단위 감사 · C-4 사후 조회 · 5-b · 5-d · C-6. | 오너 결정(2026-08-02), `plans/auth-d8-5-admin-decisions.md` §7, [work log](docs/daily_logs/2026-08-02/work_log.md) · `services/application/app/auth/access_grants.py` · 회귀 `tests/test_auth_access_grants.py` |
| 2026-08-02 | **독립 검증 반영 — "8개 호출부 전부 계측" 단언의 근거 인용이 틀렸다(문서 전용, SoT 무변)**. `bfd4690`(제품 한 장 요약)의 독립 검증 **조건부 합격**. 검증자가 문서의 사실 단언을 **코드로 직접 실측**해 대부분 확증했으나(추출 3종·Gate 4유형/5판정·RRF·화면 부재·수치·회귀 1850 전부 재현), **반복 등장하던 단언 하나가 반박**됐다: `product-overview.md`가 "8개 호출부 전부가 감사 레코드를 남긴다"면서 근거로 `observability-kpi-rationale.md`를 인용했는데 **그 파일에는 "8"이 한 번도 안 나오고, L53은 오히려 "호출부 계측 확대"를 §6 로드맵(=미완료)으로 나열**한다. **가장 아픈 점 — 같은 슬라이스가 바로 그 §6이 낡았다고 적어 놓고 그 문서를 근거로 인용했다**(판정과 인용이 한 세션 안에서 갈라졌다). **★ 구현자 재실측으로 검증자 주장 2건은 반박했다**(적대적 검증은 양방향이다): ① "8의 기준이 없다"는 틀렸다 — `LlmCallSite` enum 멤버 **8** + `ObservedProvider(` 감싸기 **8** + 코드 주석 *"eight real LLM adapters"* 로 **이중 확인**되며, 검증자가 센 scope 10·endpoint 9·TASK_TYPE 5는 다른 축의 개수다(진짜 결함은 *기준 부재*가 아니라 *문서가 기준을 안 밝힘*) ② "worker 미기록"은 프로덕션에서 틀렸다 — `=None`은 테스트 collaborator 기본값이고 워커 엔트리포인트의 기본 `build_fn`이 `main.py:2148`에서 감사를 배선한다(실제 미기록은 계약상 제외인 script·diagnostic). **수정**: 세 문서에서 "8"의 기준 명시 + "전부"를 호출부 단위로 한정 + 틀린 인용 교체 · `00-foundations` 정정문의 시점을 **v1.7.57 → v1.7.49**(D0=A)로 고치고 **그 오류의 출처인 HANDOFF도 함께**(패턴 스윕) · SoT 표에 대한 "거짓" 판정을 **"자기 모순"**으로 정밀화(L696·L709 구현 완료 ↔ L710 미구현이며, 카드/타임라인/관계 그래프는 실제 미구현이라 부분적으로 성립 — 구현자 표현이 과했다). 검증 기록 신설 + **203→204건 · 조건부 55→56**. **1850 passed / 4 skipped / 1559 subtests**(무변, 회귀 0). | [독립 검증](docs/verifications/2026-08-02/product_overview.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `docs/product-overview.md` · `README.md` · `HANDOFF.md` |
| 2026-08-02 | **기획 축 "제품 한 장 요약" 신설 + 권위 소스의 낡은 단언 5건 정정(코드 무변, SoT 무변)**. 오너 지시로 등재돼 있던 문서 부채를 닫았다 — 제품 컨셉·MVP 범위·핵심 위험이 전부 `abstract.md`(48KB) 안에 있어 평가자든 새 작업자든 §1·§2·§15·§16을 직접 뽑아 읽어야 했다. **원본 요약이 아니라 코드 실측 기준으로 다시 썼다**(HANDOFF 경고: abstract는 2026-06 초안이라 그대로 압축하면 낡은 그림이 정문에 걸린다). 실측이 드러낸 **원안 대비 차이 5건**이 문서의 §5가 됐다 — ① 단일 사용자 → 다중 사용자 ② 추출 5종 → **관찰 3종**(장소·관계 미착수, 떡밥은 `open_question_observation`) ③ Continuity/POV/떡밥 Gate 분리 → **Writing Gate 하나**(지적 4유형·판정 5종, `style`은 판정을 안 바꾼다) ④ 문체는 학습이 아니라 **선언**(프로젝트 브리프 4필드) ⑤ 관측이 제품 기능으로 추가. MVP 4는 **승인 UI만 섰음**을 실측(Review Inbox 병합/분할까지 배선, 인물카드·타임라인·관계 그래프 화면 0건). 문서는 "정직한 공백" 절로 **로그인 표면 부재·dogfood 미착수**를 명시한다. **★ 작성에 필요한 권위 소스를 전부 읽는 과정이 패턴 스윕이 됐다** — 낡은 단언 8건 중 **사실 확정이 가능한 5건을 즉시 정정**했다(어제 Blocking #1의 교훈: 결정을 기다리는 동안 독자는 거짓을 읽는다): `plans/00-foundations.md`의 **"MVP는 단일 사용자 제품이다"가 불변 원칙으로 단언**돼 있었고(취소선 + 정정 블록, 원문은 이력이라 보존) · `plans/README.md`가 **존재하지 않는 파일** `tests/test_plans_index.py`를 가드로 지목 · README의 SoT 버전 v1.7.75 → v1.7.76 · `docs/README.md` 검증 202 → 203건 · `client.ts`의 `ReviewConflict` 주석이 **"no merge/split yet"**(실제로는 배선됨). **남은 3건은 고치지 않았다** — 특히 **정본 §"현재 구현 상태"가 `Phase 2~6 UI \| 미구현`이라 단언**하는데 이는 거짓이며, 정정이 버전 개정 사안이라 오너 선택으로 올렸다(ⓑ "이력 표"로 성격 명시 추천). **1850 passed / 4 skipped / 1559 subtests**(무변, 회귀 0) · `tsc --noEmit` exit 0. | [`docs/product-overview.md`](docs/product-overview.md), [work log](docs/daily_logs/2026-08-02/work_log.md) · `README.md` · `docs/README.md` · `docs/plans/00-foundations.md` |
| 2026-08-02 | **정정 — purge의 "멱등 재시도"는 거짓이었다(SoT v1.7.76, endpoint 동작 무변)**. 독립 검증 조건부 합격의 Blocking #1: 내가 결함을 발견·확증·수습하고 HANDOFF에 정정까지 적었지만 **거짓 단언의 원천**(SoT v1.7.74 changelog · endpoint docstring)은 그대로 두었다 — 오너 결정(ⓐⓑⓒ) 이후로 미룬 판단이었으나 **그 사이 독자는 거짓을 읽는다**. 검증자 제언대로 **결정과 무관한 즉시 정정**을 했다: 지금 코드가 무엇을 하는지는 결정과 무관하게 확정된 사실이기 때문이다. ① endpoint docstring이 재시도 불가 이유(core_sot 선행 → 404 → derived 미도달)·잔류물이 무해하지 않은 이유·수습 도구를 적는다. ② **SoT v1.7.76** 신설로 정정하고, v1.7.74 행은 다시 쓰지 않고(이력) 취소선 + 정정 포인터만 달았다. ③ **패턴 스윕에서 같은 부류를 하나 더 발견** — 본문 §"Product Shell"의 *"영구 삭제 정책은 여전히 미확정이며 D5에서 결정한다"*가 D5=A 확정·D8-6 출시를 못 따라가 낡아 있었다(검증자 미지적). **★ 정정을 산문으로 끝내지 않았다** — `AdminProjectPurgeTest::test_a_second_purge_is_404_and_never_reaches_the_derived_services`가 재시도의 404와 **derived 미도달**을 spy로 단정하므로, 나중에 ⓑ·ⓒ를 구현하면 이 셀이 실패해 **문서 정정을 강제한다**(오너 결정 ⓒ를 흉내낸 뮤테이션으로 실증). 함께: 뮤테이션 표를 정밀화(하드코딩 목록에 `index_sync_outbox`를 넣으면 1건·빼면 2건 실패)하고, reconciler 요약에 `live_project_count`를 추가해 **운영자가 "안 건드리는 규모"를 실행 전에 확인**할 수 있게 했다(stdout 파싱 셀로 잠금). **부수 발견: 어제 만든 검증 인덱스 가드가 검증자의 신규 기록 미등재를 실제로 잡았다** — 등재 후 판정 분포를 재실측했다(합격 134 · 조건부 **55** · 서술형 14 = **203건**). **1850 passed / 4 skipped / 1559 subtests**(+2, 회귀 0). | 독립 검증 `verifications/2026-08-02/purge_reconciler.md`, [work log](docs/daily_logs/2026-08-02/work_log.md) · `docs/system-contract-sot.md` · `services/application/app/main.py` |
| 2026-08-02 | **purge reconciler — 파기된 project의 잔류 데이터를 찾아 지운다(D8-6 잔여, SoT 무변)**. 결함의 형태: `POST /admin/projects/{id}/purge`가 core_sot(8컬렉션)을 **먼저** 지우고 derived 8종·enqueue를 뒤이어 하므로, derived 단계에서 mongo 장애가 나면 503이 나가지만 **재시도가 불가능하다** — 두 번째 호출은 core_sot이 이미 비어 `NotFound` → **404**로 끝난다. endpoint docstring과 SoT의 "부분 실패 시 재시도(멱등)" 주장은 **derived 단계 실패에는 성립하지 않았다**. 08-01 기록은 잔류물을 "ghost(무해)"로 판단했으나 **`llm_call_audits`에는 프롬프트 본문이, `writing_drafts_scratch`에는 원고 후보가 남으므로** 파기 요청받은 데이터의 잔존이고 D5 "부분 삭제는 조용한 고아" 금지 위반이다. **드물다는 판단에는 동의**하므로 endpoint를 고치는 대신 정리 도구를 만들었다: [`scripts/purge_reconciler.py`](scripts/purge_reconciler.py)는 **컬렉션 목록을 코드에 적지 않고 DB에서 발견**해(`project_id`를 쓰는 모든 컬렉션) `projects`에 행이 없는 project_id를 고아로 판정하고, **기본 dry-run**·`--apply`로만 삭제하며, **삭제 후 마지막에** `PROJECT_PURGED`를 enqueue해 worker가 vector/lexical 5백엔드를 지우게 한다. 회귀 6셀(실 Mongo): 고아 발견 · **살아 있는 project 보존**(삭제 도구라 이 방향이 더 위험) · 모르는 새 컬렉션도 스윕 · dry-run 무해 · CLI 배선 · enqueue 순서. **★ 첫 판 순서 가드는 거짓이었고 뮤테이션이 잡았다** — 컬렉션 발견이 apply 루프 전에 일어나 빈 outbox는 발견되지 않으므로 순서가 무의미했다; 실제 조건(죽은 project를 향한 기존 sync 작업)을 넣어 복원한 뒤 뮤테이션이 정확히 그 셀만 물었다. **endpoint 자체의 재시도 가능성은 오너 결정으로 올렸다**(순서 변경 또는 404 계약 변경). **1848 passed / 4 skipped / 1559 subtests**(+6, 회귀 0). | [work log](docs/daily_logs/2026-08-02/work_log.md) · `scripts/purge_reconciler.py` · `tests/test_purge_reconciler.py` |
| 2026-08-02 | **README를 기획 · 개발 · 서비스 3축으로 재구성(문서만, SoT 무변)**. 오너 요청("KPI 설정·시스템 구성 같은 개발이 아닌 기획쪽 문서도 종합적으로 표현"). 직전 README가 링크하던 docs **8개가 전부 개발 축**이었고, 기획·서비스 자료는 **있는데 배선이 없었다**. 가장 큰 누락은 [`docs/observability-kpi-rationale.md`](docs/observability-kpi-rationale.md) — *"어떤 운영 질문에 답하려고 이 지표를 잡았고 이 숫자로 어떤 의사결정을 하는가"*를 정리한 **운영/제품 기획 문서**이며 본문이 이미 포트폴리오를 의식해 쓰여 있는데도 어디에서도 링크되지 않았다. **기획** 절(풀려는 문제 = 장편 창작에서 무너지는 것은 문장력이 아니라 일관성 · 제품 원칙 3종 · Phase↔MVP 매핑 · 운영 KPI 기획), **개발** 절(기존 결정·검증 파이프라인), **서비스** 절(구성 · **노출 정책** · 관측 · **실사용 되먹임**: 실검수 브리프 = 계약 재협상 기록, 벤치마크 = 기본값이 측정에서 왔다 · 운영 절차 · 함정 축적)로 갈랐다. 단계는 **"로컬 1인 운영(dogfood 직전)"으로 정확히 명시** — GATE-1이 오너 결정 대기이므로 실사용 지표가 있는 것처럼 쓰지 않았다. `docs/README.md`도 성격별 지도 표로 교체했다(종전 머리말 "두 영역으로 구분한다"는 하위 8개 디렉터리와 어긋난 상태였다). 가드는 링크 정규를 넓혀 **앵커가 붙은 링크의 파일 경로도 검사**한다(종전에는 `#`가 붙으면 통째로 건너뛰어 파일이 없어도 통과했다). **1842 passed / 4 skipped / 1556 subtests**(문서 변경, 회귀 0). | 오너 요청, [work log](docs/daily_logs/2026-08-02/work_log.md) · `README.md` · `docs/README.md` |
| 2026-08-02 | **포트폴리오 진입 경로 — 결정과 검증이 정문에서 보인다(문서만, SoT 무변)**. 오너 요청("결정·검증을 평가자가 보기 깔끔하게 README에 연결"). 실측한 공백: 최상위 `README.md`가 **47줄 · docs 링크 3개**뿐인데 저장소에는 **결정 브리프 73개**와 **독립 검증 기록 202건(39일치, 전부 `## Verdict` 보유)**이 있었다 — 이 저장소에서 가장 특징적인 것(브리프 선행 → 구현 + 양방향 가드 → 적대적 독립 검증 → 정본 개정)이 **정문에서 전혀 안 보였다**. ① **`docs/verifications/README.md` 신설** — 검증이 *반증 시도*임과 기록 골격(Scope/Methodology/Findings/Issues/Verdict/Outstanding), **뮤테이션 기법**("테스트가 통과한다"로는 그 테스트가 무엇을 잡는지 알 수 없다), **판정 분포 합격 134 · 조건부 합격 54 · 서술형 14**(조건부 **27%**가 형식적 통과가 아니라는 증거), 대표 기록 4건, 전체 202건 최신순 표. ② **`README.md`에 "어떻게 만들어졌는가" 절** — 5단계 파이프라인과 각 단계 실측 규모 + 평가자용 3단계 짧은 경로. ③ 가드를 `test_docs_indexes.py`로 일반화해 **검증 인덱스 커버리지**와 **최상위 README 링크 해석**까지 잠갔다(진입점의 깨진 링크가 가장 비싸다). **함께 판단**: 디렉터리 재편은 **하지 않기로** 했고 근거를 실측으로 남겼다 — 브리프 경로 인용이 **1,083건 / 253개 파일**이고 **183개가 과거 검증 기록**이라 옮기면 지나간 기록 본문을 대량 수정하게 되는데, 도달 불가 증상은 인덱스로 이미 닫혀 남은 동기가 미학뿐이다. **1842 passed / 4 skipped / 1556 subtests**(+3, 회귀 0). | 오너 요청·판단 위임, [work log](docs/daily_logs/2026-08-02/work_log.md) · `README.md` · `docs/verifications/README.md` · `tests/test_docs_indexes.py` |
| 2026-08-02 | **`docs/plans/` 인덱스 부채 해소 — 브리프가 다시 찾아진다(문서만, SoT 무변)**. 오너 지시(2026-07-28)의 첫 조각이며, 착수 계기는 오너가 **자기 결정 브리프를 찾지 못한 것**이다. 실측: 문서 **90개 중 51개가 README 미등재**였고(HANDOFF 기록 88개 중 50개보다 **더 나빠져 있었다** — 방치되는 동안 자란다) 미등재분은 대략 2026-07-20 이후 전부라 **최근 것일수록 안 보이는** 형태였다. **가드를 먼저 쓰고**(처음 실행이 51건을 나열하며 실패 → 그것이 작업 목록이 됐다) README의 39항목 평평한 목록을 **트랙별 표 15개**로 재구성해 **89개 전부 등재**했다. 파일명 접두 체계가 이미 무너져 있어(`00`~`07` 계열 + 접두 없는 최근 것들) **접두가 아니라 트랙으로** 묶었고, "읽는 순서"는 처음 오는 사람용 6개로 줄여 *순서*와 *인덱스*의 역할을 갈랐다. **파일 이동·병합·삭제 0건** — 브리프는 오너 결정의 근거 기록이라 통폐합하면 "왜 그렇게 정했는가"가 사라지고 디렉터리 재편은 미결정이다. 이 슬라이스가 회복한 것은 정리가 아니라 **도달 가능성**이다. 전수 가드 `tests/test_plans_index.py`가 양방향을 잠근다: 미등재 문서 존재 시 실패(새 브리프 등재가 규칙이 아니라 강제) · 인덱스가 없는 파일을 가리켜도 실패(이동·개명 후 링크 방치 방지). 둘 다 뮤테이션으로 실증. **1839 passed / 4 skipped / 1556 subtests**(+2, 회귀 0). | 오너 지시(2026-07-28), [work log](docs/daily_logs/2026-08-02/work_log.md) · `docs/plans/README.md` · `tests/test_plans_index.py` |
| 2026-08-02 | **인증 D8-7 G1=C — "외부 노출 금지"가 해제됐다. 인증해서가 아니라 노출을 없애서다(SoT v1.7.75)**. 오너가 브리프의 4지선다에서 **C**(노출면 축소를 지금, 자격증명은 원격 배포 시점)를 확정했다. 실측된 위험은 저장소 3종(mongo 27520·chroma 8523·elasticsearch 9520)이 **0.0.0.0으로 게시돼 LAN에서 무인증으로 열려 있다**는 것이었고, 그 위험은 자격증명 없이도 완전히 사라진다. 각하가 아니라 **단계 판단**이다 — `mongod --auth --replSet`은 keyfile을 강제해(직접 확인) 커밋 불가 시크릿이 머신 3대마다 필요해지고 repo의 정의적 성질("어느 머신에서든 `docker compose up`만으로 뜬다")이 깨지는데, 로컬 1인 단계에서 그 비용을 낼 실익이 없다. 축소는 자격증명을 막지 않으므로 **되돌릴 필요 없이 겹쳐 쓴다**. 시행은 compose 한 곳뿐이다: `mongo`·`chroma`·`elasticsearch`·`gateway`·`embedding` + `test-mongo`가 **`127.0.0.1:` 바인드**(`docker compose config` 실측 host_ip 5건, `docker port` 라이브 확인). **애플리케이션 코드 0줄** — 컨테이너 간 통신은 compose 네트워크 이름이고 호스트 툴링(mongosh·migrate·호스트 pytest·runbook curl)은 localhost로 그대로 돈다. **일부러 공개인 것 셋**: `application`·`frontend`(제품 표면 + 세션 뒤) · `llama` 9080(GPU 머신이 12B를 못 올리는 머신에 모델을 주는 유일한 크로스머신 의존 — 노출되는 것은 내용이 아니라 연산). 정본은 §"제품과 프로젝트 경계"의 해제 조건을 *인증*에서 *노출 없음*으로 개정하되 **저장소가 여전히 무인증이라는 사실**과 **포트를 다시 열면 원래 위험이 그대로 돌아온다**는 것을 함께 못박았다. 전수 가드 `tests/test_compose_exposure.py`가 세 compose 파일을 읽어 ① 저장소 loopback ② 공개 표면 유지(과잉 교정 방지) ③ 게시 서비스 집합 ↔ 분류 리터럴 대조(미분류 신규 서비스 강제)를 잠그고, 세 방향 전부 뮤테이션으로 재실패를 실증했다. **1837 passed / 4 skipped / 1556 subtests**(기준선 1831 대비 +6 passed·+7 subtests, 회귀 0). **독립 검증 합격**(blocking 0) 후 3건 반영: ① 검증자가 인라인 `ports: ["…"]` 표기로 분류 강제를 **실제로 우회**했고(docker는 수용, 가드는 통과) 파서를 인라인·작은따옴표·무따옴표까지 넓혀 닫았다 — 회귀 `ComposeParserTest`가 네 표기 + "ports 없는 서비스는 안 잡는다"를 함께 잠근다. ② 정본의 tier 카운트가 낡아 있어(69·관리자 4·project 59) 코드 실측대로 **71·관리자 5·project 60**으로 정정하고 **숫자의 권위 소스는 문서가 아니라 코드**임을 명시했다. ③ **★ "시행 완료"는 compose 파일 수준에서만 참이었다** — 포트 매핑은 컨테이너 생성 시점에 고정되므로 기존 컨테이너는 옛 `0.0.0.0` 매핑을 유지한다(현재 Exited라 노출은 없으나 그대로 살리면 복귀). HANDOFF 함정으로 못박았고 **재생성은 오너 결정 대기**다. G2~G6(자격증명 설계)은 각하가 아니라 원격 배포 시점의 실행 계획으로 브리프에 남는다. | 오너 결정(G1=C), [work log](docs/daily_logs/2026-08-02/work_log.md) · `plans/auth-d8-7-infra-auth-decisions.md` · `docker-compose*.yml` · `tests/test_compose_exposure.py` |
| 2026-08-01 | **앱 route를 치는 스크립트 9종에 세션 로그인이 붙었다 — D8-3a가 만든 401 부채 해소(SoT 무변)**. D8-3a 이후 모든 route가 세션을 요구해 운영 smoke·진단 스크립트가 전부 401을 받았고, 특히 `diagnose_writing_report`·`diagnose_writing_gate`는 report·gate 실패 원인을 보는 **유일한 진단 도구**인데 막혀 있어 2026-07-29 장애 조사에서 쓰지 못했다. 공용 `scripts/script_auth.py` 한 곳이 로그인을 담당하고, **운영자용 8종**은 `--username` + `$APPLICATION_PASSWORD`(비밀번호 argv 금지 — `create_user.py` 선례: shell history·`ps` 노출), **자기 스택을 소유하는 in-process smoke 1종**(`phase2a_provider_live_smoke`)은 일회용 계정을 스스로 발급한다. 자격증명이 없으면 종전대로 익명 진행. **★ 세션은 명시 `Cookie` 헤더로 싣는다** — 세션 쿠키가 `Secure`라 httpx 쿠키 저장소는 plain http(`application:8000`·ASGI `application-smoke`)에서 조용히 버린다(뮤테이션 실측 `cookie: None`). 다만 헤더가 유일한 정답은 아니고 `cookies.set`도 동작하므로(실측) 회귀는 *행동*만 잠근다. **가드 두 겹**: 레지스터 9종이 실제로 로그인하는가 + `scripts/*.py`를 읽어 앱 route 마커가 있는데 미등록인 스크립트를 잡는 **디스커버리 가드**. 첫 판이 `application:8000` 문자열만 스윕해 ASGI 호스트를 쓰는 9번째를 놓친 채 "8종 전부"라고 보고했고(독립 검증이 잡음), 그 실패 유형을 구조적으로 닫은 것이 디스커버리 가드다. backend **1831 passed / 4 skipped / 1549 subtests**. | [work log](docs/daily_logs/2026-08-01/work_log.md) · `scripts/script_auth.py` · `tests/test_script_login.py` |
| 2026-08-01 | **D8-6 영구 삭제 트랙 종료 — `POST /admin/projects/{id}/purge`로 D5 전체 그래프 파기가 완성됐다(SoT v1.7.74, operation 70→71)**. 6a(core_sot 8컬렉션) · 6b(derived 10컬렉션) · **6c**(vector/index 5백엔드 `purge_project` + worker `PROJECT_PURGED` drain) · **6d**(endpoint)가 모두 붙어, 관리자가 project를 지목하면 정본 → 파생 → 색인까지 하나도 남지 않는다. endpoint는 `core_sot.purge` → derived 8 service purge → `enqueue_project_purged`(워커가 Chroma/ES를 파기) 순서로 조율하고 **204**를 낸다(파기 = 소멸이라 payload 없음, archive와 구분). 부분 실패는 **503 + 재시도**(전역 handler·purge 멱등). 인가는 `_REQUIRE_ADMIN`이며, **`{project_id}` 경로인데 소유권 대신 관리자 인가를 쓰는 유일한 의도적 예외**다(관리자는 id로 파기할 뿐 내용을 읽지 않는다 — D5). worker drain은 **whole-event all-or-retry**(한 entry가 memory·candidate 두 composite로 흘러 per-sink 격리 키가 충돌하므로). 독립 검증은 **조건부 합격**이었고 — endpoint→derived fan-out이 어떤 회귀에도 잠기지 않아 8개 호출을 전부 지워도 green이었다 — `_PurgeSpy` 회귀로 그 빈 칸을 닫아 조건이 해소됐다. **남은 것**: 프론트 purge UI(D8-5 오너 결정 C-1~C-6 선행) · 감사 로그 · 완전 멱등 재시구(core_sot 파기 후 derived 장애 시 재시도가 404가 되는 좁은 창). | [work log](docs/daily_logs/2026-08-01/work_log.md) · [verification](docs/verifications/2026-08-01/d8_6d_purge_endpoint.md) · `services/application/app/main.py` · `services/application/app/indexing/service.py` |
| 2026-07-31 | **R-a 유도를 `revise-and-gate` 루프와 `/writing/accept`로 확장 — R-a 적용 지점 5곳이 전부 채워졌다(SoT v1.7.66)**. v1.7.65 ⑤가 "루프는 범위 밖, 패키지 병합·`retrieve_more`와의 상호작용은 별도 판단"으로 남겨둔 것을 닫고, 패턴 스윕으로 accept 누락도 잡았다. `/writing/revise-and-gate` 엔드포인트만 `body.max_tokens`를 가공 없이 쓰고 있어 루프의 report 다리(출력 상한 6144 + 후보 산문)가 창을 넘으면 K-3 가드가 400으로 거부했었다. **판단 = 진입 시 1회 유도**: report 엔드포인트와 같이 `derive_context_budget`를 거쳐(후보가 이미 있으므로 `candidate_tokens_from_text` 사용) `search_request.context_budget`에 넣는다. **루프 본체는 무변경** — 루프가 `context_budget.max_tokens`를 패키지 예산·merge 상한 양쪽에 그대로 쓰므로 유도값이 들어가면 `merge_context_packages`가 retrieve_more를 포함한 패키지 성장을 그 값으로 묶는다. per-round 재유도는 기각(이미 만들어진 패키지는 merge 없이 못 줄이므로 라운드마다 숫자만 유도해 봤자 후보 성장을 못 품). 남는 초과는 K-3 가드가 백스톱. **패턴 스윕으로 accept도 같은 결함임을 잡았다** — `WritingAcceptService.run` → `reporter.enrich`로 report 다리를 지나는데 원래 구현(2026-07-12)부터 raw 예산이었다(v1.7.65가 놓침). 같은 수정으로 닫았다. **공개 스키마 구조는 무변**, `WritingReviseRequest`·`WritingAcceptRequest`의 `max_tokens` description만 generate·report와 같은 "창에 맞춰 줄일 수 있는 상한(늘리지 않음)"으로 정렬. gate 엔드포인트는 report 다리가 없어 유도 대상 아님(raw 그대로). 뮤테이션(under-strict: 두 엔드포인트를 raw로 되돌리면 `8192 != 1182`로 재실패) + 양방향 wiring(revise-gate·accept 각 2건). backend **1777 passed / 1 skipped / 1502 subtests**(직전 1773 대비 +4 = 신규 wiring 4건, 회귀 0). | [work log](docs/daily_logs/2026-07-31/work_log.md) · [brief](docs/plans/context-budget-korean-tokens-decisions.md) · `services/application/app/main.py` · `services/application/app/writing/report_budget.py` |
| 2026-07-30 | **K-1(a) 한글 토큰 환산 + 입력 예산 8192 — 예산이 이제 자기 말대로다(SoT v1.7.63)**. 오너 지시 ①·④의 구현이며 컨텍스트 예산 트랙의 마지막 구현 조각이다. `len/4`(영어 어림값)는 한글에서 **−56% 과소평가**한다 — 배포 원고 **429블록 · 21,774자**를 외부 12B `/tokenize`로 전수 계수해(실제 **12,747 tok**, 밀도 **1.708 자/tok**) 브리프의 상수 1.7을 실 원고에서 확인했다. 합성 표본의 밀도 범위(1.30~2.46)보다 실측이 좁았다(1.20~2.02). **1.4~1.5로 더 보수적으로 잡지 않은 이유**는 코퍼스가 +20~24% 과대평가로 기울어 멀쩡한 항목이 예산에서 잘리기 때문이고, 남는 항목별 −20%는 **K-3 가드가 받는다**(가드는 이 추정을 쓰지 않고 서버 계수를 쓴다) — **K-3이 있어서 K-1이 무편향을 고를 수 있는 구조**다. 회계/실제 비율이 **0.46 → 1.07**이 됐다. **예산 4096→8192는 확장이 아니라 회계 수정의 짝**이다: 종전 `4096 회계 ≈ 실제 8,904 tok`, 지금 `8192 회계 ≈ 실제 7,656 tok` — 숫자는 두 배인데 **실제 분량은 줄었다**(회계만 고치면 실효 컨텍스트가 절반이 된다). **함정 하나를 실측으로 잡았다**: 프론트가 `max_tokens`를 명시적으로 보내므로 **서버 기본값만 올리면 제품에는 아무 효과가 없다** — 양쪽을 함께 올렸고 두 회귀가 그 대칭을 잠근다. **K-1(c)(색인 시점 정확 계수)는 착수하지 않았다** — 브리프가 (c)를 추천한 유일한 하중 근거가 K-3 가드로 사라졌고 (c)는 색인에 LLM 의존을 새로 만들기 때문이며, 추천은 보류다(오너 결정 대기). 뮤테이션 4종 양방향. backend 1738 passed / 1 skipped / 1498 subtests, frontend 219 passed, 공개 계약 = OpenAPI default 6곳. | [work log](docs/daily_logs/2026-07-30/work_log.md) · [brief](docs/plans/context-budget-korean-tokens-decisions.md) · `services/application/app/context_search/service.py` · `frontend/src/writing/WritingPanel.tsx` |
| 2026-07-30 | **K-3 창 가드 — 창을 넘는 요청은 모델을 부르기 전에 400으로 거부하고, 빠듯한 호출은 경고로 보인다(SoT v1.7.62)**. 오너 결정: "400 거부 및 경고로 가자. **비용 측면에서 필요한 가드**니까." 가드 식은 **`입력 + 출력상한 ≤ 창`**이다 — `입력 ≤ 창`으로는 부족하다(프롬프트 단독 초과는 서버가 400을 주지만 `프롬프트+출력` 초과는 **200인데 출력만 조용히 잘려** 아무도 모른다). 판정은 게이트웨이 seam 한 곳에서 하고 **걸리면 모델 왕복 0회**다. **입력 수는 추정이 아니라 서버가 센 값**이다 — `/apply-template`+`/tokenize`(`add_special`, 같은 `chat_template_kwargs`)로 실제 `usage.prompt_tokens`와 **delta 0**(실측 51/51 · 49/49 · 6,143/6,143). 덕분에 가드는 밀도 보정(K-1)과 **독립**이며, `len/4` 위에 세웠다면 −55% 과소평가로 걸려야 할 때 걸리지 않았다. 신규 literal `provider_context_window_exceeded`는 "우리가 거부(왕복 0)"와 "모델 서버가 거부(왕복 1)"를 구분하고, 앱은 **400**·job은 별도 사유 `context_window_exceeded`로 낸다(재시도가 반드시 같은 실패로 끝나므로 화면에서 섞이면 안 된다). **판정할 수 없으면 통과**시킨다 — 방어가 자기 실패로 기능을 깨뜨리지 않는다. **경고는 저장하지 않고 KPI가 읽기 시점에 파생한다** — v1.7.59가 "헤드룸은 파생값이라 저장하지 않는다"로 정했으므로, 감사의 원천 세 값에서 `여유 < 창의 10%`를 세어 **분모와 함께** per-site·전역에 싣고 관측 화면이 그린다. **설계 하나를 되돌렸다**: 첫 호출까지 덮으려고 가드가 창을 기다리게 했더니 v1.7.60의 "기다리지 않는다"를 어겨 B1 회귀 셀이 매달렸다 — 되돌리고 그 공백(첫 호출·`/props` 실패 프로세스)을 오너 결정 대기 부채로 남겼다. 뮤테이션 15종 실증(게이트웨이 7·앱 3·KPI 3·프론트 2). backend 1736 passed / 1 skipped / 1492 subtests(직전 1720 대비 +16), frontend 219 passed(+2). 공개 계약 변경은 KPI 2필드 + compare 400 선언뿐. | [work log](docs/daily_logs/2026-07-30/work_log.md) · [brief](docs/plans/context-budget-korean-tokens-decisions.md) · `services/llm_gateway/app/client.py` · `services/application/app/observability/kpi.py` |
| 2026-07-30 | **R-e — report가 포인터를 베끼지 않고 항목 번호를 인용한다(SoT v1.7.61)**. 2026-07-29에 `long` report를 4/4 죽였던 지배 항을 없앤 슬라이스다. 프롬프트 항목이 `- [label] {포인터 JSON} text` → **`- [N] [label] text`**가 되고 모델은 `related_context_pointers: [1, 3]`처럼 **번호**를 쓰며 **번호→포인터 매핑은 서버가** 한다 (대응표를 프롬프트에 실으면 절감이 0이라 그 안이 아니다). **오너 결정 K-6=R-e(2026-07-29)의 구현**이며, 결정이 R-a에서 R-e로 뒤집힌 이유는 "출시된 프롬프트라 새 버전이 필요하다"는 전제가 사실이 아니었고(report 템플릿은 Mongo 영속이 아니다) 오너가 MVP 단계라 프롬프트 변경이 자유롭다고 확인해 **R-e가 가장 싼 안이면서 유일하게 근거를 늘리는 안**이 됐기 때문이다. **실측(베타, 외부 12B `n_ctx=16384`, 같은 heavy project·항목 69개)**: report 프롬프트가 **11,841 → 3,216 tok(−72.8%)**, 헤드룸이 **−1,665 → +7,024**로 부호를 바꿨다. before 값은 어제 배포 레코드(11,837·11,905)와 사실상 일치하므로 재구성이 아니다. 실 12B 관통 **3/3 strict parse OK·repair 0회** — 번호 인용은 모델이 정확히 한다(브리프가 미검증으로 남겼던 항목). **공개 계약 무변**(`schema.d.ts` diff 0, backend 1720 passed / 1 skipped / 1477 subtests): 도메인은 여전히 `ContextPointer`이고 바뀐 것은 모델 wire 한 겹이다. 오히려 D2=A가 강해졌다 — 모델이 만들 수 있는 것이 **범위 안의 정수**뿐이다. 번호는 **1-based**(모델이 "없음"으로 쓰는 `0`이 조용히 첫 항목을 근거로 만들지 않게), `bool`은 `int` 하위형이라 별도 거부, 구 포인터 객체도 거부. **부수 정리**: 예산 회계와 프롬프트 렌더링이 한 정의(`item_render.render_context_item`)로 합쳐져 "의도된 중복" 부채가 닫혔다(사본을 두게 했던 순환 import가 사라졌다). **남은 것은 창 가드(K-3)** — 창 8192에서는 `3,216 + 6,144 = 9,360 > 8,192`이고 그 초과는 400이 아니라 조용한 잘림이다. | [work log](docs/daily_logs/2026-07-30/work_log.md) · [brief](docs/plans/context-budget-korean-tokens-decisions.md) · `services/application/app/context_search/item_render.py` · `services/application/app/writing/report.py` |
| 2026-07-29 | **컨텍스트 예산 관측(K-3 1a·1b) — 자원 배분 지표가 측정 가능해졌다(SoT v1.7.58·v1.7.59)**. 오너 목적은 "효과적인 자원 배분"과 컨텍스트 효율 분석이며 외부 API를 써도 같은 분석이 필요하다. **1a**: `total_tokens` 하나로는 입력 8,000/출력 500과 그 반대가 같은 값으로 접혀 "창의 얼마를 입력에 쓰는가"를 물을 수 없었다. 데이터가 없던 게 아니라 `ObservedProvider`가 게이트웨이가 주는 분해를 버리고 있었다 — 살렸다. **1b**: 분모(창)를 채웠다. 생성 응답에 `n_ctx`가 없음을 실측하고 transport에 `get_json`을 더해 `/props`를 **한 번만 조회·캐시**한다. **조회 실패는 생성을 깨뜨리지 않고 재시도하지 않는다**(감사 격리와 같은 원칙). **헤드룸은 파생값이라 저장하지 않는다** — 원천 셋만 남기고 사후 판정하며, 이 형태가 **"거부하지 않는다"는 오너 결정을 구조적으로 보장**한다. `None`은 두 슬라이스 모두 **"모른다"이고 0/기본값이 아니다** — 0으로 적으면 효율 지표가 낙관 쪽으로 오염되고, 창에 기본값을 채우면 **헤드룸이 지어낸 분모 위에서** 계산된다. **관측을 켜자마자 계약 위반이 데이터로 드러났다**: `writing_report`가 입력 11,905 + 상한 6,144 = **18,049 > 16,384**로 **헤드룸 −1,665**인데 **job은 성공**(실제 출력 1,371로 상한 미사용) — **초록불이면서 계약이 깨진 상태**가 산술이 아니라 레코드로 보인다. report는 **창의 73%를 입력에 쓴다**. 뮤테이션으로 공백 2건을 잡았다: Mongo 왕복이 **빈 값(None==None)이라 필드 누락을 통과시키던 것**, 그리고 창 조회 생략·재시도. backend **1712 passed / 1 skipped / 1468 subtests**, 공개 계약 무변. **남은 것은 초과를 없애는 것**이며 K-6=R-e(포인터 제거)가 그 자리다 — 성공 판정을 이제 데이터로 할 수 있다. | [work log](docs/daily_logs/2026-07-29/work_log.md) · [brief](docs/plans/context-budget-korean-tokens-decisions.md) · [verification 1a](docs/verifications/2026-07-29/slice1a_io_token_breakdown_audit.md) · `services/llm_gateway/app/client.py` · `services/application/app/observability/` |
| 2026-07-29 | **컨텍스트 예산 슬라이스 1 — 예산이 항목의 *렌더링된 형태*를 센다(코드 수리, SoT bump 없음)**. 베타 실 스택 관통에서 **`long` 프리셋이 실제 원고 분량 입력에 4/4 실패**하는 것을 관측한 뒤 원인을 잡아 고쳤다. 실패는 예측됐던 "조용한 잘림"이 아니라 **400 하드 거부**(`exceed_context_size_error`)였고 generation job 전체가 죽었다. **근본 원인은 한글 밀도(2.4배)가 아니라 회계가 항목 `text`만 세고 항목마다 붙는 포인터 JSON(64자 `content_hash` 포함)을 한 토큰도 세지 않은 것**이다 — 시드 3,586자 project에서 `token_estimate_total` 887 vs 실제 렌더링 11,304 tok(**12.7배**), **포인터가 report 컨텍스트의 79%**. 생성은 같은 항목을 포인터 없이 싣기 때문에(`include_pointers`는 report 한 곳뿐) **report만 죽는 비대칭**이 이것으로 설명된다. `estimate_rendered_item_tokens()`를 더해 생성 3곳이 쓰게 했고, 소비자 2곳(`_apply_budget`·`writing/retrieval.py`)은 `item.token_estimate`를 소비할 뿐이라 손대지 않았다. **회귀는 생산자마다 다른 셀에 걸린다** — 뮤테이션에서 메모리/후보 생산자를 되돌리면 기존 목표 테스트는 **통과**하고 새 셀만 실패했다(목표 픽스처의 8개 항목이 전부 `source_blocks`였다). **독립 검증이 착수 전에 테스트 보정 오류를 잡았다**: 첫 판 기준이 전체 패키지 렌더링(463)이었는데 항목별 회계로 도달 가능한 상한은 431이고 차이 32는 어떤 항목에도 귀속되지 않는 구조적 래퍼라, 올바른 수정으로도 빨간불이 남았을 자리였다 — 기준을 항목별로 바꾸고 **양변을 같은 렌더러로 재계산해 자명해지는 함정**도 피했다. **실측 전후**: 회계 887→**4,049**, 예산 통과 항목 69→**60**, report 프롬프트 12,462→**11,027** tok, 그리고 오전에 4/4 실패한 관통이 **4/4 성공**(`writing_report` 4/4 `provider_error`→4/4 success). **귀속 주의**: 오너가 같은 날 베타 외부 서버 창을 8192→16384로 올렸으므로 이 반전은 둘의 합작이며, 다만 **창 확대 단독으로는 400이 조용한 잘림으로 바뀌었을 뿐**이고 회계 효과는 창과 무관하게 분리 측정됐다. backend **1703 passed / 1 skipped / 1468 subtests**(기준선 1700/1/1468 대비 +3 = 신규 테스트 수와 일치), `schema.d.ts` diff 0(공개 계약 무변). **의도된 한시적 중복**: 렌더링 형식이 `writing/prompt.py`와 `context_search`에 두 벌인데 되돌려 import하면 순환이라 사본을 두고 일치를 회귀로 잠갔다 — **K-6=R-e가 포인터를 프롬프트에서 없애면 사본 대부분이 사라지며**, 판단 시점·근거를 코드 주석과 HANDOFF 추적 부채에 남겼다(오너 지시: 테스트만으로 잠그지 말고 문서에도 명시). 남은 순서는 **R-e → 가드(K-3) → 밀도(K-1)**이고, 수정 후에도 `프롬프트 + 출력 상한 = 17,171 > 16,384`라 상한 기준으로는 여전히 빠듯하다. | [work log](docs/daily_logs/2026-07-29/work_log.md) · [brief §2-3](docs/plans/context-budget-korean-tokens-decisions.md) · [verification](docs/verifications/2026-07-29/beta_long_report_pointer_root_cause.md) · `services/application/app/context_search/service.py` · `tests/test_context_search.py` |
| 2026-07-28 | **인증 D8-5c — 전역 관측 KPI `GET /admin/observability/kpi`(SoT v1.7.57)**. F1=C의 하위 결정에 의존하지 않는 슬라이스라 5-b·5-d보다 먼저 섰다. 감사 저장소 둘에 `list_all()`을 더하고(nullable `project_id`가 아니라 별도 메서드 — None이 흘러 경계를 넘는 형태를 만들지 않는다) 집계는 `_fold`를 공유해 **같은 코드 경로**로 넓혔다. 응답은 per-project 읽기의 `project_id` 자리에 **`projects_considered`**를 싣는다 — project 축을 분모의 형태로만 남기고 **어떤 project도 이름 짓지 않는다**. **per-project 분해를 넣지 않은 것은 결정이다**: 넣으면 관리자에게 project 식별자를 노출해 5-b와 F1=C 하위 결정을 앞지른다. **구현 중 발견**: 오독 방어 3종 중 `multi_call_correlations`가 입력을 넓히는 것만으로 무너졌다 — 버킷 키 `correlation_id`는 호출자가 준 `request_id`라 서로 다른 project가 같은 문자열을 쓸 수 있고, 전역에서 두 project의 1회 호출이 **일어나지 않은 repair**로 접혔다. 키를 `(project_id, correlation_id)`로 고치고 양방향으로 잠갔다(per-project 결과 불변). 관리자 tier는 4개가 되어 전체 **69 operation = public 4 · 인증 전용 2 · 관리자 4 · project 59**. backend **1700 passed / 1 skipped / 1468 subtests**(신규 19 · subtest +13), frontend 217 passed·build 무변, `schema.d.ts` +74줄 additive. | [decision brief](docs/plans/auth-d8-5-admin-decisions.md) · [work log](docs/daily_logs/2026-07-28/work_log.md) · `services/application/app/observability/kpi.py` · `tests/test_observability_kpi.py` |
| 2026-07-28 | **인증 D8-5a — 관리자 경계와 사용자 관리(SoT v1.7.56)**. D6=A(최소 관리자)의 첫 하위 슬라이스이며, v1.7.51부터 저장만 되고 **읽는 코드가 없던** `is_admin`의 첫 소비자다. `require_admin_user`가 `/admin/*` 3개 operation(`GET·POST /admin/users`, `POST /admin/users/{id}/deactivate`)을 지키고 인증된 비관리자는 **403**이다(세션은 살아 있으므로 401이 아니다). **403의 생산자가 소유권·관리자 정확히 둘**이 되어 §H3 상태코드 표와 결합 불변식을 그렇게 고정했고, boundary matrix는 68 operation을 public 4 · 인증 전용 2 · 관리자 3 · project 59로 분할한다. **관리자는 project 내용에 접근하지 않는다** — 오너 결정 **F1=C**에 따라 타인 project 접근은 감사·만료를 동반한 별도 승격이며 이 슬라이스에 없다. **F2=A**로 마지막 활성 관리자는 비활성화되지 않고(409), 비활성화는 살아 있는 세션을 즉시 죽인다(D2가 JWT를 각하한 이유). 뮤테이션 7종으로 새 셀을 검증했고 그중 하나에서 **`response_model`이 해시 누출을 구조적으로 거른다**는 것을 실측했다(모델을 없애거나 모델에 필드를 더하는 실제 누출 구성은 회귀가 문다). backend **1681 passed / 1 skipped / 1455 subtests**(test-mongo ON, 종전 1653/1418에서 신규 28 테스트), frontend 217 passed·build 무변. 5-b·5-d는 F1=C의 하위 결정 C-1~C-5 선행이라 착수하지 않았고 5-c(전역 KPI)는 무관해 진행 가능하다. | [decision brief](docs/plans/auth-d8-5-admin-decisions.md) · [work log](docs/daily_logs/2026-07-28/work_log.md) · `services/application/app/auth/users.py` · `tests/test_auth_api.py` |
| 2026-07-28 | **인증 D8-3c — 401·403 최종 결합 감사로 HTTP 시행 3단계가 닫혔다(SoT v1.7.55)**. 정책·endpoint 동작은 그대로 두고, 3-a(인증 축)와 3-b(소유권 축)가 각자 전수였기에 주인이 없던 **교차 칸**만 채웠다. 65개 operation이 route dependency 기준으로 public 4 · 인증 전용 2 · project-scoped 59 중 정확히 하나로 분류되고 tier 구성원이 리터럴로 고정된다. 결합 선언 불변식(403은 401 없이 불가 · 소유권 dependency는 인증 없이 불가 · identity 중복 불가)과 결합 실동작(세션 없는 요청이 타인 project를 지목해도 **401**, 59개 전수)을 잠갔고, 독립 검증 H-1의 소유자 통과·`owner_id=None` 403·미존재 404를 1개 route 표본에서 **59개 전수 subtest**로 올렸다. 뮤테이션 6종으로 새 셀이 실제로 무는지 확인했고, 그 과정에서 **인증 두 겹 중 한 겹만 빠지면 관측 상태코드가 변하지 않는다**는 것을 실측해 안쪽 겹을 별도 셀로 잠갔다. **독립 검증 합격(차단 0건)**이며, 비차단 H-a(시그니처 단정의 구현 결합)는 바깥 겹을 뺀 일회용 앱에 소유권 dependency만 마운트해 상태코드로 단정하도록 바꿔 검증 후 닫았다 — 합성 dependency 리팩터링은 통과하고 인증 소실은 여전히 실패함을 양방향 실측했다. backend **1653 passed / 1 skipped / 1418 subtests**(test-mongo ON, 종전 1644/1058에서 신규 9 테스트·360 subtest), 소스·공개 계약 무변. 다음은 D8-7 인프라 인증이며 그때까지 외부 노출 금지를 유지한다. | [work log](docs/daily_logs/2026-07-28/work_log.md) · `docs/system-contract-sot.md` · `tests/test_auth_api.py` |
| 2026-07-28 | **D8-3b 독립 검증 조건 폐쇄(SoT v1.7.54)**. 조건부 합격의 차단 B-1·B-2를 모두 닫았다. §H3 상태코드 표에 사용자↔project 소유권 불일치 403을 추가하고, 404는 하위 자원의 `project_id` 불일치에 한정해 계약 자기모순을 제거했다. Mongo 회귀는 반환값만 보던 테스트와 별도로 `find({"owner_id": owner_id})` filter 자체를 스파이로 단정해 Python 후처리 뮤테이션을 막는다. 비차단 H-2의 dependency 내부 저장소 장애→503도 회귀로 잠갔고, H-4는 test-mongo ON 전량 실행을 주 기준선으로 복구했다. backend **1644 passed / 1 skipped / 1058 subtests**, frontend 계약·타입·테스트·빌드 재검증 완료. H-1은 D8-3c, H-3·H-5는 후속 UX/정밀화로 남긴다. | [verification](docs/verifications/2026-07-28/auth_d8_3b_project_ownership.md) · [work log](docs/daily_logs/2026-07-28/work_log.md) · `docs/system-contract-sot.md` · `tests/test_core_sot_mongo.py` · `tests/test_auth_api.py` |
| 2026-07-28 | **인증 D8-3b 시행 — project 소유권 격리 + 목록 저장소 필터(SoT v1.7.53)**. project-scoped **59개 operation 전부**가 인증 위에 별도 `require_project_owner` dependency를 선언해 타인 소유와 `owner_id=None`을 403으로 거부한다(E1=A). 없는 project는 기존 404, 소유자는 정상 통과한다. `GET /projects`는 응답 후 가공이 아니라 `list_projects_for_owner` 저장소 경계에서 좁히며 Mongo는 `{"owner_id": current.id}`로 조회해 타인·무소유 project의 id/name/archive 상태가 wire에 닿지 않는다(E2=A). OpenAPI 403도 같은 59개에만 additive로 선언하고 `POST /projects`·`GET /projects` 등 비project operation에는 거짓 403을 싣지 않았다. 전수 가드는 route dependency+선언 양방향 일치와 타인 project로 59개 실제 호출을 함께 잠그고, 소유자 200·missing 404·비project 403 부재를 over-strict 방향으로 보호한다. 전체 백엔드 무인프라 기준 **1554 passed / 89 skipped / 1058 subtests**, 신규 경계+실 Mongo 집중 **6 passed / 126 subtests**, 인증 전체 **32/326**, 프론트 **217/14**·tsc/build 성공(진입 404.87 kB, 관측 lazy 385.71 kB). 다음은 동작 추가 없는 D8-3c 최종 401·403 결합 감사이며 D8-7 인프라 인증 전까지 외부 노출 금지를 유지한다. | [work log](docs/daily_logs/2026-07-28/work_log.md) · [E1~E3 brief](docs/plans/auth-d8-3-enforcement-decisions.md) · `services/application/app/main.py` · `tests/test_auth_api.py` |
| 2026-07-27 | **인증 D8-3a 시행 — 무인증 표면이 닫혔다(SoT v1.7.52)**. `/health`와 공개 `/auth` 두 곳을 뺀 **61개 operation 전부**가 dependency `require_authenticated_user`를 선언하고, 세션 없는 요청은 handler 이전에 **401**을 받는다(D7=A: 미들웨어가 아니라 dependency + 전수 가드). 공개 예외 4개는 각각 이유를 계약에 적고 테스트의 `PUBLIC` 리터럴로 열거해 **새 endpoint는 기본이 보호**가 되게 했다. 전수 가드는 두 겹이다 — route 객체의 dependency 선언 검사(선언만 있고 배선이 빠진 drift는 OpenAPI로는 안 보인다)와 **61개를 실제로 세션 없이 호출**하는 런타임 검사. 정상 세션이 통과하는 over-strict 가드, `/health`·`/auth/logout`이 **낼 수 없는 401을 선언하지 않는지** 검사하는 over-strict 가드도 함께 넣었다. `POST /projects`는 이제 `owner_id=None`을 만들지 않는다(생성자는 가드가 해석한 값). 도메인 스위트 ~130개 클라이언트는 `tests/auth_support.py` 한 곳의 dependency override로 인증 상태가 되며, **경계 자체는 override 없는 앱으로만 검증**한다. 백엔드 **1631 passed / 4 skipped / 873 subtests**, 프론트 **217 passed**·build 무변(진입 404.87 kB). **인가(403·소유권)는 아직 없어 로그인만 하면 남의 project도 열린다 — 외부 노출 금지 유지**; 다음은 D8-3b. | [work log](docs/daily_logs/2026-07-27/work_log.md) · [E3 brief](docs/plans/auth-d8-3-enforcement-decisions.md) · `services/application/app/main.py` · `tests/test_auth_api.py` |
| 2026-07-27 | **인증 D8-4 프론트 로그인 선행 + E1~E4 확정(SoT v1.7.51)**. 오너가 E1~E4를 모두 A로 확정했다: 무소유 project는 탈퇴·삭제 누락 같은 미래 비정상 잔존까지 항상 deny, project 경로는 소유권·그 외는 인증, 목록은 저장소 경계에서 본인 소유만 필터, D8-3은 3-a 인증→3-b 소유권/목록→3-c 전수 가드로 분할한다. E4는 번호보다 자연스러운 제품 흐름을 우선해 **로그인 UI를 인가보다 먼저** 세우는 결정이다. 프론트는 `/auth/me` 확인 전 보호 route를 mount하지 않고, 미인증/만료 시 현재 경로를 유지한 로그인 표면으로 전환하며 성공 후 원래 작업으로 복귀한다. 로그인 실패 메시지는 계정 존재를 구분하지 않고 비밀번호 필드를 비우며, 로그아웃은 서버 revoke 성공 뒤 상태를 버린다. 공통 API 전송 경계가 `credentials: same-origin`과 보호 API 401 만료 통지를 맡고, 패턴 sweep에서 발견한 partial-envelope 직접 fetch 2곳도 같은 경계로 수렴했다. 공개 백엔드 계약 무변(`gen:api` diff 0). **독립 검증 조건부 합격의 B1~B3를 오너 지시로 폐쇄**했다: effect 완료 대기, 로그아웃 pending/실패 양방향, `revise-and-gate` 401 회귀. 최종 프론트 **217 passed / 14 files**, tsc+build 성공(진입 404.87 kB, 관측 lazy 385.71 kB), 배포 데스크톱/모바일 렌더 확인, 검증 판정 **합격**. **백엔드 인가는 아직 없으므로 외부 노출 금지**; 다음은 D8-3a. | [work log](docs/daily_logs/2026-07-27/work_log.md) · [verification](docs/verifications/2026-07-27/auth_d8_4_frontend_login.md) · [E1~E4 brief](docs/plans/auth-d8-3-enforcement-decisions.md) · `frontend/src/auth/` |
| 2026-07-26 | **단계 전환 — MVP 단일 사용자 유예 만료, 다중 사용자로 확장(SoT v1.7.49·v1.7.50, 오너 결정 D0=A)**. 오너가 로그인·인증·아이디별 제작물 관리·CMS 삭제·관리자 기능을 요청했고, 이것이 정본 §"제품과 프로젝트 경계"의 두 조항("MVP는 계정/인증이 없는 단일 사용자 시스템"·"`user_id`를 지금 억지로 넣지 않는다")과 충돌해 D0로 올렸다. **오너 판정: 그 조항들은 MVP 단계의 제약이었고 단계가 끝났다 — 정본을 수정·확장하면 된다.** 실제로 두 조항은 스스로 "MVP는"·"지금"이라는 **시점 한정어를 달고 있었고**, 만료 조건이 조항 안에 적혀 있었다. **정본 개정 4곳**: ① 두 조항 → "다중 사용자로 확장한다", 단 **"아직 구현되지 않았으므로 배포 스택은 여전히 무인증 — 이 조항을 근거로 외부에 노출하지 말 것"**을 함께 못박음(계약이 의도를 먼저 적고 코드가 뒤따라오는 구간의 가장 위험한 오독이 "계약에 있으니 있겠지"다). ② **`project_id` 강제 존치** — 소유권은 그 위에 얹히는 두 번째 경계이지 대체가 아니다. ③ CORS 조항의 근거는 **사라지는 게 아니라 강해진다**. ④ §Product Shell의 "보관/삭제 정책 미확정"을 **보관은 확정 / 영구 삭제만 미확정**으로 분리 — 독립 검증이 짚었듯 이건 §Core SOT("보관 확정")와 §Product Shell("미확정")이 **같은 정본 안에서 충돌하던 기존 부채**였고 이번에 함께 해소됐다. **설계 결정 D1~D8은 승인 대기**(`plans/multi-user-auth-cms-decisions.md`): 별도 서버 여부(추천 A=내부 모듈 — 분리해도 인가는 application에 남는다)·인증 방식(추천 A=세션 쿠키 — JWT는 즉시 무효화가 안 돼 관리자 정지/삭제와 충돌)·소유권 모델·기존 데이터 귀속·삭제 의미·관리자 범위·인가 시행 지점·슬라이스 순서. 인가 대상 규모 **61 operation / 51 path**(`/health` 제외; 전체 62/52). Mongo·ES 무인증은 v1.6.53이 스스로 "인증 slice 선행 필요"라 적어 둔 항목이라 함께 온다. **독립 검증 합격(조건 없음)** 후 비차단 2건 반영(규모 숫자의 `/health` 포함/제외 통일 · CORS credentials와 CSRF를 다른 축으로 정정). 코드·회귀 무변. | [work log](docs/daily_logs/2026-07-26/work_log.md) · [brief](docs/plans/multi-user-auth-cms-decisions.md) · [verification](docs/verifications/2026-07-26/multi_user_d0_contract_transition.md) |
| 2026-07-26 | **관측 KPI 대시보드 첫 화면(프론트 전용, SoT bump 없음, 오너 결정 D1=C·D2=A·D3=A)**. 증분 5가 연 `GET …/observability/kpi`를 소비하는 `/projects/:id/observability` route 신설 — 요약 카드 + 호출부별 스택 막대·토큰 막대 + 전 컬럼 표. **화면의 진짜 일은 숫자를 보여주는 게 아니라 오독을 막는 것**이라, 계약(SoT v1.7.48)이 정한 방어 3종을 화면이 물려받고 각각 회귀로 잠갔다: 게이트 점수 `null`은 **"측정된 호출 없음"**(0점 아님) · 루프 미수렴율 `null`은 **"루프 감사가 꺼져 있음"**(발산 없음 아님, opt-in이라 기본 배포의 정상 상태) · "여러 번 호출된 워크플로"는 **재시도 수가 아님**(작성 루프는 게이트를 설계상 최대 3회 부른다). 정렬·반올림은 API가 보장하므로 화면이 다시 하지 않는다(서버 순서 보존을 회귀로 고정). **오너가 추천(숫자 표만)과 달리 차트 라이브러리 도입을 선택**해 `recharts@3.10.1`을 넣었고, 실측에서 진입 번들이 **399.03 → 786.13 kB**로 뛰어(Vite 경고) 관측 화면만 `React.lazy`로 **코드 분할** → 진입 **401.19 kB**(+2.16) + 차트 청크 385.67 kB는 그 화면을 열 때만 로드. 결정은 그대로 출하하고 **비용만 지역화**했다. 차트 색은 눈이 아니라 검증기로 골랐다 — 직관적인 초록(성공)+호박(거부)이 **적록색약에서 ΔE 2.4로 붕괴**해 탈락, 최종 팔레트는 명도대·채도·전 쌍 CVD·정상시·대비 전 항목 PASS(색만으로 식별하게 두지 않음: 범례+표 병기). 백엔드·계약 무변(`gen:api` no diff), 프론트 tsc 0 · **vitest 207 passed / 14 files**. **독립 검증 합격(차단 0건)** 후 비차단 3건 반영 — 루프 비율 역산 제거(API가 한 나눗셈을 화면이 다시 하지 않는다) · 빈 상태 요약 중복 제거(단 **루프 감사만 있는 경우는 유지**, per-call 계측 이전 기록이 있을 수 있어 over-strict 회귀로 잠금) · 최저 대비 3.55→4.14 보강(호박만 어둡게 하면 적갈과의 분리가 무너져 실패 색을 함께 옮겼다). | [work log](docs/daily_logs/2026-07-26/work_log.md) · [brief](docs/plans/observability-dashboard-decisions.md) · `frontend/src/observability/` |
| 2026-07-26 | **LLM 파이프라인 관측(KPI) 페이즈 완료 — 계측부터 read-out까지(SoT v1.7.41~v1.7.48)**. 오너 지시(2026-07-24) "실제 운영 단계에서 LLM 성능·검색·작성이 제대로 진행되는지 KPI를 잡아 로그화"에서 시작해 **모든 LLM 호출부가 공통 레코드 1건을 남기고, 그 위 집계를 API 하나로 읽는 것**까지 닫았다. QUAL-1(제품 품질·수기·dogfood 종속)과 **별개 트랙**임을 오너가 명시했다. **결정 브리프 3개**가 이 페이즈를 끌었다 — 저장 구조·파생점수·범위·노출(D1=B·D2=C·D3=A·D4=A, `observability-kpi-decisions.md`), **계측 seam**(`observability-instrumentation-seam-decisions.md`, C안=provider 데코레이터), **site 매핑·scope 범위**(`observability-site-mapping-decisions.md`, D1~D4). **핵심 설계**: 계측 지점을 provider 데코레이터로 잡아 **`generate()` 1회 = 레코드 1건**이 구조적으로 보장된다 — endpoint 레벨 계측은 gate에서만 성립했고(나머지는 1요청=N호출) 특히 **repair 횟수는 오너가 관측 대상으로 지목한 지표라 뭉개면 얻을 수 없기** 때문이다. 워크플로 귀속은 `contextvars`가 나르고, 도메인만 아는 판정(gate decision·파생점수·최종 거부)은 flush 전에 얹는다. **계약이 방어하는 오독 3종**(전부 응답이 분모를 함께 실어 막는다): 토큰 집계는 `success`+`parse_error`만(`provider_error`의 0은 "모른다") · **표본이 0이면 비율은 `null`이지 `0.0`이 아니다** · `multi_call_correlations`는 repair 수가 아니다(loop은 gate를 설계상 최대 3회 부른다). **결과**: `call_site` 8종 전 호출부 계측 + scope 개방 9경로 + async worker, `GET /projects/{id}/observability/kpi`(H3 선언·전수 가드). 회귀 1471→**1554 passed** / 579→**610 subtests**, 공개 계약은 마지막 슬라이스에서만 변경(`schema.d.ts` +128줄, 프론트 build 399.03 kB 무변). **독립 검증 4회 전부 합격**(증분 4·seam·증분 C·증분 5), 비차단 hardening은 모두 반영. 다음은 **대시보드 페이즈**(오너가 분리) — 입력은 이 API 하나다. | [work log](docs/daily_logs/2026-07-26/work_log.md) · [brief](docs/plans/observability-kpi-decisions.md) · [verification](docs/verifications/2026-07-26/increment_5_kpi_readout.md) · `services/application/app/observability/` |
| 2026-07-24 | **`auto_promote_job` 부분 실패 의미론 확정 — 503 partial envelope(SoT v1.7.35, 오너 결정 D1=B·D2=A)**. H3 에러 계약 페이즈가 남긴 마지막 미매핑 500 부채를 닫는다. 승격 루프가 모든 `try` 밖이라 저장소가 N번째 candidate에서 실패하면 **앞의 N-1건이 이미 영속된 채** opaque 500이 나갔고, 무엇을 반환할지가 코드 매핑이 아니라 계약 질문이라 결정 브리프를 거쳤다. **오너 결정 D2=A — 503에 세 번째 얼굴(정본 저장소 장애)을 신설**한다: 502는 상류(LLM·검색·임베딩) 실패를 뜻하도록 설계됐고 정본 저장소는 상류가 아니라 저장 계층이라, 둘을 같은 코드에 넣으면 운영상 완전히 다른 두 상황("AI가 이상하다" vs "DB가 죽었다")이 구분 불가가 된다. 계약된 500 신설은 H3가 500을 "선언되지 않은 누수"로 규정해 온 가드를 약화시키므로 각하했다. **오너 결정 D1=B — 부분 성공 봉투**(`{auto_promotion_threshold, promoted[], promotion_error}`)를 503과 함께 반환한다: canonical mint는 append-only라 롤백되지 않으므로, 실패를 bare error 본문으로만 알리면 응답이 실제 저장 상태와 어긋난다. 승격 idempotency 덕에 **저장소 복구 후 같은 요청 재호출이 정확한 복구**이며 남은 candidate만 승격된다. 이로써 균일 `{detail}` 에러 본문의 Union 예외 지점이 5곳 → 6곳이 되고(에러 arm은 여전히 단일 모델), 새 Union이 drift로 생기지 못하도록 analysis 트랙에도 over-strict 가드를 신설했다. **오너 요청 독립 검증이 첫 구현의 결손을 잡아 같은 날 폐쇄했다(v1.7.36)**: 승격은 정본 mint와 재색인 outbox enqueue **두 번의 write**이고 한 transaction이 아닌데, 첫 구현이 "실패는 항상 mint 이전"을 전제해 **enqueue만 실패하면 이미 저장된 memory가 `promoted[]`에서 누락**됐다 — 위 계약이 그 모드에서 거짓이었다. 이제 mint 성공을 결과와 함께 실어 올려 봉투에 포함하고, `promotion_error`가 **어느 단계에서 실패했는지** 명시한다. **세 번째 지적(F4)도 오너 결정으로 같은 날 닫았다(v1.7.37)**: mint 이후 유실된 재색인이 재호출로 회복되지 않던 것은 v1.6.46이 idempotent replay를 enqueue에서 면제했기 때문인데, 그 면제가 canonical memory를 **영구 비색인**으로 남기는 유일한 경로였다. 이제 replay도 재enqueue해 **재색인 choke point가 무조건**이 되고(Phase 2B.5 D3=B가 세우려던 불변식), 503이 안내하는 재시도 한 번이 승격과 색인을 모두 복구한다. 비용은 유계다 — outbox가 PENDING/RUNNING 항목에만 dedup하므로 pending 중 replay는 no-op이고 drain 이후 replay만 재색인을 한 번 더 돌린다(upsert라 낭비일 뿐 오염 아님). 다른 endpoint의 저장소 장애는 **여전히 500으로 새며**, 전 저장소 예외 taxonomy는 별도 슬라이스로 남는다(정본이 그 슬라이스가 따를 의미론만 미리 고정). | [work log](docs/daily_logs/2026-07-24/work_log.md) · [결정 브리프](docs/plans/auto-promote-partial-failure-decisions.md) · `services/application/app/main.py` · `tests/test_memory_api.py` |
| 2026-07-23 | **테스트 Mongo 고정 — 전용 포트 단일노드 replica set을 compose로 승격(오너 결정 "전용 포트로 고정")**. 백엔드 스위트의 `41 skipped`가 매 세션 재발하던 원인은 **축이 둘인데 포트 고정이 하나만 고쳤기 때문**이었다: 27018(`agent-memory-mongodb`)은 **인증 축**을 풀지만 **standalone**이라 트랜잭션 미지원 → `test_core_sot_mongo`/`test_analysis_mongo` **40건이 "needs a replica set"으로 skip**. 종전 RS 절차(27017 + `/etc/hosts`)는 호스트 27017을 `shared-mongo`가 점유해 막혀 있었다. 신규 **`docker-compose.test.yml`**의 `test-mongo`가 전용 포트 **27020**(`TEST_MONGO_PORT`)에 RS `rs-test`를 띄우고 멤버를 **`localhost:27020`으로 광고**해 `/etc/hosts`도 27017도 필요 없앤다(볼륨 없음 → 잔여물 0, 배포 `mongo_data`와 충돌 불가; 배포 오버레이가 아니라 독립 파일이라 실수 기동 불가). healthcheck는 `rs.initiate`를 멱등 수행하되 **writable PRIMARY일 때만** healthy를 보고한다(initiate는 선거 전에 ok=1을 돌려줘 write가 `NotWritablePrimary`로 깨지는 창이 있다). **테스트 기본 URI를 `mongodb://localhost:27020/?replicaSet=rs-test`로 변경**(4개 `*_mongo.py`) — env 변수를 기억할 필요가 없어진 것이 고정의 본질이며, 잊어도 잘못된 green이 아니라 skip이다. **함께 발견·폐쇄한 결손**: Docker 기본 soft `nofile` 1024로는 스위트의 throwaway DB 대량 생성에 WiredTiger가 EMFILE에 닿아 `WT_PANIC … Too many open files` → fatal assertion으로 **mongod가 실행 중 사망**한다(실측 19 failed/1 error) — 증상이 skip이 아니라 failure라 코드 회귀로 오인되는 것이 위험. `ulimits.nofile 64000` 명시로 폐쇄(mongod 기동 경고 0). **결과: env 없이 `pytest -q`만으로 1419 passed / 1 skipped / 0 failed / 384 subtests**(종전 대비 +40 passed; 남은 skip 1건은 Chroma live = `chromadb` 미설치). 미기동 시 실패가 아니라 skip임도 양방향 확인(61 skipped/0 failed). 배포 `docker-compose.yml` 무변. **추적 부채**: 배포 `mongo` 서비스도 `ulimits` 부재라 같은 크래시 조건을 공유한다(오너 판단 — 스택 재기동 필요). | [work log](docs/daily_logs/2026-07-23/work_log.md) · `docker-compose.test.yml` · `tests/test_core_sot_mongo.py`·`test_analysis_mongo.py`·`test_memory_mongo.py`·`test_indexing_mongo.py` |
| 2026-07-23 | SoT v1.7.30: **H3 에러 응답 계약 S2 — CRUD family 20 endpoint 에러 선언(D2=A)**. S1의 전역 의미론을 첫 endpoint 집합에 적용했다. **H1 spine 14 + CRUD 형제 6 = 20**의 realistic 에러 상태를 `responses=`로 선언해, 런타임에는 정확히 반환되지만 공개 계약에는 없던 404·409·400·503이 OpenAPI·프론트 생성 타입에 나타난다(선언 18곳 — `POST /projects`·`GET /projects`는 도메인 에러 없음). 공유 상수 7종으로 조립하고 전부 단일 `ErrorDetailResponse`를 참조한다(D1=A). **CRUD family의 503은 전부 데이터 무결성 얼굴이라 description에 `scripts/migrate_ordered_units.py` 실행이 해결책임을 명시** — 상태코드만 적으면 이 페이즈가 없애려는 "로그에서 유추" 갭이 남는다. **런타임 0줄 변경**(상태코드·detail·분기·프론트 UX 무변, D4=A). 회귀 신규 7(+53 subtest): 선언 집합 exact lock(under/over-strict 양방향)·에러 본문 단일 모델 참조·migration 503 description·**실제 wire 본문 `{detail}` 단일 키**(후속 `reason`은 drift 아닌 명시 결정으로만). mutation 3종 실증. backend **1379 passed/41 skipped/384 subtests**(27018 URI), frontend **194/13**(무변), `gen:api` **+270/-0 순수 additive**, tsc clean, build JS 399.03 kB. 다음은 S3(analysis). LLM 미사용. **오너 독립 검증 PASS(조건 없음)** — 검증자가 18 endpoint 선언 == 실제 raise 집합을 endpoint 본문 직독으로 양방향 재확인했고, 비차단 H-1~H-3(문서 오기·reorder 비대칭 근거 정밀화·migration description 3번째 pin)을 반영했다. | [verification](docs/verifications/2026-07-23/h3_error_response_contract_s1_s2.md) (PASS) · [brief](docs/plans/api-error-response-contract-decisions.md) (D1~D4=A) · [work log](docs/daily_logs/2026-07-23/work_log.md) · `services/application/app/main.py` · `tests/test_application_api.py::CrudErrorContractDeclarationTest`·`CrudErrorBodyExactKeyTest` |
| 2026-07-23 | SoT v1.7.29: **H3 에러 응답 계약 S1 — SoT 전역 HTTP 에러 계약 섹션 신설(D1=A/D3=A, 문서 전용)**. H1 성공 `response_model`(v1.6.95)·H2 입력 검증이 남긴 **에러 응답**의 전역 의미론을 정본화했다. 계기는 07-22 dogfood의 레거시-데이터 500→503 수정 검증 H-2("503이 새 public 상태코드인데 정본·OpenAPI 어디에도 없다") — **오너 판단으로 부채가 아니라 페이즈로** 처리한다. 에러 본문은 균일 `{"detail": <string>}`이고 계약은 **3층**(상태코드=기계용 의미론 · `detail`=사람용 메시지 · 후속 `reason`=기계용 코드)이며 **`detail` 문자열 자체는 계약이 아니다**(클라이언트 패턴 매칭 금지, 기계 분기가 필요하면 `reason`을 additive로 — D1=B의 자리를 이 구조가 연다). 400/404/409/422/502/503/504 + 동적 `ProviderError` 의미론을 표로 고정하되 **endpoint별 코드 집합은 OpenAPI가 기계적 진실**이고 SoT는 endpoint×코드 표를 중복 유지하지 않는다(drift 방지). **503의 두 얼굴**(협력자 미구성 15 vs `DraftOrderIntegrityError` 3 = 마이그레이션 필요)과 **`InvalidDraftOrder` 의도적 비대칭**(reorder 409 유지), **알려진 결손 `start_next_unit` 500 누수**(S5에서 폐쇄)를 함께 명문화했다. 런타임·코드·OpenAPI 무변. 수치는 브리프 인용이 아니라 `main.py`에서 재도출했고 **503 분해(15+3)는 브리프에 없던 축이라 이번에 추가**했다. **오너 독립 검증 PASS(조건 없음)**, 비차단 H-2로 reorder 비대칭 근거 문안 정밀화(행동·선언 무변). | [brief](docs/plans/api-error-response-contract-decisions.md) (D1~D4=A) · [verification](docs/verifications/2026-07-22/h3_error_response_contract_plan.md) (조건부 합격 F1~F3 반영) · [work log](docs/daily_logs/2026-07-23/work_log.md) · [SoT v1.7.29](docs/system-contract-sot.md) |
| 2026-07-22 | **실 12B 풀스택 e2e 관통(오너 테스트 머신) + 라이브 결손 2건 폐쇄**. HANDOFF 잔여 후속이던 "실 12B medium/long 생성→worker→패드 오너 관통"을 브라우저 동등 경로(`:5173/api`)로 닫았다. 전 구간 PASS — 집필 루프(brief→원고→동기 short 200→Gate `pass`→accept 200→version 2), 분석(accept가 심은 job과 동일 ID 수렴→run succeeded candidate 3, 중복 0)→검토함(confirm→memory 승격, inbox 3→2), 비동기 패드(medium/long 202→worker claim→실 12B 실행→scratch에 `version_id` 보존 append), **retry(v1.7.28) 라이브 검증**(FAILED→pending→자동 재claim→succeeded 41s; succeeded에 retry→409), export(verbatim + manifest). **v1.7.23 `aspect`도 라이브 최초 확증**(실 12B가 `"aspect":"trait"`/`"voice"` 추출). **결손 1 — prompt template 본문 변경으로 기존 배포 기동 불가(오너 결정 A=v4 신설)**: v1.7.23이 `analysis_extract_v3`의 version은 그대로 둔 채 본문에 aspect 1줄을 추가해, 기존 Mongo를 가진 배포에서 `seed_template()`이 `PromptTemplateConflict`를 던지고 그 호출이 `create_app()` 안이라 **application·worker·generation_worker가 전부 기동 실패**했다(sandbox는 항상 빈 DB라 CI가 잡을 수 없는 축). v3를 immutable 구본문으로 복원(배포 DB와 sha256 byte-identical 확인)하고 aspect 본문을 `analysis_extract_v4`로 승격 — 선례(v1·초기 v2 보존)와 일치하며 과거 candidate 61건의 `prompt_version` 추적성이 보존된다. 재발 방지선은 **출시 본문 sha256 핀**(고치면 실패하고, 해시 갱신이 아니라 새 버전을 만들라고 지시) + 레거시 저장소 재시딩 회귀. **결손 2 — self-report 출력 상한 1024 고정 → `invalid_report` truncation(오너 위임 판단=6144)**: v1.7.22가 산문만 프리셋화(1024/2048/4096)하고 그 산문을 요약하는 report 상한은 1024 고정(compose 미선언)이라 report JSON이 잘렸다. 실패 지점이 입력 길이와 무관하게 항상 같은 구간(char 2199/2267/2288/2344)이고 상한을 올리면 truncation 0건으로 확증. 231자 짧은 산문에서도 재현 → **동기 short 포함 전 경로**. 기본값 6144 + compose 양쪽 명시. 오너 제시 7~8000에서 하향한 근거 = `max_tokens`는 VRAM 선점이 아니라 프롬프트와 llama 슬롯 ctx(8192)를 나눠 쓰는 예산이라 8000은 서버가 클램프해 truncation이 재발하며, ctx 확대는 VRAM(실측 여유 2.9GB/12GB, 하한 8GB 머신) 제약. 회귀는 숫자가 아니라 **결합**을 잠근다(`WRITING_OUTPUT_LENGTH_LONG`만 올리면 실패). backend **1333 passed/76 skipped/331 subtests**, 두 결손 모두 mutation으로 회귀 물림 확인. | [work log](docs/daily_logs/2026-07-22/work_log.md) · `services/application/app/analysis/prompt_templates.py`·`main.py` · `docker-compose.yml` · `tests/test_prompt_templates.py`·`test_writing.py::WritingReportBudgetTest` |
| 2026-07-22 | SoT v1.7.28: **비동기 생성 job 재시도 endpoint + 결과 패드 "다시 시도" 버튼(async-pad D4=A 재시도 UI 슬라이스)**. 증분 2b에서 "재시도 UI 슬라이스로 지연"했던 `FAILED→PENDING` 전이를 그 전이를 구동하는 endpoint·프론트 버튼과 함께 구현(dead 분기 방지). 신규 `POST .../writing/generation-jobs/{job_id}/retry`(200 + `WritingGenerationJobPayload`)가 **FAILED job을 PENDING으로 되돌려** failure_reason/detail·claim lease를 지우고, 생성 worker의 claim 루프가 이를 다시 집어 재실행한다. **Analysis retry 미러이되 별도 `run` 호출 없음** — 생성 worker는 PENDING을 자동 claim하므로 retry만으로 재실행. **FAILED만 재시도 가능**, 그 외(pending/running/succeeded)는 409, 미발견·타 프로젝트는 404. `generation_job.py` `_ALLOWED_TRANSITIONS`에 `(FAILED,PENDING)` + `mark_pending_for_retry`(failure/claimed_at clear). **함정**: `InvalidJobStateTransition`이 analysis·writing 두 모듈에 각각 존재 → retry endpoint는 writing 것을 별칭 import해 catch(안 하면 500). 프론트 `client.retryGenerationJob`·훅 `retry`(job을 pending으로 되돌려 폴링 재개, 요청 실패 시 failed 유지)·GenerationPad "다시 시도" 버튼. 브리프 불필요(선례+지연-노트 폐쇄). backend **1322 passed/73 skipped/328 subtests**(신규 10: 서비스 5+mongo 1+endpoint 4), frontend **193 passed/13 files**(신규 4 + 오너 독립 검증 후 보강 1: DraftEditor 통합 retry e2e), `gen:api` retry path 1개 additive(49줄, 0 삭제), tsc clean, build 103 modules(JS 398.69 kB). LLM 미사용. **오너 독립 검증 PASS** — `docs/verifications/2026-07-22/retry_slice_d4_generation_job.md`(boundary matrix 전 cell·이름충돌 함정 실재+해결+잠금·리터럴 일치; 비차단 보강 e2e까지 추가). **mongo 환경 메모**: `test_memory_mongo` 4건 실패는 본 슬라이스 무관 환경(27017 `shared-mongo` 인증 필수 → write `Unauthorized code 13`); 메모리 전용 27018(`agent-memory-mongodb`)로 백엔드 전체를 돌리면 **1358 passed / 41 skipped / 0 failed / 328 subtests**(오너 판단 "별도 컨테이너라 스킵/실패 없어야 맞다" 정확). | [brief](docs/plans/async-generation-pad-decisions.md) (D4=A) · [verification](docs/verifications/2026-07-22/retry_slice_d4_generation_job.md) (PASS) · [work log](docs/daily_logs/2026-07-22/work_log.md) · `services/application/app/writing/generation_job.py`·`main.py` · `frontend/src/writing/{useGenerationJobs,GenerationPad}.tsx`·`api/client.ts`·`drafts/DraftEditor.test.tsx` · `tests/test_writing_generation_job.py::RetryTest`·`test_writing.py::WritingGenerationJobRetryTest` |
| 2026-07-22 | **accept 후 미저장 편집 소실 결손 수정(프론트 전용, SoT bump 없음)**. generate는 clean+latest에서만 되지만 후보가 뜬 뒤 편집기에 입력하면(dirty) accept는 frozen `base_version_id`+후보 텍스트로 새 version을 저장하고 성공 후 `reloadLatest()`가 편집기를 새 latest로 덮어써 **미저장 입력이 조용히 소실**됐다(비동기와 무관한 별개 결손, async-pad 브리프 Deferred에 명시). 앱이 dirty 파괴적 동작을 전부 `window.confirm`으로 가드하는 관용구(이동/version/근거)를 accept에도 적용 — `WritingPanel.accept()` 초입(네트워크·키 민팅 전)에 `dirty` 시 confirm, 취소면 중단. **차단이 아니라 confirm**인 이유: accept의 후보 base는 이미 frozen이라 accept 자체는 유효하고 문제는 덮어쓰기뿐(차단하면 저장 강요→base stale→409→재생성 dead-end). 브리프 불필요(선례 관용구 적용). frontend **188 passed/13 files**(183→188: WritingPanel 3[취소 미발화=under-strict·confirm 진행·clean 미표시=over-strict]+DraftEditor 통합 2[취소 시 편집 텍스트 보존+version 미저장 / confirm 시 accept 저장+편집기 새 latest 재로드]), `tsc` clean, build 103 modules(JS 398.16 kB). 백엔드/OpenAPI 무변. 오너 독립 검증 PASS(차단 없음) 후 비차단 보강(proceed 경로 통합 테스트) 반영→188. LLM 미사용. | [brief](docs/plans/async-generation-pad-decisions.md) (Deferred 절) · [verification](docs/verifications/2026-07-22/accept_dirty_guard_unsaved_edits.md) (PASS) · [work log](docs/daily_logs/2026-07-22/work_log.md) · `frontend/src/writing/WritingPanel.tsx` · `frontend/src/writing/WritingPanel.test.tsx`·`drafts/DraftEditor.test.tsx` |
| 2026-07-22 | **비동기 생성 + 결과 패드 증분 3 — 읽기 전용 패드 + 완료 배지 + 5초 폴링(D6=A, 프론트 전용, SoT bump 없음)**. 2c가 개통한 async 경로(medium/long → `202` job)의 결과를 보여주는 UI를 채워 **비동기 슬라이스 전체(증분 1~3)를 종료**한다. 신규 폴링 훅 `useGenerationJobs`가 세션이 시작한 job을 in-memory 추적하고 **active(pending/running)가 있을 때만** 5초 간격으로 `GET .../generation-jobs/{job_id}`를 폴링한다(D6=A "생성 중일 때만"). terminal 전이 시 `settledUnseen`↑ + `onSettled` → succeeded는 추적 제거(결과는 worker가 scratch에 append), failed는 패드에 남겨 노출("실패 조용함 없음"); 폴링 fetch 실패는 active 유지 후 재시도. 신규 `GenerationPad`가 진행 중/실패를 렌더하고, 완료 결과는 기존 `ScratchRecovery`(= scratch 목록)가 담당한다(D1=A: async 결과와 복구분은 같은 저장소). `DraftEditor`가 훅을 **편집기 레벨**에서 호출(탭 전환에 살아남고 탭 배지 구동) — 이어쓰기 탭에 완료 배지(unseen>0), 탭 활성 시 acknowledge로 소거, settle 시 scratch 재조회로 결과 표시. `WritingPanel`에 신규 optional prop `onAsyncJobStarted`가 async 분기에서 job을 부모에 넘긴다. `client.ts`에 `getGenerationJob`. **백엔드/OpenAPI 무변**(기존 엔드포인트만 소비)이라 계약 리터럴 변경 없어 SoT bump 없음(§264 전방 마커가 이미 이 read를 서술). frontend **183 passed/13 files**(163→180 구현 + 오너 독립 검증 PASS 후 비차단 hardening 3건[H-1 5초 주기 리터럴 핀·H-2 idle 가드 setInterval mutation-pin·H-3 다중 job 동시 폴링]→180→183, 두 방향 잠금), `tsc` clean, build 103 modules(JS 397.98 kB), `gen:api` byte-identical. LLM 미사용. | [brief](docs/plans/async-generation-pad-decisions.md) (D6=A) · [verification](docs/verifications/2026-07-22/increment3_d6_generation_pad_polling.md) (PASS) · [work log](docs/daily_logs/2026-07-22/work_log.md) · `frontend/src/writing/useGenerationJobs.ts`·`GenerationPad.tsx`·`WritingPanel.tsx` · `frontend/src/drafts/DraftEditor.tsx` · `frontend/src/api/client.ts` |
| 2026-07-21 | SoT v1.7.27: **비동기 생성 + 결과 패드 증분 2c — generate endpoint 동기/비동기 분기(D5=A)**. 2b 저장소·worker에 이어 **생산자(endpoint) 배선**으로 async 경로 end-to-end 개통. `POST .../writing/generate`가 `output_length`로 분기 — **`short`(1024) 동기**(200 + WritingCandidatePayload, 무변), **`medium`(2048)/`long`(4096)은 job enqueue 후 `202 Accepted`로 `{job, idempotent_replay}` 즉시 반환**(비블로킹; worker가 claim해 실행). **async+`current_position` 없음→400**. 신규 `GET .../writing/generation-jobs/{job_id}`(200 + WritingGenerationJobPayload; 404 미발견·타 프로젝트)이 상태 read 담당. endpoint는 async 분기에서 writing/context_search/scratch 미사용(전부 worker 역할) + 동기 전용 503 우회. **상태코드 202=구현자 판단**: 200+response_model 정직성 유지 + `responses={202}` 문서화(revise-and-gate/accept 분기 메커니즘 동형) + HTTP 의미론; 200+JSONResponse(거짓)·200+Union oneOf(선례 없음) 기각. enqueue `(project_id, request_id)` 멱등. 신규 `WritingGenerationJobPayload`·`WritingGenerationJobAcceptedPayload`·`GENERATE_ASYNC_RESPONSES`. 프론트 `runGenerate`가 `"job" in produced` 가드로 async 감지 → "백그라운드 생성 시작" notice + candidate/Gate/루프 스킵(패드·5초 폴링은 증분 3); `generateWriting` 반환 `WritingCandidate | WritingGenerationJobAccepted`. SoT §271 forward-marker 현재형 전환 + §508 status 동기 한정. **adversarial self-verification(6 dimensional) PASS**: blocking 1건(async 503-우회 회귀 부재, mutation 입증)→`test_async_bypasses_sync_only_503_checks` 폐쇄(동 mutation re-fail 확인)+비차단 보강(context 미호출·멱등키 over-strict·§NNN 수정). backend **1311 passed/73 skipped/325 subtests**, frontend **163 passed/11 files**, tsc clean, build 101 modules, gen:api 신규 2 스키마+202 additive. LLM 미사용(단위). | [brief](docs/plans/async-generation-pad-decisions.md) (D5=A; 상태코드 202=구현자 판단) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `services/application/app/writing/http_models.py`·`main.py` · `frontend/src/writing/WritingPanel.tsx`·`api/client.ts` · `tests/test_writing.py::WritingGenerateAsyncBranchTest`·`WritingGenerationJobEnvelopeKeyTest` |
| 2026-07-21 | SoT v1.7.26: **비동기 생성 + 결과 패드 증분 2b — 생성 worker 실행 루프(D3=B)**. 2a 저장소에 이어 worker가 job을 claim해 실행하는 조각. **worker 최초 LLM/gateway 호출**. 신규 `writing/generation_worker.py`(`execute_generation_job` — 동기 generate와 같은 ContextSearch build→WritingService.generate 파이프라인 → 결과를 scratch에 `version_id`(D7) 포함 append → `mark_succeeded(result_scratch_id=)`; 예외는 taxonomy대로 `mark_failed`) + `scripts/generation_job_worker.py`(색인 worker 패턴 미러: graceful shutdown·`--loop`·JSON 이벤트) + compose `generation_worker` 서비스(application과 같은 gateway/context env, `stop_grace_period 180s`, `restart: unless-stopped`). **2a 검증 hardening 반영**: H-2 catch-all `internal` reason(매핑 안 된 infra/버그 → 종료 상태, RUNNING livelock 방지), H-3 저장 직전 `clear_accepted_item(request_id)`로 reclaim 시 scratch 중복 방지(2a per-request delete 재사용). worker 조립 seam = `main.build_async_generation_collaborators()`(create_app env 팩토리 재사용, create_app 무변). SoT §261 (2) 비동기 결과 보관 용도 확장(증분 1에서 지연) + `writing_generation_jobs`/worker 계약 clause. **endpoint 배선(D5)은 2c**라 async 경로 dormant(generate 여전히 전부 동기). **독립 검증 후 보강(H-1(2b))**: catch-all을 result-persist 단계(scratch 저장)까지 확장해 generate 성공 후 scratch 장애가 worker 루프 crash→비싼 generate reclaim 재실행 루프로 가지 않고 INTERNAL 종료. backend **1296 passed/73 skipped/326 subtests**, frontend·gen:api 무변. **실 12B 라이브 스모크 PASS(192.168.1.22:9080)**: gateway-backed `execute_generation_job`이 166자 실 한국어 산문 생성→job succeeded·scratch 1건·version_id 보존(context search는 stub, Phase 4에서 이미 라이브 검증). | [brief](docs/plans/async-generation-pad-decisions.md) (D3=B) · [verification](docs/verifications/2026-07-21/increment2b_d3_generation_worker.md) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `services/application/app/writing/generation_worker.py`·`generation_job.py` · `scripts/generation_job_worker.py` · `tests/test_writing_generation_worker.py`·`test_generation_job_worker.py` |
| 2026-07-21 | SoT v1.7.25: **비동기 생성 + 결과 패드 증분 1 — scratch tier 패드 준비(D2=A/D7)**. accept 정리를 draft 전체 삭제에서 **채택 항목 단위**로 축소(정본 저장 accept는 그 accept의 `request_id`와 일치하는 scratch 항목만 정리, 같은 draft의 다른 생성 결과는 보존→패드가 accept를 살아남음; 대응 없으면 no-op). 신규 `delete_for_request`/`clear_accepted_item`. scratch schema에 nullable `version_id` 추가(generate의 `current_position.version_id`에서 옴; `intent` 선례 동형 additive seam이라 마이그레이션 불요) — 패드가 "이 version 기준 생성"을 표시할 근거. 정본(`source_snapshots`/`draft_versions`)·명시 save·stale guard·accept 원자성은 **무변**(패드는 accept를 타지 않음). SoT §264 정리 granularity 항목 단위 명시(검증 H2)·§267 version_id seam. backend **1257 passed/73 skipped/326 subtests**, frontend **162/11**(`ScratchCandidate` hand-declared 타입에 version_id 미러), gen:api byte-identical(scratch endpoint `response_model` 없음). | [brief](docs/plans/async-generation-pad-decisions.md) (D2=A/D7) · [verification](docs/verifications/2026-07-21/increment1_d2_d7_scratch_pad_prep.md) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `services/application/app/writing/scratch.py`·`scratch_mongo.py`·`main.py` · `tests/test_writing_scratch.py`·`test_writing_scratch_mongo.py` |
| 2026-07-21 | SoT v1.7.24: **문체/분량 슬라이스 증분 3(2/2) — Gate style finding + 문체 우선순위(D5=A/D6=A)**. Writing Gate가 후보 산문을 저자 ProjectBrief 문체 설정(tone/style_rules/preferred_patterns/forbidden_patterns/style_examples)과 대조해 이탈 시 `style` finding을 낸다. **style은 advisory**: parse 경계에서 warning+needs_user_review로 고정, **decision 우선순위에서 제외**되어 findings가 style뿐이면 여전히 pass(경고이지 차단 아님). D6 근거 — 어투 이탈은 저자가 알아차리면 충분·의도적 변경 정당이라 차단/재생성 강요 금지, 그래서 escalate(accept 차단) 대신 decision 제외로 pass 유지·최종 채택은 사용자. style은 auto-revise 대상 아님(continuity 전용 잠금)·block 불가. D6 우선순위 `저자 설정 > canonical 관찰 > candidate 관찰`을 문체에 적용, Gate는 설정 기준만 판정, 관찰→설정 자동 반영 없음. 캐릭터 어투(D4 aspect) 설정 저장 deferred라 이번엔 프로젝트 문체 대조까지. backend **1250 passed/73 skipped/326 subtests**(신규 GateStyleFindingTest 5 + revise style ineligible 1), frontend **162/11**(WritingPanel +1), gen:api `WritingGateFindingType`에 style 1개 additive. **증분 3 전체(D4+D5+D6) 완료 → 문체/분량 슬라이스 종료.** LLM 미사용. | [brief](docs/plans/writing-style-and-length-control-decisions.md) (D5=A/D6=A) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `services/application/app/writing/gate.py`·`gate_prompt.py` · `tests/test_writing_gate.py::GateStyleFindingTest` |
| 2026-07-21 | SoT v1.7.23: **문체/분량 슬라이스 증분 3(1/2) — character_observation optional aspect(D4=B, 오너=optional)**. `character_observation` payload가 캐릭터 어투를 식별하는 optional `aspect`(자유 문자열, 예: voice/trait)를 담을 수 있다. 브리프 원 리터럴은 exact `(name,observation,aspect)` 필수였으나, candidate 생성 경로가 payload를 검증해 필수화 시 테스트 25파일+live candidate 무효화+마이그레이션이 필요하고 이 필드 가치(어투 검증)는 설정 저장 deferred라 forward-defense임을 근거로 **오너가 optional 선택**. validator가 required+optional 모델로 바뀌어 character에 optional aspect 허용(present 시 non-empty string, event/open_question 불허), 기존 `{name,observation}`는 마이그레이션 없이 유효. taxonomy 3종 무변(2A D5=A). 추출 프롬프트가 aspect를 optional로 안내하고, 저장 payload는 `dict(candidate.payload)` wholesale 직렬화라 review inbox/detail/conflict diff에 추가 코드 없이 surface(색인·Gate는 D5). backend **1245 passed/73 skipped/322 subtests**(신규 `CharacterAspectPayloadTest` 7), frontend/gen:api 무변. 증분 3의 D5(Gate style finding)·D6(우선순위 계약)는 다음. LLM 미사용. | [brief](docs/plans/writing-style-and-length-control-decisions.md) (D4=B, 오너=optional) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `services/application/app/analysis/schema.py` · `tests/test_analysis_extractor_schema.py::CharacterAspectPayloadTest` |
| 2026-07-21 | SoT v1.7.22: **문체/분량 슬라이스 증분 2 — 생성 분량 프리셋(D3=A)**. 출력 길이를 서버 전역 고정값에서 요청별 프리셋으로 승격했다. `POST .../writing/generate`에 신규 필드 **`output_length=short\|medium\|long`**(생략 시 `short`=pre-slice 단일값, 하위 호환)을 노출하고 **서버가 프리셋→토큰 매핑을 소유**한다(기본 **1024/2048/4096**, `WRITING_OUTPUT_LENGTH_SHORT\|MEDIUM\|LONG` env 조정·값이 1 미만이거나 정수 파싱 불가면 기동 실패, `short`는 기존 `WRITING_GENERATE_MAX_TOKENS` 승계 — 증분 1 style-example 상한 선례 동형). 알 수 없는 프리셋은 400. **출력 토큰 축은 입력 예산 `max_tokens`(5개 endpoint 공유)와 독립**이다. **`long`(4096≈91초)은 loop wall clock 60000ms 초과라 단일 generate 전용** — `/writing/revise-and-gate` 요청 필드가 아니고 프론트도 `long` 뒤 자동 루프를 스킵한다. loop 예산 무변. backend **1238 passed/73 skipped/320 subtests**(신규 `WritingOutputLengthPresetTest` 9), frontend **161/11**(WritingPanel +2), `gen:api` generate 요청에 `output_length` 1개 additive, build 101 modules. 오너 독립 검증 PASS(조건 없음) 후 비차단 hardening #1(short 경로 독립 잠금)·#2(비정수 env 기동 실패 SoT 명시) 반영. 증분 2 완료로 비동기 패드 D5 분기 기준 성립. 다음은 증분 3(D4+D5+D6). LLM 미사용. | [brief](docs/plans/writing-style-and-length-control-decisions.md) (D3=A) · [verification](docs/verifications/2026-07-21/increment2_d3_output_length_preset.md) · [work log](docs/daily_logs/2026-07-21/work_log.md) · `tests/test_writing.py::WritingOutputLengthPresetTest` · `services/application/app/writing/models.py` · `frontend/src/writing/WritingPanel.tsx` |
| 2026-07-20 | SoT v1.7.21: **문체/분량 슬라이스 증분 1 — ProjectBrief 문체 정본 통합(D1=A/D2=A), 독립 검증 B-1 closure 후 PASS**. 런타임에서 사용되지 않던 `WritingBrief`를 삭제하고 `style_rules`·`preferred_patterns`·`forbidden_patterns` 전부와 신규 자유 텍스트 `style_examples`를 append-only `ProjectBriefVersion` required key로 옮겼다. **오너 보완 결정 1=A/2=B**: 일부 필드를 버리지 않고 네 배열 모두 이전하며, 예시는 기본 최대 3개·항목당 1,000자이고 `PROJECT_BRIEF_STYLE_EXAMPLES_MAX_ITEMS`·`PROJECT_BRIEF_STYLE_EXAMPLE_MAX_CHARS`로 조정한다(1 미만 기동 실패). HTTP trim 후 blank·중복·상한 초과 422, Mongo 구문서는 빈 배열로 호환 read. ProjectOverview는 예시별 textarea로 내부 줄바꿈을 보존한다. W0 schema/OpenAPI/ContextPackage prompt를 동형 갱신. 검증 B-1(read 상한 무관 회귀 부재)은 env 1/1에서도 기존 current/history/detail read 200+3개 보존 회귀와 SoT/W0 PB-13 clause로 닫았고 H1~H4도 보강했다. backend **1226 passed/76 skipped/313 subtests**, frontend **159/11**, build 101 modules. 다음은 D3 출력 분량 프리셋. LLM 미사용. | [brief](docs/plans/writing-style-and-length-control-decisions.md) · [verification](docs/verifications/2026-07-20/project_brief_style_integration.md) · [work log](docs/daily_logs/2026-07-20/work_log.md) · `tests/test_project_brief.py` · `frontend/src/projects/ProjectOverview.tsx` |
| 2026-07-20 | SoT v1.7.20: **미채택 candidate 복구 안전망(scratch) 보존/만료 정책 정본 승격** — 구현자 잠정값 → 오너 승인 정본 계약. **승격 대상은 "상한 20"이라는 숫자가 아니라 "기본 20 + 운영자 조정 가능"이라는 계약**이다(오너: "실제로 어떻게 될지는 사람에 따라 달라서" — 숫자를 상수로 굳히면 dogfood에서 승격 근거를 만드는 관찰 자체가 코드 수정을 요구한다). SoT **Source of Truth** 절에 `writing_drafts_scratch`가 **Core SOT 외부·정본 아님·복구 전용**임을 명시하고, 키 규칙·상한(기본 20/env 조정/1 미만 기동 실패)·정리 분기(저장된 accept만 = 200 + 502 partial, 비-PASS 미정리)·시간 만료 없음·best-effort 격리·schema seam(`intent` nullable, `next_unit` 제외)을 계약화했다. Phase 5 절에 "accept 전 candidate는 소실되지 않는다"를 추가하되 "accept 전 canon 불변"과 충돌하지 않음을 명시(scratch는 정본이 아니라 복구 tier). 브리프는 `완료`로, HANDOFF ★는 오너 선택 대기로 전환. 코드 변경 없음(문서 승격). backend 1228/70/299·frontend 159/11 유지. | [SoT v1.7.20](docs/system-contract-sot.md) · [brief](docs/plans/unaccepted-candidate-persistence-decisions.md) · [verification](docs/verifications/2026-07-20/writing_scratch_recovery.md) · [work log](docs/daily_logs/2026-07-20/work_log.md) |
| 2026-07-20 | **미채택 Writing candidate 영속(복구 안전망) — 오너 결정 D0=B/D1=B/D2=A + 첫 구현**. `generate`가 만든 candidate는 accept 전까지 in-memory라 새로고침하면 소실됐다. 신규 Core-SOT-**외부** collection `writing_drafts_scratch`에 `(project_id, draft_id)`별 **미채택 이력**을 append하고, `GET/DELETE /projects/{id}/writing/scratch?draft_id=`로 복구/버리기를 제공한다. **오너 결정과 근거**: D0=B는 단순 기능 추가가 아니라 **"게이트 우선 → 게이트 ↔ UI/UX 동반 정합" 단계 전환**의 신호이며(따라서 SoT 변경이 잦아지는 구간), Phase 7 P1(D0=C)을 각하가 아니라 **향해 가는 방향으로 염두**에 둔 좁은 첫 걸음이다. D1은 브리프 추천(A: 최신 1건)이 아니라 **B(이력)** — "개발하면서 후속 정책까지 함께 만든다"는 이 구간의 성격이고 이력은 Phase 7 `conversation_turn`으로 자연 흡수되기 때문. D2=A는 D1=B가 서버 영속을 요구해 유일하게 정합적이라 확정. **보존/만료(per-draft 상한 20·accept 성공 시 즉시 삭제·시간 만료 없음)는 오너 위임에 따른 구현자 잠정값 → 오너 SoT 승격 대기**(정본 계약 아님). 프론트는 편집기 진입 시 복구 배너(복사/모두 버리기). **오너 독립 검증 조건부 합격 → blocking B-1(Mongo 어댑터 회귀 부재, 선례 관행 이탈) closure**: `tests/test_writing_scratch_mongo.py` 6건 추가(mutation 6/6 bite) + H-2(502 partial도 정리하도록 정합화, 세 분기 브리프 명시)·H-1/H-4(best-effort 격리)·H-3(`next_unit` 제외 근거) 보강. **per-draft 상한은 환경변수화**(오너: "실제로 어떻게 될지는 사람에 따라 달라서") — `WRITING_SCRATCH_MAX_PER_DRAFT`(기본 20, compose `${...:-20}`, `WRITING_LOOP_MAX_*` 선례와 동형), **1 미만은 기동 실패**(0/음수 허용 시 안전망이 지켜야 할 초안을 조용히 삭제하므로 조용한 손실 대신 시끄러운 실패). 이로써 **SoT 승격 대상은 "20"이라는 숫자가 아니라 "기본 20 + 운영자 조정 가능"이라는 계약**이 된다. backend **1228 passed/70 skipped/299 subtests**(+27), frontend **159/11**(+4), `gen:api`에 scratch path 2개 추가(additive). LLM 미사용. | [brief](docs/plans/unaccepted-candidate-persistence-decisions.md) · [work log](docs/daily_logs/2026-07-20/work_log.md) · [verification](docs/verifications/2026-07-20/writing_scratch_recovery.md) · `services/application/app/writing/scratch.py` · `frontend/src/writing/ScratchRecovery.tsx` |
| 2026-07-19 | SoT v1.7.19: **export UI 옵션화 — include_archived 토글 + manifest 토글** (frontend 슬라이스, backend 무변). DraftList에 "보관된 원고 포함"(archived draft 있을 때만, combined·bundle 양쪽 `include_archived`)과 "추적 정보(manifest) 함께" 체크박스 추가. manifest on이면 combined은 body+`{id}.manifest.json` 2파일, bundle은 zip에 `manifest.json` 추가(**bundle manifest를 always-on→opt-in으로 변경**). archived-only gating을 "숨김"→"버튼 disable+토글로 활성화"로 재정의. backend W4 계약 그대로 소비(신규 endpoint 없음). frontend **155/10**(+3), build JS 389.54 kB, `gen:api` byte-identical. LLM 미사용. | [work log](docs/daily_logs/2026-07-19/work_log.md) · `frontend/src/drafts/DraftList.tsx` · `frontend/src/drafts/DraftList.test.tsx` |
| 2026-07-19 | SoT v1.7.18: **프로젝트 export 프론트 UI + 회차별 개별 ZIP(bulk)** — frontend 소비 슬라이스, backend 무변. DraftList에 "전체 원고를 한 파일로"(TXT/MD, W4 export body 다운로드)와 **오너 결정으로 추가한 "회차별 개별 파일 (ZIP)"**(TXT/MD)를 배선했다. bulk는 `?manifest=true`로 unit 열거→각 unit을 기존 단일 version export로 verbatim 취득→`jszip`으로 `{NN}-{제목}.{ext}`+`manifest.json` 묶어 다운로드(신규 endpoint 없이 재사용). 동시 실행 가드·진행 중 disable·비보관 unit 없으면 미표시·보관 원고 제외. 오너 독립 검증 PASS(조건 없음) 뒤 H2/H3/H4(blob 본문·entry명 sanitize·archived-only gating) 보강. frontend **152/10**(zip 언팩 검증 포함), build JS 388.63 kB, `gen:api` byte-identical, `jszip@^3.10.1` 추가. LLM 미사용. | [work log](docs/daily_logs/2026-07-19/work_log.md) · [verification](docs/verifications/2026-07-19/w4_export_frontend_zip.md) · `frontend/src/drafts/DraftList.tsx` · `frontend/src/api/client.ts` |
| 2026-07-19 | SoT v1.7.17: **Writing Workspace V2 W4 — 프로젝트 전체 ordered-latest export(D6=A)**. 신규 `GET /projects/{id}/export?format=txt\|markdown&manifest=<bool>&include_archived=<bool>`가 project의 Draft를 `position` 순으로 각 unit의 latest version snapshot을 이어 붙인다. body는 단일 version export 선례대로 **AI metadata 미삽입 verbatim**이며 unit 경계에만 제목을 넣는다(오너 결정: **md=`# {title}`, txt=제목 줄**). archived는 **기본 제외+`include_archived=true` 옵트인**(오너 결정), version 없는 Draft는 skip. delivery manifest는 **요청 시에만**(오너 결정: 같은 endpoint `manifest=true`, 미요청 null) `{project_id,format,include_archived,units[]{draft_id,title,unit_kind,position,version_id,version_number,snapshot_id,content_hash}}`로 body와 동형 traceability를 기록한다. saved publication manifest는 계속 Deferred. W0 §4에 **EX-01~13** 양방향 회귀(fire 6/not-fire 7)+exact-key envelope, §6 export 계약 추가. backend **1198 passed/73 skipped**, frontend **146/10**, gen:api export 타입 추가·build OK. LLM 미사용. 오너 독립 검증 PASS(조건 없음) 뒤 §6.3 빈-body clause를 EX-13으로 보강. **W1~W4 전체 완료.** | [work log](docs/daily_logs/2026-07-19/work_log.md) · [verification](docs/verifications/2026-07-19/w4_project_export.md) · [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · `tests/test_core_sot.py::ProjectExportContractTest` |
| 2026-07-19 | SoT v1.7.16: **Writing Workspace V2 W3 증분 2 — explicit Writing intent + W3 전체 closure(WI-01~22, SC-01/02)**. `intent=append_current\|start_next_unit`을 generate/accept 계약에 구현했다. accept가 provider/write 전 intent/next_unit binding을 400으로 막고 replay를 stale base·Gate보다 먼저 한다. `start_next_unit`은 신규 Core SOT 6-surface 원자 write(position shift·새 Draft·version 1·snapshot·blocks·accept receipt)를 한 transaction으로 commit하고 실패 시 전부 rollback하며, append는 receipt 없는 save-key replay를 유지한다. 두 intent 모두 `analyze:{snapshot_id}` job으로 수렴하고 실패는 `502+accepted=true+intent+saved` partial이다. SC-01/02를 W3 OU·Writing intent fragment까지 W0 catalog와 동형 대조했다. 프론트에 채택 방식 선택+새 유닛 입력을 배선했다. Mongo 미기동 시 backend **1181 passed/73 skipped**, frontend **146/10**, gen:api byte-identical·build 96 modules. **live replica-set 실증**: `docker compose up -d mongo` 후 backend **1254 passed/4 skipped(Chroma+ES, W3 무관)**로 WI-11 six-surface transaction rollback 통과. | [work log](docs/daily_logs/2026-07-19/work_log.md) · [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · `tests/test_writing_accept.py` · `tests/test_core_sot_mongo.py` · `tests/test_project_brief.py` |
| 2026-07-19 | SoT v1.7.15: **W3 ordered-unit 독립 검증 B1 closure**. duplicate reorder가 422로 빠져 §2.2 exact 409를 어기고 OU-08이 이를 허용하던 조건부 합격 사유를 닫았다. OpenAPI `uniqueItems:true`는 유지하되 runtime 중복 판정을 Core SOT full-permutation 경계로 내려 missing/duplicate/foreign/unknown/set-change 모두 409/write 0으로 통일하고 regression을 exact 값으로 강화했다. migration ID 보존 직접 assertion과 프론트 exact Draft fixture·“정본 순서” 표기도 보강했다. 원 conditional 기록은 보존한다. | [verification](docs/verifications/2026-07-19/w3_ordered_unit.md) · [work log](docs/daily_logs/2026-07-19/work_log.md) · `tests/test_ordered_units.py` |
| 2026-07-19 | SoT v1.7.14: **Writing Workspace V2 W3 증분 1 — ordered unit 완료**. Draft에 archived-inclusive contiguous `unit_kind/position`, full-permutation reorder API, fail-closed one-shot migration과 전체 성공 후 unique index 설치를 구현했다. Mongo transaction 및 single-writer fallback before-image 복구를 실제 replica-set에서 검증했고, 프론트에는 단위 생성·순서/종류 표시·위/아래 reorder를 연결했다. OU-01~14 named matrix 전부 구현, backend **1159 passed/60 skipped**, live Mongo **37 passed/0 skipped**, frontend **144 passed/10 files**, fresh gen+build 96 modules. W3 Writing Intent(WI-01~22)는 다음 증분이다. | [work log](docs/daily_logs/2026-07-19/work_log.md) · [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · `tests/test_ordered_units.py` |
| 2026-07-19 | SoT v1.7.13: **Writing Workspace V2 W2 완료 + 독립 검증 PASS/hardening closure**. W0 exact ProjectBrief를 append-only Core SOT/Mongo + current-base/same-key replay/clear-history API 4개와 exact OpenAPI로 구현했다. Writing ContextPackage는 brief를 별도 authoritative item으로 소비한다. `/projects/:id/overview`는 optional onboarding·skip/edit·이력 보존 clear, canonical-only 인물/사건/미해결 질문, pending 별도 badge/link를 제공한다. 독립 검증 PASS 뒤 H1 OpenAPI `pattern:\S`, H2 no-brief service path, H3 collision→stale deterministic/live-Mongo test를 보강했다. 최신 backend **1144 passed/52 skipped**, frontend **143 passed/10 files**, build 96 modules. Docker/Node/live Mongo는 테스트 머신 인계, LLM 미사용. 다음은 W3다. | [verification](docs/verifications/2026-07-19/w2_project_brief_overview.md) · [work log](docs/daily_logs/2026-07-19/work_log.md) · [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · `frontend/src/projects/ProjectOverview.tsx` |
| 2026-07-18 | SoT v1.7.12: **W1 독립 검증 blocking closure**. 독립 검증은 핵심 W1을 확인했지만 cross-Draft source 이동이 dirty confirm 없이 SPA navigate해 미저장 본문을 잃는 B1과 회귀 empty cell B2~B6으로 조건부 합격했다. 다른 Draft source와 editor 내부 SPA Link 전체에 dirty 이탈 확인을 적용하고, 상태줄·reject·same/cross-Draft dirty guard·snapshot not found·quote/hash mismatch named 회귀를 추가했다. source notice도 edit/save/version switch/reload에서 clear해 stale 표시를 닫았다. 원 검증 verdict은 역사 보존. frontend **139 passed/9 files**, build 95 modules, backend/OpenAPI/W0 schema 무변. | [verification](docs/verifications/2026-07-18/w1_split_workspace.md) · [work log](docs/daily_logs/2026-07-18/work_log.md) · `frontend/src/drafts/DraftEditor.tsx` |
| 2026-07-18 | SoT v1.7.11: **Writing Workspace V2 W1 split workspace 완료**. 기존 DraftEditor를 editor+docked right rail(`이어쓰기|분석|검토`)로 재구성하고 좁은 화면은 같은 tab 정보구조의 단일 열로 유지한다. `panel/candidate/source` query로 tab·후보 detail·근거 선택을 복원하고, source `snapshot_id`를 project의 Draft/version에 대조해 exact version을 연 뒤 raw Unicode code-point offset을 textarea UTF-16 selection으로 변환한다. quote/hash 불일치는 차단하며 최신/과거 근거를 명시한다. 상태줄에 저장·분석·검토 대기를 분리하고 rail confirm/reject는 서버 어포던스를 그대로 소비한다. backend/OpenAPI/W2·W3 runtime은 무변. 프론트 **130 passed/9 files**, build 95 modules. 다음은 W2 ProjectBrief onboarding+canonical overview다. | [work log](docs/daily_logs/2026-07-18/work_log.md) · [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · `frontend/src/drafts/DraftEditor.tsx` · `frontend/src/review/WorkspaceReviewPanel.tsx` |
| 2026-07-18 | SoT v1.7.10: **Writing Workspace V2 W0 계약/migration 완료 + 독립 검증 blocking closure(runtime 구현 전)**. ProjectBrief append-only exact version/API, ordered Draft의 contiguous position·full reorder·legacy fail-closed migration, `append_current|start_next_unit` binding과 next-unit Core SOT 원자 write/accept receipt를 정본화했다. 독립 53-agent 검증은 구조 건전성을 확인했으나 empty cell 7개로 조건부 합격했고, 오너 지시에 따라 이를 모두 독립 named 행으로 닫아 matrix를 **50행(PB12+OU14+WI22+SC2)**으로 확장했다. schema fragment-only 소비/trim authority, migration exclusive window·index gate, archived slot UI, 원자성 표현, manifest 용어도 보강했다. 원 verification verdict은 closure 전 상태의 역사로 보존한다. runtime code/OpenAPI는 무변이며 다음은 W1 editor+docked right rail/source deep-link다. | [W0 contract](docs/plans/writing-workspace-v2-w0-contract.md) · [verification](docs/verifications/2026-07-18/w0_contract_migration.md) · [work log](docs/daily_logs/2026-07-18/work_log.md) |
| 2026-07-18 | SoT v1.7.9: **Writing Workspace V2 구조 방향 확정(문서/결정, 구현 대기)**. 오너 dogfood를 근거로 prompt-only 보완이 아닌 구조적 해결을 선택했다. 확정 조합은 D1=A 별도 `ProjectBrief`, D2=A 평면 ordered unit(`chapter|scene|other`+`position`), D3=C `append_current|start_next_unit`과 next-unit accept 원자 저장, D4=A editor+docked right rail, D5=A canonical-only overview, D6=A ordered-latest TXT/Markdown+manifest, 전체 C 세로 슬라이스다. W0 계약/migration → W1 split workspace → W2 brief/overview → W3 ordered unit/intent → W4 export 순서이며 runtime code/schema/API는 무변이다. | [work log](docs/daily_logs/2026-07-18/work_log.md) · [실검수 브리프](docs/live_review_briefs/2026-07-18/writing_workspace_ux_restructure.md) · [Product Shell](docs/plans/product-shell.md) |
| 2026-07-18 | SoT v1.7.8: **이어쓰기 accept 후 재분석 `source_ref not found` 예방·복구(오너 C, 독립 검증 PASS)**. 새 실검수 브리프 분류를 만들고 실제 실패 job/catalog/report를 근거로 계약화했다. immutable `analysis_extract_v3`가 exact candidate/full-anchor shape, advisory report→authoritative current catalog 순서, report ID를 제외한 repair catalog를 잠근다(v1·초기 v2 보존; strict validation·자동 remap 금지·repair 1회 무변). 명시 `POST .../analysis/jobs/{job_id}/retry`만 same-ID `failed→pending`+failure clear, 다른 상태 409; 프론트는 failed create replay를 retry→run하고 HTTP 200 non-succeeded를 성공/후보 0으로 오인하지 않는다. 원 실패 job 라이브 재검수에서 same ID succeeded·candidate 5, 재클릭 replay-only/ID 불변·중복 0. 검증 후 repair prompt의 구 payload-key 지칭을 교정하고 구 pointer 미노출 회귀를 보강했다. backend 1126/48/276, frontend 124/8, build·OpenAPI PASS. | [work log](docs/daily_logs/2026-07-18/work_log.md) · [실검수 브리프](docs/live_review_briefs/2026-07-18/analysis_retry_after_accept.md) · [verification](docs/verifications/2026-07-18/analysis_retry_v3_live.md) |
| 2026-07-18 | SoT v1.7.6: **테스트베드 사용가능화 슬라이스 A+B+C**(오너 브라우저 dogfood 발견 3건 대응). **A(502 견고화, 오너=UI 재시도+백엔드 2차 repair)**: 이어쓰기 502 "report field must be an array"는 간헐적 12B 비-배열 report가 원인 — `report.py` repair를 1→**2회**(`MAX_REPORT_REPAIRS=2`, 계약 D4=A "strict+1 repair"→"strict+최대 2 repair" 개정, usage 누적·strict schema·OpenAPI 무변) + 프론트 `describeWritingError`로 사람이 읽을 안내+"다시 생성" 재시도. **B(검토 트리거+진입)**: 신규 `AnalysisTrigger`("이 원고 분석")가 최신 snapshot을 분석해 needs_review candidate 생성→검토함으로 잇고 DraftEditor 헤더에 "검토함 →" 링크. 분석 추출은 **source_ref catalog 선행 필수**(없으면 `source_ref catalog is required` 400)라 `analyzeVersion`이 catalog가 비면 블록별 full-span source_ref를 먼저 생성(offset만 필요) 후 job create→run. **C(관측성)**: WritingPanel이 파이프라인을 coarse 단계(근거 검색·초안 생성→Gate→자동 개선)로 표시 + "근거 주장 N개" 요약. 백엔드 **1118 passed/48 skipped**, 프론트 **116 passed/8 files**(AnalysisTrigger 7·WritingPanel report retry 1), build 94 modules, `gen:api` diff 0. **라이브(실 12B, 브라우저 동등)**: generate 정상(claims 3)·analyzeVersion catalog 3→run 200→candidate 3→검토함 3(어제 400 해소). 차후(오너 보류): 멀티턴·스트리밍·2중 탭. | [work log](docs/daily_logs/2026-07-18/work_log.md) · `services/application/app/writing/report.py` · `frontend/src/review/AnalysisTrigger.tsx` · `frontend/src/writing/WritingPanel.tsx` |
| 2026-07-17 | SoT v1.7.5: **B Review Inbox 두 번째 슬라이스 — candidate edit + conflict merge/split**(첫 슬라이스가 남긴 두 무거운 표면, 어포던스 순수 소비). backend·schema·회귀 무변, `gen:api` diff 0. `client.ts`에 `editCandidate`(`POST .../candidates/{id}/edit`, `{payload}`)·`reconcileConflict`(`POST .../review-queue/{entry_id}/reconcile`, `{action}`) 추가(void). **candidate edit**: detail의 read-only payload `<dl>`를 "수정"으로 필드별 `<textarea>` 폼으로 토글(taxonomy 정확 키 집합·non-empty 문자열 in-place 편집, 서버 재검증 400·409). 빈 필드 시 저장 disabled(UX·서버 authority), 취소→복귀. **conflict merge/split**: conflict card에 어포던스 소비 버튼(merge=character+matched·split=character 자격은 **서버 선언**, 프론트 미재계산). 모든 write 성공→목록 재조회. edit 모드 중 confirm/reject·merge/split 숨김. 회귀 순 +9(추가 10·삭제 1='merge/split 미렌더' 제거. 양방향: 렌더·**merge/split eligible=false→disabled+reason**·각 reconcile endpoint+action body·edit endpoint+payload body·빈 필드 disabled·취소 복귀·edit 400 폼 유지·edit 모드 중 타 action 숨김). **mutation bite 2종 실증**: edit URL→`/confirm` 변이 시 edit 회귀 단독 bite·reconcile action `"merge"` 하드코딩 시 split 회귀 단독 bite(각 write endpoint pin, v1.7.4 검증 교훈). 전체 프론트 **108 passed/7 files**(review 26), build 93 modules, backend/scope diff 0. Review Inbox 핵심 검토 루프 완결. 다음 = 실 데이터 dogfood 관통(오너 풀스택)+트리거 검토. | [work log](docs/daily_logs/2026-07-17/work_log.md) · [brief](docs/plans/frontend-review-inbox-decisions.md) · `frontend/src/review/ReviewInboxDetail.tsx` |
| 2026-07-17 | SoT v1.7.4: **B Review Inbox 최소 action UI — 첫 B 슬라이스**(오너 D=B: 목록 + 근거 detail + 이진 action). Phase 6 어포던스 계약(v1.6.67 `{action,eligible,reason}`)을 **순수 소비**한다 — backend·OpenAPI schema·공개 payload·회귀 무변, `gen:api` diff 0. 신규 route `/projects/:id/review`(목록: candidate confirm/reject + gate finding resolve/dismiss)·`/review/:candidateId`(근거 detail: payload·원문 quote·conflict diff read-only + confirm/reject), DraftList에 진입 링크. **프론트는 자격을 재계산하지 않는다** — 버튼 `disabled`는 서버 `eligible`, tooltip은 `reason`(display text, pattern-match 금지)에서 렌더하고 이 슬라이스는 **이진 action 4종만** 배선한다(candidate `edit`·conflict `merge`/`split` 어포던스는 payload에 실려 오지만 렌더하지 않음 — 다음 B 슬라이스). action 후 목록을 **서버에서 재조회**(낙관적 패치 없음), detail confirm/reject 성공은 목록 복귀. review-inbox/gate-finding read는 `dict[str,object]` 무타입이라 `client.ts` 손선언 소비(v1.6.94 선례; response_model 타입화는 additive 후속). 회귀 +17(양방향: 렌더·단일 origin `/api` path·4 action endpoint+재조회·**어포던스 소비 over-strict**[`eligible=false`→disabled+reason, mutation bite 실증]·deferred edit/merge/split 미렌더·missing source_ref·action 실패 시 항목 보존). 전체 프론트 **99 passed/7 files**(review 17), build 93 modules, backend/scope diff 0. 다음 = candidate edit + conflict merge/split. 실 데이터 관통은 오너 풀스택 후속. | [work log](docs/daily_logs/2026-07-17/work_log.md) · [brief](docs/plans/frontend-review-inbox-decisions.md) · `frontend/src/review/` |
| 2026-07-16 | SoT v1.7.3: **C2 자동 revise/retrieve loop UI 완료**. C1의 generate→Gate 뒤 안전한 continuity revise finding만 골라 `/writing/revise-and-gate`를 자동 호출한다(권장 decision=`revise`, nonblank evidence, candidate 내 정확히 1회; error severity 우선·동급 Gate 순서). C0 생성 타입 `WritingReviseGateResponse`/`WritingReviseGatePartial`을 손선언 없이 소비하고, 마지막 candidate·Gate·loop stage와 status 6종(`pass|terminal_decision|not_eligible|budget_exhausted|no_change|failed`)을 사용자 행동으로 매핑한다. partial 4xx/5xx에서도 candidate·stage error를 보존하며 5xx만 원 요청 exact body로 재시도, 4xx는 확정 실패로 표시한다. 최종 revised candidate accept는 C1 H1 exact-body idempotency pin으로 새 key를 사용한다. backend/schema 무변, `schema.d.ts` IDENTICAL. 독립 검증 **PASS(조건 없음)** 후 H1~H4(2회 evidence 차단·non-revise 미진입·4 discriminator 표시·성공 후 retry 숨김)를 테스트로 보강해 프론트 C2 +15, 전체 **82 passed/5 files**(WritingPanel 33), build 91 modules. H5(설정성 503의 retry 문구)는 현 5xx 계약을 바꾸지 않고 후속 UX 검토로 남겼다. `OPS-1`은 실 12B 관통+오너 dogfood 결정 전까지 Waiting. 다음은 B Review Inbox. | [work log](docs/daily_logs/2026-07-16/work_log.md) · [verification](docs/verifications/2026-07-16/c2_writing_loop_ui.md) · [brief](docs/plans/frontend-writing-workspace-decisions.md) |
| 2026-07-16 | SoT v1.7.2: **C1 기본 Writing 작업공간 UI — editor에 generate→Gate 근거→accept/save 기본 루프**(v1.7.0 D1=A·D2=A·D4=A의 code slice). C0(v1.7.1) 생성 타입을 손선언 없이 소비(backend/schema 무변, `schema.d.ts` IDENTICAL). 신규 `frontend/src/writing/WritingPanel.tsx`가 editor 안에서 이어쓰기를 담당하고 `DraftEditor`가 D1 게이팅 상태와 accept 후 reload를 배선한다. **D1=A**: clean latest에서만 generate를 허용하되 zero-version·과거 version·dirty·archived 각각에 **이유+해소 행동 설명 텍스트**를 함께 보인다(disabled control만 두지 않음). **D2=A**: 공백-only·in-flight 중복 generate 차단, generate→별도 Gate 호출로 decision+findings 표시, **pass만 accept 활성**, `accepted=false`는 실패가 아니라 재-Gate 결과. **D4=A**: read-only candidate panel, accept 전 원문 무변. **accept intent**: idempotency key를 exact accept body에 결박(5xx 재시도 같은 key, candidate/base 변경 새 key). **outcome**: `502 + accepted=true + saved`를 저장 성공으로 처리(재조회+분석 재시도 안내, generic error로 저장 version을 잃지 않음)·`409 stale`은 candidate 보존+재생성 안내·`400/404/422` 확정 거부·5xx candidate/intent 보존. 프론트 회귀 +20(WritingPanel 18 + DraftEditor seam 2, 양방향; mutation 3종 bite 실증: pass-only guard 3·dirty 게이팅 3·signature 가드 1), 전체 프론트 **67 passed/5 files**, build 91 modules. backend·schema 무변. 독립 검증 PASS(조건 없음) 후 비차단 H1(idempotency over-strict pin)·H2(400/422 accept 회귀) 반영. 실 LLM 관통은 12B 필요라 풀스택 후속. 다음은 C2 자동 loop. | [work log](docs/daily_logs/2026-07-16/work_log.md) · [brief](docs/plans/frontend-writing-workspace-decisions.md) · `frontend/src/writing/WritingPanel.tsx` |
| 2026-07-16 | SoT v1.7.1: **C0 Writing HTTP contract 구현 — generate/gate/revise-and-gate/accept 응답 타입화**(v1.7.0 D3=A의 code slice, `ARCH-1` 발화·종결). **안전망 먼저**(v1.6.95 절차 재사용): 4 endpoint 응답의 완전한 exact-key 회귀(`Writing*EnvelopeKeyTest` 9 — generate·gate·accept 성공+partial·revise-and-gate 성공+4 partial)를 **모델보다 먼저** 추가해 현 dict payload에서 통과시켰다(`response_model`은 미선언 필드를 조용히 삭제하므로). **`ARCH-1` 첫 단계**로 Writing HTTP 모델을 별도 모듈 `writing/http_models.py`로 분리(router 추출·전 `main.py` 일괄 분리는 안 함). **성공 경로**(dict 반환)에 `response_model`: generate→`WritingCandidatePayload`, gate→`WritingGatePayload`, revise-and-gate→`WritingReviseGateResponse`, accept→`WritingAcceptResponse`. **partial 경로**(`JSONResponse`, runtime validation 우회)는 `responses={}` OpenAPI 문서화만: revise-and-gate 400/502/503/504 = `WritingReviseGatePartial | ErrorDetailResponse`, accept 502 = `WritingAcceptAnalysisPartial | ErrorDetailResponse`(**502+`accepted=true`+`saved`** 보존). payload 빌더 무변(D2=A 동형). 모델=payload와 정확히 같은 너비 실증(`WritingGatePayload.evaluated_by_model` 제거 시 gate·accept 중첩 회귀 bite). `gen:api` 재생성 → 프론트 타입에 성공 4 endpoint + partial union arm 5(revise-and-gate 4 status + accept 502; discriminator 필드 6). 회귀 +9, 백엔드 **1117/48/273**(ES 미설치 기준선 1108). 프론트 build 90 modules·**47 passed**(소비 코드 무변, schema.d.ts만 재생성). 다음은 C1 기본 UI. | [work log](docs/daily_logs/2026-07-16/work_log.md) · [brief](docs/plans/frontend-writing-workspace-decisions.md) · `services/application/app/writing/http_models.py` |
| 2026-07-16 | SoT v1.7.0: **Frontend Writing 작업공간 C 착수 계약 확정, 구현 대기**. 오너가 D1~D5를 모두 A로 승인했다: clean latest에서만 Writing을 허용하고 zero-version·과거 version·dirty 상태에는 이유와 해소 행동을 설명하는 UI text를 동반한다. C1은 generate→Gate 근거→accept/save 기본 루프, 성공·partial 응답은 C0에서 backend HTTP model+OpenAPI+exact-key 회귀로 계약화하며 accept의 502 partial save를 보존한다. 후보는 우선 read-only panel이지만 일부 수정·저장 후 재생성 UX는 C1 실제 사용 뒤 재검토한다. 구현 순서는 C0 contract→C1 basic UI→C2 bounded loop. runtime code/public payload 무변. | `docs/daily_logs/2026-07-16/work_log.md` · `docs/plans/frontend-writing-workspace-decisions.md` |
| 2026-07-16 | SoT v1.6.99: **Frontend editor/save A2, Product shell A 완료**. editor 안에 최신순 version history와 component-state 선택을 추가했다. dirty 전환은 최소 확인 UI로 취소 시 완전 무변, 확인 시 detail 성공 후에만 본문/baseline/선택을 교체하고 실패는 현재 입력을 보존한다. 과거 version 편집 저장은 기존 history를 유지한 채 append-only 새 latest를 선택한다. 선택된 저장 version의 서버 export envelope을 body·MIME·filename 그대로 txt/Markdown Blob 다운로드하며 AI metadata를 삽입하지 않는다. 독립 검증 PASS 후 실제 backend filename·charset 포함 MIME fixture와 selection in-flight 잠금 양방향 회귀를 보강했다. backend/Core SOT 무변. 프론트 47 passed/4 files, build·gen:api PASS. A 체크포인트에서 ARCH-1은 HTTP 표면 무변으로 미발화; 다음은 C Writing 작업공간. | `docs/daily_logs/2026-07-16/work_log.md` · `docs/plans/frontend-editor-save-decisions.md` · `docs/verifications/2026-07-16/frontend_editor_save_a2.md` |
| 2026-07-16 | SoT v1.6.98: **Frontend editor/save A1 구현**. `/projects/:projectId/drafts/:draftId` direct route에서 latest/empty 원고를 평문 textarea로 열고 명시적 Save로 append-only version을 만든다. 저장 intent는 승인된 D1=A대로 UUID+exact body에 결박해 모호한 실패의 동일 본문만 같은 key로 재시도하고, 변경 본문은 새 key를 사용한다. unchanged empty는 저장하지 않되 nonempty→empty 변경은 허용하며, in-flight 중복 POST·idempotent replay·404/409/5xx 입력 보존·project/draft archive read-only를 회귀로 잠갔다. **독립 검증 CONDITIONAL PASS의 B1(저장 중 타이핑 silent loss)을 1줄 제거+전용 red-first 회귀로 폐쇄**했고, H1 replay fixture를 실가능 흐름으로 정정·H3 beforeunload 양방향 회귀를 추가했다. 생성 OpenAPI 타입만 소비하고 backend/Core SOT는 무변. 프론트 39 passed/4 files, build·gen:api PASS. 다음은 A2 history selection·dirty confirmation·txt/Markdown export. | `docs/daily_logs/2026-07-16/work_log.md` · `docs/plans/frontend-editor-save-decisions.md` · `docs/verifications/2026-07-16/frontend_editor_save.md` |
| 2026-07-16 | SoT v1.6.97: **Frontend editor/save A1 계약 확정(구현 대기)**. 오너가 D1=A 저장 intent UUID+exact body 결박(모호한 실패+동일 본문만 같은 key 재시도)·D2=A draft route 고정+version component state·D3=A A1 editor/save→A2 history/export 분리를 승인했다. D2는 B(query parameter)의 공유/복원 가치도 인정했으나 현재는 추가 기능이므로 A부터 시작하고 B를 additive 후속으로 유지한다. backend/Core SOT 무변, autosave·offline journal·rich editor 보류. | `docs/daily_logs/2026-07-16/work_log.md` · `docs/plans/frontend-editor-save-decisions.md` |
| 2026-07-16 | SoT v1.6.96: **Product shell 프로젝트→원고 목록 세로 슬라이스**. 오너는 B(자체 History API)를 선호했으나 editor·Writing·Review까지의 route 확장성을 위해 **A(`react-router` Declarative BrowserRouter)**를 확정했다. `/`→`/projects/:projectId` route spine과 direct URL/뒤로가기/404 shell을 추가하고, 선택 프로젝트 원고 목록·빈 상태·생성·서버 재조회·보관 프로젝트 read-only를 연결했다. Data mode·loader/action·editor/save/version/export는 범위 밖. backend 무변, frontend 22 passed/3 files·build·gen:api·실 API smoke PASS. **독립 검증 PASS(조건 없음)** — matrix 16행 빈 셀 없음. 비차단 H3의 공백 submit-level 회귀를 mutation bite로 보강했고, 새 항목 강조는 실사용 위치 상실 관측 시로 명확화했다. | `docs/daily_logs/2026-07-16/work_log.md` · `docs/plans/frontend-project-navigation-decisions.md` · `docs/verifications/2026-07-16/frontend_project_navigation.md` |
| 2026-07-16 | **제품화 준비를 트리거 기반 개선 백로그로 전환**(문서 전용, SoT/public contract/code 무변). 오너가 전체 리팩터링·운영 경량화·품질 계측·공개 준비를 한 번에 수행하지 않고 **적절한 시기가 올 때마다 점검해 하나씩 제거**하는 관리 방식을 선택했다. 새 `plans/product-readiness-backlog.md`가 UX-1(A→C→B 기본 루프)·ARCH-1(`main.py` 점진 분리)·OPS-1(Lite/Full)·QUAL-1(2주 실제 원고 dogfood)·PROC-1(문서 비용 계층화)·REPO-1(rename)·LEGAL-1(라이선스)·GATE-1(Phase 7 진입)를 각각 **상태/착수 트리거/종료 조건/다음 점검 시점**으로 관리한다. 현재는 UX-1만 진행, PROC-1 상시, 나머지는 trigger 전 Waiting이며 **UX-1+QUAL-1 전 Phase 7 미착수**가 기본 게이트다. | [work log](docs/daily_logs/2026-07-16/work_log.md) · [backlog](docs/plans/product-readiness-backlog.md) |
| 2026-07-16 | SoT v1.6.95: **백엔드 공개 계약 조이기 — 척추 응답 모델(H1) + 이름 입력 검증(H2)**. v1.6.94 독립 검증이 올린 H1(응답 무타입)·H2(입력 검증 spec-silent)를 오너가 **H1→H2 순서**로 착수 지시하고 **D1=A/D2=A/D3=A** 확정(브리프 `plans/frontend-api-contract-decisions.md`). **착수 전 실측이 그림을 바꿨다**: **`/writing/revise-and-gate`·`/writing/accept`의 partial-failure envelope은 `JSONResponse` 직접 반환이라 FastAPI가 검증·문서화를 모두 우회**한다(H1이 구조적으로 못 덮는 구멍, 하필 Writing 트랙 표면) → Deferred로 명시. *(검증 후속 정정 H1-d2: 착수 당시 "2개 endpoint 전체가 적용 불가"로 적었으나 부정확 — 두 endpoint의 성공 경로는 dict라 적용 가능하고 uncoverable한 것은 partial-failure envelope뿐이다. 결론·Deferred 불변.)* **D1=A**: 척추 14 endpoint만 — 문제는 "50개에 타입이 없다"가 아니라 "프론트가 손선언하는 자리에 없다"이고, Writing·Review envelope은 UI가 아직 만나지 않아 지금 고정하면 두 번 만지게 된다. **D2=A**: `response_model=` 파라미터, payload 헬퍼 20개 무변. **핵심 절차**: response_model은 미선언 필드를 **조용히 삭제**하는데 기존 회귀엔 exact-key assertion이 0이었다 → **안전망을 모델보다 먼저** 깔았다(실증: `snapshot.project_id` 제거 시 1104개 중 새 회귀 1개만 bite). save/read surface가 같은 키에 다른 shape을 담아 모델 분리가 강제됐다. **H1 성과 실증**: 백엔드 필드 rename → 프론트 `tsc`가 소비 지점을 짚어 실패(v1.6.94의 "컴파일 타임에 안 잡힌다"가 척추에서 해소). **D3=A(H2)**: `NonBlankName`(strip 후 min_length=1)을 project/draft create·rename 4종에 — `"  x  "`→`"x"`, 공백-only 422. **Core SOT 정본 계약 무변**(모든 클라이언트가 HTTP를 지나므로 경계가 authority). 회귀 +12(양방향, mutation 4종 bite), 백엔드 1099→**1111/45/273**. live 관통 PASS. | `docs/daily_logs/2026-07-16/work_log.md` |
| 2026-07-16 | SoT v1.6.94: **Frontend 첫 슬라이스 구현 — 이 저장소의 첫 사용자 표면**(v1.6.93 계약의 code slice, D1=A/D2=B/D3=A). 백엔드 50 endpoint 위에 `frontend/`(Vite+React+TS SPA)와 compose `frontend` 서비스(nginx)를 세워 **계정 없이 앱을 열어 프로젝트를 만들 수 있는 경로**를 열었다(`product-shell.md` 수용 기준 1의 첫 절반). **오너 결정 2건**: (1) **슬라이스 범위 = scaffold + 프로젝트 목록/생성**까지, 원고 목록→에디터는 다음 슬라이스 — 스택 배선이 실제로 도는지 먼저 확인하고, 에디터는 Core SOT 계약(명시적 version save only + `idempotency_key` 필수)이 두꺼워 독립 슬라이스로 다루는 게 낫다는 근거를 오너가 수용(브리프 D3 각주 "크면 둘로 쪼갠다"의 발화). (2) **프론트 테스트 도구 = Vitest + RTL** — Vite와 설정/트랜스폼을 공유해 추가 빌드 체인 0이고 이 저장소의 양방향 회귀 관례를 그대로 적용 가능(브리프가 "첫 슬라이스에서 결정"으로 남긴 항목). **D2=B의 대가 상쇄**: nginx `location /api/`→`proxy_pass http://application:8000/`(trailing slash prefix strip)로 단일 origin을 유지해 **CORS 미개방**을 지킨다(D2=C 각하 근거를 D2=B 안에서 보존); 회귀가 `fetch` 첫 인자 exact `/api/...`로 이 경계를 잠근다. **발견된 계약 갭**: OpenAPI→TS 생성은 경로·요청 바디만 잠그고 **응답은 무타입**(엔드포인트가 `dict[str, object]` 반환 → 빈 object schema) → 응답 shape는 손으로 선언, 해소하려면 백엔드 `response_model` = 오너 결정 대기. 백엔드·계약·회귀 **무변**(1099/45/260 동일). 프론트 회귀 9(양방향), 실 컨테이너 관통 PASS(SPA·fallback·prefix strip·한글 왕복). | `docs/daily_logs/2026-07-16/work_log.md` |
| 2026-07-15 | SoT v1.6.93: **Frontend 착수 계약 확정(구현 대기)**. 오너가 프론트엔드 착수를 지시하고 D1=A/D2=B/D3=A를 확정 — **v1.1부터 유지되던 "frontend framework 최종 선택 보류"가 해소**되고 "editor shell은 현재 범위 밖"이 범위 안으로 들어왔다. **D1=A** React+TS+Vite SPA(만들 화면이 원고 에디터·Writing candidate/Gate 흐름·Review Inbox diff라 클라이언트 상태가 두꺼워 서버 렌더링의 약점 지점과 겹침). **D2=B**(오너 선택, 추천 A와 다름) 별도 compose nginx 서비스로 프론트를 application 이미지에서 분리 — Gateway/Worker/embedding/ES와 같은 독립 서비스 경계 원칙. 단일 origin 상실은 nginx `/api` reverse-proxy로 상쇄하고 CORS는 열지 않는다(인증 없는 단일 사용자 API). **D3=A** 첫 슬라이스는 Product shell 척추(프로젝트→원고→에디터) — Writing은 `current_position`, Review는 저장된 원고의 candidate를 전제하므로 의존성이 A→C(Writing)→B(Review) 순서를 강제. 백엔드 50 endpoint·계약·회귀 무변(프론트는 조립만). 이번 커밋은 계약 기록만이며 코드 0. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/frontend-kickoff-decisions.md) |
| 2026-07-15 | SoT v1.6.92: **Writing stable context pointer 구현**(v1.6.91 계약의 code slice, D1=A/D2=A/D3=A). 신규 `ContextPointer`+`writing/context_pointer.py`(projection·allowlist)로 self-report **D2=A first→B**의 B를 완결했다. claim의 `related_context_pointers`가 report wire·HTTP·Gate prompt·accept advisory copy에 동일 literal로 실리고, pointer 노출은 report extractor 한 turn뿐(`include_pointers=True` opt-in — generation/revise 평문 prompt 무변). parser가 exact 4-key/string + **current-package exact membership** + claim 내 중복 금지를 검증해 hallucinated/other-package pointer를 fails-closed로 막는다. **오너 결정 sub-decision P-i**: 구현 착수 전 브리프 follow-up의 origin 불변식 재확인에서 잠김 계약 1("네 non-empty string")이 실경로와 모순임을 발견 — memory는 `content_hash`, candidate는 `version_id`·`content_hash`가 구조적 빈값(store에 필드 자체가 없음)인데 회귀 매트릭스는 세 origin 인용을 요구했다. 오너가 **origin별 불변식 테이블**(source_blocks=4필드 non-empty / memory_entries=hash만 빈값 / analysis_candidates=version·hash 빈값, 미지 collection 거부)을 채택해 계약 1을 개정 — 빈값은 store에 필드가 없는 자리에만 허용하므로 source-block의 빈 hash(실 결함)와 cross-project item은 provider 호출 전 거부한다. 신규 persistence/registry/retention 없음, 쓰기 수·repair 횟수·Gate decision schema 무변. 회귀 +31(양방향), full 1096/48/260. **오너 독립 검증 PASS(조건 없음)** — 비차단 hardening H2(generation/revise service-seam no-pointer tripwire, call-site mutation에서 정확히 2개만 bite 실증)·H1(비-배열 pointer 필드 매개변수화) 반영, H3(실 12B 준수율)은 sandbox 본질적 한계로 오너 풀스택 후속. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/05-writing-stable-context-pointer-decisions.md) · [verification](docs/verifications/2026-07-15/writing_stable_context_pointer.md) |
| 2026-07-15 | SoT v1.6.91: **Writing stable context pointer 계약 확정(구현 대기)**. 오너가 D1=A/D2=A/D3=A를 승인했다. 기존 self-report **D2=A first→B**의 A(pointer 없는 최소 schema)는 완료됐고 이번 결정으로 B(full `related_context_pointers`) 확장을 승인했다. 새 브리프의 별도 D2=A는 B 구현 시 report extractor에만 pointer를 노출하고 current-package exact allowlist로 검증한다는 뜻이다. pointer wire는 기존 `IndexPointer`의 collection/document/version/hash projection, claim 배열은 required(empty 허용). 신규 persistence/registry/retention과 source_ref mint·canon/save 권한은 열지 않는다. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/05-writing-stable-context-pointer-decisions.md) |
| 2026-07-15 | SoT v1.6.90: **Writing Gate 판별 품질 라벨 벤치마크**. Gate 과민 `revise`/`not_eligible` 튜닝 전에 5 decision을 7개 fixture로 고정. 정상 역내 이동·정본과 양립하는 새 행동을 `pass` over-strict guard로 두고, 나머지 `revise|retrieve_more|needs_user_review|block`도 정본 경계별 라벨링. `scripts/benchmark_writing_gate.py`는 production Gate factory로 expected/actual decision·finding type·token·error·accuracy JSON을 출력하며 fault 격리·fails-closed. 쓰기 0, raw prose 미출력, runtime prompt/schema/literal 무변. 회귀 +6. **실 12B baseline(`192.168.1.22:9080`) 7×3 = 21/21 match, accuracy 1.0, fault 0** → 명확한 경계에서 과민 판정 미재현, prompt 튜닝 보류. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [baseline](docs/benchmarks/2026-07-15/writing_gate_quality_q4_baseline.json) |
| 2026-07-15 | SoT v1.6.89: **B2b aggregate budget production 기본값 default-on**(Option A 종결). v1.6.87 M-i 도구로 실 12B(`gemma-4-12B-it-qat-q4_0`, 외부 llama 9080) 라이브 per-stage 수집(3 passes, complete) → 합성 raw 최악경로 max_total_tokens 4991·max_wall_clock_ms 51755. context_search 27s는 측정 하네스 콜드스타트 아티팩트(프로덕션 warm ~4s → steady-state wall-clock ~28.8s). **오너 B4: ~2x 여유율 → `WRITING_LOOP_MAX_TOTAL_TOKENS=10000`·`WRITING_LOOP_MAX_WALL_CLOCK_MS=60000`, default-on**. docker-compose.yml 배포 기본을 off→발화로 변경(code 기본 env-미설정은 계속 off → M6=A 코드 회귀 무변). 라이브 검증: 재생성한 application 컨테이너에 10000/60000 적재 확인. raw 아티팩트 docs/benchmarks/2026-07-15/. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/05-writing-loop-ceiling-composition-decisions.md) · [benchmark](docs/benchmarks/2026-07-15/writing_loop_per_stage_ceiling_q4.md) |
| 2026-07-15 | SoT v1.6.88: **Writing loop multi-finding revise**. bounded loop의 `_eligible_revision_finding`이 자동 revise 대상을 정확히 1개로 제한해, Gate가 revise 분기에서 다수 continuity finding을 낼 때 `len != 1` 이유만으로 not_eligible 종료하던 gap을 닫는다. 오너 결정 **D1=A**(continuity-only 유지 — do_not_use/pov는 자동 splice 제외)·**D2=A**(sequential — 자격 함수를 "정확히 1개"→"N개 중 최우선 1개"로 완화, loop 기존 re-gate가 다음 라운드 finding 선택, finding당 revision round 1, 구조 상한 bound)·**D3=A**(error severity 먼저, 동급 Gate 순서). 변경 표면 = 자격 함수 1개(reviser/splice/report/Gate/audit/budget 무변). public literal·schema·서비스 경계 무변. 회귀 +7(`EligibleRevisionFindingTest` 5·`MultiFindingSequentialLoopTest` 2, 실 reviser 관통), 기존 "2 findings→not_eligible" case를 새 계약으로 정정. **독립 검증 PASS(조건 없음)** `docs/verifications/2026-07-15/writing_multi_finding_revise.md` — 비차단 H1(브리프 정밀도)·H2(DO_NOT_USE 케이스)·H3(first-round 비대칭 문서화) 반영. full 1062/45/240. Deferred: batch reviser(D2=B), do_not_use/pov 자동 revise(D1=B), max_revision_rounds 상향(live 후). | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/05-writing-multi-finding-revise-decisions.md) · [verification](docs/verifications/2026-07-15/writing_multi_finding_revise.md) |
| 2026-07-15 | SoT v1.6.87: **Phase 5.10 Option A (M-i) per-stage 측정 도구**. B2b 재측정에서 실 12B가 Gate 독립성(D3=A)으로 loop 최악경로를 걷지 못해 `WRITING_LOOP_MAX_TOTAL_TOKENS`/`MAX_WALL_CLOCK_MS` 기본값 근거 ceiling을 loop-level 직접 측정할 수 없다. 오너가 이미 **Option A(per-stage 합성)**을 택했고(v1.6.86 합성 코어), 본 버전은 합성 입력을 얻는 **측정 메커니즘 M-i(오너 확정)**를 구현한다. 신규 `writing/per_stage_measure.py`(`run_per_stage_measurement`)+`scripts/measure_writing_stages.py`가 revise/report/gate/retrieve_plan/context_search를 production seam으로 조립·실 gateway 격리 호출해 `TokenUsage`+`perf_counter`를 stage별 기록. **context_search는 token 제외(항상 0)·wall-clock만**(loop metered() 경계 동형), **retrieve_plan은 합성 retrieve_more Gate**(코드 구성)로 Gate 독립성 회피, repeats간 보수적 MAX, stage fault surface. CLI가 측정 dict→`compose_worst_case_ceiling`(env 정책)→raw ceiling JSON, **incomplete 측정은 fails-closed**(ceiling 숫자 null+`complete=false`, 검증 H6). 쓰기 0(gate/report 진단 동형, 출력 숫자만이라 persist 안전). 회귀 +14(`tests/test_writing_per_stage_measure.py`). full 1055/45/235. **독립 검증 PASS(조건 없음)** `docs/verifications/2026-07-15/writing_per_stage_measure_mi.md` — 비차단 H6(incomplete ceiling fails-closed)·H2(env→policy wiring)·H1(SoT v1.6.86 합성 코어 역참조) 보강. **live per-stage 수집→합성→B4 여유율/default-on 승인은 sandbox 밖 풀스택 후속(오너)**. | [work log](docs/daily_logs/2026-07-15/work_log.md) · [brief](docs/plans/05-writing-loop-ceiling-composition-decisions.md) · [verification](docs/verifications/2026-07-15/writing_per_stage_measure_mi.md) |
| 2026-07-14 | SoT v1.6.86: **잔존 4개 strict JSON parser fence-strip 스윕(추적 부채 해소, 잔존 0)**. gate(v1.6.83)·report(v1.6.85)와 동일 root-cause(`json.loads` 직접 호출, whole-content markdown fence strip 없음)가 남아 있던 `analysis/compare_judge`·`analysis/extractor`·`context_search/planner`·`writing/retrieval` 4개 terminal-JSON parser에 공유 `writing/json_extract.py::strip_code_fence`를 적용했다. 오너의 D2=A 추출 프레이밍("fence 래핑=정상 포맷 변형, parser가 JSON 추출") 계승 — parser 정규화이며 strict schema/enum/literal 검증 무변(public literal·schema·서비스 경계 무변). 4개 모두 repair(1회 재호출) 완충이 있었으나 first·repair 양쪽 fence 반복 시 502/invalid 가능했던 잔여 위험 제거. `retrieval.TEMPLATE`에 fence 금지 추가(나머지 3개는 기존 template/repair에 존재). **SoT 정합**: Phase 2A extraction clause가 "markdown-fenced JSON=first-parse 실패→repair"로 규정하던 것을 fence 추출 정책에 맞게 정정(fenced valid=first-call 통과, malformed/schema mismatch만 repair). 양방향 회귀 +8(각 parser fenced-valid under-strict + fenced-invalid over-strict; compare/extractor/planner 기존 fence→repair 테스트 4개[compare 1·extractor 2·planner 1] 정정). full 1035/45/235. 새 owner 결정 없이 진행(기결정 D2=A 정책의 스코프된 적용). | [work log](docs/daily_logs/2026-07-14/work_log.md) · SoT v1.6.86 |
| 2026-07-14 | SoT v1.6.85: **Writing report fence-strip remediation(D2=A (a)+(c))**. report 진단(v1.6.84) live 실행으로 report first output이 gate와 동일하게 ```` ```json ```` fence로 감싸짐을 확인(1-call repair가 살림). 오너 결정 (a) `report.parse_report`에 fence 추출 적용 + (c) `TEMPLATE` fence 금지. **first·repair 모두 parse_report를 거쳐 양쪽 fence 자동 커버**(repair가 ```` ```json ```` 내도 추출). gate v1.6.83과 동일 정규화(parser 완화 아님). fence 추출을 공유 유틸리티 `writing/json_extract.py::strip_code_fence`로 추출(gate_prompt private helper 이동·공용화) — "버그가 아니라 추출 과정" 프레이밍. 양방향 회귀 7개(`ReportFenceStrippingTest`: first+repair fence 추출 under-strict·fenced invalid over-strict·mutation bite). tracked debt `report.py:113` 해소(잔존 4: compare/extractor/planner/retrieval). public literal·schema·서비스 경계 무변. | [work log](docs/daily_logs/2026-07-14/work_log.md) · [brief](docs/plans/05-writing-report-live-diagnostics-decisions.md) |
| 2026-07-14 | SoT v1.6.84: **Writing candidate report live diagnostics(D1=A)**. gate fence fix(v1.6.83)로 `invalid_gate_result` 502가 0건이 됐으나 B2b 재측정(1/12 success)에 `invalid_candidate_report` 502가 남아("report field must be an array"), P1 bodyless audit으로 raw report output이 없어 exact 위반 clause(fence? schema? 어느 필드?)와 first/repair 중 어느 시점 실패인지 알 수 없었다. gate D1=A와 동일 패턴의 `scripts/diagnose_writing_report.py` + `report_live_diag.py`가 `/writing/revise-and-gate` 사전 report 파이프라인(context→revise→report)을 동일 production config로 재현해 report **first + 1-call repair raw response** + strict-parse error만 stdout 출력(report는 repair가 있어 gate와 달리 두 raw를 모두 잡음). Mongo/audit/file 쓰기 0. report는 `_build_report_service(capture)`로 production config 재사용. 회귀 11개(parity·first+repair raw capture·fence 미strip 관측·upstream·no-write). report remediation(fence strip tracked debt / prompt / parser)은 D2=A 별도 브리프. public literal·schema·서비스 경계 변화 없음. | [work log](docs/daily_logs/2026-07-14/work_log.md) · [brief](docs/plans/05-writing-report-live-diagnostics-decisions.md) |
| 2026-07-14 | SoT v1.6.83: **Writing Gate fence-strip remediation(D2=A (a)+(c))**. 독립 검증(v1.6.82)이 확정한 root cause — B2b 502의 `invalid_gate_result` exact 위반 clause는 markdown code fence 래핑이었다(`gate_prompt.py` `json_object()`가 fence strip 없이 `json.loads`). 오너 결정 (a) `json_object`에 `_strip_code_fence` 추가(parse 전 whole-content ```` ```lang…``` ```` fence 정규화) + (c) `WRITING_GATE_TEMPLATE`에 fence 금지. **parser 정규화·구조 완화 아님** — strict schema/enum/priority/evidence는 parsed dict에 동일 적용, public literal·schema·서비스 경계 무변(유일한 동작 변화 = fence-wrapped 유효 JSON이 502→정상 parse). 양방향 회귀 9개(under-strict: fence-wrapped valid parse — strip 제거 시 B2b 502 재현 / over-strict: fence-wrapped invalid가 올바른 이유로 거부) 추가, mutation으로 bite 실증. prompt version `writing_gate_v1` 유지. 동일 root-cause 5곳(report/compare/extractor/planner/retrieval)은 repair로 완충돼 있어 Gate만 우선 수정, 나머지는 tracked debt. | [work log](docs/daily_logs/2026-07-14/work_log.md) · [brief](docs/plans/05-writing-gate-live-diagnostics-decisions.md) |
| 2026-07-14 | SoT v1.6.82: **Writing Gate live diagnostics(D1=A)**. 오너 채택 **D1=A, D2=A**. B2b 502은 Gate `invalid_gate_result`로 좁혀졌으나 persisted audit P1 bodyless라 raw model output이 없어 exact 위반 clause를 판별할 수 없었다. operator-only one-shot `scripts/diagnose_writing_gate.py` + `services/application/app/writing/gate_live_diag.py`가 `/writing/revise-and-gate` 엔드포인트의 사전 Gate 파이프라인(context→revise→report→gate)을 동일 production collaborators/prompt/model/`thinking=false`/`max_tokens`로 재현해 Gate raw response + strict-parse error만 stdout에 출력한다. Mongo/audit/API/file 쓰기 없음. `_default_writing_gate_service(provider=None)` seam(reviser/report factory 선례 일치, 기본 동작 무변)으로 Gate config를 production과 구조적으로 동일 재사용. 회귀는 Gate request parity·ContextSearchRequest parity·raw capture(parse 실패 under-strict/성공 over-strict)·upstream 실패 분류·no-write call sequence를 양방향 잠갔다(13개). D2=A에 따라 prompt/repair/parser 변경은 이 CLI로 exact clause 관측 뒤 별도 브리프. 실행: `docker compose run --rm --no-deps application python scripts/diagnose_writing_gate.py --project-id <id>`. public literal·schema·서비스 경계 무변. | [work log](docs/daily_logs/2026-07-14/work_log.md) · [brief](docs/plans/05-writing-gate-live-diagnostics-decisions.md) |
| 2026-07-14 | SoT v1.6.81: **Writing loop B2b full-stack benchmark harness**. 사용자 승인 B1=A/B2=A/B3=A/B4=A에 따라 deployed `/writing/revise-and-gate` HTTP의 terminal/retrieve_more/max structural path 세 fixture를 측정하는 `scripts/benchmark_writing_loop.py`를 추가했다. opt-in audit detail에서 aggregate token·loop wall-clock을 읽고 POST latency와 분리하며, trace mismatch/failure는 p95 성공 표본에 넣지 않고 raw report에 남긴다. warmup 1·measured success 3 절차를 채택했다. 독립 검증 B1을 closure해 warmup 실패 iteration=0 보존+measured run 지속을 회귀로 잠갔고, live provenance는 required `--model`/`--quant`/`--compose-revision`으로 기록한다. 결과를 본 후 별도 승인 전까지 `WRITING_LOOP_MAX_TOTAL_TOKENS|WRITING_LOOP_MAX_WALL_CLOCK_MS` 기본값은 off다. | [work log](docs/daily_logs/2026-07-14/work_log.md) |
| 2026-07-13 | SoT v1.6.80: **Writing loop aggregate token/wall-clock budget(B2 increment)**. 오너 결정 M1~M6에 따라 revise/report(+repair)/Gate/retrieval planner(+repair)의 provider usage를 내부 `*_metered` 채널로 loop까지 전달하고 run-level token/time을 집계한다. optional `WritingLoopPolicy.max_total_tokens|max_wall_clock_ms`와 env/Compose 설정을 추가했으며 기본은 off(`None`), production 숫자는 B2b live loop benchmark 후 확정한다. token은 post-accounting(`> limit` 결과 미채택, `== limit` 완료 허용), wall-clock은 다음 provider/search 단계 전 deadline 검사다. aggregate는 opt-in persisted audit summary/detail에만 additive이고 ephemeral `loop`/`stages`는 무변이다. 첫 Gate 전 소진은 마지막 완전 candidate+`gate=null`을 200 `budget_exhausted`로 보존한다. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.79: **Writing 감사 opt-in 재개정 (P2=A→B)**. H3(감사 쓰기 실패가 성공한 loop 결과를 raw 500으로 잃음) 논의 중 오너가 더 근본적 방향을 지시: "감사와 실제 loop 작업을 분리, 항상이 아니라 필요할 때만, on/off 토글로 loop 실행 시 바로 적용." 앞서 잠근 **P2=A(항상 감사)를 P2=B(opt-in)로 되돌리는 개정**(CLAUDE.md §5 — 기록된 결정과 충돌하므로 명시·확인 후 반영). 채택 **A(request 플래그)+기본 off**: `/writing/revise-and-gate`의 `persist_audit`(기본 null→env `WRITING_LOOP_AUDIT_DEFAULT` 기본 false, 요청 플래그 override)가 true일 때만 persist. loop는 trail을 항상 서버 권위로 in-memory 계산하되 persist만 gate. **H3 흡수**: persist를 loop critical path 밖 try/except-격리 → 쓰기 실패해도 loop 결과 반환+`audit_id=null`+`audit_error:{type:"audit_persist_error"}`. P1=B/P3=A/P4=A/P5=A·side-effect 0·감사 레코드 구조/읽기 계약 불변. 회귀 +3(default-off·env default·persist-failure isolation), 전체 947 passed/45 skipped/215 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.78: **Writing persisted bounded-loop audit (L9 B)**. L9=A first→B 후속으로 `/writing/revise-and-gate`의 모든 loop 종료를 durable append-only 감사 레코드로 영속화한다. 오너 결정 **P1=B**(bodyless per-stage hash/fingerprint/pointer + detail의 final candidate text 하나만 본문)·**P2=A**(모든 종료 자동 기록, pre-loop 요청 거부는 loop 아님→미기록)·**P3=A**(append-only uuid — provider 샘플링으로 loop가 비결정적이라 재시도는 별개 감사 사건; 결정적 id로 묶으면 두 번째 실행이 감사에서 사라짐)·**P4=A**(`GET .../writing/loop-audits` 요약 목록+`/{audit_id}` 전체 trail)·**P5=A**(immutable, 상태전이·자동삭제 없음). **오너 추가 지시**: 오래된 run은 검증 보조자료로 쓰일 수 있으니 보존을 기본으로 두고 **retention(TTL/archive)은 명시된 운영과제 후속**으로 남긴다(P5 미룸=정리 로직 없음이지 run 폐기 아님). 응답은 기존 `{candidate,gate,loop,stages}`에 `audit_id`만 additive이고 side effect는 감사 쓰기 1건뿐(Core SOT/Analysis/memory 0). aggregate token/time 집계는 B2 usage plumbing 후속. 신설 `writing/loop_audit.py`(+`loop_audit_mongo.py`)·`audit_hash.py`(공유 fingerprint), `WritingLoopStage`에 per-stage 감사 필드 additive. 회귀 +12(service 5·HTTP 7). 독립 검증 조건부 합격 → **B1 closure**(detail trail top-level 15키+stage row 6키 exact-set lock, token/latency·stage body 부재 forward-defense; mutation 재-bite 확인) + H1(Mongo fake round-trip)·H2(나머지 4종 loop_status 감사) 보강 +4, H3(audit-write 실패 fail-loud vs degrade는 오너 결정 대기). 전체 944 passed/45 skipped/213 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) · [verification](docs/verifications/2026-07-13/writing_persisted_loop_audit.md) |
| 2026-07-13 | SoT v1.6.77: **Writing bounded revise/retrieve loop + 관측 schema**. 사용자 결정에 따라 기존 `/writing/revise-and-gate`가 단일 continuity finding auto-revise와 targeted retrieval을 순서와 무관하게 잔여 구조적 budget 안에서 실행한다. 기본 총 revision=2·retrieval=1·Gate=3은 `WritingLoopPolicy`와 `WRITING_LOOP_MAX_*`/Compose 설정으로 조정 가능하다. pass·사람 decision·자격 밖·예산 소진·자동 no-change를 200 `loop.status`로 구분하고, 최소 `stages[{stage,ordinal,status}]`를 additive 공개한다. 자동 후속 unchanged만 전용 타입으로 `no_change`; 최초/standalone unchanged=502는 유지한다. 단계 실패는 마지막 candidate/Gate+`failed` stages를 보존하고 save/accept/Analysis/persistence는 없다. aggregate token/time·전체 artifact·persisted loop 감사는 후속. Writing focused 115 passed/108 subtests, 전체 927 passed/45 skipped/209 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.76: **Writing `retrieve_more` targeted 1회 lifecycle**. 사용자 방향에 따라 전수 need 재실행 대신 별도 follow-up retrieval planner LLM이 첫 Gate의 부족 findings에서 strict JSON `query+needs`를 선택한다. 기본 canonical 5종 중 `current_position` 부재 시 position-dependent current/recent를 제외한다. 기존 `/writing/revise-and-gate`가 비영속 이전 package와 targeted delta를 pointer-dedup·delta 우선·전체 budget 재적용으로 merge하고, candidate/최신 report를 유지한 채 Gate만 최대 한 번 재평가한다(`max_retrieval_rounds=1`). 성공 `{candidate,gate}` 유지(A first→`stages`), planner/context 실패는 `{candidate,gate:<첫 Gate>,retrieval_error}` partial artifact 보존. 사용자 이유: 로컬 LLM 재호출 비용보다 불필요한 3종 전수 재검색 낭비를 줄이고 이전 결과를 이어가는 lifecycle을 우선. 독립 검증 B2(position 조건 spec gap)를 정본에 보충하고 누락 taxonomy/no-save/identity 회귀도 보강, 전체 917 passed/45 skipped/198 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.75: **partial revise→report→Gate 3단계 합성**. 기존 `/writing/revise-and-gate`를 동일 ContextPackage의 revise→report enrich→Gate로 승격. report 실패는 502/504 `{candidate,gate:null,report_error}`로 revised artifact를 보존하고 Gate를 호출하지 않으며, reporter 미구성은 503으로 조용한 2단계 퇴행을 막는다. 성공 `{candidate,gate}` 유지(R5=A first→C); 다회 합성 시 `stages` additive 확장 가능. focused 94 passed/84 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.74: **partial revise→Gate 1회 합성**. 별도 `/writing/revise-and-gate`, 동일 ContextPackage, report 미재추출(A first→B), revise/Gate 각 1회. 성공 `{candidate,gate}`, Gate decision 5종 모두 200; Gate 실패는 `{candidate,gate:null,gate_error}` partial envelope로 revised artifact를 보존하며 검증 400/provider·invalid·fallback 502/timeout 504로 구분. 원격 실모델에서 Gate invalid JSON이 발생해 `partial_ok` candidate 보존 경로 실증. 독립 검증 B1 closure로 composition 전용 revise validation 400·provider timeout 504와 Gate 미호출을 추가 잠금. focused 93/80, 전체 905 passed/45 skipped/181 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.73: **finding evidence 기반 부분 revise**. continuity+revise 단일 finding의 exact evidence가 한 번 있을 때 모델은 replacement 평문만 반환하고 Application이 prefix/suffix 보존 splice. inline `POST /projects/{id}/writing/revise`, stale report reset, unchanged/empty=502, validation pre-provider 400. 장기 D4 C multi-finding·D5 A→B→C Gate loop·D7 AgentLoop budget·D8 200 changed=false·persisted 감사 API를 순차 후속으로 잠금. 모델은 호출/응답, Application은 anchor/validation/splice/loop 제어를 소유한다. 원격 `192.168.1.22:9080` live replacement 및 splice PASS. 독립 검증 조건부 합격(`verifications/2026-07-13/writing_partial_revise.md`) B1(context 504/502) closure + H1~H4(pre-context validation/no-save/fence unwrap) 보강. focused 84/68, 전체 896 passed/45 skipped/169 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-13 | SoT v1.6.72: **inline Writing report 재평가 API**. 오너 결정 A=비영속 inline candidate+서버 ContextPackage 재구성, B=persisted candidate/report 감사 이력과 id 기반 API는 additive 후속. `POST /projects/{id}/writing/report`가 기존 strict extractor를 side-effect-free 재사용하며 공개 candidate envelope를 반환한다. 실 LLM live에서 prompt가 enum 목록/JSON item shape를 주지 않아 repair도 실패하는 결함을 발견해 exact schema prompt로 보강; `192.168.1.22:9080` 재실행 `status=ok`(claims=2, hints=1). 독립 검증 조건부 합격(`verifications/2026-07-13/writing_report_api.md`)의 B1(report 전용 unsupported task 400) closure + H1(invalid context 400)·H2(no-save spy)·H3(query override)·H4(current_position) 보강. focused 71 passed/54 subtests, 전체 883 passed/45 skipped/155 subtests. | [work log](docs/daily_logs/2026-07-13/work_log.md) |
| 2026-07-12 | SoT v1.6.71: structured candidate report. 평문 후 별도 extractor, typed claims/hints/risks, strict+repair, generate 합성, Gate JSON 소비, accepted report→AnalysisJob advisory→snapshot extraction prompt. direct candidate/memory mint 금지. 독립 검증 조건부 합격(`verifications/2026-07-12/writing_self_report.md`)의 B1(HTTP enriched 직렬화 `type` key)·B2(analysis advisory 소비: runner snapshot 부착·prompt 포함·job round-trip) closure + H3(gate prompt claims/hints 이름 노출 일관성) 보강, 회귀 +5, 전체 873 passed/48 skipped. production 코드 무변경. | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.70: **Writing accept→save→analysis 재진입**. latest base/stale 409, paragraph append(A first→C patch 확장), accept-time Gate pass only, accept key 기반 save/job idempotency, pending Analysis job(A first→C background 확장), non-pass 200 accepted=false, job failure 502 partial-success+retry 수렴. 신규 `writing/accept.py`, `POST /projects/{id}/writing/accept`, 회귀 service+HTTP. 독립 검증 PASS(`verifications/2026-07-12/writing_accept.md`) 후 accept 표면 provider/context 502/504·dependency 503·non-pass 4종·cross-project·SoT/whitespace 문구 보강, 전체 861 passed/48 skipped. | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.69: **Phase 5.2 Writing Gate**. D1=A(별도 1-turn LLM Gate, 정확도 우선)·D2=B(decision+구조화 findings)·D3=A(side-effect-free 판정; 자동 전체 재생성 대신 evidence/recommended_decision으로 후속 부분 revise 연결)·D4=A first(별도 evaluate API, generate 합성 확장 가능). 다섯 decision과 우선순위, finding type/severity를 public contract로 잠금. 신설 `writing/gate.py`·`gate_prompt.py`, 확장 models, `POST /projects/{id}/writing/gate`; malformed model output 502, provider timeout 504, 기타 provider 502. 독립 검증 조건부 합격(`verifications/2026-07-12/writing_gate.md`)의 B1(priority over-strict)·B2(hard finding 전 경계)·B3(HTTP 400) 회귀를 닫고, priority 전 사슬·package 격리·context-search 502/504·문서 drift까지 보강. 회귀 +19(127 subtests), 전체 843 passed/48 skipped. | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.68: **Phase 5.1 Writing 생성 (첫 slice, 생성만)**(브리프 `plans/05-writing-generation-decisions.md`, HANDOFF 방향 결정 Phase 5). 순차 계획상 Phase 6보다 앞이나 미구현이던 Writing AI(글 생성)를 연다. 오너 결정 **Q1=생성만**(Writing Gate는 do_not_use/POV 의미검사가 LLM 기반이라 독립 slice)·**Q2=평문 프로즈**(서비스가 WritingCandidate 래핑, JSON-in-창작프로즈 fragility 회피; self-report 구조 필드는 계약 정의·Gate slice가 채움). 파생: continue_scene 한정·직접 1-turn Gateway 호출(extractor/compare_judge 패턴)·output_type=draft_patch·결정적 안전선=project isolation+task/instruction 검증·HTTP가 context_search→generate 오케스트레이션. 신설 `writing/`(models·prompt·service), `POST /projects/{id}/writing/generate`(503/400/404/502/504). status는 항상 candidate. 회귀 +19(prompt 조립 4·generate 7·HTTP 8), pytest 824 passed/48 skip. 오너 독립 검증 합격(조건 없음, `docs/verifications/2026-07-12/writing_generation.md`, 매트릭스 10행+mutation 6종) → 비차단 H2(504/502 orchestration 브랜치 회귀 부재) HTTP +2 보강. Writing Gate·accept→save·구조적 self-report·revise/outline/critique는 후속 | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.67: **Phase 6 Review Inbox 액션 어포던스**(브리프 `plans/06-review-inbox-affordances-decisions.md`, HANDOFF Next Tasks #1 Phase 6 UI 잔여). Review Inbox가 항목별 가용 review action을 선언 안 해 프론트가 자격 규칙을 재구현해야 하던 gap을 닫는다. 오너 결정 **D1=자격 주석형 descriptor**(`{action,eligible,reason}`)·**D2=candidate+conflict+gate finding 전부**·**D3=list+detail 둘 다**. 자격 규칙(read-time 재계산, write가 authority): candidate(confirm/reject/edit 항상 eligible)·conflict(merge=character+matched canonical, split=character)·gate finding(resolve/dismiss=open일 때만). read-only 어포던스 계산 + 기존 payload에 `actions` additive(도메인 write·상태·색인 무변). 신설 `ActionAffordance`+`candidate_affordances`/`conflict_affordances`/`gate_finding_affordances`, review-inbox list/detail·conflict·gate finding payload에 `actions`. 회귀 +7(pure 자격 5·HTTP envelope 2), pytest 805 passed/48 skip. 부분 승인·merge/split 일반화·frontend는 계속 미확정 | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.66: **Phase 6 candidate edit (백엔드 계약)**(브리프 `plans/06-candidate-edit-decisions.md`, HANDOFF Next Tasks #1 Phase 6 UI 잔여). review action 3종 중 마지막 edit을 연다(수용 기준 §60 approve/reject/**edit**). 오너 결정 **D1=원 후보의 새 version**: 편집 payload로 candidate의 새 version(append-only, 새 id)을 mint→`confirmed`+canonical 승격, 원 후보는 신규 terminal `superseded`로 보존(감사)·de-index. D2=edit-and-confirm 단일 액션·D3=`superseded`(edit 전용, confirm/reject 전이 채널 아님)·D4=idempotency(`logical_key=edit:{원본id}`가 기존 `(project,task,logical_key)` unique index 재사용 — 클라이언트 key 불요·원본 1회 edit)·D5=근거/provenance/confidence 보존(payload만 schema 재검증)·D6=confirm 선례 재사용(promote 링크=successor·conflict resolve·CANDIDATE_REMOVED). 결정적 백엔드 로직(라이브 불요); 부분 승인·merge/split·frontend는 계속 미확정(Phase 6 UI slice). 신규 literal `AnalysisCandidateStatus.SUPERSEDED`·`AnalysisCandidate.supersedes_candidate_id`. 신설 `AnalysisService.edit_candidate`+`CandidateEdit`·`CandidateReviewService.edit`+`CandidateEditResult`. HTTP `POST .../candidates/{cid}/edit`{`payload`}(404/409/400). 신규 repo 메서드 없음. 회귀 +15(`EditTest` 9·transition superseded 거부 1·`EditCandidateApiTest` 5), pytest 798 passed/48 skip(mongo env 제외), git diff --check clean. 실 Mongo edit round-trip은 sandbox 밖 | [work log](docs/daily_logs/2026-07-12/work_log.md) |
| 2026-07-12 | SoT v1.6.65: reject Context Gate finding durable persistence, idempotent identity/lifecycle, Review Inbox additive integration 및 Gate 전용 API 추가. 최소 재현 fingerprint/pointer manifest 포함. 독립 감사 조건부 합격의 D5/D7 빈 셀과 권장 404/Mongo adapter 회귀 보강, 최종 783 passed/48 skipped | [work log](docs/daily_logs/2026-07-12/work_log.md) · [verification](docs/verifications/2026-07-12/gate_finding_persistence.md) |
| 2026-07-12 | SoT v1.6.64: Phase 6 Review Inbox 백엔드. 미승격 needs_review candidate 목록, open conflict 중첩, matched canonical field diff, source_ref 정본 pointer를 제공하는 read-only list/detail API 추가. 독립 감사 PASS 후 resolved source pointer 전체 envelope와 direct-promoted legacy 억제 회귀 보강, 최종 773 passed/48 skipped | [work log](docs/daily_logs/2026-07-12/work_log.md) · [verification](docs/verifications/2026-07-12/review_inbox_backend.md) |
| 2026-07-12 | SoT v1.6.63: (c-2) 동명이인 semantic 반증, 라벨 기반 threshold calibration CLI, review-entry 기반 append-only merge/split reconciliation API 구현. 독립 적대적 검증 PASS 후 reconcile HTTP 200/404/409 envelope·replay·superseded target 회귀와 명시적 domain-error→409 매핑 보강. 최종 767 passed/48 skipped | [work log](docs/daily_logs/2026-07-12/work_log.md) · [verification](docs/verifications/2026-07-12/character_homonym_reconciliation.md) |
| 2026-07-12 | SoT v1.6.62: (c) **character 별칭 semantic 보강**(브리프 `plans/02b-7-character-alias-homonym-decisions.md`, HANDOFF Next Tasks #1 (c)). 2B.3 D2=A가 "별칭/동명이인은 merge/split review 후보(**자동 병합 없음**)"로 잠갔고 2B.6 D5=A가 "character semantic은 별도 결정"으로 남긴 경계를 연다 — 착수 브리프가 이 계약 충돌을 먼저 surface(CLAUDE.md §1). 결정적 name-key의 **false negative(별칭: `김철수`↔`철수`, name-key=0이라 중복 canonical 양산)**를 semantic으로 탐지. 오너 결정 **D1=A 탐지만**(자동 병합 없음 — semantic 매칭을 `conflict`[review]로 surface, judge 미호출; D2=A 존중·canon 오염 회피)·**D2=A 별칭만 먼저**(동명이인 false-positive 반증은 name-key=1 분기라 메커니즘 반대, 별도 후속). D3=A(conflict + matched id→review_queue 풍부)·D4(별도 env)·D5=A(off 기본, 회귀 0)·D6=A(top-1)·D8(2B.6 guard 재사용). 성격: **2B.6 `EmbeddingSemanticMatcher`·`query_similar` read 인프라 재사용, 계약 확장 + off 기본**. 신규 env literal `ANALYSIS_CHARACTER_ALIAS_MATCH_THRESHOLD`(기본 off). 확장: `AnalysisCompareService.alias_matcher` seam(name-key=0 + character일 때만 alias 조회, 히트→`conflict`+matched id; name-key≥1은 종전 judge 불변)·`main.py` `_build_memory_semantic_matcher(threshold_env)` 공유 빌더 + `_build_character_alias_matcher`. 회귀 +8(`CharacterAliasTest` 6[alias→conflict+matched id·judge 미호출·below-threshold create·off 기본·same-name 결정적 우선·alias self-exclusion 같은 job·scope-less 미영향]·compare_api 2[alias wiring fail-fast·off none]), pytest 757 passed/48 skip. **라이브 관통 PASS**(`scripts/phase2b7_character_alias_live_smoke.py`, 풀스택 머신): 실 BGE-m3-ko + Chroma `memory_vectors`로 `철수`(별칭)→conflict+canonical id·`영희`(타인)→create — **관통·wiring 검증이며 라벨 정확도·threshold 실값 캘리브레이션은 후속**(D5/D7). 동명이인 반증·threshold 캘리브레이션·merge/split write는 후속 | [work log](docs/daily_logs/2026-07-12/work_log.md) **(오너 독립 적대적 검증 PASS 조건 없음 → `docs/verifications/2026-07-12/character_alias_semantic.md`)** 비차단 관찰 Obs2(alias self-exclusion)·Obs4(scope-less 미영향) 회귀 보강(+6→+8, mutation bite), Obs1(SoT 산문 순서 기존 부채)·Obs3(rationale id) 코드 무변 → suite 757 passed |
| 2026-07-11 | SoT v1.6.61: **Phase 6 candidate 상태 전이 (백엔드 계약)**(브리프 `plans/06-candidate-state-transition-decisions.md`, HANDOFF Next Tasks #1 Phase 6). 5개 forward-defense stub(candidate de-index·retriever needs_review 필터·drain self-heal·review queue resolve/dismiss·(e) dedup)의 수렴점 — SoT §464(`confirmed`/`canonical` 미확정)·§465(review idempotency) 확정. 오너 결정 **D1=분리 모델**(승인=candidate `needs_review→confirmed` + canonical promotion[`source_candidate_id` 링크]; 거절=`needs_review→rejected` 무promotion)·**D2=`CANDIDATE_REMOVED` 대칭 이벤트**(색인 upsert-only stub 실경로화)·**D3=review_queue `resolved`/`dismissed`**(confirm→resolve·reject→dismiss)·**D4=idempotent 단건 전이**(replay no-op)·**D5=rejected 보존**. 결정적 백엔드 로직(라이브 불요); UI·source deep link·merge/split·부분 승인은 계속 Phase 6 UI slice 미확정. 신규 literal: `AnalysisCandidateStatus.CONFIRMED`/`REJECTED`·`IndexSyncEvent.CANDIDATE_REMOVED`·`ReviewQueueStatus.RESOLVED`/`DISMISSED`. 신설 `analysis/candidate_review.py`(`CandidateReviewService.confirm/reject`); 확장 `AnalysisService.transition_candidate`(상태 머신, `_transition_job` 미러)·repo `update_candidate`·`enqueue_candidate_removed`·`ReviewQueueService.resolve_for_candidate/dismiss_for_candidate`. HTTP `POST .../candidates/{cid}/confirm|reject`(404/409). 회귀 +22(candidate_review 12·review_queue 4·memory_api 6; candidate_index 2 stub→실경로 갱신), mutation 양방향(불법 전이 제거→3 fail·idempotency 게이트 제거→2 fail, cp 복원), pytest 748 passed/48 skip. 실 Mongo update_candidate·live de-index는 sandbox 밖 | [work log](docs/daily_logs/2026-07-11/work_log.md) **(오너 독립 검토 CONDITIONAL PASS → `docs/verifications/2026-07-11/candidate_state_transition.md` 합격)** 비차단 관찰 2건 closure: Obs1 rejected→needs_review 거부 boundary 명시+테스트·Obs2 CANDIDATE_REMOVED/UPSERTED 단일 reconcile 경로 문서화+worker routing 테스트(M3 bite) → suite 749 passed |
| 2026-07-11 | SoT v1.6.60: (e) **canonical↔candidate 승격 dedup**(브리프 `plans/04-canonical-candidate-dedup-decisions.md`, HANDOFF Next Tasks #1 (e)). v1.6.50 D7이 수용한 한계("같은 지식이 **승격 후** canonical·candidate 양쪽 노출")를 닫는다. 원인은 결정적이다: 승격 시 canonical `MemoryEntry.source_candidate_id = candidate.id`가 박히지만 candidate status는 `needs_review`로 남아(`needs_review→confirmed` 전이는 Phase 6) candidate retriever가 승격 candidate를 계속 반환. 오너 결정 **D1=승격됐으면 항상 억제**(store 권위 `find_memory_by_candidate` — canonical이 같은 package에 있든 없든; `service.py:349` D5=A "승격 candidate는 canonical 경로로만 서빙"에 부합)·**D2=지금 retrieval-time interim 억제**(context_search 조립 단계 additive, 상태 모델·색인 무변; Phase 6 candidate de-index 도입 시 상위집합 흡수 forward-defense). **성격: embedding/semantic 아님 — `source_candidate_id` identity dedup이라 라이브·캘리브레이션 불요**(서브 머신 완결 가능). 구현: `memory/service.py`에 `is_candidate_promoted` public 메서드, `context_search/service.py`에 `PromotedCandidateResolver` structural Protocol(`MemoryService` 충족) + `ContextSearchService`에 optional `promoted_candidate_resolver` 주입(**미주입 시 억제 없음 = 종전 D7 하위호환**), `_run_candidate_memory_step`이 retrieval 후 kept/suppressed 분할(suppressed는 `ExcludedHit reason="candidate_promoted"` trace), resolver 예외는 candidate step `backend_error` degrade로 접힘. `main.py`가 `promoted_candidate_resolver=memory` 배선. 계약 literal·public 응답 envelope 무변. 회귀 +6(dedup 5[승격 억제 under-strict·혼합 양방향·미승격 유지 over-strict·resolver 미주입 D7 over-strict·resolver 예외 degrade]·`is_candidate_promoted` 1[양방향+scope]), mutation 3 test 재실패 재실증, pytest 724 passed/48 skip(mongo env 제외). resolve/dismiss·candidate de-index 실경로는 Phase 6 | [work log](docs/daily_logs/2026-07-11/work_log.md) **(오너 독립 검토 조건부 합격 → `docs/verifications/2026-07-11/canonical_candidate_dedup.md` 합격)** 비차단 관찰 I1~I3 closure: I1 N>1 전부-승격 경계 회귀·I2 project scope 배선 회귀·I3 mutation 증거 보강(sha256 정확 복원·5 failed/2 passed) → 회귀 +2, suite 726 passed |
| 2026-07-10 | SoT v1.6.59: (2B.4 후속) **conflict review queue 영속화**(브리프 `plans/02b-4-review-queue-persistence-decisions.md`, HANDOFF Next Tasks #1 (d)). v1.6.44 2B.4 apply가 `conflict`(review-only, D7)를 `SKIPPED_REVIEW`로 처리하지만 어디에도 영속화하지 않아 apply 응답 이후 조정 대기 conflict를 조회·추적할 수 없던 gap을 닫는다. 오너 결정 **D1=최소 영속화만**(status=`open` 단일, resolve/dismiss 전이는 Phase 6 forward-defense — candidate needs_review-only와 동형)·**D2=GET list 엔드포인트**·**D3=결정적 id upsert**(멱등)·**D4=action generic 저장**(merge/split 스키마만). 신설 `analysis/review_queue.py`(`ReviewQueueEntry`·`ReviewQueueStatus.OPEN`·repo Protocol·InMemory·`ReviewQueueService`·`derive_review_queue_id` canonical-JSON SHA-256)·`analysis/review_queue_mongo_repository.py`(collection `review_queue`, deterministic `_id` upsert). `MemoryApplyService`에 optional `review_queue` 주입(conflict 분기 enqueue, 미주입 시 동작 불변)·`create_app` `_default_review_queue_service` 배선·`GET /projects/{id}/analysis/review-queue`. 회귀 +13(store/service 5 + apply 통합 4[under/over 양방향] + API 4), pytest 717 passed/48 skip(mongo env 제외). resolve/dismiss·merge/split·재조정 write는 후속(Phase 6) | [work log](docs/daily_logs/2026-07-10/work_log.md) **(오너 독립 감사 `docs/verifications/2026-07-10/review_queue_persistence.md` 합격)** 비차단 관찰 3건 closure: I1 over-strict 4종 parametrize·I2 conflict+ghost raise 회귀·I3 SoT §380 stale 정정 → suite 718 passed |
| 2026-07-09 | SoT v1.6.58: (b-6 후속) **ES-lexical backfill 스크립트**(브리프 `plans/04-es-lexical-backfill-decisions.md` Resolved). v1.6.57 accepted limitation #1("ES-lexical backfill 부재 → G4-B 아래 per-sink-max terminal drop된 ES 실패 수렴 수단 없음")을 닫는다. 오너 결정 **G1=A**(기존 `scripts/phase2b5_reindex_memory.py`에 ES leg 추가 — 같은 canonical 순회 memory vector+lexical 동시 backfill)·**G2=A**(candidate lexical+vector를 신규 `scripts/phase2b5_reindex_candidate.py`로 함께 — b-2 관찰 (a) candidate vector backfill 부재까지)·**G3=A**(memory/candidate 분리 스크립트)·**G4=A**(버전 bump, 새 literal 없음). 성격: 순수 운영 스크립트 + 회귀, 새 계약 literal 없음, forward-defense stub 무관(candidate needs_review 단일). helper는 v1.6.52/54/55에서 구현됨. 구현: `phase2b5_reindex_memory.py`에 `_build_memory_lexical_index`(ELASTICSEARCH_URL 시 `connect_elasticsearch_memory_index`, 아니면 InMemory fake) + `run_reindex` vector+lexical 동시 적재 + summary `vector_backend`/`lexical_backend`/`*_records_written` 개환; 신규 `phase2b5_reindex_candidate.py`(`list_needs_review_candidates` 전수 → vector+lexical). 둘 다 outbox 우회 일괄 재적재(D7), ES 미구성 시 InMemory fake. 회귀: memory summary rename+lexical 양방향 guard + 신규 candidate 회귀, mutation 양방향 4건 재실증. pytest 704 passed/45 skipped(`ConnectElasticsearchTest` 3개 sandbox `elasticsearch` 패키지 부재로 환경 의존 failed — b-5 도입·skip guard 없음·제 slice 무관·HEAD 동일). 실 backfill live는 sandbox 밖 후속 | [work log](docs/daily_logs/2026-07-09/work_log.md) |
| 2026-07-09 | SoT v1.6.57: (b-6) 증분2 — index-sync outbox **per-sink bookkeeping**(브리프 `plans/04-worker-compose-outbox-bookkeeping-decisions.md`). v1.6.52/54가 두 번 미룬 per-target bookkeeping 해소. 이전 outbox `targets`는 존재하나 **inert**(성공=entry 삭제·실패=whole-event `$set`, per-sink 미전이)라 vector·lexical 중 한 sink가 죽으면 healthy sink까지 whole-event DLQ drop됐다. 오너 결정 **G3=B**(full per-sink status + per-sink 재시도 예산)·**G4=B**(실패 sink만 재시도·성공 sink SUCCESS 동결·all-terminal 시 삭제)·**G6=A**(새 public literal 없음 — 내부 `IndexSyncStatus` + free-form target key `"vector"`/`"lexical"` 재사용). 구현: `IndexSyncTargetState`에 per-sink `attempt_count`/`last_error`·`backend` `str` 완화(ES `"elasticsearch"`)·신설 `SinkOutcome`; `_enqueue_event` targets → **빈 dict**(sink-agnostic, worker가 claim 시 materialize); composite `index_memory`/`index_candidate`(all-or-nothing raise) → `drain(entry, *, skip)`(각 sink try/except → per-sink `SinkOutcome`, skip된 SUCCESS sink 미실행); worker `_drain_sinks`(memory/candidate)가 `_merge_target_states`+`_classify_targets`로 all-SUCCESS/all-terminal 삭제·else requeue, archive는 종전 `_drain_archive` 단일-sink 경로; `record_outbox_success`/`record_outbox_failure`에 `targets`/`terminal` optional 인자(archive 후진호환); mongo round-trip str backend·per-sink 필드·requeue 시 `targets` `$set`; `_build_*_adapter` ES 무관 **항상 composite**. **성격: 성공-경로 dedup 의미 이동**(즉시 삭제→all-terminal 삭제)·Protocol per-target 표면, 새 public literal 없음. 수용 한계: ES-lexical backfill 부재 → per-sink-max drop된 ES 실패 수렴 수단 없음(후속). `test_sink_failure_propagates_not_swallowed` all-or-nothing → per-sink 격리 재lock + `PerSinkBookkeepingTest`(failing sink 자기만 requeue·healthy 미재색인·per-sink-max 삭제·transient 복구), pytest 702 passed/45 skip | [work log](docs/daily_logs/2026-07-09/work_log.md) **(오너 독립 감사 `docs/verifications/2026-07-09/outbox_per_sink_bookkeeping_b6_increment2.md` 합격)** 비차단 관찰 보강: 양-sink 동시 실패 경계 회귀 +1·per-event attempt_count backoff-only 주석 → suite 703 passed |
| 2026-07-09 | SoT v1.6.56: (b-6) 증분1 — index-sync worker를 docker-compose **상시 서비스**로 전환(브리프 `plans/04-worker-compose-outbox-bookkeeping-decisions.md`). v1.6.53(b-5) G6=A가 남긴 "worker out-of-band" gap(수동 실행이라 배포 ES `memory_lexical`/`candidate_lexical`이 안 채워짐)을 닫는다. 오너 결정 **G0=A**(in-script `--loop`+SIGTERM graceful)·**G1=A**(health port 없음 — per-call request_timeout이 hang을 bound)·**G2=A**(single-replica+graceful SIGTERM finish-in-flight; atomic outbox claim+lease(600s)가 이미 이중 실행 방지해 leader guard 불필요). **G5=A 2증분 중 증분1**(outbox 모델 미접촉; per-sink bookkeeping는 증분2 v1.6.57, G3=B/G4=B). 계약 literal 변경 없음(b-5 G6 "worker out-of-band"→"compose 서비스" posture 반전만). 구현: `indexing/service.py` `run_once` `stop_check`(claim 직전 검사→in-flight entry 완료 후 다음 경계 종료), `scripts/index_sync_worker.py` `--loop`/`--interval`/`_GracefulShutdown`/`_install_signal_handlers`/`run_loop`/`main` dispatch, compose `worker` 서비스(`restart: unless-stopped`·`stop_grace_period: 120s`·depends_on 4 sink), Dockerfile `COPY scripts/`, `.dockerignore` `scripts/` 제외 해제. 회귀 +10(실 SIGTERM→graceful flag flip end-to-end·退化·env override 포함, mutation 양방향), pytest 699 passed/45 skip. 오너 독립 감사(`docs/verifications/2026-07-09/worker_compose_increment1_b6.md`) 조건부 합격 → 전제 폐쇄: 브리프 G2 lease-release 자기모순 정정(canonical=finish-in-flight)·#8 정정(2026-07-08 "targets.chroma.status combined 반영"→inert)·HANDOFF 능동 모숸 정정·signal wiring 회귀 락(2-1)·stop_grace_period 60→120s(3-1). 실 Mongo end-to-end drain smoke PASS | [work log](docs/daily_logs/2026-07-09/work_log.md) |
| 2026-07-09 | 기획: **Phase 7(대화형 수정·아이디에이션·저작 감독)** 아이디에이션 → 정식 페이즈 승격(docs 전용, 코드/SoT 무변). 오너 아이디어(생성 글의 부분수정·아이디에이션 회의 + 맥거핀 use/skip·중요도 저작 정보관리, 생성물 DB 1급화)를 기존 아키텍처(⑤ ContextPackage·Core SOT draft/version·2B memory/scope)에 접붙여 결정 D1~D10 잠금·슬라이스 P1~P5 정리. 신규 `plans/07-conversational-authoring.md`(아이디에이션 원본 `chat-revision-ideation.md`), plans/README 인덱스·Phase/MVP 표 갱신. **순차 구현이라 Phase 5·6 이후 착수**, directive 감독면은 Phase 6와 공동 설계. 착수 브리프는 슬라이스별 구현 시점 | [work log](docs/daily_logs/2026-07-09/work_log.md) |
| 2026-07-09 | SoT v1.6.55: ⑤ §5 B 후속 — `needs_review` candidate retrieval의 **vector + lexical + hybrid(RRF)** 확장((b-2) 증분2, 브리프 `plans/04-writing-candidate-retrieval-decisions.md`). v1.6.54 색인 파이프라인 위에 canonical(v1.6.51/52)과 대칭인 retriever를 얹는다. 오너 결정 **G4=A**(vector+lexical+hybrid 한 slice). 신설 `VectorCandidateMemoryRetriever`(per candidate_type query→`get_candidate` 권위 재유도→needs_review-only·`_merge_hits` 단일 풀)·`LexicalCandidateMemoryRetriever`·`HybridCandidateMemoryRetriever`(RRF k=60, id dedup) — `.retrieve()` 반환 타입 불변이라 step/item/Gate 무변경. `main.py`의 `_build_candidate_memory_retriever`가 canonical과 동일 env 스위치로 backend 택1(없으면 Mongo-direct). 계약 literal 변경 없음. 회귀 +9(mutation 양방향), pytest 682 passed/45 skip | [work log](docs/daily_logs/2026-07-09/work_log.md) **(검증 후속 보강)** lexical retriever transitioned-candidate guard(needs_review 필터 mutation 재실증)·hybrid 단일 backend 저하 +2·SoT §3/§5B body·`CandidateMemoryRetriever` docstring 갱신 → 증분2 회귀 +9→+11, suite 689 |
| 2026-07-09 | SoT v1.6.54: ⑤ §5 B 후속 — `needs_review` candidate **색인 파이프라인** 신설((b-2) 증분1, 브리프 `plans/04-writing-candidate-retrieval-decisions.md`). candidate는 색인 표면이 없어 retrieval을 못 바꿨다 — 2B.5(canonical 색인)를 candidate에 재현. 오너 결정 **G0=A**(2증분)·**G1=A**(물리 분리 `candidate_vectors`/`candidate_lexical`, `IndexRecordKind.CANDIDATE`·`IndexSyncEvent.CANDIDATE_UPSERTED` 신설)·**G2=A**(`record_candidate(s)` choke point에서 신규 candidate만 enqueue, `AnalysisService.reindex_outbox` 주입)·**G5=A**(`derive_memory_index_text` 재사용)·**G6=A**(worker composite drain, out-of-band). 신설 `indexing/candidate_index.py`·`indexing/candidate_lexical_index.py`(ES status: needs_review 필터·nori)·`chroma.py` `ChromaCandidateVectorIndexAdapter`·`indexing/service.py` enqueue/dispatch/worker 주입·`scripts/index_sync_worker.py` `_build_candidate_adapter`. **candidate 상태는 needs_review 하나뿐이라 de-index 이벤트 없음** — 색인 upsert-only, needs_review 필터·self-heal delete는 Phase 6 forward-defense. outbox per-target bookkeeping 미룸. 회귀 +19(mutation 양방향), pytest 673 passed/45 skip | [work log](docs/daily_logs/2026-07-09/work_log.md) **(검증 후속 보강)** composite sink failure 전파·worker `_build_candidate_adapter` 3분기(G1 `candidate_vectors` collection mutation 재실증)·batch `record_candidates` enqueue(production runner.py 경로, N=1 테스트가 못 잡는 iteration mutation 재실증) +5 → 증분1 회귀 +19→+24, suite 689 |
| 2026-07-09 | SoT v1.6.53: 배포 인프라 — v1.6.52 lexical/hybrid를 **compose 배포에서 발화**시키는 전용 ES 서비스 추가((b-5), 브리프 `plans/04-compose-elasticsearch-service-decisions.md`). 계약 literal 변경 없음. 오너 결정 **G0~G6=A**: 자체 `services/elasticsearch/Dockerfile`(공식 ES 8.13.4 + analysis-nori; 공식 이미지에 nori 미포함), `docker-compose.yml` `elasticsearch` 서비스(single-node·`xpack.security.enabled=false`·`es_data` 볼륨·curl `_cluster/health` healthcheck) + application `ELASTICSEARCH_URL` 배선·`depends_on: service_healthy` → 배포 canonical retriever 기본=hybrid. `connect_elasticsearch_memory_index(request_timeout=30)`으로 cold nori create timeout 견고화(호출부 무변, 기본 상속). worker는 out-of-band 유지(G6). 실 bring-up 검증(nori 로드·plaintext·green·앱 connect_ 실 인덱스 생성). 회귀 +3(mutation 양방향), pytest 654 passed/45 skip. 오너 독립 감사 합격 → 보강: single-node 영속 yellow 방지 `number_of_replicas:0`(파생 인덱스, 실 재현 green), 브리프 G5 문구 정정 | [work log](docs/daily_logs/2026-07-09/work_log.md) |
| 2026-07-08 | SoT v1.6.52: ⑤ §5 B / §8 — Writing canonical memory retrieval의 **ES lexical(nori) + hybrid(RRF)** 확장((b-3), 브리프 `plans/04-writing-memory-lexical-retrieval-decisions.md`). v1.6.51 vector leg에 이은 lexical leg. 오너 결정 **E0/E1/E2/E4/E5/E6=A + E3=hybrid(RRF) 지금**. 신설 `indexing/memory_lexical_index.py`(`ElasticsearchMemoryIndexAdapter`·nori 매핑·`MemoryLexicalIndexSyncAdapter` worker drain·`connect_elasticsearch_memory_index`) + `LexicalCanonicalMemoryRetriever`(ES query→`get_memory` 권위 재유도→canonical-only) + `HybridCanonicalMemoryRetriever`(RRF k=60 융합) + `CompositeMemoryIndexSyncAdapter`. env 배선이 vector/lexical/hybrid/Mongo-direct 택1, seam 불변. outbox per-target bookkeeping은 미룸(worker configured-sink fan-out). 회귀 +14, 실 ES live smoke 통과, pytest 651 passed/45 skip | [work log](docs/daily_logs/2026-07-08/work_log.md) |
| 2026-07-08 | SoT v1.6.51: ⑤ §5 B 후속 — Writing canonical memory retrieval의 **vector 확장**((b), 브리프 `plans/04-writing-memory-vector-retrieval-decisions.md`). v1.6.48/v1.6.50이 위임한 canonical retrieval leg. 오너 결정 **D0=A**(canonical vector만; candidate는 색인 index 부재로 별도 slice)·**D1=A**(권위 재유도)·**D2=단일 풀 병합**(`_merge_hits` 격리)·**D3=A**(env fallback)·**D4=A**(fake 회귀, 실 Chroma 후속)·**D5=A**(ES 별도). 신설 `VectorCanonicalMemoryRetriever`(`memory_vectors` per-type query→cosine 병합→`get_memory` 재유도→canonical-only). `.retrieve()` seam 불변 → step/item/Gate 무변경. 회귀 +6(mutation 3방향), pytest 637 passed/45 skip | [work log](docs/daily_logs/2026-07-08/work_log.md) |
| 2026-07-08 | SoT v1.6.50: ⑤ §5 B 후속 — `needs_review` candidate의 Writing 포함(v1.6.48이 남긴 "바로 다음 slice"). canonical inclusion machinery 재사용. 오너 결정 **D1/D2/D3/D5/D6/D7=A + D4=B**(review_status 필드 신설) + 안전선=A(Phase 6 §62 위장 금지를 라벨+권위필드 배제로 지킴). 신설 `ContextNeed.CANDIDATE_MEMORY`(micro) + `ContextItem.review_status` + `CandidateMemoryRetriever`/`MongoDirectCandidateMemoryRetriever`(needs_review-only) + analysis `list_needs_review_candidates` + `_run_candidate_memory_step`/`_item_from_candidate`(status=candidate·candidate pointer·review_status). **Gate 재편**: candidate 금지를 폐지 아닌 candidate origin만 예외로 좁힘(analysis store 재검증; 다른 origin candidate-status는 종전대로 거부, v1.6.48 회귀 무변). candidate는 micro만·권위필드 배제(§62). 정정: needs_review→confirmed/rejected 전이는 Phase 6이라 승격 candidate 양쪽 노출 가능(D7 no-dedup). 회귀 +9, pytest 630 passed/45 skip. vector/ES retrieval·dedup·소비 강제는 후속 | [work log](docs/daily_logs/2026-07-08/work_log.md) |
| 2026-07-08 | SoT v1.6.49: 추적 부채 #8(`ProviderError`→502 패턴) 조사·폐쇄. **부채는 stale**로 확인 — `/context-search`(service `_build_plan`의 `except Exception → ContextSearchFailed(llm_error)`가 async await까지 감쌈)와 2A `/run`(엔드포인트 catch-all `except Exception → 502`) 모두 이미 502였고, 노트의 "uncaught → 500"은 catch-all 없는 compare 엔드포인트에만 해당했다. 오너 결정으로 동작 변경 없이 compare와의 대칭·가독·refactor 방어를 위해 두 경로에 명시적 `except ProviderError` 추가(2A extraction adapter에는 wrap 금지 — 400 오분류 회피). 회귀 +2(context-search async 실 ProviderError→502·`/run` 실 ProviderError→502), mutation 양방향 재실증. pytest 621 passed/45 skip. HANDOFF 추적 부채 없음으로 갱신 | [work log](docs/daily_logs/2026-07-08/work_log.md) |
| 2026-07-08 | HANDOFF.md 정리(410줄→129줄): 완료 이력/검증 실행 로그를 걷어내고 현재 상태 스냅샷으로 축약. 상단 목적 타이틀 추가, Project Structure 드리프트 교정. 이력은 CHANGELOG/daily_logs/verifications에 보존 | [work log](docs/daily_logs/2026-07-08/work_log.md) |
| 2026-07-07 | SoT v1.6.48: Writing ContextPackage에 canonical memory 포함(⑤ §5 B). 2026-07-05 D1=B(candidate 포함 Phase 2B 종속)를 Phase 2B 완료가 해소. 오너 결정 **D1=A**(canonical만, candidate는 이 machinery 재사용하는 다음 slice), **D2=A**(Mongo-direct 먼저 + retrieval/권위-재유도 분리로 vector·검색엔진 확장 대비). 신설 `ContextNeed.CANONICAL_MEMORY` + `CanonicalMemoryRetriever` seam + `MongoDirectCanonicalMemoryRetriever`(canonical-only) + `_run_canonical_memory_step`(memory→micro `ContextItem`, memory pointer) + `evaluate_context_gate` memory-item 분기(memory store로 canonical 재검증, source-block과 별도 lineage). candidate 금지 유지. 회귀 +8, pytest 618 passed/45 skip. relevance vector retrieval·candidate 포함은 후속 | [work log](docs/daily_logs/2026-07-07/work_log.md) |
| 2026-07-07 | SoT v1.6.47: Phase 2B.6(event/open_question 의미적 identity resolution). 오너 결정 **D1=A**(semantic은 2B.3 compare `_find_matches`에 — HANDOFF/2B.2의 "PriorMemoryBackend 교체"는 seam 위치 오기 정정), **D4=A**(threshold 주입값·기본 off, 실 embedding 캘리브레이션 후 발화; auto-promotion D2=B 선례). `MemoryVectorIndexAdapter.query_similar`(memory_type 필터) + `analysis/semantic_matcher.py` `EmbeddingSemanticMatcher`(쓰기와 동일 projection·embedding → top-1 ≥ threshold, self-exclusion·canonical 필터) + `AnalysisCompareService.semantic_matcher` seam. character는 결정적 유지(D5=A). event/open_question always-create 누적 오너 확인 항목 해소. 회귀 +10, pytest 608 passed/45 skip. 실 threshold 캘리브레이션은 sandbox 밖 후속 | [work log](docs/daily_logs/2026-07-07/work_log.md) |
| 2026-07-07 | SoT v1.6.46: Phase 2B.5 증분 2(라이브 배선). 오너 결정(2026-07-07): canonical 생성 3경로(apply·수동 promote·auto-promote)가 모두 재색인 enqueue. enqueue를 **`MemoryService` 단일 choke point로 중앙화**(`promote_candidate`/`_versioned_upsert` 비-replay mint 시; `MemoryReindexOutbox` 로컬 Protocol로 memory→indexing 순환 회피). 증분 1 apply-seam 흡수·제거. 실 `ChromaMemoryVectorIndexAdapter`(`memory_vectors` collection) + `create_app` 기본 배선 + worker `_build_memory_adapter` drain + `phase2b5_reindex_memory.py` backfill + `phase2b5_memory_reindex_live_smoke.py`(sandbox 밖). 회귀 +20, pytest 598 passed/45 skip | [work log](docs/daily_logs/2026-07-07/work_log.md) |
| 2026-07-07 | SoT v1.6.45: Phase 2B.5(memory→vector 재색인) 증분 1(계약+fake+회귀). 오너 결정 **D3=B**(async outbox→worker, skew 무관용 — memory 쓰기와 vector 색인을 재시도/로그로 수렴)·**D6=write-only**(semantic read=`PriorMemoryBackend` vector 교체는 후속 slice). `indexing/memory_index.py` 신설: `derive_memory_index_text`(D1 projection), 별도 `memory_vectors` collection·`IndexRecordKind.MEMORY`(D2), `MemoryIndexSyncAdapter`(D4 canonical-only self-heal). apply→`enqueue_memory_upserted`→worker `MEMORY_UPSERTED` dispatch 경로. **구현 중 D4 계약 교정**(CLAUDE.md §1): 원 추천이 2B.4 append-only(version별 새 id)와 모순임을 발견해 status/`supersedes` 기반 self-heal로 교정. 라이브 배선(실 Chroma adapter+worker/create_app+backfill+live)은 증분 2(sandbox 밖) | [work log](docs/daily_logs/2026-07-07/work_log.md) |
| 2026-07-05 | SoT v1.6.40: Phase 2B.1 canonical `MemoryEntry` store + candidate 승격. 새 패키지 `services/application/app/memory/`. status `canonical`(단일), 승격이 candidate payload/provenance/source_refs/confidence 보존 + `version=1` + 감사 필드(analysis_job_id/source_candidate_id/promotion_mode/applied_threshold). 승격 두 경로: 수동 승인(항상 canonical) + 결정적 threshold gate(`MEMORY_AUTO_PROMOTION_THRESHOLD` 주입·기본 off; threshold 이상만 자동 canonical, 미만은 candidate `needs_review` 유지). 승격 idempotency `(project_id, source_candidate_id)` unique index. HTTP: candidate promote / job auto-promote / memory 조회. 오너 결정 D2=B의 "AI 아닌 결정적 시스템 threshold gate 승격" 화해를 코드로 실현. 2B.1 경계: D3 scope key 매칭·중복 canonical 화해는 compare(2B.3)에 위임 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 real vector 백엔드 B.5 deployed live smoke 완료. 전체 compose stack에서 real embedding(`dragonkue/BGE-m3-ko`, 1024-dim) + Chroma + 실제 12B planner 관통, `backend="chroma"` rebuild와 vector hit, application/Chroma 재시작 후 hit 생존 확인. Live 중 발견한 Chroma numpy-like container truthiness bug와 Chroma volume mount 경로(`/chroma/chroma`)도 회귀/compose 보강 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | SoT v1.6.36: Phase 4 real vector 백엔드 B.4 wiring. `create_app`이 env 기반으로 real Chroma(`CHROMA_HOST`)+real embedding(`EMBEDDING_SERVICE_URL`, `dragonkue/BGE-m3-ko` 1024-dim guard)을 선택, 미설정 시 fake 폴백. rebuild summary `backend="chroma"`, compose application→embedding/chroma depends_on. embedding은 gateway와 분리돼 LLM-독립 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 real vector 백엔드 B.3: `ChromaVectorIndexAdapter`(주입형 collection, VectorIndex/VectorSearch seam + list_records, record↔chroma 직렬화, cosine/project scope/archived 제외) + compose `chroma` 서비스 + `chromadb` 의존성. 인메모리 fake collection 단위 회귀 + skip-aware live | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 real vector 백엔드 B.2: embedding 서비스 컨테이너(`services/embedding/`, FastAPI `/embed`+`/health`, `dragonkue/BGE-m3-ko` lazy 로드, base compose 서비스 + HF 캐시 볼륨). producer↔consumer round-trip 회귀로 B.1 wire 계약 드리프트 방지 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 real 영속 vector 백엔드(후보 B) 착수 브리프 승인 + B.1 embedding provider seam 구현. 오너 결정: B를 지금 앞당김(2026-07-02 "최후속" 갱신, LLM-독립이라 2-환경 제약과 맞음), Chroma vector만, embedding=`dragonkue/BGE-m3-ko`(1024-dim), 전부 컨테이너. `RemoteEmbeddingProvider`(sync httpx.Client) 추가 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | SoT v1.6.35: Phase 4 공유 in-process vector index 도입(create_app이 단일 adapter 소유, rebuild가 write·context search가 read → 같은 프로세스 rebuild 후 실제 vector hit; rebuild summary는 snapshot scope로 누적 없음 계약 유지). 오너 결정: 방향 A 채택, rebuild summary 기존 계약 유지 | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 deployed context-search smoke를 rebuild → context-search 2-step으로 확장(공유 index vector 실hit 관통 검증, exit 규칙 rebuild+search 모두 게이팅) | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-05 | Phase 4 공유 in-process vector index 결정 브리프 추가(`docs/plans/04-shared-vector-index-decisions.md`) | [work log](docs/daily_logs/2026-07-05/work_log.md) |
| 2026-07-04 | Phase 4 context search deployed smoke 스크립트(`scripts/phase4_context_search_deployed_smoke.py`) 추가 및 실제 12B stack live 통과(gateway 컨테이너 관통) | [work log](docs/daily_logs/2026-07-04/work_log.md) |
| 2026-07-04 | SoT v1.6.34: Phase 4 Slice 4.3 context search HTTP API(`POST /projects/{id}/context-search`) + `build_context_package` async wiring + create_app planner 배선 | [work log](docs/daily_logs/2026-07-04/work_log.md) |
| 2026-07-04 | SoT v1.6.33: Phase 4 Slice 4.2 터미널 JSON LLM planner adapter 구현(`TerminalJsonSearchPlanner` + `context_search_plan_v1` template + strict parse/1회 repair/literal 검증→llm_error) | [work log](docs/daily_logs/2026-07-04/work_log.md) |
| 2026-07-03 | SoT v1.6.32: Slice 4.1 독립 검증 차단 조건 폐쇄(SOT 백엔드 다운 → sot_error 매핑 + 백엔드 예외 주입 회귀 + NotFound 경로 분기 명문화) | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.31: Phase 4 Slice 4.1 context search 구현(domain 계약 + planner 주입 orchestration + Context Gate) | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.30: Phase 4 착수 브리프 승인. 사용자 결정: 터미널 JSON LLM planner 즉시 채택(tool-call planner는 전환 계획 추적), 계열 구분 error taxonomy, package 단일 schema로 시작하되 Writing/Analysis 비교용 모두 완성 의무 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | Phase 4 agentic search 착수 결정 브리프 추가 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | Phase 3B worker/retry slice 검증 후속 보강 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.29: Phase 3B one-shot index sync worker 첫 slice 구현 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.28: Phase 3B worker/retry 실행 경계 조건부 승인 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.27: Phase 3B archive outbox 검증 후속 보강 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.26: Phase 3B archive outbox 첫 code slice 구현 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-03 | SoT v1.6.25: Phase 3B sync/outbox 브리프 승인·수정 | [work log](docs/daily_logs/2026-07-03/work_log.md) |
| 2026-07-02 | Phase 3B automatic sync/outbox 결정 브리프 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | SoT v1.6.24: Phase 3A source-block stale validation 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | Phase 3A deployed rebuild smoke 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | SoT v1.6.23: Phase 3A explicit rebuild HTTP API 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | SoT v1.6.22: Phase 3A explicit rebuild script 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | SoT v1.6.21: Phase 3A source block indexing 검증 후 보강 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | SoT v1.6.20: Phase 3A source block indexing fake-adapter slice 추가 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-02 | Phase 2A deployed E2E smoke 추가 및 compose Application→Gateway wiring 보강 | [work log](docs/daily_logs/2026-07-02/work_log.md) |
| 2026-07-01 | SoT v1.6.19: Phase 2A source_ref catalog anchor repair 보강 | [work log](docs/daily_logs/2026-07-01/work_log.md) |
| 2026-07-01 | SoT v1.6.18: Phase 2A source_ref catalog HTTP API 추가 | [work log](docs/daily_logs/2026-07-01/work_log.md) |
| 2026-07-01 | SoT v1.6.17: Phase 2A Application-side JSON repair retry 추가 | [work log](docs/daily_logs/2026-07-01/work_log.md) |
| 2026-07-01 | SoT v1.6.16: Phase 2A provider/Gateway runner factory wiring 첫 구현 slice 추가 | [work log](docs/daily_logs/2026-07-01/work_log.md) |
| 2026-07-01 | SoT v1.6.15: Phase 2A provider/Gateway wiring pre-implementation 결정 승인 | [work log](docs/daily_logs/2026-07-01/work_log.md) |
| 2026-06-30 | SoT v1.6.14: Slice 1 draft version export 추가(plain text/Markdown, version 추적·body verbatim). 사용자 결정: 막힌 Next Tasks 대신 export 진행, 형식은 plain text + Markdown | [work log](docs/daily_logs/2026-06-30/work_log.md) |
| 2026-06-30 | SoT v1.6.13: Gemma Q4 benchmark 기반 AgentLoopRunner budget/retry 기본값 확정 | [work log](docs/daily_logs/2026-06-30/work_log.md) |
| 2026-06-30 | SoT v1.6.12: Phase 2A analysis run endpoint 추가 | [work log](docs/daily_logs/2026-06-30/work_log.md) |
| 2026-06-30 | SoT v1.6.11: Phase 2A analysis job/candidate HTTP API 추가 | [work log](docs/daily_logs/2026-06-30/work_log.md) |
| 2026-06-29 | SoT v1.6.10: job-state slice 2 검증 조건 폐쇄(state-agnostic replay + base schema_invalid 회귀) + all-or-nothing 범위 명확화 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A job 상태 전이 runner 통합(slice 2): 새 job만 실행·실패 지점→failure_reason 매핑·replay 비재실행 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A job-state slice 1 검증 조건 폐쇄(failure_reason enum 5종 + skip-aware live job-state 회귀) | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | SoT v1.6.9: Phase 2A job 상태 전이/실패 저장 계약 승인 + 상태 전이 slice 1 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | SoT v1.6.8: Phase 2A candidate write-error 분류 정밀화(duplicate-key 한정 매핑) | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A Analysis Mongo persistence 검증 후 보강 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A Analysis Mongo persistence 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A runner 검증 후 보강(F1/F3) | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A extraction runner 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A source_anchors set 의미론 보강 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A logical_key anchor-order idempotency 보강 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A 최소 taxonomy schema + fake-provider extraction adapter 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A Snapshot Loader + source validation 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | Phase 2A domain model + in-memory repository 구현 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-29 | SoT v1.6: Phase 2A 착수 최소 계약 승인 | [work log](docs/daily_logs/2026-06-29/work_log.md) |
| 2026-06-28 | Gemma Q4 benchmark harness 추가 | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | SoT v1.5.1: Mongo index setup 계약 명확화 | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | Core SOT reusable fixture 추가(plan 01 #7 완료) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | LLM Gateway client container compose 편입 | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | archive API endpoint 추가(DELETE=archive, CRUD API 완성) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | SoT v1.5: archive 읽기전용 명문화 + source_ref carve-out | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | project/draft rename API 추가(CRUD 수정 완성) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | version read API 추가(version/snapshot 재조회 표면) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | project/draft list/get API 추가(Core SOT round-trip 완성) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | SourceRef persistence 추가로 Slice 1 마무리(§113/R3 폐쇄) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | Application + MongoDB(replica set) Docker 런타임 추가 | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | SoT v1.4: non-transaction fallback single-writer 제약 명시(R2) | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-28 | Core SOT MongoDB adapter + transaction-backed repository 추가 | [work log](docs/daily_logs/2026-06-28/work_log.md) |
| 2026-06-26 | Core SOT 검증 보강(C1~C3) | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-26 | Core SOT 최소 구현 골격 추가 | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-26 | Core SOT persistence/retention 계약 승인 | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-26 | Core SOT text/reference 계약 승인 | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-26 | Slice 1 실행 경계 승인 | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-26 | System Contract SoT v1.0 승인 | [work log](docs/daily_logs/2026-06-26/work_log.md) |
| 2026-06-25 | AgentLoopRunner provider composition slice 구현 | [work log](docs/daily_logs/2026-06-25/work_log.md) |
| 2026-06-25 | AgentLoopRunner A3 검증 후 보강(I1/I3) | [work log](docs/daily_logs/2026-06-25/work_log.md) |
| 2026-06-25 | AgentLoopRunner A3 decision 합성 회귀 구현 | [work log](docs/daily_logs/2026-06-25/work_log.md) |
| 2026-06-25 | AgentLoopRunner A2 registry 계약 회귀 구현 | [work log](docs/daily_logs/2026-06-25/work_log.md) |
| 2026-06-24 | 개발 계획 문서 구조 도입 | [work log](docs/daily_logs/2026-06-24/work_log.md) |

## 2026-07-03

### Changed

- Phase 3B worker/retry slice 독립 검증(합격) 후속 보강: `tests/test_indexing_mongo.py`에 worker lifecycle live smoke 4개(success terminal-move / backend_error 1분→5분→terminal / stale running reclaim `attempt_count` 미소비 / terminal→reenqueue 새 요청)를 추가해 Mongo claim/retry/terminal-move 경로를 CI에서 잠갔다. throwaway `mongo:7`에서 live 7개가 통과했다. 브리프 §5·Owner 결정의 `not_found` retry 문구를 query-time(3회, 후속 selector)/archive worker-time(idempotent success)으로 정정해 브리프 내부 모순을 제거했다(결정 변경 없음).
- SoT를 v1.6.29로 갱신해 Phase 3B one-shot index sync worker 첫 slice를 반영했다. Worker는 `pending` 또는 stale `running` outbox entry를 claim하고, recording-only fake archive mutation을 실행한 뒤 `index_sync_logs`에 attempt result를 append한다. `claimed_at`으로 10분 claim timeout을 판정하고, backend failure는 1분 → 5분 backoff 뒤 terminal `failed`가 된다. Terminal 이동을 채택해 `success|failed`가 되면 active outbox entry를 제거하고 history는 `index_sync_logs`가 소유한다. Archive worker-time `not_found`는 idempotent success다.
- SoT를 v1.6.28로 갱신해 Phase 3B worker/retry 실행 경계를 조건부 승인 상태로 반영했다. 첫 worker는 one-shot command로 구현하고, 장기적으로 UI-triggered background/daemon이 같은 service를 재사용할 수 있게 둔다. Claim timeout은 10분이고 `claimed_at` lease timestamp로 stale running을 판정한다. Backoff는 1분 → 5분 → terminal `failed`, `backend_error`/query-time `not_found`는 둘 다 `max_attempts=3`을 사용한다. Status-aware dedup은 `pending|running` active entry에만 적용하되, terminal-location/index 전략과 archive worker-time `not_found` 처리는 구현 전 오너 결정으로 남겼다.
- SoT를 v1.6.27로 갱신해 Phase 3B archive outbox 독립 검증 후속 보강을 반영했다. `index_sync_outbox`를 `mongo_collections.md` 운영 collection 레지스트리에 추가했고, Mongo outbox document 직렬화/역직렬화를 fake collection round-trip 회귀로 잠갔다. `analysis_completed`가 아직 code enum에 열리지 않았음도 명시 회귀로 고정했다.
- SoT를 v1.6.26으로 갱신해 Phase 3B archive outbox 첫 code slice를 반영했다. Application archive endpoint는 Core SOT archive 성공 후 `index_sync_outbox` pending entry를 idempotent하게 생성한다. Event는 `project_archived`/`draft_archived`, dedup key는 `(project_id, event, source.mongo_collection, source.mongo_id)`, target envelope는 `targets.chroma.status="pending"` + `targets.chroma.backend="in_memory_fake"`다.
- `IndexSyncOutboxEntry` 모델과 in-memory/Mongo repository skeleton을 추가했다. Mongo repository는 `index_sync_outbox` dedup index와 future `index_sync_logs` join index를 설치한다.
- Retry metadata는 `attempt_count`, `max_attempts=3`, `next_attempt_at`, `last_error`로 시작한다. 서버/backend 계열 오류와 데이터 없음/not-found 계열 오류는 `backend_error`, `not_found`로 분리한다. Retry 실행, backoff 숫자, worker claim, actual ChromaDB/Elasticsearch mutation은 후속이다.
- SoT를 v1.6.25로 갱신해 Phase 3B automatic sync/outbox 브리프의 owner 결정을 반영했다. 첫 automatic event source는 archive events(`project_archived`, `draft_archived`)로 시작하고, 오너가 장기적으로 더 맞는 흐름으로 본 `analysis_completed`는 후속 event candidate로 열어 둔다.
- Delivery는 로컬 1인 프로젝트 기준 외부 queue 없이 Mongo outbox/polling으로 시작한다.
- 저장 단위는 단일 `index_sync_logs`가 아니라 `index_sync_outbox` + `index_sync_logs` 분리로 확정했다. 두 collection은 `sync_request_id`로 조인한다.
- 빠져 있던 브리프 항목 4/5/6을 보강했다. Archive 반영은 첫 slice에서 outbox 기록 + stale validation guard로 제한하고, worker slice에서는 tombstone/status update를 우선 검토한다. Retry/backoff는 outbox schema가 bounded retry metadata를 수용하되 실행·숫자는 worker slice로 둔다. 실제 Chroma/Elasticsearch adapter는 persistent outbox/log 계약 뒤로 미룬다.

사용자 결정: `analysis_completed`가 장기적으로 가장 맞는 event source처럼 보이지만, 지금은 Phase 3A의 archive stale gap을 먼저 줄이기 위해 archive events부터 구현한다. 외부 queue 운영 확장성은 로컬 1인 프로젝트 단계에서 고려하지 않는다. 상태 필드는 계속 늘어날 가능성이 있으므로 queue lifecycle과 result history를 단일 collection에 섞지 않고 분리한다.

사용자는 archive 반영 방식으로 C first, B later를 승인했다. 즉, 지금은 outbox 기록과 stale validation guard로 안전성을 확보하고, 정리는 가능하도록 후속 worker/adapter slice에서 tombstone/status update를 우선 검토한다. Retry는 bounded local metadata를 채택하고, 서버/backend 오류와 not-found 오류를 다른 error type으로 분리하되 둘 다 기본 3회로 시작한다. Fake vector 단계는 persistent outbox/log 계약만 먼저 만들고 품질·embedding·actual adapter는 핵심 코어 완료 뒤 최후속으로 둔다.

사용자는 worker/retry 구현 전에 브리프를 먼저 확정하고 다음 작업자가 활용할 수 있게 장단점을 남기기로 했다. Worker는 one-shot command로 시작하되 장기적으로 서비스 UI와 결합된 background/daemon이 같은 service를 재사용할 수 있게 둔다. Docker/Compose restart가 DB의 `running` 상태를 자동 회수하지 않는다는 이유로 claim timeout 10분을 채택했고, backoff는 1분 → 5분이다. 독립 검증 후 terminal-location/index 전략과 archive worker-time `not_found` 처리는 별도 결정 항목으로 분리했고, 아래 사용자 결정으로 닫았다.

사용자는 terminal-location/index 전략으로 B를 채택했다. 즉, outbox는 active queue로 두고 `success|failed` terminal history는 `index_sync_logs`가 소유한다. Archive worker-time `not_found`도 B를 채택해 archive/tombstone/delete 대상 record가 이미 없으면 idempotent success로 처리한다. Query-time `not_found`는 별도 selector/orchestration retry loop의 error type으로 남긴다.

## 2026-07-02

### Changed

- Phase 3B automatic sync/outbox 결정 브리프를 추가했다. 추천안은 archive events(`project_archived`, `draft_archived`)를 첫 automatic source로 삼고, inline adapter 호출/외부 queue 대신 Mongo `index_sync_logs` pending outbox entry를 먼저 생성하는 방향이다. 아직 owner-approved SoT/public contract가 아니므로 code slice는 승인 후 진행한다.
- Phase 3B automatic sync/outbox 결정 브리프 검증 권고를 반영했다. 승인 전 schema lock 항목으로 기존 `success` literal 재사용, canonical `targets` shape, required `project_id`, outbox dedup key, §64 stale-hit job과 Phase 3A `content_hash` 기준의 긴장을 명시했다.
- SoT를 v1.6.24로 갱신해 Phase 3A source-block index hit stale validation을 추가했다. `validate_source_block_record(record)`는 Core SOT 정본을 재조회해 `project_archived`, `draft_archived`, `snapshot_missing`, `draft_mismatch`, `content_hash_mismatch`, `block_missing` reason으로 hit 사용 가능 여부를 판정한다. 이 guard는 automatic sync가 아니라 query/Context Gate 계층이 hit 사용 전에 호출하는 방어선이다.
- SoT를 v1.6.23으로 갱신해 Phase 3A explicit rebuild HTTP API를 추가했다. `POST /projects/{project_id}/snapshots/{snapshot_id}/index/source-blocks/rebuild`는 deterministic fake vector adapter rebuild summary를 반환하며 `backend`는 `in_memory_fake`다. Persistent vector backend와 automatic sync는 후속이다.
- SoT를 v1.6.22로 갱신해 Phase 3A explicit rebuild script 계약을 추가했다. `scripts/phase3a_rebuild_source_block_index.py`는 `project_id + snapshot_id`를 받아 Core SOT MongoDB에서 source blocks를 읽고 deterministic fake vector adapter rebuild summary를 JSON으로 출력한다. Exit code는 full write 성공 0, partial write 1, usage/config/domain error 2다. Application HTTP API endpoint와 persistent vector backend는 후속이다.
- SoT를 v1.6.21로 갱신해 Phase 3A `IndexSyncRequest`/`IndexSyncResult` reduced shape와 `contracts.md` §7.3 persistent sync log/outbox envelope의 관계를 명시했다. 검증 F2 대응으로 draft-only archive query exclusion 회귀를 추가했고, archive/status filter가 explicit rebuild 시점 metadata 기준임을 명확화했다.
- SoT를 v1.6.20으로 갱신해 Phase 3A source block indexing 첫 slice를 추가했다. 첫 target은 Core SOT source block only이며, backend는 Chroma-like vector contract with deterministic fake adapter, embedding은 fake provider only, delivery는 explicit snapshot rebuild, archive/delete 반영은 status/version filter다.

### Added

- Phase 3A deployed rebuild smoke 스크립트를 추가했다. `scripts/phase3a_deployed_rebuild_smoke.py`는 이미 실행 중인 Application HTTP endpoint로 project/draft/version snapshot을 준비하고 HTTP rebuild endpoint를 실행한다. `--mongo-uri`가 있으면 같은 snapshot을 CLI rebuild 경로로도 읽어 HTTP/CLI summary 일치를 확인한다.
- Phase 2A 배포형 E2E smoke 스크립트를 추가했다. `scripts/phase2a_deployed_e2e_smoke.py`는 이미 실행 중인 Application HTTP endpoint를 대상으로 project/draft/version 저장, source_ref catalog 준비, analysis job run, candidate read-back을 수행한다.
- Compose runtime에서 Application이 Gateway 컨테이너를 실제 네트워크로 호출하도록 `LLM_GATEWAY_BASE_URL=http://gateway:8001` 기본 env를 추가했다. Host port는 `APPLICATION_PORT`/`GATEWAY_PORT`/`MONGO_PORT`로 override 가능해 로컬 포트 충돌을 피할 수 있다.

### Verified

- Phase 3A deployed rebuild smoke를 실제 compose stack에서 실행해 HTTP rebuild summary와 CLI rebuild summary가 같은 snapshot 기준으로 일치함을 확인했다. 실행 중이던 Application 이미지가 오래돼 첫 smoke는 404였고, 같은 포트 override로 `docker compose up -d --build application` 후 `summaries_match=true`, HTTP/CLI `records_attempted=2`, `records_written=2`, `records_query_visible=2`를 확인했다.
- 실제 compose stack과 llama.cpp endpoint `http://192.168.1.29:9080`로 배포형 E2E를 실행해 `run_http_status=200`, final job `succeeded`, candidates 3개를 확인했다. 모델 처리 속도를 고려해 `LLAMA_TIMEOUT_SECONDS=900` 및 smoke client timeout 1000초로 리턴 시그널을 기다렸다.

## 2026-07-01

### Changed

- SoT를 v1.6.19로 갱신해 Phase 2A repair retry가 valid JSON이지만 source_ref catalog anchor literal을 보존하지 못한 출력도 1회 repair 대상으로 삼도록 보강했다. Adapter는 parsed candidate의 `source_ref_id`, span, quote, content_hash를 입력 catalog와 대조해 mismatch가 있으면 repair prompt를 재호출한다. repair 후에도 mismatch가 남으면 성공으로 보정하지 않고 기존 source validation 경계가 `source_invalid`를 보존한다.
- SoT를 v1.6.18로 갱신해 Phase 2A source_ref catalog 준비용 Application HTTP API를 추가했다. `POST /projects/{project_id}/snapshots/{snapshot_id}/source-refs`, `GET /projects/{project_id}/snapshots/{snapshot_id}/source-refs`, `GET /projects/{project_id}/source-refs/{source_ref_id}`를 열어 snapshot span에서 source_ref를 만들고 같은 project 안에서만 catalog/ref를 조회한다. invalid span은 400, missing/cross-project snapshot/ref는 404이며 archived project에서도 source_ref 생성·조회는 허용된다.
- SoT를 v1.6.17로 갱신해 Phase 2A Application-side JSON repair retry를 추가했다. `/v1/generate` 경로는 유지하고, `VersionedPromptAnalysisExtractionAdapter`가 strict parser 실패 시 원문 output/parser error/original prompt payload로 repair prompt를 1회만 재호출한다. Repair output도 같은 Phase 2A strict parser/source validation/candidate schema를 통과해야 하며 실패하면 `schema_invalid`로 보존된다.
- SoT를 v1.6.16으로 갱신해 Phase 2A provider/Gateway runner factory wiring 첫 구현 slice를 추가했다. Core SOT source_ref catalog read surface, versioned prompt template DB, `analysis_extract_v1` prompt builder, `/v1/generate` 기반 Application→Gateway provider adapter, env 기반 default runner wiring을 추가했다. `LLM_GATEWAY_BASE_URL`이 없으면 기존 pending run 503 behavior를 유지한다.
- SoT를 v1.6.15로 갱신해 Phase 2A provider/Gateway wiring pre-implementation 결정을 승인했다. 첫 실제 provider wiring은 tool-call 없는 terminal JSON extraction으로 진행하고, 모델은 입력 source_ref catalog의 id를 선택한다. Prompt template은 DB에 저장해 versioned 관리하며 첫 prompt version은 `analysis_extract_v1`, task_type은 `analysis_extract`다.

사용자 결정: source_ref 후보는 글쓰기 프로그램 특성상 정적/기계적으로 anchor 선택이 가능하므로 모델이 새 source_ref를 만들 필요가 없다. Prompt/template은 변경과 version 추적, agentic loop context/ContextPackage 체계화를 위해 DB 저장 방향을 택한다. Gateway 호출 surface는 `/v1/generate` 임시 사용과 `/v1/generate-structured` 최소 구현 비용을 확인한 뒤 더 나은 쪽으로 진행한다.

## 2026-06-30

### Added

- Gemma Q4_0 llama.cpp live benchmark report를 추가했다. `docs/benchmarks/2026-06-30/gemma_q4_llama_cpp_repeats3_warmup1.json`은 endpoint `http://192.168.1.29:9080`, model `google/gemma-4-12B-it-qat-q4_0-gguf:Q4_0`, `repeats=3`, `warmups=1` 결과를 담는다.
- Phase 2A analysis run endpoint를 추가했다. `POST /projects/{project_id}/analysis/jobs/{job_id}/run`은 pending job만 injected runner로 실행하고 요청 안에서 await해 terminal job과 candidate 목록을 반환한다. `running`/`succeeded`/`failed` job은 재실행하지 않고 `idempotent_replay=true`로 현재 상태와 저장된 candidate를 반환한다. 기본 runtime은 아직 실제 provider/Gateway runner를 구성하지 않으므로 pending run은 runner 미구성 시 503이며, Gateway runtime wiring과 source_ref 자동 생성은 별도 slice로 남겼다.
- Phase 2A analysis job/candidate HTTP API를 추가했다. `POST /projects/{project_id}/analysis/jobs`는 job을 `project_id + snapshot_id + idempotency_key` 기준으로 idempotent 생성/replay하고, `GET /projects/{project_id}/analysis/jobs/{job_id}`와 `GET /projects/{project_id}/analysis/jobs/{job_id}/candidates`는 job 상태와 저장된 candidate를 읽는다. 이 surface는 runner나 Gateway 호출을 시작하지 않으며, 존재하지 않는 project와 cross-project job/candidate 접근은 404로 잠갔다.

### Fixed

- `scripts/benchmark_llm_provider.py`를 file path로 직접 실행할 때 repo package import가 실패하던 문제를 보강하고 subprocess 회귀를 추가했다.
- Phase 2A run endpoint 독립 검증 조건을 폐쇄했다. `/run` HTTP 경로의 duplicate conflict 409, provider/기타 exception 502, `snapshot_not_found` 404를 named regression으로 추가하고, `snapshot_not_found` 404를 SoT v1.6.12 계약에 명시했다.

### Changed

- SoT v1.6.13과 `docs/plans/flat-loop-gate.md`에 AgentLoopRunner production budget/retry 기본값을 확정했다. 초기 local MVP 기준 기본값은 benchmark p95/max token에 여유를 둔 `analysis_compare`, `context_search`, `writing_generate` profile별 상한이다.

## 2026-06-29

### Changed

- SoT를 v1.6으로 갱신해 Phase 2A 착수 최소 계약을 승인했다. 최소 taxonomy는 `character_observation`, `event_observation`, `open_question_observation` 3종으로 시작하되 후속 확장성을 열어 둔다. provenance는 `source_observed`/`ai_inferred`만 허용하고 `user_declared`는 WritingBrief/Product Shell 이후로 미룬다. Phase 2A candidate action은 `create` only, status는 `needs_review`, confidence는 `0.0 <= confidence <= 1.0` 범위만 강제한다. `create_source_ref` primitive는 non-idempotent로 유지하고 candidate/job 저장층이 retry idempotency를 소유한다. 2A와 2B는 별도 milestone로 분리한다.

사용자는 추천 3종 taxonomy를 채택하되 확장성을 염두에 둘 것을 요청했다. provenance는 사용자 위임에 따라 작업자가 보수적으로 결정해 WritingBrief 이전에는 `user_declared`를 넣지 않는다.

### Added

- Phase 2A Analysis Mongo persistence를 추가했다. `MongoAnalysisRepository`는 `analysis_jobs`, `analysis_tasks`, `analysis_candidates`를 저장하고, job/task/candidate idempotency를 required unique index(`uniq_analysis_job_request`, `uniq_analysis_task_request`, `uniq_analysis_candidate_request`)로 강제한다. Runner candidate write는 service batch API를 통해 저장소에 전달되며, Mongo transaction 경로는 한 트랜잭션으로 commit하고 non-transaction fallback은 single-writer local/test 전용으로 실패 시 이번 시도 candidate만 rollback한다. Live Mongo 통합 테스트는 미가용 시 skip되고, index setup은 fake collection 단위 회귀로 잠갔다.
- Phase 2A extraction runner를 추가했다. Runner는 `AnalysisJob`을 idempotent 생성/재사용하고 Snapshot Loader → provider extraction → task 생성/재사용 → 전체 draft 사전 검증 → candidate 저장 순서로 실행한다. Task는 `project_id + job_id + candidate_type`으로 재사용하므로 같은 job retry가 candidate idempotency의 `task_id`를 흔들지 않는다. logical_key/source/schema validation 실패가 하나라도 있으면 candidate write를 시작하지 않는다. focused 37개와 전체 discovery 260개(27 skip)를 통과했다.
- Phase 2A domain model과 in-memory repository를 추가했다. `services/application/app/analysis/`가 `AnalysisJob`/`AnalysisTask`/`AnalysisCandidate`, approved literal enum, `AnalysisRepository` Protocol, `InMemoryAnalysisRepository`, `AnalysisService`를 제공한다. job retry는 `project_id + snapshot_id + idempotency_key`, candidate retry는 `project_id + task_id + logical_key`로 idempotent replay한다. Candidate status는 `needs_review` 고정이고 confidence는 range만 검증한다. `user_declared` 같은 미승인 provenance 문자열은 runtime에서 거절한다. 독립 검증 조건 보강으로 NaN confidence를 거절하고 action≠`create` 회귀와 `logical_key` 계약을 추가했다. focused 18개 회귀와 전체 discovery 241개(27 skip)를 통과했다.
- Phase 2A Snapshot Loader와 candidate source validation을 추가했다. `CoreSotSourceAdapter`가 Core SOT snapshot raw text/hash/block ids를 analysis 입력으로 로드하고, `AnalysisService`가 resolver 구성 시 `CandidateSourceAnchor(source_ref_id, start_offset, end_offset, quote, content_hash)`를 실제 `SourceRef`와 대조한다. 같은 project source_ref만 허용하며 source_ref 없음, cross-project, span/quote/hash mismatch, source_ref_ids-anchor mismatch, source_anchors 누락을 거절한다. focused 25개 회귀와 전체 discovery 248개(27 skip)를 통과했다.
- Phase 2A 최소 taxonomy schema와 fake-provider extraction adapter를 추가했다. Payload는 `character_observation {name, observation}`, `event_observation {event}`, `open_question_observation {question}`만 허용하고 모든 field는 non-empty string이다. `AnalysisExtractionAdapter`는 fake/provider content의 top-level `{candidates: [...]}` JSON object를 파싱해 approved type/provenance/confidence/source_anchors/payload를 검증하고, `candidate_type + payload + source_anchors` canonical JSON SHA-256으로 deterministic `logical_key`를 만든다. focused 30개 회귀를 통과했다.

### Fixed

- Phase 2A Analysis Mongo persistence 독립 검증 조건을 보강했다. Live Mongo replica set 실행에서 `insert_many` duplicate가 `BulkWriteError`로 누출되는 것을 확인해 `DuplicateAnalysisCandidateRequest`로 매핑했고, fallback/transaction 통합 6개를 실제 Mongo에서 통과시켰다. `record_candidates()`의 intra-batch 동일 `project_id + task_id + logical_key` request는 idempotent replay로 정규화한다고 SoT v1.6.7/plan에 명시하고 focused 회귀를 추가했다.
- Phase 2A runner의 candidate 저장 단계를 batch API로 바꿔 Mongo repository가 all-or-nothing 경계를 소유할 수 있게 했다. 단건 `record_candidate()`는 batch API의 1건 wrapper로 유지해 기존 호출 표면은 보존한다.
- Phase 2A runner 독립 검증의 non-blocking F1/F3을 보강했다. Runner는 source validation이 구성된 `AnalysisService`만 받으며, 같은 run의 duplicate `(task_id, logical_key)` draft는 result/write에서 1개로 정규화한다. focused 39개와 전체 discovery 262개(27 skip)를 통과했다. 당시 이월된 F2(Mongo all-or-nothing)는 이후 Analysis Mongo persistence slice에서 닫았다.
- 로컬 기본 Mongo가 인증을 요구하는 환경에서 `tests/test_core_sot_mongo.py` probe cleanup이 `Unauthorized`로 import 실패하던 문제를 보강했다. cleanup 실패도 Mongo 미가용으로 보고 skip하므로 infrastructure-free 전체 discovery가 다시 통과한다.
- Phase 2A slice2 독립 검증 G1을 보강했다. `logical_key` derivation에서 같은 `source_anchors` set은 provider 출력 순서와 무관하게 같은 identity로 정규화하도록 SoT v1.6.2와 plan에 명시하고, extractor가 canonical anchor를 정렬하도록 수정했다. anchor set 순서 무관 회귀를 추가해 focused 31개와 전체 discovery 254개(27 skip)를 통과했다.
- Phase 2A slice3 독립 검증의 non-blocking D1~D3을 보강했다. SoT 상단 계약 버전을 v1.6.3으로 갱신하고, `source_anchors` identity를 unordered set으로 명확화해 동일 anchor 중복을 parsed draft와 logical_key에서 하나로 정규화한다. ordered evidence chain이 필요해지면 `sequence`/`evidence_order` 같은 명시 필드로 후속 계약화한다. focused 32개와 전체 discovery 255개(27 skip)를 통과했다.

## 2026-06-28

### Added

- Gemma Q4 benchmark harness를 추가했다. `scripts/benchmark_llm_provider.py`는 기존 `HttpxJsonTransport` + `LlamaCppProvider` 계약으로 외부 llama.cpp-compatible endpoint에 `short_smoke`, `json_extraction`, `continue_scene` case를 반복 호출하고, latency/token/error summary와 raw run을 JSON으로 출력한다. 실제 live endpoint 수치 없이 budget/retry production 기본값을 추측하지 않기 위해 harness만 추가했으며, 기본값 확정은 benchmark report 이후로 남긴다. 검증 후 보강으로 CLI fake-runner wiring, warmup failure의 `iteration=0` 기록, report field schema 명시와 raw run field assertion을 추가했다.
- Core SOT 후속 Phase 재사용 fixture를 추가해 plan 01 최소 산출물 #7을 채웠다. `tests/fixtures/core_sot.py`가 deterministic raw text, expected content hash, block matrix, source_ref span, `build_core_sot_fixture()`를 제공하고, `tests/test_core_sot_fixture.py`가 snapshot/block/source_ref/idempotent replay 계약을 잠근다. 검증 후 보강으로 `ExpectedBlock.block_index`를 direct field로 추가하고, 한글 multibyte raw text로 Unicode code point offset 회귀를 추가했다. Phase 2 schema가 아직 미정이므로 analysis candidate fixture는 만들지 않았다.
- LLM Gateway를 compose에 독립 `gateway` 서비스로 편입했다. 이 서비스는 llama.cpp 서버를 띄우지 않고, `LLAMA_BASE_URL`로 지정한 외부 llama.cpp-compatible endpoint의 클라이언트로만 동작한다. 기본값은 repo-local Docker host 기준 `http://host.docker.internal:9080`이고, 이전 live smoke의 `192.168.1.29:9080` 같은 머신별 주소는 env override로만 사용한다. `services/llm_gateway/Dockerfile`은 dependency manifest를 먼저 설치한 뒤 `services/`를 복사해 캐시 순서를 보존한다. `services/llm_gateway/app/main.py`는 `/health/live`, upstream readiness용 `/health/ready`, provider 계약을 쓰는 `POST /v1/generate`를 제공한다. focused gateway/provider/httpx 18개 회귀, `docker compose config`, `COMPOSE_BAKE=false docker compose build gateway`, gateway container liveness/health를 확인했다. 검증 후 보강으로 provider error 5종→HTTP status 매핑을 모두 lock하고, application API TestClient hang을 ASGITransport 기반 test harness로 해소했다.
- archive API endpoint를 추가해 Core SOT CRUD를 API로 완성했다. `DELETE /projects/{id}`와 `DELETE /projects/{id}/drafts/{draft_id}`를 archive(soft delete)로 매핑했다(계약 §115 "삭제는 archive로 처리"). archived 엔티티를 200으로 반환하고, 없음/missing draft/cross-project draft는 404, project·draft 재archive는 idempotent(상태전이는 쓰기차단 대상 아님)다. service의 archive_project/archive_draft를 재사용하며 SOT/계약 변경은 없다. 독립 재검증(archive_api_endpoint.md) 대응으로 archived project 하위 draft archive 허용 분기를 over-strict guard로 lock(mutation 증명)하고 §115 :121에 "하위 draft archive도 상태전이 예외"를 비의미 명확화했다. 전체 207개(Mongo 미연결 27 skip / 연결 시 전통과).

### Changed

- SoT를 v1.5.1로 갱신해 Mongo adapter setup 계약을 명확화했다. required query index는 `uniq_save_request`와 `blocks_by_snapshot`이며, MongoDB가 required index 생성을 거부하면 `MongoRepositorySetupError`로 표면화한다. 검증 O2가 지적한 `source_refs_by_snapshot`은 현재 query path가 없는 speculative/dead index라 제거했다. source_ref by-snapshot 조회 API가 생기기 전까지 해당 index는 required contract가 아니다.
- SoT를 v1.5로 갱신해 archive를 읽기 전용 상태로 명문화했다(rename_api.md R1, 사용자 결정). archived project/draft는 읽기(get/list, version/snapshot 재조회) 허용, 본문 쓰기(draft 생성·version 저장)와 메타데이터 수정(rename)은 차단(409), SOT 본문은 archive 무관 항상 불변, unarchive/상태전이는 차단 범위 밖("archived 동안 차단"으로 한정). `source_refs` 생성은 immutable snapshot 파생 주석이라 archived에서도 허용(carve-out)하며 회귀로 잠갔다. 사용자가 제안한 write-level 다단계(강제/수정/일반) 모델은 검토 후 과설계(YAGNI, archived에서 다른 쓰기는 source_ref 1개뿐)로 채택하지 않고 연산 카테고리 prose로 정리했다. 기존 구현·회귀와 정합하는 문서 명문화이며 코드 변경은 carve-out 회귀 1건뿐이다.

### Added

- project/draft rename API를 추가해 Core SOT CRUD의 "수정"을 완성했다(plan 01 L13–14 범위 중 마지막 미구현). `PATCH /projects/{id}`(name)와 `PATCH /projects/{id}/drafts/{draft_id}`(title)를 추가하고 service에 `rename_project`/`rename_draft`를 더했다. 없음/cross-project draft는 404, archived project/draft의 rename은 409(기존 save_draft/create_draft와 일관된 archive=쓰기차단 계약). repo/SoT 계약 변경 없음. archive API endpoint는 후속(현재 archive는 service-only). 전체 199개(Mongo 미연결 27 skip / 연결 시 전통과).
- version read API를 추가해 version/snapshot 재조회 public 표면을 열었다. `GET /projects/{id}/drafts/{draft_id}/versions`(version_number 순 목록)와 `GET .../versions/{version_id}`(단건 full read-back: snapshot raw_text + blocks text)를 추가하고, service/repository에 `list_versions`/`list_draft_versions`/`get_draft_version`과 `DraftVersionDetail`을 더했다. version은 project_id·draft_id 일치를 강제하고(없음/cross-draft 404), 메타 payload에서 `idempotency_key`는 내부 save 토큰이라 의도적으로 제외했다. SoT 계약 변경 없음. 전체 190개(Mongo 미연결 25 skip / 연결 시 전통과).
- project/draft list/get API를 추가해 Core SOT round-trip을 완성했다. `GET /projects`, `GET /projects/{id}`, `GET /projects/{id}/drafts`, `GET /projects/{id}/drafts/{draft_id}`를 추가하고, service/repository에 `list_projects`/`list_drafts`와 격리된 `get_project`/`get_draft`를 더했다. project_id 격리(다른 프로젝트 draft 미노출), 없음/cross-project 404, persisted round-trip을 API + Mongo 양 경로 회귀로 잠갔다. 응답 shape는 기존 create와 동일하며 rename(수정)·version read는 후속으로 남겼다. SoT 계약 변경 없음(canonical: SoT v1.4 §96–134 + plan 01 L50–95 조회·목록). 전체 182개(Mongo 미연결 23 skip / 연결 시 전통과).
- SourceRef persistence를 추가해 Slice 1을 마무리하고 재검증 R3(§113 `source_refs` 보존 gap)을 폐쇄했다. `SourceRef`에 `id`/`project_id`를 더하고 `create_source_ref`가 `source_refs` collection(in-memory + Mongo)에 persist하며, `get_source_ref`는 project_id 격리를 강제한다. archive 후 source_ref 보존을 in-memory와 Mongo 양 경로(fallback/transaction) 회귀로 잠갔다. source_ref ↔ owning candidate 연결은 Phase 2 범위로 남기고, archive 후 신규 ref 생성 차단은 §113이 침묵하므로 추가하지 않았다(spec-faithful). SoT 계약 변경 없음(v1.4 §113 구현 충족). 전체 175개(Mongo 미연결 21 skip / 연결 시 통합 21개 포함 전통과).
- Application + MongoDB Docker 런타임을 추가했다. `services/application/Dockerfile`은 빌드 캐시 보존을 위해 `requirements.txt`를 먼저 설치하고 소스를 뒤에 복사한다(Active Decision 준수). `docker-compose.yml`은 Slice 1 runtime으로 MongoDB 단일 노드 replica set(transaction 기본 계약을 위해 필수)과 application을 정의하며, mongo healthcheck가 `rs.initiate`를 idempotent하게 수행하고 member host를 `mongo:27017`로 두어 compose 네트워크 내 replica-set discovery가 성공한다. `.dockerignore`로 build context를 최소화했다. `docker compose up` 후 transaction 경로로 API save/replay end-to-end를 검증했다(version 1, idempotent replay, `draft_versions`=1). application requirements에 `uvicorn[standard]`을 추가했다. gateway 서비스는 이후 같은 날 외부 llama.cpp client container로 별도 편입했다.
- Core SOT를 실제 MongoDB 저장소에 연결했다. `services/application/app/core_sot/`에 method 기반 `CoreSotRepository` Protocol(`repository.py`)과 pymongo(sync) 기반 `MongoCoreSotRepository`(`mongo_repository.py`)를 추가했다. 승인된 persistence/retention 계약(v1.3)을 구현으로 충족한다: `draft_versions`의 unique index `(project_id, draft_id, idempotency_key)`로 idempotency 경계를 강제하고, transaction 경로(기본, Docker/replica-set)는 save write set을 원자적으로 commit하며, non-transaction fallback(local/test)은 retry guard·orphan cleanup·ordered write를 갖춘다. `CoreSotService`는 dict 직접접근 대신 Protocol method만 사용하도록 리팩터됐고 기존 in-memory 골격과 14개 단위 테스트는 그대로 유지된다. `tests/test_core_sot_mongo.py`에 skip-aware live 통합 테스트(fallback+transaction 17개)를 추가했고 단일 노드 replica set에서 전부 통과했다.

사용자는 transaction을 실제로 검증해야 한다는 이유로 mongomock 대신 real pymongo + live Mongo 통합 테스트(미가용 시 skip)를 선택했고, 로컬 단일 사용자 MVP 단순성을 위해 sync 드라이버(pymongo)를 선택했다. 이로써 통합 테스트 층은 인프라를 요구하지만, 기본 단위 스위트는 skip-aware로 여전히 인프라 없이 실행된다.

### Changed

- 독립 재검증(`docs/verifications/2026-06-28/mongo_adapter_recheck.md`, 조건부 합격) 대응으로 SoT를 v1.4로 갱신하고 R1~R3를 보강했다. R1(load-bearing): `tests/test_core_sot_mongo.py`의 pymongo/adapter import를 try/except로 감싸 pymongo 미설치 시에도 `unittest discover`가 깨지지 않고 skip되도록 복원했다. R2(사용자 결정 option b): non-transaction fallback을 single-writer 전용으로 SoT §112 / plan §01 / adapter docstring에 명시했다(동시성 안전은 transaction 기본 경로 담당). R3: `source_refs` 보존(§113) literal은 SourceRef persistence slice 추적 포인트로 남겼다.

R2는 사용자 결정이다. fallback 동시성 correctness bug에 대해, fallback이 spec상 "local/test 제한 경로"이고 production이 transaction path(동시성 안전)를 쓰므로, 제한된 경로에 동시성 방어 복잡도를 더하는 대신 single-writer 제약을 계약에 명시해 닫는 방향을 선택했다.

## 2026-06-26

### Changed

- Core SOT 최소 구현 골격을 추가했다. `services/application/app/core_sot/`에 immutable dataclass models, raw UTF-8 SHA-256 hash, deterministic source block splitter, source_ref 생성, in-memory repository/service를 추가했고, `services/application/app/main.py`에 FastAPI shell(health/project/draft/version save)을 추가했다. focused 11개와 전체 148개 회귀가 통과했다. MongoDB adapter/transaction-backed repository/export/editor shell은 후속이다.
- Core SOT minimal skeleton 독립 검증의 조건부 합격 항목 C1~C3를 보강했다. `***` scene marker, `##` heading, `archive_project` 경로 회귀를 추가하고, `content_hash`를 known SHA-256 UTF-8 vector로 독립 고정했다. within-block `source_ref` 제약은 SoT와 Phase 1 계약에 명시했다.
- Slice 1 실행 경계를 승인하고 SoT를 v1.1로 갱신했다. monorepo + 독립 LLM Gateway, FastAPI backend, 느슨하게 분리 가능한 Worker 경계를 확정했다. frontend framework 최종 선택은 보류하며, standalone frontend가 필요할 때 React 또는 Vue를 기본 후보로 검토한다. 초기 개인 로컬 runtime은 외부 queue 제품 없이 단순 in-process/background boundary로 시작한다.
- Core SOT text/reference 계약을 승인하고 SoT를 v1.2로 갱신했다. raw snapshot을 MongoDB SOT로 두고, offset은 raw Unicode code point, `content_hash`는 raw UTF-8 SHA-256으로 정했다. MVP `source_blocks`는 Markdown heading, 단독 `---`/`***` scene marker, 빈 줄 paragraph 기반 deterministic split만 사용한다. adaptive/semantic/length-based chunking은 Phase 3 이후 파생 index 전략 후보로 분리했다.
- Core SOT persistence/retention 계약을 승인하고 SoT를 v1.3으로 갱신했다. MongoDB transaction을 기본으로 하되 non-transaction fallback은 local/test 제한 경로로 두고, MVP는 명시적 version save만 지원한다. draft save는 `idempotency_key` 필수이며 project/draft는 archive, snapshot/version/source_ref는 보존한다.
- `docs/system-contract-sot.md`를 `Approved` v1.0 정본 계약 인덱스로 승격했다. 승인 범위는 문서 우선순위와 이미 확정·검증된 계약의 인덱스 역할이며, 미확정 결정 목록은 계속 추측 구현 금지로 남긴다.
- 향후 사용자 결정으로 정본 계약이 바뀔 수 있으므로 SoT 내부에 계약 버전 관리 규칙과 계약 변경 이력을 추가했다.

사용자는 FastAPI 경험을 이유로 backend framework를 FastAPI로 승인했다. frontend framework는 단일 local service UI 가능성을 열어두기 위해 보류하고, 필요 시 React/Vue 중 선택하는 방향으로 남겼다. Worker/queue는 개인 로컬 시스템 특성상 과도한 외부 queue 제품을 피하고 느슨한 연결과 후속 분리 가능성을 우선했다.

사용자는 SOT는 MongoDB, 검색·인덱스는 vector/index DB라는 경계를 다시 확인했고, 업데이트/upsert와 version 확장성을 고려해 SOT에 version 추적성을 유지하기로 했다. 해시는 로컬이어도 두는 방향을 채택했다. adaptive chunking과 1화 단위 길이 기반 chunking은 MVP SOT block 규칙에 넣지 않고, 후속 파생 index 전략 후보로 남겼다.

사용자는 Docker 기반 사용을 전제로 MongoDB transaction 문제가 일반 runtime에서는 크지 않을 것이라고 판단했지만, fallback은 있으면 좋다고 승인했다. autosave는 AI 생성 결과가 항상 맞지 않는다는 이유로 초기 구현에서 제외하고, 실제 필요성이 확인될 때만 별도 기능으로 검토하기로 했다. 분석 후보의 부분 승인/부분 retry는 Slice 1 저장 계약이 아니라 Phase 2/6 review action idempotency에서 다룬다.

사용자는 SoT가 앞으로 사용자 결정에 의해 업데이트될 수 있음을 명시했고, 그 변경은 문서 내부에서 버전 관리되기를 원했다. 이에 v1.0 승인 이력과 future-change rule을 SoT에 직접 기록했다.

## 2026-06-25

### Added

- AgentLoopRunner provider composition slice를 구현했다. `runner.py`가 provider 호출 전 budget check, iteration 기록, provider retry, usage 기록, post-accounting budget check, `parse_self_report_payload`, `judge_completion` 순서를 연결한다. I2 forward-lock으로 token overrun이 `completed`로 위장되지 않음과 provider retry가 iteration budget을 소비함을 양방향 회귀로 잠갔다. 실제 domain tool handler와 task별 artifact schema 평가는 Slice 1·3 이후로 남겼다. 전체 discovery 137개 통과.
- AgentLoopRunner A3 독립 검증(합격) 후 비차단 2건을 보강했다. I3로 `InvalidBudgetPolicy`에 `decision = blocked`를 추가해 budget/registry 예외→종료 decision uniform 매핑을 완성했고, I1(유일한 spec↔impl 갭)로 `BudgetPolicy`에 `provider_retry_cap`/`tool_retry_cap`(0 이상)을 추가해 계약 §retry "retry cap은 필수 policy 값"을 구현에 실현했다. I2(runner 합성 순서)는 spec이 A3를 순수 원시로 규정해 runner slice forward-lock으로 뒀다. 전체 회귀 117→121, retry cap 검증 변이 증명(`_RETRY_DIMENSIONS=()` FAIL/복원 PASS).
- AgentLoopRunner A3를 구현했다. `judge_completion`(종료채널 self-report `FINALIZE`/`DEFER` + 구조 조건 하이브리드 판정 → `completed`/`awaiting_review`), `resolve_retry`(retry 우선순위: non-retryable 즉시 종료 → cap 소진 → cap 남음+budget 허용 retry → cap 남음+budget 차단 `budget_exhausted`에 원래 error literal trace 보존), `next_step_budget_decision`(budget 5차원 → `budget_exhausted` 매핑)을 fake/인프라 없이 양방향 회귀로 잠갔다. terminal-decision 우선순위(error > blocked/invalid_tool_arguments > budget_exhausted > completion)는 순차 합성으로 뒀다.
- A3의 F1 방어로 `BudgetTracker.record_tokens`가 음수/None/bool/비-int token count를 0으로 보정하지 않고 `InvalidProviderUsage`(decision=`provider_error`)로 거부하도록 했다(명시적 0은 유효).
- AgentLoopRunner A2를 구현했다. `ToolRegistry`가 task profile별 v1 domain tool allowlist, strict JSON argument validation(`required`·type·`additionalProperties`·array `items`만; `enum`/bounds는 후속), context-only argument 차단, canonical tool-call signature를 fake/인프라 없이 양방향 회귀로 잠근다.
- A2 독립 검증의 비차단 권고를 반영해 중첩 object schema와 array `items`를 등록 시점에 재귀 검증하고, runtime schema guard의 `assert` 의존을 명시 검사로 교체했다.
- 서비스 경계와 확정 계약을 한 곳에서 추적하기 위한 `docs/system-contract-sot.md` 초안을 추가하고, `docs/README.md`와 `docs/plans/README.md`의 진입점을 갱신했다.
- SoT 독립 검증 R1을 보강해 `docs/plans/README.md`의 문서-precedence tree를 SoT(`docs/system-contract-sot.md` §문서 우선순위)의 5-level과 통일하고, SoT를 정본 precedence로 defer 했다. 정본 precedence tree를 SoT 한 곳으로 단일화.

사용자는 HANDOFF의 다음 작업을 이어 진행하도록 요청했다. 이에 A2 범위를 실제 domain handler 구현이 아니라 registry/argument/signature 계약 회귀로 좁혀 완료하고, handler 실행·retry·completion 합성은 A3 이후로 남겼다.

독립 검증(2026-06-25)이 `flat-loop-gate.md` §33 "enum, bounds 적용" 명시와 구현이 일치하지 않음을 실증 발견했다. 사용자 결정으로 v1/A2 validator 범위를 `{required, type, additionalProperties, array items}`로 계약에 명시 좁히고 `enum`/bounds는 keyword 사용 tool 등록 시점까지 deferred로 reconcile 했다(§33·본 로그·검증 기록에 반영). 상세 기록은 `docs/verifications/2026-06-25/agent_loop_a2_registry.md`.

사용자는 여러 계획 문서가 나뉘어 있어 계약 및 서비스에 대한 정본 문서를 SoT로 활용하고 싶다고 결정했다. 이에 새 SoT 문서는 세부 Phase 계획을 대체하지 않고, 문서 우선순위·서비스 책임·확정 계약·미확정 결정을 먼저 확인하는 정본 인덱스 역할로 작성했다.

사용자는 A3 범위를 fake provider/tool을 주입받아 루프를 실구동하는 러너 골격이 아니라 A1·A2와 동일한 인프라 없는 순수 decision 합성 원시로 진행하기로 결정했다(결과: `completion.py`·`resolution.py`·budget F1 방어). self-report의 구체 wire 형식은 provider-response parser slice로, retry cap policy 배치(`BudgetPolicy` cap 추가 여부)는 별도 slice로 남겼다.

사용자는 A3 독립 검증이 "retry cap 정책 근원 부재"를 유일한 spec↔impl 갭(I1)으로 지적한 뒤, retry cap 배치를 별도 `RetryPolicy`/`TaskProfile`이 아니라 `BudgetPolicy` 확장(Option A)으로 결정했다. 이유: `allows_tools`도 budget이 아닌데 이미 `BudgetPolicy`에 있어 "run policy" 역할과 일관되고 runner가 policy 객체 1개만 전달하면 된다. tradeoff: `BudgetPolicy`가 소비 budget과 retry 한도를 함께 가져 이름이 약간 불일치하지만, 단일 run-policy 객체의 단순함이 우선했다. `resolve_retry(retries_remaining)` 시그니처는 그대로이고 runner가 `policy.<cap> - used`로 남은 retry를 계산한다.

## 2026-06-24

### Added

- 초기 아이디에이션과 실제 개발 계획의 문서 지위를 분리했다.
- `abstract.md`를 공통 기반과 Phase 1~6 계획으로 재구성했다.
- 구현 Phase와 MVP 가치 묶음이 별도 축이라는 계획 기준을 명시했다.
- 단일 사용자 Product Shell과 프로젝트/원고 CRUD·내보내기 계획을 추가했다.
- 분석 memory taxonomy와 Agentic Search/RAG 기반 변경 후보 흐름을 추가했다.
- monorepo 기반 구현 순서와 독립 LLM Gateway/Gemma Q4 검증 계획을 추가했다.
- 기존 `gemma4_12b`의 선택 이관 계획과 flat Agentic Loop Gate 보강 기준을 추가했다.
- 외부 참조 repo 없이 동작하는 portable LLM payload, provider/fake, stable errors와 fake-transport llama.cpp client를 구현했다.
- 독립 검증 조건 F1/F2를 계약·회귀로 보강하고 direct live Gemma Q4 smoke를 확인했다.
- httpx 기반 실제 JSON transport와 재현 가능한 provider smoke command를 추가했다. Mock contract 6개 회귀와 독립 검증 환경의 actual adapter live smoke까지 통과했다.
- flat loop의 task별 tool allowlist, strict argument validation, read-only v1 domain tool 6종과 Gate 비우회 원칙을 확정했다.
- 누적 token budget 우회를 막기 위해 Gateway usage를 필수화하고, flat loop budget 5차원의 계측·초과·retry 우선순위를 확정했다.
- flat loop task별 completion criteria를 확정했다. `completed`/`awaiting_review`를 하이브리드(구조 조건 AND self-report)로 판정하고, "완결된 산출 vs loop 미해결" 구분으로 Analysis/Context/Writing의 종료 기준을 잠갔다.
- AgentLoopRunner 구현을 시작했다(A1). `services/application/` 패키지에 `LoopDecision` 종료 decision 7종과 `BudgetPolicy`/`BudgetTracker` 5차원 budget을 fake/인프라 없이 양방향 회귀로 잠갔다.

사용자는 기존 문서를 초기 아이디에이션으로 보존하면서 실제 개발 전 검토가 쉽도록 긴 초안을 세분화하기를 선택했다. 이에 원문은 유지하고 `docs/plans/`를 작업용 계획 진입점으로 추가했다.

또한 혼자 사용하는 제품이므로 계정 시스템은 MVP에서 제외하고, 프로젝트 관리와 원고 내보내기를 사용자 제품 표면에 포함하기로 했다. 분석 대상은 고정 5종으로 확정하지 않고 분위기·목표·줄거리 등을 논의한 뒤, 기존 기억과의 대조 및 versioned update까지 고려한다.

LLM 운영은 같은 monorepo에서 계약을 함께 관리하되 Gateway를 독립 프로세스/컨테이너로 분리하는 제안안을 채택 후보로 기록했다. 참조 repo에서 Gemma 12B QAT GGUF Q4_0과 llama.cpp CUDA 구성을 확인했으며, 실제 하드웨어 benchmark 전에는 성능 기준을 확정하지 않는다.

사용자는 기존 `gemma4_12b`의 loop/agentic 구현 재사용과 sub-agent spawn 제외를 요청했다. 검토 결과 inference 구성과 평면형 loop 골격은 선택 이관하되, domain tool 실행은 Application/Worker가 소유하고 반복·인자·시간·token budget Gate를 보강하도록 정리했다.

사용자는 tool registry를 Application/Worker가 소유하고 task별 서버 allowlist로 제한하는 방향을 승인했다. 모델 arguments는 strict JSON Schema로 검증하고 `project_id`는 신뢰된 실행 문맥에서 주입하며, compare/validate tool은 preflight로만 사용해 독립 domain Gate를 우회하지 않도록 했다.

사용자는 budget 안전성을 위해 이전의 optional usage 계약을 의도적으로 역전했다. `usage`와 두 token count는 필수이며 누락은 `provider_invalid_response`로 처리하되, 명시적 0 token은 정상값으로 계속 허용한다. 이 결정은 token usage를 `unknown`으로 전파하는 대안보다 단일 Gateway 경계에서 누락을 차단하는 단순성을 택한 것이다.

사용자는 task별 completion criteria를 하이브리드 판정으로 확정했다. `completed`는 구조 조건(목표 산출물 존재)과 자율 조건(모델이 미해결 분기를 self-report하지 않음)을 모두 충족해야 한다. `analysis_compare`의 부분 모호는 loop decision과 candidate status의 직교성을 활용해 run `completed`로 두고 개별 모호 후보는 candidate status로 표현하며, tool 없는 `writing_generate`는 모델이 산출물 자체의 모호·충돌을 self-report할 때만 `awaiting_review`로 종료한다. 이 방향은 개별 항목의 불확실성을 loop 미완료로 승격하지 않고 완결된 산출로 표현하는 직교 모델을 택한 것이다.

여러 개발 머신에서 참조 repo가 없을 수 있으므로 외부 경로는 runtime dependency로 사용하지 않는다. 첫 구현 slice로 llama.cpp thinking payload 경계를 현재 repo에 자립적으로 이관했으며, 작업용 머신의 real-model smoke는 보류한다.
